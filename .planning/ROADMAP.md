# Roadmap

## Overview

`v1.5.0 — Self-description corrections` is shipped — the latest in the lineage
from `v1.0.0` through `v1.1.0`, `v1.2.0` (cross-model review seam,
durable-decision recall, DeepSeek provider), the `v1.2.1` sweep-highs patch,
the `v1.3.0` liteSpeed flow-and-latency pass, the `v1.3.1` tech-debt cycle that
closed all 13 bugs from the post-v1.2.0 sweep, the four stated grammars of
`v1.4.0`, and the `v1.4.1` pair of self-contradicting contracts. Git history
and each release tag are that cycle's archive. `v1.4.1` and `v1.5.0` shipped as
issue-driven `/cad-task` work with no cycle open, which is why they appear in
`## Shipped` with no phase.

No cycle is open. The next one starts at `/cad-phase add`.

## Phases

No cycle is open. The shipped milestone's entries were pruned at its close;
the `v1.4.0` tag and git history are their archive. `/cad-phase add` opens the
next cycle's first entry.
