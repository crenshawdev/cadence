# Roadmap

## Overview

**No cycle open.** `v3.5.8 - the transition that claims to be one` closed on
2026-08-22: two phases, both verified, `/cad-audit` PASS on both arms. Its
narrative is in `CHANGELOG.md`, its shipped rows in `REQUIREMENTS.md`'s
`## Shipped`, and its per-phase residue in `.planning/ARCHIVE.md`. The merge and
the `v3.5.8` tag are `/cad-land`'s, still outstanding.

`/cad-phase add` appends the next cycle's first phase entry. `/cad-plan` seeds
each requirement's Traceability row as its phase is planned.

## Phases


## Phase Details
