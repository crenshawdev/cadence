# Roadmap

## Overview

Cadence `v1.1.0-rc.1` is tagged (release candidate, unpublished): zero-dep BM25
recall over `.planning/` wired into cad-context/cad-planner/cad-debug, plus a
measured, CI-enforced context-weight seam. Its three phases are complete and
pruned from the live list — git history and the `v1.1.0-rc.1` tag are their
archive.

Versioning: `v1.0.0` is the public baseline. The work toward the next public
release ships as release candidates (`v1.1.0-rc.1`, `-rc.2`, …), one per
iteration; the final `v1.1.0` tag is cut only at publish.

The next iteration's phases are not planned yet. Run `/cad-plan` to write them.

## Phases

_No active phases — the next iteration has not been planned._
