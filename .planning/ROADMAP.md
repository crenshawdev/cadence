# Roadmap

## Overview

`v1.4.0 — Stated grammars` is shipped — the latest in the lineage from
`v1.0.0` through `v1.1.0`, `v1.2.0` (cross-model review seam, durable-decision
recall, DeepSeek provider), the `v1.2.1` sweep-highs patch, the `v1.3.0`
liteSpeed flow-and-latency pass, and the `v1.3.1` tech-debt cycle that closed
all 13 bugs from the post-v1.2.0 sweep. Git history and each release tag are
that cycle's archive.

No cycle is open. The next one starts at `/cad-phase add`.

## Phases

No cycle is open. The shipped milestone's entries were pruned at its close;
the `v1.4.0` tag and git history are their archive. `/cad-phase add` opens the
next cycle's first entry.
