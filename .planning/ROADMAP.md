# Roadmap

## Overview

No active cycle. `v2.6.1 — the defects the sweep found` shipped on 2026-08-09
with all four of its requirements delivered and verified, and its phase was
pruned from the lists below at the close. `/cad-phase add` opens the first
phase of the next cycle, once one is scoped — it is not, deliberately, and
`.planning/PROJECT.md`'s `### Active` says why.

`v2.6.1` and everything before it are pruned from this file by design. The
shipped requirement rows live in `.planning/REQUIREMENTS.md` under `## Shipped`,
the phase records in `.planning/_archive-v2.6.1/` and `_archive-v2.6.0/`, and
the narrative in `CHANGELOG.md`. Git history is the archive.

## Phases


## Phase Details


