# Capture

## Todos

- [ ] (backlog) Bump the plugin manifest version. `.claude-plugin/plugin.json` `"version"` is still `1.0.0`: the whole v1.1.0-rc.1 cycle (recall, context weight, updated skills) shipped and merged to `main` (PR #7, merge `91d068f`) without touching it, so `/plugin` reports "already at latest 1.0.0" even though the new content is live. Set it to `1.1.0-rc.1` (or plain `1.1.0` if the Claude Code plugin comparator mishandles pre-release tags), land on `main`, then `/plugin update`. `marketplace.json` carries no version (`source: "./"`), so `plugin.json` is the only edit. Consider the `claude-mem:version-bump` skill. Root cause to avoid next time: fold a manifest-version bump into `/cad-milestone` or `/cad-land` when the project is a distributed plugin.
- [ ] (backlog) `self-verify.mjs` tools-lint `tools:` parser handles only the inline comma-separated frontmatter form; a YAML block-sequence form (`tools:\n  - Read\n  - Bash`) or a CRLF-saved agent file would mis-parse and could false-positive as `undeclared-tool`. Latent only - all 7 current `agents/*.md` are inline + LF. Surfaced by the advisory diff review (finding 1, medium/low) during v1.1.0 Phase 3. Harden the parser if a future agent uses block-sequence declarations.

- [ ] (phase 1) `decideBranch` returns `{action:'create', branch:null}` in `milestone`+`auto`/`ask` on a protected base when no version is derivable (absent from `PROJECT.md ### Active` and ROADMAP title); rail 1's `create -> git checkout -b <branch>` has no null guard, so a version-less project on `main` would attempt an unnamed checkout. Surfaced by the advisory diff review (high). Fix: downgrade create/ask -> naming-problem signal (or ask) when `integrationName` is null, and/or guard null in rail 1. Not triggered in this repo (version resolves to `cadence/v1.1.0-rc.2`).

- [ ] (phase 2) (blocker, ROUTE TO /cad-plan) GitHub `auto_close` cannot open a PR unattended: `gh pr create --head <branch>` will NOT push a local-only integration branch non-interactively, so a push is needed first - but git-guard.mjs asks on EVERY `git push` (rail 3; `isPush = subs.includes('push')`, no protected-branch check), which halts the unattended chain at the prompt. So the naive "add `git push -u` before `gh pr create`" fix is wrong - it reintroduces the very prompt the design routed around. This is a design decision, not a one-liner: reconcile `auto_close` with the push guard (e.g. git-guard skips the ask when `git.auto_close` is true, since the user opted into unattended publish; or exempt a non-protected feature-branch push) - a safety-rail change that must go through the plan + adversarial-review gate. Also correct the false "which pushes it" prose in cad-land SKILL.md step 4b as part of the fix. GitLab's `glab mr create --fill` pushes the source branch itself, so only GitHub is affected. Routed here by /cad-verify 2 (item 9).
- [x] (phase 2) (medium) RESOLVED in 6d415fd (/cad-verify 2 fix): `decideCleanup` now gates `reap` on `branch != null` (was `mergedIntoBase` alone, which emitted `{reap:true, branch:null}` on the GitHub delete-at-merge path). Added a close-decision unit test for the null-branch case and split the seam test into a real-git happy path + the deleted-branch case.
- [x] (phase 2) (design-note) ADJUDICATED by /cad-verify 2: the code is correct - it delivers ROADMAP criterion 1 ("after a successful land/merge ... reaps the merged integration branch") exactly, and never reaps an unmerged branch (no data loss). The goal's "clean base by default" holds for the merge paths; Open-PR/Direct-push/Leave-local intentionally leave a live unmerged branch. Accepted as-designed; no change.
- [ ] (phase 2) (low) `resolveReapBranch`'s sole-`cadence/*` fallback can reap a different (but merged, so no data loss) branch after the shipped branch is deleted and PROJECT.md is evolved. Documented tradeoff in the code comment; note only.

- [ ] (phase 3) (release-prep) Public-facing documentation drift sweep before the tag. Walk every public doc - README.md, MANIFESTO.md, CHANGELOG.md, DESIGN.md, plus LINEAGE.md and NOTICE.md - and reconcile each against the live v1.1 codebase: file paths, command/skill names, config keys, counts, feature descriptions. Correct any drift in place. `/cad-docs-verify` reports drift but does not rewrite, so this is verify-then-fix. Most drift risk is in feature-level prose, since this cycle added the integration-branch model, land cleanup, `auto_close`, the push seam, and builtin recall. CHANGELOG needs a v1.1 entry as part of this. GSD POSITIONING (with John 2026-07-17): keep GSD OUT of plugin keywords/topics (identity-first keywords only); GSD stays credited only in LINEAGE.md/README/LICENSE prose. Reconsider the word "rewrite" - John leans away from it; likely reframe as lineage/influence + distillation (took the discuss->plan->execute->verify methodology GSD introduced him to, built a ground-up independent implementation, shed ~97% of the mass; credit GSD for the spark and for being studied). John's own first-impression phrase for GSD, which he may want to use in the framing: "a mouse designed by a government agency" (absurdly over-engineered / bloated / committee-built) - sets up Cadence as the lean distillation (~3% mass). John will share more of his GSD impressions before we draft; hold the LINEAGE/README wording until then. Confirm NOTICE.md/LICENSE actually satisfy GSD's license attribution terms for any lifted code, and propagate the chosen wording across LINEAGE.md/README/DESIGN.
- [ ] (phase 3) (release-prep) README feature-coverage gap: some shipped features are not surfaced in README.md that should be. Named so far: "the parser" - an idea John recalls adapting from RTK ("Rust Token Killer", his token-optimizing CLI proxy). OPEN QUESTIONS for John before writing: (1) which parser exactly (git-guard's `gitSubcommands` command parser? the recall/BM25 corpus parser? something else?); (2) what specifically the RTK-borrowed idea is; (3) whether RTK stays at all - John is reconsidering dropping it, so the attribution may need to stand on its own merits rather than crediting a sunset tool. LINEAGE.md currently attributes only GSD, so decide whether any RTK credit belongs there and/or in README. NOTE: list is still open - John is adding more items as they surface.
- [ ] (phase 3) (release-prep) Manifest version bump 1.0.0 -> 1.1.0 - see the (backlog) plugin.json item above; fold it into this pre-tag batch.
- [ ] (phase 3) (release-prep, HIGH BAR - hand-reviewed by Anthropic engineers) Official Anthropic/Claude plugin store submission. GOAL: get Cadence accepted into Anthropic's official/curated plugin store (distinct from a self-hosted git marketplace). Sub-tasks: (a) research the real submission/application process, where to apply, and Anthropic's published review criteria/requirements/guidelines (security, manifest metadata, naming, quality, licensing); (b) audit Cadence's docs + system against those criteria and close every gap so the submission is first-class; (c) prepare whatever application materials they require. Idea originated in a claude.ai web chat. RESEARCH DONE 2026-07-17 (full brief: design-notes/store-submission-2026-07-17.md). KEY CORRECTION to our assumption: the OFFICIAL marketplace (anthropics/claude-plugins-official) has NO application and is invite-only at Anthropic's discretion; the actionable path is the COMMUNITY marketplace (anthropics/claude-plugins-community) via the form at clau.de/plugin-directory-submission, gated by AUTOMATED screening (not hand-review by engineers). Quality bar still worth maxing - a clean community submission is also how you get noticed for the discretionary official curation. Actionable checklist: kebab-case name; `claude plugin validate --strict` clean; complete plugin.json metadata (displayName/author/homepage/repository/license/keywords); README + CHANGELOG; semver matching the release tag.
- [ ] (phase 3) (release-prep) Document reverted design decisions in DESIGN.md - honestly, with rationale, because a hand-reviewing engineer should see the design evolution told straight. Known instance: the founding "workflows never auto-push; publishing is always the user's call at a no-preselected-default prompt" principle was partially reversed - John decided (2026-07-16/17) to add opt-in `auto_close` that CAN publish unattended, which required one sanctioned code-guarded push seam. Document (1) what the original decision was, (2) that it changed and when, (3) WHY it changed - get John's product rationale, not just the functional trigger (item-9 UAT revealed the GitHub arm can't open a PR without a push). ALSO sweep git history + DESIGN.md + phase CONTEXT/CAPTURE for any OTHER made-then-reversed decision and document each. Inventory pass dispatched 2026-07-17.

## Seeds

- **Integration-branch git strategy (config + workflow).** Adopt a two-tier branch model as Cadence's one opinionated flow. Tier 1 = an integration branch per milestone (the cycle's reconciliation point; lands to `main` as one PR + tag). Tier 2 = the ephemeral per-plan worktree branches that fork from the integration tip and are reaped after merge. Best fit for the parallel/worktree path: reconciliation churn stays off `main`, and the file-overlap gate keeps those merges clean by construction (with a stop-and-ask safety net).
  - **Grounded findings** (pressure-test 2026-07-16 of `workflows/execute.md` `execute_parallel`, lines ~147-156): worktree branches `cadence/phase-<N>-plan-<k>` already fork from HEAD, merge back into HEAD sequentially (conflict -> stop & ask), and are deleted in step 4. So integration mode is *mostly already supported when HEAD is a milestone branch*, and the worktree tier already cleans itself up.
  - **What that means for the design:** (a) a `worktree_base` knob is unnecessary - worktrees already fork from the current branch, which is the integration branch when you are on one; (b) the load-bearing gap is branch *ownership* - nothing guarantees you are on an integration branch before parallel execution, so on `main` all reconciliation lands on `main` (the "branch pre-existed by luck" problem from the v1.1 cycle); (c) the other real gap is *integration-branch* cleanup after the final PR merge (return-to-base + reap the merged branch), done by hand this cycle - separate from worktree cleanup, which is already handled.
  - **Proposed config (small, philosophy-preserving):** `git.integration_branch: "milestone" | "trunk"` (default milestone); `git.auto_branch: "ask" | "auto" | "off"` (create/switch to the integration branch at cycle start - THE load-bearing knob); `git.on_land_cleanup: "ask" | "auto" | "off"` (post-merge return-to-base + reap merged integration branch). `git.base_branch` stays the landing/guard base (`main`), distinct from the integration branch. Do NOT add a phase-level branch unit (redundant middle tier) and do NOT touch cad-land's no-preselected-default publish rule - this governs how work is organized, never how it is published.
  - **Where it lands:** `references/git.md` (a fifth rail, or an extension of rail 1), `config.schema.json`, `cad-config`, and the cycle-start workflows (`cad-milestone` setting up the next cycle, `cad-plan`). Self-verify CONTRACTS/config-key tests come along for any new keys.

## Notes
