{"findings":[{"file":"cadence-core/bin/lib/phase-plans.mjs","line":75,"severity":"blocker","claim":"Plan-file reads are unbounded and follow symlinks, allowing a PLAN filename discovered under the planning directory to block or exhaust the resolver before frontmatter parsing.","failure_scenario":"A cloned planning tree contains `phases/3/PLAN-1.md` as a symlink to `/dev/zero` (or a multi-gigabyte file). It passes the name-only `PLAN_FILE` filter, and `readFileSync(file, 'utf8')` attempts to consume the target with no size limit or regular-file check. Every resolve for phase 3 can then hang indefinitely or exhaust memory instead of returning the fail-closed configured stakes."}]}
