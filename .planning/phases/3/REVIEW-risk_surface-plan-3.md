{"findings":[{"file":"cadence-core/bin/lib/phase-plans.mjs","line":218,"severity":"high","claim":"`phaseDirsIn` follows symlinks for both `_archive-*` groups and their phase entries, so the new replay locator is not confined to the planning root.","failure_scenario":"An untrusted project can create `.planning/_archive-x` as a symlink to an arbitrary readable directory, or `.planning/phases/1` as such a symlink. `entriesIn(dir)` and then `planFilesIn(join(dir, name))` follow it; replay subsequently calls `declaredFilesIn` on the external directory and scans the files its PLAN declares. Thus `route.mjs replay --file <project>/.planning/config.json` can be induced to read and process PLAN data outside the selected project rather than only `phases/` and archive directories beneath its planning root. Reject symlinks/non-directories with `lstat` and enforce that resolved paths remain below `planningRoot`."}]}
