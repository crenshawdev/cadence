{
  "status": "gaps",
  "score": "17/20",
  "truths": [
    {
      "n": 1,
      "truth": "A gate that will not fix a finding asks in that same step and never writes to CAPTURE.md (criterion 1)",
      "status": "VERIFIED",
      "uat_item": 8,
      "evidence": "cadence-core/references/triage-gate.md:244 '## What happens to a finding this fire will not fix', pointed at by the blocking arm (:43) and the adjudicated arm (:235); the section names exactly one `unfixed` call (:256) and one `file` call (:284) and forbids the per-finding prompt (:267-271, the only 'per finding' hit in the file). cadence-core/bin/issue-filing.mjs writes only .planning/FILED.md (:311) - no CAPTURE.md path anywhere in it. `node --test cadence-core/bin/issue-filing.test.mjs` passes '`unfixed` writes nothing at all - the ask is not a filing'. End-to-end run in a temp tree with a stub `gh`: `issue-filing.mjs file` returned ok:true and .planning/ held only config.json and FILED.md - no CAPTURE.md was created. triage-gate.md is reached from workflows/{execute,verify,plan,debug,suggest,report}.md and references/{git-guard,execute-parallel}.md."
    },
    {
      "n": 2,
      "truth": "The unfixed set is read off the structured RULINGS payload, never re-parsed out of REVIEW prose (criterion 2)",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/bin/lib/filing-decision.mjs:74-81 `unfixedFindings` filters `buildEntries(payload)` on (ruling, severity) only. `node --test cadence-core/bin/filing-decision.test.mjs` passes 'a survived blocker stays behind; a downgraded high and a survived low are the set' and 'the payload and a prose fixture disagree, and the answer is the payload'."
    },
    {
      "n": 3,
      "truth": "The fingerprint is (file, claim) and ignores line (criterion 11)",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "cadence-core/bin/lib/filing-decision.mjs:133-140 digests `file` NUL `claim` only. Named passes: 'two findings differing only in LINE share one fingerprint', 'two findings differing in CLAIM do not', 'severity is outside the fingerprint', 'the NUL join cannot be forged by moving the boundary'; and issue-filing.test.mjs 'a decline recognized by (file, claim) survives the file shifting by a line'."
    },
    {
      "n": 4,
      "truth": "One pinned create and lookup argv per forge, with the decline label applied at CREATE time (criterion 7, criterion 11)",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "FILING_TABLE at cadence-core/bin/lib/filing-decision.mjs:375-419 - forgejo `--labels`, github `--label`, gitlab `--label` with `-y` on create; DECLINE_LABEL = 'cadence-declined' (:158). filing-decision.test.mjs pins all three vector pairs exactly ('the forgejo create and lookup vectors are exactly these', github, and 'the gitlab create vector carries -y'). issue-decision.mjs's read-only contract is untouched - the new table is a separate module."
    },
    {
      "n": 5,
      "truth": "One decline lookup call per fire, whatever the finding count (criterion 11)",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "cadence-core/bin/issue-filing.mjs:269 calls readDeclines once, outside the finding loop. issue-filing.test.mjs passes 'a five-finding fire makes exactly ONE list call and no create call', 'the one list call is label-filtered and carries the page size', 'a finding the lookup already carries is absent from the answer'."
    },
    {
      "n": 6,
      "truth": "An incomplete decline lookup refuses the fire and creates nothing (criterion 12)",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "normalizeDeclines (filing-decision.mjs:303-305) returns incomplete when `parsed.length >= limit`; issue-filing.mjs:225-231 turns that into reason 'incomplete-lookup'. Named passes: 'a response of exactly the page size is INCOMPLETE and carries no records', 'a lookup that filled its page refuses, and no create call is made', 'a list call that exits nonzero refuses rather than reading no declines'."
    },
    {
      "n": 7,
      "truth": "A write that does not land refuses, names what was not filed, and never drops or parks the finding (criterion 9)",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "issue-filing.mjs:424-487 emits reason 'create-failed' with `filed`/`unfiled` arrays, a mirror result and an honest ambiguous-create retry hint; resolveForge returns 'no-forge'/'no-cli'/'no-login' before any spawn (:132-171). Named passes: 'a create exiting nonzero refuses and NAMES the findings that were not filed', 'a create that fails part way still mirrors the issues that DID land', 'a create failure whose mirror ALSO failed says so', 'the create-failed hint is honest that the failed create may have LANDED', 'an unconfigured forge refuses by naming the unset keys, spawning nothing'."
    },
    {
      "n": 8,
      "truth": "A filed finding stays reachable by recall - the CAP-01 guarantee survives routing to the tracker (criterion 13)",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "One value traced across every seam: `issue-filing.mjs file` in a temp tree with a stub `gh` returned filed fingerprint 56d89f78cae9c56e; .planning/FILED.md then held `- 2026-08-25 github acme/widget 56d89f78cae9c56e: [cadence 56d89f78cae9c56e] the lock is never released` and the declined entry was NOT written; `planning.mjs recall \"lock is never released\" --dir <tmp>/.planning` returned {\"score\":0.863,\"source\":\"FILED.md\",...}. Wiring: appendFiledRow/parseFiledRows at lib/planning-files.mjs:1279-1320, consumed at planning/recall.mjs:165-166. Named passes: 'three accepts and two declines leave exactly THREE bullets in FILED.md', 'a fire of declines ALONE writes no FILED.md at all', 'recall: a tree with no FILED.md answers byte-identically to one that never had it'."
    },
    {
      "n": 9,
      "truth": "The tree stops claiming glab is absent and names no host, org or username as an implementation literal (criterion 6)",
      "status": "VERIFIED",
      "uat_item": 9,
      "evidence": "grep 'glab is absent' over cadence-core/ returns nothing; lib/issue-decision.mjs:39-41 and :250-251 now record '`glab` IS installed here - /usr/bin/glab, version 1.114.0' with the 2026-08-25 date. `grep -rn jcrenshaw cadence-core/` outside tests/fixtures returns six hits, all COMMENT lines and all predating the phase - self-verify.mjs:516 (1ffa48f6, 2026-07-29), lib/redact-url.mjs:66 (81bdb5d4, 2026-08-14), lib/forge-decision.mjs:139-140 (8230221c) and :227-228 (fd55f19d), both confirmed ancestors of the phase base b157ccdc via `git merge-base --is-ancestor`. No host is a literal in code."
    },
    {
      "n": 10,
      "truth": "The refusal vocabulary is pinned forward and the whole suite is green (criterion 14)",
      "status": "VERIFIED",
      "uat_item": 10,
      "evidence": "cadence-core/bin/reason-census.test.mjs passes all five arms including 'every committed refusal token is still produced by the live tree' and 'the census is ONE-DIRECTIONAL'; its header states the derivation method and re-derives 266 sites / 104 distinct / 82 literal on this tree. `node cadence-core/bin/test.mjs` -> tests 3199, pass 3199, fail 0. `npx tsc -p tsconfig.ci.json` exit 0. `node cadence-core/bin/self-verify.mjs` -> problems []. Known limit, disclosed in SUMMARY and confirmed: the five new tokens (no-forge, no-cli, no-login, lookup-failed, incomplete-lookup) are helper returns emitted through an interpolated `reason`, so they land in the expression class the census does not watch; their hints are covered by self-verify check 22."
    },
    {
      "n": 11,
      "truth": "The write targets the resolved tracker, proved LIVE against Forgejo, GitHub and GitLab (criterion 6)",
      "status": "UNCERTAIN",
      "uat_item": 11,
      "evidence": "Plan 1 task 8 is DEFERRED - no commit in the range b157ccdc..0169ef62, and the procedure sits uncommitted at .planning/phases/3/live-forge-check.md with empty result fields. Every argv in FILING_TABLE is proved against PATH-injected stubs only (my own end-to-end run used a stub `gh` that exits 0 and reads nothing). Confirmed independently: `.planning/config.json` carries only `git.auto_close`, and `issue-filing.mjs unfixed --payload <valid adjudication payload> --dir /code/cadence` returns {\"ok\":false,\"reason\":\"no-forge\",...\"git.forge_provider, git.forge_repo are unset\"}, so this repository cannot exercise the path it just built."
    },
    {
      "n": 12,
      "truth": "The walked-queue reading counts substantive bullets and excludes the `- None.` placeholder (criterion 3)",
      "status": "VERIFIED",
      "uat_item": 12,
      "evidence": "lib/capture-health.mjs:PLACEHOLDER='None.' skipped in the count loop; capture-health.test.mjs imports the real EMPTY_CAPTURE from lib/capture-file.mjs (:14) and passes 'a freshly created queue counts ZERO substantive bullets', 'the placeholder does not hide a real bullet beside it', 'a checked bullet counts, and a continuation line and a `* ` line do not', 'a fenced heading does not mint a section, and a CRLF body counts the same'."
    },
    {
      "n": 13,
      "truth": "An item is resolved by removal: an annotated walked bullet is reported, an unannotated one is not (criterion 4)",
      "status": "VERIFIED",
      "uat_item": 13,
      "evidence": "ANNOTATIONS at lib/capture-health.mjs (`/\\bKEPT \\d{4}-\\d{2}-\\d{2}/` case-sensitive, `/recorded not fixed/i`); the prose rule has one home at references/triage-gate.md:294-299, with the grammar half at references/capture-grammar.md:51-59. Proved by FIXTURE not against the live file: capture-health.test.mjs 'an annotated bullet is named with its section, line and text' and 'an unannotated queue reports no annotation sites at all'; planning-capture-check.test.mjs 'the annotated bullets and the archived section are all named'. The live tree carries zero annotations (capture-check on this repo: annotations []), which is exactly why the fixture is load-bearing."
    },
    {
      "n": 14,
      "truth": "capture-check is the one command the health walk and the phase close both read (criterion 5)",
      "status": "VERIFIED",
      "uat_item": 14,
      "evidence": "planning/capture-check.mjs registered at planning.mjs:312 and declared at lib/arg-contract.mjs:921; skills/cad-health/SKILL.md:43 invokes it; planning/phase-done.mjs:14 imports the same `captureHealth` core. planning-capture-check.test.mjs passes all eight arms including the absent-file, unreadable-file, valueless --file and torn-config-layer cases."
    },
    {
      "n": 15,
      "truth": "The configured bound fails loud and refuses nothing (criterion 8)",
      "status": "VERIFIED",
      "uat_item": 15,
      "evidence": "planning.max_capture_bullets int/min 1/default 40 in cadence-core/config.schema.json:60, with the reach row at references/config-reach.md:150 and the catalog row at references/config-catalog.md:56; self-verify checks config-keys, config-reach and budgets and reports problems []. Named passes: 'capture-check: a queue over its bound is REPORTED, with both numbers, and refuses nothing' and 'capture-check: the same tree under a bound of 4 reports no crossing'. Live: `planning.mjs capture-check` returns ok:true with over_bound:false."
    },
    {
      "n": 16,
      "truth": "Phase close ASSERTS the queue is empty rather than performing a roll-out (criterion 3)",
      "status": "VERIFIED",
      "uat_item": 16,
      "evidence": "planning/phase-done.mjs:128 reads the queue before runTransition and :200 rides a `capture` field on the CLOSE arm only; readQueue (:215-227) treats absence as empty and an unreadable file as `unread`, never a refusal. planning-phase-done.test.mjs passes 'a non-empty queue is NAMED at close, and the phase still closes', 'a queue holding only the placeholders names nothing', 'an absent CAPTURE.md is an EMPTY queue, and closes cleanly', 'a queue that cannot be READ closes anyway, reported unread', '--undo: reopening a phase makes NO assertion about the queue'. workflows/verify.md:315-325 is the reader that prints each items[] entry."
    },
    {
      "n": 17,
      "truth": "/cad-health prints the capture verdict these checks return (criterion 5)",
      "status": "VERIFIED",
      "uat_item": 17,
      "evidence": "skills/cad-health/SKILL.md:40-70 invokes capture-sections AND capture-check and names all three things to print - substantive against bound with the crossing, every annotations[] entry with section/line/text, and archive.heading with archive.bullets - and forbids filtering against an expected list. self-verify's `invocations` check passes (problems []), so the command line in the skill resolves. Running it against this repository yields {\"substantive\":37,\"bound\":40,\"over_bound\":false,\"annotations\":[],\"archive\":{...\"present\":false}} - the count and the bound with no annotation and no archive line, which is item 17's expected shape. The rendering itself is a model step over that envelope."
    },
    {
      "n": 18,
      "truth": "`## Archive` has left the CAPTURE contract and .planning/ARCHIVE.md is untouched (criterion 7)",
      "status": "VERIFIED",
      "uat_item": 18,
      "evidence": "`grep -n Archive cadence-core/references/capture-grammar.md` returns exactly one line, :44 '`## Archive` is NOT part of this file (CAP-03)' - no sections-table row and no milestone-close instruction to move bullets into it. Across cadence-core/ and skills/ the only other '## Archive' mention is skills/cad-health/SKILL.md:70, describing the incident. capture-health.mjs reports the heading rather than walking or ignoring it; capture-health.test.mjs 'a `## Archive` heading is reported with its bullet count'. The separate .planning/ARCHIVE.md surface is unchanged - planning/milestone-prune.mjs:160 still writes it and planning/recall.mjs:111 still reads it."
    },
    {
      "n": 19,
      "truth": "CAPTURE cannot accumulate, because nothing durable is ever routed into it (goal, sentence 3)",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "cadence-core/workflows/execute.md:446-457 still instructs the phase summary step to 'File each open item into `.planning/CAPTURE.md` through the seam, one call per item', unchanged by this phase and in neither plan's lease. workflows/verify.md:315-325 then prints phase-done's assertion that the same queue is empty, so the workflow that closes a phase fills the queue the close asserts empty. Observed on this tree: `planning.mjs capture-check` reports substantive 37 (Todos 28, Seeds 9) against bound 40, where SUMMARY.md's own goal check records 30 measured the same day - the queue grew by seven while the phase that declared it transient was being written, and nothing in the phase drains it."
    },
    {
      "n": 20,
      "truth": "A declined finding leaves no artifact behind (criterion 1) - and the shipped behavior files it with a label (criterion 11)",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "ROADMAP criterion 1 requires 'declined findings are DROPPED' and 'no artifact anywhere holds a declined one'; ROADMAP criterion 11 requires 'a declined finding is filed and immediately marked' so a later fire with the same fingerprint does not re-ask. The code implements 11: issue-filing.mjs:421-423 creates an issue for every entry and sets `declined: disposition === 'decline'`, and issue-filing.test.mjs 'three accepts and two declines make five creates, and exactly two carry the label' pins it. My end-to-end run confirmed a declined finding becomes a labeled issue on the tracker (it is only kept out of FILED.md). The two criteria contradict each other and criterion 1's text is the one no longer true of the tree."
    }
  ],
  "passes": [
    { "k": 1, "name": "Unfixed set comes from the structured payload", "evidence": "lib/filing-decision.mjs:74-81 filters on (ruling, severity) off buildEntries; filing-decision.test.mjs passes 'a survived blocker stays behind; a downgraded high and a survived low are the set' and 'the payload and a prose fixture disagree, and the answer is the payload'." },
    { "k": 2, "name": "Fingerprint is (file, claim) and ignores line", "evidence": "lib/filing-decision.mjs:133-140 digests file NUL claim only; passes 'two findings differing only in LINE share one fingerprint', 'two findings differing in CLAIM do not', 'the NUL join cannot be forged by moving the boundary'." },
    { "k": 3, "name": "One pinned create and lookup argv per forge", "evidence": "FILING_TABLE at lib/filing-decision.mjs:375-419; all three vector pairs pinned exactly by filing-decision.test.mjs, gitlab create carrying -y; DECLINE_LABEL frozen at :158." },
    { "k": 4, "name": "One lookup call per fire, whatever the finding count", "evidence": "issue-filing.mjs:269 calls readDeclines once outside the loop; issue-filing.test.mjs 'a five-finding fire makes exactly ONE list call and no create call' and 'a finding the lookup already carries is absent from the answer'." },
    { "k": 5, "name": "An incomplete decline lookup refuses the fire", "evidence": "normalizeDeclines refuses at `parsed.length >= limit` (filing-decision.mjs:303); issue-filing.mjs:225-231 emits 'incomplete-lookup'; passes 'a response of exactly the page size is INCOMPLETE' and 'a lookup that filled its page refuses, and no create call is made'." },
    { "k": 6, "name": "A write that does not land refuses instead of dropping", "evidence": "issue-filing.mjs:424-487 'create-failed' carries filed/unfiled, the mirror result and an ambiguous-create warning; passes 'a create exiting nonzero refuses and NAMES the findings that were not filed' and 'a create failure whose mirror ALSO failed says so'." },
    { "k": 7, "name": "A filed finding stays reachable by recall", "evidence": "Traced one value end to end: `issue-filing.mjs file` (stub gh) -> fingerprint 56d89f78cae9c56e -> a single FILED.md row (declined entry absent) -> `planning.mjs recall` returned score 0.863 source FILED.md. Wired at lib/planning-files.mjs:1279-1320 and planning/recall.mjs:165-166." },
    { "k": 8, "name": "The gate asks once, in the step that decided", "evidence": "references/triage-gate.md:244, pointed at from :43 (blocking) and :235 (adjudicated); one `unfixed` call at :256, one `file` call at :284, per-finding prompting forbidden at :267-271; issue-filing.mjs has no CAPTURE.md write and a live `file` run created none; self-verify problems []." },
    { "k": 9, "name": "The tree stops claiming glab is absent, and names no host", "evidence": "'glab is absent' greps to nothing; lib/issue-decision.mjs:39-41 records /usr/bin/glab 1.114.0 dated 2026-08-25; the six surviving `jcrenshaw` hits are comments in self-verify.mjs, redact-url.mjs and forge-decision.mjs whose commits are all ancestors of the phase base b157ccdc." },
    { "k": 10, "name": "The refusal vocabulary is pinned forward", "evidence": "reason-census.test.mjs green on the live tree, one-directional by construction with the derivation method stated (266 sites / 104 distinct / 82 literal, re-derived)." },
    { "k": 12, "name": "The walked queue reading counts substantive bullets", "evidence": "capture-health.test.mjs imports the real EMPTY_CAPTURE and passes 'a freshly created queue counts ZERO substantive bullets', 'a checked bullet counts, and a continuation line and a `* ` line do not', and the fenced-heading/CRLF case." },
    { "k": 13, "name": "An annotated bullet is reported, an unannotated one is not", "evidence": "Two annotation shapes in lib/capture-health.mjs; proved by fixture in capture-health.test.mjs and planning-capture-check.test.mjs, not against the live file, which carries annotations []." },
    { "k": 14, "name": "capture-check is the one command both readers use", "evidence": "Registered at planning.mjs:312, declared at lib/arg-contract.mjs:921, invoked at skills/cad-health/SKILL.md:43, and phase-done.mjs:14 imports the same captureHealth core; all eight planning-capture-check.test.mjs arms pass." },
    { "k": 15, "name": "The configured bound fails loud and refuses nothing", "evidence": "config.schema.json:60 (int, min 1, default 40), config-reach.md:150, config-catalog.md:56; self-verify problems []; passes 'a queue over its bound is REPORTED, with both numbers, and refuses nothing' and the under-bound twin." },
    { "k": 16, "name": "Phase close asserts the queue is empty", "evidence": "phase-done.mjs:128 reads before runTransition, :200 rides `capture` on the close arm only; all five planning-phase-done.test.mjs queue arms pass including the --undo case." },
    { "k": 17, "name": "/cad-health prints the capture verdict", "evidence": "skills/cad-health/SKILL.md:40-70 names the three prints and forbids filtering; the live envelope on this repository is {substantive:37, bound:40, over_bound:false, annotations:[], archive.present:false}, the count and bound with no annotation or archive line." },
    { "k": 18, "name": "`## Archive` is out of the CAPTURE contract", "evidence": "One `Archive` line in references/capture-grammar.md (:44), saying the heading is NOT part of the file and is reported - no sections-table row, no milestone-close instruction; .planning/ARCHIVE.md still written by milestone-prune.mjs:160 and read by recall.mjs:111." },
    { "k": 19, "name": "The whole suite is green", "evidence": "`node cadence-core/bin/test.mjs` -> tests 3199, pass 3199, fail 0. `npx tsc -p tsconfig.ci.json` exit 0. `node cadence-core/bin/self-verify.mjs` -> problems [] across all 26 checks. No unreferenced debt marker in any of the 30 files touched by b157ccdc..0169ef62." }
  ],
  "gaps": [
    {
      "name": "The phase-summary step still routes open items into the file this phase declared transient",
      "reason": "behavior wrong - the goal's third clause ('nothing durable is ever routed into it') is contradicted inside the shipped tree, and the queue is observably accumulating",
      "evidence": "cadence-core/workflows/execute.md:446-457 instructs 'File each open item into `.planning/CAPTURE.md` through the seam, one call per item' at the phase SUMMARY step, unchanged by this phase and in neither plan's lease. cadence-core/workflows/verify.md:315-325 then has phase-done assert the same queue is empty, so the workflow that closes a phase fills the queue the close reports on. Measured: `node cadence-core/bin/planning.mjs capture-check` on this repository returns substantive 37 (Todos 28, Seeds 9, Notes 0) against bound 40, where .planning/phases/3/SUMMARY.md's goal check records 30 measured the same day (2026-08-25). Nothing in the phase drains the queue, and CAPTURE.md is untracked here so the growth is not even reviewable in git.",
      "severity": "major",
      "missing": "Either route execute.md's open items to the same `issue-filing.mjs` ask the gates now use (so a phase's leftovers leave at the step that produced them), or state in execute.md and capture-grammar.md exactly which durable writes CAPTURE.md still accepts and reconcile that with the close assertion. Separately, drain the 37 items now sitting in .planning/CAPTURE.md, since the phase's own close will name every one of them."
    },
    {
      "name": "ROADMAP criterion 1 and criterion 11 disagree about what happens to a declined finding, and the code follows 11",
      "reason": "behavior wrong against criterion 1 as written - a declined finding IS filed as a labeled issue rather than dropped",
      "evidence": "ROADMAP.md criterion 1 requires 'declined findings are DROPPED' and asserts 'no artifact anywhere holds a declined one'; criterion 11 requires 'a declined finding is filed and immediately marked' with the label applied at CREATE time. cadence-core/bin/issue-filing.mjs:421-423 creates an issue for every entry with `declined: disposition === 'decline'`, pinned by issue-filing.test.mjs 'three accepts and two declines make five creates, and exactly two carry the label'; references/triage-gate.md:276-277 states the same to the user. An end-to-end run confirmed the declined finding becomes a labeled issue and is only kept out of FILED.md. The CAPTURE-byte-unchanged half of criterion 1 does hold; the 'dropped' half does not.",
      "severity": "minor",
      "missing": "Amend ROADMAP.md criterion 1 so it reads with criterion 11 - a declined finding is filed carrying the decline label and is what stops the re-ask - so a later reader does not take 'no artifact anywhere holds a declined one' as the shipped contract."
    }
  ],
  "human_checks": [
    {
      "name": "Live forge filing against tea, gh and glab (plan 1 task 8, still deferred)",
      "expected": "One gate-driven blocking fire on three operator-owned scratch repos asks from inside the fire; five findings produce ONE ask step; three issues land without the decline label and two with it; a second fire carrying the same fingerprints does not re-ask; and the open ROADMAP question is settled - whether the `cadence-declined` label must pre-exist or each forge creates it on the create call.",
      "why_human": "Out of reach, not merely unexercised: it needs authenticated tea/gh/glab credentials against three real scratch repositories and live network writes, all of which this pass is forbidden to touch. It also cannot be run on this repository at all - .planning/config.json carries only `git.auto_close`, and `issue-filing.mjs unfixed` against a valid adjudication payload returns {\"ok\":false,\"reason\":\"no-forge\",\"detail\":\"...git.forge_provider, git.forge_repo are unset\"}, so every argv in FILING_TABLE is currently proved against PATH-injected stubs only. The procedure and the per-forge argv at this commit are at .planning/phases/3/live-forge-check.md, uncommitted."
    }
  ]
}
