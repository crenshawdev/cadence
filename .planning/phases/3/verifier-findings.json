{
  "status": "gaps",
  "score": "12/14",
  "truths": [
    {
      "n": 1,
      "truth": "A task record exists on disk in the corpus's own grammar and every figure in it is DERIVED from the range rather than retyped onto a flag",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": ".planning/tasks/bound-plan-size/RECORD.md carries '## Commits' with 40-char shas and '- **Files:**' under '### Task 1:'. Writer exercised in an isolated repo at scratchpad/probe1: `planning.mjs task-record --dir <tmp>/.planning --slug probe-slug --base HEAD~1 --head HEAD --text-file text.txt` -> {ok:true, commits:1, files:1, written:true}; the rendered table named exactly the one commit in the range and the Files line exactly the one path the range touched. A subject containing '|' rendered as one escaped cell ('feat: second\\|thing with pipe'). Two identical runs left the file byte-identical (md5 ffc09b3c... both times). `--slug ../escape` refused with {ok:false, reason:'bad-args'} and nothing written."
    },
    {
      "n": 2,
      "truth": "The recall corpus reaches the tasks tier - a query naming what a task did returns that task's RECORD.md",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "`node cadence-core/bin/planning.mjs recall \"plan size ceiling max_plan_tasks task count\"` returns source 'tasks/bound-plan-size/RECORD.md' at rank 4 of 5 (score 10.5144, total 324), with NO `phase` key on the hit as references/recall.md requires. Wiring: planning.mjs:2586 `for (const {slug, path} of taskRecordsIn(dir))` -> :2589 `parseTaskRecordSnippets(record)` -> :2590 `corpus.push({text, source: `tasks/${slug}/RECORD.md`})`, appended after the ARCHIVE.md walk."
    },
    {
      "n": 3,
      "truth": "On a tree with no `.planning/` the writer creates nothing and returns a stated reason on its envelope",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "Isolated repo with no planning root: envelope {ok:true, written:false, reason:'no planning root at <path>, and this command creates neither it nor tasks/', trace:{written:false,reason:'ENOENT'}}. `ls -a` of the repo afterwards showed only a.txt, b.txt, .git, text.txt - no .planning/ and no tasks/."
    },
    {
      "n": 4,
      "truth": "/cad-why on a file a task touched resolves that commit to the task's slug as an off-roadmap task, not to a phase and not to a gap",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "`node cadence-core/bin/why.mjs cadence-core/templates/config.json` renders for commit 093408c9: 'phase: off-roadmap task bound-plan-size - a /cad-task run, not a roadmap phase (tasks/bound-plan-size)', 'plan task: task 1 - feat(config): add workflow.max_plan_tasks...', 'declared by: declared in RECORD.md / task 1: bound-plan-size (declares cadence-core/templates/config.json)'. No NOT RESOLVED block for that commit, no phase number, no milestone label. Wiring traced end to end: why.mjs:344 `buildTaskIndex(joinPath(dir,'.planning'))` -> why-corpus.mjs:846 `mergeCommitIndexes(disk, tasks, recovered)` -> the slug marker consumed by why-render's fieldPhase arm."
    },
    {
      "n": 5,
      "truth": "The tiers are ordered and asked in order - a sha named by both a phase SUMMARY and a task record still resolves to the phase",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "Named tests run and passing: why-corpus.test.mjs 'a sha named by BOTH a phase summary and a task record resolves to the PHASE' and 'this repository's merged index resolves 093408c9 to the bound-plan-size task, and 00537356 still to its phase' (9 pass / 0 fail on the filtered run). Also 'the tasks tier is asked AHEAD of the git-recovered one, and prunes still come off recovered alone' (why-corpus.test.mjs:761)."
    },
    {
      "n": 6,
      "truth": "A /cad-task run's phase-0 bracket pairs under a PER-RUN correlation id: two runs of one slug from one unchanged HEAD produce two paired brackets, nothing unpaired, cad-task in roles with no invented figures",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "Ran workflows/task.md's own anchor/dispatch/close lines twice against an isolated planning root (--dir <scratchpad>/probe1/.planning), same slug, same HEAD. `trace render --phase 0` returned brackets:[2 entries, both role cad-task, plan probe-slug], unpaired:[], roles:{cad-task:{dispatches:2, unrecorded:2}} with NO tokens and NO turns key. The six lifecycle events carried exactly two distinct correlation ids, three to a run - '0-f9e486d1-1040900-1' and '0-f9e486d1-1040900-2' - neither the bare '0', which is what the run-token half of the --sha anchor buys over a per-commit one."
    },
    {
      "n": 7,
      "truth": "An abandoned run does not mis-fund the next one: run 2's close pairs with run 2's own dispatch and run 1 is reported unpaired",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "Second isolated root (<scratchpad>/probe2/.planning): run 1 anchor+dispatch with NO close, run 2 anchor+dispatch+close. Render: brackets:[1] under corr '0-f9e486d1-1041129-2', unpaired:[1] under corr '0-f9e486d1-1041129-1'. FIFO pairing did not cross runs."
    },
    {
      "n": 8,
      "truth": "The bracket cannot be edited out of workflows/task.md with the suite green, and the coordinator-billed --turns exception did not widen",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "trace.test.mjs:1765 adds `cadence-core/workflows/task.md` to BRACKETING with floor 1. Mutation: deleting the `--event dispatch --plan <the task's slug>` line failed with 'cadence-core/workflows/task.md: expected at least 1 written `--event dispatch` bracket(s), found 0'. Restored (git checkout, diff clean). Non-widening mutation: stripping `--turns` from execute.md's cad-executor close failed with 'cadence-core/workflows/execute.md: a `trace close` with no `--turns` ... Every close site carries it but the ONE stated exception - the coordinator-billed `cad-task` close in cadence-core/workflows/task.md'. Restored, diff clean. The exception at trace.test.mjs:1985 is keyed on file AND role and is self-limiting - it asserts the exempted close carries neither --tokens nor --turns."
    },
    {
      "n": 9,
      "truth": "The FST-02 prose pin fails loudly, in both directions, with a message naming which line is missing",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "Mutation A - removed `--phase 0` from workflows/task.md:125's risk-check run line: prose-agreement.test.mjs failed with \"task.md's risk_check step calls the seam without `--phase 0` - a task sits outside the phase spine and 0 is the one number no roadmap phase carries...\". Mutation B - removed the risk_check step's 'On `written: false` ... do not report done' sentence: failed with \"task.md's risk_check step states the written flag but not that done is withheld on it - detection without enforcement is the outcome RSK-02 exists to prevent\". Two distinct, self-naming messages. Both edits reverted with `git checkout --`; `git status --porcelain cadence-core/workflows/task.md` empty and `git diff --stat` empty afterwards. Note the withholding pin stays non-vacuous even though task.md now says `written: false` in three places: the second assertion's `[^.]*` window cannot reach 'report done' from the task-record step's sentence."
    },
    {
      "n": 10,
      "truth": "/cad-task gained no context step, no plan gate and no verify walk (ROADMAP criterion 5)",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "`grep -n 'cad-plan-checker\\|cad-verify' cadence-core/workflows/task.md` returns nothing. `grep -c 'cad-context'` returns 2, at :35 ('This is phase-sized. Route it through /cad-context -> /cad-plan') and :254 (the outgrows-its-lane guardrail) - both route work OUT to the phase spine. `git diff 4ede0907^ HEAD -- cadence-core/workflows/task.md | grep cad-context` returns nothing, so this phase moved neither line. No workflow.max_dispatch_tokens.cad-task key was added (D-08 held)."
    },
    {
      "n": 11,
      "truth": "Self-verify is clean and the full suite is green",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "`node cadence-core/bin/self-verify.mjs --root .` -> ok:true, problems: []. `node cadence-core/bin/test.mjs` -> tests 2903, pass 2903, fail 0, exit code 0 (single full-suite run)."
    },
    {
      "n": 12,
      "truth": "workflows/task.md calls the record writer on BOTH paths and reports the landing site, withholding the line on written:false",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "task.md:190 `<step name=\"record\">` says '(Both work paths...)' and :199 invokes `planning.mjs task-record --slug ... --base <the echoed start sha> --head HEAD --text-file <scratch>/what-shipped.txt`; :234 the done block reports 'Record: {the `record` path from the task-record envelope}'; :238 withholds it on `written: false` and states the reason instead. The invocation's subcommand and all five flags are declared at lib/arg-contract.mjs:649-655, which is what self-verify check 2 (unknown-subcommand) proves by returning problems: []. The seam's contract half is proved by truths 1 and 3; the live end-to-end half is truth 14."
    },
    {
      "n": 13,
      "truth": "Every acceptance criterion the phase carries is satisfiable against the shipped tree as written",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "Two criteria are not. AC3/UAT item 3's clause 'that arm's trace holds zero lifecycle/dispatch events' is provably incompatible with the same item's first clause - renderTrace pairs a terminal only against a dispatch, so an arm with zero dispatch events has no bracket and no cad-task role row at all; my probe shows the inline arm holds exactly one lifecycle/dispatch, which is the bracket AC3 also demands. AC5/UAT item 5's 'a grep for cad-context, cad-plan-checker and cad-verify in that file returns nothing' is false against the shipped file: cad-context is at task.md:35 and :254, both pre-dating this phase (git diff over the phase's commits touches neither line). PLAN-3.md's Notes name both re-readings explicitly ('AC3's third clause, read literally, contradicts its first'; 'AC5's grep, read literally, already fails against the shipped file'), but CONTEXT.md's AC3/AC5 and the generated UAT items were never amended, and SUMMARY.md records 'Deviations: None - plans executed as written'. A human walking items 3 and 5 literally will fail two items whose underlying behavior is correct."
    },
    {
      "n": 14,
      "truth": "A live /cad-task session end to end writes the record, brackets exactly once, and reports the record's path in its done block",
      "status": "UNCERTAIN",
      "uat_item": 7,
      "evidence": "Not machine-settleable here. `grep -c cad-task .planning/trace.jsonl` returns 0 - the live tree has never carried a cad-task bracket, and SUMMARY.md states this limit itself ('what is pinned is the prose and the census rather than a live-tree run'). Everything a probe can reach is verified: the prose (truth 12), the census pin (truth 8), the seam behavior (truths 1, 3, 6, 7)."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "Task record written and reachable by recall",
      "evidence": "recall 'plan size ceiling max_plan_tasks task count' returns tasks/bound-plan-size/RECORD.md at rank 4 of 5 with no phase key; the record carries a '## Commits' table of 40-char shas and a '- **Files:**' line. Writer exercised in an isolated repo: derived-not-retyped (commits and files read off the range), byte-identical on re-run, '../escape' slug refused, and with no .planning/ present it created nothing and answered written:false with the reason 'no planning root at <path>, and this command creates neither it nor tasks/'."
    },
    {
      "k": 2,
      "name": "/cad-why resolves a task-touched file to its task",
      "evidence": "why.mjs cadence-core/templates/config.json renders 093408c9 as 'off-roadmap task bound-plan-size - a /cad-task run, not a roadmap phase (tasks/bound-plan-size)' with 'declared by: declared in RECORD.md', no gap block, no phase number. Tier ordering holds: a sha in both a phase SUMMARY and a task record still resolves to the phase (why-corpus.test.mjs named cases pass)."
    },
    {
      "k": 3,
      "name": "Phase-0 bracket pairs under a per-run correlation id",
      "evidence": "Substantive half verified against an isolated planning root. Two runs of one slug from one unchanged HEAD: brackets 2, unpaired [], roles {cad-task:{dispatches:2, unrecorded:2}} with no tokens and no turns key, and two DISTINCT correlation ids ('0-<sha>-<pid>-1' and '-2'), neither the bare '0'. Abandoning run 1's close leaves exactly one unpaired dispatch from run 1 while run 2 pairs with its own. The item's clause 'that arm's trace holds zero lifecycle/dispatch events' is NOT satisfied and cannot be - see the appended criteria-text gap."
    },
    {
      "k": 4,
      "name": "FST-02 prose pin fails loudly when either line is removed",
      "evidence": "Both mutations proved by editing, running and reverting. Removing '--phase 0' fails naming that flag; removing the risk_check withholding sentence fails naming the withholding. Distinct messages, each naming its own subject. Tree restored - git diff --stat empty."
    },
    {
      "k": 5,
      "name": "task.md names no planning machinery",
      "evidence": "ROADMAP criterion 5 holds: cad-plan-checker and cad-verify appear nowhere in cadence-core/workflows/task.md, and no workflow.max_dispatch_tokens.cad-task key was added. cad-context appears exactly twice (:35, :254), both routing work OUT to the phase spine and both byte-unchanged by this phase's commits. The item's expected text ('returns nothing') is literally false against the shipped file - see the appended criteria-text gap."
    },
    {
      "k": 6,
      "name": "Self-verify clean and full suite green",
      "evidence": "self-verify.mjs --root . -> ok:true, problems: []. test.mjs -> 2903 tests, 2903 pass, 0 fail."
    }
  ],
  "gaps": [
    {
      "name": "AC3 and AC5 carry clauses the delivery deliberately contradicts, and neither CONTEXT nor SUMMARY records the change",
      "reason": "behavior wrong - the criteria text, not the code. AC3/item 3's 'that arm's trace holds zero lifecycle/dispatch events' is incompatible with the bracket the same criterion demands (a role row and a paired bracket exist only because a dispatch event does), and AC5/item 5's 'a grep for cad-context ... returns nothing' is false against a file this phase never touched on those two lines. PLAN-3.md's Notes state both re-readings; CONTEXT.md's acceptance criteria and the generated UAT items were never amended, and SUMMARY.md's Deviations section says 'None - plans executed as written'. A human walking items 3 and 5 literally fails two items whose behavior is correct, and the phase record then disagrees with itself about what was contracted.",
      "evidence": "cadence-core/workflows/task.md:35 and :254 hold the two cad-context mentions; `git diff 4ede0907^ HEAD -- cadence-core/workflows/task.md | grep cad-context` is empty. Live probe of the bracket shows one lifecycle/dispatch on the inline arm - removing it drops roles.cad-task and brackets entirely (proved by the BRACKETING mutation). .planning/phases/3/CONTEXT.md:186-197 (AC3, AC5); .planning/phases/3/PLAN-3.md Notes ('AC3's third clause, read literally, contradicts its first'; 'AC5's grep, read literally, already fails against the shipped file'); .planning/phases/3/SUMMARY.md:48-53 (Deviations: None).",
      "severity": "minor",
      "missing": "Amend CONTEXT.md's AC3 third clause to what PLAN-3 delivered and ROADMAP criterion 5 states - 'no dispatch event on the inline arm names any role but cad-task, because the record costs no subagent there' - and AC5 to 'cad-plan-checker and cad-verify appear nowhere in cadence-core/workflows/task.md, and the two /cad-context mentions stay at exactly 2, byte-identical, both routing work out to the phase spine'. Re-word UAT items 3 and 5 to match, and record the two re-readings in SUMMARY.md's Deviations rather than leaving it at 'None'."
    }
  ],
  "human_checks": [
    {
      "name": "Run /cad-task end to end in a live session on this repository and read back its done block and `planning.mjs trace render --phase 0`",
      "expected": ".planning/tasks/<slug>/RECORD.md is written; `trace render --phase 0` shows exactly one new paired bracket for that slug with role cad-task under a correlation id that is not the bare '0' and nothing new in `unpaired`; the done block prints a `Record:` line naming the record's path.",
      "why_human": "Out of reach, not merely unexercised. The behavior is an interactive slash-command session driven by the coordinator's own context, which no probe in this pass can launch. It must also run against the LIVE .planning/ tree to be the check it claims to be, and a verification pass is barred from mutating it: an abandoned or partial probe run would append a permanent unpaired cad-task dispatch to .planning/trace.jsonl and misprice this milestone's per-role accounting forever. Every part of it a probe CAN reach was executed against an isolated planning root and passed (record written and byte-stable, bracket paired per-run, second run not mis-funded by an abandoned first, prose and flags declared and pinned)."
    }
  ]
}
