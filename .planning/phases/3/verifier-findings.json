{
  "status": "gaps",
  "score": "5/12",
  "truths": [
    {
      "n": 1,
      "truth": "The lean-posture reference exists, is read at the step its register row anchors, and the register/budget checks are clean",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/references/lean-build.md exists (3,089 B, `node cadence-core/bin/weight.mjs` -> {\"surface\":\"cadence-core/references/lean-build.md\",\"bytes\":3089}) and is substantive prose stating one boundary, not a taxonomy (D-03): 'Those are illustrations of ONE boundary, never buckets to sort a change into.' Register row at cadence-core/bin/lib/deferred-reads.mjs:231-236 {skill:'cad-executor-contract', reference:'references/lean-build.md', anchors:['1'], read_paragraphs:1}; the anchored Read is skills/cad-executor-contract/SKILL.md:22-25 process step 1, '`${CLAUDE_PLUGIN_ROOT}/cadence-core/references/lean-build.md` (one consult site - this step)'. `node cadence-core/bin/self-verify.mjs` -> {\"ok\":true,\"checked\":\"config-keys, invocations, paths, internals-paths, budgets, tools, agent-skills, agent-behaviour, rung-effort, verifier-write-grant, routing-cells, effort-enums, config-reach, dispatch-phrasing, route-relay, merge-warnings, deferred-reads, script-contracts, nul-bytes, include-consumers\",\"problems\":[]} - so no deferred-read-unread, no budget-overrun, no unbudgeted-surface. Budget row cadence-core/bin/weight-budgets.json:33 = 3089, equal to measured."
    },
    {
      "n": 2,
      "truth": "The executor contract routes a declined fuller shape to an Open items line, keeps the deviation definition narrow, and still returns exactly five digest fields",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "skills/cad-executor-contract/SKILL.md <deviation_rules> (:82-129): 'A fuller shape you declined to build is an open item of the same kind: one `Open items:` line naming it and why the lean shape met the `Verify:`, never a `[deviation]` line, because nothing turned out wrong.' The <report> block (:204-219) still reads 'exactly these five fields' - PLAN/Tasks/Commits/Deviations/Open items - and explicitly forbids a sixth. The wording half rides in the reference: cadence-core/references/lean-build.md '## Writing the declined shape down' carries the example Open items line and states the routing belongs to <deviation_rules>, so neither restates the other. `node --test cadence-core/bin/*.test.mjs` (workflow.test_command, .planning/config.json:5) -> tests 1420, pass 1420, fail 0, exit 0; prose-agreement.test.mjs is in that run."
    },
    {
      "n": 3,
      "truth": "The minimalism command added no seventh role and no sixth review trigger",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "`node -e` over cadence-core/route-table.json -> [\"cad-planner\",\"cad-assumptions-analyzer\",\"cad-verifier\",\"cad-reviewer\",\"cad-executor\",\"cad-plan-checker\"], length 6. `grep -n 'minimalism' cadence-core/config.schema.json` -> no output (no minimalism review trigger, and no minimalism config key at all, so D-06's schema arm was not taken). No agents/cad-minimalism* file exists (`ls agents/*.md` -> 19 rung files, matching README:146). cadence-core/workflows/minimalism-review.md <purpose> states 'no entry in references/review-triggers.md's wiring table, no `review.triggers` key, no gate and no verdict'; self-verify's routing-cells and effort-enums checks are green."
    },
    {
      "n": 4,
      "truth": "A /cad-minimalism-review run returns a ranked delete-list in the shared findings shape and mutates nothing",
      "status": "UNCERTAIN",
      "uat_item": 4,
      "evidence": "Structure is in place but no run was observed. skills/cad-minimalism-review/SKILL.md allowed-tools = Read, Bash, Glob, Grep, Task, AskUserQuestion - no Write, no Edit. It @-includes cadence-core/workflows/minimalism-review.md (8,244 B, budget row :65 = 8244), whose <dispatch> step brackets one cad-reviewer via `planning.mjs trace append --family lifecycle --event dispatch --role cad-reviewer` and closes with return/checkpoint, and whose <guardrails> pin the return shape to {findings:[{file,line,severity,claim,failure_scenario}]} with severity carrying the rank. Bash is granted, so 'writes nothing' is a prose rail over a granted tool, not a structural impossibility - only a walked run settles it."
    },
    {
      "n": 5,
      "truth": "A /cad-capture --cadence run from a host project appends host + provoking command to the global Cadence queue and leaves the host repo untouched",
      "status": "UNCERTAIN",
      "uat_item": 5,
      "evidence": "skills/cad-capture/SKILL.md step 5 (the --cadence arm) resolves the queue as CAPTURE.md in the directory part of CADENCE_GLOBAL_CONFIG else ~/.claude/cadence/, explicitly 'Never `${CLAUDE_PLUGIN_ROOT}`', writes `- <YYYY-MM-DD> <text> (host: <host>, command: /cad-<name>)` with host from `git remote get-url origin` else toplevel basename else cwd, and carries three rails: no commit, nothing transmitted, and 'NEVER fall back to `.planning/CAPTURE.md`' on an unresolvable directory. Step 1 routes --cadence past steps 2-4 and step 4 is marked 'the project-directed arm only'. This arm is entirely prose over granted Write/Edit/Bash - there is no seam or test asserting it - so the landing location and the untouched host tree are only settleable by a walked run."
    },
    {
      "n": 6,
      "truth": "Both CTW-06 prose cuts landed, the two named keeps survive verbatim, and both budget rows were re-pinned in the same commit",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "`git show d2c6544` touches exactly skills/cad-land/SKILL.md + cadence-core/bin/weight-budgets.json: the guardrails re-derivation ('it SKIPS the 4a ask rather than preselecting a default in it, and it still halts on a blocking `pre_ship` finding') is replaced by 'its mechanic is stated once, at steps 3 and 4(b), beside the code that runs it.' `git show a87c1ee` touches exactly skills/cad-executor-contract/SKILL.md + weight-budgets.json: step 3's duplicated carve-out becomes 'A failure here is a blocker and gets a carve-out of its own - see `<deviation_rules>`', with the single surviving copy at :110-114. Keeps: `diff` of lines 110-118 against `git show f98c0c4:skills/cad-land/SKILL.md` is empty (the 'NOT scoped to the GitHub arm' clause is byte-identical at :115), and the no-preselected-default text survives at :79-80 and :198. Re-pins equal measured bytes: weight.mjs reports cad-land 12041 / cad-executor-contract 10718, weight-budgets.json:92 = 12041 and :89 = 10718. self-verify green (problems:[]), so no deferred-read-unread from the edits."
    },
    {
      "n": 7,
      "truth": "/cad-suggest presents each recommendation with its trace figures and its /cad-config key and writes no config",
      "status": "UNCERTAIN",
      "uat_item": 7,
      "evidence": "The seam half is probed: `node cadence-core/bin/planning.mjs trace suggest --dir /data/code/cadence/.planning` -> {\"ok\":true,\"scope\":\"all\",\"events_read\":285,\"suggestions\":[{\"kind\":\"suggest\",\"subject\":\"cad-executor\",\"evidence\":\"3 checkpoint return(s) - plans may exceed one context\",\"action\":\"workflow.max_plan_tasks\"},{\"kind\":\"suggest\",\"subject\":\"cad-plan-checker\",...,\"action\":\"model.effort.cad-plan-checker\"},...]}. Both returned action keys are real schema keys (cadence-core/config.schema.json:31 workflow.max_plan_tasks, :19-23 the model.effort.* family), so the value traced end to end is a config key the relay can name. skills/cad-suggest/SKILL.md allowed-tools = Read, Bash only (no Write, no Edit) and @-includes cadence-core/workflows/suggest.md, whose <present> step requires the evidence verbatim plus the action key and whose <guardrails> forbid writing .planning/config.json or the global layer. The presentation itself was never observed - Bash is granted, so read-only is a rail, not a structural bar."
    },
    {
      "n": 8,
      "truth": "/cad-suggest refuses a thin trace in exactly one line with zero suggestions",
      "status": "UNCERTAIN",
      "uat_item": 8,
      "evidence": "Seam half probed against a fresh empty .planning: `node cadence-core/bin/planning.mjs trace suggest --dir <scratch>/thin/.planning` -> {\"ok\":true,\"scope\":\"all\",\"events_read\":0,\"suggestions\":[]}, which matches D-13's measurement that events_read is the only discriminator. cadence-core/workflows/suggest.md <step name=\"thin_record\"> encodes exactly that: it REPLACES present, is 'exactly one line', splits on events_read 0 vs non-zero, and forbids stating a floor figure or reporting 'no trace' for a below-floor record. Whether the command emits exactly one line needs the command run."
    },
    {
      "n": 9,
      "truth": "/cad-suggest is registered on all three surfaces and workflows/suggest.md is the single statement of the presentation rules",
      "status": "VERIFIED",
      "uat_item": 9,
      "evidence": "cadence-core/references/COMMANDS.md:53 carries the `/cad-suggest [N]` row under ## Support (and :31 the `/cad-minimalism-review` row); skills/cad-help/SKILL.md @-includes COMMANDS.md, so that row IS the help registration (D-16), and its budget row :23 = 4771 equals the measured 4771. README.md:128 carries the /cad-suggest bullet and :108 the minimalism bullet. .planning/DOCS-CLAIMS.md:235 re-pins README-39 to 105-130 (the widened command lists, which contain both new bullets) and :240 rewrites README-44 to 'Today it is 27 skills and 6 agent roles across 19 rung files' with resolution 'corrected - fe9b9b9'; that count is independently true - `grep -L 'user-invocable: false' skills/*/SKILL.md | wc -l` -> 27 and `ls agents/*.md | wc -l` -> 19. Single statement: cadence-core/workflows/milestone.md:155-162 step 8 now invokes /cad-suggest and says 'Its rules live in `cadence-core/workflows/suggest.md` and are not restated here' (no `trace suggest` seam call survives in milestone.md - `grep -n suggest` returns only :157 and :159), and cadence-core/workflows/report.md:68-69 points at /cad-suggest 'whose rules live in `cadence-core/workflows/suggest.md`'."
    },
    {
      "n": 10,
      "truth": "The phase's own contract documents agree on where a declined fuller option is recorded",
      "status": "FAILED",
      "uat_item": null,
      "evidence": ".planning/ROADMAP.md phase-3 success criterion 1 still reads 'records the fuller option in its deviation record rather than building it speculatively'. The shipped contract does the opposite by design: skills/cad-executor-contract/SKILL.md <deviation_rules> says 'never a `[deviation]` line'. .planning/REQUIREMENTS.md:21 (MIN-01) was corrected to 'records the fuller option as an `Open items:` line', so the ROADMAP is the only surface still stating the superseded shape. Independently confirmed, not taken from SUMMARY."
    },
    {
      "n": 11,
      "truth": "The Cadence-directed capture arm is reachable by a user reading Cadence's own command surfaces",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "cadence-core/references/COMMANDS.md:48 - '`/cad-capture [todo\\|seed\\|note] <text>` | Park a phase-linked todo, a backlog seed, or a note without derailing.' - names no --cadence arm, and that table IS the /cad-help registration. README.md:123 - '**`/cad-capture`** - a phase-linked todo or a seed idea, captured without losing your place.' - likewise. `grep -n 'cadence' .planning/DOCS-CLAIMS.md` finds no row covering the arm. Only skills/cad-capture/SKILL.md's own frontmatter (description + argument-hint) names it. ROADMAP criterion 3 is met in behaviour and not on the surfaces a user would read; SUMMARY names this and the check confirms it."
    },
    {
      "n": 12,
      "truth": "The new minimalism dispatch's bracket is guarded against silent removal the way every other bracketing prose file's is",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "cadence-core/bin/trace.test.mjs:729-737 BRACKETING holds rows for context.md, plan.md, plan-revision.md, review-triggers.md, execute.md, decision-review.md and verify-deep.md - and no row for cadence-core/workflows/minimalism-review.md. The map's own header comment at :726-727 states the consequence: 'A file absent from this map is checked by nothing, so the row travels with the prose.' The workflow does carry an opened and closed bracket today (its <dispatch> step writes dispatch plus return/checkpoint appends), and the global census arm still validates family/event/--role/--read, but nothing asserts the pair keeps existing in that file."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "Lean-posture reference is registered and read",
      "evidence": "deferred-reads.mjs:231-236 row (anchors ['1']) + cad-executor-contract SKILL.md:22-25 step-1 Read of references/lean-build.md; `node cadence-core/bin/self-verify.mjs` -> problems:[] across all 20 checks (no deferred-read-unread, no budget-overrun, no unbudgeted-surface); weight-budgets.json:33 = 3089 = measured."
    },
    {
      "k": 2,
      "name": "Declined fuller option routes to Open items",
      "evidence": "cad-executor-contract SKILL.md <deviation_rules>: a declined fuller shape is 'one `Open items:` line ... never a `[deviation]` line'; <report> still 'exactly these five fields' and forbids a sixth; `node --test cadence-core/bin/*.test.mjs` -> 1420 pass, 0 fail (prose-agreement.test.mjs included)."
    },
    {
      "k": 3,
      "name": "Minimalism command added no role and no trigger",
      "evidence": "route-table.json roles array length 6 (unchanged six names); `grep -n minimalism cadence-core/config.schema.json` -> no match; no agents/ rung file added (19 rung files)."
    },
    {
      "k": 6,
      "name": "Both prose cuts landed with their keeps and re-pins",
      "evidence": "d2c6544 and a87c1ee each touch their SKILL.md plus weight-budgets.json only; guardrails re-derivation and step-3 carve-out duplicate both deleted and replaced by pointers; diff of cad-land:110-118 vs f98c0c4 empty (GitHub-arm clause verbatim), no-preselected-default text intact at :79-80 and :198; weight.mjs 12041/10718 equals weight-budgets.json:92/:89; self-verify problems:[]."
    },
    {
      "k": 9,
      "name": "/cad-suggest is registered and is the one statement of the rules",
      "evidence": "COMMANDS.md:53 (help registration via cad-help's @-include), README.md:128, DOCS-CLAIMS.md:235 README-39 re-pinned 105-130 and :240 README-44 corrected to 27 skills (independently measured 27 skills / 19 rung files); milestone.md:155-162 invokes /cad-suggest and no longer calls the seam or restates the rules, report.md:68-69 points at workflows/suggest.md."
    }
  ],
  "gaps": [
    {
      "name": "ROADMAP phase-3 criterion 1 still names a record shape the shipped contract forbids",
      "reason": "behavior wrong - documentation contract disagrees with the code it governs. .planning/ROADMAP.md criterion 1 says the executor 'records the fuller option in its deviation record'; skills/cad-executor-contract/SKILL.md routes it to `Open items:` and says 'never a `[deviation]` line'. The gate that reads that criterion therefore reads it against a contract that cannot satisfy it as worded.",
      "evidence": ".planning/ROADMAP.md phase-3 Success Criteria item 1 vs skills/cad-executor-contract/SKILL.md <deviation_rules> (:82-129). .planning/REQUIREMENTS.md:21 was already corrected to the `Open items:` wording, so ROADMAP is the lone stale surface.",
      "severity": "minor",
      "missing": "Rewrite ROADMAP phase-3 criterion 1 to 'records the fuller option as an `Open items:` line in its report file', matching REQUIREMENTS.md MIN-01 and CONTEXT D-02/AC2."
    },
    {
      "name": "/cad-capture --cadence is undiscoverable on every surface a user reads",
      "reason": "unwired at the documentation layer - the arm exists and is specified in skills/cad-capture/SKILL.md step 5, but the reference table that IS the /cad-help registration, the README command list and the claim ledger all still describe only the project-directed arm, so the loop the goal names ('friction found on somebody else's project reaches Cadence') depends on a flag nobody is told about.",
      "evidence": "cadence-core/references/COMMANDS.md:48 and README.md:123 /cad-capture rows mention no --cadence; no .planning/DOCS-CLAIMS.md row covers the arm. Only the skill's own frontmatter description/argument-hint names it.",
      "severity": "minor",
      "missing": "Extend the COMMANDS.md and README /cad-capture entries to name the --cadence arm and where it writes, and add the matching DOCS-CLAIMS.md row (with the surrounding README rows re-pinned if lines shift)."
    },
    {
      "name": "The minimalism-review dispatch bracket is watched by no per-file assertion",
      "reason": "missing test coverage on a new dispatch site. cadence-core/bin/trace.test.mjs's BRACKETING map carries a row for every other prose file that dispatches a worker, and none for cadence-core/workflows/minimalism-review.md, so its dispatch/return pair can be edited away with the suite still green.",
      "evidence": "cadence-core/bin/trace.test.mjs:729-737 (map contents) and :726-727 ('A file absent from this map is checked by nothing, so the row travels with the prose'). The workflow's <dispatch> step does currently write both halves.",
      "severity": "minor",
      "missing": "Add `[join('cadence-core','workflows','minimalism-review.md'), 1]` to BRACKETING in cadence-core/bin/trace.test.mjs."
    }
  ],
  "human_checks": [
    {
      "name": "Minimalism command run returns a delete-list and writes nothing",
      "expected": "A /cad-minimalism-review run against a named target returns a ranked delete-list in the reviewer findings shape ({file,line,severity,claim,failure_scenario}, ordered blocker->low, target named in the opening line), and `git status --short` is byte-identical before and after the run.",
      "why_human": "Out of reach, not merely unprobed: settling it requires invoking the /cad-minimalism-review slash command, which dispatches a live cad-reviewer subagent through the spawn-agent seam and appends real lifecycle events to .planning/trace.jsonl. A verifier cannot invoke a slash command, and the run mutates the shared run record, which this pass is barred from doing. CONTEXT AC3 marks it human-verify for this reason."
    },
    {
      "name": "Cadence-directed capture lands outside the host project",
      "expected": "A /cad-capture --cadence run from inside a non-Cadence host project appends `- <date> <text> (host: <origin url or repo basename>, command: /cad-<name>)` to ~/.claude/cadence/CAPTURE.md (or CADENCE_GLOBAL_CONFIG's directory), while the host repo's .planning/CAPTURE.md and `git status --short` are both unchanged and no commit is made in either tree.",
      "why_human": "Out of reach: it needs a slash-command invocation from inside a different repository on the user's machine, and its whole subject is a write to the user's home directory outside every project boundary. A verifier may not mutate that state, and the arm is prose over granted Write/Bash with no seam or test to exercise instead. CONTEXT AC4 marks it human-verify."
    },
    {
      "name": "/cad-suggest presents figures and writes no config",
      "expected": "/cad-suggest on this repo presents an opening scope + events_read line, then each `suggest` entry numbered with its subject, its verbatim seam evidence and its config key (e.g. workflow.max_plan_tasks, model.effort.cad-plan-checker), each `info` entry as one receipt line, and leaves .planning/config.json and the global Cadence config byte-identical.",
      "why_human": "Out of reach: the seam half is already settled by probe (trace suggest returns 285 events and 3 suggestions carrying real schema keys), but what the command PRESENTS is produced by a slash-command invocation this verifier cannot make. CONTEXT AC6 marks it human-verify."
    },
    {
      "name": "/cad-suggest refuses a thin trace in one line",
      "expected": "Run against a .planning/ whose trace is absent or below the evidence floors, /cad-suggest emits exactly one refusal line and zero suggestions - 'no events in the scope read' for events_read: 0, or 'N events read, none cleared the floors' for a non-zero read - and states no floor figure.",
      "why_human": "Out of reach: the seam's thin return is probed ({\"ok\":true,\"scope\":\"all\",\"events_read\":0,\"suggestions\":[]}), but 'exactly one line' is a property of the command's rendered output, reachable only by invoking the slash command against a scratch .planning/. CONTEXT AC6 marks it human-verify."
    }
  ]
}
