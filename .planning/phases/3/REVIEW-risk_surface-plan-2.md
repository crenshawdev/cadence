{"findings":[{"file":"cadence-core/bin/lib/planning-files.mjs","line":1253,"severity":"medium","claim":"sectionSpan matches a four-space-indented heading (CommonMark indented code) via trim(), where the pre-fix matcher /^## Phases\\s*$/ was column-0 anchored - an indented code example above the real section shadows it.","failure_scenario":"A document with an indented '    ## Active' example above the real '## Active' selects the example as start and terminates at the real heading, so the real section's ids are never read."}],"adjudication":"1 raised by openai/gpt-5.6-sol, 1 survived (confirmed against 8dd0093 pre-fix regex vs new sectionSpan trim comparison at lib/planning-files.mjs:1253). Medium - below the blocking gate's blocker/high bar; routed to phase open items."}
