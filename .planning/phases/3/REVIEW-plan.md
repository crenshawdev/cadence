# Plan review - phase 3 (advisory)

Trigger: `plan` | gate: advisory | backend: openai `gpt-5.6-luna` (effort high) | 2026-08-13
Payload: PLAN-1.md, PLAN-2.md, PLAN-3.md, PLAN-4.md

```json
{"ok":true,"provider":"openai","model":"gpt-5.6-luna","findings":[{"file":".planning/phases/3/PLAN-1.md","line":54,"severity":"high","claim":"MIN-01's executor-side minimalism is implemented as a reference file and a `Read` instruction, but nothing enforces that the executor actually chooses the lean shape or writes the fuller shape to `Open items:`.","failure_scenario":"The executor ignores the new posture, builds a generalized abstraction, and still produces a valid five-field report. `self-verify`, the prose tests, and the byte-budget check all pass because they validate wording and reachability rather than the executor's behavior."},{"file":".planning/phases/3/PLAN-2.md","line":50,"severity":"high","claim":"The workflow requires `planning.mjs trace append` dispatch and return events while also promising that the minimalism pass edits no planning file and leaves `git status` byte-identical.","failure_scenario":"A normal `/cad-minimalism-review` run appends lifecycle events to `.planning/trace.jsonl`. That is a planning-file mutation and, if the file is tracked, changes `git status`, making the stated apply-nothing and human-verification criteria mutually incompatible."},{"file":".planning/phases/3/PLAN-2.md","line":50,"severity":"medium","claim":"Target resolution is described in prose but has no concrete algorithm or test for resolving a path, directory, empty argument, ambiguous argument, or phase committed range.","failure_scenario":"A phase argument is passed through as `--read \"<target>\"` without resolving its actual commit range, or an ambiguous directory is guessed. The only automated verification is structural, so the reviewer can inspect the wrong code while the plan reports success."},{"file":".planning/phases/3/PLAN-2.md","line":83,"severity":"high","claim":"The apply-nothing guarantee is only a guardrail in prose, while the new skill explicitly grants `Bash` and `Task` access.","failure_scenario":"The workflow or spawned reviewer uses shell redirection, `rm`, `mv`, or another Bash operation while investigating a delete candidate. The skill has no tool-level prohibition or post-run enforcement; one successful manual run cannot establish that the command never edits source, planning, or configuration files."},{"file":".planning/phases/3/PLAN-3.md","line":45,"severity":"high","claim":"XCP-01's new arm has no stable invocation contract: the plan says to add an explicit Cadence-directed flag but never names the flag or its syntax, and it deliberately does not update the existing command reference or README.","failure_scenario":"An implementation chooses `--cadence`, while a user or later automation invokes `--cadence-only` or cannot discover the mode at all. The human verification says only “with the Cadence-directed flag,” so it cannot verify a defined interface or discoverability."},{"file":".planning/phases/3/PLAN-3.md","line":45,"severity":"high","claim":"The entire XCP-01 behavior is delegated to skill prose; no helper, capture seam, integration test, network assertion, or failure-path test is added.","failure_scenario":"The model leaves the existing unconditional commit step active, falls back to `.planning/CAPTURE.md` when global-path resolution fails, or transmits a host detail through an available tool. A happy-path grep for `CADENCE_GLOBAL_CONFIG` and one manual run will still pass, so the required no-commit, no-network, no-fallback guarantees are not falsifiable."},{"file":".planning/phases/3/PLAN-4.md","line":49,"severity":"high","claim":"TUN-01 has no deterministic behavioral verification for recommendations, evidence figures, thin traces, malformed traces, or the zero-suggestion refusal.","failure_scenario":"`/cad-suggest` always prints a refusal, drops the evidence figures, or prints a fabricated figure. The suggested human run can be performed against a trace with no recommendations and therefore passes vacuously; `self-verify` only checks text, flags, and budgets."},{"file":".planning/phases/3/PLAN-4.md","line":96,"severity":"medium","claim":"The single-statement requirement is verified only by checking that both files mention `suggest.md` and that one exact phrase is absent.","failure_scenario":"`milestone.md` retains the numbered-list, receipt, `/cad-config`, or empty-list rules using slightly different wording. Both grep checks still pass even though the presentation contract is duplicated and the one-statement requirement is violated."},{"file":".planning/phases/3/PLAN-4.md","line":157,"severity":"medium","claim":"Task 6's central ledger verification contains a literal placeholder rather than an executable check, and the README-44 check does not compare claim text to the README sentence.","failure_scenario":"`sed -n '<the row's line cell>p' README.md` cannot be run as written, so stale line references or a mismatched corrected claim can remain in `.planning/DOCS-CLAIMS.md` while the task is marked verified."},{"file":".planning/phases/3/PLAN-2.md","line":92,"severity":"low","claim":"The skill-count verification does not prove that the new command adds exactly one user-invocable skill: it reports only the current total and equates “no `user-invocable: false` text” with invocability.","failure_scenario":"A pre-existing skill changes its frontmatter, or a non-invocable skill omits the literal false value. The command still produces a number, but there is no before/after baseline and the README count can be updated to the wrong value."},{"file":".planning/phases/3/PLAN-4.md","line":113,"severity":"low","claim":"The close workflow is repointed to `/cad-suggest` before the later task that adds that command to `COMMANDS.md` and before the README registration task; the plan never states whether `SlashCommand` bypasses those registration surfaces.","failure_scenario":"If the SlashCommand seam resolves commands through the documented command registry, Task 3's intermediate commit invokes an unregistered command and its close path fails. The plan's sequencing leaves the dependency implicit and provides no verification at the point where the invocation is introduced."}]}

```
