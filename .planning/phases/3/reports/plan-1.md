PLAN COMPLETE
Plan: .planning/phases/3/PLAN.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Record an empty diff body as a completed EMPTY check | 25b9719 | `scanDiff` splits the non-string body (`checked:false, inconclusive:true, empty:false`) from a string body that trims to nothing (`checked:true, inconclusive:false, matches:[], empty:true`); `empty` rides every return and both the trace record and the envelope. risk-diff.test.mjs 71/71 pass, naming the same-commit row, the revert-pair row whose ids differ, the unchanged unreadable-range row, and AC4's binary-only and gitlink rows. Mutation (collapse the two arms back to the pre-fix single arm): 7 rows fail including all three named; restored, 71/71. Live scratch repo: `risk-check run --base HEAD --head HEAD` printed `checked:true, inconclusive:false, matches:[], empty:true` and the appended trace line carried the same four. `npx tsc -p tsconfig.ci.json` clean; self-verify `ok:true, problems:[]`. |
| 2 - Admit the empty record at `risk-check status` through the existing `recorded` arm | b5e9c15 | Reader-only change: the per-record object carries `empty: e.empty === true`. No new state string, no new predicate clause. risk-diff.test.mjs 73/73 pass, naming the end-to-end row (`risk-check run` drives the seam inside `repoFixture(FROZEN_PHASE_1)`, then `status --phase 1` is `ok:true` with state `recorded`) and the pre-fix-shape refusal row (`unchecked`, `risk-record-missing`, `empty:false`). Mutation (revert task 1's split): that end-to-end row reddens; confirmed separately in a scratch fixture that `status` then answers `reason:"risk-record-missing"`, `state:"unchecked"` - the deadlock itself. Restored, 73/73. `npx tsc -p tsconfig.ci.json` clean; self-verify `ok:true, problems:[]`. AC3's live `/cad-execute <N> --rerun` demonstration is human-verify and is transcribed into `.planning/phases/3/UAT.md` at `/cad-verify` (D-11). |
| 3 - State the rotated-report name test once, in the rotation module | 37fd169 | `rotationTarget`'s inline rotated pattern is lifted to one `rotatedSource(k)` builder; the new `isReportName(k, name)` export builds from that same source with NO `'i'` flag, plus a byte-exact canonical-name arm. report-rotation.test.mjs 12/12 pass (7 existing green, 5 new named). Mutations, each restored: dropping the trailing `.md` anchor reddens the anchors row on `plan-1.1.2.md` and the case row (2 fail); dropping the dot before the suffix reddens 6 rows including the anchors row naming `plan-11.md`; adding `'i'` reddens the case row naming `PLAN-1.1.MD` (1 fail). `npx tsc -p tsconfig.ci.json` clean; self-verify `ok:true, problems:[]`. |
| 4 - Widen `lease-check`'s report exemption to the rotated sibling | 3ee5898 | The single-name byte-equality test becomes two halves: the path sits DIRECTLY in `<pdir>/reports/` (same `repoRel`/`join` construction, a further separator disqualifies) and its final component satisfies task 3's `isReportName(k, ...)`, imported rather than re-spelled. `staged` is untouched. planning.test.mjs 427/427 pass, naming the rotated-report exemption row (`plan-1.1.md` and `plan-1.12.md` beside `plan-1.md`, `staged: 2`) and the refusal row that walks `plan-2.md`, `plan-11.md`, `plan-1-risk.diff`, `plan-1-risk-task-2.diff`, `PLAN-1.1.MD` and a nested `old/plan-1.1.md`. Mutation (exemption back to `p === reportFile`): the AC5 row FAILS `undeclared-files` naming `.planning/phases/1/reports/plan-1.1.md`, reproduced twice; restored, 427/427 three times. `npx tsc -p tsconfig.ci.json` clean; self-verify `ok:true, problems:[]`. |
| 5 - Pin the one-statement rule with a helper-census row | 31c2085 | One `HELPERS` row, `home: lib/report-rotation.mjs`, keyed on the pattern SOURCE built from an escaped string (the census file does not match itself). helper-census.test.mjs 9/9 pass with the new row named and its `found` list `['lib/report-rotation.mjs (x1)']`. Mutation (paste the pattern source into planning.mjs): the row FAILS naming `lib/report-rotation.mjs (x1), planning.mjs (x1)`; removed and it passes. `node cadence-core/bin/test.mjs other` is 528/529 - see the deviation below. self-verify `ok:true, problems:[]`; `npx tsc -p tsconfig.ci.json` clean. |
Deviations: [deviation] Task 5's Verify asserts `node cadence-core/bin/test.mjs other` passes. It is 528/529: `milestone-prune.test.mjs:557` `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair` fails because the live ROADMAP names completed phases while `.planning/REQUIREMENTS.md`'s `## Active` reads "No cycle open", so `archiveRequirements` moves nothing and the row's `moved.length > 0` assertion fires. Proven pre-existing rather than caused here: `git diff dc4c4b1..HEAD --name-only` lists only the six source files this plan declares, so the row's code (`milestone-prune.test.mjs`, `lib/milestone-prune.mjs`) and both its inputs (`.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`) are byte-identical to the phase's base commit. It is exactly CONTEXT's fourth flagged assumption - no requirement id was seeded for `v3.5.6` - which CONTEXT deliberately left uncorrected ("this phase serves no row, and the workflow permits correcting only a row the phase serves"), and `REQUIREMENTS.md` is outside this plan's `files:` lease. Not fixed and not worked around; recorded here and carried as an open item. Every other clause of that Verify, and AC1-AC7 apart from AC3's human-verify arm, are met.
Open items: (1) The pre-existing `milestone-prune.test.mjs:557` corpus failure above needs `.planning/REQUIREMENTS.md`'s `## Active` to carry this cycle's rows, or the row to skip on the state the way its own header already does for the between-milestones case. It will keep `node cadence-core/bin/test.mjs` and `test.mjs other` red until then. (2) Observed once in six runs of `planning.test.mjs`: `renumber remove: cuts line + detail, orphans reqs, shifts down, reports prose refs` failed in a single run and passed in the five others, including two further runs under the same mutated tree - a flaky row unrelated to this range, worth a look but not reproduced. (3) AC3 is unverified here by design: it needs a live `/cad-execute <N> --rerun`, which an executor cannot make, and its transcription belongs in `.planning/phases/3/UAT.md` at `/cad-verify` (D-11).
