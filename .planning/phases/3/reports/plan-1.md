PLAN CHECKPOINT: blocked
Plan: /code/cadence/.planning/phases/3/PLAN-1.md
Tasks: 7 of 8

| Task | Commit | Note |
|---|---|---|
| 1 — Pure record contract | f2229648afd57ef7ea369ecfe63419d70e7e6ec8 | 3 library tests passed; domain inspection clean; Clippy and TypeScript passed; lease accepted 5 files. |
| 2 — Durable writer tracer | 650df675186ed7ee8db78f53ab95ddc0b9f5f2cc | 5 integration tests passed as predicted; Clippy and TypeScript passed; lease accepted 4 files. |
| 3 — Lifecycle visibility | 0856ba0434c20af6ef4f2865ab0a4dbab958b733 | 7 integration tests passed as predicted; static checks passed; lease accepted 3 files. |
| 4 — Decisions log | fd26ea188239ab864e4a4fe5f0c6568bd2205c61 | 5 library and 9 integration tests passed as predicted; static checks passed; lease accepted 5 files. |
| 5 — External edit refusal | 2fd025d92478436e11b176c77739c47eb58d96f7 | 11 integration tests passed as predicted; static checks passed after one Clippy literal correction; lease accepted 3 files. |
| 6 — Multi-file recovery | 3e1fd87f008820dbbd6ab53cb7f80724972510b5 | 16 integration tests passed as predicted; static checks passed; lease accepted 7 files. |
| 7 — Process-kill regression guard | 57d03ad184891b8fbe8af17bba7a170c985b1bd3 | 4 crash tests passed as predicted (24 replacement kills, 3 replay kills, acknowledged restart); static checks passed; lease accepted 3 files. |
| 8 — Synchronization ordering | b71eeb7c6f2db6a565f7a38ef65701dd51736c68 | Implementation and test committed; static checks passed; Verify blocked as predicted: strace missing, OS error 2, exit 101. |

Deviations: none
Open items: (1) AC8 requires unavailable strace; verification remains blocked until the fixture can run on a suitable host. (2) An automatic collector for disposable pre-intent orphan temporary files was not added: they cannot authorize replay, and deterministic recovery/Verify needs only removal and directory synchronization of the durable intent.

## Run details

Read the published executor contract and lean-build reference, phase CONTEXT and PLAN-1, and PROJECT. No CLAUDE.md lookup. Starting HEAD a486fe32; pre-existing untracked phase 2 reports preserved. Frozen cadence-core diff against v3.7.12 was empty.

Task 1 prediction: cargo test -p cadence --lib reports 3 passed, 0 failed; domain inspection finds no runtime, filesystem, environment, clock, or lock dependency. Observed exactly that. The combined shell probe ended with rg status 1 because there were no prohibited matches; the test itself passed.

Static analysis: published config getter returned null workflow.lint_command; command detector returned cargo clippy --all-targets -- -D warnings and npx tsc -p tsconfig.ci.json. Clippy ran with RUSTC_WRAPPER cleared. TypeScript ran its installed executable through Node with ignored stdin, avoiding any package installation. Both exited 0.

Task 1 choices: caller-supplied stable identities; evidence enum distinguishes missing/null/text; SHA-256 snapshot self-digest excludes its digest field and covers both JSONL digests; mandatory Policy trait has no default allow implementation. Cargo library target is discovered through src/lib.rs. Dependencies resolved offline from existing caches; task cache/lock/temp paths are under target/. Commit uses requested owner identity and signing key. No attribution added.

Task 2 prediction: 5 integration tests pass, including real fresh-process readback, held confirmation and canceled admitted caller, both synchronization failures, and distinct concurrent request outcomes. Observed 5 passed, 0 failed. The adapter performs exclusive sibling creation, write_all, file sync_all, rename, directory sync_all, then readback. The writer uses one dedicated OS thread, bounded Tokio mpsc, and one oneshot per request. Errors after preparation poison that writer so it cannot acknowledge an uncertain generation. Multi-file crash recovery and external identity validation remain the ordered work of tasks 5-6. Signature verified; no deleted files.

Task 3 prediction and observation: 7 integration tests passed, 0 failed. The recall projection filters every declined identity before exposing any records, then exposes only the latest revision of each remaining identity. Tests exercise matching unique text before decline and no snippets/count after decline and reopen, explicit reason lookup, equal prose with different identities, completed-but-recallable captures, and guarded filing uncertainty. Frozen guarded producer and shared parser were read with git show v3.7.12. Chose a projection type with a private record vector as PLAN-3's item input seam. No deleted files; signature verified.

Task 4 predictions and observations: library 5 passed/0 failed; integration 9 passed/0 failed. Routing/gate/refusal are the only admitted decision variants. Blank observed effort is normalized to Missing and omitted from serialized records; unknown nonblank spelling and requested effort remain separate. Import normalization requires an agent identity for worker observations and creates no activity record. Historical absent receipts remain absent. Frozen trace and routing guards were inspected with git show. The writer dispatch arm was extended within PLAN-1's declared lease. Policy/integrity errors return directly without appending refusal evidence. No deleted files; signature verified.

Task 5 prediction and observation: 11 integration tests passed, 0 failed. Coverage includes same-size edits with original modification/access times restored, identical-byte inode replacement, deletion, target symlink, unreadable file, containing-directory replacement, and external edits after preparation. Other semantic targets and externally installed bytes remain unchanged; disposable preparation is removed. Corrupt/missing generations refuse reopening. Identity compares device/inode/mode and ancestor device/inodes in addition to complete bytes. The implemented filesystem adapter uses Unix metadata, matching the phase's Linux/macOS targets. Static analysis first flagged a decimal Unix permission literal in the test; one bounded repair changed 0 to 0o0 and Clippy then passed. TypeScript passed. This was a static-analysis repair, not a changed criterion or contradictory Verify result. Notes: admission/final validation cannot exclude uncooperative edits between check and replacement or serialize independent session processes; no lock or compare-and-swap guarantee is claimed. No deleted files; signature verified.

Task 6 prediction and observation: 16 integration tests passed, 0 failed. Returned-I/O-error interruption fixtures cover every semantic participant and every external-config participant. Reopen validates the whole intent before replay, recovers exactly one operation, resynchronizes already-intended files and directories, and removes/syncs intent. A second restart is a no-op. Foreign pending participants preserve the entire pending byte set. A queued reader observes the completed view only. Recovery file/directory synchronization failure blocks opening. Static checks passed without repairs.

Task 6 choices: all mutations use a versioned, self-digested .store-intent.json artifact; snapshot operations map is completion metadata and preserves exact user snapshot data. Registered repo-config/global-config aliases bind external paths supplied by PLAN-2; their parents must already exist. Transaction fingerprints exclude attempt-specific expected identities, so recovered imports can retry without duplication. Snapshot receipt and adapter primitives required changes to model.rs/filesystem.rs, both within the declared PLAN-1 lease. Architecture document records format, ordering, seams, and race limits. Disposable pre-intent orphan temps cannot authorize recovery and are not adopted; no automatic orphan collector was needed by Verify. Signature verified; no deletions.

Task 7 prediction and observation: 4 store_crash tests passed, 0 failed. All 24 replacement barriers used actual SIGKILL (confirmed by child exit signal), across three targets, four stages, and absent/existing initial states. Three further kills interrupted replay. Whole bytes matched independently recorded old/new candidates. Recovery and retried stable operations yielded exactly the completed byte set, with no duplicates. Acknowledged writes survived separate child processes. This establishes the AC4 process-kill regression guard only; it neither simulates power loss nor passes AC8. Frozen helper source and its no-fsync comment were inspected with git show. Both static checks passed. Signature verified; no deletions.

Task 8 prediction: the exact filtered Verify exits 101, with one failing test reporting BLOCKED AC8: strace is unavailable and OS error 2. Observed exactly that: 0 passed, 1 failed, 9 filtered out. No trace was generated, no negative control was exercised, and AC8 is not passing. Implemented real-adapter cfg(test) temporary/directory sync omissions and a request-specific synchronous pipe marker inside the writer's common finish_reply, immediately before the actual channel send. The checker pairs unfinished/resumed calls, checks exact descriptor paths, and requires successful completion ordering plus receipt of the actual matching success reply. All test-only hooks disappear from production compilation. The subprocess integration driver compiles the same source under cfg(test), so there is no alternate writer implementation. Static checks passed. Signature verified; no deletions.

All eight implementation commits are present; seven tasks have satisfied their Verify criteria. AC8 remains the explicitly anticipated missing-tool checkpoint. No strace installation, fake trace, attribution, git configuration write, or push was performed.

## Final suite and repository evidence

After the last implementation commit and partial report write, the published config getter returned null for workflow.test_command. Selected `cargo test --workspace` from the Cargo workspace manifest. This was the sole full-suite run of this dispatch. Prediction: all existing Rust groups pass except AC8, which fails on the known missing strace prerequisite. Observed exit 101: library 5/5 passed, binary 6/6 passed, MCP 4/4 passed, store integration 16/16 passed, crash integration 9 passed and AC8 alone failed. Total: 40 passed, 1 blocked failure. The crash integration count includes five domain tests from compiling the production source with cfg(test). Cargo stopped on that failure before its doc-test stage. The Node suite was not run. No repair round or repeat full suite was attempted: the only failure requires the prohibited tool installation or a suitable verification host, rather than a code repair.

All Cargo test and Clippy commands used `RUSTC_WRAPPER=` plus `CARGO_NET_OFFLINE=true`, `CARGO_HOME=/code/cadence/target/store-cargo-home`, and `TMPDIR=/code/cadence/target/store-tmp`. The task-local Cargo home used existing registry source/cache/index material and kept its lock/cache state under the repository; dependencies were not installed or downloaded. Node commands were spawned with stdin set to DEVNULL. TypeScript used the existing local executable, not an installing npx invocation.

Final `git log -8` verified all eight signatures as G with key 693AB15F91734B0C and owner John Crenshaw <john@jcrenshaw.dev>. `git diff --check HEAD~8 HEAD` passed. `git diff --name-only v3.7.12 -- cadence-core` and the untracked-file query for cadence-core returned no paths. Thus the frozen tree remains unchanged. Final status contains only the preserved pre-existing phase 2 reports and this phase 3 report directory; no source changes remain unstaged. The report is intentionally uncommitted, per the sequential executor contract.

## Checkpoint

CHECKPOINT: blocked
Current task: 8 — Verify synchronization precedes acknowledgement
Need: On a Linux host where strace is already available and spawned-child tracing is permitted, run `RUSTC_WRAPPER= cargo test -p cadence --test store_crash ac8_syscall_order`. Require the positive trace to pass and both independently omitted-sync traces to be rejected despite actual success replies. Then run `RUSTC_WRAPPER= cargo test --workspace` to obtain the required green final suite, including doc tests. No implementation task needs to be replayed; resume from the committed Task 8 verification at b71eeb7c. Until then, neither AC8 nor PLAN COMPLETE is claimed.

No prediction was contradicted. The only failed task criterion is the explicitly anticipated, tool-blocked AC8 fixture. AC1-AC4 have passing targeted evidence; config/import/recall composition criteria AC5-AC7 remain with their separate plans. The orchestrator owns phase state and final report staging.
