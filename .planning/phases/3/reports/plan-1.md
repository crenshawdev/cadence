PLAN CHECKPOINT: human-verify
Plan: .planning/phases/3/PLAN-1.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1 - Give the record a fifth lifecycle NAME that funds nothing | 3d2c6ebc | `WORKER_CACHE = 'worker_cache'` exported beside `COORDINATOR`; census `known` list reads it from the module. Fixture render: `counts.lifecycle` 2, `brackets` [], one `unpaired` dispatch, `roles` byte-identical with and without the fact. `test.mjs planning` 1008/1008, `tsc -p tsconfig.ci.json` clean. |
| 2 - Fold a cache-only fact onto its bracket in a post-pass | 1cb059fe | Facts collected in the forward pass off the existing guarded `cache` object, keyed `corr\0agent_id` with last-fact-wins; folded after the loop under the existing fill-only-empty clause, first matching bracket only. Six new cases in `trace.test.mjs`; the `folded()` helper renders every fixture with and without its `worker_cache` lines and asserts `roles` is byte-identical. `node --test trace.test.mjs` 149/149, typecheck clean. |
| 3 - `closeForStop` answers a LIST, and the disk half writes each event | 211cddd6 | Contract is now an array (empty for do-nothing); disk half loops behind an `Array.isArray` guard. Every case in `subagent-trace.test.mjs` reads the list, do-nothing cases assert `[]`. 19/19 green. End to end: a temp `.planning/` with one open `cad-verifier` dispatch, a `SubagentStop` payload piped in, appended exactly one line, a `lifecycle/return`, 0 bytes on stdout and stderr, exit 0. |
| 4 - Scope gate 2b's candidate set to the current run | 8206ce3c | `currentRun()` takes the `corr` of the role's newest dated row and keeps only rows carrying it; undated rows contribute no clock, and a set with no dated row stands untouched. The `twoOpen()` fixture now spells its two workers under ONE `corr`, which is what the parallel path produces and what the case always meant. 23/23 green, typecheck clean. |
| 5 - Write the cache-only fact on all three withholding gates | 6a9ba2a6 | Self-filter moved ahead of the termination gate (the fact needs the mapped role); `alreadyClosed` became `closedBracket`, answering the row that is both gate 2a's test and the fact's identity source. `cacheFact()` writes nothing without an id, a source row, or a figure. `subagent-trace.test.mjs` 32/32. End to end, once per gate: each appended exactly one `worker_cache` line and no `return`, the hook stayed silent at exit 0, the fact reached the right bracket (the sibling in the two-open case carried neither key), and `roles` was byte-identical with and without the fact - all 26 checks green both unfiltered and under `--phase 3`. Full suite 3410/3410, `self-verify --root .` `problems: []`, typecheck clean. |
| 6 - Record what phase 2's shared prefix recovered | 53de25b4 | Dated post-ship section added to `.planning/spikes/agent-prefix-cache-fragmentation/SPIKE.md` for `cad-verifier`: why that role, both commands verbatim, the per-dispatch normalisation, the asymmetry stated as method, the rejected transcripts-on-both-sides comparison, and what a zero would mean. BEFORE measured 2026-08-26 - 54 distinct `cad-verifier` agent ids in the read recorder, 34 transcripts still on disk (20 pruned by the host), 33 predating `8ca0dfdc`: 53,976,294 cache-read tokens (1,635,645 per dispatch) and 2,765,064 creation (83,790 per dispatch). AFTER is PENDING: the live record today carries 406 brackets over 2,378 events with 0 cache keys, 63 of those brackets `cad-verifier`'s, and a bracket figure needs a host firing `SubagentStop` after this change. Full suite 3410/3410, `self-verify` `problems: []`, typecheck clean. |
Deviations: none
Open items:
- Task 6's AFTER figure and the delta are unwritten, by construction. `.planning/spikes/agent-prefix-cache-fragmentation/SPIKE.md` names the exact command and the section holds a PENDING placeholder for it; it needs at least two live `cad-verifier` dispatches on the merged change first, which is the same instrument AC5 waits on.
- Declined a `plan` field on the `worker_cache` fact and a second overwrite rule in the fold: both are named out by the plan (D-11, and the two-open case has no single plan to name), so this is only recorded so a reader who expected them knows they were considered.
- Task 3's and task 5's end-to-end checks ran as scratchpad scripts rather than committed tests, because `subagent-trace.test.mjs` states in its own header that it drives the pure rule with no hook and no filesystem. A committed hook-to-disk-to-render test would need a file of its own; nothing in this plan asked for one.
