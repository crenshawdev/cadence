PLAN COMPLETE
Plan: .planning/phases/3/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1: FILING_TABLE's lookup rows ask for titles carrying the fire's fingerprints | 510fcf9e (already applied) | Verified against the tree, not redone. Both greps as the task predicts: the `DECLINE_LABEL`-filtered lookup count is 0 and `2026-08-25` is gone from `lib/filing-decision.mjs`. `LOOKUP_CHUNK = 6` exported at `:332`. `filing-decision.test.mjs` 62 pass / 0 fail, carrying the three rewritten byte-exact vector tests plus `a github chunk of six is five OR operators...`, `no provider's lookup vector carries DECLINE_LABEL...` and `the chunk constant is six...`. |
| 2: A normalizer beside normalizeDeclines returns fingerprint and issue number | 24b2a28a (already applied) | Verified. `export function normalizeLookup(text, limit, numberKey)` at `lib/filing-decision.mjs:534`, written beside `normalizeDeclines` rather than widening it; each FILING_TABLE row carries its own frozen `numberKey` (`index` forgejo :675, `number` github :703, `iid` gitlab :726). All seven `lookup:` cases present and passing, including the page-fill, whole-read-failure, skipped-token and `state: "closed"` arms. |
| 3: cmdFile looks the fire up before any create, refuses a filled page, suppresses a hit | 4e1dabd1 (already applied) | Verified. `incomplete-lookup` grep 1; the "No forge CLI's stdout is read here at all" header claim is gone. `issue-filing.test.mjs` 61 pass / 0 fail with the eight named cases present (`the list call is TITLE-SCOPED...`, `a CLOSED issue suppresses exactly as an open one does (D-05)`, `a lookup that FILLED ITS PAGE refuses...`, `a lookup child that could NOT RUN does not refuse...`, `a fire of DECLINES alone spawns no child at all`, `the forgejo arm's ISSUE list call carries --login`). `self-verify.test.mjs` 177 pass / 0 fail. |
| 4: The FILED.md grammar learns an unconfirmed row | b2ca62ef (already applied) | Verified. `FILED_ROW` at `lib/planning-files.mjs:1250` carries the optional `( unconfirmed)?` group ahead of the colon with the title still unconstrained; `appendRow` writes the marker only on `r.unconfirmed === true` (`:1429`) so every existing caller is untouched, and `appendDeclinedRow`'s comment states it never sets the flag (`:1409`). All four new cases present, including `a title that itself begins with the word is not misread as a marker`. `planning-files.test.mjs` 336 pass, `planning-recall.test.mjs` 27 pass. |
| 5: An ambiguous create writes an unconfirmed row, and the FILED.md append path skips a fingerprint already present | 922ed1a1 (already applied) | Verified. `unconfirmed` grep 18 in `issue-filing.mjs`; "nothing cheap could" gone. The dedup sits in the FILED.md arm alone (`dedup: true` at `:509` against `dedup: false` for DECLINED.md at `:510`), guarded at `:532`. Cases `a fingerprint FILED.md already carries gets no SECOND row...` and `an UNCONFIRMED write over a CONFIRMED row rewrites that row rather than adding one` both present and passing. |
| 6: A lookup that could not run falls through to FILED.md, and an unconfirmed row suppresses on its own | 75b6a22e (already applied) | Verified. `readFiled` at `issue-filing.mjs:305` with its own `filed-unreadable` and `filed-conflicted` reasons; `= readDeclines(` grep is 1, so no ledger read was added to the file face. Envelope carries `ledger_stood_in`. All five cases present, including `AC4 end to end: an ambiguous create is not re-filed on the next fire` and `an unreadable FILED.md refuses BEFORE any create, like the decline record`. |
| 7: One payload carrying a fingerprint twice spawns one create | e6a6a43a (already applied) | Verified. The collapse sits between `readDispositions` and the ledger read with the D-03/D-08 reasoning stated in the comment at `:707-710`. `a payload carrying the same fingerprint TWICE spawns one create` present and passing. |
| 8: triage-gate.md states the lookup and its budget row rises in the same commit | 9bf0c698 (already applied) | Verified. Both stale claims gone from `references/triage-gate.md`; `unconfirmed` grep 2. `weight.mjs --root .` measures the file at 26804 bytes and `weight-budgets.json:51` reads exactly 26804, confirmed independently by `wc -c`. `self-verify.mjs` exit 0 with zero `budget-overrun` problems. |
| Fix (prior run, post-review): a lookup MISS is authoritative only where the query was measured | b615aa9b (already applied) | Verified. `lookupMeasured` is `true` on the github row alone (`lib/filing-decision.mjs:704`) and `false` on forgejo (`:676`) and gitlab (`:727`); `cmdFile:844` reads it so a completed MISS overrules a confirmed ledger row only on a measured row. Cases `on a forge whose lookup is UNMEASURED, a complete miss cannot overrule a confirmed row` and `an unmeasured forge still honours a lookup HIT...` present and passing. |
| Lease and hygiene | (no commit) | `lease-check --phase 3 --plan 1` -> `ok:true`, 0 staged. The eight files touched across `510fcf9e^..b615aa9b` are all inside the declared lease; no deletions in the range; nothing uncommitted. |
| Final: full suite | (no commit) | `node cadence-core/bin/test.mjs` 3806 pass / 3806 tests / 0 fail, matching the predicted figure exactly since HEAD is unchanged at b615aa9b and this dispatch committed nothing. `npx tsc -p tsconfig.ci.json` exit 0; `node cadence-core/bin/self-verify.mjs` exit 0 with no `budget-overrun`. |
| Static analysis | (no commit) | `detect-commands` answers `lint: null` (no lint command Cadence can find, stated once and skipped) and `typecheck: npx tsc -p tsconfig.ci.json`, which exits 0. |
Deviations: none of my own. The prior run's two are carried forward unchanged and are confirmed true against the tree: [deviation] CONTEXT D-04 and the plan's Notes assert a complete tracker MISS creates "even over a confirmed row", which holds only where the query producing the miss was measured - the forgejo and gitlab lookups space-join their tokens on D-12's flagged assumption, so an empty answer there is not evidence; `lookupMeasured` now scopes D-04's stated cost to github, fixed in b615aa9b rather than overridden. [deviation] tasks 7 and 8 each name `node cadence-core/bin/test.mjs` in their `Verify:` while the contract allows the full suite once per dispatch; each was verified by its targeted run plus `npx tsc`, with the suite at its one permitted site.
Open items: one, carried from the prior run and unchanged. On forgejo and gitlab a fingerprint the tracker holds but `.planning/FILED.md` does not (filed from another checkout, or one of D-04's 9 pre-phase orphans) can still be created twice if the assumed query returns nothing, because nothing local speaks for it. The assumption is written into each of those two rows' comments in `lib/filing-decision.mjs` rather than into a transcript. Measuring `tea --keyword` and `glab --search --in title` against a live instance holding a known fingerprint is what closes it, and flipping that row's `lookupMeasured` to true is the whole change.
