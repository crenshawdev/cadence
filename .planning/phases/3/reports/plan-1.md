PLAN COMPLETE
Plan: .planning/phases/3/PLAN-1.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1 - Level-key the tier grid, add the effort grid, and return both from resolve | 5e4b883 | `tiers` re-keyed on (level, trigger); new dense `efforts` grid plus an `effort_names` vocabulary array beside `tier_names`; envelope gains `reviewer_tiers` and `reviewer_efforts` beside `reviewers`. Ladder: critical flagship/high, shipped balanced/medium, solo cheap/low for `plan` and `risk_surface`; `diff` and `phase_diff` monotone from their old schema values, holding at `cheap`/`minimal`. 10 new/updated route.test.mjs cases WATCHED FAILING AT 478b1ff (observed 10 fail / 96 pass on that tree). |
| 2 - Validate both grids in both directions | c968b00 | `cellIssues` walks `tiers` and `efforts` per level through one shared spec; new `unknown-effort` code; every detail locates `<grid>/<level>/<trigger>`; absent block and absent vocabulary array each stay ONE problem. 20 route-cells cases + 6 self-verify check-8 cases WATCHED FAILING AT 478b1ff. |
| 3 - Move the tier and effort schema defaults onto the unset sentinel | b3a5515 | All 8 `.tier`/`.effort` defaults now `null`; purposes carry per-level ladders and keep `cross-model reviewers only` verbatim (check 9 green). `GATE_KEY` widened to `LEVEL_KEY` matching gate/tier/effort by shape, with a per-field noun in the warning. 2 of 4 new config.test.mjs cases WATCHED FAILING AT 478b1ff. |
| 4 - Rewrite the tier and effort rows in the reach table and the catalog | 71b238b | 8 reach rows now name `route.mjs resolve` and the `reviewer_tiers`/`reviewer_efforts` handoff; both catalog Default cells moved to the unset-decides form. Budgets re-pinned per D-17: config-reach.md 19521->20701, config-catalog.md 9257->9343. |
| 5 - review-triggers.md takes both fields off the resolve line | a41f64c | Step 1 names `reviewer_tiers`/`reviewer_efforts` and the degraded-resolve fallback covers all three fields; step 4's cross-model arm indexes the provider map with the RESOLVED tier; "which fields reach which backend" corrected; the subagent arm's mismatch line now names the resolved effort. Budget re-pinned 30644->31419. |
| 6 - Settle the grid count everywhere it is stated | 25fd067, 9d47e87 | INTERNALS.md:17 states five grids and names each; `route.mjs`'s torn-table comment re-scoped and says why `tiers`/`efforts` are deliberately outside that guard; ledger rows INTERNALS-42, CONFIG-CATALOG-09 and CONFIG-CATALOG-10 re-verdicted `stale` with `corrected - RVW-03 (v3.5.4)` resolutions plus a narrative paragraph explaining the sha-less form. Second commit closes the plan's "no prose anywhere" bullet: the 9 `review.providers.*.tiers.*` reach rows stopped naming `tiers[trigger.tier]`; config-reach.md re-pinned 20701->21025. |
Deviations: none
Open items: `route.mjs` does not check a config-SET `review.triggers.<t>.tier` or `.effort` against the table's `tier_names`/`effort_names` before letting it win, the way it checks a config-set gate against `gates`. That asymmetry predates this plan (the tier arm always behaved this way) and nothing in the plan's `Verify:` or `## Must be true when done` asks for it; an out-of-vocabulary tier still surfaces as a dropped provider with its cause in `warnings[]`, and an out-of-vocabulary effort would reach `review-provider.mjs --effort`, which validates its own flag. Worth a task if a later phase wants the gate's refuse-then-warn shape on all three fields.
Open items: no drift check ties the table's `effort_names` to `config.schema.json`'s effort enum. `tier_names` has never had one either, and `cellIssues` reads the table's own array exactly as it does for tiers, which met the task's `Verify:`; a `vocabularyIssues` comparison in the shape of `gate-vocabulary-drift` is the fuller form if a later task states it.
