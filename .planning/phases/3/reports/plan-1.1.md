PLAN COMPLETE
Plan: .planning/phases/3/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1: FILING_TABLE's lookup rows ask for titles carrying the fire's fingerprints | 510fcf9e | 54 tests pass (3 new); both greps as predicted; tsc 0. Signature is `lookup(slug, limit, fingerprints, login)` - login stays last, matching `create`. Exported `LOOKUP_CHUNK = 6`. |
| 2: A normalizer beside normalizeDeclines returns fingerprint and issue number | 24b2a28a | 61 tests pass (7 new); `git diff -U0 \| grep '^-[^-]'` on the test file printed nothing, so no existing case was edited; tsc 0. `normalizeLookup(text, limit, numberKey)` -> `{complete, records:[{fingerprint, number}], detail}`; each FILING_TABLE row states its own `numberKey`. |
| 3: cmdFile looks the fire up before any create, refuses a filled page, suppresses a hit | 4e1dabd1 | 50 tests pass (8 new); self-verify.test.mjs 177 pass (check 12 unchanged); `incomplete-lookup` grep 1; the "No forge CLI's stdout is read here at all" claim is gone; self-verify.mjs exit 0; tsc 0. Envelope gains `suppressed[]` and `suppressed_count`; `LOOKUP_TIMEOUT_MS` restored at 30000. |
| 4: The FILED.md grammar learns an unconfirmed row | b2ca62ef | planning-files 336 pass (4 new, 0 existing lines removed); planning-recall 27 pass; tsc 0. The parser reports `unconfirmed: true` only on a marked row - absent means confirmed on both sides - which is what keeps the existing round-trip `deepEqual` cases passing untouched. |
| 5: An ambiguous create writes an unconfirmed row, and the append path skips a fingerprint already present | 922ed1a1 | 52 tests pass (2 new, 1 rewritten); `unconfirmed` grep 12; "nothing cheap could" gone; tsc 0. The existing case `a create that fails part way still mirrors the issues that DID land` asserted the ambiguous entry gets NO row, which is exactly what this task reverses, so it was rewritten in place rather than left red. `markUnconfirmed` finds its line by parsing it with `parseFiledRows`, so no second copy of the row grammar exists. |
| 6: A lookup that could not run falls through to FILED.md, and an unconfirmed row suppresses on its own | 75b6a22e | 57 tests pass (5 new); `= readDeclines(` grep 1; tsc 0; self-verify 0 problems. New reason tokens `filed-unreadable` and `filed-conflicted`; envelope gains `ledger_stood_in`; each suppressed entry names `authority` (`tracker` or `FILED.md`), `issue` and `filed_on`. `readFiled` runs before `resolveForge`, so its refusal precedes every child. |
| 7: One payload carrying a fingerprint twice spawns one create | e6a6a43a | 59 tests pass (2 new); tsc 0. Entries collapse by fingerprint between `readDispositions` and the ledger read, first occurrence wins. Full suite deferred to its one allowed site - see the deviation below. |
| 8: triage-gate.md states the lookup and its budget row rises in the same commit | 9bf0c698 | self-verify.mjs exit 0 with no `budget-overrun`; both stale claims gone; `unconfirmed` grep 2; budget row re-pinned 25593 -> 26804, the figure `weight.mjs --root .` measured. |
| Final: full suite | (no commit) | `node cadence-core/bin/test.mjs` 3803 pass / 0 fail; `npx tsc -p tsconfig.ci.json` exit 0; `node cadence-core/bin/self-verify.mjs` exit 0. |
| Fix (review): a lookup MISS is authoritative only where the query was measured | b615aa9b | filing-decision 62 pass (1 new); issue-filing 61 pass (2 new); tsc 0. `FILING_TABLE` gains `lookupMeasured` (true on `github` alone, false on `forgejo` and `gitlab`) and `cmdFile`'s precedence rule 2 reads it: a completed MISS is final only on a measured row, so a confirmed `.planning/FILED.md` row keeps suppressing on the two assumed rows and sets `ledger_stood_in`. A HIT still suppresses everywhere and rules 1 and 3 are untouched. Falsified by flipping the forgejo flag to true and re-running: the new case fails 2 creates against 1, so the gate is load-bearing rather than decorative. Suite after the fix: `node cadence-core/bin/test.mjs` 3806 pass / 0 fail, exit 0 (3803 + the 3 cases added here, the predicted figure); `npx tsc -p tsconfig.ci.json` exit 0; `node cadence-core/bin/self-verify.mjs` exit 0. No lint command exists - `planning.mjs detect-commands` answers `lint: null`. |
Deviations: [deviation] the plan's Notes and CONTEXT D-04 state that a complete tracker MISS creates "even over a confirmed row", and the code shipped that on all three providers. That is only true where the query producing the miss was measured: the forgejo and gitlab lookups space-join their tokens on D-12's flagged assumption, so an empty answer there is not evidence and was overriding the ledger row that used to suppress the duplicate on its own - the phase made duplicate filing more likely on those two than before it landed. D-04's stated cost now holds on `github` only; on the other two a confirmed row wins over a miss. Raised `high` three times across the `diff` and `risk_surface` reviews and fixed rather than overridden. [deviation] tasks 7 and 8 each name `node cadence-core/bin/test.mjs` in their `Verify:`, and the executor contract allows the full suite exactly once per dispatch, never between tasks. Each of those tasks was verified with its targeted `node --test` run plus `npx tsc`, and the suite ran once at the end of the dispatch, green - so the criterion is met at the one permitted site rather than dropped or run three times.
Open items: the plan recorded as not-applied that an empty response from the tea or glab arm is read as a complete MISS, so duplicate protection on those two forges rides CONTEXT D-12's flagged assumption (whether `tea --keyword` and `glab --search --in title` match a bracketed hex token in a title, and whether space-joined tokens read as more than one term). The assumption is written into each of those two rows' comments in `lib/filing-decision.mjs` rather than into a transcript. That finding is now FIXED for the case the ledger can speak for (commit b615aa9b): a confirmed row is no longer overruled by an unmeasured miss. The RESIDUAL is narrower and still open - on forgejo and gitlab a fingerprint the tracker holds but `.planning/FILED.md` does not (filed from another checkout, or one of the pre-phase rows) is still created twice if the assumed query returns nothing, because nothing local speaks for it. Measuring `tea --keyword` and `glab --search --in title` against a live instance holding a known fingerprint is what closes it, and flipping that row's `lookupMeasured` to true is the whole change.
