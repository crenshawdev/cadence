PLAN CHECKPOINT: human-verify
Plan: .planning/phases/3/PLAN-1.md
Tasks: 7 of 8
| Task | Commit | Note |
|---|---|---|
| 1: The set that reaches the ask, taken off the structured payload | 6c539751 | `lib/filing-decision.mjs` created; `unfixedFindings` selects off `ruling` + raised `severity` via `buildEntries`, `fingerprint` is sha256 over `(file, claim)` NUL-joined, 16 hex chars. 15 arms pass. |
| 2: One pinned argument vector per forge, for the create and the lookup | d65604b6 | `FILING_TABLE` (3 rows), `DECLINE_LABEL = 'cadence-declined'`, `issueTitle`/`issueBody`/`fingerprintInTitle`, and a `normalizeDeclines` written here rather than imported. Flags re-measured live against gh 2.98.0, glab 1.114.0, tea 0.15.1 before writing the table. 34 arms pass. |
| 3: The seam that asks the tracker once and writes what it is told | 99ca9e4b | `bin/issue-filing.mjs` with `unfixed` and `file`; ONE `mergeLayers` callsite shared by both faces. CONTRACTS row added (185->188 entries, 18->19 rows, re-pinned), merge-layers census re-pinned 16/12 -> 17/13 with the registry row's subjects extended in the same commit. 24 seam arms pass; self-verify 0 problems; arg-contract, adoption, census-registry and self-verify.test all green. |
| 4: A filed finding stays reachable by recall | 2e70b0d1 | `parseFiledRows`/`appendFiledRow` beside the ARCHIVE.md pair in `lib/planning-files.mjs`; recall walks `FILED.md` LAST; `file` mirrors ACCEPTED entries only through `withPlanningFileLock` + `atomicWrite`. 30 seam arms, 324 planning-files arms, 26 recall arms; the four named test files = 501 arms green, neither planning census moved. |
| (fix) the no-forge hint names a setup step that exists | 922d9353 | Task 3's `no-forge` hint named `/cad-setup`, which is not a command in this tree. Corrected to `/cad-new-project` and `/cad-adopt`, the two workflows that carry the forge setup step. |
| 5: The gate asks once, in the step that decided | 5ffb9040 | New `## What happens to a finding this fire will not fix` section in `references/triage-gate.md`, with the blocking and adjudicated arms pointing at it; one `unfixed` call and one `file` call per fire, the two caps deferred not restated, resolution-by-removal stated with no command named. Budget re-pinned 18030 -> 21328 in the same commit. self-verify 0 problems; prose-agreement + seam-calls 50 arms green; `grep -n "per finding"` returns one line and it forbids the shape rather than instructing it. |
| 6: The tree stops claiming glab is absent | 33099d75 | Both comments corrected honestly rather than deleted: the header records `glab` present at `/usr/bin/glab` 1.114.0 measured 2026-08-25 and states that the PATH-injected-stub discipline was never a consequence of absence; `HOST_TABLE`'s header now gives paging-to-`--per-page` as the reason the gitlab row carries no `resolve`. `grep` for both false phrases returns nothing; `git diff` is 15 comment lines in, 4 out, no argv/limit/export touched; issue-decision + issue-check 62 arms green. |
| 7: The refusal vocabulary is pinned forward | 57f71bd2 | Amended per the user's decision on the prior run's structural checkpoint - see the deviation below. `reason-census.test.mjs` ships the 82-token sorted list, derived by the stated method and asserted FORWARD by containment, with NO `CADENCE-CENSUS` marker and NO `CENSUSES` row; the prior run's uncommitted `lib/census-registry.mjs` addition was reverted before this commit and the file is untouched at HEAD. 5 arms pass. Whole suite `node cadence-core/bin/test.mjs` 3174/3174 green; `npx tsc -p tsconfig.ci.json` exit 0 (`detect-commands` reports `lint: null`, so typecheck is the whole static-analysis answer here). |
| 8: The write proved against all three forges, live | - | CHECKPOINT: human-verify. Nothing committed. See below. |

Deviations:
- [deviation] Task 7 asserted the refusal-token list ships as a REGISTERED census - a `CADENCE-CENSUS` marker plus a `CENSUSES` row with subjects `cadence-core/bin/` - and its `Verify:` required `census-registry.test.mjs` to pass with that row. What is actually true, re-measured on this tree at 57f71bd2's parent by replaying the shipped predicate: `cadence-core/bin/planning-lease-check.test.mjs:700` bounds any registry entry at half of this repository's own plans; 45 plans declare a path under `cadence-core/bin/`, so the line is 22.5; subjects `cadence-core/bin/` refuse 44 of the 45, and the narrowest honest subject set - the 40 files carrying a literal refusal token today - still refuses 26. No honest subject set clears the bound, and that rail's own message says a rail that fires wrong is deleted rather than tuned. What I did, per the user's decision carried in this dispatch: dropped the marker and the row, kept the forward-assertion test, reverted the prior run's uncommitted `lib/census-registry.mjs` addition, dropped the `census-registry.test.mjs` clause from the Verify, and stated in the test's own header WHY it is not a registered census - the assertion is one-directional, so no plan can invalidate it, which makes a plan-time refusal a false alarm on 26 of 45 plans. The rest of task 7's `Verify` was met in full (see below). The plan's Notes paragraph "The reason-token census will be felt by later plans" is now false as written and is superseded by this deviation; PLAN-2 inherits no lease tax from it.
- [deviation] Task 7 asks for the derived count in the test's header. The prior run's header recorded 256 sites / 99 distinct / 82 literal / 17 expressions; re-derived on the live tree it is 266 / 104 / 82 / 22 across 46 files. The gap is exactly this phase's own `bin/issue-filing.mjs` - 10 sites, 5 new distinct tokens, all 5 in the expression class - so the prior figure was taken on a tree without it. Corrected in the header, with the 256/99/17 reading named there as what a pre-`issue-filing.mjs` tree answers, because a number that cannot be re-derived is the defect this census exists to refuse.

Task 7 Verify, as run:
- `node --test cadence-core/bin/reason-census.test.mjs` - 5 arms, 5 pass, 0 fail, on the live tree.
- Deleting `'bad-json'` from the committed list: 5 pass, 0 fail. Reverted, byte-identical.
- Renaming `reason: 'no-diff'` to `'no-diff-RENAMED'` in `planning/risk-check.mjs` (its only site): FAILS, the message reading `1 refusal token(s) the tree no longer produces ... no-diff`. Reverted, `git diff --stat` empty. Predicted ONE failing arm and observed TWO - the ONE-DIRECTIONAL arm re-asserts containment over the same list minus its head, so a rename reddens it as well. Not a defect and not a criterion miss: the Verify asks that a rename FAIL naming the token, which it does, and the second arm is the same claim re-stated over a shortened list.
- `node cadence-core/bin/test.mjs` - 3174 tests, 3174 pass, 0 fail. `reason-census.test.mjs` is picked up by the runner's directory scan into the `other` group, so no `GROUPS` edit was needed.
- `npx tsc -p tsconfig.ci.json` - exit 0.
- `lease-check --phase 3 --plan 1` - `ok:true`, 1 staged, 20 declared.

Open items:
- `.planning/FILED.md` is declared in the plan's lease and is deliberately NOT created in this repository. The seam creates it with its own preamble on the first accepted filing, the way `milestone-prune` creates `.planning/ARCHIVE.md` on the first close; committing an empty one ahead of that would be an artifact making no claim.
- Several of this phase's new refusal tokens (`no-forge`, `no-cli`, `no-login`, `lookup-failed`, `incomplete-lookup`) are set in `issue-filing.mjs` helper RETURNS and emitted through an interpolated `reason`, so `refusalSites` does not see them as sites (its own D-02 defines a site as the EMITTING call) and the token census cannot watch their spelling. Their hints ARE watched by self-verify check 22 at the emitting sites. Stated in the new test's header rather than left to be discovered.
- Six `jcrenshaw` hits survive outside `*.test.mjs` and `bin/fixtures/` - `lib/redact-url.mjs:66`, `lib/forge-decision.mjs:139,140,227,228`, `self-verify.mjs:516`. All six are comment lines, all predate this phase, and none is a literal any code reads. Recorded in the task-8 file as the pre-run baseline so the post-run check measures growth rather than presence.

Task 8, and why it stops here:
Task 8's `Verify:` is `human-verify` in the plan, and its work is outside an executor's reach on all three arms: it needs `tea`, `gh` and `glab` authenticated against three scratch repositories the OPERATOR owns, it creates real issues on real remote trackers, and one of the three runs must be driven by a real gate fire rather than by the two subcommands. Nothing was committed for it.

Prepared, UNCOMMITTED, at `.planning/phases/3/live-forge-check.md` (8,244 B): the procedure, the preconditions, the exact create and lookup argv each `FILING_TABLE` row will run at this commit, a reproducible five-finding payload generator, and empty result fields per provider. Verified against `lib/filing-decision.mjs` while writing it: that payload selects all five findings through `unfixedFindings`, and the two declined ones fingerprint to `1e20a4c96da309d2` and `de3dd594e6372bfc`, which are the two tokens the second `unfixed` must not return. The file's own first line says results are pending; it is left uncommitted deliberately, because a transcript that claims a run nobody made is the failure this task exists to prevent.
