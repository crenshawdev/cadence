PLAN COMPLETE
Plan: .planning/phases/3/PLAN-2.md
Tasks: 3 of 3
| Task | Commit | Note |
|---|---|---|
| 1. The `## Phases` scanners see fences | 74c8dcc | `parseRoadmapPhases` and `classifyPhaseList` both locate the section through `sectionSpan`; canonical walk and classification walk each skip fenced lines with a scanner started fresh at the heading. Measured before/after against the real functions: shipped `templates/ROADMAP.md` `live` + 2 phantom `[Name]` phases -> `no-section`; fenced-example-above-real fixture `live` + `[[1,"[Name]"]]` -> `live` + `[[1,"Auth"],[2,"Ship"]]`. 3 regression tests added; 253 pass in `planning-files.test.mjs`; typecheck clean. |
| 2. The `## Active` scanner sees fences | 47eab72 | `classifyActiveSection` takes both bounds from `sectionSpan` and skips fenced body lines; `elsewhere` reuses the same `end`. `parseActiveIds` untouched, still the one-line delegate. Measured before/after: shipped `templates/REQUIREMENTS.md` `["[CAT]-01","[CAT]-02"]` + 3 `active-non-id-bullet` -> `{ids:null,issues:[]}` and `parseActiveIds` -> `null`; fenced-example-above-real fixture `["[CAT]-01"]` -> `["REV-01","GRM-02"]`; a fenced `## ` inside the section no longer ends it early (pre-fix dropped `GRM-02`). 3 regression tests added, asserted through both entry points; 607 pass across the two files; typecheck clean. |
| 3. `detect-commands`/`detect-surfaces` refuse a blank `--root` | 79346fc | Both dispatch rows take `debt-harvest`'s exact predicate (trim clause included) and its string-form root argument. All four spellings now print `{"ok":false,"reason":"bad-args",...}` byte-matching `debt-harvest`'s shape; pre-fix `--root ""` answered `ok:true` about the cwd and `--root "   "` answered `no-root`. 6 refusal rows (2 commands x 3 shapes, the pre-existing valueless row folded into the table) plus 2 real-root controls asserting the echoed `root`; 358 pass in `planning.test.mjs`. AC5 gate on this tree: `node --test cadence-core/bin/*.test.mjs` 1723 pass / 0 fail, `node cadence-core/bin/self-verify.mjs` `{"ok":true,...,"problems":[]}` - no `unbudgeted-surface`, no `budget-overrun`. |
Deviations: none
Open items:
- `detect-commands` reports `lint: null` for this repo, so there is no lint command Cadence can find; typecheck (`npx tsc -p tsconfig.ci.json`) was the whole static-analysis step, run after every task and clean each time.
- Declined a `sectionSpan`-shaped rewrite of the roadmap WRITE paths (`setPhaseBox`, `cutPhaseDetail`, `cmdRenumber`'s list filter), which stay fence-blind: D-02 puts them out of scope and no `Verify:` in this plan exercises them, so the reader/writer divergence is deliberate and now costs exactly one call site to close.
- Accepted widening recorded in the code: `sectionSpan` matches a heading by trimmed equality where `/^## Phases\s*$/m` allowed trailing whitespace only, so an indented `## Phases` / `## Active` heading now matches. CommonMark reads up to three spaces of indent as a heading; beyond three it would be an indented code block, and the span helper does not distinguish. No shipped grammar document forbids either.
- AC5 was run on THIS worktree only. PLAN-1's changes are not in this tree, so whichever lands second still owes the integrated run.
