PLAN COMPLETE
Plan: .planning/phases/3/PLAN-2.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1 - what the walked queue actually holds, as a pure reading | e4336f55 | `lib/capture-health.mjs`: `captureHealth(text)` returns `{substantive, sections[], annotations[], archive{present,bullets}}`. Sections located with the exported `sectionSpan` (fence-aware at both ends); the in-section line test is the walk's own `^-\s+`. 8 fixture tests green, all by fixture (live tree has no annotation and no `## Archive`). tsc exit 0; `detect-commands` reports `lint: null` for this project, so no lint command exists to run. |
| 2 - one command the health walk and the close both read | eae3a002 | `planning/capture-check.mjs` + dispatch wiring + a one-word `capture-check` CONTRACTS row (`--file` only; no flag names the bound, which is config). Every verdict rides `ok:true`; the only refusals are bad args and a present-but-unreadable file. Censuses re-pinned in the same commit: arg-contract flag entries 188 -> 189, planning-detail-sites 14 -> 15 (the trace-refusal-sentences census did not move - it counts four flag sentences this command adds none of). 5 command tests green; self-verify problems []; tsc exit 0. Smoke on this tree: 30 substantive (Todos 21, Seeds 9, Notes 0), no annotations, no archive - the plan's measured figures exactly. |
| 3 - a configured bound that fails loud and refuses nothing | a6b5675d | `planning.max_capture_bullets` (int, min 1, default 40) with its one reader in the same commit; reach row (`capture-check report only`, verbatim in the key's `purpose`) and catalog row added. Envelope gains `bound` and `over_bound`; a torn config layer rides `warnings`. Re-pinned in the same commit: merge-layers census 17/13 -> 18/14 plus the registry subject, the every-key coverage fixture in self-verify.test.mjs, and both reference budgets (config-catalog 11464 -> 11863, config-reach 22525 -> 22998). Fixture PAIR green: 3 bullets over a bound of 2 reports the crossing at exit 0, the same tree at 4 does not. Full suite 3193 pass / 0 fail; self-verify problems []; tsc exit 0. |
| 4 - the close asserts the queue is empty | 87b157d5 | `cmdPhaseDone` reads `<dir>/CAPTURE.md` BEFORE the transition and carries a new `capture` field on the close arm only: `{substantive, items[]}` when read or absent, `{unread: true}` when present-but-unopenable. `--undo` omits it. `roadmap`, `reqs` and `wrote` are untouched. `captureHealth` grew an `items[]` (section, line, text) so the close can NAME each leftover rather than count it - the plan's `Verify` asks phase-done to name them and the task-1 reading is where the bullet grammar lives. `workflows/verify.md` step 1 now prints each item and says resolving means removing, never annotating (budget re-pinned 17622 -> 18281). 5 new phase-done cases; full suite 3199 pass / 0 fail; self-verify problems []; tsc exit 0. |
| 5 - /cad-health prints the verdict | b4c857b2 | The capture step now makes both calls: `capture-sections` stays the lower NAMED NOTE and `capture-check`'s three readings are ISSUES (count against bound with the crossing named, every annotation site with section and line, the `## Archive` heading with its bullet count). The sentence calling archiving inside CAPTURE.md intended is gone, replaced by `## Debt markers` as the reason an out-of-walk section is not by itself an error; the five-lost-bullets incident and the no-allowlist rule are kept verbatim. Budget re-pinned 7827 -> 8865. Verify by execution of what the step prescribes on this repo: substantive 30, bound 40, `over_bound:false`, `annotations:[]`, `archive.present:false`, one out-of-walk note (Debt markers, 1). self-verify problems [] (it validates the new invocation against the CONTRACTS row); weight + surface-scan 52 pass. |
| 6 - `## Archive` leaves the capture contract | 0169ef62 | Struck at all three sites: the sections-table row is gone, the D-03 paragraph now covers `## Debt markers` alone (with its own reason - written wholesale by `debt-harvest`, not a queue), and the `Not in this grammar` bullet no longer calls retirement into `## Archive` a milestone-close judgment. Replaced by statements: `## Archive` is NOT part of this file and is REPORTED by `capture-check`; the resolved-by-removal RULE points at `references/triage-gate.md` and this file carries only the grammar half (the `KEPT <date>` / `recorded not fixed` shapes). Also corrected two now-false claims in the same surface: the header said two modules implement the grammar (three), and the `- None.` bullet did not mention that the substantive count excludes it. `.planning/ARCHIVE.md` untouched. `grep -n "## Archive"` returns one line, the NOT-part-of-this-file statement. Budget re-pinned 6358 -> 7639; whole suite 3199 pass / 0 fail; self-verify problems []; tsc exit 0. |
Deviations: none
Open items: none

Gates after the last commit: `node cadence-core/bin/test.mjs` 3199 pass / 0 fail; `npx tsc -p tsconfig.ci.json` exit 0; `node cadence-core/bin/self-verify.mjs` problems []. `detect-commands` reports `lint: null` for this project, so there is no lint command to run - typecheck ran before every commit. `lease-check --phase 3 --plan 2` returned ok:true on all six staged sets.
