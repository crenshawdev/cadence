PLAN COMPLETE
Plan: .planning/phases/3/PLAN-2.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1 - the canonical schema carries the bounds | 174fd38 | `FINDING_SCHEMA` gains `minimum:1` on `line`, `minLength:1` + `maxLength` on the three strings (file 1024, claim/failure_scenario 2000) and `maxItems:100`; no `minItems`. Now exported so tests read the live schema, not a copy. New test asserts all three wire dialects carry the keywords, that none of OpenAI's unsupported set rode in, and that `stripAdditionalProperties` still removes only `additionalProperties`. 65 tests pass, tsc clean. |
| 2 - validateFindings refuses by name | 21f7844 | Seven distinct diagnostics naming field and bound; unknown keys refused at BOTH levels; lengths in Unicode code points. Existing five diagnostics and first-defect order intact, empty `findings: []` still accepted. PLAN-1's `bodyOfBytes` fixture repadded (see deviation). 66 tests pass, tsc clean. |
| 3 - keyword-limited schema evaluator | 5f4b044 | New `cadence-core/bin/lib/schema-eval.mjs`: 10 keywords, code-point lengths, throws on any unimplemented keyword across the WHOLE schema tree (not only branches a value reaches) and on `additionalProperties` other than `false`. 67 tests pass, tsc and self-verify clean. |
| 4 - the agreement table | b212a8d | 18 fixtures, each run through the evaluator against the live `FINDING_SCHEMA` and through `validateFindings`, verdicts compared; 2 accept cases, 2 non-BMP rows. Watched failing both ways: deleting `minimum` from the schema gives "line: 0: FINDING_SCHEMA says ACCEPT but validateFindings says REJECT (finding.line must be at least 1, got 0)", deleting the validator's mirror gives the same fixture inverted. Both restored. 68 tests pass, tsc clean. |
| 5 - provider-api.md records the wire facts | d8bf428 | D-07's research recorded in `## Notes on record` with its 2026-08-17 date: OpenAI's full unsupported set, Gemini's documented bounds, DeepSeek enforcing nothing server-side. `grep -c maxItems` = 2. Budget re-pinned 5048 to 6304 after watching self-verify report `budget-overrun ... exceeds budget 5048B by 1256B`. self-verify ok:true, tsc clean. |
| 6 - the watched FAIL for RVP-02 | cdf8676 | Falsifier drives three schema-refused answers (zero line, empty claim, undeclared key) through `__runCommandForTests` and the fault fixture. Re-watched at 15b5d4c in a detached worktree with this file + `lib/schema-eval.mjs` copied in: exit 1, all three resolved `ok:true` there; header quotes the observed output and the recipe. Full suite 2106 pass, self-verify ok:true, tsc clean. |
Deviations: [deviation] Task 2's Verify assumed the existing suite was unaffected by the new bounds; observed `bound: a body one byte under the ceiling still resolves exactly as before` FAILING. PLAN-1's `bodyOfBytes` helper builds its 4 MiB fixture by padding the finding's `claim`, which the new `maxLength: 2000` refuses, so the fixture degraded to `bad-shape` and stopped exercising the response ceiling at all. Moved the filler to a sibling key of `output_text` (which `extractText` ignores), leaving the wire byte count identical and the finding inside every bound; the ceiling test asserts what it always meant to.
Open items:
- D-09, for the phase SUMMARY: `CONSULT_SCHEMA` and `validateConsult` carry the IDENTICAL gap in identical code shape and were deliberately out of scope. Mirroring them is a mechanical copy of this plan's shape - four constraint keywords on the schema, four named diagnostics in the validator, the fixtures added to the same agreement table (`evaluateSchema` needs no change to evaluate it).
- `FINDING_SCHEMA` is now a public export of `review-provider.mjs`, used only by the test file. The Action named it as an existing symbol and the exporting is what lets task 4 evaluate the LIVE schema rather than a copy; the test reaches it through a namespace import so the falsifier still links against the unpatched tree.
- `lib/schema-eval.mjs` is test-only but lives under `bin/lib/` so `tsconfig.ci.json` checks it (the plan's Notes settle this). It carries no CONTRACTS row and no weight budget: self-verify's `uncontracted-script` check skips directories, and the weight budgets cover only `agents/`, `references/`, `templates/` and `workflows/`.
