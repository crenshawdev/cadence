PLAN COMPLETE
Plan: .planning/phases/3/PLAN-2.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: The tasks tier of the commit index | b680a372 | `buildTaskIndex` + a `TaskDir` typedef beside `buildCommitIndex` in `lib/why-corpus.mjs`, enumerating through `taskRecordsIn` from `lib/task-record.mjs` (no second walk of `tasks/`) and reading through the existing `readArtifact` + `parseCommitRows`. Descriptor is `{label: 'tasks/<slug>', path: <the directory>, group: 'tasks', phase: null, milestone: null, slug}` - `null` rather than a placeholder so nothing can print it as a phase number. Module header amended: the tier list gains the off-roadmap tier and "ONE ENUMERATOR" becomes "ONE ENUMERATOR PER TIER", since `phaseDirsIn` is no longer the only one. Verify met: 5 new cases (one record -> one dir, its two rows in table order, each carrying the slug, and a task dir with a PLAN and no record excluded; absent `tasks/` and absent root both `{dirs: [], rows: [], warnings: []}`; a `RECORD.md` symlinked out of its own directory yields exactly one `EESCAPE` warning naming the artifact and no rows, with the refused bytes absent from the warning; two builds deep-equal and in slug order; this repository's own tier holds `tasks/bound-plan-size` and the `093408c9` row). `why-corpus.test.mjs` 37/37. Static analysis: `npx tsc -p tsconfig.ci.json` exit 0 (no lint command configured; `detect-commands` reports `lint: null`). `lease-check` `ok:true` (staged 2, declared 6). Prediction note: I predicted "31 tests -> 35"; observed 32 -> 37, because I miscounted the pre-change total and wrote five cases rather than four. The four properties the Verify names are each covered. |
| 2: The tier is merged, ordered, and asked | 335a590e | `mergeCommitIndexes(disk, tasks, recovered)` - three ordered tiers, `prunes` still off the recovered argument alone, and the function comment now states the order's reason (the phase spine is the authority when both name a commit; the tasks tier goes ahead of the recovered one because both on-disk tiers are records a reader can OPEN). `why.mjs` builds all three indexes at its one call site, its "THREE TIERS, NOT TWO" header paragraph became "FOUR TIERS" plus a new paragraph naming the cost (one directory listing plus one read per task record), and `brief()` carries the `slug` marker onto the join so the renderer never has to parse a label. `mergedHere()` in the test file updated in the same change. `resolveCommit` untouched. Verify met: a sha in BOTH a phase SUMMARY and a task record resolves to `phases/1` (and the tier list reads `[['phases/1'], ['tasks/both-name-it'], []]`), a sha only the record names resolves to `tasks/both-name-it` with state `resolved`; the tasks tier beats a synthetic recovered tier claiming the same sha while `prunes` stays the recovered tier's; over this repository `093408c9` resolves to `tasks/bound-plan-size` with `phase: null` while `00537356` still resolves to `_archive-v3.4.0/1`; and at the SEAM a built repo whose only planning artifact is a task record returns `join.state: 'resolved'` with `slug`, `phase: null`, `task: '1'`, `plan: ''` and no `NOT RESOLVED`. `why-corpus.test.mjs` 40/40, `why.test.mjs` 35/35. Static analysis: `npx tsc -p tsconfig.ci.json` exit 0. `lease-check` `ok:true` (staged 4, declared 6). |
| (repair of task 2) | b6c6ebf5 | An apostrophe I typed into a single-quoted assertion message in `why-corpus.test.mjs` after that file's last test run left `335a590e` carrying a `SyntaxError` - the suite loads the file and 0 of 40 cases ran. Caught by re-running the siblings during task 3, fixed by rewording the message, `why-corpus.test.mjs` back to 40/40. Recorded here rather than folded into a task row because it is a defect I shipped, not part of any task's work. |
| 3: A task entry renders as a task, never as a phase | b871829c | `fieldPhase` gained a `j.slug` arm placed AHEAD of the recovered and resolved-phase arms, rendering `off-roadmap task <slug> - a /cad-task run, not a roadmap phase (tasks/<slug>)`; `JoinMatch` gained the optional `slug`; `fieldTask` gained a comment stating why it needs NO arm (a record has no Plan column, so `j.plan` is `''` and the existing three-column rule already drops the prefix) and no code change. Verify met: the task line is byte-exact and matches neither `phase <digit>`, nor a `vN.N.N` milestone label, nor the string `null`; the `plan task:` line reads `plan task: task 1 - ...` with no `plan ` prefix; and four unmoved-arm cases pin the resolved-phase (`v3.4.0 phase 1 (_archive-v3.4.0/1)`), recovered (`recovered from d3313bf7:.planning/phases/1`), ambiguous (both candidates named, none picked) and gap (`NOT RESOLVED - `) renderings. `why-render.test.mjs` 11/11, `why-corpus.test.mjs` 40/40, `why.test.mjs` 35/35. Static analysis: `npx tsc -p tsconfig.ci.json` exit 0. `lease-check` `ok:true` (staged 2, declared 6). |
| 4: The record's own declaration reaches the join, end to end | 72aeb59c | `readPhaseRecords` tries `RECORD.md` FIRST when `dir.slug` is set, ahead of the `PLAN-<key>.md`/`PLAN.md` spellings, with the reason stated in its docblock (a task's PLAN.md declares what the run set out to touch, not what it did, and the inline arm writes none at all); `CONTEXT.md` and `SUMMARY.md` untouched. Verify matched prediction exactly: `node cadence-core/bin/why.mjs cadence-core/templates/config.json` renders `093408c9` as `phase: off-roadmap task bound-plan-size - a /cad-task run, not a roadmap phase (tasks/bound-plan-size)`, `plan task: task 1 - feat(config): add workflow.max_plan_tasks, the plan-size ceiling`, `declared by: declared in RECORD.md` with `task 1: bound-plan-size (declares cadence-core/templates/config.json)`, no `NOT RESOLVED` block and `warnings: []` - where the pre-change tree printed the gap block for that commit; two runs byte-identical. New cases: a task directory holding BOTH a PLAN.md and a RECORD.md returns the RECORD as `planFile` while the PLAN's declaration reaches nothing; a PHASE directory carrying a decoy `RECORD.md` still resolves `PLAN.md`; and the inline arm (no PLAN.md at all) names its declaring task at the seam. `why-corpus.test.mjs` 42/42, `why.test.mjs` 38/38. FINAL GATE: `self-verify.mjs --root .` `{"ok":true,...,"problems":[]}`, `test.mjs` 2903 tests / 2903 pass / 0 fail, `npx tsc -p tsconfig.ci.json` exit 0. Byte-identity against the PRE-TIER tree proved empirically rather than by argument: a `git worktree` at `b680a372~1` and the current tree produce identical stdout both for a built repo holding a phase directory and no `tasks/`, and for `cadence-core/bin/lib/issue-decision.mjs --top 20` over this repository. `lease-check` `ok:true` (staged 3, declared 6). |
Deviations: none
Open items:
- A resolved TASK entry still renders `deviation: PHASE-SCOPED - ...` with `(this phase's SUMMARY records no deviation)`, and `review: no adjudication record in this phase's directory` - two fixed strings in `lib/why-render.mjs` that say "phase" about a directory that is not one. The plan's task 3 requires those arms stay byte-identical and CONTEXT D-02 anticipates the state ("the decision and deviation edges then state their own absence, which is the true answer and is what the renderer already prints"), so the ANSWERS are correct and only the noun is wrong. Rewording them moves bytes on every phase entry too, so it is its own decision.
- A resolved TASK entry renders `decision: not yet joined`, because `fieldDecision` returns `undefined` for a directory with no CONTEXT.md and the renderer's absent-placeholder is worded as not-yet-joined rather than this-record-carries-none. Same file and same reason as above.
