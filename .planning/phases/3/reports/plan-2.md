PLAN COMPLETE
Plan: .planning/phases/3/PLAN-2.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: A bounded, read-only tags subcommand over readTags | 58cb940 | `git-branch.mjs tags --dir <path>` prints `{ok,tags[]}` through `readTags(dir, dir)` - `--dir` is both the directory asked from and the project root the answer must belong to; contract row `tags: []` added to self-verify. Manually: empty list for a non-repo `sub/` inside a repo tagged `v9.9.0`, `["v9.9.0"]` at that repo's root. 17 file tests pass, self-verify `ok:true`, `tsc -p tsconfig.ci.json` clean. |
| 2: Milestone step 2 decides release mode without git.create_tag | 4a05bf1 | Step 2 confirms the version first, probes `git-branch.mjs tags --dir <root>`, and treats EMPTY tags + no confirmed version as the non-release arm; the bump stays gated on the confirmed version (D-08). Up-front read is `git.auto_close` alone. `grep -n create_tag` and `grep -n "git tag"` both empty in milestone.md; budget re-pinned 11413 -> 12183; DOCS-CLAIMS MILESTONE-01 settled `stale` / `corrected - REL-01 (v3.5.4)`. 2214 tests pass, self-verify `ok:true`. |
| 3: The key's documented words name the land-time tag cut | de2886c | Schema `purpose`, catalog Purpose cell and reach row all name cad-land's cleanup-step cut on the pulled base after the merge confirms; reach stays `universal` so check 9 owes the purpose no verbatim phrase. Budgets re-pinned (catalog 9343 -> 9412, reach 21025 -> 21131). `config.mjs get git.create_tag` prints the value line, self-verify `ok:true`. |
| 4: A check that fails on the pre-fix tree | 79b337c | `REL-01: git.create_tag is read at one prose site, and its purpose names that site` in prose-agreement.test.mjs - the read sites are FOUND under `cadence-core/workflows/` and `skills/` and the command the purpose must name is DERIVED from the single site (`cad-land` -> `land`). WATCHED FAILING AT c78cbdb: copied into a detached worktree there it fails on the site count (2 sites: milestone.md + cad-land) and the whole file exits 1. Green here; 2215 tests pass, self-verify `ok:true`, tsc clean. |
Deviations: [deviation] Task 3 asserts DOCS-CLAIMS CONFIG-26 must move with the catalog rewrite (D-13/D-14 name it as "the catalog row"). CONFIG-26's claim is the catalog's DEFAULT column - "`git.create_tag` true, `git.on_land_cleanup` true, `git.auto_close` false" - which this fix does not touch; only the Purpose cell changed. Re-verdicting it `stale` would file a correction over a still-true sentence, so the row is left `accurate` and the phase-3 ledger narrative (committed with task 2) records the re-read, why it stands, and that the seven `MILESTONE-*` rows below the rewritten step are not re-pinned.
Open items: none
