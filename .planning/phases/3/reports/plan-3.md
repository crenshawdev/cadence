PLAN CHECKPOINT: structural
Plan: .planning/phases/3/PLAN-3.md
Tasks: 5 of 6
| Task | Commit | Note |
|---|---|---|
| 1 - A phase directory, wherever the project keeps it | a9becff | `declaredFilesIn(dir, planKey?)` is the one by-path reader; `declaredPhaseFiles`/`declaredPlanFiles` delegate with `phases/<N>` joined. `phaseDirsIn` lists live + archived dirs holding a conforming plan file, sorted by label. 25/25 tests pass; live locator returns 30 entries (3 live + 27 archived), exactly as Verify predicted. |
| 2 - The replay, off the one floor implementation | c5d38c3 | `route.mjs replay` + `levelFor` (the one scope-to-level implementation `resolve` and `replay` share). 155/155 tests pass (route 144, arg-contract 11); census 167 -> 168. Live: 30 rows, `regressions: []`, 27 of 30 raised - the plan's Notes figure re-derived against PLAN-2's current behaviour - and both rows computing below today (`_archive-v2.2.0/5`, `_archive-v2.6.0/1`) name no surface. |
| 3 - Lowering below the computed floor takes a named waiver | 97807b4 | `review.triggers.risk_surface.waive_routing_floor`, array_enum over the eight categories, default null. 151/151 route tests pass (7 new); `self-verify --root .` `ok:true, problems: []`; `config.mjs check` accepts `["secrets"]` and refuses `["nope"]` naming the enum; both budgeted references re-pinned from `weight.mjs` (config-catalog 9623->10077, config-reach 21453->21833). See the deviation below. |
| 4 - The retired rail, pinned byte for byte | 11030c1 | sha256 pin of `lib/retired-keys.mjs` in `retired-keys.test.mjs`, node builtins only. 20/20 pass. Falsified: one appended byte reddens that test alone with the file and D-03 named; byte removed and suite green before the commit. `lib/retired-keys.mjs` untouched. |
| 5 - A detected surface floors the configured rung too | 12aa4ab | The clamp is against the FLOORED cell's rung and is gated on a RAISE, so a discounted scope and a waived surface both leave the dial alone. `riskFloor` returns the whole record instead of a bare level. 156/156 route tests (5 new), 433/433 in the `routing` group, typecheck and `self-verify --root .` clean. |
| 6 - The seam states the waiver, the clamp and the replay | (not committed) | The `seams.md` statement and the `weight-budgets.json` re-pin (24132 -> 25068, from `weight.mjs`) are WRITTEN and sit UNCOMMITTED in the working tree. Half the Verify is met - `self-verify --root .` returns `ok:true` with `problems: []`. The other half is not: `node cadence-core/bin/test.mjs routing prose` is 704/705, failing only on the outside-lease census below. |
Deviations: [deviation] The plan's `files:` lease does not cover the change task 3 specifies, and task 6's Verify is what that costs. Adding the schema key task 3 mandates makes `self-verify.test.mjs`'s "placeholder keys expand: <t> prose covers every trigger key" census FAIL with `inert-config-key`: that fixture enumerates every schema key so check 1b reports nothing inert, so ANY new schema key needs one token added there. `cadence-core/bin/self-verify.test.mjs` is declared by no plan in this phase (PLAN-1, PLAN-2 and PLAN-4 do not name it either), so the fix is outside this lease and outside every sibling's. Task 3's own Verify was unaffected and green - `self-verify --root .` is `ok:true` because the two reference rows DO reach the key in the real tree - so it was recorded and not fixed. Task 6's Verify names `node cadence-core/bin/test.mjs routing prose`, and the `prose` group contains `self-verify`, so it cannot be met as written from inside this lease.
Open items: (1) `cadence-core/bin/self-verify.test.mjs:319` needs ONE token added to the fixture prose blob: `` `review.triggers.risk_surface.waive_routing_floor` ``, or the `` `review.triggers.<t>.waive_routing_floor` `` placeholder spelling its `surfaces` neighbour on line 319 already uses. Nothing else in that file changes. (2) `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair` fails on this branch at HEAD~5 as well - pre-existing, not this plan's, and outside the `routing prose` groups. (3) Declined a `--role` or `--phase` flag on `replay` - the plan's Action forbids both and the Verify exercises neither, so the subcommand takes `--file` alone. (4) Declined per-row surfaces diagnostics in `replay`: the malformed-`surfaces` warnings stay `resolve`'s, and a replay makes its scope visible through the envelope's `surfaces` and `surfaces_answered` instead.
