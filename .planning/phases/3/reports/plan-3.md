PLAN COMPLETE
Plan: .planning/phases/3/PLAN-3.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1. The thirteen questions and the migration live in the config workflow, and the catalog reaches the roles keys | 7bbc46cb | self-verify ok:true, no problems; `roles.cad-` rows = 12; `stakes` word count 0 in catalog/SKILL/COMMANDS; `--roles` present in config.md (5), SKILL.md (2), COMMANDS.md (1); prose-agreement 73/73. `workflow.verifier`'s schema purpose also rewritten (it still said "the stakes level decides"), which the catalog row it presents required. |
| 2. The two init workflows run the interview, and the spawn seam states the new contract | 71bc9a12 | `git grep -nw stakes` over the four files prints only the three `table stakes` lines of new-project.md; "Roles interview" 1 in each init workflow; "Stakes migration" 1 in seam-spawn-agent.md; "replay" 0 there (already 0 before this task - PLAN-1/2 had removed it); self-verify ok:true; prose-agreement 73/73. |
| 3. The remaining shipped prose and the code comments stop describing a level | 45d21e52 | Every file the Action names is swept and committed: 31 files, 33 rewritten rows in config-reach.md, the nine other prose surfaces, every comment site D-13 lists, the `route-table.json` citations, the `replay` remarks, and `RETIRED_KEYS_SHA256` re-cut with the reason in its docblock. self-verify ok:true; prose-agreement + retired-keys 95/95 (with `FORCE_COLOR` unset - see open items); `npx tsc -p tsconfig.ci.json` clean; weight budgets re-pinned for all 13 edited surfaces. **The task's two whole-tree sweep assertions do NOT pass** - see Deviations. Checkpointed; ACCEPTED 2026-09-05, the `## Must be true when done` bullet rewritten to the criterion that actually holds. |
| 4. The public docs, the design record and the claim ledger say what HEAD does | 64fac04c | All five Verify assertions pass as predicted: `git grep -nw stakes` over the six docs prints nothing (exit 1); `grep -c route-table` prints 0 for README.md, INTERNALS.md and docs/WORKFLOW.md; `grep -c "SUPERSEDED (2026-09-05)" DESIGN.md` prints 1; the `[Unreleased]` slice matches `roles` 8 times; each of the 13 named ledger ids selects exactly 1 non-empty claim cell with 0 `stakes` in it. `node --test cadence-core/bin/prose-agreement.test.mjs` 73/73. self-verify ok:true, no budget-overrun. `npx tsc -p tsconfig.ci.json` clean; `detect-commands` reports `lint: null`, so there is no lint command to run. 45 ledger rows rewritten, not the 13 the Action listed - see Deviations. INTERNALS.md:17's `route-table.json` pointer was already gone (PLAN-2 re-pointed it at `config.schema.json`), so that half of the Action was already satisfied at HEAD. Three surfaces needed byte trims to stay under their ceilings rather than a budget raise: config-reach.md, review-triggers.md, plan.md. |
| Suite repair (one round) | 05e390d2 | The full suite came back 3750 pass / 1 fail: `citation-census.test.mjs`'s "grammar two: each pinned row resolves to the code it names" - `DOCS-CLAIMS.md`'s `EXECUTE-22` is pinned to carry `TRACE_IGNORE_LINE` at `cadence-core/bin/planning/trace.mjs:245`, and that line read `const file = join(root, '.gitignore');`. A regression from this plan's own task 3: 45d21e52 grew that file's header comment by one line, pushing the symbol to :246. Repaired inside the lease by reflowing the corrected sentence back into four lines rather than re-pinning, which is what 04655c44 did for the same class in phase 2 - `citation-census.test.mjs` is NOT in this plan's `files:` lease, so moving the pin would have needed a file it may not touch. `node --test citation-census.test.mjs` 5/5; self-verify ok:true; tsc clean. Confirming full suite: **3751 pass, 0 fail**. |
Deviations:
[deviation] Task 3's `Verify:` asserts `git grep -nw stakes -- cadence-core skills ':!*.test.mjs' ':!cadence-core/bin/lib/retired-keys.mjs' ':!cadence-core/bin/fixtures' ':!cadence-core/workflows/config.md'` prints exactly three `table stakes` lines. It prints fourteen. The eleven extra are all code COMMENTS, and every one of them already states HEAD's behaviour by recording that the level is gone: `cadence-core/bin/route.mjs:12` ("There is no stakes level and no cells grid"), `:416`, `:567` ("The old rule held such a scope at the CONFIGURED level, which for the shipped unset `stakes` was the raised row"), `:888` ("It used to open on the stakes level's source, and that claim died with the level"), `:1374` ("`model_source` takes the place `stakes` held (D-05)"), `:1396` ("`stakes` and `stakes_set` are GONE from this envelope with the key itself"); `cadence-core/bin/lib/gate-agreement.mjs:17` ("The rows used to sit on a `null` sentinel meaning \"the stakes level decides\" ... The level is gone and so is that table"); `cadence-core/bin/config.mjs:488,494,499` ("That sentence went with the level"; "The key is retired (v3.7.12)"); `cadence-core/bin/lib/rung-agent.mjs:24` ("It lived in the routing data table until the stakes level was deleted and that table with it"). PLAN-1 and PLAN-2 wrote all eleven, in this same phase, as the design record. `route.mjs` and `gate-agreement.mjs` are NOT in this plan's `files:` lease. Nothing was done: the task was committed with the Action fully satisfied over every file it names. RESOLVED 2026-09-05 by the checkpoint's option (a): the criterion was stale arithmetic and the `## Must be true when done` bullet now states the rule the fourteen hits satisfy.
[deviation] Task 3's `Verify:` also asserts `git grep -ln "route-table" -- cadence-core skills ':!*.test.mjs'` prints nothing. It prints two files, neither in this plan's lease: `cadence-core/bin/lib/gate-agreement.mjs:19` ("`route-table.json`. The level is gone and so is that table: the schema `default` IS the answer now") and `cadence-core/bin/self-verify.mjs:1095,1159` ("the deleted route-table.json read never needed"; "It used to compare them against route-table.json's level-keyed grids ... the level is gone"). Both are historical comments naming a deleted file to say it is deleted. RESOLVED 2026-09-05 by the same checkpoint ruling.
[deviation] Task 4's Action lists thirteen `.planning/DOCS-CLAIMS.md` row ids "plus any other row `git grep -nw stakes .planning/DOCS-CLAIMS.md` finds that is a row". That grep finds eleven rows; the ledger actually carries **45** rows that stated the level, the cells grid, `route-table.json` or a retry rung as HEAD's behaviour, because most of them never spell the word (`README-18` "`solo` / `shipped` / `critical` are the three answers", `METHOD-95` "`risk_surface` is blocking at all three levels", `INTERNALS-09` "A cell is model + start rung + retry rung ..."). All 45 were handled to the `## Must be true when done` line rather than to the grep: 35 `accurate` rows had their claim rewritten to what HEAD does with the resolution cell set to `restated - ROL-02 (v3.7.12) - ...`; 9 `stale` rows whose RESOLUTION asserted a level as present-tense fact took a `; re-corrected - ROL-02 (v3.7.12) - ...` suffix, the ledger's own convention (`METHOD-59`, `INTERNALS-13` already carried one); and `PLAN-26` flipped to `stale | pending` because the document behind it is outside this lease (open items).
[deviation] Task 4's Action names ten files, on the premise that Task 3 had already swept every shipped surface describing a level. It had not, and could not: Task 3's sweep keyed on the WORD `stakes`, and the level survives in prose that never spells it. `references/review-triggers.md:313-335` still carried a three-column `Gate (solo/shipped/critical)` table with the sentence "The gate column is per LEVEL: solo / shipped / critical, in that order"; `workflows/plan.md:456-470` still resolved the plan gate per level; `workflows/execute.md:415,639`, `workflows/task.md:238,270`, `references/review-record.md:130`, `references/config-reach.md:179-190` and `references/review-triggers.md:187-201,257,286` the same. All nine sites are inside this plan's `files:` lease and were rewritten in this commit; `new-project.md`'s "a 20th rung file" also became "a 31st", the ladder having been squared to thirty in phase 1. Sites of the same kind OUTSIDE the lease were not touched and are open items.
Open items:
- `cadence-core/bin/prose-agreement.test.mjs:2763` ("triage-gate.md: the re-arm read-back, RUN, answers per PLAN and not per corr alone") is not hermetic about `FORCE_COLOR`. It shells a fenced block out of `references/triage-gate.md` and compares the subprocess's stdout to the literal `'1'`; with `FORCE_COLOR=3` in the environment the subprocess's `console.log` of a NUMBER emits `\x1B[33m1\x1B[39m` and the assertion fails. Pre-existing and untouched by this plan - neither that test nor `triage-gate.md` is in this lease, and the same failure reproduces with `FORCE_COLOR=3` against the unedited file. Every run in this dispatch was made with `FORCE_COLOR=0`.
- Four prose surfaces outside this plan's lease still describe the deleted level as current behaviour, all found by `git grep -n "at every level\|at all three levels\|at solo\|at shipped\|at critical" -- cadence-core skills`: `cadence-core/references/plan-revision.md:11-12` ("climbs the re-dispatch to the retry rung this level's cad-planner cell names"), `cadence-core/references/execute-parallel.md:76-80` ("`off` at `solo` and at the default `shipped`, `adjudicated` at `critical`"), `cadence-core/references/triage-gate.md:398` ("deep, rare gates - `plan` and `phase_diff` at `critical`") and `cadence-core/references/git-guard.md:125` ("That gate is `blocking` at every level"). `.planning/DOCS-CLAIMS.md`'s `PLAN-26` row is marked `stale | pending` against the first of them.
- Two comments in `cadence-core/bin/route.mjs` still read as present tense about the level - `:295` ("the level's `efforts` row answers it when no layer did") and `:329` ("the reason D-04 can let a gate win over the level's gate"). Outside this plan's lease; PLAN-1 owns that file.
- Five test-file comments still say "blocking at every stakes level": `cadence-core/bin/risk-diff.test.mjs:415,1668,1878`, `cadence-core/bin/planning-adjudication.test.mjs:293`, and a fixture AC string at `cadence-core/bin/planning-files.test.mjs:1636`. Outside this lease; the four in `prose-agreement.test.mjs`, which is in the lease, were corrected in this commit.
- `docs/figures/effort-ladder.svg` still renders the words "shipped project" inside the picture. It is not in this plan's lease and cannot be re-rendered here; `docs/WORKFLOW.md`'s alt text was rewritten to describe the figure without repeating the level name, so no prose repeats it.
- Three `.planning/DOCS-CLAIMS.md` rows are wrong for reasons this phase did not create and did not touch: `README-23` and `INTERNALS-12` both claim `model.escalate_on_failure` is "on by default" (it is off, and both documents say off), and `README-24` enumerates four gate values where the schema has five. Left as found - they are not level claims.
