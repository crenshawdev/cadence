Verified at 9c3df4c2d18bc4ce29816cd7b0295a1f21983f9e on cadence/binary-owns-process

| Item id | Truth(s) | Kind | Verdict | What I observed |
|---|---|---|---|---|
| P29-T1-C (C1) | T1 | check | accepted | Ran `cargo test -p cadence --test phase29_limits phase29_check_without_command_is_refused -- --exact`, exit 0, `running 1 test`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 3 filtered out; finished in 2.39s`. At `crates/cadence/tests/phase29_limits.rs:376`, the real native fixture installs a valid winner, then submits missing/null/boolean/number/object/array/empty/ASCII-whitespace/Unicode-whitespace commands at plan entry 1, item 1. Both public responses require `check-command`, full id `check/full/delivery`, phase 27, entry 1 and `submission.plans[1].content.evidence_map.items[1].spec.command` through `:393` and `:365`. Nonexistent/custom, broad and padded command controls publish; the sentinel stays absent (`:397`); blank auxiliary fields and draft/allocation controls remain legal (`:221`, `:407`). The shared real-boundary and durable-oracle inspection below applies. `git show 0d832da3f2709d8991d03133bae843a36e99793d` contains only the new test file and its helpers (419 insertions), no rule implementation. Its `:367` is the rule assertion reached after native setup and both calls; the recorded red is `String("arguments")` versus `"check-command"`, with missing identity, not a compile error (`.planning/phases/29/reports/plan-1.md:15`). |
| P29-T1-A | T1 | artifact | accepted | Opened `crates/cadence/src/plan/limits.rs:8`: malformed check/link fields are located before typed decoding, including approval and replacement copies; `:23` preserves readable item identity and `:36` constructs the actual JSON path. `:110` checks every candidate contribution's command for nonblank text without changing it. Module registration is real at `crates/cadence/src/plan/mod.rs:2`. Preview routes raw diagnostics at `crates/cadence/src/server.rs:633`; submission does so at `crates/cadence/src/plan_service.rs:115`, with typed diagnostic extraction at `:333`. `crates/cadence/src/plan/associations.rs:169` invokes content validation on the attached candidate; the commit path is traced in P29-A-COMMIT. `crates/cadence/src/plan/evidence.rs:51` retains structural Check fields without new deserialization/content rules. |
| P29-T2-C (C2) | T2 | check | accepted | Ran `cargo test -p cadence --test phase29_limits phase29_check_without_expected_output_is_refused -- --exact`, exit 0, `running 1 test`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 3 filtered out; finished in 5.08s`. `crates/cadence/tests/phase29_limits.rs:422` authors native slots, installs a valid winner and exercises malformed expected containers/tags, then missing/wrong/blank values for both literal and property tags, including Unicode whitespace (`:428`, `:436`). Both independent paths assert `check-expected`, full check id, bound phase/entry and the handwritten container/kind/value path at nonzero plan/item positions (`:445`, `:453`, `:365`). Literal/property and padded-text controls publish verbatim (`:456`); an unapproved blank expectation remains a draft (`:465`). Shared durable assertions apply. `git show 67d8e8c419a05cd3913b68b6f7edfc658b58e97b` adds only this test (57 lines), no production change. The recorded red `String("evidence-item-shape")` versus `"check-expected"` matches the real located-rule assertion at that revision's `:367` (`.planning/phases/29/reports/plan-1.md:16`), not a test compilation failure. |
| P29-T2-A | T2 | artifact | accepted | `crates/cadence/src/plan/evidence.rs:61` retains tagged literal/property strings. `crates/cadence/src/plan/limits.rs:65` locates malformed expected containers, tags and values; `:26` selects `check-expected`. `:114` extracts either tag's string and refuses only whitespace-empty content, preserving payload bytes and adding no predicate evaluator or auxiliary-field content rule. The same candidate call (`crates/cadence/src/plan/associations.rs:169`) and raw preview/submission routing (`crates/cadence/src/server.rs:635`, `crates/cadence/src/plan_service.rs:118`) reach these rules. |
| P29-T3-C (C3) | T3 | check | accepted | Ran `cargo test -p cadence --test phase29_limits phase29_distinct_checks_across_plans_are_refused -- --exact`, exit 0, `running 1 test`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 3 filtered out; finished in 2.65s`. `crates/cadence/tests/phase29_limits.rs:504` publishes plan 1 with `check/saved`, then proposes plan 2 with `check/proposed` on the same full truth/version, using identical handwritten specs. Both paths compare the complete ordered conflict detail, including returned plan identities, saved/proposed labels and exact origin paths (`:509`, `:514`, `:482`). Cases cover two/three distinct ids, reordered items/batches, aliases with all origins, partial/coordinated replacement, initial attached batch, shared/separate truths, retained map/receipt payloads, supersession and historical replay (`:520`, `:538`, `:555`, `:566`, `:590`, `:608`). A second real caller replaces the saved check between preview and submit; stale approval refuses and refreshed direct publication identifies the actual current saved check (`:617`). Shared reopen/byte assertions apply. `git show b42e24794f3891d229f0ef3d556376d3ef20542f` changes only tests/helpers (168 insertions), including the explicit publication-status assertion at `:360`; no limit implementation is present. The recorded red `String("ok")` versus `"refused"` is that outcome assertion after real publication (`.planning/phases/29/reports/plan-1.md:17`). |
| P29-T3-A | T3 | artifact | accepted | `crates/cadence/src/plan/associations.rs:36` retains numeric plan and saved/proposed provenance; `:42` removes all replaced contributions before adding proposals. Existing membership/version/shared-definition validation precedes the aggregate call (`:142`, `:172`). `crates/cadence/src/plan/limits.rs:155` groups by full truth id/version, then distinct check id, retaining each origin once; `:175` emits all conflicting ids in BTreeMap order and sorts origins by numeric phase/plan, source and numeric item index. `:181` includes all ids/origins in the reason. `crates/cadence/src/plan/model.rs:135`, `:158`, `:164`, `:170` define the compatible answer and complete structured detail; `:220` and `crates/cadence/src/plan_service.rs:333` preserve it in the public refusal. |
| P29-T4-C (C4) | T4 | check | accepted | Ran `cargo test -p cadence --test phase29_limits phase29_link_value_absent_from_truth_is_refused -- --exact`, exit 0, `running 1 test`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 3 filtered out; finished in 16.88s`. Handwritten native truths at `crates/cadence/tests/phase29_limits.rs:665` isolate approved slot content from metadata/other-truth values. Both paths refuse absent/embedded/case/synonym/pronoun values (`:670`) with full link id, exact value path and associated truth/version/path (`:651`); trigger/observer/outcome matches and padded values publish (`:674`). A second association lacking the value refuses, while all-associated matches publish (`:677`); malformed/blank endpoint/value cases run on both paths (`:682`). Every lexical fixture installs a valid winner, and the accepted/refused expectations are handwritten booleans (`:698`): W1 `parcelé` refuses (`:699`), `parcel_2` refuses (`:703`), comma-delimited `parcel` accepts (`:709`); Unicode/ASCII runs, later valid occurrence, exact case/internal whitespace/punctuation are also exercised. Cross-slot `parcel recipient` refuses (`:728`). Shared real-boundary/durable assertions apply. `git show bde7da9cac80a8162e05808dd666f5891e48e7cb` adds only this test/helpers (94 lines). Its recorded red is the existing publication assertion at `:360`, `String("ok")` versus `"refused"`, when `invoice` publishes against the parcel-only association (`.planning/phases/29/reports/plan-1.md:18`), not a compile error. Unresolvable authority is separately inspected under P29-T4-A, as the map requires. |
| P29-T4-A | T4 | artifact | accepted | `crates/cadence/src/plan/associations.rs:136` loads native context from the same candidate snapshot; membership/version validation at `:104`/`:108` precedes link validation at `:171`. `crates/cadence/src/context/persistence.rs:38` loads the retained `ApprovedContext`, whose submission and slots exist at `crates/cadence/src/context/model.rs:95`, `:45`, `:8`; current native version 1 is authored at `crates/cadence/src/context/persistence.rs:23`. `crates/cadence/src/plan/limits.rs:222` requires exact native approval, bound phase, unique full current identity and version-1 provenance, one retained slot record, and nonblank trigger/observer/outcome availability. Its failure becomes located `link-truth-unresolvable` at `:211`, with association detail from `:206`; lexical absence separately becomes `link-value-not-named` at `:213`. `:195` checks every association of every contribution. `:246` searches all character offsets inside each individual slot with exact bytes and W1's alphanumeric-or-underscore boundary; `:199` trims outer Unicode whitespace for comparison only. No rendered Truth.text, joined slots or unrelated prose is searched. Endpoint/value nonblank validation is at `:128`. The unresolvable safeguard was traced, not forced through forged context. |
| P29-A-COMMIT | T1, T2, T3, T4 | artifact | accepted | `crates/cadence/src/plan/associations.rs:42` loads retained maps, excludes every replaced publication (`:48`), keeps untouched saved items (`:57`) and adds proposed items (`:60`). `:169` applies all three policy functions to this whole union only for a fresh attached batch; therefore saved blank commands/outputs, distinct checks and unnamed values also block it. Preview reaches `validate_candidate` at `crates/cadence/src/plan_service.rs:299`; `crates/cadence/src/plan/persistence.rs:208` reaches the same association validation. Receipt replay precedes new validation in `contribute` (`:96` before `:101`) and in the service (`crates/cadence/src/plan_service.rs:128`, `:192`). The service builds against the owned view (`:201`) and uses CompareTransact with generation/integrity (`:245`). `crates/cadence/src/store/writer.rs:653` revalidates publication against its current snapshot before persist (`:668`); `crates/cadence/src/store/transaction.rs:292`/`:312` validates against the retained prior snapshot. `crates/cadence/src/plan/persistence.rs:272` recomputes the contribution and compares the complete proposed state/documents. Typed payloads survive both error conversions (`crates/cadence/src/store/writer.rs:660`, `crates/cadence/src/store/transaction.rs:319`, `crates/cadence/src/plan/limits.rs:146`, `crates/cadence/src/plan_service.rs:333`). Historical deserialization remains structural (`crates/cadence/src/plan/persistence.rs:11`, `crates/cadence/src/plan/map_history.rs:55`, `crates/cadence/src/plan/evidence.rs:51`); phase diff confirms evidence types, map_history, map_view and context types/persistence unchanged. Old receipts are retained while only the new receipt is inserted (`crates/cadence/src/plan/persistence.rs:103`, `:167`); history appends revisions and separate supersession relations without altering old payloads (`crates/cadence/src/plan/map_history.rs:84`, `:101`). Readback only observes files (`crates/cadence/src/plan/map_view.rs:26`, `:49`) and acquires no writer. Explicit provisional authoring/drafts and admission remain at `crates/cadence/src/plan/associations.rs:147`, `crates/cadence/src/plan/persistence.rs:212`, `:40`, `crates/cadence/src/plan_service.rs:150`. No retained strings are normalized or migrated; historical-policy branches are traced artifacts, not claimed old-binary runtime experiments. |
| P29-A-INSTRUCTIONS | T1, T2, T3, T4 | artifact | accepted | Ran `cargo run -p cadence --bin cadence -- plan-instructions` with stdout in `/tmp/cadence-phase29-plan-instructions.vVTGJh`, exit 0; `diff -u skills/cad-plan/SKILL.md /tmp/cadence-phase29-plan-instructions.vVTGJh` returned exit 0 with no output. `crates/cadence/src/main.rs:52` emits the compiled `markdown()`; `crates/cadence/src/plan/instructions.rs:441` combines ROLE with the derived submission schema. Compared the 513-byte Planner block verbatim: `docs/architecture/acceptance.md:331` equals `crates/cadence/src/plan/instructions.rs:21` and `skills/cad-plan/SKILL.md:20`. Compiled refusals/content limits are substantive at `crates/cadence/src/plan/instructions.rs:107`, lexical rule and limitations at `:129`, exact preview/approval at `:211`, saved-map correction/history at `:279`, complete located correction guidance at `:323`, provisional/execution boundary at `:164`, and pending P28/P29 observations at `:397`/`:415`. Opened the corresponding rendered guidance at `skills/cad-plan/SKILL.md:106`, `:128`, `:328`, `:396`, `:414`, and schema at `:438`. Public query/apply descriptions state the contract at `crates/cadence/src/server.rs:570`/`:575`; derived public output types include the plan Answer at `:291`/`:303`, whose optional conflict detail schema is defined at `crates/cadence/src/plan/model.rs:133`/`:156`. |
| P29-O1 | T2, T4 | observation | not seen | `.planning/phases/29/reports/pilot.md` did not exist when checked before reporting. `.planning/phases/29/CONTEXT.md:266` specifies the live-host episode, and `.planning/phases/29/PLAN.md:347` retains it as pending. No observer, observation date, conversation showing the two named typed refusals, or corrected plan landing with its attached map was available. Automated checks and compiled pending text do not supply this observation. |

Rejected items: none.

Shared check inspection: all four tests use `crates/cadence/tests/phase29_limits.rs:22` to launch the real `CARGO_BIN_EXE_cadence serve --project-root` over initialized stdio MCP, with empty global configuration and actual temporary projects (`:98`). `native_context` (`:117`) submits exact approved handwritten slots through the real context operation, stops/reaps the server (`:129`), and verifies retained slots and version-1 truth records against caller-authored values (`:132`). The only production constant used in fixture setup is the risk-category configuration vocabulary; it supplies no expected refusal, link match or stored-map outcome.

The handwritten map/spec inputs and section grammar (`:216`, `:221`, `:234`) never invoke a production renderer, validator or typed-map serializer to create the oracle. Complete preview (`:292`) and independently approved publication use the same otherwise publishable caller submission, including exact replacement consent (`:267`). `refusals` (`:350`) does not short-circuit publication after refused preview. It stops/reaps each server before durable comparison, and compares the full `.planning` tree and parsed snapshot to the pre-call baseline (`:297`). Reopening uses `Snapshot::parse` on state/items/decisions (`:155`), asserts journal absence before opening the real Filesystem/Store, and calls ReadVerified (`:197`); the tree is compared again afterward. Successful controls (`:304`) anchor returned records/specs/approvals to handwritten input, read the installed document and retained map, and perform fresh evidence-read and replay followed by unchanged-byte assertions. No named boundary is faked. All four exact commands ran at the stated HEAD and selected one test; no full suite or clippy was run by this verifier.

The four red commit patches were opened in full. Recorded failure text was checked against the corresponding assertion and real-call setup in those revisions; historical red commands were not rerun or represented as new observations. The executor's suite/linker narrative is not used as acceptance evidence.

Links: the evidence map has no link items (`.planning/phases/29/PLAN.md:62`). Links submitted inside C4 are caller data.

Counts: 10 accepted / 0 rejected / 1 not seen. These are item verdicts only; no truth status is assigned.
