VERIFICATION FAILED

Reviewed 2026-09-10 in `/code/cadence`, branch `cadence/binary-owns-process`, HEAD `32cbe1975df2cbc3a6cef4525b20996eaef72334`. All three reviewed plans are untracked. The relevant tracked source, instructions and contexts match HEAD. No Cargo command, build, test, mutating Git command, native project initialization or fixture capture was run. Only this report was written. The reserved phase-27 temporary root and its lock were not inspected or touched; `.codex-analysis/` was ignored.

Reviewed SHA-256 values:

- PLAN-1.md: `3bdaa386ca96b926a8e8af373947ae21959de256c2c2b23e3762b2314d34968e`
- PLAN-2.md: `e44f596751234f9b3926351634a0e98944e1b06c4736449829a18cdc1e21afb6`
- PLAN-3.md: `a843ef6b2897700dcb75add8a0a82998e33a26670cc2e180cf5b5b55a2b465c5`

The design, installed checker contract and contexts were read in the requested order before the plans. The phase-29/28 reports supplied review shape and the historical-fixture lesson, not proof of current behavior. `P1:n`, `P2:n`, and `P3:n` below mean `.planning/phases/12/PLAN-1.md:n`, `PLAN-2.md:n`, and `PLAN-3.md:n`. All other paths are repository-relative unless absolute. Commands are inspected specifications, not reported executions.

## Findings by severity

Counts: **1 BLOCKER, 5 WARNING, 0 NOTE.** W1 groups six occurrences of the same verification defect; each occurrence is listed explicitly.

### B1 — D-115's task-skill artifact has no delivering task or lease

**BLOCKER — `.planning/phases/12/PLAN-3.md:240`, Task 2 at :335 and Task 6 at :445.** D-115 expressly requires the classical guidance to be compiled into the executor Rust role source and rendered into both dispatch and the “native executor and task skill artifacts” (`.planning/phases/12/CONTEXT.md:235-246`). The plan implements the executor contract and `cad-execute` frontdoor only: its renderer has a contract output and an execute selector (P3:350-355), and its artifact/task lists name only `skills/cad-executor-contract/SKILL.md` and `skills/cad-execute/SKILL.md`. No task or lease reaches the task skill. The existing `skills/cad-task/SKILL.md:18-30` still delegates to the frozen task workflow; it does not consume this new compiled guidance. Calling both existing outputs “the two skills” does not deliver the distinct task-skill obligation.

**Suggested fix:** add the narrowly scoped task guidance artifact and its generation/consumption path to P12-A-ROLE and the delivering task's/frontmatter leases. Render its guidance from the same Rust source. Keep cad-task's four behavioral truths, treeless execution, task recording and planned-task completion parked: D-107 parks those promises and style as a separate truth, not D-115's explicitly required guidance artifact (`CONTEXT.md:40-48`, :118-124). Do not implement the entire task workflow to resolve this artifact omission.

### W1 — Unchanged regression tests do not settle six new production tasks

**WARNING — `.planning/phases/12/PLAN-1.md:276`, :300; `.planning/phases/12/PLAN-2.md:296`, :326, :348, :374.** The common exception at P1:110-112 permits artifact tasks to use an existing regression command. Such a command remains permissible only when it settles the task. Here these tests can pass while the task's new behavior is absent. Appending “inspect the artifact” does not make the named command settle that behavior. This misses the Planner block at `docs/architecture/acceptance.md:335-337` and the contract's task-completeness requirement.

| Task / Verify location | What the current selected test actually settles | What it leaves unsettled |
|---|---|---|
| P1 Task 2, P1:276 | `phase29_link_value_absent_from_truth_is_refused` exercises approved publication's link rules (`crates/cadence/tests/phase29_limits.rs:660`). It is a relevant guard on factoring the shared validator. | The newly factored complete admission contract and typed task/check allocation. The test never submits native execution admission. |
| P1 Task 3, P1:300 | `legacy_record_snapshot_and_operation_receipt_encodings_round_trip_and_mix` round-trips old records and appends an existing BoundaryV1 observation (`crates/cadence/tests/execution_boundary_compat.rs:272-332`). | New immutable admission, expected-set-version enforcement, gap extensions and committing-snapshot revalidation. |
| P2 Task 1, P2:296 | The same historical codec test. | New task/run/attestation/progress records and their confirmed replay. This task introduces three distinct record families under one old-code Verify. |
| P2 Task 2, P2:326 | `tool_schemas_all_inputs_have_properties_without_root_unions` lists tools and checks root object properties and absence of root unions (`crates/cadence/tests/mcp.rs:259-287`). | Task start, claim-before-spawn, real observed results, material binding, liveness and replay without a second process. It would pass with no new operations advertised. |
| P2 Task 3, P2:348 | The same root-schema test. | Attributed owner origin, exact inspected-material binding, persisted statements and replay. |
| P2 Task 4, P2:374 | `exact_committed_and_null_head_staged_records_settle` assesses constructed legacy risk records and material equality (`crates/cadence/tests/phase7_receipts.rs:120-160`). | Observing new evidence commits through real Git, their source leases, rename endpoints and the native risk adapter. |

**Suggested fix:** reorganize these artifact increments with the task that actually delivers their one existing C1/C2/C3/C4, or give the artifact task a substantive, narrow inspection command over the actual new artifact/public operation. Keep one check per truth. Do not add six acceptance tests or relabel regression tests as new truth checks. Their regression value can remain explicit without standing in for the task's Verify. Severity is WARNING because later check tasks do exercise these behaviors; the present task boundaries can nevertheless report success prematurely.

### W2 — Scheduling removal reuses C1 without completing C1

**WARNING — `.planning/phases/12/PLAN-1.md:357` (Task 5).** C1 is already delivered by Task 4 (P1:317-340). Task 5 edits the scheduler and earlier scheduling assertions, but leases no `phase12_execution.rs` and merely requires C1 to “still” pass. C1's multi-plan admission control does not distinguish the existing overlap computation from its removal: the current graph already returns the lowest ready number (`crates/cadence/src/execution/plan.rs:502-557`). An unchanged graph can pass this Verify. This is not a second evidence-map check, but it is an unjustified repeat as a task Verify.

**Suggested fix:** use one of the explicitly changed numeric-scheduling tests as the narrow Verify, with the new expected selection stated, and inspect removal of the old pairwise computation. Alternatively move the scheduling change into the task that genuinely completes C1. Preserve the source-lease tests. This task can ship correctly, so the defect is a WARNING, not evidence that T1's check itself is vacuous.

### W3 — Plan 3's suite task uses Plan 2's completed T2 check

**WARNING — `.planning/phases/12/PLAN-3.md:410` (Task 4).** The task installs suite eligibility, atomic launch and plan-completion gating (P3:400-409), but Verify runs `phase12_task_close_requires_red_then_green`, already delivered in Plan 2 Task 5 (P2:389-406). The task neither leases that test file nor adds a C2 obligation; its expected result explicitly keeps task close independent of suite settlement (P3:411). C2 can pass if the entire new suite gate is missing. This is precisely reuse of another plan's check without genuinely completing that check. The evidence map still contains only seven checks; the violation is in task verification and repeated work.

**Suggested fix:** make the suite-gate work part of the task that delivers the sole C6, or provide a narrow substantive artifact inspection for this increment and keep C6 as the sole runner-history check. Do not run C2 again as proof of suite completion. The later C6 prevents treating this as an unavoidable failure of T6, hence WARNING.

### W4 — Agent manifests are called generated artifacts without a renderer path

**WARNING — `.planning/phases/12/PLAN-3.md:267`, :350-355, :451-468.** P12-A-HOST requires inspecting generated provenance for the two skills and five agent manifests. The new entrypoint, however, is specified to emit only the contract and execute frontdoor. Task 6 explicitly regenerates “both skills” and changes agent permissions, but specifies no compiled manifest source or emission path for the five agents. At HEAD those manifests are independent files; for example `agents/cad-executor.md:1-13` owns its tool list and references the contract. `main.rs:52-60` demonstrates emitting a compiled skill, not generating agent variants automatically. It is therefore not possible to confirm that all seven files are rendered artifacts of the proposed entrypoint as requested.

**Suggested fix:** make the manifest provenance explicit: either deliver their compiled emission path and generation selection in the leased renderer task, or accurately describe them as metadata adapters and keep every instruction they contain sourced from the compiled contract. Verify the final metadata/tool compatibility with the already leased, deliberately updated MCP test. This is a WARNING because direct metadata updates can still give the agents the needed tools and preserve the real C7 boundary.

### W5 — Result classification leaves the evidence signal unspecified

**WARNING — `.planning/phases/12/PLAN-2.md:122-126`, :315-317; `.planning/phases/12/PLAN-3.md:153-162`, :427-432.** The plans correctly reject nonzero exit alone as behavioral red and empty stdout alone as proof of no results, but stop at “recognized actual test-result evidence” and “inspected no-results evidence.” They do not state the bounded observation that distinguishes a Python assertion result, setup/import failure, a custom command's output and an unobserved crash. C2 supplies a Python unittest example; C6 uses controlled programs emitting handwritten results. An executor is likely to generalize those strings into an undocumented runner grammar or treat a supplied classification as binary observation. D-109 leaves the reason a red proves the truth to inspection; D-112 permits one explicitly confirmed result-free relaunch, not a guessed result classification (`CONTEXT.md:150-168`, :192-210).

**Suggested fix:** specify the minimal retained evidence and classification boundary for these fixtures and production runs, including how unrecognized output stays Unknown and what the operator actually confirms for a result-free launch. Keep fixture examples in the existing C2/C6 functions, and preserve the distinction between binary-observed bytes/disposition and owner interpretation. Do not add a general semantic test analyzer or a planning-time command grammar. This is a precision WARNING: the plans state the right invariants, but the observation contract still needs a concrete implementation choice.

## Per-truth evidence table

All seven check functions are creation specifications in `crates/cadence/tests/phase12_execution.rs`. Each command names that integration target, a single function, and `-- --exact`; the common contract expressly requires `1 passed; 0 failed` and treats zero selection as failure (P1:110-116). Actual selection cannot be certified without implementation and execution. `A-` abbreviates the item prefix `P12-A-` below.

| Truth | Plan / delivering task | One check | Other items | Command |
|---|---|---|---|---|
| T1: incomplete native contract refused | Plan 1, Task 4; P1:120, :317 | P12-T1-C / `phase12_incomplete_execution_contract_is_refused` | A-HISTORY, A-CONTRACT, A-BASIS, A-EXTENSION, A-ORDER | `cargo test -p cadence --test phase12_execution phase12_incomplete_execution_contract_is_refused -- --exact` |
| T2: missing red then green refused | Plan 2, Task 5; P2:69, :389 | P12-T2-C / `phase12_task_close_requires_red_then_green` | A-RECEIPTS, A-RUNS, A-MATERIAL, P12-O1 | `cargo test -p cadence --test phase12_execution phase12_task_close_requires_red_then_green -- --exact` |
| T3: missing affirmative owner attestation refused | Plan 2, Task 6; P2:150, :418 | P12-T3-C / `phase12_task_close_requires_owner_no_stub_attestation` | A-ATTESTATION | `cargo test -p cadence --test phase12_execution phase12_task_close_requires_owner_no_stub_attestation -- --exact` |
| T4: acknowledged progress survives restart | Plan 2, Task 7; P2:192, :447 | P12-T4-C / `phase12_acknowledged_progress_survives_restart` | A-RUNS, A-PROGRESS, A-MATERIAL | `cargo test -p cadence --test phase12_execution phase12_acknowledged_progress_survives_restart -- --exact` |
| T5: continued executor receives unfinished tasks only | Plan 3, Task 1; P3:74, :317 | P12-T5-C / `phase12_continuation_dispatches_only_unfinished_tasks` | P12-T5-L, A-CONTINUATION, A-HOST, P12-O1 | `cargo test -p cadence --test phase12_execution phase12_continuation_dispatches_only_unfinished_tasks -- --exact` |
| T6: retained named runs and one plan-close suite | Plan 3, Task 5; P3:127, :425 | P12-T6-C / `phase12_runner_retains_task_commands_and_one_suite` | A-SUITE | `cargo test -p cadence --test phase12_execution phase12_runner_retains_task_commands_and_one_suite -- --exact` |
| T7: actual dispatch contains checks, state and instructions | Plan 3, Task 3; P3:192, :373 | P12-T7-C / `phase12_dispatch_contains_admitted_checks_state_and_instructions` | P12-T7-L, A-ROLE, A-PROMPT, A-HOST, P12-O1 | `cargo test -p cadence --test phase12_execution phase12_dispatch_contains_admitted_checks_state_and_instructions -- --exact` |

The check commands appear with expected properties at P1:131-163; P2:87-112, :156-175, :198-219; P3:83-101, :140-174, :203-228. No truth lacks a check or an item. No item lacks a truth. Shared artifact/observation associations do not create extra check identities.

## Independent goal-backward obligations

Derived before opening the plans, from the Executor block, execution refusals and phase-12 context:

1. Admission must distinguish a valid native approved contract from every missing/stale contract component, against the snapshot actually committed, without a legacy escape.
2. Closing each task must use its admitted check allocation and retained real run/commit evidence: unchanged committed test material, distinct ordered red and green commits, and one signed designated completion commit.
3. Owner inspection must remain distinct from executor assertion and bind the exact check revision and inspected material; absent, negative and stale attestations cannot close the task.
4. Acknowledged progress, attempts, checkpoints and receipts must survive process death; replay returns the committed result and uncertainty remains visible.
5. Explicit continuation must honor Stop and dispatch only unfinished tasks from retained history.
6. The binary runner must retain launch before process/result afterward, select admitted commands and enforce one plan-close suite with only D-112's explicit exception.
7. The actual outgoing dispatch must contain authoritative checks/state and compiled execution guidance, with its required host artifacts rendered from that source.

These obligations follow from `docs/architecture/acceptance.md:193-212`, :331-345 and `.planning/phases/12/CONTEXT.md:19-29`, :63-95, :125-246. B1 is a missing delivery under the seventh obligation; W1-W5 qualify task verification and precision.

## Evidence discipline, links and O1

The frontmatter requirements partition T1 / T2-T4 / T5-T7 exactly (P1:4, P2:4, P3:4). The seven truth sentences match context. There are seven unique check items, fifteen artifact items, two links and one shared observation. Tasks reference these items rather than inventing per-function acceptance. The repeated C1/C2 task Verifies are W2/W3, not an eighth or ninth evidence-map check.

Both links are justified:

- **P12-T5-L** names `the plan's unfinished tasks` (P3:105-113). That exact contiguous phrase appears inside T5's outcome, “only the plan's unfinished tasks” (`CONTEXT.md:74-75`). The truth names the executor as recipient; handing it the filtered task list is part of the truth itself. The link traces the same real C5 response into a passive test recipient, not a second test.
- **P12-T7-L** names `its admitted checks` (P3:231-239). That exact phrase appears inside T7's outcome (`CONTEXT.md:79-81`). D-101 requires lexical occurrence in one approved slot; it does not ban a phrase containing a pronoun or require resolving the pronoun. No synonym substitution or cross-slot join is needed. The real outgoing C7 prompt supplies the handoff. Correct storage with omitted dispatch content would fail it.

This agrees with D-101's exact-slot rule (`.planning/phases/29/CONTEXT.md:115-141`) and the design's handoff criterion (`docs/architecture/acceptance.md:158`, :171, :334). It does not claim that a lexical match proves a handoff or host obedience. T1-T4/T6 add no internal-seam links.

P12-O1 appears once by identity with the same episode/specification in Plans 2 and 3 (P2:249-269; P3:283-302). Both attach it to T2, T5 and T7; both label it pending/not yet seen. The owner/date describe specification approval, not a fabricated observation. Association reasons may differ in wording without changing the shared non-association definition. No check claims to prove model obedience, and no deterministic run marks O1 seen. Phase 13 owns its verdict/cap. No status derivation is implemented here.

## Check boundaries and falsifiability

The shared protocol at P1:61-108 applies to every check through P2:59-65 and P3:64-70. It requires the actual `CARGO_BIN_EXE_cadence serve` process, initialized MCP stdio, real temporary Git projects, `context-submit` owner-approved native truths, `plan-read` allocation, and exact approved `plan-submit` publication with an attached typed map. Setup cannot insert native authority through Store transactions or edited JSON. The old capture is a separately identified historical input.

The referenced harness patterns exist: real Client startup and handshake at `crates/cadence/tests/phase28_evidence.rs:22-44`, context authoring at :117-129, plan payload at :132-141, and journal-before-store reopen at :186-198. These are patterns to adapt, not a claim that the old fixed parcel fixture automatically satisfies the new execution contract.

Git defaults are disabled per invocation, including reads and helper commands; designated completion commits explicitly use disposable fixture signing material and `commit -S` (P1:79-86). That is coherent with D-109's signed completion requirement, not use of the owner's key. The isolation helper exists at `crates/cadence/tests/support/signing.rs:8-25`. The plans require following its isolation pattern with the stronger per-call overrides, rather than changing global Git settings.

Handwritten inputs and expected outcomes are mandatory. Refusal assertions stop/reap the server, inspect journal absence, reopen the files/store, and preserve exact serialized record slices, installed plan/map/publication bytes and receipts (P1:88-102). An independently appended refusal decision may change the outer generation/log; it cannot mutate protected accepted facts. This avoids demanding an impossible byte-identical entire log while also recording refusals.

| Check | Boundary and discriminating controls |
|---|---|
| C1 | One function includes the valid approved native positive control, whose test file deliberately does not yet exist; provisional mapless and genuinely captured historical blank-map negatives; malformed/missing/stale allocation; foreign bindings and projection drift; mixed native/legacy refusal; whole-current-phase union; stale admission basis; idempotent admission and explicit extension (P1:120-160). Both a blanket refusal and a permissive bypass fail it. Impossible publication states are artifact inspection, not forged runtime cases. |
| C2 | Real Python unittest tests a tiny subject returning 6 where 7 is handwritten; the red commit/run fails behaviorally, only the subject changes, and the descendant signed green commit/run passes with unchanged test bytes (P2:71-89). Missing red, missing green, reverse/unrelated ancestry and a nonexistent hash are explicitly present, along with missing runs, altered material, dirty source and tool/setup failure (P2:92-109). No fixed SHA or caller exit field can substitute for observed Git/run provenance. W5 concerns result classification detail. |
| C3 | Real valid red/green pairs isolate the attestation gate. Absent, false, old-revision, different-material/evidence and executor-only statements refuse; an attributed/timed exact owner record permits the same task to close (P2:150-177). Two-check and empty-allocation controls distinguish admitted-set enforcement from a caller-chosen subset. This proves the weaker record gate, not detection of mocks. |
| C4 | The server is stopped/killed after acknowledged A completion, partial B progress and a checkpoint; exact task/attempt/deviation/failure/checkpoint content survives real reopen, with C unstarted (P2:192-204). Lost replies are replayed after observing committed disk state; real child handshakes coordinate Unknown launches. Stop and commit-without-progress uncertainty remain visible (P2:205-217). No fake writer or renderer oracle. |
| C5 | After A completes and B's checkpoint records Stop, a restarted `execute-next` must stay stopped. Explicit linked owner continuation precedes another restart; the real returned executable task/allocation list must be `[B, C]`, with A only historical (P3:74-99). Lost/replayed requests, uncertain B work, declined continuation and explicit gap extension preserve prior history. |
| C6 | Actual named commands leave distinct append-only markers. Real task closes precede one suite launch/result and plan completion; replay leaves one marker, early/unnamed/repeated/CI claims refuse (P3:127-148). A separate real death case retains Unknown, requires operator confirmation bound to the dead launch and no-results evidence, permits one relaunch, and retains both. A real failed suite cannot be relabeled result-free and gets only a new approved linked gap identity (P3:149-169). Both suite and existing risk settlement are required. |
| C7 | The real fresh-server dispatch response is the subject. It must carry original approved check identities/revisions/specs, retained task state, commands, lease and handwritten instruction phrases. It includes actual config precedence, progress, Stop/resume and response replay (P3:192-228). Neither generated Markdown nor a direct renderer call is its oracle. B1 concerns a required companion artifact, not a fake C7 subject. |

No specified C1-C7 substitutes a stub for the boundary it asserts. Controlled child programs in C6 are inputs to the real runner, so their markers can honestly establish process launches. The seven commands are each specified to select one test function; all negative/control cases remain inside that function. No full-suite command appears in a task Verify. Full suites remain once at implementation-plan close, separate from the fixture runner's suite-history assertion.

## Decision delivery

| Decision | Delivery / assessment |
|---|---|
| D-107 | Scope exclusions at P1:49, P2:55, P3:60 and :478-482 keep the ten parked promises out. No task delivers the new SUMMARY table, recall-reachable authored deviations/open items, D-NN correction, four cad-task outcomes, pre-commit clearance, review-fix continuation or a separate style truth. Retaining raw deviations for T4 is expressly required by D-113, not the parked recall surface. |
| D-108 | P1 Tasks 2-4 deliver complete contract factoring, immutable committing-snapshot basis and actual public admission; P1:185-212 specifies all required components. Every fresh `require_execution_ready` site must change, including reobservation, writer and recovery (P1:323-326). No publication flag, release number or missing-test gate establishes readiness. W1 affects the early task Verifies. |
| D-109 | P2 Tasks 1/2/4/5 deliver new typed receipt/history records, binary observations, evidence-commit scope and close validation (P2:115-146, :234-245, :285-295, :389-403). Distinct resolvable ordered trees, unchanged test material/command, stage/sequence, disposition/digest/retained output and signed designated completion are specified. W5 asks for the concrete result-evidence boundary. |
| D-110 | Explicit typed allocation is submitted at admission in P1:268-271 and :320-327; P2 Task 2 echoes/checks it at task start (:312-314). C1 covers every task including explicit empty allocation, every canonical check once, shared aliases and invalid owners/revisions. Close gets the expected set from admission, never prose or its own submitted list (P2:127-128, :393-397). |
| D-111 | P2 Tasks 1/3/6 retain attributed/timed exact owner inspection statements and enforce them in the same atomic close/recovery path (P2:339-347, :418-429). Exact item revision, material and inspection evidence bind the statement. Executor `no_stub: true` and self-assigned origin do not become owner approval. P3:343-345 carries the weaker claim in compiled instructions. |
| D-112 | P2 Task 2 retains claim before spawn and observed result afterward, owns child lifecycle, keeps resident polling/Stop responsive and avoids another writer (P2:311-325). P3 Tasks 4/5 add suite eligibility after the final task, passing result before completion, Unknown retention and one atomic explicitly confirmed result-free relaunch (P3:400-438). Both launch histories remain. Actual suite failure needs a new gap; replay is not a process. W3/W5 qualify verification and precision. |
| D-113 | P2 Tasks 1/7 create immutable linked task attempts/progress and confirmed idempotent receipts using existing checkpoint history; P3 Task 1 selects unfinished tasks only and moves authorization before active replay (P2:447-459; P3:317-328). C4 causes a lost reply after committed progress; C5 reads the real resumed task list. New-id duplicate close refuses, Stop is not acceptance, and uncertain commits require reconciliation. |
| D-114 | All checks use initialized disposable projects; P1:365 and P2:469-470 explicitly prohibit self-hosting on this rewrite's planning tree. No task requires importing `/code/cadence/.planning`. |
| D-115 | P3 Tasks 2/3 compile and dispatch the verbatim Executor block/classical default, with retained commands taking precedence and unknown-language explicit-command diagnostics (P3:240-266, :340-360). No catalog, free-text override, style gate, coverage or count rule is added. **Incomplete required artifact delivery: B1.** Agent-generation provenance is W4. |
| D-120 | P1 Tasks 3/4 protect all admitted identities and append explicit versioned extensions from new approved gap publications; no in-place replacement, owner reassignment or old-receipt rekeying (P1:213-223, :288-299, :154-160). C5/C6 exercise preservation with actual completions. Shared authority must remain exact; changed specs cannot inherit old receipts. Gap presence never authorizes a rerun. |
| D-121 | No hook or registration is leased or deleted (P3:355-356, :465). The requested roadmap amendment is already present at HEAD, `.planning/ROADMAP.md:963-968`; P3:478-480 accurately avoids a redundant edit. Verifier dispatch remains phase 13. |
| D-122 | Explicit historical encodings, canonical preimages and historical replay remain separate (P1:201-212; P2:285-295; P3:257-260). P1:331-334 refuses legacy patches against new native dispatches even between plans. Native admission failures never downgrade. P2:360-373 and P3:405-407 consume risk/fire/consequence and pending history; P3:458-459 preserves review handoff without claiming fixes complete. No filing, trace census, parallel worktree or host-adapter redesign is tasked. |

**Receipt placement and identity.** P2:115-146 and :276-295 put new records in `execution/receipts.rs`, `execution/history.rs`, adjacent execution types and existing store writer/intent operations, bound through immutable admission to plan/task/attempt. P1:204-206 binds the basis to root/occurrence/exact authority, and P1:188-194 includes item definitions/revisions. Neither `plan/evidence.rs` nor `plan/map_history.rs` is a receipt-writing lease. This is consistent with keeping runtime receipts outside `Item::Check.spec` and immutable `acceptance_maps` history, as explicitly required by `CONTEXT.md:417-423`. The join is the real `map_history::Revision.item_revisions` map (`crates/cadence/src/plan/map_history.rs:12-20`), whose definition digest is checked by `map_view.rs:155-164`. Exact new namespace/type spelling is a creation choice; the plans do not falsely claim an existing receipt type already has these fields.

**Carried decisions.** D-31/D-32's post-commit source enforcement, both rename endpoints and zero exemptions are preserved in P2:101-105 and :360-373. D-119 explicitly supersedes overlap scheduling and parks D-31's pre-commit delivery (`CONTEXT.md:283-294`); changing obsolete phase-7 scheduling assertions is therefore authorized. D-84's admitted-identity immutability extends to admitted but undispatched plans, while D-88's intent becomes fresh validation rather than blanket refusal. D-90 through D-98 retain one canonical check per current truth, supplementary observations, opaque IDs, one exact approval/plan-map publication, current union, authoritative readback, immutable map revisions and historical replay. D-99 through D-106 are consumed by complete current-union validation, without adding publication-time command parsing, test-existence requirements or semantic link/expectation evaluation. No task edits these contexts or retroactively certifies historical evidence.

## Existing seams, creation leases and special lease questions

| Seam or path | HEAD evidence / assessment |
|---|---|
| Blanket native refusal | `crates/cadence/src/plan/persistence.rs:40-49` refuses any retained native publication in the phase regardless of projection bytes. Service calls are `execution_service.rs:178`, :1435; writer calls `store/writer.rs:695`, :962; intent calls `store/transaction.rs:322`, :725. P1 Task 4 leases all these paths. |
| Complete union and publication authority | `plan/associations.rs:42-65` assembles saved/proposed contributions; :134-174 validates membership/lower bound and phase-29 content/link/upper bound. `plan/persistence.rs:242-286` validates a publication delta against exact approval and documents. `plan/map_view.rs:127-164` cross-checks retained publication/receipt/map/item revisions and installed digest. P1 requires factoring/reusing these rules, not fabricating a new publication to validate execution. |
| Admitted-plan protection | `plan/validation.rs:74-81` currently recognizes active, completed and receipt-backed plans. P1:296-299 correctly extends this to the whole admitted set. |
| Existing execution types / evidence | `execution/model.rs:96-148` contains the limited CommandReceipt and one-commit TaskOutcome; new check receipts are real work. `execution/patch.rs` contains the named attach/validation seams. `execution_service.rs:1474` observes real commits; `rail/risk.rs:439` defines ExecutionBasis; `execution_service.rs:2160` reads risk material; `rail/receipts.rs:591` finalizes existing execution. These need the planned explicit native adapters, not renamed legacy receipts. |
| Existing checkpoint/continuation | `evidence/checkpoint.rs:24-62`, `evidence/gates.rs:29-45`, `evidence/persistence.rs:53-71`; `next_action/continuation.rs:95-119` selects checkpoints and :153-166 handles Stop/SuiteRed. Active replay returns at `execution_service.rs:385-395`, before checked continuation at :399. P3's ordering correction targets a real seam. |
| Resident and runner module integration | `server.rs:59-66` mounts service modules with path attributes; `recall/mod.rs:496-504` currently awaits execution in its serial request loop. P2 Task 2 leases both files and expressly requires children outside that wait. `execution_runner_service.rs` can be registered here without an unleased `lib.rs` or `main.rs` edit. |
| New domain modules | P1 Task 2 creation-leases `execution/admission.rs` and `allocation.rs`; P2 Task 1 creates `history.rs` and `receipts.rs`; P2 Task 2 creates `runner.rs` and `execution_runner_service.rs`; P3 Task 2 creates `instructions.rs`. They are absent at HEAD as expected. The appropriate `execution/mod.rs` registration leases are present. |
| C1-C7 test target | `crates/cadence/tests/phase12_execution.rs` is absent at HEAD, created by P1 Task 4 and reused sequentially. All seven function names are explicit creation specifications, not phantom existing tests. |
| Rendering pattern | `main.rs:35-36`, :52-60 emits `cadence::plan::instructions::markdown()` without project startup. `plan/instructions.rs:1-27` owns compiled role text. P3 Task 2 leases main.rs and explicitly creates `executor-instructions`; it does not pretend that entrypoint already exists. |

### (a) Python history helper

`crates/cadence/tests/support/phase12_history.py` is absent and honestly creation-leased (P1:23, :240). A small test-only driver for an old binary is justified here: current phase-29 publication cannot produce the required historical blank map, the captured binary must be independent of new Rust implementation, and normal C1 runs must not rebuild it. Python is not prohibited merely because production is Rust; the standing rule prohibits shipped JavaScript/shims and keeps production execution in the binary (`.planning/ROADMAP.md:188-200`). This helper lives under tests and must remain capture/inspection support, never a product command runner or runtime dependency of the shipped plugin.

`python3 ... --inspect` at P1:252-254 is narrow for **this artifact task**: its required expected result checks provenance, exact approval, historical blank content, hashes and bound root. It does not settle T1 admission, and the plan explicitly says so at P1:247-251. Unlike W1's unchanged regression guards, the new inspector is required to examine the very artifact the task creates. No additional acceptance check is needed. Proportionality depends on keeping this bounded driver/inspection role; no general fixture framework is requested.

### (b) Captured pre-29 fixture

`crates/cadence/tests/fixtures/phase12_pre29_blank.json` is absent and creation-leased. P1:167-184 and :242-251 require the helper to build the actual pinned old binary from an isolated source export/target, drive real stdio native context approval and attached map publication, then capture confirmed filesystem/store bytes after shutdown. The pinned object resolves to `b353f09dba50db20992bd39ae52b0b331168d2f0`; its `plan/associations.rs::validate` has membership/coverage validation but no phase-29 content gates. Thus a typed blank-command check with nonblank expected output is a possible old publication, rather than hand-manufactured authority. The recipe also includes a valid native control/publication so the whole-union case is available without asking current publication to accept a bad saved union.

Phase 28's absolute-path lesson applies directly: `config/reload.rs:10-20` canonicalizes paths, `config/write.rs:19-32` derives active config paths, and `import/mod.rs:1005-1012` refuses changed active-layer mapping. P1:178-182 addresses the core problem by requiring restoration at the **recorded disposable root**, exclusive ownership, a sibling lock, refusal of foreign contents and no rewriting/rehashing. The exact new phase-12 root spelling is left to capture, then becomes fixed provenance; the plan does not direct C1 to relocate it to a fresh random root. P1:74-77 fixes ordinary global config to the empty convention. Capture/restore must preserve that mapping as well as root bytes. The prior report's failure was relocation, not a universal prohibition on captured fixtures (`.planning/phases/28/reports/plan-check.md:20-47`). There is no remaining explicit relocation contradiction in these plans.

### (c) Earlier phase's lease test

`crates/cadence/tests/phase7_lease.rs` already exists; this is an edit lease, not a creation. P1:224-230 and Task 5 authorize changing obsolete overlap expectations while retaining path, rename and zero-exemption enforcement. Its graph tests really assert prerequisites/ready sets at :434-498. The other graph callers are in leased `execution/plan.rs`, `execution/tests.rs` and `execution_service.rs`. Editing an earlier phase's test is permitted when its scheduling premise is explicitly superseded by D-119; weakening D-31/D-32's lease assertions would not be permitted. W2 concerns the chosen Verify, not the authority to edit this file.

### (d) Other phases' tests as Verifies

Every named existing test function is present and top-level at HEAD. The phase29 link test, execution_boundary_compat test, MCP root-schema test and phase7_receipts test are all real narrow regression selections, but none settles the corresponding new task as written: see W1's complete table. Reading a test name alone would have missed that the MCP test checks neither presence nor behavior of new operations. P3 Task 6 is different: it explicitly leases and updates `mcp.rs` to test changed artifact metadata/tool permissions (P3:450-468), while removing Markdown policy content as an acceptance subject. Its selected compatibility test is at `crates/cadence/tests/mcp.rs:1134-1270`; that is a relevant Verify for the task's adapter metadata, with host conduct still O1.

### (e) Compiled output and rendered artifacts

The existing `PlanInstructions` pattern supports the new project-free renderer. The two specified skill outputs are artifacts inspected for substantive compiled source and generation, not C7's check subjects (P3:240-252, :357-360). C7 reads the actual outgoing dispatch. Agent variants exist at all five leased paths and can be updated without inventing a new route/model choice. Their claimed generation path is incomplete as W4 explains, and the separately required task-skill artifact is missing as B1 explains. No rendered Markdown equality test can repair either omission by proving only the execute frontdoor's text.

## Sequencing, file completeness, proportionality and excluded scope

Task counts are **5, 7 and 6**, each below the ceiling of eight (P1:238-359; P2:276-462; P3:306-469). The user's explicit sequential order overrides the checker skill's generic prohibition on shared-file split plans. All three plans share `execution/mod.rs`, `execution_service.rs`, `store/writer.rs`, `store/transaction.rs`, `server.rs`, `recall/mod.rs` and the new integration test. They must run 1, then 2, then 3; no parallel execution is sound.

The dependency chain is concrete:

1. Plan 1 captures historical input, factors admission/allocation, stores the immutable basis/extensions, and provides the actual public admission and initial owner-authorization path before C1. It does not depend on Plan 2 to fabricate its positive-control dispatch (P1:70-73, :327-334).
2. Plan 2 consumes that basis, creates its own history/receipt modules in Task 1, runner in Task 2 and owner records in Task 3, adapts material in Task 4, then closes tasks in Tasks 5/6 and completes progress/checkpoint operations in Task 7. Suite launches remain unavailable. Later Plan 2 tasks do not assume that Plan 1 already created these modules.
3. Plan 3 consumes the completed Plan 2 history/runner/owner operations for C5, creates compiled instructions before C7, then supplies suite completion and the relaunch exception before final host metadata generation. Its references to Plan 2-created symbols are valid preceding dependencies, not missing Plan 1 leases. New native close cannot escape through a legacy patch in the intermediate state.

Every task-listed repository file is present in its plan's frontmatter. All non-creation leases exist at HEAD. Module registration paths are leased; no task demands a new dependency, so a manifest/lockfile edit is not intrinsically required. B1 identifies an **implied required write absent from both task and frontmatter**, which simple file-list equality cannot detect. W4 identifies an unfinished generation obligation rather than an invented existing API. There is no known compulsory unleased source edit in the scoped admission/runner/continuation implementations.

The eighteen tasks are not eighteen acceptance checks. There is no arithmetic ceiling violation, but splitting storage, public adapters and check delivery creates the weak artifact-task Verifies in W1. Reorganizing those increments should improve proportionality without adding checks. The capture driver is bounded fixture preparation for a historical input the user explicitly requires; it is not a new tool to police the phase's work. Updating earlier graph tests and generated adapter metadata consumes superseded behavior, not extra truths.

Phases 13, 14, 15, 20, 21, 26 and 30 remain excluded: no verifier verdict/status/waiver implementation, progress/landing/undo surface, filing/deferred completion, truth revision or plan-review edit persistence is tasked. No SUMMARY redesign, recall feature, pre-commit clearance, review-fix continuation or self-hosting slips in through a helper. D-121's roadmap correction and D-119's zero-exemption wording are already present at HEAD, so not leasing ROADMAP for another edit is correct. B1 should be repaired as the small D-115 artifact obligation, not used to reopen the parked cad-task behavior.

This verdict is a plan review. It does not certify runtime results, historical fixture capture, source implementation, actual single-test selection or a seen O1. The real-boundary designs of C1-C7 are sound as specified; the missing D-115 artifact prevents full delivery as written, and W1-W5 identify likely execution deviations that should be resolved with the plan revision.
