VERIFICATION PASSED

## Findings by severity

**0 BLOCKER, 0 WARNING, 0 NOTE.** Second-pass B2 and B3 are resolved under the amended D-109 and D-112 at HEAD `47a9f283b2bc7a5f780106fe4676cc0587664361`. The earlier resolved findings remain resolved. No new finding arose from the full review.

Reviewed 2026-09-10 in `/code/cadence`, branch `cadence/binary-owns-process`. The three plans are untracked implementation documents. Relevant tracked source, contexts and host artifacts match HEAD. The production source also matches the plans' inspected `32cbe197` baseline; the intervening relevant changes are the context amendments. This report evaluates what executing the plans requires, not an implemented or tested result.

Citation shorthand: `P1:n`, `P2:n`, `P3:n` mean `.planning/phases/12/PLAN-1.md:n`, `PLAN-2.md:n`, `PLAN-3.md:n`. Other paths are repository-relative unless absolute. Commands below are inspected specifications, not commands executed by this review.

## Per-truth evidence table

All seven acceptance functions are explicitly created in `crates/cadence/tests/phase12_execution.rs`. Each command selects one named function with `-- --exact`; the common contract requires `1 passed; 0 failed` and treats zero selected tests as failure (P1:110-118). Invalid cases and positive controls belong inside that function. `A-` abbreviates `P12-A-`.

| Truth | Plan / delivering task | One check | Other items | Command |
|---|---|---|---|---|
| T1: incomplete native execution contract refused | Plan 1 Task 4, P1:328 | P12-T1-C / `phase12_incomplete_execution_contract_is_refused`, P1:122 | A-HISTORY, A-CONTRACT, A-BASIS, A-EXTENSION, A-ORDER | `cargo test -p cadence --test phase12_execution phase12_incomplete_execution_contract_is_refused -- --exact` (P1:165, :361) |
| T2: missing red then green refused | Plan 2 Task 5, P2:543 | P12-T2-C / `phase12_task_close_requires_red_then_green`, P2:132 | A-RECEIPTS, A-RUNS, A-MATERIAL, P12-O1 | `cargo test -p cadence --test phase12_execution phase12_task_close_requires_red_then_green -- --exact` (P2:208, :588) |
| T3: missing affirmative owner attestation refused | Plan 2 Task 6, P2:592 | P12-T3-C / `phase12_task_close_requires_owner_no_stub_attestation`, P2:256 | A-ATTESTATION | `cargo test -p cadence --test phase12_execution phase12_task_close_requires_owner_no_stub_attestation -- --exact` (P2:281, :614) |
| T4: acknowledged progress survives restart | Plan 2 Task 7, P2:618 | P12-T4-C / `phase12_acknowledged_progress_survives_restart`, P2:298 | A-RUNS, A-PROGRESS, A-MATERIAL | `cargo test -p cadence --test phase12_execution phase12_acknowledged_progress_survives_restart -- --exact` (P2:325, :644) |
| T5: continued executor gets unfinished tasks only | Plan 3 Task 1, P3:362 | P12-T5-C / `phase12_continuation_dispatches_only_unfinished_tasks`, P3:74 | P12-T5-L, A-CONTINUATION, A-HOST, P12-O1 | `cargo test -p cadence --test phase12_execution phase12_continuation_dispatches_only_unfinished_tasks -- --exact` (P3:101, :385) |
| T6: task-command history and one plan-close suite | Plan 3 Task 4, P3:453 | P12-T6-C / `phase12_runner_retains_task_commands_and_one_suite`, P3:127 | A-SUITE | `cargo test -p cadence --test phase12_execution phase12_runner_retains_task_commands_and_one_suite -- --exact` (P3:209, :508) |
| T7: actual dispatch contains admitted checks, state and instructions | Plan 3 Task 3, P3:425 | P12-T7-C / `phase12_dispatch_contains_admitted_checks_state_and_instructions`, P3:237 | P12-T7-L, A-ROLE, A-PROMPT, A-HOST, P12-O1 | `cargo test -p cadence --test phase12_execution phase12_dispatch_contains_admitted_checks_state_and_instructions -- --exact` (P3:273, :449) |

## B2 and B3: exact amended-rule assessment

The complete D-109 and D-112 decisions were read first, including their clarifications, before evaluating the old findings. The design and installed checker contract then supplied independent obligations; the binding contexts and both earlier reports were read before the plans. The orchestrator's amended rules govern this pass, rather than the superseded policy against which the second report was written.

### B2 — YES: the plans now state the amended rule exactly

**Binary observation has only `results observed` and Unknown.** P2:70-92 specifies retained bytes, per-stream digests, capture-completeness flags and observed timestamps, with a finite named vocabulary: a complete Cargo/libtest line beginning `test result:`, or a complete Python `Ran N test(s)` summary plus `OK`/`FAILED (...)`. A terminated command with complete but unfamiliar output is Unknown. Pending launches remain Unknown; no result timestamp is invented. The plan explicitly prohibits a binary `no results` class and inference from arbitrary assertion text, diagnostics or exit status alone. This is implemented by Plan 2 Task 2 (P2:435-446) and repeated without a contradictory third class in C6, its artifact and Plan 3 Task 4 (P3:148-166, :219-229, :477-488). It matches `.planning/phases/12/CONTEXT.md:216-224`.

**The relaunch confirmation is an operator attestation, not a binary absence finding.** P2:119-127 and P3:175-195 require a separate attributed/timed typed apply record naming the dead launch and attesting over its retained bytes that no test results were produced. The record remains beside the launch; Unknown is not rewritten. A recognized result causes outright refusal, including a recognized failing Cargo result and a unittest `FAILED (errors=1)` result (P3:183-187). Only an Unknown dead launch can receive the one confirmed relaunch, with exact identity and an atomically consumed per-admitted-plan budget (P3:489-501). Replay returns the original receipt rather than another process. The binary's inability to establish the truth of the operator's absence claim is explicit at P3:181-182, :194-195 and :495-497. That is precisely the clarified D-112 boundary, not the second report's old binary certification of absence.

**The custom-check path is delivered, not merely relabelled Unknown.** P2:105-117 requires an owner-attributed classification record for each Unknown custom run before it can become red-eligible or green-eligible. Its identity binds the run, both retained stdout/stderr digests, and the check revision; attribution/time and interpretation remain distinct from binary observation. Failing/successful observed disposition, ordered commits, unchanged material and a separate no-subject-stub attestation still apply. Task 1 creates this record family (P2:403-414); Task 5 provides its public apply operation and atomic close integration (P2:572-582). C2 runs a real custom check with unchanged test material across actual failing/passing commits, requires refusal without both owner records, accepts with both and the other valid evidence, and rejects stale digest/revision, wrong-run and executor-only records (P2:170-185). Reopen/replay must preserve the original Unknown observations and separate owner interpretations.

The host and compiled instructions must carry both distinct owner operations: P3:325-328, :401-404 and :530-533. A custom-check classification cannot serve as the separate dead-launch absence attestation or the no-subject-stub statement (P2:126-127). No runtime interpretation becomes a new planning-time command grammar. B2 is resolved in the specification, production tasks, controls and host handoff.

### B3 — YES: the plans now state the amended rule exactly

**Reported failure and red eligibility are distinct.** P2:94-103 requires Python unittest `failures > 0` and `errors == 0`; an omitted count in a valid summary is zero, and any positive errors count is retained as a failed attempt, never red. Cargo `test result: FAILED` with failing disposition remains red-eligible because that vocabulary cannot distinguish cause; the cause remains inspection. This matches amended D-109 at `.planning/phases/12/CONTEXT.md:162-173`. The receipt artifact repeats the rule (P2:218-228), Task 2 preserves reported outcome/counts separately (P2:442-444), and Task 5 installs the actual red gate (P2:564-579). Plan 3 carries the same distinction into C6 and the suite task (P3:159-166, :481-487).

**The real setUp control isolates the corrected gate.** Inside the sole C2, a real unittest raises in `setUp()` before its body, reports `Ran 1 test` and `FAILED (errors=1)`, and must be refused as red despite otherwise valid evidence (P2:162-169). The setup exception is triggered by the red subject's state; only the subject is corrected for the descendant green run. Test/setup bytes remain unchanged, so a test-material mismatch cannot supply an accidental passing refusal. The body writes a unique marker, required absent on the setup-error run. Task 5 repeats this requirement at P2:568-571. The missing-script pre-run failure remains a separate control (P2:170).

**Owner classification cannot evade the known-errors exclusion.** The custom interpretation route is limited to Unknown output; it cannot override a recognized unittest errors result (P2:115-117, :576-579). C6 also refuses a relaunch attestation for that recognized result (P3:184-186): a failed attempt's ineligibility as red does not turn recognized results into absence. B3 is resolved without introducing semantic mock detection or claiming that a Cargo failure proves its intended cause.

## Independently derived obligations

Before opening the plans, the design's execution refusal, record and Executor block, together with the amended context, yielded these seven obligations:

1. Fresh admission validates the full current approved native contract against committing authority and installed bytes, yielding a located refusal or immutable basis.
2. Each closing task uses its admitted canonical allocation and real observed red/green runs at distinct ordered commits with unchanged material; eligibility follows amended D-109.
3. Owner inspection remains an attributed, timed, exact revision/material/evidence record; an executor assertion cannot supply it.
4. Acknowledged task, attempt, checkpoint and receipt history survives process death, and replay cannot invent progress or erase uncertainty.
5. Explicit continuation honors Stop and hands the executor only unfinished work from durable state.
6. The runner selects retained named commands, claims before spawning, records observation afterward, and enforces suite timing and exactly D-112's attested relaunch exception.
7. Actual dispatch carries authoritative checks/state and compiled execution guidance, with the amended D-115 host artifacts produced from that source.

Sources: `docs/architecture/acceptance.md:193-212`, :331-345; `.planning/phases/12/CONTEXT.md:19-29`, :63-95, :125-274. The tasks and checks mapped above deliver these obligations as specified. Acceptance evidence remains distinct from unit tests, CI, style guidance and live model conduct.

## Planning refusals, links, observations and check boundaries

Requirements partition T1 / T2-T4 / T5-T7 in the three frontmatters (P1:4, P2:4, P3:4). Enumeration yields **seven distinct checks, fifteen artifacts, two links and one shared observation**. Every item names at least one truth; every truth has exactly one check with a command and expected property. All task Verifies are narrow tests or a substantive artifact inspection. No task in another plan uses an already completed acceptance check as a regression-only Verify (P1:254-391; P2:415-646; P3:385-547).

The two links are justified under both the truth text and D-101's lexical rule:

- **P12-T5-L**, P3:105-113, carries `the plan's unfinished tasks`. That contiguous exact phrase occurs inside T5's outcome in `.planning/phases/12/CONTEXT.md:74-75`, whose recipient is the executor. C5 traces the actual outgoing `[B, C]` list; inspecting an internal filtered list alone would not suffice.
- **P12-T7-L**, P3:276-284, carries `its admitted checks`. The exact phrase occurs inside T7's outcome at `CONTEXT.md:79-81`. Matching that literal phrase requires no synonym or pronoun resolution. C7 traces retained item/revision/specification and allocation into the actual outgoing dispatch.

D-101 compares case-sensitive exact phrases inside individual approved slots, including character boundaries, never joined slots or caller-copied truth prose (`.planning/phases/29/CONTEXT.md:115-141`). Both phrases meet that rule, and both truths require the stated handoff. The same acceptance call supplies the passive recipient's trace; no second check is created. T1-T4/T6 do not acquire internal wiring links merely because their implementation has collaborators.

**O1 stays an observation.** P2:355-375 and P3:339-358 retain one identity, the same episode and specification provenance, with associations T2/T5/T7. Both label it pending/not yet seen. The owner/date concern specification approval, not an invented observed result. No test claims that the model obeys the instructions, and neither the observation cap nor verdict/status derivation is implemented here. This matches `.planning/phases/12/CONTEXT.md:425-434` and D-90/D-96.

**Common boundary.** P1:63-108 applies through P2:60-66 and P3:64-70: the actual `CARGO_BIN_EXE_cadence serve` process over initialized MCP stdio, a real temporary Git project, real phase-11 `context-submit`, real plan allocation and exact phase-27/28 attached-map `plan-submit`, and public native execution/owner operations. No direct snapshot seeding, fake service or invented approval is permitted. Existing harness patterns were read at `crates/cadence/tests/phase28_evidence.rs:22-44`, :117-141 and :186-198; the plans require adapting them to handwritten fixture authority, not treating the old parcel fixture as the new oracle.

Every test/helper Git invocation disables ambient signing and supplies explicit disposable identity/null stdin; designated completion commits alone use explicit `commit -S` with private fixture signing material (P1:79-86). That is consistent with D-109's signed completion requirement, not use of the owner's key. The existing isolation pattern is `crates/cadence/tests/support/signing.rs:8-25`.

Handwritten values and original approved payloads/returned identities are the oracle. The common protocol stops/reaps the server, reads files, checks journal absence, and reopens the actual Filesystem/Store before final durable assertions (P1:88-102). Exact protected serialized record slices, plan/map/publication bytes and receipts must be preserved on refusal; a separate refusal decision may change the outer log/generation. This avoids either a renderer oracle or an impossible assertion that an appended refusal log is itself byte-identical.

| Check | Real trigger, outcome and controls |
|---|---|
| C1, P1:122-168 | Valid approved native admission succeeds before its test exists; provisional mapless and authentic historical blank-command publications refuse. Missing/malformed/stale allocation, foreign authority, installed-byte drift, mixed native/legacy, whole-phase union, stale preparation, replay, explicit extension and admitted-plan replacement are inside one function. Both blanket refusal and a permissive bypass fail it. States publication cannot create are artifact inspection, not forged native test state. |
| C2, P2:132-210 | A real Python subject returns 6 against handwritten 7 at the red tree; the unchanged test passes after only the subject changes at a signed descendant green tree. Missing red, missing green, same/reversed/unrelated commits, nonexistent commit, missing run, altered material, dirty source, borrowed/shortened allocation and lease failures are explicit. The new setup and custom-output controls exercise the amended eligibility rule at the real close boundary. |
| C3, P2:256-283 | Real valid run pairs isolate the attestation refusal. Absent, false, stale revision/material/evidence and executor-only statements refuse; an exact attributed/timed owner record permits the same task. Two-check and empty-allocation controls prevent a caller-selected expected set. The claim is a record gate, not detection of stubs. |
| C4, P2:298-327 | Actual server death follows acknowledged A completion, partial B progress, checkpoint, deviation and failed attempt. Handwritten A/B/C state and original bytes survive reopen. A confirmed on-disk event whose reply is not consumed replays its original receipt. A real child handshake creates the Unknown case; Stop and commit-without-progress uncertainty stay visible. |
| C5, P3:74-104 | A restarted real execute-next is blocked by Stop. A public explicit successor authorization followed by another restart yields exactly `[B, C]`, with A only historical. Duplicate completion, uncertainty/reconciliation, declined continuation and a real approved gap extension preserve the original history. The check reads the actual dispatch, not a selector directly. |
| C6, P3:127-211 | Real child programs leave distinct append-only markers. The ordinary success path has exactly one suite launch after the last acknowledged task and before plan completion. Replay cannot add a marker; early/unnamed/repeated/caller/CI claims refuse. Real death leaves Unknown; one exact operator attestation permits one relaunch, retaining both launches. Recognized success/failure/error results refuse the exception; unfamiliar output stays Unknown. A failed suite's repair has a new approved linked gap identity. Suite and existing risk settlement are both required. |
| C7, P3:237-275 | The subject is the real fresh-server dispatch response: original check identities/revisions/specs, allocation, current B state, historical A, retained commands/lease and handwritten Executor/classical phrases. Real config precedence, Stop/resume, fresh linked dispatch and exact replay are required. Neither a skill Markdown file nor a direct renderer call supplies the assertion subject or expected value. |

None of C1-C7 is inherently unfalsifiable or substitutes a fake for its named boundary. C6's controlled programs are inputs to the real runner, so their markers honestly measure launches. Each is specified as one function and one exact test selection; implementation, actual selection count, fixture capture and test outcomes remain unexecuted.

## Decision delivery and preservation

| Decision | Delivering task and assessment |
|---|---|
| D-107 | P1:49, P2:56, P3:60 and :556-560 preserve all ten parked promises: SUMMARY table; authored deviations/open items via recall; D-NN correction; four cad-task truths; pre-commit clearance; review-fix continuation; style as its own truth. Raw deviation retention in C4 is required D-113 history, not the parked recall feature. The scope does not claim cad-execute/cad-task in full. |
| D-108 | Plan 1 Tasks 2-4 factor complete authority/allocation validation, retain an immutable committing-snapshot basis and replace the actual admission guard (P1:187-214, :267-287, :301-325, :340-360). Native approved versioned truths/slots, exact current approved publication/receipt/content/map/items, attached mode, whole current union, installed bytes, positive identities, ordered unique tasks, nonblank named verifies, suite, source lease and typed allocation are all specified. Test existence/red receipts are not admission requirements. |
| D-109, clarified | Plan 2 Tasks 1/2/4/5 provide explicit new receipt/history records, real committed-tree/material observations, distinct ordered commits, unchanged test/command, stage/sequence, disposition/output and signed completion with scoped evidence commits (P2:211-250, :392-414, :431-468, :513-538, :554-587). Reported failure, known unittest errors, Cargo ambiguity and owner-classified custom runs now follow the amended decision, as assessed above. |
| D-110 | Plan 1 Task 2 defines a submitted typed allocation outside the evidence-map schema, covering every task and every current canonical check revision exactly once, including explicit empty assignments and shared aliases (P1:271-287). Task 4 advertises/persists that contract. Plan 2 Task 2 echoes and validates it at task start (P2:432-434); close resolves its expected set from immutable admission (P2:558-562). |
| D-111 | Plan 2 Tasks 1/3/6 create, persist and enforce exact attributed/timed owner inspection with approved payload, item revision, test material and inspected evidence (P2:284-293, :482-499, :602-613). Absent/false/stale/executor-only statements refuse in the same atomic close and writer/intent path. Compiled instructions explain the deliberately weaker record claim (P3:399-404). |
| D-112, clarified | Plan 2 Task 2 persists the launch before spawn and observed result afterward, owns the actual child, preserves Unknown and prevents replayed launches (P2:435-468). Plan 3 Task 4 supplies post-last-task suite eligibility, passing result before plan completion, exact existing risk settlement, and the one atomically consumed operator-attested relaunch (P3:464-510). Recognized results always bar that confirmation; histories are retained. Lint/typecheck are named, CI is not closure and shell/wrapper subcommands remain outside the claim. |
| D-113 | Plan 2 Tasks 1/7 retain immutable per-task events within linked attempts, explicit checkpoint answers and confirmed receipts; C4 exercises lost-reply replay and actual death (P2:392-414, :631-646). Plan 3 Task 1 authorizes before active replay and selects only unfinished work, including explicit reconciliation of uncertain commits (P3:373-384). Stop is not acceptance; a new request id cannot double-close a task. |
| D-114 | The common fixture and O1 use initialized disposable projects; P1:397-399, P2:650-654 and P3:540-542 expressly exclude this rewrite's live planning tree. No task initializes or imports `/code/cadence/.planning` or invents native evidence for completed rewrite work. |
| D-115, amended | Plan 3 Task 2 compiles the verbatim Executor block and classical guidance, creates the project-free renderer and produces the executor contract/cad-execute artifacts; Task 3 puts that source into actual dispatch; Task 5 regenerates final host artifacts (P3:396-423, :436-451, :520-547). Explicit retained commands outrank config proposals; unknown language needs an explicit command. No preset catalog, free-text override, style gate or cad-task artifact is added. |
| D-120 | Plan 1 Tasks 3/4 retain original admission and explicit versioned extensions for newly approved gap identities, protect all admitted plans and reject reassignment/rekeying or in-place replacement (P1:215-225, :301-325, :340-360). C5/C6 preserve actual completed outcomes and original receipts across extensions (P3:89-96, :198-204). Changed specs need fresh evidence; a gap never implicitly authorizes an original-plan rerun. |
| D-121 | No hook or registration is leased or deleted (P3:418-419, :542). The required roadmap amendment already exists at HEAD, `.planning/ROADMAP.md:963-968`; P3:556-558 correctly requires no redundant edit. Verifier dispatch and hook retirement remain phase 13. |
| D-122 | Historical schema-1/BoundaryV1 encodings, canonical preimages and replay remain historical, while new native records have explicit encodings (P1:209-214, :311-312; P2:393-402; P3:306-308, :441-444). P1:354-357 refuses old executor patches against newly admitted native dispatches even during intermediate delivery. The existing phase-7 risk/fire/consequence/pending rail and phase-9 review handoff are consumed, not redesigned (P2:513-526; P3:472-473, :534-536). No native/provisional fallback, parallel worktree, trace census, filing or claim that review findings equal completed fixes appears. |

**Receipt home and join.** P2:211-243 and :384-405 place runtime receipts, observations, owner interpretations and progress in new execution receipt/history records beside execution types, persisted through the existing writer/intent protocol. Every event binds the immutable admission, plan/task/attempt and expected version (P2:395-397); P1:191-208 binds that admission to root/occurrence, exact publication/map/item authority and allocation. This follows the required key through project/occurrence/phase/check id/revision without inventing check identities.

These runtime records remain **outside `Item::Check.spec` and outside immutable `acceptance_maps` history**, as required by `.planning/phases/12/CONTEXT.md:444-450`. The actual map has only authored check spec fields (`crates/cadence/src/plan/evidence.rs:23-27`, :51-59), and its immutable revision exposes `item_revisions` (`plan/map_history.rs:8-20`), cross-checked against definition digests by `plan/map_view.rs:155-164`. Neither evidence.rs nor map_history.rs is leased for runtime receipt writes. The plans explicitly create the separate runtime modules; the exact new type/namespace spelling remains an implementation choice.

**Carried rules.** D-31/D-32 retain post-commit enforcement, both rename endpoints, rejected-commit visibility and zero exemptions through existing source/lease policy (P1:376-391; P2:197-199, :340-351, :513-538). D-119 expressly parks D-31's pre-commit delivery and supersedes pairwise overlap scheduling (`CONTEXT.md:310-321`); changing those old scheduling tests does not repeal lease enforcement. D-84 protects every admitted identity, including undispatched members; D-88's mechanism becomes complete fresh D-108 validation while its anti-bypass intent stands.

D-90-D-98 remain intact: one canonical phase-wide check, supplementary observations, exact combined plan/map approval, opaque item ids, shared definitions, current truth/version membership, authoritative readback, immutable supersession and historical receipt replay. D-99-D-106 remain the planning-time content/link/one-check rules over the committing current union; no test-existence requirement, runner parser at publication, semantic link analyzer or expectation evaluator is added (P1:187-225, :267-276). Runtime result observation is the newly authorized D-109/D-112 policy, not a change to D-100's acceptance of nonblank command text at planning.

## HEAD seams and leases

The cited production symbols were checked against the current tree. No nonexistent existing API is needed; new modules, test names and the executor-instructions entrypoint are identified as creations.

| Existing seam | HEAD evidence and required planned use |
|---|---|
| Blanket native refusal | `crates/cadence/src/plan/persistence.rs:40-49` refuses any retained native publication. Calls exist at `execution_service.rs:178`, :1435; `store/writer.rs:695`, :962; `store/transaction.rs:322`, :725. P1 Task 4 leases all those files and requires replacing every fresh native call, including reobservation/writer paths (P1:346-349). Task 3 also requires committing-view and intent validation. This is a validator replacement, not deletion of the guard or a readiness flag flip. |
| Complete union and retained authority | `plan/associations.rs:42-65` assembles contributions; :134-174 applies membership/coverage and phase-29 limits. `plan/persistence.rs:242-286` validates a publication delta against exact approval, while `plan/map_view.rs:127-164` cross-checks receipt/map/item authority and installed bytes. P1 Task 2 factors/reuses the pure rules rather than inventing a publication to validate admission. |
| Admitted-plan protection | `plan/validation.rs:74-81` currently recognizes active/completed/receipt-backed plans. P1:309-311 extends protection to the entire admitted set, covering identities not yet dispatched. |
| Execution structure and scheduling | `execution/plan.rs:43-59` requires typed execution/suite/tasks; `parse_plan` exists at :91. `PlanGraph::build` at :502-535 currently computes pairwise lease dependencies; :542-557 returns ready/next-ready selections. P1 Task 5 removes that computation, updates callers and discriminating scheduling assertions, and preserves source coverage. |
| Existing receipt and lease limitations | `execution/model.rs:96-148` has generic command receipts and one completion commit, without check/stage/tree provenance. `execution/patch.rs:504-518` only validates generic evidence shape; :222-285 attaches real paths and checks lease coverage. Plan 2 adds explicit native records and adapters rather than pretending the old fields already meet D-109. |
| Git/material and risk | `execution_service.rs:1474-1563` validates real commits, ancestry, signatures and all-parent diffs; :1567 starts lossless name-status/rename handling. `rail/risk.rs:439-449` defines ExecutionBasis; `execution_service.rs:2160-2220` binds material to accepted receipts; `rail/receipts.rs:591-658` finalizes old execution. P2 Task 4 explicitly adapts these for native evidence commits, retaining historical bases/codecs. |
| Checkpoints and continuation | `evidence/checkpoint.rs:24-62`, `evidence/gates.rs:23-52` and `evidence/persistence.rs:53-87` provide existing record/answer/history semantics. `next_action/continuation.rs:95-119`, :153-166 handles checkpoints, Stop and SuiteRed. Active replay currently returns at `execution_service.rs:385-395` before `checked_continuation` at :399: P3 Task 1 targets a real ordering seam. |
| Resident/runner service | `server.rs:59-66` registers execution/rail services; `recall/mod.rs:496-504` serially awaits execution. P2:421-430 leases these registration points and :449-461 requires real child ownership outside the resident's blocking request wait. The new execution_runner_service.rs needs no invented existing registration or unleased main/lib edit. |
| Compiled instructions | `main.rs:35-36`, :52-60 already emits the planner artifact without project startup; `plan/instructions.rs:1-27` owns its compiled source. `execution/render.rs:133-175` currently emits old whole-plan instructions and opaque body. P3 Tasks 2/3 create the analogous executor entrypoint and real native state composition. |

### (a) Python test helper

`crates/cadence/tests/support/phase12_history.py` is absent and honestly creation-leased by P1:23, :242. Its job is bounded capture and read-only inspection of an independently built old binary's output (P1:169-186, :244-256). It is not the Rust product's command runner or a shipped runtime dependency. The standing rule is zero shipped JavaScript/shims and a Rust production executable (`.planning/ROADMAP.md:188-200`); it does not prohibit a test-support Python capture driver.

The Verify `python3 ... --inspect` at P1:254 is the narrow command for this artifact task: it must validate provenance, exact captured approval, intentional blank content, file hashes and bound root. It does not claim to settle admission; C1 does that later. Running C1 here would conflate historical-input preparation with future production work. The helper is justified so long as it performs precisely this specified capture/inspection role; no general testing framework or phase-policing apparatus is requested.

### (b) Authentic captured fixture and absolute paths

P1:173-184 and :244-253 require an isolated source export/build from `b353f09d`, real stdio native context approval and attached blank-check map publication, then confirmed shutdown before capturing exact filesystem/store bytes and request/response/provenance. Read-only Git inspection resolves the pin to `b353f09dba50db20992bd39ae52b0b331168d2f0`. Its `plan/associations.rs::validate` contains membership/coverage but no phase-29 content gates, so the historical blank command is producible by that actual binary. The recipe separately retains a valid native control/publication. No current validator weakening, manual JSON authority, committed executable or build tree is required.

The phase-28 absolute-path lesson **does apply**. `config/reload.rs:10-20` canonicalizes identity; `config/write.rs:19-32` derives active mapping; `import/mod.rs:1005-1012` refuses changed active mapping. P1:180-184 therefore requires restoration at the recorded disposable root without rewriting or rehashing, exclusive ownership, a sibling lock and refusal of foreign contents. P1:74-77 isolates global configuration, and the inspector checks the exact bound root (P1:249-256). Capture must record the actual canonical root and its matching configuration provenance; restore must reuse that identity. The phase-12 spelling is selected during capture and then fixed, not randomized anew for each restore.

This satisfies the lesson in `.planning/phases/28/reports/plan-check.md:20-47`: relocation was the defect, not the mere use of a captured fixture. The plans do not require relocation. The phase-27 root and lock are expressly excluded (P1:252-253, :397-399). No capture or restore was performed in this review.

### (c) Earlier phase's test

`crates/cadence/tests/phase7_lease.rs` exists; it is an **edit lease**, not a creation. P1:380-391 replaces `textual_prefixes_and_exact_files_never_create_recursive_overlap`, currently at that file's :485, whose initial ready set is `[1, 2, 3, 4, 5]` at :494. The changed test requires only `[1]`, then `[2]`, then `[3]`, for both disjoint and overlapping leases, and no selection when all complete. The unchanged implementation fails that expectation. Related obsolete graph assertions at :434-481 may change with the superseded APIs; path, rename and zero-exemption tests must remain intact. D-119 authorizes this scheduling change, so the phase number on the existing test is not a prohibition on editing it.

### (d) Other phases' tests used as Verifies

The question's old references no longer describe these plan bytes. **None of the phase29 link, execution_boundary_compat, root MCP tool-schema or phase7_receipts tests is a Plan 1/2 Task Verify.** Their existing subjects were read:

| Old selected test | What HEAD actually tests | Current replacement |
|---|---|---|
| `phase29_link_value_absent_from_truth_is_refused`, `tests/phase29_limits.rs:660` | Planning publication's link rule | P1 Task 2: new `native_admission_validates_authority_and_allocation` (P1:278-290) |
| `legacy_record_snapshot_and_operation_receipt_encodings_round_trip_and_mix`, `tests/execution_boundary_compat.rs:272` | Historical codecs and old BoundaryV1 append/reopen | P1 Task 3: new `native_admission_commits_versioned_extensions` (P1:313-326); P2 Task 1: new `native_task_records_replay_confirmed_events` (P2:406-417) |
| `tool_schemas_all_inputs_have_properties_without_root_unions`, `tests/mcp.rs:259` | Root schema properties and absence of root unions | P2 Task 2: new `native_runner_claims_before_spawn_and_replays_once` (P2:456-471); Task 3: new `native_owner_statements_bind_exact_inspection` (P2:491-502) |
| `exact_committed_and_null_head_staged_records_settle`, `tests/phase7_receipts.rs:120` | Constructed old risk-record material equality | P2 Task 4: new `native_material_includes_scoped_evidence_commits` with real Git/material (P2:527-541) |

Each replacement uses `cargo test -p cadence --lib execution::tests::<name> -- --exact` and tests the new increment's exposed production core or durable boundary with handwritten expectations. `execution/mod.rs:9-10` already declares that test module. P2:456-458, :491-493 and :527-529 explicitly place shared production cores in leased library modules rather than duplicating a binary-only service in tests. Constructed authority inputs in the unit tests are labelled accurately; C1-C7 alone claim public native approval boundaries. These implementation tests are explicitly outside the acceptance map (P1:110-114).

Two existing-test selections remain legitimate: P1 Task 5 changes the selected scheduling test itself, and P3 Task 5 changes the selected skill/tool compatibility test at `tests/mcp.rs:1134` to check the new skills' metadata and all five agents' permissions/references (P3:536-547). Neither is an unchanged regression guard standing in for missing verification. The suite task delivers C6 itself; it no longer repeats Plan 2's completed C2.

### (e) Rendered skills and agent metadata

The existing main.rs pattern supports the new project-free `executor-instructions` creation (P3:409-423). **The two skills are renderer outputs; the five agent files are directly edited metadata adapters.** P3:316-332, :413-418 and :520-527 explicitly state that distinction, correcting the older claim that the entrypoint emits all seven files. The agents' instruction source remains a reference to the compiled contract. Current `agents/cad-executor.md:4`, :8-13 demonstrates the permissions/reference seam; all five paths exist.

The generated outputs are exactly `skills/cad-executor-contract/SKILL.md` and `skills/cad-execute/SKILL.md`, matching amended D-115. `skills/cad-task/SKILL.md:25-30` still delegates to the frozen workflow and is not leased. C7 observes the actual dispatch; these rendered files are substantive artifacts, not substitute acceptance subjects. The final compatibility test verifies metadata/permissions, while actual host conduct remains O1.

## Sequencing, file completeness, proportionality and scope

Task counts are **5 / 7 / 5**, all within eight (P1:240-391; P2:382-646; P3:362-547). Frontmatter file counts are **20 / 22 / 27**. Every task-listed repository path belongs to its plan's frontmatter; all non-creation paths exist. The shared paths are `execution/mod.rs`, `execution/boundary.rs`, `execution_service.rs`, `store/writer.rs`, `store/transaction.rs`, `server.rs`, `recall/mod.rs` and `tests/phase12_execution.rs`. Thus all six files named in the dispatch are confirmed shared, with two additional shared leases. The user's explicit sequential-split instruction governs over the installed skill's generic no-shared-files rule.

The order **1, 2, 3** is executable (P1:395-404, P2:52, P3:56):

1. Plan 1 captures historical input before production changes, creates admission/allocation and immutable extension storage, supplies public initial authorization/admission plus C1, then updates numeric scheduling. C1 does not depend on a future Plan 2 owner API to invent its positive control: P1:73 and :350-357 explicitly require the initial real authorization adapter and bar legacy native closure.
2. Plan 2 consumes that completed basis. Task 1 creates history/receipts and the owner-classification record; Task 2 creates runner/service; Task 3 provides owner inspection; Task 4 adapts Git/material; Tasks 5/6 install both close gates, with custom classification created in Task 5 before C2 needs it; Task 7 integrates progress/checkpoints. The suite remains unavailable. No unidentified Plan 1 symbol is presumed: modules not created by Plan 1 are explicitly created in the same or an earlier Plan 2 task.
3. Plan 3 consumes the completed history/runner/owner/progress operations. It delivers continuation/C5, creates compiled instructions before C7, then delivers suite gating and C6 together before final host regeneration. Its references to history and runner are valid **Plan 2** prerequisites, not missing Plan 1 creations. The two close gates and the custom owner-classification path already exist before its checks use them.

Creation leases are complete: Plan 1 creates `execution/admission.rs`, `execution/allocation.rs`, `tests/phase12_execution.rs`, `tests/support/phase12_history.py` and `tests/fixtures/phase12_pre29_blank.json`; Plan 2 creates `execution/history.rs`, `execution/receipts.rs`, `execution/runner.rs` and `execution_runner_service.rs`; Plan 3 creates `execution/instructions.rs` and the executor-instructions entrypoint in leased main.rs. Module registration, public adapters, resident, writer and intent integration are all leased. Earlier-phase tests and existing skills/agents are edits. No specified work necessarily adds a third-party dependency, so no compulsory missing manifest/lockfile lease was identified. Temporary test repositories, signing material and output markers are fixture inputs, not unlisted product-source edits.

Proportionality was checked independently of coverage: the tasks follow coherent authority, persistence, runner, owner-input, material, close, continuation and rendering boundaries. The six implementation tests verify substantive increments without becoming additional acceptance items. The capture helper has one historical-input purpose. The suite task combines its gate and its one acceptance test; it does not split off another task verified by C2. No artificial task-count compression or new machinery to police the phase's own work was found.

No task delivers phases 13, 14, 15, 20, 21, 26 or 30: no verifier dispatch/verdict/status/waiver/cap, progress/landing/undo surface, filing/deferred completion, truth revision or plan-review edit persistence. Existing review/risk consumption is preserved without claiming review-fix continuation. D-107's ten parked promises and D-114's self-hosting prohibition remain explicit (P1:49, P2:56, P3:60, :540-560).

## Review identity and limits

Reviewed SHA-256 values:

- PLAN-1.md: `98dcf0023b475e273ffc30568a3dc639ec38c7677157b61adfeb2bc106991b58`
- PLAN-2.md: `2fc54d83916c44bc83381ca912564a87cab6200ed595d7e026d2cd2fce313312`
- PLAN-3.md: `fd633597aa872f8711fff0a000c60c6a47bfe530436d14db109fcf0695127cef`
- Earlier plan-check.md: `d3a2f922b068c979725ad90b740b1cf0be7da67229e1f14019dc345b020132c8`
- Earlier plan-check-2.md: `d09db6f90edfae529e0c2c4e273fb236e0b74f120e160e3ce057e41df3a18fd9`

Only this new report was written. No plan/source edit, cargo command, build/test, mutating Git command, native initialization or historical capture was performed. The reserved phase-27 temporary root and its lock were not accessed; `.codex-analysis/` was ignored. The earlier reports remain untouched.

This is a passing plan verdict: the revised plans can deliver the scoped goal under the amended decisions. It is not runtime certification. Actual fixture capture, implementation, exact test selection/results, artifact inspection and the unseen O1 episode remain subsequent work.
