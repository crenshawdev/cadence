Verified at 8253fe32993f9b60a9592d77aaf89fd88eebd384 on cadence/binary-owns-process

Verifier pass, 2026-09-10. Every check was run at HEAD by its plan's exact
`--exact` command, one at a time, in order C1..C7 (logs kept in the session
scratchpad; the literal `test result:` line is quoted per row). Red/green
commits were inspected with `git show --stat`, `git show` and
`git diff <red> <green> -- crates/cadence/tests/phase12_execution.rs`.
Nothing was committed, checked out, stashed or edited; the target lock was
never contended. `pilot.md` did not exist at any point during this pass.

Paths are repository-relative. `T` = crates/cadence/tests/phase12_execution.rs,
`S` = crates/cadence/src.

| item | truth(s) | kind | verdict | observed |
|---|---|---|---|---|
| P12-T1-C | T1 | check | **rejected** | Runs green at HEAD: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 4.21s` (1 selected). Body `T:1473-1599` is one function: real `cadence serve` over stdio (`T:22-49`), native truths via `context-submit` (`T:126-150`), attached typed maps via `plan-submit` (`T:313-357`), real git with `-c commit.gpgsign=false` (`T:359-364`); cases present: provisional/mapless (`T:1567-1571`), historical blank-command map restored under its lock (`T:1450-1470`, `T:1573-1589`), missing/omitted/unknown/artifact/stale/dropped/two-owner allocation (`T:1489-1510`), stale publication/content/map/occurrence bindings and PLAN-byte drift (`T:1511-1521`), mixed legacy set (`T:1522-1525`), competing replacement invalidates basis (`T:1528-1532`), admit/replay/extend/refuse stale-implicit-reassignment-replacement (`T:1533-1565`); each refusal reopens store+fs and compares bytes (`T:404-414`, `T:306-311`). Red commit 87b6b18a touches only `T` (+562) and is byte-identical to green a44a4a49 (diff empty). **Rejected because the recorded red is not a behavioral assertion**: the red failure is `error[E0425]: cannot find type NativeApply in module cadence::execution::boundary` at `T:449` (line `let _:cadence::execution::boundary::NativeApply=serde_json::from_value(valid.clone()).unwrap();`, confirmed at 87b6b18a; `NativeApply` absent at 87b6b18a^, present at a44a4a49 `S/execution/boundary.rs:17`). No admission-not-refused assertion was ever observed failing. The executor report cites a "compile-red exception" from its own dispatch; the verifier dispatch carries none. |
| P12-A-HISTORY | T1 | artifact | accepted | `python3 crates/cadence/tests/support/phase12_history.py --inspect` exits 0: `history: complete; commit=b353f09dba50db20992bd39ae52b0b331168d2f0; binary_sha256=3177488871b8...; root=/tmp/cadence-phase12-pre29-5b7de640; files=37; approved_publications=2; blank_command=1; valid_control=1`. Helper `phase12_history.py:95-188` builds the pinned commit from a `git archive` export into a temp target dir, drives real stdio (`:48-83`), captures after shutdown, holds `LOCK_EX|LOCK_NB` on the sibling lock (`:126`), refuses a pre-existing root (`:127`), removes only the owned root (`:186-187`); inspect mode (`:190-238`) checks provenance, archive sha, file hashes, exact root binding, blank command `""` on `check/historical` and `custom-delivery-check` on `check/control`. Fixture keys: schema/root/owner/lock/provenance/transcript/environment/files; transcript ops context-submit, plan-read x2, plan-submit. Phase-27 root/lock not referenced. |
| P12-A-CONTRACT | T1 | artifact | accepted | `S/execution/admission.rs:174-238` `validate`: nonempty approved versioned truths equal to retained slots (`:178-188`), current occurrence (`:189-193`), mixed legacy PLAN refusal (`:195-201`), per-binding publication_request/content_revision/map_revision (`:203-217`), whole-union coverage (`:218-220`), `validate_retained` + `checked_map` + installed-byte digest + `parse_plan` (`:225-236`), `associations::validate_union` and `allocation::validate` (`:237-238`). `decode` locates schema field before serde (`:130-171`). `S/execution/allocation.rs:23-65`: every task assigned once, empty legal, unknown item/kind/stale revision/two owners/unowned check refused with slot. No readiness flag, test-existence gate or preset gate found. |
| P12-A-BASIS | T1 | artifact | accepted | `S/execution/admission.rs:47-53` `Record{schema,root_binding,set_version,request_digest,request}`; `replay` refuses reused id with changed payload/root (`:66-75`); `contribute` validates against the committing snapshot (`:77-116`). Writer `S/store/writer.rs:740-757` `native_admission` replays, reads the installed inventory, persists with `IntentKind::NativeAdmissionV1`; intent revalidation recomputes `contribute` from the prior snapshot and compares bytes (`S/store/transaction.rs:357-376`). `require_execution_ready` now validates the retained contract (`S/plan/persistence.rs:52-60`) at `writer.rs:778`, `transaction.rs:829`, `execution_service.rs:271`. |
| P12-A-EXTENSION | T1 | artifact | accepted | `admission.rs:87-105`: extension must keep same root/occurrence, preserve every admitted binding (`admitted-plan`), preserve every assignment (`admission-reassignment`), add a new identity (`admission-extension`); records append, originals untouched (`:106-113`). `S/plan/validation.rs:56-57,74-78` refuses replacement of any admitted identity via `admission::records`. C1 asserts original record bytes and `execution` basis unchanged after extension (`T:1554-1558`). |
| P12-A-ORDER | T1 | artifact | accepted | `S/execution/plan.rs:496-519` `PlanGraph` holds only plan numbers; `next_ready` is the first incomplete number; no overlap/prerequisite/pairwise code in `plan.rs`, `dispatch.rs` or `execution_service.rs` (grep). `lease::covers` still enforced at `plan.rs:172`, `patch.rs:273`, `receipts.rs:456,467`, `boundary.rs:397`. `crates/cadence/tests/phase7_lease.rs:477-495` asserts `[1]`, `[2]`, `[3]`, empty for both disjoint and overlapping leases. |
| P12-T2-C | T2 | check | **rejected** | Runs green at HEAD: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 36.24s` (1 selected). Body `T:636-743` one function: real Python unittest subject through the real runner (`T:476-560`), red `6 != 7`/`FAILED (failures=1)` and green `OK` asserted on retained bytes with `{"class":"results-observed",...}` (`T:641-651`); thirteen invalid pairs (`T:653-672`), dirty source (`T:674-679`), changed test bytes (`T:681-685`), replay/reuse/duplicate (`T:686-691`), empty allocation (`T:696-702`), setup-error `errors=1` never red and owner classification refused (`T:704-710`), missing script Unknown, out-of-lease and rename refusals (`T:711-714`), custom Unknown pair needing both owner classifications (`T:716-740`); observation enum has only `Unknown` and `ResultsObserved` (`S/execution/receipts.rs:44-48`); red-eligibility `failures>0 && errors==0`, cargo FAILED red-eligible (`receipts.rs:274-287`). Each refusal exits the server and reopens store+fs (`T:627-633`). Red c130ab3a touches only `T` (+284), byte-identical to green 78345c52. **Rejected because the recorded red is not a behavioral assertion**: red failure is `error[E0425]: cannot find type CloseApply in module cadence::execution::receipts` at `T:591` (confirmed at c130ab3a; `CloseApply` absent at c130ab3a^). No close-accepted-without-red-then-green assertion was observed failing. |
| P12-A-RECEIPTS | T2 | artifact | accepted | `S/execution/receipts.rs:8-66` Material/Launch/Disposition/Capture/Observation/Summary/RunResult; `validate_pairs:289-332` requires distinct resolvable commits, launch before green, red result observed before green launch, same test file/digest/command, `merge-base --is-ancestor` red->green->completion, test bytes and tree re-read from git at both commits; expected set from admission (`allocated:246-250`), never the caller list (`:324-327`). `run_eligible:274-287` implements the D-109 clarification; Unknown needs an owner classification bound to output identity and revision (`validate_classification:252-272`). |
| P12-A-RUNS | T2, T4 | artifact | accepted | `S/execution/runner.rs:140-197` `launch`: replay returns the prior record without a child (`:143-150`), command must be in the admitted task's `verify` (`named-command`, `:156-158`), material captured on a clean tree before the claim (`clean:104-109`, `material:111-117`), claim appended (`:183`) before `tokio::spawn` (`:185`); `observe_child:264-306` rechecks material pre-spawn, PDEATHSIG, captures both streams with 64 KiB bound and digests (`capture:250-262`), rechecks material after; `classify:308-337` recognizes only `test result:` and `Ran N test(s)` + `OK`/`FAILED (...)`, else Unknown. `S/execution_runner_service.rs` (106 lines) adapts the same core. |
| P12-T3-C | T3 | check | accepted | `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 17.27s` (1 selected). Body `T:746-810` one function: absent (`:748`), false (`:749-750`), stale revision/material/evidence and executor-only (`:751-760`), affirmative superseding positive control with exact record readback and replay (`:761-765`), two-check task with one attestation (`:765`), partial inspection (`:768-771`), close once then duplicate refused (`:773-776`), no-check task (`:778-781`), stale owner after fresh material (`:785-809`). Red 1c74553f touches only `T` (+67), byte-identical to green 531e8d97; recorded red `left: String("ok") / right: "refused"` at `T:580:39` is the `close_refused` assertion (confirmed at 1c74553f): a close accepted without the attestation. |
| P12-A-ATTESTATION | T3 | artifact | accepted | `receipts.rs:77-102` Inspection/OwnerApproval/OwnerStatement (attributed, timed, echoing payload); `validate_approval:141-143`; `validate_inspection:145-172` binds check revision, test digest and retained launch/result evidence, links supersession; `owner_eligible:174-178` requires `no_subject_stub` true and exact bindings; `validate_close_owner:358-378` refuses with `owner-attestation` naming missing checks, executor assertions explicitly excluded. Contract text states the weaker gate (`skills/cad-executor-contract/SKILL.md:80-81`). |
| P12-T4-C | T4 | check | **rejected** | Runs green at HEAD: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 7.54s` (1 selected). Body `T:819-919` one function: A closed, B started, real failed child (`:830-839`), progress/deviation/failed-attempt (`:840-845`), checkpoint and Stop (`:846-857`), server killed and reaped (`:859-860`), bytes compared after reopen (`:861`), fresh server reads A complete/B partial/C unstarted and checkpoint+gate history (`:862-869`), replays (`:870-872`), lost reply observed on disk then kill/replay (`:875-886`), conflicting/stale/duplicate refused (`:887-889`), Stop overwrite refused (`:890-893`), unacknowledged commit uncertainty (`:896-898`), hanging launch killed via real child handshake and retained Unknown (`:899-912`), handwritten B event order (`:911-913`). Red cf06049e touches only `T` (+113/-2), byte-identical to green 7c61bd27. **Rejected because the recorded red is not a behavioral assertion**: red failure is `error[E0425]: cannot find type ProgressApply in module cadence::execution::history` at `T:782` (confirmed at cf06049e; `ProgressApply` absent at cf06049e^). No progress-lost-across-restart assertion was observed failing. |
| P12-A-PROGRESS | T4 | artifact | accepted | `S/execution/history.rs:12-62` Task/Event/Request/Record/Projection; `contribute:161-271` replay first, admission and allocation binding, `task-completed`/`task-version` refusals, attempt linkage echoing allocation, Close via `validate_close`, checkpoint answers require owner+time and go through `checkpoint_projection`, progress requires text+commit, result must match launch and `classify`; raw events appended (`:255-260`), projection separate (`project:74-90`). Checkpoint integration in `S/evidence/checkpoint.rs`, `gates.rs` per 7c61bd27 stat. |
| P12-A-MATERIAL | T2, T4 | artifact | accepted | `receipts.rs:441-479` `observe_source`/`reobserve_source`: every evidence commit and the completion must be full ids after base, ancestors of HEAD, all paths `lease::covers` (no exemption), `verify-commit` on the completion, staged paths covered, re-observed at commit; `read_name_status:380-403` NUL protocol keeps both rename endpoints. `S/rail/risk.rs:452-499` `NativeExecutionBasis` keyed by dispatch/task/source/request digest, projected at close (`history.rs:266-269`). C2 asserts out-of-lease and rename refusals name `outside.txt` (`T:712-714`). |
| P12-T5-C | T5 | check | accepted | `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 9.07s` (1 selected). Body `T:974-1077` one function: Stop refuses dispatch after restart (`:987-991`), declined/unlinked/unknown/blank authorizations (`:994-1003`), linked resume (`:1006-1012`), server killed then real `execute-next` (`:1013-1016`); handwritten `["B","C"]` asserted on `dispatch.tasks` and on the operational input parsed out of the returned prompt (`:1017-1020`, `operational:946-951`), allocation compared with the reopened admission record (`:979-982`, `:1021`), A only in `completed` (`:1025-1027`), replay identity retained in decisions (`:1033-1037`), duplicate close (`:1039`), reconciliation-required then acknowledged (`:1043-1052`), gap extension preserves A bytes and ownership (`:1055-1076`). Red 90507acb touches only `T` (+160), byte-identical to green 4a9bc7cb; recorded red at `T:942` is `assert_eq!(refused["status"],"refused","the retained Stop must prevent executor dispatch; a dispatch here re-schedules completed A...")` (confirmed): a completed task dispatched again. |
| P12-T5-L | T5 | link | accepted | Traced: `S/execution_service.rs:1108-1123` splits `completed_view` tasks into `completed` and pushes only unfinished views into `tasks`/`executable`; `native_dispatch` sets `dispatch.tasks = executable` (`S/execution/dispatch.rs:176-193`); `render_native_prompt` places that operational object in the prompt (`S/execution/render.rs:157-176`); C5 observes `["B","C"]` in both `dispatch.tasks` and the prompt's operational input (`T:1017-1020`) against the handwritten list. |
| P12-A-CONTINUATION | T5 | artifact | accepted | `S/next_action/continuation.rs:153-169` a Stop stays in force until a later Progress authorization names the same checkpoint (restart and unlinked approval never continue it); `S/execution_service.rs:499-500` native phases skip active-dispatch replay until continuation is authorized (`checked_continuation:2472`, refusal code `continuation-refusal:662`); `reconciliation-required` at `:1116` with the task named; resumed dispatch id is a linked digest over the admitted id and state (`dispatch.rs:180-190`). |
| P12-T6-C | T6 | check | accepted | `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 116.55s` (1 selected). Body `T:1280-1447` one function, six real fixtures: task-named marker commands leave handwritten `A\nA\nB\nC\n` with a repeated repair distinct (`:1283`), no suite before the last task (`:1284`), one suite process after it (`:1286-1287`), replay without a marker (`:1294-1296`), second suite `suite-once` (`:1298`), caller command/CI result/unnamed command refused (`:1299-1307`), attestation refused for a recognized result `suite-results-observed` (`:1309`), `risk-pending` then real `risk-check` settlement, completion, `plan-completed` (`:1311-1320`), restart and reopened history in handwritten launch order with exact commands, 64 KiB bound, digests, completeness (`:1322-1341`); dead launch via child handshake, kill, Unknown with no `observed_at`, replay, `suite-early`, `suite-once`, `suite-unknown`, wrong-launch/wrong-bytes/executor-role attestations refused, one typed operator confirmation accepted and retained, replay does not consume budget, `suite-relaunch-budget`, one relaunch, both launches retained (`:1345-1377`); recognized cargo failure and unittest `errors=1` refuse the attestation outright and completion (`suite-failed`), gap plan admitted at its own identity with originals byte-exact (`:1382-1412`); custom failure text Unknown, no auto completion/relaunch (`:1416-1421`); startup text Unknown, attested, one relaunch, original class/bytes retained (`:1424-1446`). Reds 65fe159a (+301/-4), a6e02a3e (+21/-19), 6578540b (+1/-1) each touch only `T`; `git diff 6578540b 0ae44c6e -- T` is empty; all three recorded failures are the same assertion text `the suite must be available after the last task is acknowledged` (`T:1278` at 65fe159a, `T:1227` in `suite_run` at a6e02a3e and 6578540b, confirmed) with `unknown variant execution-suite`: a second/first suite launch not accepted because no lifecycle existed. |
| P12-A-SUITE | T6 | artifact | accepted | `runner.rs:207-247` `suite_launch` replays, claims before spawn, observes; `history.rs:543-680` `plan_contribute`: `plan-completed`, `suite-failed` naming the D-120 gap path, `suite-early` listing unfinished tasks, `suite-once` for a recognized result and for an unconfirmed Unknown, `suite-results-observed` refusing the attestation, `suite-relaunch-budget` (exactly one), `suite-unknown` for missing/unrecognized results, `risk-pending` requiring the native risk basis; `suite_failed/suite_passed:426-437` use only the two classes. Retention of both launches and the confirmation shown by C6 (`T:1374-1377`). |
| P12-T7-C | T7 | check | accepted | `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 6.93s` (1 selected). Body `T:1085-1181` one function: conflicting global command through real `config-apply` on a private fixture global file (`:1079-1082`, `T:23-28`, `T:479`), A closed, Stop, linked resume, kill, real `execute-next` (`:1086-1093`); the REAL dispatch prompt is parsed (`operational:946-951`), not Markdown or a renderer; handwritten `DISPATCH_SPEC` (`T:428-432`) asserted as the admitted check with revision from the reopened map record (`:1101-1104`); B current state, checkpoint answer, A history, continuation link (`:1107-1113`); admitted suite/lease/precedence, configured command shown as provenance only, unknown-language warning (`:1116-1123`); twenty-one handwritten instruction phrases including the Executor block's sentences and the four classical clauses, D-111 weaker gate, launch-claim, Stop, once/relaunch/attestation refusal, wrapper limit, CI, no guessed runner, no style gate (`:1127-1136`); override sentence confined to the delimited body (`:1137-1139`); exact replay with retained `prompt_bytes` (`:1141-1145`); fresh linked dispatch after progress (`:1147-1152`); later config change cannot replace admitted commands (`:1154-1158`); blank verify refused at publication (`:1161-1165`); reopened close and gate records match the response (`:1167-1180`). Red 285fdba0 touches only `T` (+121/-5), byte-identical to green ed372ced; recorded red at `T:1076` is `assert_eq!(ops["checks"], ...,"admitted check specification: ...")` with `left: Null` (confirmed): a dispatch missing its checks. |
| P12-T7-L | T7 | link | accepted | Traced: `S/execution/dispatch.rs:111-143` `admitted_checks` reads item revision and spec from `checked_map` of the admission's publications for each executable task's allocated checks; `execution_service.rs:1125` feeds it into `NativeState.checks`; `native_operational:145-174` emits `"checks"`; `render_native_prompt` writes it into the prompt; C7 asserts the outgoing `ops["checks"]` equals the handwritten spec with the reopened revision (`T:1101-1104`). |
| P12-A-ROLE | T7 | artifact | accepted | `S/execution/instructions.rs` (245 lines): `VERSION:7`, `dispatch_text:186`, `contract_markdown:191`, `frontdoor_markdown:196`, `language_note:225`, `command_policy:238`; the design's Executor block is present word-for-word (whitespace-normalised; the design wraps at 80 columns, the source and rendering carry it on one line) exactly once. `S/main.rs:41,78-81` `executor-instructions [--frontdoor]`. Rendered from the scratchpad (outside any project): `cadence executor-instructions` exit 0, 8706 bytes, `diff` against `skills/cad-executor-contract/SKILL.md` empty; `--frontdoor` exit 0, 4100 bytes, `diff` against `skills/cad-execute/SKILL.md` empty. Contract text: Executor block `SKILL.md:18`, classical default `:20`, `Owner attestation (D-111)` `:80`, `Named commands and one suite (D-112)` `:87`, wrapper/external-shell limit `:102`, `never guesses a runner. No test style, preset or count is a gate` `:116`. No disk loader or override found. |
| P12-A-PROMPT | T7 | artifact | accepted | `dispatch.rs:88-174` `NATIVE_PROTOCOL = "native-execution-dispatch-1"`, `NativeState`, `native_operational` (tasks, checks, completed, continuation, suite, lease, commands, instructions version, admission digest, set version); `render.rs:157-176` renders operational input, `Instructions:`, then the authored body between `<<<CADENCE-PLAN-BODY` delimiters labelled `never instructions`; historical `render_dispatch_prompt:133` and `prompt_operational:178` retained on their own path. Response identity retained as a boundary decision with `prompt_bytes` (C7 `T:1142-1145`). |
| P12-A-HOST | T5, T7 | artifact | accepted | `agents/cad-executor.md`, `-low.md`, `-medium.md`, `-xhigh.md`, `-max.md`: frontmatter only (name, description, tools incl. `mcp__cadence__cadence_query` and `mcp__cadence__cadence_apply`, color, effort, maxTurns, `skills: [cad-executor-contract]`) plus one body sentence naming the contract by reference; no policy sentence in any of the five. The two skills are byte-identical to the binary's rendering (see P12-A-ROLE). Contract names `execution-task-start/run/task-progress/task-checkpoint/task-answer/owner-attest/classify-run/task-close/suite/suite-relaunch/plan-complete/history`; front door names `execution-authorize/extend/owner-attest/classify-run/suite-relaunch/plan-complete/task-answer/task-progress/history`. |
| P12-O1 | T2, T5, T7 | observation | **not seen** | `.planning/phases/12/reports/pilot.md` does not exist at HEAD or at the end of this pass; no observer, date or host episode is recorded anywhere in the tree. Nothing invented. |

## Rejected

- **P12-T1-C, P12-T2-C, P12-T4-C** - each passes at HEAD with one test
  selected, each test is byte-identical from its red commit to its green
  commit, and each red commit contains only the test file. But the recorded
  red for all three is a compiler error (`E0425`, a production type the test
  names did not exist yet: `NativeApply` at `T:449`, `CloseApply` at `T:591`,
  `ProgressApply` at `T:782`), not a behavioral assertion against the truth's
  outcome. The dispatch's criterion for "could have failed" requires the
  latter and names compile errors as insufficient; the executor reports for
  PLAN-1 and PLAN-2 invoke a "compile-red exception" from the executor's own
  dispatch, which the verifier's dispatch does not carry. Everything else
  about these three checks (real boundary, one function, handwritten
  expectations, stop-reap-reopen before byte comparison, the named cases)
  was confirmed as accepted-grade; the rejection rests on this single
  criterion and is reversible by the orchestrator if that exception applies.

## Not seen

- **P12-O1** - live-host observation; no `pilot.md`.

## Notes that did not change a verdict

- Tests use `cadence::store::model::digest` (`S/store/model.rs:120-122`,
  plain SHA-256) to check that retained digests match retained bytes, and
  take item revisions from the reopened phase-28 map records; CONTEXT names
  those revisions as permitted expected values and the hash is not the code
  under test.
- The Executor block in the rendering is the design's words on one line; the
  design wraps it at 80 columns.

## Orchestrator adjudication (2026-09-10, reversible by the owner)

- **P12-T1-C, P12-T2-C, P12-T4-C: ACCEPTED over the rejection above.** The
  rejection rests on the verifier dispatch's sentence "not a compile error in
  the test itself", which the design does not carry. `docs/architecture/
  acceptance.md:199-206` defines red as the recorded failure of a test written
  before the code and says why: a failure you watched is the only proof the
  test reaches the code. It inverts the old rule 6 ("do not write a test for
  code that does not exist"), so a test naming a production type that does
  not yet exist is the design's normal case, not an exception. D-109 excludes
  only "a tool or setup failure" from counting as a behavioral red; a missing
  production type is neither. All three executor dispatches (PLAN-1, PLAN-2,
  PLAN-3) carried the same rule: a compile failure counts as red only if the
  test is complete and the failure is the missing production code. The
  verifier confirmed everything else about the three checks: test-only red
  commits, byte identity red to green, real boundary, one function,
  handwritten expectations, stop-reap-reopen before byte comparison, every
  named case present, one test selected and passing at HEAD.
- Net: accepted 24 / rejected 0 / not seen 1 (P12-O1, pending the pilot).
