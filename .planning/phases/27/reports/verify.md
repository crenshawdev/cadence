Verified at 1372ed11 on cadence/binary-owns-process

Verifier inspection on 2026-09-10. Paths below are relative to `/code/cadence`; `src/` and `tests/` abbreviate `crates/cadence/src/` and `crates/cadence/tests/`. Tracked working-tree files matched HEAD. C1, C5 and C7 were rerun by their exact commands at 1372ed11; C2, C3, C4 and C6 retain the passing runs at 8d30609b recorded below. No full suite was run. Counts: **19 accepted / 0 rejected / 2 not seen** (21 mapped items; the two observation items share O1). No truth status is assigned here.

Common check inspection: `tests/phase27_plan.rs:23` launches the actual `cadence serve --project-root` binary with isolated global configuration and stdio MCP initialization; `:55` sends real public tool calls; `:109` establishes native approved truths through `context-submit`. Expected bodies, numeric identities, structural fields and refusal rules are handwritten. Preview values are caller inputs, not the numeric oracle; original receipts/revisions are compared across retries as the specified historical-equality property, without calling the allocator or renderer to generate expected output. `:78` closes stdin and waits for server exit; `:136` parses reopened state and both actual JSONL files; `:146` reopens directory/file trees; `:178` checks journal absence before reopening real `Store`/`Filesystem` and requesting `ReadVerified`. Refusal/retained-intent comparisons use read-only files. The permitted owner gate/answer setup at `:229` uses real policy, `SessionFactory` and `Session::commit_evidence` at `:254`; it does not seed execution admission. The legacy control at `:859` requires public `execute-next` to dispatch and then reopens its admission record. The three reruns now complete that setup and their real execution-admission controls.

Inspected `git show 1372ed11`: its only changed file is `crates/cadence/tests/phase27_plan.rs`. The Git helper at `:193` now sets `commit.gpgsign=false` and its own user identity on every fixture call; the fixture commit no longer requests `-S` or a signing key. The full diff and byte comparison from the first test onward confirm that check setup data, public calls, expected results, and admission/reopen helpers are unchanged. All production artifacts remain byte-identical to the inspected 8d30609b versions; their prior verdicts and source anchors remain applicable. Historical red-commit line numbers below refer to those commits.

Original environment failure: at 8d30609b, C1/C5/C7 failed during signed fixture commits because the sandbox GPG home was read-only; all three now pass with fixture signing disabled.

| Item id | Truth(s) | Kind | Verdict | What I observed |
|---|---|---|---|---|
| P27-T1-C (C1) | T1 | check | accepted | Ran at 1372ed11: `cargo test -p cadence --test phase27_plan phase27_approved_plan_is_published_at_returned_identity -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 1.03s`. The complete check at `tests/phase27_plan.rs:262` passed through real stdio MCP and native approved truths: handwritten Unicode/line-ending bodies, draft/changed-approval and native-membership controls, exact publication/readback, and post-exit filesystem/store reopen. Public execute-next returns the named provisional-authoring refusal without dispatch at `:311`; the independently authored legacy control at `:416` obtains an actual dispatch and reopens its admission at `:440`, establishing the otherwise-dispatchable fixture. Reopened PLAN parsing, approval, structure/body, occurrence, revision/history, prior data and journal assertions are at `:324`. Reopened `git show 966290b7`: only the new test file and its helpers were committed. The recorded red assertion at then-line 294 (`.planning/phases/27/reports/plan-1.md:14`) expects status ok, but `966290b7:src/plan_service.rs:115` returns publication-unavailable. This is a genuine outcome assertion failure, not a test compilation failure; no subject boundary is faked. |
| P27-T1-A1 | T1 | artifact | accepted | `src/plan/model.rs:25` stores strict typed content and preserved body; `:90` stores identity, occurrence, revision, approval, readiness and history; `:108` retains full historical publications in receipts. `src/plan/persistence.rs:154` renders and round-trips approved content, hashes its document, appends revision history and retains publication/approval at `:160`; receipts are retained at `:181`. `src/plan/render.rs:8` emits only strict frontmatter fields, appends the exact body at `:18`, and invokes the native reader at `:20`; metadata is outside frontmatter. |
| P27-T1-A2 | T1 | artifact | accepted | `Writer::execute_store` in `src/store/writer.rs:593` recognizes only scoped same-phase PLAN participants, checks expected old documents at `:601`, reads current inventory at `:639`, validates the complete approved publication at `:653`, and selects `IntentKind::PlanPublication` at `:661`. `src/store/transaction.rs:14` defines that participant kind; `Intent::validate` checks its target set at `:194`, unchanged unrelated records at `:279`, old bytes at `:308` and semantic publication at `:312`. `src/plan/persistence.rs:213` reconstructs the approved namespace/result/document delta and compares all participants. Both commit (`src/store/transaction.rs:939`) and recovery (`:1004`) invoke intent validation; commit confirms installed bytes and removes the intent before returning (`:978`, `:992`), and recovery confirms/resyncs then removes it (`:1014`). |
| P27-T1-A3 | T1 | artifact | accepted | Public contracts exist in `src/plan/model.rs:60`, `:71`, `:80`, `:145` and `src/server.rs:239`, `:278`. `src/server.rs:403` binds the canonical project root; query/apply route that root at `:629` and `:820` through the resident mailbox (`src/recall/mod.rs:404`, `:553`). `src/plan_service.rs:23` reads existing verified bytes; preview/readback at `:29` is read-only, with native phase membership at `:38`, non-reserving targets at `:76` and provisional readback at `:86`. Draft return at `:128`, exact approval at `:134`, bound native truths at `:143`, and read-only contribution validation at `:152` precede `first_touch` at `:166` and every writer request. The read helper at `src/context/persistence.rs:110` only opens actual state/JSONL bytes. |
| P27-T1-A4 | T1 | artifact | accepted | D-88's concrete mechanical admission check is `cadence::plan::persistence::require_execution_ready` (`src/plan/persistence.rs:40`). It consults retained native publication identity and returns an identity-bearing `provisional-authoring` conflict independently of file/set fingerprints. `src/execution_service.rs:178` calls it before observing plans or returning/constructing dispatch; `:1435` repeats it on reobservation. Both writer admission paths call it against the current snapshot: `Writer::boundary_v1`'s `BoundaryChange::Dispatch` arm (`src/store/writer.rs:694`) and `Writer::admit_execution` (`:962`). Both dispatch intent validators repeat it (`src/store/transaction.rs:322`, `:725`), including recovery via `:1004`. This is executable refusal logic, not an instruction warning; absence of native publication records allows the separate legacy path. |
| P27-T1-O1 | T1 | observation | not seen | `.planning/phases/27/reports/pilot.md` did not exist at final inspection. No observer/date/result episode showing real-host planner publication, the deliberate typed identity refusal, and approved PLAN landing with no preapproval writes was available. The specification in `.planning/phases/27/PLAN-1.md:159` is not an observation. |
| P27-T2-C (C2) | T2 | check | accepted | Ran at 8d30609b (retained evidence; unchanged check and production code): `cargo test -p cadence --test phase27_plan phase27_identity_mismatch_is_refused -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.14s`. `tests/phase27_plan.rs:449` establishes native phases and an installed winner; `:463` submits wrong-phase/wrong-number content, asserts handwritten `identity-mismatch` and both identities, exits and compares reopened trees at `:482`; matching approval succeeds and reopens store/document at `:505`. `git show 2fe09deb` adds only this test. Its reported red at `:471` expected `identity-mismatch` but received `publication`; `2fe09deb:src/plan_service.rs:116` maps contribution errors to that generic rule. This is a genuine typed-outcome assertion failure. |
| P27-T2-A1 | T2 | artifact | accepted | `src/plan/model.rs:39` preserves distinct `target` and `content` identities with strict decoding. `src/plan/validation.rs:89` compares submitted, target and bound phase and returns `identity-mismatch` naming each. `src/plan_service.rs:112` applies that validation before persistence; `src/plan/persistence.rs:52` repeats it for approved contribution. `src/plan/render.rs:20` and `src/plan/persistence.rs:155` retain strict native-reader identity validation. |
| P27-T3-C (C3) | T3 | check | accepted | Ran at 8d30609b (retained evidence; unchanged check and production code): `cargo test -p cadence --test phase27_plan phase27_out_of_phase_target_is_refused -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.12s`. `tests/phase27_plan.rs:524` uses handwritten outside/other-phase sentinels; `:537` tests traversal, absolute and other-phase destinations; `:563` creates real target and ancestor symlinks. Typed `path-confinement` names the attempted/unsafe path; post-exit trees and sentinels are unchanged. Safe canonical publication reopens store and parsed PLAN at `:609`. `git show 578957ff` adds only this test. Its reported red at `:547` received `submission` rather than `path-confinement`; `578957ff:src/plan_service.rs:83` strictly decodes directly and returns that generic rule at `:89`, losing the required path-specific refusal. This is a genuine assertion, not a compile error in the test. |
| P27-T3-A1 | T3 | artifact | accepted | `src/store/filesystem.rs:102` maps typed PLAN targets to canonical phase/number paths; `:264` parses their positive canonical identities using `:276`. `validate_plan_path`/`safe_plan_path` (`:189`, `:198`) reject unsafe targets/ancestors with path details. `read` validates first (`:338`), binds safe directories (`:356`), and `ensure_directory` (`:223`) rejects symlinks and syncs creation. `prepare` (`:431`) and `install` (`:477`) recheck bound parents; `confirm` (`:498`) rereads and compares bytes. `src/plan_service.rs:116`, `:280` expose confinement refusals; `src/plan/validation.rs:65` names forbidden raw destinations. `src/store/transaction.rs:169`, `:194` validate canonical same-phase intent targets and `:878` checks actual participants at commit/recovery. |
| P27-T4-C (C4) | T4 | check | accepted | Ran at 8d30609b (retained evidence; unchanged check and production code): `cargo test -p cadence --test phase27_plan phase27_multiple_plans_have_distinct_numeric_order -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.25s`. `tests/phase27_plan.rs:974` uses two actual stdio clients and occupied 1–8 inputs; expected batch `[9,10,11]`, numerical listing, body pairing and stale loser are handwritten. Both clients exit before final directory/store inspection (`:1038`, `:1044`, `:1066`); fresh 12, last-entry conflict with no partial publication, and maximum-u32 exhaustion controls follow at `:1092`, `:1111`, `:1144`. `git show 4063d460` adds only this test and its occupied-fixture helper. Reported red `:680` expected persisted true but got the disabled ordered-batch refusal; `4063d460:src/plan/persistence.rs:100` explicitly refuses more than one entry. Genuine outcome assertion. |
| P27-T4-A1 | T4 | artifact | accepted | `src/plan/inventory.rs:121` merges durable occupancy/high-water; `:152` never lowers it. `src/plan_service.rs:76` proposes checked numbers without reservation. `src/plan/persistence.rs:111` checks the inventory basis; `:136` allocates in approved entry order with checked arithmetic and duplicate/target checks, retaining high-water/results/receipt at `:174`, `:181`. `src/plan_service.rs:215` submits one conditional transaction for the complete batch. `src/store/writer.rs:539`, `:631` and `src/store/transaction.rs:194`, `:312` validate the current snapshot and scoped batch before installation. |
| P27-T5-C (C5) | T5 | check | accepted | Ran at 1372ed11: `cargo test -p cadence --test phase27_plan phase27_gap_plan_uses_previously_unused_identity -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.89s`. The complete check at `tests/phase27_plan.rs:875` passed: handwritten 9/10/11 identities, deleted native/report number retention, reopened directory/store and unchanged legacy history, independent/decimal phases. The real admitted-legacy helper at `:859` calls public execute-next at `:866`, requires dispatch and reopens admission at `:870`; deletion/gap publication preserves its consumed identity/history at `:938`. Alias/leading-zero/conflicting-frontmatter cases at `:952` return typed inventory refusals with explicit-resolution guidance and unchanged reopened trees. Reopened `git show e042cb36`: only C5 and its helpers were committed. The recorded red (`.planning/phases/27/reports/plan-2.md:13`) fails the public-response assertion at then-line 59 on a JSON-RPC internal error for ambiguous aliases; `e042cb36:src/plan_service.rs:142` propagates that inventory error from apply instead of returning the specified typed refusal. A genuine outcome assertion failure, not a compile error; execution admission, storage and publication remain real. |
| P27-T5-A1 | T5 | artifact | accepted | `src/plan/inventory.rs:29` reserves bare PLAN as 1 and numbered plan/report inputs; `:51` inventories files/reports and retains provenance, rejecting aliases/frontmatter conflicts at `:70`, `:95`. `:128` retains consumed native numbers and `:136` adds real execution active/outcome/receipt numbers. `src/plan/persistence.rs:170` preserves provenance, consumed identities and monotonic high-water in the approved transaction, leaving existing publications/receipts intact. `src/plan_service.rs:180`, `:215` uses the same contribution/conditional transaction for additional gap identities. |
| P27-T5-A2 | T5 | artifact | accepted | `src/plan/model.rs:90`, `:108`, `:115` define native publication identity/occurrence and occurrence-owned request receipts, retained publications, consumed numbers and provenance. `src/plan/persistence.rs:30` reuses the saved explicit `active-cycle:phase:<N>` occurrence; `:120` persists cycle/occurrence ownership, and `:158`, `:181` retain publication history and original results. No execution plan-set fingerprint defines this identity. `src/plan_service.rs:158` refuses a non-live cycle; migration remains deferred. |
| P27-T6-C (C6) | T6 | check | accepted | Ran at 8d30609b (retained evidence; unchanged check and production code): `cargo test -p cadence --test phase27_plan phase27_acknowledged_allocation_replays_original_identity -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.75s`. `tests/phase27_plan.rs:671` covers installed/newer-authorized/missing/drifted states with handwritten bodies and identities 1/2/3; same-connection acknowledgment retry at `:682`, real replacement at `:702`, restart retries at `:718`, and changed-payload refusal at `:732`. Final post-exit trees/snapshots/receipts/high-water, directory membership and body bytes are checked at `:728`, `:741`; historical revisions are compared to the original acknowledgment as the plan requires, not recomputed by a renderer. `git show 9a546b6f` adds only C6. Its reported red at `:684` received a stale-inventory refusal rather than replay true; `9a546b6f:src/plan/persistence.rs:96` checks that stale basis before any receipt replay. Genuine outcome assertion. |
| P27-T6-A1 | T6 | artifact | accepted | `src/plan/model.rs:108` stores exact payload digest and ordered full original publication results within occurrence receipts (`:124`). `src/plan/persistence.rs:73`, `:79` bind the approved payload/request/occurrence and return the saved receipt before contribution's allocation/preconditions (`:96`). `src/plan_service.rs:106` resolves replay before inventory and repeats under the writer view at `:171`; `:252` separately reports installed/newer-authorized/missing/drifted projections without writing. `src/plan/persistence.rs:213` reconstructs and checks the receipt/results as part of `src/store/transaction.rs:312` intent validation. |
| P27-T7-C (C7) | T7 | check | accepted | Ran at 1372ed11: `cargo test -p cadence --test phase27_plan phase27_unauthorized_replacement_is_refused -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 1.02s`. The complete check at `tests/phase27_plan.rs:759` passed: absent/declined/original/wrong-target/stale/changed-content authorization refusals, unchanged reopened files, exact authorized replacement, stale loser, and retained history/receipts at `:822`. Real public execution admission at `:866` is followed by admitted-plan replacement refusal at `:831`; occupied bare legacy alias refusal at `:844` preserves bytes and creates no canonical replacement. Expected rules/body/identity and revision relations are explicit; digest calls at `:837`, `:850` construct caller authorization inputs, not expected refusal results. Reopened `git show 3b6605af`: only C7 and its replacement-request helper were committed. The recorded red at then-line 710 (`.planning/phases/27/reports/plan-2.md:21`) expects replacement-authorization but receives submission for unknown replacement; `3b6605af:src/plan/model.rs:38` defines a strict Entry with only target/content. This is a genuine public-decoding outcome assertion, not a compile error in the test; no admission record is manufactured. |
| P27-T7-A1 | T7 | artifact | accepted | `src/plan/model.rs:48` represents exact replacement target, old revision/document, new content and identified approval. `src/plan/validation.rs:28` checks exact replacement and outer approval; `:37` refuses any execution admission, using active/outcome/receipt records at `:55`; `:40` refuses legacy/consumed aliases and `:45` compares old revision and actual observed bytes. `src/plan_service.rs:125` checks before first touch, then rechecks contribution at `:180` and expected old bytes at `:203`. `src/plan/persistence.rs:158`, `:181` retain history/original approvals/results; `:201`, `:213` validate old bytes and full delta. Writer (`src/store/writer.rs:601`, `:653`) and commit/recovery intent validation (`src/store/transaction.rs:308`, `:312`) repeat these checks. |
| P27-T7-A2 | T7 | artifact | accepted | `src/plan/instructions.rs:21` contains the design's verbatim substantive Planner block. Native-truth intake, opaque map/provisional readiness, exact preview/approval, replacement, replay and typed-refusal correction appear at `:31`, `:75`, `:92`, `:127`, `:148`, `:164`; `:187` forbids direct/frozen writers. `:210` renders the compiled role with the actual model contract. Registered by `src/plan/mod.rs:1`; `src/main.rs:36`, `:52` route directly to stdout without project/store opening. Opened generated `skills/cad-plan/SKILL.md`: provenance at `:16`, matching role/approval/replay guidance at `:20`, `:112`, `:126`, `:147`, and strict submission schema at `:207`; allowed tools at `:5` are read/question/query/apply only. Inspected as an artifact, not a generated-string acceptance test or proof of O1. |
| P27-T7-O1 | T7 | observation | not seen | Same O1 episode as P27-T1-O1. `.planning/phases/27/reports/pilot.md` did not exist at final inspection; no observer/date and the three specified live-host results were supplied. `.planning/phases/27/PLAN-2.md:203` supplies only the pending specification. |

Rejected items: none. Both O1 items remain not seen; no live observation was invented.
