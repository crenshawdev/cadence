Verified at 93a6a62e94d99eb91ae38250998af4f96da8ce4c on cadence/binary-owns-process

Verifier: Codex, 2026-09-10. Verdicts apply to the evidence items in phase 11 PLAN.md, interpreted with CONTEXT.md (including D-79) and docs/architecture/acceptance.md. No truth status is assigned here.

All seven commands below ran in /code/cadence at the stated HEAD and exited 0. No full suite was run. The inspected tracked source matched HEAD (`git diff --exit-code HEAD` was clean). Each cited red commit was opened with `git show --format=fuller --stat --patch`: each changes only crates/cadence/tests/phase11_context.rs, adding its named test and any supporting helpers, without production changes. Historical failures were traced against those commits' source and the report's assertion locations, not represented as freshly rerun failures.

Common check boundary: crates/cadence/tests/phase11_context.rs:17 launches the actual CARGO_BIN_EXE_cadence with `serve --project-root`, isolated global configuration and piped stdio; :32 initializes MCP; :53 sends tools/call and rejects transport/MCP errors. :89 and :234 create real temporary projects and real store fixtures. :71 closes stdin and waits for successful process exit. :128 reopens the planning tree, recording directory membership and file bytes; :146 parses a present snapshot against its actual items.jsonl/decisions.jsonl bytes and validates both record streams. No boundary spy, model call, validator-derived refusal expectation or renderer-derived sentence expectation appears in these checks. Fixture creation uses production store helpers; outcome expectations are handwritten submissions, literal sentences/rules/slots or previously captured bytes. C1's permitted changed-file entries in its tree comparison are separately checked for content before that membership comparison.

| Item | Truth(s) | Kind | Verdict | What I observed |
|---|---|---|---|---|
| C1 | T1 | check | accepted | Ran `cargo test -p cadence --test phase11_context phase11_approved_context_persists_truths_and_decisions -- --exact`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.43s`. Test at crates/cadence/tests/phase11_context.rs:295 uses seven handwritten sentences (:296), one-to-seven truth sets in both existing-empty and missing phase directories (:347), both kinds and all verbs. After server exit (:365), it reads the document's authored scope, Markdown/non-ASCII decisions, assumptions and exact ordered truth section (:367); reopens Store with Filesystem (:390); checks exact submission/approval and complete version-1 pending truth records (:406); preserves unrelated snapshot/JSONL bytes and checks membership for extra intent/temp files (:417). Red 802a715b adds only this test/helpers; its test :228 asserts approved status `ok`, while that commit's context_service.rs explicitly returns `publication-unavailable` for the approved request. The report's `refused` versus `ok` failure is a real outcome assertion, not a test compile error. |
| C2 | T2 | check | accepted | Ran `cargo test -p cadence --test phase11_context phase11_sentence_fault_names_rule_and_slot -- --exact`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.91s`. crates/cadence/tests/phase11_context.rs:465 supplies handwritten trigger, conjunction/list observer, verb and kind faults; :495 varies missing/empty/whitespace/null/non-string slots; :507 covers with/without approval. :453 checks typed status/code/rule/slot/phase/entry/id, and :525 exits/reopens for unchanged tree bytes. Red 71b65625 adds only the test/helper; its :279 expects `refused`, while its context_service.rs has no sentence validation and returns `ok` draft for the first malformed trigger. The reported failure matches this assertion and production path. |
| C3 | T3 | check | accepted | Ran `cargo test -p cadence --test phase11_context phase11_unobservable_attestation_is_refused -- --exact`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.26s`. crates/cadence/tests/phase11_context.rs:532 varies absent/false observable in three positions, with/without approval, keeping fixed_oracle true and supplying an internal outcome (:545). :565 expects literal `unobservable`/`observable` and affected identity; :572 accepts the identical sentence with true attestation as a nonpersisted control; :579 exits and reopens unchanged bytes. Red 67c99152 adds only this test; its validation.rs lacks the observable refusal, so the first draft reaches `ok` and fails the existing :279 `refused` assertion exactly as reported. This verifies D-79's attestation rule, not semantic classification of internals. |
| C4 | T4 | check | accepted | Ran `cargo test -p cadence --test phase11_context phase11_prose_oracle_attestation_is_refused -- --exact`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.26s`. crates/cadence/tests/phase11_context.rs:587 varies absent/false fixed_oracle in three positions with/without approval, keeps observable true, and names a fresh model-prose answer (:600). :620 asserts literal `prose-oracle`/`fixed_oracle` plus identity; :627 accepts the same text with true attestation only as a draft; :634 exits/reopens unchanged bytes. Red 0b61c4a8 adds only this test; its validation.rs checks observable but lacks fixed_oracle validation, explaining the reported `ok` versus `refused` assertion at :279. No model supplies the expected answer. |
| C5 | T5 | check | accepted | Ran `cargo test -p cadence --test phase11_context phase11_eighth_truth_requires_phase_split -- --exact`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.06s`. crates/cadence/tests/phase11_context.rs:642 creates eight unique valid truths and prior CONTEXT, both with/without approval. :669 asserts refused, `seven-truths`, `truths`, phase 11 and literal `split the phase`; :679 uses a seven-truth nonpersisted control; :686 exits/reopens the prior file/store/tree unchanged. Red 2fb54362 adds only this test; its validation.rs has no upper-count check, so the first eight-truth draft returns `ok` and fails its :401 refused assertion as reported. |
| C6 | T6 | check | accepted | Ran `cargo test -p cadence --test phase11_context phase11_identity_collision_is_refused -- --exact`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.25s`. crates/cadence/tests/phase11_context.rs:692 covers repeated truths, decisions within/between headings, and truth/decision collisions with/without approval, asserting exact full identity, slot and phase. :730 approves a first set, exits, captures the winner, then restarts with different content and one reused native identity at a time (:741), preserving winner bytes after each exit (:766). :774 checks exact retry; :788 permits spellings in phase 12 while retaining phase 11; :734 checks distinct T1/D-01. Red 9a8f628c adds only this test; its context_service.rs has no identity validation and accepts the first duplicate draft, producing the reported :279 `ok` versus `refused` assertion failure. |
| C7 | T7 | check | accepted | Ran `cargo test -p cadence --test phase11_context phase11_unapproved_context_changes_nothing -- --exact`. Literal result: `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 0.09s`. crates/cadence/tests/phase11_context.rs:162 covers prior CONTEXT/native store, missing phase directory/store, retained intent, intake-only exit and intake followed by missing/declined/incomplete approval. :178 requires supported intake; :205 requires incomplete approval refusal; :208 requires valid drafts reach supported authoring with persisted false. :192/:215 compare intermediate tree bytes; :218 exits and read-only reopens, preserving absent paths and intent. Red 09b252a2 creates only this test/helpers. Its test :54 asserts no MCP error; the historical server has no context-intake branch and routes unknown query arguments through refuse_raw to the execution fallback. The recorded -32603 `execution store or controlling policy is unavailable` is that runtime assertion failure on unsupported intake, not compilation failure or a missing test target. |
| A1 | T1 | artifact | accepted | crates/cadence/src/context/model.rs:8/:38 defines typed slots and complete authored submission; :50 approval carries owner/time/exact submission; :59/:75 provides both kinds and all five design statuses; :84 defines all eight truth fields; :95 retains slots/attestations beside approval and rendered records. crates/cadence/src/context/persistence.rs:8 requires approved=true, nonblank owner/time and exact submission equality; :20 writes phase/version 1/pattern/text/kind/pending; :31 retains submission and approval. :6/:38/:53 stores/reopens a dedicated context/context-1/phases namespace within snapshot data, preserving other data. model.rs:103 exposes only context-submit, with no truth-status transition operation. |
| A2 | T1 | artifact | accepted | crates/cadence/src/context/render.rs:4 renders slot sentences and :20 renders substantive authored sections in order. crates/cadence/src/context_service.rs:100 constructs the approved record, :130 contributes its snapshot, :136 couples rendered CONTEXT to one transaction, and :147 awaits CompareTransact before acknowledging. crates/cadence/src/store/writer.rs:575 validates the context participant against approved snapshot data, :607 assigns ContextPublication intent, and :1287/:1306/:1313 adds store files with state last and commits the journal. crates/cadence/src/store/transaction.rs:185 restricts context-intent participants and :275 validates approved document/snapshot correspondence during intent validation; :835/:903/:912/:923 publishes/confirms the intent and participants then removes the intent. :927/:935/:945 validates and resumes restart recovery, resyncing already-installed bytes. crates/cadence/src/store/filesystem.rs:102 binds the phase CONTEXT path; :171/:275 safely creates/syncs missing parents and binds their identities; :350/:376 syncs prepared files; :388 renames and syncs the directory; :406 confirms actual installed bytes; :416 resyncs recovery and :426 synchronizes intent removal. |
| A3 | T1, T7 | artifact | accepted | crates/cadence/src/context/instructions.rs:4 holds compiled role text; :17 forbids direct planning/legacy writes; :23 gives actual intake contract and owner interview; :47 assembles typed truths/decisions with D-79 limits; :72 obtains the two independent owner attestations; :110 requires exact full-set approval with actual owner/time; :124 ends without writing on missing/declined approval; :129 waits for confirmed persisted acknowledgment; :137 handles typed refusals and correction; :163 preserves the live-host limitation. :180 renders the role plus the compiled Apply schema without loading instructions from disk. crates/cadence/src/main.rs:34/:50 exposes that renderer directly, without opening a project/store. Opened skills/cad-context/SKILL.md:1/:14/:23/:109/:134/:175 as the rendered counterpart containing the role, procedure and schema; it invokes the public tools and no frozen writer. Acceptance is artifact/source-registration inspection, not a rendered-Markdown check or live-host observation. |
| A4 | T2 | artifact | accepted | crates/cadence/src/context/validation.rs:33/:44 checks required typed sentence slots; :56 trigger alternatives, :63 observer conjunction/list forms, :73 allowed verbs and :80 kinds; :34 returns structured affected-entry/identity information. crates/cadence/src/context/model.rs:8 defines typed slots and :111/:143 defines structured context answers with ok/refused/unknown/not-applicable vocabulary and rule/slot/phase/entry/id. crates/cadence/src/server.rs:236/:268/:279/:290 registers context input/output types; :360/:385 derives the input schemas; :591 decodes intake and :780 routes raw context-submit before generic apply fallback. crates/cadence/src/context_service.rs:37 validates raw slots before serde decoding, preserving specific slot refusals as successful structured MCP results. |
| A5 | T3 | artifact | accepted | crates/cadence/src/context/model.rs:21 defines the independent per-truth observable attestation; crates/cadence/src/context/validation.rs:83 refuses any value other than true as `unobservable`, slot `observable`, with the affected truth from :34. No internal-name classifier is present. context/model.rs:95 and context/persistence.rs:8/:31 retain that attestation inside the exact submission and approval with owner/time. |
| A6 | T4 | artifact | accepted | crates/cadence/src/context/model.rs:24 defines independent fixed_oracle; crates/cadence/src/context/validation.rs:90 requires explicit true and returns `prose-oracle`/`fixed_oracle` with affected truth. It is not inferred from observable or approval and no model/keyword oracle is used. context/model.rs:95 and context/persistence.rs:8/:31 retain it in the exact approved submission with owner/time. |
| A7 | T5 | artifact | accepted | crates/cadence/src/context/validation.rs:23 refuses the whole set above seven with `seven-truths`, slot `truths`, and `at most seven truths: split the phase`; it does not truncate. crates/cadence/src/context_service.rs:37 executes this validator before any writer work, including before the approval branch at :81 and first_touch at :104. |
| A8 | T6 | artifact | accepted | crates/cadence/src/context/validation.rs:101 seeds one full-ID set from existing phase truths plus both decision headings; :124 chains all submitted families into that same scope; :154 refuses collisions with full ID/slot/entry/phase. crates/cadence/src/context/persistence.rs:38 selects only the requested phase and :53 rechecks identities before contributing records. crates/cadence/src/context_service.rs:57 reads native state without a writer, :63 recognizes exact retry, :66 checks occupied identities, :107/:127 repeats against the writer view, and :148 supplies snapshot generation/integrity preconditions. crates/cadence/src/store/writer.rs:524 rejects stale conditional transactions; context/persistence.rs:78 also binds publication/recovery to the prior phase-bound records. |
| A9 | T7 | artifact | accepted | crates/cadence/src/server.rs:591/:780 routes context intake/submission directly to the resident context path. crates/cadence/src/recall/mod.rs:538 queues its request and :399 executes context_service without eager first_touch. crates/cadence/src/context_service.rs:12/:26 performs read-only intake; :37 validates raw authoring input; :57 uses the read-only snapshot helper (context/persistence.rs:110, actual file reads without Store/recovery). context_service.rs:81/:82/:84 requires affirmative approval, owner/time and exact submission equality; only after those checks does :104 call first_touch and request a writer. :159 returns nonpersisted draft success. There is no refusal logging, recovery, token write or directory creation in the unapproved path. |
| O1 | T1, T7 | observation | not seen | .planning/phases/11/reports/pilot.md was absent when inspected during verification. No observer/date/seen record was available for the real-host interview reaching the binary, the deliberately malformed truth's typed conversational refusal, and approved publication with nothing written before approval. The deterministic checks and compiled instructions do not supply that observation. |

Rejected items: none.

Accepted: 16. Rejected: 0. Not seen: 1 (O1).
