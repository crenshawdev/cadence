VERIFICATION PASSED

## Findings by severity

**0 BLOCKER, 0 WARNING, 0 NOTE.** All nine prior findings are resolved. No new finding arose from the fresh review of all four plans. This verdict assesses the execution specification; it does not certify implementation, successful test execution, installation, retirement or adoption.

Reviewed 2026-09-11 in `/code/cadence`, branch `cadence/binary-owns-process`, HEAD `42cf984c0da3fd5ccf4a556e6f90c78e2a9e6725`. Read-only Git inspection confirmed all four plans are untracked and no tracked files differ from HEAD. The design, in-tree checker contract and contexts were read in the dispatch's order, and obligations derived before opening the plans. The revised plans were read in full, not diffed. The phase-12 reports supplied review precedent; the previous phase-13 report supplied the nine findings to resolve, not the current plan specification.

`P1:n`, `P2:n`, `P3:n`, `P4:n` below abbreviate `.planning/phases/13/PLAN-1.md:n`, `PLAN-2.md:n`, `PLAN-3.md:n`, `PLAN-4.md:n`. Other citations are repository-relative. Commands below are inspected specifications, not commands run during this check. The user's explicit sequential-overlap authorization governs instead of the checker contract's generic split-plan prohibition at `skills/cad-plan-checker-contract/SKILL.md:81`.

## Resolution of the nine prior findings

| Prior finding | Resolution and current evidence |
|---|---|
| **B1 — two Verify commands in the execution-usability task** | **RESOLVED.** `.planning/phases/13/PLAN-1.md:58` now contains one command for Task 5. At :328 the task extends the ONE existing `execution_calls_refuse_noninteger_phases_and_legacy_plans_without_dispatch` function to inspect compiled/generated instructions and drive real malformed-input and lifecycle-conflict refusals through restart/replay. Its sole Verify is at :337. It expressly prohibits invoking another test function. Task 4's metadata regression remains its own task (:301); the bounded usability task required by CONTEXT:31 remains intact. |
| **B2 — close requires native receipts with no producer for the rewrite checks** | **RESOLVED.** `.planning/phases/13/PLAN-4.md:299` assigns the seven outer implementation red/green SHAs and independent exact-command reruns to the orchestrator, with checkout, command, test/digest, full source/material, binary, output, selection/count and observer identities. At :304 these are explicitly ordinary observed records, not native verification-run receipts; :305 binds verifier item judgments to those outer subjects. At :308 native phase-13 rewrite approvals/attempts/completion are neither required nor manufactured. Disposable native receipts and copied-history identities are separately attributed at :311 and cannot substitute for the outer checks. Actual readiness remains required (:319, :392). |
| **W1 — unspecified link values and missing map reasons** | **RESOLVED.** `.planning/phases/13/PLAN-1.md:185` pins caller/callee and `spec.value: "binary-composed prompt"`; `.planning/phases/13/PLAN-4.md:154` pins caller/callee and `spec.value: "selected target"`. These match respectively T1's outcome and T6's trigger, without crossing slots (CONTEXT:94, :176; D-101 at `.planning/phases/29/CONTEXT.md:115`). Every map item now has an item reason and an explicit current-truth-version association reason: P1:135–195, P2:91–164, P3:114–180, P4:123–194. No semantic matcher, truth revision or extra check was introduced. |
| **W2 — hand-edited agents described as generated** | **RESOLVED.** `.planning/phases/13/PLAN-1.md:174` distinguishes the two generated verifier skills from the five directly edited metadata adapters. Task 4 repeats the distinction at :290; installation does too at `.planning/phases/13/PLAN-4.md:374`. This matches HEAD's separate generated-byte and adapter assertions at `crates/cadence/tests/mcp.rs:1135` and :1164. |
| **W3 — existing review selection module described as new** | **RESOLVED.** `.planning/phases/13/PLAN-4.md:143` says EDIT `review/selection.rs` and CREATE `review/instructions.rs`. Task 1 at :213 explicitly preserves `select_next`, `dispatch_roster`, `delivery_completion`, `attempt_usage` and module registration. These existing functions are at `crates/cadence/src/review/selection.rs:49`, :119, :143 and :87; registration is `review/mod.rs:23`. |
| **W4 — oversized combined completion/review/audit/close plan** | **RESOLVED.** `.planning/phases/13/PLAN-3.md:4` owns only T4/T5, with 32 exact file leases and five tasks (:5, :41). `.planning/phases/13/PLAN-4.md:4` owns only T6/T7, with 28 leases and three tasks (:5, :38). The former keeps publication, completion transaction and lifecycle tasks separate (P3:253, :279, :320); the latter contains review, audit and close (P4:202, :241, :277). Sequential dependencies and overlaps are explicit at P3:76, :364 and P4:66, :341. This is the requested substantive split, not task-count compression. |
| **W5 — retirement script in the retiring 3.x executable tree** | **RESOLVED.** `.planning/phases/13/PLAN-1.md:38` leases `.planning/phases/13/close/retire-rules-gate.py`. Its close-only, unshipped lifetime and exact-root/preimage constraints are explicit at :351–363. Rehearsal consumes that procedure (:366); actual close consumes the same path at `.planning/phases/13/PLAN-4.md:381`. D-134 authorizes this bounded procedure; no binary settings-deletion service or new product runtime is needed. |
| **W6 — close prose attached as unrelated truth evidence** | **RESOLVED.** `.planning/phases/13/PLAN-1.md:85` declares P13-CLOSE-HOOK mandatory close work outside the map; `.planning/phases/13/PLAN-4.md:69` does the same for P13-CLOSE-READINESS. The maps omit the old T1-A4 and T7-A2. Executor-authored procedures/rehearsal records, orchestrator-authored installed results and verifier inspection are explicit at P1:347 and P4:283, :365. Neither close identifier is an evidence-map item or observation. Final close is still required, not silently dropped. |
| **W7 — generated verifier bytes deferred beyond PLAN-2's suite** | **RESOLVED.** `.planning/phases/13/PLAN-2.md:20` leases both generated verifier skills; Task 1 includes them at :180 and requires same-task regeneration whenever its compiled schema/authority changes at :202–207. No refresh is deferred to PLAN-3. The same rule applies to later schema-changing tasks (P3:209, :243, :309, :351; P4:266), with their outputs leased. P4:322 explicitly refuses to use close as a deferred renderer repair. |

## Per-truth evidence table

All seven checks are specified as ONE top-level function in the creation-leased `crates/cadence/tests/phase13_verification.rs`. Each command names that function and uses `-- --exact`. Variants and controls are inside the same function. The accepted names and episodes remain those in `.planning/phases/13/CONTEXT.md:94` through :207.

| Truth | Plan / delivering task | Check | Other map items | Command |
|---|---|---|---|---|
| T1 — current verifier dispatch | PLAN-1 Task 2, P1:222 | P13-T1-C, `phase13_dispatch_carries_current_verification_inputs` (P1:135) | P13-T1-A1, A2, A3; P13-T1-L | `cargo test -p cadence --test phase13_verification phase13_dispatch_carries_current_verification_inputs -- --exact` |
| T2 — mismatched patch refusal | PLAN-2 Task 1, P2:172 | P13-T2-C, `phase13_mismatched_verdict_patch_is_refused` (P2:91) | P13-T2-A1, A2 | `cargo test -p cadence --test phase13_verification phase13_mismatched_verdict_patch_is_refused -- --exact` |
| T3 — all-item status derivation | PLAN-2 Task 2, P2:212 | P13-T3-C, `phase13_report_derives_truth_status_from_every_item` (P2:133) | P13-T3-A1 | `cargo test -p cadence --test phase13_verification phase13_report_derives_truth_status_from_every_item -- --exact` |
| T4 — owner waiver beside met | PLAN-3 Task 1, P3:188 | P13-T4-C, `phase13_owner_waiver_is_distinct_from_met` (P3:114) | P13-T4-A1 | `cargo test -p cadence --test phase13_verification phase13_owner_waiver_is_distinct_from_met -- --exact` |
| T5 — unfinished completion refusal | PLAN-3 Task 5, P3:320 | P13-T5-C, `phase13_incomplete_verification_cannot_complete_phase` (P3:141) | P13-T5-A1, A2 | `cargo test -p cadence --test phase13_verification phase13_incomplete_verification_cannot_complete_phase -- --exact` |
| T6 — selected review target and intent | PLAN-4 Task 1, P4:202 | P13-T6-C, `phase13_review_surface_selects_target_and_intent` (P4:123) | P13-T6-A1; P13-T6-L | `cargo test -p cadence --test phase13_verification phase13_review_surface_selects_target_and_intent -- --exact` |
| T7 — broken audit traces | PLAN-4 Task 2, P4:241 | P13-T7-C, `phase13_audit_reports_broken_verification_traces` (P4:168) | P13-T7-A1 | `cargo test -p cadence --test phase13_verification phase13_audit_reports_broken_verification_traces -- --exact` |

The four maps contain **7 checks, 11 artifacts, 2 links, 0 observations**. Requirements partition exactly T1 / T2–T3 / T4–T5 / T6–T7. No truth is delivered by a second plan's evidence map. Every item has a truth, an item reason and an association reason; every check has a test locator, setup, call, expected property, command and real boundary. The supporting regressions in the task lists are not additional acceptance-map checks (P1:130, P2:86, P3:110, P4:118).

## Independent goal-backward obligations and planning refusals

The design requires observable delivery of the current verification inputs, refusal of foreign or stale evidence, reduction of all applicable item judgments, a distinct owner exception, refusal of incomplete acceptance, delivery through the existing reviewer, and truthful read-only trace reporting. Those are the seven approved owner episodes, not a set of functions to cover. The associated obligations are exact evidence identity, durable history, actual runs and handoffs, owner-only decisions, and preservation of human input. This follows from `docs/architecture/acceptance.md:137`, :147, :173, :225 and :251 and the binding decisions, independently of the plans' task breakdown.

The Planner block at `docs/architecture/acceptance.md:338` is satisfied by the one-check partition above and the narrow task commands below. No function/call-site census or suite/CI acceptance item appears. Artifact entries specify substantive role sources, retained records, reducers, projections and command surfaces needed by their truths; the separately required close prose has been removed from the maps. P1:182 and P4:138 explicitly keep rendered Markdown/provider fragment inspection as artifacts, not replacement acceptance subjects.

Both links are justified semantically and lexically. T1 names a prompt crossing from binary dispatch to verifier; T6 names the selected target crossing to reviewer. The exact pinned values are delimited contiguous phrases in a single approved slot, with case and punctuation unchanged (P1:185; P4:154; CONTEXT:94, :176). D-101's implementation resolves retained approved slots and never joins them (`crates/cadence/src/plan/limits.rs:195`, :222, :246). Passing its lexical rule permits the link; the actual outgoing handoffs in T1/T6 must still be traced. No additional links are owed for the internal reducer, transaction or audit join.

**There is no observation item anywhere in these four maps.** P2:139–148 uses an observation-kind item only in a separate generic fixture phase inside T3's ONE function, to exercise the `concerns` cap and rejected/not_seen precedence. That is the accepted T3 setup, not evidence about phase 13. It complies with the owner's empty Observations section at CONTEXT:404–406 and the design's 2026-09-11 ruling at `docs/architecture/acceptance.md:166`. Neither installed close report has been smuggled back as an observation.

## Check boundaries and falsifiability

The common setup in each plan (P1:101–131, P2:57–87, P3:81–111, P4:89–119) requires the real `CARGO_BIN_EXE_cadence` server over newline-delimited JSON-RPC stdio, a fresh disposable project, native exact-approved context, attached typed map publication, explicit admission/allocation and real execution evidence where needed. Tiny checks produce real committed red and green receipts and exact owner Inspection approvals; applicable task, suite and risk work is settled. Accepted check verdicts need independent public verification-run receipts. No fixture seeds approval/admission/verdict records or substitutes a reducer, fake process, fake store or model response for the boundary.

Git is real. Fixture invocations specify `-c commit.gpgsign=false` and fixture author identity, private configuration/signing inputs, and null stdin for child commands; required signed completion commits use the fixture's own key and explicit `-S` (P1:121–125, repeated in each plan). Server shutdown, reap, filesystem/store reopening and handwritten expected values are required before final durable assertions. Controlled caller payloads test authority, not model judgment. Review local delivery needs no simulated external provider.

| Check | Boundary and discriminating controls retained |
|---|---|
| T1 | P1:139–152 parses the actual outgoing operational JSON from `verify-next`, compares two-plan contributions, associations, full revisions/digest, real receipt pairs and owner statements, and checks commands equal the saved map commands despite a conflicting configured command and misleading SUMMARY. Restart replay preserves the retained attempt/prompt; absent or inconsistent native authority refuses. A stubbed recipient or source-string inspection cannot satisfy this check. |
| T2 | P2:95–111 includes an item absent from the map, missing/duplicate items, wrong revision, stale map/truth/content/source/occurrence, and forbidden phase-pass/truth-status/write-file fields. Real commits and additional approved publications create staleness. Complete valid control, replay and conflicting request-id reuse make refusal assertions nonvacuous. After restart, effective acceptance/completion/UAT bytes must be unchanged for refusals; bounded refusal/claim history may append. It does not wrongly demand an unchanged entire journal. |
| T3 | P2:137–153 drives verification-run/submit/read, including all accepted, rejected artifact, rejected shared link, explicit not_seen and the separate generic observation fixture. The expected met/concerns/unmet/pending values and shared-association effects are handwritten. Reads before completion and after actual repair/revision/fresh attempt expose applicability; rejected history survives restart. A direct reducer call is explicitly excluded. |
| T4 | P3:118–129 uses met and unmet native truths plus retained rejection. Exact owner waiver approval is compared against absent/false/mismatched approval, missing attribution, stale version and verifier-authored attempts. Restart/replay must retain one waiver beside the underlying rejection, without increasing met count; later verifier patches cannot erase it. Reaffirmation/supersession/revocation are owner events. |
| T5 | P3:145–163 starts with actual Pending-row publication and executed work without verdicts, progresses through unmet and independently rerun all-accepted evidence, and retains a historical human failure plus a pass control. Public human input alone resolves the failure. Repeated completion calls, interleaved publication/read/execute-next and restart must identify unfinished work until the fully applicable positive control, then install authority/projections once while preserving approved context and human history. No invented progress/undo operation substitutes for the episode. |
| T6 | P4:127–140 uses actual decision/source/native-plan material, explicit kind/target and all three aliases. It fires `review-select` into existing `review-admit`/`review-next`, inspects retained material and real dispatch, and exercises replay and missing/ambiguous targets. Exact target bytes and handwritten intent are required; invalid selection cannot broaden scope. Minimalism keeps its base reviewer and plan keeps its configured trigger/gate. Provider-fragment parity is inspection of shared compiled authority, not a fake provider exchange. |
| T7 | P4:172–186 constructs actual missing, absent-phase, unclaimed and unknown-requirement cases alongside native rejected/not_seen/met chains, superseded maps and a waiver. Public publications, runs and verdicts supply the associations. Audit and its aliases must report literal expected edges, origins, current verdicts and breaks; phase-scoped joins cannot become semantic requirement-to-truth claims. Durable before/after snapshots remain unchanged. |

These checks can fail on the defects their truths name, and their positive controls prevent blanket refusals or empty output from succeeding. The specified `-- --exact` selections are executable as top-level integration functions; actual one-test selection and observed results must still be established during execution, including the outer rerun records required by P4:299–307. No test has been run in this review.

## D-123 through D-134: task delivery and current seams

| Decision | Delivery required by the plans |
|---|---|
| **D-123 — one atomic phase-attempt patch** | P1 Task 1 defines the complete strict shape (P1:204–217); P2 Task 1 implements `cadence_apply verification-submit` (P2:182–207). Canonical membership comes from the retained coherent union, with explicit not_seen instead of omission. One complete patch, original bounded claims, application outcomes and replay identity are stored separately from effective verdicts. Atomicity comes from one writer `CompareTransact` against expected snapshot identity, full committing-snapshot validation, versioned namespace protection, participant/recovery validation and reobservation before confirmation—not a loop committing individual rows. HEAD exposes `CompareTransact` at `store/writer.rs:107` and validates all participants before changing any at `store/transaction.rs:983`. Invalid/partial patches cannot advance effective evidence; replay cannot reinstall old judgments as current. |
| **D-124 — verified-at basis and storage** | P1 Task 2 retains the attempt/prompt before returning, in the new verification namespace owned by `verification/model.rs`, `inputs.rs`, `persistence.rs` and the writer (P1:224–246). Its basis includes project/root, occurrence, attempt, approved truth/context versions, every contributing publication/content/map revision, exact coherent map digest, original admission/execution history, full HEAD/tree and explicit index/material identity. P2 Task 1 binds the patch and independent runs to that basis and revalidates it at commit/confirmation (:185–200); Task 2 renders current/historical basis (:221–233). `plan/map_view.rs:163` carries contribution revisions and :190 association origins/reasons; `map_history.rs:66` excludes associations from the item definition hash. The plans correctly prohibit using that smaller hash as verified-at authority. |
| **D-125 — independent runner** | P1 Task 3 creates `verification/runner.rs` and public verification-run (P1:264–282). Selection is by saved attempt and canonical item/revision; the command comes only from the saved check. Immutable launch precedes the real child; immutable result/material follows. Replay does not relaunch, unanswered launch remains Unknown, command/suite/CI substitution refuses, and zero/vacuous output remains inspectable rather than automatically accepted. The plan expressly shares useful child-capture/material mechanics with leased `execution/runner.rs` while keeping verification lifecycle independent. At HEAD `execution/runner.rs:140` requires an active task command and :255/:269 contain private capture/observation helpers; those mechanics may be factored in the leased file, but calling its active-task launch to reopen a closed task would violate the plan. It neither requires duplicating the process engine nor confuses independence of evidence with duplicated code. |
| **D-126 — owner waiver** | P3 Task 1 creates the owner-only exact-approved typed waiver and immutable reaffirmation/supersession/revocation events (P3:197–214). Reason, name/date, truth/version, root/occurrence and reviewed basis remain visible with rejected history; multiple waivers cue revisiting the plan. A model may prepare a payload but cannot supply approval. Name/time are provenance claims, not authentication or observed time. T4 checks the owner boundary and separate met count. |
| **D-127 — human UAT** | P3 Task 2 implements attributed `verification-human-result`, separate imported originals, immutable replies/first-pass/result history and binary-rendered phase UAT (P3:229–248). Blank is not consent and skip is not waiver. Only a human-result update resolves human work; verifier submission and completion cannot overwrite it. The owned UAT preimage/participant and guard integration are explicitly leased. T5 and the human-record regression require reopened preservation. SUMMARY creates neither cases nor criteria. |
| **D-128 — coverage compatibility** | P4 Task 2 makes cad-coverage and cad-audit select the same read-only verification-audit operation/view, with no generation arm or competing Markdown authority (P4:250–271). The two skills are regenerated from compiled audit instructions. |
| **D-129 — compiled verifier entrypoint** | P1 Task 1 extends `main.rs` with project-free `cadence verifier-instructions [--frontdoor]` and compiles the verbatim Verifier block, strict item protocol and permissions in the new verification source (P1:204–220). This follows HEAD's `main.rs:53` project-free renderer and `execution/instructions.rs:191`/ :196 modes. Task 2 shares the same authority with actual dispatch; Task 4 generates two skills and directly edits five agent metadata adapters (P1:290–304). They are artifacts, not T1's check subjects. P3:237–239 and :299–304 extend exact binary-owned projection protection while preserving drift detection and avoiding claims of arbitrary-shell control. |
| **D-130 — readiness without self-hosting** | P4 Task 3 separates the isolated historical copy, separately authored native demonstration, and outer rewrite implementation/rerun records, each with real producer and subject identities (P4:289–321). Backup/restore, interruption/recovery, one writer, history/incompatibility classification, full native path and honest audit/status are required. Native phase-14 context/plans, human-result/UAT location and explicit delivery order are chosen at :323–328; phase 14 is not authored now. Installation and final readiness are orchestrator close steps (:372–397). Missing evidence, installation or retirement keeps close unfinished. No live rewrite first-touch/import or retroactive native approvals are permitted. |
| **D-131 — completion and lifecycle** | P3 Task 3 seeds only missing Pending trace rows in the actual confirmed publication transaction (:261–277). Task 4 requires actual execution completion, complete current evidence, met/effectively waived truths and resolved required human results; concerns stays incomplete and waiver completion is separately labelled (:290–314). It atomically commits completion authority plus narrow ROADMAP/REQUIREMENTS projections, leaving UAT/context unchanged. Task 5 overlays native acceptance before lifecycle consistency and updates capture/recheck/memo plus separate execution and next-action callers (:332–356). It explicitly prevents empty-set `phase_complete` from certifying native execution and distinguishes its own projection writes from external source changes without weakening the full observed basis. |
| **D-132 — precise audit** | P4 Task 2 joins observed active requirements, explicit assignments, real claiming plans, phase truths, typed associations and applicable verdict/waiver records (:252–265). It exposes origins, missing/unavailable edges, scope limits and next actions, including unknown/unclaimed active requirements. No inferred semantic edge, first-touch import, memo write, repair or status change is permitted. T7's valid and broken controls exercise that join. |
| **D-133 — existing review subsystem** | P4 Task 1 adds explicit kind-plus-target selection and exact command forms, with old names as aliases (:217–233). It resolves the actual decision document/id and selected source/native-plan material before existing `review_service` admission/acquisition/dispatch; caller-supplied substitute paragraphs do not suffice. Existing provider, trigger, roster, original-return, adjudication and settlement contracts remain. Shared compiled intent feeds both `invoking::local_dispatch` and `provider::payload::prepare`; minimalism uses its existing base selection and plan uses `manual_plan_request`. No edit application or replacement review backend is specified. |
| **D-134 — rehearsal then actual retirement** | P1 Task 6 authors/rehearses the explicitly rooted close-only Python procedure on disposable settings (:347–374). It binds inspected hook/settings preimages, preserves recovery originals and unrelated ordering/guards, handles absent/ambiguous/stale/partial cases, multiple matches and near matches, and uses real stdio dispatch for prerequisites. P4's close first installs/identifies the current binary and generated skills/edited adapters, inspects actual installed state, then uses the same procedure on the explicitly selected root and confirms both actual absences and preserved unrelated bytes (:372–391). The test never touches owner settings. Rehearsal is not removal; actual retirement remains an orchestrator close obligation. |

The specific D-131 conflict at HEAD is real: `crates/cadence/src/derivation/mod.rs:93` currently promotes lifecycle using SUMMARY/UAT. `derivation/query.rs:72` derives and immediately checks consistency; recheck captures again at :107. `derivation/memo.rs:4` has versioned identity but :39–88 only encode existing inputs. `execution_service.rs:2447` independently prepares and checks memo, while `next_action_service.rs:59` and :278 independently recapture. P3 Task 5 leases all these paths and requires the overlay, encoding/semantic changes and all caller updates together. Merely adding verification-complete would be insufficient; the plan does not stop there.

Similarly, HEAD's publication validator reconstructs exact approved snapshot/documents at `plan/persistence.rs:282` and rejects differences at :290. P3:265 requires new versioned participant semantics, preserving old intent and approval reconstruction. `store/writer.rs:617` currently recognizes only particular external targets and rejects unknown targets at :646; P3 Tasks 2–4 lease writer, transaction and filesystem for the new narrow UAT/trace/completion participants. `guard/mod.rs:284` is the real protection seam. The plans do not assume arbitrary document writes already work.

## Earlier binding decisions and scope

- **D-88 and D-108:** authoring labels are not execution or acceptance authority. P1:234–245 requires the real coherent map and admitted execution, and P1:314–327 teaches admission/allocation and located refusals before execute-next. At HEAD `execution/admission.rs:174` checks the complete approved context and :199–216 the full publication set. `plan/persistence.rs:52` uses fresh native admission; its legacy refusal at :40 remains a different path. No task removes the gate or silently downgrades native work.
- **D-90–D-98:** the maps preserve one canonical check per current truth, attached approved plan/map contributions, explicit associations/reasons and immutable history. P1:234–242, P2:185–200 and P2:264–269 preserve full union/digest, exact replay and original approved context. HEAD seeds Pending at `context/persistence.rs:27` and reconstitutes exact approval at :89; the planned overlay avoids rewriting those records.
- **D-99–D-106:** expected properties are nonblank and handwritten; saved commands remain explicit with no new planning command grammar. Both lexical link values comply. Canonical aliases remain one item; full current union and committing-snapshot validation are required (P1:234–245; P2:185–200). No task grandfathers invalid current maps, invents phase-26 truth revision or adds a new planning-time test-existence rule.
- **D-109–D-112:** real receipt ancestry/material and owner Inspection remain prerequisites. P1:113–125 and :264–278 require independent reruns without reopening task lifecycle, replacing executor receipts, rerunning the suite as verification or promoting Unknown/zero/vacuous output. The existing execution runner is shared only for useful mechanics. P1:385–395 retains red-cause inspection and the executor's separate one plan-close suite; CI cannot supply acceptance.
- **D-113–D-115 and D-118–D-120:** the single usability task clarifies existing continuation/Stop, owner approval, task subjects, signing and Inspection rather than delivering parked continuation/task features (P1:306–336). Gaps use explicit existing extension before execute-next (:315); old admissions/outcomes are immutable (P1:82; P2:264; P3:343). No parallel worktrees, lease exemptions, treeless task workflow or retroactive self-hosting is introduced. Actual replacement of admitted plans still refuses at `plan/validation.rs:56`; repairs must respect D-120's new approved gap identity and extension.
- **D-116 and D-117:** D-NN amendment and mechanical SUMMARY work remain parked in `.planning/phases/12/CONTEXT.md:278` and :290. P2:230–233, P3:343–350 and P4:399–402 preserve approved context and keep historical SUMMARY/UAT separate. None of the four plans creates a decision-correction or SUMMARY-rewrite task.
- **D-121 and D-122:** the explicit phase-13 close placement in `.planning/phases/12/CONTEXT.md:336` governs the older design sentence mentioning phase 12. P4:372–391 requires all three compiled dispatches installed/inspected before removing hook and registrations; unrelated guards remain. Review/risk contracts are consumed, not rebuilt (P4:223–231). Historical execution remains classified and replayable without certifying it under new gates.

The scope boundary and flagged assumptions in phase-13 CONTEXT:408–442 are preserved: no model-quality oracle, CI/suite status promotion, installed-host acceptance, function/caller census, semantic requirement mapping, phase-30 edits, phase-14 progress/repair, phase-15 undo, phase-26 correction or retrospective approvals. P3:349–350 supplies only future applicability seams; P4:330–337 and :395–397 explicitly retain those limits. Mandatory D-130/D-134 close work is authorized scope even though it is not a truth-map item.

## Paths, symbols, leases and the four-plan sequence

All exact frontmatter paths were checked against the tree. There are **70 distinct leased paths** across the phase: **43 already exist and 27 are explicit creations**. Each task's Files entries are covered by its plan's frontmatter; no duplicate declaration, unknown frontmatter field, trailing directory separator, empty lease or dependency addition requiring an undeclared lockfile was found. These are source/close leases for execution, not assertions that the unimplemented operations exist at HEAD.

| Plan | Tasks / exact files | New paths first created here; other absent paths are prior-plan dependencies |
|---|---|---|
| PLAN-1 | 6 / 36 (P1:5, :45) | `src/verification/{mod,model,instructions,inputs,dispatch,persistence,runner}.rs`, `src/verification_service.rs`; `tests/phase13_verification.rs`, `tests/support/phase13.rs`, `tests/phase13_support.rs`, `tests/phase13_close.rs` under `crates/cadence/`; `.planning/phases/13/close/retire-rules-gate.py`, `close/rules-gate.md`, `reports/hook-retirement.md`. |
| PLAN-2 | 2 / 18 (P2:5, :27) | `crates/cadence/src/verification/{verdicts,status,render}.rs`. Its remaining verification modules and tests come from PLAN-1. |
| PLAN-3 | 5 / 32 (P3:5, :41) | `crates/cadence/src/verification/{waivers,human,projections,completion}.rs`. Prior models, inputs, runner, verdicts, status/readback and public service come from PLAN-1/2. |
| PLAN-4 | 3 / 28 (P4:5, :38) | `crates/cadence/src/review/instructions.rs`, `skills/cad-review/SKILL.md`, `crates/cadence/src/verification/audit.rs`, `.planning/phases/13/close/readiness.md`, `reports/adoption-readiness.md`. Existing `review/selection.rs` is explicitly edited. Waiver/human/completion records consumed by audit and demonstration come from PLAN-3. |

Creation intent is explicit at P1:378–383, P2:242–247, P3:364–369 and P4:341–346; task actions give the corresponding creation/wiring work. No speculative Rust function signature is required for a not-yet-created internal API. New public operation/test names are explicit specifications. All named existing seams were found: map read/definition, native admission/publication validators, runner capture/material, main renderer, derivation/capture/memo, `derivation_refusal`/`checked_execution`, `stable_reason` (`execution_service.rs:2295`), `protected_target`, review selection, target acquisition and both prompt producers.

The specific questioned leases are appropriate after revision:

1. **Close script home:** P1:351 places the standard-library Python procedure beside the phase close materials with an unshipped lifetime. D-134 expressly requires this small reviewed procedure, not a product deletion API. It no longer adds machinery under the retiring `cadence-core/bin/` tree. This is compatible with the binary-only shipped-product rule at `.planning/ROADMAP.md:188–200`.
2. **Close prose and authorship:** `close/rules-gate.md`, `close/readiness.md`, `reports/hook-retirement.md` and `reports/adoption-readiness.md` are required authored close work, not artifacts needed by the T1/T7 triggers. The exact leases remain; executor and orchestrator producers and actual-result sections are explicit at P1:87–97 and P4:71–85. The verifier inspects, never authors or accepts prospective completion prose.
3. **Generated skills versus metadata:** all five verifier agents and both verifier skills already exist; P1 Task 4 replaces frozen skill authority with generated binary text while editing agents as metadata. The four review skills consist of a new merged skill plus three existing aliases; P4 Task 1 supplies a project-free renderer. Executor skills remain in P1 Task 5. HEAD's `mcp.rs:1135` generated-byte comparison and :1164 metadata assertions, and the phase-12 passing report at `.planning/phases/12/reports/plan-check-3.md:168`, confirm the pattern. No task claims hand-edited agent Markdown is generated.
4. **PLAN-3/4 size and dependencies:** PLAN-3's five tasks are a coherent owner-input/completion/lifecycle slice; PLAN-4's three are review/audit/close. They no longer combine 53 leases in eight tasks. PLAN-3 consumes only prior verification infrastructure or modules created by its own preceding tasks. PLAN-4 correctly consumes PLAN-3's waiver/human/completion outputs. Existing review command routing already reaches the service through `recall/mod.rs:216`, :434 and :606, so extending the existing review command/service does not require a missing new resident-route lease. Verification routing is created with server/recall leases in P1:224–249. No circular dependency was found.
5. **Test support layout:** `tests/support/phase13.rs` is the shared helper module; `tests/phase13_support.rs` is an integration target for the named implementation/artifact regressions; `tests/phase13_close.rs` is the target for disposable close rehearsals. These are distinct roles, not three helper implementations. A file under `tests/support/` does not compile merely by existing: each consuming leased integration root must include it, for example `#[path = "support/phase13.rs"] mod phase13;`. HEAD uses that pattern for signing at `tests/mcp.rs:21` and `tests/phase7_guard.rs:1198`. Phase 12's `Client`, `fixture` and `native_context` helpers are local to its integration target (`phase12_execution.rs:15`, :104, :126), not an exported support API. The plan correctly says follow that pattern and create new shared support; it does not require importing the entire phase-12 test file or its protected fixture. All consuming roots are leased. `crates/cadence/Cargo.toml:1` through :65 has no test-target override requiring another manifest lease. The three integration targets and one shared helper are a reasonable bounded layout; the helper must contain shared setup, not duplicate acceptance tests.

All four plans explicitly order **1 → 2 → 3 → 4** (P1:378; P2:242; P3:364; P4:341). Their common leases are `verification/mod.rs`, `verification/model.rs`, `verification_service.rs`, `server.rs`, `tests/phase13_verification.rs`, `tests/support/phase13.rs` and the two verifier skills. PLAN-3/4 additionally share `verification/render.rs`. `verification/persistence.rs`, `store/writer.rs` and `store/transaction.rs` are shared by PLAN-1/2/3; PLAN-4 reads those records and does not claim new write leases there. Thus the old dispatch's “all three share” description is not incorrectly carried forward as “all four share.” The declarations cover the actual revised overlaps.

Within PLAN-1, compiled schema precedes dispatch; T1 needs completed execution but no accepted verifier patch, so it does not depend on PLAN-2. The independent runner follows retained dispatch, and the hook rehearsal only requires verify-next, not later completion. PLAN-2 submits patches only after that runner exists, then adds status readback. PLAN-3 adds waiver and human records before completion, preserves separate publication/transaction/lifecycle boundaries, and completes the T5 episode in Task 5. PLAN-4 uses those completed records for audit and full native demonstration before installed close. Same-task generated-output refresh prevents an intermediate plan-close suite from depending on a later plan to repair drift.

## Task Verify and commit audit

The 16 tasks each have **one** frontmatter Verify matching **one** task-body Verify: **15 exact single-test commands and one project-free binary invocation**. No task uses bare `cargo test`, a whole-suite Verify, multiple command alternatives or a helper that invokes another test function. The `cargo test --workspace` strings belong only to each plan's separate executor plan-close suite; clippy is separately identified quality work, never acceptance (P1:392–398 and corresponding Notes in P2/P3/P4).

| Task | Narrow Verify subject / location | Why it settles this increment |
|---|---|---|
| P13-1-T1 | `cargo run -p cadence -- verifier-instructions`, P1:218 | Project-free compiled contract, verbatim role block and strict item schema. |
| P13-1-T2 | T1 exact function, P1:251 | Actual retained current-input dispatch and restart/refusal controls. |
| P13-1-T3 | `phase13_support::phase13_runner_retains_independent_receipts`, P1:279 | Real saved-command child, once-only replay side effect, source mismatch and interrupted Unknown. |
| P13-1-T4 | Existing MCP metadata/skill function, P1:301 | The leased function is extended for both generated verifier outputs and all five agent permissions. |
| P13-1-T5 | Existing MCP malformed-phase/lifecycle function, P1:337 | The same function now checks rendered usability and real located public refusals. |
| P13-1-T6 | `phase13_close::phase13_rules_gate_retirement_rehearsal`, P1:371 | Actual close procedure on disposable settings, exact removal/preservation and failure/recovery controls. |
| P13-2-T1 | T2 exact function, P2:208 | Atomic valid patch plus all located invalid/refusal/replay variants. |
| P13-2-T2 | T3 exact function, P2:236 | All four statuses from public item patches and current/historical associations. |
| P13-3-T1 | T4 exact function, P3:215 | Owner approval, waiver lifetime, preserved rejection and distinct met count. |
| P13-3-T2 | `phase13_support::phase13_human_results_preserve_first_pass`, P3:249 | Attributed human-record preservation, authorized update, blank input and forbidden verifier overwrite. |
| P13-3-T3 | `phase13_support::phase13_publication_seeds_only_missing_trace_rows`, P3:274 | Actual publication's missing-only Pending seed, exact replay and stale-preimage refusal. |
| P13-3-T4 | `phase13_support::phase13_completion_projection_transaction_recovers`, P3:315 | Real completion transaction, stale input and interruption/reopen without acknowledged split authority/projections. |
| P13-3-T5 | T5 exact function, P3:357 | Full owner completion episode across lifecycle callers. |
| P13-4-T1 | T6 exact function, P4:236 | Actual selected review material and intent through existing delivery. |
| P13-4-T2 | T7 exact function, P4:272 | Broken/current traces and aliases with read-only durable snapshots. |
| P13-4-T3 | `phase13_close::phase13_adoption_copy_preserves_history_and_recovers`, P4:334 | Classified disposable historical copy, explicit backup/restore and real interruption/recovery; installed outcomes remain close work. |

The `target::function` notation in this last table is descriptive; the plans' actual commands correctly use `--test target function -- --exact`, not a Rust module prefix. The two existing selected MCP functions are present and top-level at `crates/cadence/tests/mcp.rs:947` and :1134. All other named phase-13 functions are honest creations.

Every task delivering an acceptance check explicitly records its red and green commit subjects: P1:231/:247 (T1), P2:183/:202 (T2), P2:220/:235 (T3), P3:198/:209 (T4), P3:334/:351 (T5), P4:213/:235 (T6), P4:251/:266 (T7). The common Notes retain real red-cause inspection and orchestrator commit ownership. No fixture receipt is promoted into an outer implementation commit or independent outer rerun; P4:299–318 preserves that distinction through final readiness.

## Review provenance and limits

Only this report was written. No Cargo command, build, test, mutating Git command, fixture initialization, installation or hook operation was executed. Plans, source, prior reports, owner settings, protected fixtures/roots/locks and `.codex-analysis/` were not modified; the protected fixtures/roots/locks and `.codex-analysis/` were not accessed. This pass used the in-tree contract and references, not an installed Cadence plugin.

Reviewed plan SHA-256 identities:

- PLAN-1: `b419f61be55f60a2adb8a2f494912928a771ee08077bd903694ba7f8fc446af2`
- PLAN-2: `b44db483dab781cf3d9b06976993b0bc53a85ba85cdf02c162b780efb11ded5c`
- PLAN-3: `377c83577682f581e4bac437dac2ef65eac3475b0fb45c9f10da47065967d21d`
- PLAN-4: `b4a38fe93ee43c23e427894743f8b6f0b9e97fad9446572e0db937f7a289a663`
