# Phase 13 verification report

Verifier: dispatched Claude agent, 2026-09-11. Read-only on the repository.
Bound to HEAD `99126c45fe8e36b244752d059f5ba9ad2d04cd68`, tree
`19aa8f2411ff3084003187ad54dcc05f098745ad`, test file
`crates/cadence/tests/phase13_verification.rs` sha256
`80e5ba6cf3f11436f17b386b8af3439ff1a4a93a64f77fbcb1eeecbc47aafe3e` (the same
identities as adoption-readiness.md section 2a). `git diff HEAD -- crates skills
docs` is empty; the working tree differs from HEAD only in two orchestrator
report files under `.planning/phases/13/reports/` (adoption-readiness.md,
hook-retirement.md) plus the four untracked executor reports.

Every check was run by me with the exact command
`cargo test -p cadence --test phase13_verification <function> -- --exact`, one
at a time, foreground, from `/code/cadence` against the main target directory.
Every red commit was run by me in a throwaway worktree at the recorded red SHA
with `CARGO_TARGET_DIR` pointed at the scratchpad; the worktree was removed
afterwards and the main checkout was never touched. No mold/SIGBUS retry was
needed.

## Verdicts

| item | verdict | observed |
|---|---|---|
| P13-T1-C | accepted | Run at HEAD: `test phase13_dispatch_carries_current_verification_inputs ... ok` / `1 passed; 0 failed; 0 ignored; 0 measured; 6 filtered out; finished in 7.82s`. Red `095a30d3` run in worktree: `FAILED` at phase13_verification.rs:15 `verifier dispatch must be retained: {"status":"refused","code":"invalid-patch",...}` left `"refused"` right `"ok"` (fixture completed execution, then `verify-next` did not exist: behavioral). Body identical between green `79e0ab4f` and HEAD (sha256 prefix c63084fce242). Body (lines 191-284): one function; real `cadence serve` over stdio (support/phase13.rs:24-31), real git, real gpg key, real python child checks; handwritten truths, item ids, associations, commands (`python3 -B tests/a.py`, `tests/b.py`), verbatim Verifier block, `Strict item patch schema`, no `findings_file`, no SUMMARY prose, no configured alternative; replay after restart (`query` opens a fresh server), dirty-source refusal `verification-source`, request-reuse refusal, drifted PLAN refusal `installed-plan`, missing-authority refusal `native-approved-truths` with no fabricated attempt; `reopened()` reopens the store after `client.finish()` waits the child. |
| P13-T1-A1 | accepted | `crates/cadence/src/verification/instructions.rs:2` holds the Verifier block verbatim as `VERIFIER`; `:101 contract_markdown()` embeds it, the protocol and the strict schema; `main.rs:93-98` routes `cadence verifier-instructions [--frontdoor]` to `contract_markdown()`/`frontdoor_markdown()`; `verification/dispatch.rs:3-10` puts `instructions::contract_markdown()` at the head of every dispatched prompt. `verification/model.rs` (138 lines) holds the typed Query/patch vocabulary. |
| P13-T1-A2 | accepted | `verification/inputs.rs:95 observe`, `:138` dirty-source refusal `verification-source`, `:163-177 reobserve_external`; `verification/persistence.rs:43-52 prepare` composes and digests the prompt, `:54-77 contribute` refuses a changed payload under a reused id and checks the authority digest; `verification/runner.rs:124 launch`, `:159-162` LaunchFailed/Unknown when source changes before spawn, `:76-93` launch/result receipt validation; `verification_service.rs:122-155` Query::Next path writes through `Operation::VerificationV1` (`store/writer.rs:58,403,884`, `store/transaction.rs:34`). |
| P13-T1-A3 | accepted | `cadence verifier-instructions` output is byte-identical to `skills/cad-verifier-contract/SKILL.md`; `--frontdoor` output byte-identical to `skills/cad-verify/SKILL.md` (cmp). cad-verify `allowed-tools`: `mcp__cadence__cadence_query`, `mcp__cadence__cadence_apply`, `Task`; no Write/Edit; both say never assign a findings file. Five agents `agents/cad-verifier{,-low,-medium,-xhigh,-max}.md` carry `tools:` with the two cadence MCP tools plus Read/Bash/Grep/Glob/excerpt and preload `cad-verifier-contract`. Executor refresh: `executor-instructions` and `--frontdoor` byte-identical to `skills/cad-executor-contract/SKILL.md` and `skills/cad-execute/SKILL.md`. |
| P13-T1-L | accepted | Traced: `server.rs:243-244` deserialises `verify-next`; `server.rs:631` calls `service.verification(.., Query::Next)`; `verification_service.rs:122-155` -> `persistence::prepare` (`persistence.rs:49`: `let prompt = super::dispatch::prompt(&inputs, &documents)?`) -> `dispatch.rs:3-10` composes contract + `<operational-input>` JSON of the current inputs; the attempt is committed and returned as `{"status":"ok","attempt":saved}` whose `prompt` field the check parses (test line 201-204). Literal `binary-composed prompt` is in T1's outcome slot in CONTEXT.md ("the verifier gets a binary-composed prompt containing ..."). |
| P13-T2-C | accepted | Run at HEAD: `test phase13_mismatched_verdict_patch_is_refused ... ok` / `1 passed; 0 failed; ... 6 filtered out; finished in 51.88s`. Red `dedf770c`: `FAILED` at :104 `refusal must identify the mismatched item/input: {"status":"refused",...,"rule":"verification-unavailable","slot":"operation"}` left `"verification-unavailable"` right `"verification-item"` (dispatch and independent runs succeeded, `verification-submit` not implemented: behavioral). Body identical green `e1def106` vs HEAD. Body (64-190): unknown/missing/duplicate item, wrong item_revision, 13 basis slots (map digest, truth version, content revision, occurrence, root, project, context, admissions, execution, head/tree/index/material), missing run, executor run, phase_pass/truth_status/write_file shape refusals, each with `acceptance_bytes` unchanged and claim retained; valid control persists (`patches == [valid]` after reopen); exact replay identical; changed payload -> `verification-request-reuse`; second completion -> `verification-attempt-complete`; real repair commit -> `verification-basis`/`basis.source` with requested/current; real approved additional publication -> `basis.map_digest` against the new `input_digest`; no findings/UAT file. |
| P13-T2-A1 | accepted | `verification/verdicts.rs:27 claims`, `:32 patches`, `:37 refusal` with bounded requested/current, `:56 replay` (changed payload under same id refused), `:161 contribute` appends immutable claims, `:190 transaction`; `store/transaction.rs:14 VerificationSubmitV1`; `store/writer.rs:817`. |
| P13-T2-A2 | accepted | `verification/verdicts.rs:95-114` compares every basis field of the patch against the retained attempt and the current map digest, source and authority digest at commit time; `:122-156` item membership, duplicate, revision, run and completeness; `verification/runner.rs:76-93` launch/result material checks; `inputs.rs:163-177` re-observes source before confirmation. |
| P13-T3-C | accepted | Run at HEAD: `test phase13_report_derives_truth_status_from_every_item ... ok` / `1 passed; 0 failed; ... 6 filtered out; finished in 84.78s`. Red `f6c2b060`: `FAILED` at :328 `{"status":"refused",...,"reason":"retained attempt absent","rule":"verification-attempt"}` left `"refused"` right `"ok"` (phase-level report readback did not exist: behavioral). Body identical green `5082bf2d` vs HEAD. Body (319-515): pending before any attempt and with an open attempt; all-accepted -> `met` rows handwritten via `row()`; rejected shared artifact -> both truths `unmet`; rejected link (associated with truth/A only) -> A unmet, B met; explicit not_seen -> unmet; history keeps prior rows and reasons; approved context unchanged; real repair commit -> all historical with `differs == ["basis.source"]`; uncommitted edit -> `current verification inputs unavailable`; fresh attempt restores current with the rejected attempts retained; restart via `reopened()` and fresh server; separate `Completed::with_observation()` fixture: accepted observation -> `concerns`, not_seen -> `unmet`, rejected -> `unmet`, capped judgment kept in history. |
| P13-T3-A1 | accepted | `verification/status.rs:52-58 reduce` (any rejected/not_seen -> unmet; any observation -> concerns; else met), `:62 rows`, `:94 differs`, `:115 judgment`, `:142 counts`, `:149 report`; `verification/render.rs:17 text` renders current/history/waived/legacy lines; `verification_service.rs:110-120` Read path attaches runs, claims and rendered report. |
| P13-T4-C | accepted | Run at HEAD: `test phase13_owner_waiver_is_distinct_from_met ... ok` / `1 passed; 0 failed; ... 6 filtered out; finished in 56.17s`. Red `7a337777`: `FAILED` at :543 `assertion left == right` left `Null` right `{"met":1,"concerns":0,"unmet":1,"pending":0,"waived":0}` (met/unmet rows already derived; no counts/waived reporting or waiver operation yet: behavioral). Body differs green `1589adfb` vs HEAD by one clippy fix only (`format!` with no arguments -> string literal, `09b3c0eb`); semantically identical. Body (527-727): unapproved/mismatched/anonymous/undated approval, blank reason/owner/at, stale version, unknown truth, met truth, stale source, stale map, orphan supersession, revoke-nothing, each refused with rule and slot and tree unchanged; absent approval and verifier-patch `waivers` arm -> `verification-shape` and no waivers namespace; effective waiver shows `waived (derived unmet)` beside met, counts `waived 1` and `met 1`; replay identical, changed payload `verification-waiver-reuse`, duplicate refused; a later verifier patch leaves the record but makes it ineffective with a located reason; reaffirm and revoke as separate owner events; two waivers -> `revisit the plan: 2 truths are waived`; context unchanged; reopen after every server exit. |
| P13-T4-A1 | accepted | `verification/waivers.rs:16 SCHEMA`, `:80 prepare` with refusals `verification-approval` (:115-124), `verification-waiver` (:129,161,167), `verification-truth` (:149,153 refuses a met truth), reaffirm/revoke kinds (:158-164); `status.rs:130 overlay` keeps the derived status and never counts waived as met; `render.rs:42` Waived line; `store/transaction.rs:18 VerificationWaiverV1`. |
| P13-T5-C | accepted | Run at HEAD: `test phase13_incomplete_verification_cannot_complete_phase ... ok` / `1 passed; 0 failed; ... 6 filtered out; finished in 40.89s`. Red `b2c1da4e`: `FAILED` at :793, unfinished list left `[truth/B unmet]` right `[truth/B unmet, {"kind":"human","id":"1","status":"fail","source":"imported","first_pass":"fail"}]` (completion refused the unmet truth but did not yet see the imported human failure: behavioral). Body identical green `85880907` vs HEAD. Body (736-921): publication seeds exactly `| T1 | Phase 13 | Pending |`; refusals at publication (`admission-required`), after execution (`verification-incomplete`/`verification`), open attempt, one unmet truth (names truth/B, check/B and human 1), stale after real repair commit (`verification-basis`), all-accepted with human failure (`humans`, id 1), repeat identical; patch-arm `humans` -> `verification-shape`; wrong attempt -> `verification-attempt`; public `verification-human-result` resolves with `first_pass` kept `fail` and rendered UAT.md keeping the original verbatim above; stale requirements preimage -> `verification-projection`; positive completion writes ROADMAP box and `T1 ... Complete` once, leaves UAT.md and context; replay identical, second completion refused; `execute-next` then `phase-not-current`; later publication makes completion inapplicable and `execute-next` reports `state-conflict` naming `ROADMAP.md:2` without repairing. |
| P13-T5-A1 | accepted | `verification/human.rs:1-9` doc, `:36-43` first_pass and imported original, `:94 imported_items`, `:130 uat_path`, `:134 prepare`; `verification/projections.rs:120 seed_requirements` (missing-row-only Pending), `:139 seeded_requirements` from exact preimage, `:146 uat` render; `store/transaction.rs:22 VerificationHumanV1`. |
| P13-T5-A2 | accepted | `verification/completion.rs:19 RULE`, `:99 applicable`, `:136 prepare`, `:223-287` projection preimage refusals, `:260` unfinished truth/human slot, `:266` already-complete refusal, `:293` `complete-with-waivers` label, `:303 contribute`; `derivation/mod.rs:168,237` derive the native lifecycle from the completion record; `store/transaction.rs:26 VerificationCompleteV1`, `store/writer.rs:681,844`. |
| P13-T6-C | accepted | Run at HEAD: `test phase13_review_surface_selects_target_and_intent ... ok` / `1 passed; 0 failed; ... 6 filtered out; finished in 18.39s`. Red `459c60e3`: `FAILED` at :997 `cad-review []: {"status":"refused","code":"invalid-review-operation","reason":"unknown variant `review-select`, ..."}` left `"invalid-review-operation"` right `"review-kind-required"` (behavioral; matches the orchestrator's rerun). Body identical green `4abb9da5` vs HEAD. Body (960-1225): 20 missing/unknown/ambiguous/unresolvable selections each with a located code and no admission; decision D-2 material lines [7,7] with context [1,9] and handwritten `DECISION_INTENT`; alias `cad-decision-review` equals the canonical selection; tampered decision text refused at admit; real `review-admit`/`review-next` dispatch carries the intent and `Review retained target`, `review-material` bytes equal the document; minimalism file/dir/phase-range with base reviewer, null gate/trigger/routing and `MINIMALISM_INTENT`; plan 13 with CONTEXT + PLAN-1 + PLAN-2 through `manual-plan` with the configured `blocking` gate and `PLAN_INTENT`; single plan slice; target bytes unchanged and `git status --porcelain` empty; provider payload and invoking both call `instructions::intent(`; four skills byte-identical to `review-instructions` renders. |
| P13-T6-A1 | accepted | `review/selection.rs:55 select_next`, `:125 dispatch_roster`, `:149 delivery_completion` preserved; `:397 select` and `:260 command_kind`; `review/instructions.rs:8-12` the three handwritten intents, `:20 intent`, `:31 intent_for`, `:43 frontdoor_markdown`; `review/invoking.rs:24` and `review/provider/payload.rs:25` both consume `instructions::intent`; `review_service.rs:45,1285-1318` the `review-select` handler. |
| P13-T6-L | accepted | Traced: `review_service.rs:1285` `selection::select(command, arguments, ..)` resolves the target against real files; `:1304-1310` builds the admission with `"target":selected.target` (and decision material, re-verified against the document at `:774`); `review-admit` persists that admission; `review-next` at `:1496` calls `review::invoking::local_dispatch(&admission,&attempt)`; `invoking.rs:8` `Review retained target {target}` and `:24` `instructions::intent(admission)` compose the reviewer prompt; entries are read back through `review-material`. Literal `selected target` is in T6's trigger slot in CONTEXT.md ("When the owner requests a review of a selected target, ..."). |
| P13-T7-C | accepted | Run at HEAD: `test phase13_audit_reports_broken_verification_traces ... ok` / `1 passed; 0 failed; ... 6 filtered out; finished in 23.50s`. Red `743967fb`: `FAILED` at :1309 `{"status":"refused",...,"reason":"verification-audit is not implemented","rule":"verification-unavailable"}` left `"refused"` right `"ok"` (behavioral; matches the orchestrator's rerun). Body identical green `8b0717cb` vs HEAD. Body (1256-1488): handwritten traces for T3 (phase->plan missing), T4 (requirement->phase missing), T5 (phase->roadmap missing, phase 42), T7 (plan->requirement missing), T6 out of scope; T1/T2 pending with `evidence->verdict` missing; phase-scoped association note and limits; `cad-audit` and `cad-coverage` select the same traces, `cad-generate` refused; rejected check -> both requirements `unmet` with structural edges present; waiver shows `waived` with rejected items still visible; met control on fresh runs with the four breaks still listed; superseded plan-3 map -> current inputs unavailable (`admission-plan-set`), `gap-old` absent from traces; every audit call asserts tree and reopened snapshot unchanged (`audit()` helper); REQUIREMENTS/ROADMAP bytes unchanged, no UAT.md, context status still `pending`; two skills byte-identical to `audit-instructions` renders with `allowed-tools` only `cadence_query`. |
| P13-T7-A1 | accepted | `verification/audit.rs:1` read-only doc, `:14 ALIAS`, `:16-17` the two limits, `:21 association_note`, `:83 report` refusing non-audit commands (`:85`), `:198-263` broken edges with reasons and next actions; `render.rs:79 audit_text`; `instructions.rs:109 audit_frontdoor_markdown`; `skills/cad-audit/SKILL.md` and `skills/cad-coverage/SKILL.md` render byte-identical from the binary. |

Counts: accepted 20, rejected 0, not seen 0.

## Outside the map, for the owner

1. **T1's recorded red is the second of two test commits.** The function
   `phase13_dispatch_carries_current_verification_inputs` was committed at
   `98b12099` ("... red P13-1-T2"); the recorded red `095a30d3` is a 4-line
   fixture correction on top (task-start now takes the check from the
   admission contract). The red as recorded does fail behaviorally, so the
   verdict stands; the record just names the later of the two.
2. **T5's red is a partial red.** At `b2c1da4e` the completion refusals for
   publication, execution, open attempt and one unmet truth already passed
   (delivered by P13-3-T2 to T4); the check went red only on the imported
   human failure being named. Still a behavioral failure of the committed
   check, so accepted; noted because the red covers less of T5 than the
   function does.
3. **T3's "rejected shared link" case is a single-truth link.** `link/parcel`
   is associated with truth/A only, so that case shows the other truth stays
   met; propagation of a shared rejection to every association is shown by
   the shared artifact case instead.
4. **T7's check exercises a rejected chain, not a not_seen chain.** The
   not_seen reduction is proven in T3-C and the audit consumes the same
   report rows; the map wording "rejected/not_seen" is covered only half
   within T7-C itself.
5. **T1-C's "configured alternative" assertion is negative only.** The fixture
   writes `workflow.test_command` into the global layer the server reads
   (`server.rs:204`), but nothing inside T1-C confirms the layer was loaded;
   the positive control for that layer lives in `phase12_execution.rs:1123-1159`.
   T1-C's own positive assertion (commands equal the saved check commands)
   is what carries the truth.
6. **Section 2a records the binary sha256 as `unknown`.** Not a verdict
   input, but the orchestrator's own record is incomplete on that column.
7. **Working tree.** During this verification two tracked report files
   (`adoption-readiness.md`, `hook-retirement.md`) were modified by the
   orchestrator, so the section-2a material digest no longer describes the
   whole working tree; source, skills and docs are identical to HEAD.
