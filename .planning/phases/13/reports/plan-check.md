VERIFICATION FAILED

## Findings by severity

**2 BLOCKER, 7 WARNING, 0 NOTE.**

Reviewed 2026-09-11 in `/code/cadence`, branch `cadence/binary-owns-process`, HEAD `42cf984c0da3fd5ccf4a556e6f90c78e2a9e6725`. The three plans are untracked. Read-only Git inspection confirmed that the relevant tracked source, skills, agents, design and binding contexts match HEAD. This is a review of the instructions for future execution, not a claim that the implementation or checks pass.

The design, checker contract and phase contexts were read before the plans; the obligations below were derived independently. The phase-12 reports were used as review precedents, not current implementation evidence. The user's explicit sequential-plan authorization overrides the contract's generic prohibition on overlapping split plans. The user's requested report format governs this document.

Citation shorthand: `P1:n`, `P2:n`, `P3:n` mean `.planning/phases/13/PLAN-1.md:n`, `.planning/phases/13/PLAN-2.md:n`, `.planning/phases/13/PLAN-3.md:n`. Other paths are repository-relative. Commands in this report are inspected specifications; none was executed.

### B1 — One task has two Verify commands

**BLOCKER — `.planning/phases/13/PLAN-1.md:58-61`, Task 5 at :312-318.** The frontmatter and task body both require two separate tests: the rendered-skill test and the malformed-phase/lifecycle-refusal test. Each command is narrow, but the task's Verify is not the single exact test or single binary invocation this dispatch explicitly requires. The design's Planner block also says to name the narrowest command settling the task (`docs/architecture/acceptance.md:338-344`). This is a task-contract violation, not an extra evidence-map check: T1 still has only one acceptance check.

**Fix:** keep the single bounded execution-front-door task required by `CONTEXT.md:31-38`, but give it one substantive existing test function whose assertions settle the task's rendered instructions and public refusals, or one substantive binary invocation. Update both frontmatter and body together. Do not split the owner's explicitly single task or add a new acceptance truth/check.

### B2 — The required close record asks for native receipts whose provenance no task produces

**BLOCKER — `.planning/phases/13/PLAN-3.md:521-534`, with the prerequisite fixture separation at :511-519.** Task 8 requires collecting the **seven truth checks'** independent verification receipts and current verdict/waiver/human/completion records by reference to retained evidence. The planned native receipts are bound to disposable projects' approved maps and tiny child checks (P1:98-104, :243-256; P2:174-176). Those are not native execution/verification records for the rewrite's seven outer `phase13_verification` functions. The plans create ordinary red/green implementation commits for those outer functions, but no task creates their native approved publication, allocation, execution history and verification attempt. The copied historical planning tree and the separately authored native demonstration expressly must remain different authorities (P3:511-519).

That distinction matters mechanically: the retained native execution contract must resolve to actual publication and allocation authority (`crates/cadence/src/execution/admission.rs:174-210`); run launches bind their selected task/check and committed material (`crates/cadence/src/execution/runner.rs:140-170`). D-125's verification receipts likewise belong to the saved check item and attempt, not whichever outer test happens to invoke that fixture (`CONTEXT.md:239-245`). Borrowing fixture receipts would certify a different subject. Producing native rewrite receipts retroactively would conflict with D-130 and D-114's prohibition on invented approvals/maps/reds (`CONTEXT.md:270-278`, :368), and live initialization is expressly forbidden at P3:594-595.

**Fix:** name the evidence producers and identities in the close task. Record the rewrite checks' real orchestrator-observed red/green commits and independent rerun outputs as such; record the disposable native path's receipts, verdicts and completion separately with their actual root, occurrence and item identities. Do not require native phase-13 rewrite receipts that this non-self-hosting phase cannot supply, and do not treat fixture records as substitutes. Keep D-130's actual verification/readiness work and honest limits; no phase-14 implementation is needed to resolve this.

### W1 — Link values and map reasons are left for the executor to author

**WARNING — `.planning/phases/13/PLAN-1.md:120-137`, :170-175; `.planning/phases/13/PLAN-2.md:88-108`, :125-145; `.planning/phases/13/PLAN-3.md:145-160`, :169-191, :207-237, :241-259.** Both proposed links have a legitimate semantic handoff: T1 names the prompt the verifier receives, and T6 names the selected target and corresponding reviewer instructions. However, neither item pins a literal `spec.value`. Their full descriptive phrases are paraphrases, not exact contiguous phrases from one approved slot. For example, P1's “containing current truths, map” differs from the outcome's “containing the phase's current truths, evidence map”; P3's “decision/minimalism/plan instructions” differs from “decision, minimalism or plan instructions.” D-101 compares the whole selected value exactly, not matching words or meaning (`.planning/phases/29/CONTEXT.md:115-141`; `crates/cadence/src/plan/limits.rs:195-256`). Publishing those full descriptions as the value would refuse.

The seven check items also omit an explicit item/association reason, unlike the artifact entries' `Reason` paragraphs. D-96 requires both reasons (`.planning/phases/28/CONTEXT.md:130-140`); they are required typed strings in `crates/cadence/src/plan/evidence.rs:14-27`. The headings identify which truth each item serves, but do not supply the missing “what change would break this” statement. These prose maps are not demonstrated typed publication payloads.

**Fix:** retain both semantically justified links, but explicitly pin their caller, callee and exact value, for example T1's `binary-composed prompt` and T6's `selected target`, each occurring wholly within an approved slot. Give every check and link an explicit item/association reason; make artifact association reasons explicit too, allowing the same stated reason when appropriate. No truth revision, new check, semantic matcher or additional wiring census is needed. The semantic links are confirmed; lexical compliance of the as-written unspecified values is not.

### W2 — The artifact description again calls hand-edited agents generated

**WARNING — `.planning/phases/13/PLAN-1.md:153-160`, compared with Task 4 at :270-279.** “Generated” introduces a list containing both skill outputs and all five agent metadata files. Task 4 actually renders **both skills** and directly makes the agent files thin metadata adapters. Its entrypoint has contract/front-door modes, not an agent-manifest output. Thus the task follows the phase-12 pattern, while the evidence description overclaims generation provenance.

At HEAD the distinction is explicit: `crates/cadence/tests/mcp.rs:1135-1149` checks generated skill bytes against binary output; :1164-1209 separately checks directly edited metadata adapters. `agents/cad-executor.md:1-13` and `agents/cad-verifier.md:1-14` are real metadata files. The phase-12 passing report makes this same distinction (`.planning/phases/12/reports/plan-check-3.md:166-170`).

**Fix:** describe the two skills as generated and the five agents as directly edited metadata adapters referring to the compiled contract. Keep both as artifacts, and keep T1's subject the real dispatch. No manifest generator is required.

### W3 — An existing review module is described as a creation

**WARNING — `.planning/phases/13/PLAN-3.md:225-226`, Task 6 at :438-462.** `review/selection.rs` is not new. At HEAD it implements `select_next`, roster freezing and delivery completion over retained attempts (`crates/cadence/src/review/selection.rs:1-4`, :49-83, :119-173), and is already registered by `review/mod.rs:23`. These are existing phase-9/10 delivery contracts, not the proposed user target selector. Treating the file as a new module risks replacing the very subsystem D-133 says to preserve.

**Fix:** explicitly call this an edit lease and preserve its existing functions, adding the target-selection logic without replacing delivery selection, or choose and lease a distinct creation path. `review/instructions.rs` really is absent and is an honest creation. The existing `review_service.rs::acquire_target` and handoff are sufficient integration seams; no new review backend is needed.

### W4 — PLAN-3 combines two substantial delivery plans

**WARNING — `.planning/phases/13/PLAN-3.md:5-86`, Tasks 1-5 at :283-434 and Tasks 6-8 at :436-539.** The actual frontmatter leases **53 files**, not 50, across eight tasks. The first five tasks form a coherent owner-input/completion transaction and lifecycle change. The remaining three add a separate review command family, read-only audit command family and installation/adoption/retirement close. These are distinct user operations and subsystem boundaries, with separate checks and artifact procedures; their coupling is mostly shared service/test files and final readiness. A single plan-close suite and report must now settle all of them at once. This is a proportionality warning independently of truth coverage and the numerical task ceiling.

**Fix:** split the completion/lifecycle work from the review/audit/front-door and close work into sequential plans, preserving one check per truth and declared overlaps. The dispatch permits sequential shared leases. Do not merge the completion transaction and lifecycle tasks merely to reduce task count: their different validation boundaries are substantive. The current eight tasks can deliver their functional scope, so this is not a correctness blocker.

### W5 — The one-time retirement tool is placed in the retired product executable tree

**WARNING — `.planning/phases/13/PLAN-1.md:38`, :161-169, :322-341.** The Python procedure is honestly a creation and D-134 expressly authorizes a small reviewed close procedure. It is not a replacement acceptance gate or production verifier, so the design does **not** require turning this into a binary settings-deletion service. However, putting it in `cadence-core/bin/` gives new phase-close machinery the location and lifetime of the in-tree 3.x executables the rewrite removes. The final shipping constraint is one executable binary, with the named bootstrap exception (`.planning/ROADMAP.md:188-200`); this script must remain unshipped close support.

**Fix:** keep the bounded explicitly rooted rehearsal/removal procedure outside the shipped product, beside the phase's close materials, and update its exact lease and consumers. State its close-only lifetime. Do not add a general deletion command to the binary solely to satisfy this warning. The safeguards in Task 6 and P3:579-590 are appropriate; the location/provenance is the problem.

### W6 — Required close prose is attached as acceptance evidence for unrelated triggers

**WARNING — `.planning/phases/13/PLAN-1.md:161-169`; `.planning/phases/13/PLAN-3.md:265-275`.** The two close procedures and two close reports are required D-130/D-134 work, but their proposed map associations do not follow from T1's dispatch trigger or T7's audit trigger. A verifier can receive the promised prompt before hook removal, and the audit can return the promised broken traces before a human-authored adoption report exists. The plans themselves put final retirement after implementation and verification (P3:565-595). These are authored close records, not binary-rendered verification status records and not components the two truths need to work.

**Fix:** retain the exact close deliverables and leases, but track them as mandatory close work rather than adding unrelated artifacts to T1/T7's evidence maps. Explicitly assign the executor the procedure/rehearsal record and the orchestrator the final installed-state inspection and report completion. The verifier inspects actual artifacts/records; it must not author these reports or accept prospective success prose. Do not replace these artifacts with an observation item: this phase has none.

### W7 — PLAN-2 permits renderer drift until after its own required suite

**WARNING — `.planning/phases/13/PLAN-2.md:188-190`, :240-246, with `.planning/phases/13/PLAN-3.md:524-525`.** PLAN-2 explicitly allows finalizing the compiled schema and defers generated-skill refresh to PLAN-3. But PLAN-1 Task 4 first extends the existing MCP test to require exact generated verifier-contract/front-door byte equality (P1:277-283). PLAN-2's required plan-close suite includes that test. If finalization changes rendered bytes, the plan cannot close green using its own lease: neither generated verifier skill is in P2:5-21.

**Fix:** either settle and preserve the rendered schema in PLAN-1, or lease and regenerate the affected verifier skill outputs in the same PLAN-2 task that changes their compiled authority, before its plan-close suite. Do not weaken byte equality or postpone the required suite. This is a conditional execution hazard, not a claim that the schema necessarily must change.

## Per-truth evidence table

All seven checks use `crates/cadence/tests/phase13_verification.rs`, a creation lease. Each is specified as one top-level test function with all variants and controls inside it. Each listed command selects that one function with `-- --exact`; actual nonzero selection and results remain to be verified after implementation. The seven names and accepted episodes match `CONTEXT.md:94-207`; none is replaced with an implementation-unit check.

| Truth | Plan / delivering task | Check | Other items as written | Command |
|---|---|---|---|---|
| T1: verifier receives current binary-composed inputs | PLAN-1 Task 2, P1:201-232 | P13-T1-C, `phase13_dispatch_carries_current_verification_inputs` (P1:120) | P13-T1-A1/A2/A3/A4; P13-T1-L | `cargo test -p cadence --test phase13_verification phase13_dispatch_carries_current_verification_inputs -- --exact` |
| T2: mismatched patch refused with location | PLAN-2 Task 1, P2:160-194 | P13-T2-C, `phase13_mismatched_verdict_patch_is_refused` (P2:88) | P13-T2-A1/A2 | `cargo test -p cadence --test phase13_verification phase13_mismatched_verdict_patch_is_refused -- --exact` |
| T3: status derives from every current item | PLAN-2 Task 2, P2:196-222 | P13-T3-C, `phase13_report_derives_truth_status_from_every_item` (P2:125) | P13-T3-A1 | `cargo test -p cadence --test phase13_verification phase13_report_derives_truth_status_from_every_item -- --exact` |
| T4: owner waiver is beside met truths | PLAN-3 Task 1, P3:283-307 | P13-T4-C, `phase13_owner_waiver_is_distinct_from_met` (P3:145) | P13-T4-A1 | `cargo test -p cadence --test phase13_verification phase13_owner_waiver_is_distinct_from_met -- --exact` |
| T5: incomplete acceptance refuses completion | PLAN-3 Task 5, P3:400-434 | P13-T5-C, `phase13_incomplete_verification_cannot_complete_phase` (P3:169) | P13-T5-A1/A2 | `cargo test -p cadence --test phase13_verification phase13_incomplete_verification_cannot_complete_phase -- --exact` |
| T6: selected target receives corresponding review intent | PLAN-3 Task 6, P3:436-469 | P13-T6-C, `phase13_review_surface_selects_target_and_intent` (P3:207) | P13-T6-A1; P13-T6-L | `cargo test -p cadence --test phase13_verification phase13_review_surface_selects_target_and_intent -- --exact` |
| T7: audit returns broken traces and current verdicts | PLAN-3 Task 7, P3:471-499 | P13-T7-C, `phase13_audit_reports_broken_verification_traces` (P3:241) | P13-T7-A1/A2 | `cargo test -p cadence --test phase13_verification phase13_audit_reports_broken_verification_traces -- --exact` |

The maps contain **7 checks, 13 artifacts, 2 links, 0 observations**. Every item is under a named T1-T7 heading; no item is syntactically orphaned. Requirements partition T1 / T2-T3 / T4-T7 in the three frontmatters. Every check has a command and nonblank expected property. W1 concerns missing reasons and the unpinned lexical link values; W6 concerns two nominal associations without a real truth dependency. No second map check is hidden among the separately named artifact regressions.

**No observation item appears in any phase-13 map.** P2:129-137 describes a separate generic fixture phase with an observation solely inside T3's one function, to exercise `concerns` and negative observation verdicts. That is exactly `CONTEXT.md:130-140`, not phase-13 evidence and not a live-host acceptance claim. The empty phase-13 Observations section remains binding (`CONTEXT.md:404-406`).

## Independently derived obligations and check boundaries

Before opening the plans, the design's Verifier block, records, reduction/refusals and the binding context yielded these seven obligations:

1. Dispatch must retain and deliver the complete current native phase authority, exact saved check commands and compiled verifier contract; misleading summaries/configuration must not determine the subject.
2. A complete item patch must match that retained attempt and current source/map authority, be applied atomically, retain original claims/refusals separately, and replay without changing effective evidence twice.
3. Current status must derive from every explicitly associated item, with rejected/not_seen winning, observations capping acceptance, incomplete verification pending, and stale results preserved as history.
4. Only exact owner approval can create, reaffirm, supersede or revoke a basis-bound waiver; the report preserves rejection and never counts waived as met.
5. Execution, evidence, waivers and required human results must jointly authorize completion and its projections; neither publication, stale lifecycle memo nor a verifier can erase human failure or complete the phase early.
6. Explicit review kind/target selection must deliver exact retained material and compiled intent through the existing review subsystem, with aliases and no edit application.
7. A read-only audit must expose actual and broken requirement/phase/plan/truth/map/verdict traces, state their phase-scoped limits, and leave durable authority unchanged.

Sources: `docs/architecture/acceptance.md:215-266`, :354-358; `.planning/phases/13/CONTEXT.md:13-64`, :94-207, :226-311. D-130/D-134 additionally require honest disposable rehearsal, installation and inspected hook retirement at close. They do not create an eighth truth.

**Common boundary is substantive.** P1:86-110, P2:54-78 and P3:111-135 require the real `CARGO_BIN_EXE_cadence serve` process, initialized newline-delimited JSON-RPC over stdio, real temporary project files/store/journal/Git/child commands, native context approval and typed plan publication, allocation/admission, real committed red/green pairs, owner Inspection approval, task closure and suite/risk settlement. There is no preseeded native authority. Handwritten expectations and acknowledged public identities are the oracle. Shutdown/reap and reopened bytes are explicitly required for durability. All seven checks claim durable state or unchanged durable bytes and inherit that requirement; it must include the final assertion after the final server is stopped, not merely a mid-test restart.

All fixture Git calls disable ambient signing and provide a private identity. Required completion commits explicitly use `-S` and disposable key material (P1:107-110 and repeated common setup), preserving D-109 rather than borrowing the owner's key. The live rewrite, protected roots/locks and the bound phase-12 fixture are excluded. The actual phase-12 client pattern is at `crates/cadence/tests/phase12_execution.rs:15-95`; its private environment is at :25-32. The current fixture is an example to adapt, not authority to copy wholesale.

| Check | Boundary and falsifiability assessment |
|---|---|
| T1 | P1:122-135 parses the **actual outgoing operational JSON**, not a renderer or `.md` file. Two current plan contributions and a shared artifact exercise the union; misleading SUMMARY and conflicting configuration discriminate saved commands from alternatives. Identities, associations, full vector/digest, red/green pairs and owner statements are compared to public records and handwritten expected sets. Restart/replay and inconsistent-authority refusal prevent a fixed prompt from passing. |
| T2 | P2:90-106 includes unknown/missing/duplicate items, wrong item revision, stale map/content/truth references, foreign source/occurrence, phase pass, truth status and file-writing payloads. Real commit/publication changes create actual staleness. It includes a valid control and conflicting request-id replay. Reopened **effective acceptance/completion/UAT** bytes remain unchanged on refusal; separately retained refusal history may append. This avoids an impossible whole-store byte-equality oracle. |
| T3 | P2:127-143 calls verification-run, verification-submit and verification-read. It obtains met, concerns, unmet and pending through complete real patches/current applicability, never a direct reducer. Rejected artifacts/shared links and explicit not_seen prevent green checks alone from passing; a separate generic observation fixture proves the cap. Real repair/revision and restart retain rejection and old identity while a fresh attempt establishes current status. |
| T4 | P3:147-158 starts with met and unmet truths backed by independent runs; absent/false/mismatched approval, missing metadata, stale version and verifier variants discriminate exact owner authority. Waiver history and underlying rejection survive restart/replay and later verifier patches. Waived remains a separate count/category. The task also requires owner-only reaffirmation, supersession and revocation. This is an attributed approval boundary, not authentication of a human based on a supplied name. |
| T5 | P3:171-189 exercises real publication Pending seeding, completed execution without verdicts, unmet/current/all-accepted stages, historical and native human results, public authorized human resolution and positive completion. Interleaved publication/read/execute-next and restart expose lifecycle bypasses. Refusals must name unfinished item/input/human work; human bytes and first-pass history persist; context stays immutable. Real progress/undo remain future extensions, not fake operations. |
| T6 | P3:209-222 requires actual review-select/admit/next delivery and retained material for all kinds and aliases, using local delivery without external provider fakes. Missing/ambiguous targets and unchanged target bytes discriminate broad fallback and unauthorized edits. Provider prompt-fragment parity is artifact inspection. The check does not pretend that a successful model review or adjudication is deterministic. |
| T7 | P3:243-257 authors actual requirements/roadmap inputs and native map/verdict associations, including no phase, absent phase, unclaimed and unknown requirements, negative and met chains, superseded map and waiver. The same read-only query serves audit and coverage aliases. Handwritten trace rows and durable before/after snapshots expose hidden or invented edges and side effects. |

None of the seven specified truth checks inherently cannot fail, or fakes the boundary its truth names. The outstanding blockers concern the execution/close contract, not a replacement of those accepted episodes. Artifact regressions are permitted project tests outside the map; calling them artifact regressions does not make their output acceptance authority for another subject.

## D-123 to D-134 delivery

| Decision | Task and exact delivery assessment |
|---|---|
| D-123 | PLAN-1 Task 1 defines the complete strict patch vocabulary (P1:184-196); PLAN-2 Task 1 submits it through `cadence_apply` operation `verification-submit` (P2:97, :171-187). Expected canonical membership comes from the retained union, aliases count once, and explicit not_seen is required instead of omission. Claims, application outcomes, item verdicts and replay remain distinct. Atomicity is one validated snapshot transaction through the existing single writer, with committing-snapshot revalidation, protected namespace/intent arms and recovery validation, not a loop committing rows. |
| D-124 | PLAN-1 Task 2 (P1:213-224) retains the verified-at basis in the new verification model/persistence namespace in the existing store, bound to project/root/occurrence/attempt. It includes current approved truth versions, the **full** publication/content/map revision vector, exact coherent map digest, execution allocation/history and full HEAD/tree with explicit index/material identity. It requires clean committed source and reobservation at commit/before confirmation. PLAN-2 Task 1 rechecks that basis; Task 2 exposes current versus historical identities (P2:174-185, :205-217). It is not stored by mutating approved context or map definitions. |
| D-125 | PLAN-1 Task 3 creates a verification-scoped runner selecting the retained check by item id/revision and deriving its command from that spec (P1:243-262). Launch precedes process execution, result follows observation, replay does not relaunch, interruption remains Unknown, and source/index/material is observed around execution. It rejects alternative commands/suite/CI and retains zero/vacuous/ambiguous output for inspection. The intended relationship is **shared low-level capture/material helpers, separate verification lifecycle**, not reuse of the active task's Verify stage. Existing `capture`/`observe_child` are private and `observe_child` takes an execution Launch (`execution/runner.rs:255-305`), so sharing requires real factoring in the leased file; they are not already a public generic verification runner. “Where useful” leaves the exact factoring to implementation, not permission to reopen closed tasks. |
| D-126 | PLAN-3 Task 1 creates the separate exact-approved owner waiver operation/history (P3:291-303). The model may prepare a typed payload but cannot supply its approval. Missing/false/mismatched/stale approval refuses; changes require owner reaffirmation, revocation/supersession are owner events, rejection stays visible and several waivers cue revisiting the plan. T4's check verifies the origin distinction and met count. |
| D-127 | PLAN-3 Task 2 adds `verification-human-result`, attributed immutable replies, original first-pass and subsequent history, separated imported originals, and owned UAT rendering/guard protection (P3:318-332). Blank is not pass, skip is not waiver, SUMMARY creates no cases. Only the human operation resolves human input. Task 4 leaves UAT untouched while completing; T5 proves a human fail survives all-accepted verifier evidence. This is neither a verifier document-writing patch nor advanced UAT repair. |
| D-128 | PLAN-3 Task 7 routes cad-coverage and cad-audit to the same read-only verification-audit join, removes generation and separate Markdown evidence authority, and renders thin skills (P3:479-495). No coverage percentage, test generation or alternative evidence store is introduced. |
| D-129 | PLAN-1 Tasks 1/2/4 add the project-free `cadence verifier-instructions [--frontdoor]` renderer in main.rs, compile the verbatim Verifier block and strict patch/permission text, use the same authority in operational dispatch, and render `skills/cad-verifier-contract/SKILL.md` and `skills/cad-verify/SKILL.md` (P1:184-199, :225-228, :270-284). Agent files are metadata edits, subject to W2. PLAN-3 Tasks 2/4 extend exact known-path output protection (P3:326-328, :383-384), without claiming control of arbitrary shell. Rendered skills/agents are artifacts, not T1's subject. |
| D-130 | PLAN-3 Task 8 and mandatory close require isolated planning-tree copy, actual classification of history/incompatibilities, backup/restore and interruption/recovery, one writer, a separately authored native demonstration, installed identities, explicit phase-14/native-UAT/delivery-order decisions and go/no-go (P3:509-539, :565-595). No live self-host/import or phase-14 context authoring is authorized. **B2 prevents crediting the evidence-collection requirement as executable as written.** W6 concerns map attachment, not deletion of this close obligation. |
| D-131 | PLAN-3 Tasks 3-5 deliver missing-row-only Pending seeding, full completion authority and required ROADMAP/REQUIREMENTS projections in one confirmed transaction, then native lifecycle/memo applicability (P3:346-362, :374-398, :410-434). All required execution, complete current evidence, met/effectively waived truths and resolved required human results are necessary; concerns is incomplete and waivers do not raise met count. The overlay precedes consistency checks, replaces native SUMMARY/UAT completion at `derivation/mod.rs:93`, extends memo inputs and both execution/next-action paths, and preserves historical semantics. Expected transaction-owned projection writes are accounted for explicitly; they cannot invalidate the very transaction that installed them. Queries cannot repair completion. |
| D-132 | PLAN-3 Task 7 reads active requirements, explicit assignments, claiming plans, phase truth set, actual associations and current verdicts/waivers, retaining edge origins, unavailable reads, limits and next actions (P3:481-499). Orphan/unknown/untraceable claims remain visible; no direct requirement-to-truth authoring or inferred semantic edge is introduced. |
| D-133 | PLAN-3 Task 6 requires canonical kind plus explicit target and all three aliases (P3:445-469). It resolves source document/decision id or exact file/directory/native plan material, feeds existing `review-admit`, `acquire_target` and `review-next`, preserves policy/provider/roster/return/settlement and shares intent in local/provider payloads. Minimalism uses the base reviewer; manual plan retains its configured gate. New renderer outputs the four skills. W3 requires correcting the existing-file claim; this task need not reimplement review. |
| D-134 | PLAN-1 Task 6 defines an explicit-root/preimage procedure and a real-stdio compiled-dispatch prerequisite on disposable native projects, exercising absent/ambiguous/stale/partial cases, recovery, multiple exact matches and near matches (P1:325-346). Real removal is deferred to the orchestrator's close sequence, after installation, actual-byte inspection and backups, preserving siblings/order/matchers/event groups/other guards and confirming both absences (P3:572-590). It is not a test against HOME, a new hook or general deletion service. W5/W6 address location and map provenance; the actual safety/close sequence is specified correctly. |

The existing `CompareTransact` seam is real (`crates/cadence/src/store/writer.rs:107-112`, :484-495). It alone does not implement a verifier authority gate: the planned writer/transaction namespace ownership and intent validation must actually be added. Existing transaction participants have exact preimage/replay validation (`store/transaction.rs:104-115`) and are validated before changes (`store/transaction.rs:983-1024`). PLAN-2 Task 1 and PLAN-3 Tasks 2-4 lease both integration files and explicitly require those additions. No new independent writer is proposed.

## Preservation of earlier decisions and parked scope

- **D-88/D-108:** authoring is not admission or completion. The live `require_execution_ready` now revalidates the retained admission (`crates/cadence/src/plan/persistence.rs:52-59`); the old blanket refusal remains separately named `require_legacy_execution` at :40-49. The plans correctly consume current admission instead of changing a readiness flag or downgrading a native failure (P1:80-81, :217-224; P3:374-382).
- **D-90 to D-98:** one canonical check per current truth, attached exact-approved publication, opaque item identity, explicit shared associations/reasons, current native versions, coherent full-map readback, immutable history and receipt replay remain the prerequisites. `map_history::definition` actually removes associations (`crates/cadence/src/plan/map_history.rs:64-69`), so the planned full digest/vector cannot be replaced by an item hash (P1:213-217; P2:49, :174-176). W1 identifies specification omissions in these plans' own prose maps; no task authorizes weakening the existing validators.
- **D-99 to D-106:** expected properties remain authored text; planning accepts nonblank command text without a runner grammar; runtime saved-command selection is separate. P1:250-253 rejects substitutions without inventing a publication grammar. T2's actual stale union comes from approved publication, not hand-editing the store. Both links need the exact D-101 value correction in W1. No task invents a truth-revision API or publishes a map detached from its plan.
- **D-109/D-110/D-111/D-112:** fixtures use admitted allocations, distinct real red/green receipts and separate owner Inspection approval, followed by required suite/risk settlement; verification runs do not reopen tasks or replace execution history (P1:98-110, :243-262). Failed/Unknown history remains inspectable. Zero/vacuous verification output cannot mechanically become accepted evidence. Fixture signing isolation preserves explicit signed completion commits.
- **D-113/D-114/D-120:** old attempt/progress/receipt history is retained, gaps require actual approved publication and applicable authority, and the live rewrite is not initialized (P1:112-114, :219-224; P2:94-104; P3:511-519, :594-595). B2 is an unsupported close-evidence demand, not permission to bypass those rules.
- **D-115/D-116/D-117/D-118/D-119:** executor compiled guidance is refreshed only within the bounded usability task. No decision amendment, mechanical SUMMARY delivery, full cad-task, precommit clearance, parallel scheduling or other parked execution work is implemented. Approved truth Pending seeds stay immutable; readback is an overlay (P2:214-218, :248-253). This is necessary because `context/persistence.rs:8-35` constructs the original record and `execution/admission.rs:181-184` compares exact approved equality.
- **D-121/D-122:** hook retirement is phase-13 close, not the design scaffolding's superseded phase-12 date. Review results do not imply selected fixes, and provider/return/settlement contracts stay in phase 9/10 (P3:453-464, :579-590). Read/subagent trace-hook removal, progress/health/why/suggest, live-host/model acceptance and selected review edits remain parked to 14/18/30 (P3:529-534, :595).

## Tree, lease and sequencing audit

Frontmatter is structurally consistent with the inspected schema: positive phase/plan, requirements, exact `files`, execution schema 1, nonempty suite and ordered unique task ids with nonempty verify lists. There are no extra frontmatter keys, directory declarations, trailing-separator leases or duplicate exact paths. Counts are **36 / 16 / 53 files** and **6 / 2 / 8 tasks**. Each task-body Files path is in its own frontmatter. None of the specified work inherently adds a third-party dependency, so no required new manifest/lockfile write was identified. B1 is the sole multi-command task Verify.

The eleven shared paths across all three plans are:

`crates/cadence/src/verification/mod.rs`, `verification/model.rs`, `verification/instructions.rs`, `verification/inputs.rs`, `verification/persistence.rs`, `verification_service.rs`, `server.rs`, `store/writer.rs`, `store/transaction.rs`, `crates/cadence/tests/phase13_verification.rs`, and `crates/cadence/tests/support/phase13.rs` (the abbreviated source paths are under `crates/cadence/src/`). All seven shared source files named in the dispatch are included. Numeric sequential execution 1, 2, 3 is explicit at P1:350-355, P2:226-231 and P3:543-548; concurrency would be wrong.

| Existing seam | HEAD evidence and lease assessment |
|---|---|
| Library and binary service registration | `crates/cadence/src/lib.rs:1-18` exports existing domains; P1 Task 1 leases it for verification. `server.rs:25-80` registers service modules with path attributes and P1 Task 2 leases it plus recall. The resident already carries typed review commands (`recall/mod.rs:216-219`, :434-441, :606-613); PLAN-3 can extend the existing review command without inventing a separate resident service. |
| Map and admission | `plan/map_view.rs:49-95` is real filesystem/store readback, with retained publication checks and exact association origins at :146-195. `execution/admission.rs:174-210` validates native context/current authority. These are existing read seams, not new functions. |
| Independent run implementation | `execution/runner.rs:104-116` observes clean committed material; :140-170 selects admitted commands; :255-305 captures real children. `verification/runner.rs` is new. It cannot call the task-stage launch as a verification receipt or assume private capture functions are already exported. Its task explicitly leases both files for factoring. |
| Publication transaction | `plan/persistence.rs:282-297` reconstructs the exact proposed publication snapshot/documents. P3 Task 3 explicitly requires new versioned participant semantics so Pending seeding does not invalidate old reconstruction/replay; it leases plan_service, persistence, writer, transaction and filesystem. This is a real cross-layer change, not a post-publication append. |
| Completion and lifecycle | `derivation/mod.rs:93-109` currently derives completion from SUMMARY and UAT. `derivation/memo.rs:39-92` hashes captured planning inputs, not verification authority. `execution_service.rs:2440-2458` has a separate prepare/memo path. P3 Task 5 leases all those paths plus capture/model/query/consistency, derivation_service and next_action_service and explicitly requires overlay before consistency. New native data must not silently change historical API/encoding semantics. |
| Located execution refusal | `execution_service.rs:2400-2422` currently collapses a derivation error to `lifecycle input validation failed`; P1 Task 5 accurately targets this seam and the actual `DerivationError::StateConflict` fields in `derivation/model.rs:147-151`. `execution_calls_refuse_noninteger_phases_and_legacy_plans_without_dispatch` really exists at `tests/mcp.rs:947`. |
| Review targets and delivery | `review_service.rs:138-153` has the retained admission inputs; :721-775 resolves existing target variants and :777-826 retains inline material. Decision input currently comes from the supplied material, so P3's document-resolution work is necessary. `review/invoking.rs:21-48`, `review/provider/payload.rs:20-25`, `review/specialist.rs:23` and `review/policy.rs:41` are real, correctly named seams. Existing `Target` and Admission fields at `review/model.rs:94-151` can carry the selected material through the current machinery; no necessarily missing model-file lease was established. W3 corrects selection.rs's status. |
| Projection protection | `guard/mod.rs:284-311` currently recognizes store files and SUMMARY. P3 Tasks 2/4 lease it and explicitly add native-owned UAT/completion targets. `store/writer.rs:617-646` currently rejects unknown external participants; writer/transaction/filesystem are all leased to extend this, not bypass it. |

**Order is executable for the core truth implementations.** PLAN-1 creates instruction/model authority, dispatch/persistence/public routing and the common test helper, then independent runner, rendered metadata, usability and disposable retirement rehearsal. T1 needs no submitted verdict, so it does not depend on PLAN-2. PLAN-2 uses the already created attempt/runner for strict submission and then status/readback. PLAN-3 uses those completed operations for owner waivers and human input, adds publication seeding and completion transactions before lifecycle/T5, then review/T6 and audit/T7. Its waiver/human/completion/audit modules are explicitly created by the relevant task; no missing PLAN-1/PLAN-2 creation is presumed. W7 is the conditional renderer/suite problem; B2 is the non-executable final evidence demand. W4 recommends a smaller split, not a reversal of these dependencies.

**Creation leases:** PLAN-1 creates the verification domain files, verification_service, phase13_verification target, common phase13 helper, independent runner/support target and the three retirement procedure/report/test files plus the close document. PLAN-2 adds verdicts/status/render. PLAN-3 adds waivers/human/projections/completion/audit, review instructions, the merged review skill and readiness documents; all are absent at HEAD. Existing verifier/alias/audit/coverage/executor skills and the five verifier agents are edits. Existing `review/selection.rs` is the one incorrectly described creation (W3). No plan requires a pre-existing function with one of the new test names; those are explicit creation specifications.

### Test support layout

`tests/support/phase13.rs` is the **one shared helper module**. `tests/phase13_support.rs` and `tests/phase13_close.rs` are separate integration-test roots containing the named regressions, not two more helper copies (P1:19-23, :41, :254-256, :335-339; P3:22, :54). Separating the truth checks, implementation regressions and destructive-procedure rehearsal is defensible; the file count alone is not unnecessary test apparatus.

The helper does not compile merely by existing under `tests/support`. It must be included from each integration-test root, for example by the existing pattern `#[path = "support/phase13.rs"] mod phase13;`. The root files that must declare that module are all leased. There is no required new `support/mod.rs` or Cargo manifest change. At HEAD `tests/mcp.rs:21-22` and `tests/phase7_lease.rs:22-23` use that exact path-attribute pattern for signing support. Phase 12's main native harness is **private in its own integration target** (`tests/phase12_execution.rs:15`, :98-126), not an already exported phase-12 helper library. Follow/adapt the pattern; do not import that entire test file, which would also drag in its unrelated tests and protected historical fixture path. No helper layout blocker is established, provided the leased roots actually declare the module and the seven acceptance tests remain top-level exact names.

### Task verification and red/green convention

The seven check-delivering tasks each explicitly name a red test commit and green implementation commit: P1:209-226; P2:169-188 and :203-218; P3:291-303, :410-429, :445-464 and :479-495. The common notes retain cause inspection and exclude broken setup/tools from valid red. Supplemental artifact regressions do not gain map check identities or require a second acceptance check.

Every individual test command uses `-- --exact`; PLAN-1 Task 1 uses the single project-free `cargo run -p cadence -- verifier-instructions` invocation with a substantive expected output (P1:197-199). No task uses bare `cargo test` or the full suite. The full suite appears only in each plan's close contract; clippy is separately named project quality work, never acceptance evidence. B1 identifies the task containing two individually narrow commands. A future successful exit selecting zero tests would not satisfy any task's specified “one function” result.

## Review identity and limits

Reviewed SHA-256:

- PLAN-1.md: `9b074be075efce6e1d63ece7a71632ed866354f79731101e8c91b481d34a3f56`
- PLAN-2.md: `aa78cc185758107324fd0ffcba301bd5830cdfe246bfbabc514f4a72c3f3e042`
- PLAN-3.md: `c0154e3f1684797167bd0c91390d3edbd0c19901c8264c1739a3d40d9a72a2c0`

Only this report was written. No plan or source was edited, no Cargo command or mutating Git command was run, and no project was initialized. The protected phase-27 temporary root and lock, the phase-12 captured JSON fixture and its bound root/lock, and `.codex-analysis/` were not accessed. The Cadence plugin was not invoked or treated as installed; 3.x references were inspected only in-tree. Installation, actual hook removal, native runtime behavior and test selection/results remain future execution work.
