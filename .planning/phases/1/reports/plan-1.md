PLAN COMPLETE
Plan: .planning/phases/1/PLAN-1.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1: Add the three forge config keys and every surface they drag | 35d89823 | Continuation of the checkpointed run: its uncommitted working-tree edits verified and one schema alignment column corrected, then the two census tests the checkpoint named (`config.test.mjs` SCP-01, `self-verify.test.mjs` placeholder-keys fixture) updated under the widened lease. `test.mjs routing prose` 722/722. |
| 2: A forge seam that answers what setup should do | 8230221c | `lib/forge-decision.mjs` (pure) + `bin/forge.mjs` (`detect`), CONTRACTS row, census bumped 179/17 to 180/18. 34 new cases. `test.mjs git other` 1276 pass 0 fail; `self-verify.mjs` ok:true. |
| 3: Offer the origin-derived defaults the user confirms | fd55f19d | `isForgeSlug` + `originDefaults` in the pure module; the `ask` arm alone reads the origin and carries `defaults: {provider, repo}`. AC3 both directions proved. `test.mjs git other` 1297 pass 0 fail; `self-verify.mjs` ok:true. |
| 4: Both entry points ask the forge question once | 346f62b5 | `detect` appended to the existing setup chain in both files; three-arm prose; the exception to "ask no configuration questions" stated in both. Two budgets re-pinned, ledger rows NEW-PROJECT-07 and ADOPT-08 updated. `self-verify.mjs` ok:true; `test.mjs prose` 278/278 after the mergeLayers census went 15/9 to 16/10. |
| 5: Providers offered and none picked is a refusal, not a fall-through | 04b11a6c | NONE option plus a stop arm in both entry points, with a `prose-agreement.test.mjs` case asserting option, reason, hint and ORDER by text offset, and a second case proving the check reddens when the arm is deleted. `test.mjs prose` 280/280; `self-verify.mjs` ok:true. |
| 6: The tracker resolves the persisted forge, not the origin host | 96ee016d | `issue-check.mjs` resolves the persisted record; `classifyOrigin` demoted to a one-argument default builder; the forgejo row rebound to `--login <name>` off `teaLoginNameForHost`; two classification skips collapsed to one naming the unset keys, `no-login` rebound and one-line-guarded. Four AC4 cases including origin-removed. Whole suite 3033 pass 0 fail; `self-verify.mjs` ok:true; the reason-literal diff under `bin/` is exactly the three lines named. |
| 7: README names a forge CLI among the prerequisites | a3592980 | One clause added to the prerequisites sentence, zero-dependency claim kept true, README-04 amended. `self-verify.mjs` ok:true; `test.mjs prose` 280/280. |
Deviations: none
Open items:
- The `defaults` object rides the `ask` envelope alone, so `forge.mjs detect`'s three arms are not key-identical. That is the plan's own instruction ("nothing here changes the `configured` or `refuse` arms") and it buys one fewer `git` spawn on the two arms with no question to pre-fill; noted because a later reader may expect the uniform shape the other five fields have.
- `issue-check.mjs`'s envelope `host` is now the persisted `git.forge_host`, which is null on `github` and `gitlab` whose hosts are fixed. Nothing reads that field - `/cad-land` step 1 branches on `action` and prints `reason`/`referenced`/`open` - so this is diagnostic only, but it is a shape change no acceptance criterion named.
- One test is skipped by the suite for an environmental reason unrelated to this plan: `milestone-prune.test.mjs:562` skips itself when the live roadmap holds no completed phase (between milestones). Pre-existing.
