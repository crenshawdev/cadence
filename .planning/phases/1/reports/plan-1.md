PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1 - Demonstrate the ungated GitLab path against the tree as it stands | c42135c | Fixture built outside the repo (scratchpad); all three seam envelopes matched the prediction; `$CAD_SPAWN_MARKER` recorded `glab mr create` then `glab mr merge` with no refusal between. Baselines recorded for later tasks: whole suite 1913 pass / 0 fail, `issue-check.test.mjs` 18 pass. |
| 2 - Extract the repo-layer authorization read into lib/ | 864861f | Continuation dispatch: the prior executor's uncommitted seam edit (import + deletion of the private `repoAutoClose`) and `lib/repo-auto-close.mjs` were inspected against the task and kept as written - behaviour byte-for-byte, raw `JSON.parse`, header carrying the fail-closed reason. The missing half, `repo-auto-close.test.mjs`, was written here: 6 pass / 0 fail. `JSON.parse` count in git-publish.mjs 0, `mergeLayers(` count 1, `git-publish.test.mjs` 23 pass / 0 fail unchanged. `tsc -p tsconfig.ci.json` clean; `detect-commands` reports `lint: null`, so there is no lint command to run. |
| 3 - Word the refusal from one core that receives both booleans | 22935e6 | `authorizationDetail({requested, authorized})` added to `lib/publish-decision.mjs`, pure and total, null when the repository authorized. Gate 1 alone carries it as `detail`; `reason` stays `auto-close-off`. 34 pass / 0 fail (6 new arms), `git-publish.test.mjs` 23 pass unchanged, `tsc` clean. |
| 4 - Wire both booleans into the seam and add the `authorized` subcommand | ff60d9a | `readProtectedBranches` extended to return the merged `git.auto_close` (no second `mergeLayers(` callsite); `publish`'s gate-1 refusal carries `detail`; `authorized [--dir]` emits `ok:true action:repo-authorized` or `ok:false reason:auto-close-off` + the same detail, with no `tornLayerRefusal`. CONTRACTS row gained `authorized: []`. Live fixture: exit 1 with the repository-naming detail, exit 0 when the repo layer sets it. 27 pass / 0 fail; `self-verify` `"problems":[]`; `tsc` clean. |
| 5 - Prove the GitLab arm refuses with no forge CLI spawned | acf5318 | `issue-check.test.mjs` gained the entry-file `test` binding and exports `stub`; still 18 pass / 0 fail. New `git-publish.test.mjs` arm: gh/glab/tea stubs on the child PATH, marker set, `authorized` refuses and the marker file is absent - plus a control run showing the marker is not vacuously absent. Whole suite 1930 pass / 0 fail (baseline 1913 + 17 new arms, so nothing registered twice). |
| 6 - Rewire the two doc surfaces that say GitLab needs no seam call | a20ed10 | `skills/cad-land/SKILL.md` 3(b) and `references/git-publish.md` rail 3 both state the rule instead of the exemption; the seam call sits before the `glab mr create --source-branch` invocation inside 3(b), so the `3(a)`/`3(b)` numbering did not move. Budgets re-pinned (SKILL.md 12268 -> 13005, git-publish.md 4629 -> 4955) in the same commit. The new order pin was watched RED on both mutations the plan names (old sentence restored; call moved after the create) and green again after. `self-verify` `"problems":[]`. |
| 7 - State the two-boolean behaviour where the user sets the key | cb6a163 | `config.schema.json`'s `git.auto_close` purpose and `config-reach.md`'s consumer cell both name the two resolutions and which arm reads which; the reach phrase stays verbatim and the reach cell itself is unchanged. Watched: paraphrasing the phrase out of the purpose reports `unstated-reach` naming `git.auto_close`, restored green. `config-reach.md` re-pinned 17232 -> 17436. `config.mjs get git.auto_close` still answers the merged value; `self-verify.test.mjs` 144 pass / 0 fail. |
| 8 - Pin the two resolutions and the pairing they must not break | fdc13d2 | The `config-seams.test.mjs` divergence arm now asserts all four facts on one global-true/repo-unset pair (`get` true, `authorized` refuses, they differ, gate still halts) plus the repo-layer contrast; watched RED with `repoAutoClose` re-pointed at the merged value. New `prose-agreement.test.mjs` call-site pin: step 3's branch value comes from the up-front `config.mjs get` and from no other source; watched RED with step 3 branching on a direct `.planning/config.json` read. AC7: whole suite 1932 pass / 0 fail, `self-verify` `ok:true problems:[]`, `tsc` clean. |
Deviations: none
Open items: none
