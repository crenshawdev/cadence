PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1: Record the phase's five decisions as CONTEXT.md | ff3236b8 | criteria-size reports context_found:true, context_criteria:6, over:[]; grep -c '^- D-0' prints 5 |
| 2: Repair the six /cad-why tests the v3.6.0 close reddened | 2c01a7a5 | full suite fail 0 (was fail 4); the two live-phases/1 cases were already green, see deviation |
| 3: closeOver orders by parsed instants and guards an unparseable date | e43ff284 | why-corpus.test.mjs 45 pass 0 fail; with the comparison reverted to `p.date >= when` both new cases go red, restored after; closeOver(prunes,'garbage') returns null |
| 4: The seam measures what history simplification excluded | 9ec9c556 | `why.mjs cadence-core/bin/lib/release-decision.mjs --dir .` names exactly b86fc25c, 051f0df1, 9237a539 as `excluded`, each parentCount 2, none of the chain's 7; hermetic repo() names none; why-query + why.test 73 pass 0 fail |
| 5: The chain states that exclusion in words | e6834a79 | `text` names count 3, "3 of them are merges", all three shas and the --full-history invocation; why-corpus.mjs reports 1; hermetic repo() prints no block; truncation note stays last |
| 6: The entry cap carries a claim measurement supports | f25580d5 | re-measured after task 5: at --top 6 capture-file 9,129 B / issue-decision 8,158 B / planning 8,526 B, all under 10,000; at --top 7 capture-file 10,343 B is over. DEFAULT_TOP=6 confirmed maximal; grep shows no surviving claim of 10 |
| 7: The pin that reddens when the number and its reason disagree | 65267c0a | fixture re-renders byte-identically to the live seam (9,129 B at 6, 10,343 B at 7); DEFAULT_TOP=7 reddens two cases, DEFAULT_TOP=5 reddens two, 6 passes. Full suite 2,935 tests fail 0; self-verify ok:true problems:[] |
Deviations:
- [deviation] The plan and the dispatch both assert the suite is red with 6 failures, 5 of them in why-corpus.test.mjs including two pinned to a live `phases/1`. Measured on the clean tree at dispatch: 2,907 tests, fail 4 - three in why-corpus.test.mjs (prune count 25 now 26, --full-history control 25 now 26, labelled-close count 7 now 8) and one in why-record.test.mjs. The two live-`phases/1` cases pass today because commit 321b9b65 ("chore: add phase 1") re-created `.planning/phases/1/`. Their fragility is real and unchanged, so task 2's repair was applied anyway: the both-tiers-are-walked proof moved onto a built root, the discrimination it exists for is now proved on a built root too, and the real-corpus case asserts only the group-level discrimination it can uniquely prove.
- [deviation] The dispatch flagged that task 6's re-measurement might land somewhere other than 6, which would move task 6's `--top 7` overflow assertion. It did not: re-measured AFTER task 5's exclusion block landed, 6 is still the largest cap under which the worst path stays under 10,000 B (9,129 B at 6, 10,343 B at 7 on `lib/capture-file.mjs`). Recorded because the plan named the re-measurement as the thing that must not be trusted from the Notes, and the confirmation is the outcome of that check rather than an inherited figure. No assertion changed.
- [deviation] The dispatch noted that task 7's assertions as planned inspect byte lengths and `DEFAULT_TOP` only, and never the D-13 comment's own claimed figure, which leaves Success Criterion 4 half-closed. Closed properly instead: `lib/why-render.mjs`'s header now carries a parseable `// MEASURED CAP: 6 entries, 2026-08-23.` line, and `why-render.test.mjs` reads that line out of the module source and asserts it equals `DEFAULT_TOP`. Proved falsifiable in both directions - `DEFAULT_TOP = 7` reddens the under-threshold case AND the header-claim case, `DEFAULT_TOP = 5` reddens the over-threshold case AND the header-claim case.
Open items:
- The plan's dispatch stated `.planning/ROADMAP.md:53-56` and `PROJECT.md`'s `### Active` are measurably wrong about which three commits are missing on `release-decision.mjs`. Independently re-measured and CONFIRMED: `0bf62847` is single-parent (`0bba96f4`) and is one of the 7 the chain already carries; the three `--full-history` adds are the merges `b86fc25c`, `051f0df1`, `9237a539`. `.planning/phases/1/CONTEXT.md`'s D-01 records the correction, but the ROADMAP and PROJECT prose still carry the false account and are outside this plan's lease.
- `cadence-core/bin/why.test.mjs`'s new "this repository: the exclusion reaches the rendered text" case names three shas that the exclusion block caps at three; a later merge into main touching `lib/release-decision.mjs` would push one out of the listing and redden it. Left as the plan's Verify asks, with the fragility stated in the case's own comment.
