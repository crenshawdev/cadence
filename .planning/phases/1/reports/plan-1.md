PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1: Register rows name their own file, and the rule takes its rows as a parameter | d91a721 | `file` field + `rows` parameter + `deferred-read-missing-file`; new `deferred-reads.test.mjs` with the AC3 pair, the missing-file arms and the register byte-identity snapshot. 7/7 tests pass, self-verify.test.mjs 133/133 unchanged, tsc clean. |
| 2: regionLabels labels named steps, nested numbered items and plain tag blocks | a0f5578 | Frame STACK, `name=` labels, tag-name labels, nested `execute_parallel(6)`, empty-stack clause. AC1 pair, AC4 falsifier + prefix falsifier, D-04 wrapper-less step, the D-07 `worktree_mode` pair and the four-row label snapshot. 18/18 in the lib test, 1490/1490 repo-wide, tsc clean, self-verify `ok:true`. |
| 3: Heading-scoped labels for workflows that carry no tags | 4fb109e | `##`-and-deeper path tracked while the frame stack is empty, H1 ignored, verbatim text, block frames win. AC2 pair on `config.md` plus both wrong-region falsifiers (walk step 1, `## Direct set`). 24/24 lib tests, self-verify.test.mjs 133/133, tsc clean, self-verify `ok:true`. |
| 4: resident-weight exports the eager set it already builds | 936e135 | `commandEagerSets(root)` extracted and consumed by `residentWeight`; JSDoc names `surface` as the root-relative field and `key` as a non-root-relative realpath. `weight.mjs resident --root .` byte-identical before/after (diff clean), weight.test.mjs 25/25, tsc clean, self-verify `ok:true`. |
| 5: The include-consumer rule, with its one stated waiver | 9eaef70 | New `lib/include-consumers.mjs`: `CODES` (neverNamed/staleWaiver/expiredWaiver), one-row frozen `WAIVED` stated as a phase-2 bridge, `workflows/` branch exemption, self-surface + `@`-line exclusions, `<branch>/<file>` word-boundary matching. 14/14 in the new lib test incl. AC5 both halves, AC6, the self-citing falsifier and both waiver bounds. Live tree: `[]` under the shipped register, exactly one `include-never-named` naming `cadence-core/templates/UAT.md` under `[]`. tsc clean. |
| 6: Register the check as check 16 in self-verify | b52c590 | Header entry 16, import beside the other rule libs, issues pushed after check 15, `include-consumers` appended to `checked`, no `CONTRACTS` row (D-11). Check-13 disk half gained the `CADENCE_DEFERRED_READS` rows seam (same shape as `CADENCE_ROUTE_TABLE`), reporting an unusable rows file rather than falling back. Six new CLI tests: the workflow-anchored pair, the unusable-seam arm, the fixture `include-never-named`, the live-tree zero for all three codes plus `WAIVED.length === 1`, and `include-waiver-stale` at the integration boundary. self-verify.test.mjs 139/139, repo-wide 1516/1516, tsc clean, `ok:true` with `problems:[]`. |
| 7: Ledger row for the new check, and the whole-phase live-tree proof | 4de81e4 | ROADMAP phase 2 criterion 1 and its `Depends on:` line now carry the `WAIVED`-dies-with-the-include pairing and name both waiver arms. `## Claims added after run 1` appended AFTER the run-1 table with the two-sentence preamble and one row, `SELFVERIFY-01`, at `self-verify.mjs:90-104`. All six Verify steps pass: self-verify `ok:true`/`problems:[]`; 1516/1516 repo-wide, 0 fail; the register byte-identity assertion FALSIFIED on purpose (an edited anchor makes `deferred-reads.test.mjs` report 2 fail, reverted with `git checkout --`); both greps exit 0; the run-1 `## Claims` region holds 548 `^\| ` lines before and after. tsc clean. |
Deviations: [deviation] Task 1 asks for a SECOND contract pair anchored at `worktree_mode`, but `<worktree_mode>` is a plain tag that only Task 2's grammar labels - added in Task 2 instead so Task 1's own Verify passes. [deviation] `detect-commands --root /data/code/cadence` returns `lint:null, typecheck:null` (no package.json); the repo's CI typecheck `./node_modules/.bin/tsc -p tsconfig.ci.json` exists locally and is run as the static-analysis step at every commit. [deviation] Task 6 names a check-13 "test seam" without choosing its shape; implemented as the `CADENCE_DEFERRED_READS` path-override env var, matching the repo's existing `CADENCE_ROUTE_TABLE` / `CADENCE_CONFIG_SCHEMA` / `CADENCE_PLUGIN_MANIFEST` precedent rather than adding a CLI flag to a shipped seam's contract, plus a third test asserting an unusable rows file is REPORTED rather than silently falling back to the shipped register. [deviation] Task 7's Verify step 6 specifies `grep -c '^| ' .planning/DOCS-CLAIMS.md` whole-file, which necessarily changes when a row is appended anywhere; counted region-scoped instead (`awk` bounded by `## Claims` and the next `## `), which is the property the step actually states - 548 before and after.
Open items: none

Phase-wide "must be true when done", each checked directly:
- Workflow `<step name="...">`, heading-scoped walk step and contract-skill anchors all pass/fail as one `deferred-read-unread` on the sentence alone (AC1/AC2/AC3 pairs, `deferred-reads.test.mjs`), and through the CLI as well (`self-verify.test.mjs`, the `CADENCE_DEFERRED_READS` pair).
- Wrong-region falsifiers hold in all four families: another numbered item in the same named step, another walk step, another `##` heading, another plain tag block.
- `includeConsumerIssues(root, [])` over a byte-copy of `skills/cad-verify/SKILL.md` + `cadence-core/workflows/verify.md` reports exactly one problem naming `cadence-core/templates/UAT.md`; `cad-help` reports none.
- `node cadence-core/bin/self-verify.mjs` on the live tree: `ok:true`, `problems:[]`, `checked` ends `nul-bytes, include-consumers`.
- The four register rows are byte-identical (asserted against a checked-in literal, falsified on purpose), `DEFERRED_READS.length === 4`, and no measured prose surface moved in the whole phase (`git diff --stat 3cc4549..HEAD -- skills cadence-core/{references,templates,workflows} agents weight-budgets.json` is empty), so `weight.mjs resident` is byte-identical by construction.
- Deleting the waived `@`-include line while keeping the `WAIVED` row turns `self-verify` red with `include-waiver-stale`, asserted at both the lib and the CLI level; the upward `include-waiver-expired` arm fires on a checked-off ROADMAP phase 2.
