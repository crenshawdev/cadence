PLAN COMPLETE
Plan: .planning/phases/1/PLAN-1.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1. Coordinator marker in the lifecycle vocabulary and the append seam | e6edc60 | `COORDINATOR = 'coordinator'` exported from lib/trace.mjs; `--step` added to `trace append` (bare/blank refused as `bad-args`), to planning.mjs's usage block and to self-verify's flag-lint table; census admits the name, exempts it from the `--role` assertion, and refuses `--tokens`/`--role` on any prose line writing it. 51 tests pass, self-verify ok, tsc clean. |
| 2. Commit verbatim's trace as a fixture and pin today's reader output | cb933fa | `cadence-core/bin/fixtures/verbatim.trace.jsonl` copied byte-for-byte from `/data/code/verbatim/.planning/trace.jsonl` (`diff` clean). Render pin in trace.test.mjs (corr `1-573f325`, 28 events, counts 8/0/18/2, five role rows, the one unpaired `cad-reviewer` at `corr: "1"`); suggestion pin in trace-suggest.test.mjs (two entries, `cad-executor` 44% share + `risk_surface`). Both literals, not recomputed. docs/EVIDENCE.md's falsified "only Cadence has a trace.jsonl" passage corrected, EVD-01's deferral left standing. 65 tests pass, tsc clean. |
| 3. Compute the per-step coordinator residue in renderTrace | f4105d4 | `renderTrace` gains a conditional `coordinator` block (`wall_ms`, `bracket_ms`, `residue_ms`, `steps[]` of `{phase, step, ts, residue_ms}` in time order), emitted through `trace render` the way `roles` is. Windows keyed by PHASE not `corr` (D-04); spans taken at the pairing site so an unpaired dispatch subtracts nothing; overlaps merged into a union before subtraction; one `millis()` parser so an unparseable `ts` yields null rather than NaN; residue floored at zero. Seven new tests, including the 10-minute window over a 4-minute bracket at `residue_ms: 360000` and the fixture rendering no `coordinator` key at all. 59 tests pass, self-verify ok, tsc clean. |
| 4. trace suggest reads the coordinator block | 1c81054 | R6 after R5, `kind: 'info'`, `subject: 'coordinator'`, `action: null`; floor `MIN_RESIDUE_MS_FOR_COORDINATOR_INFO = 600000` (the one floor measured in time, not events). Reads the render's figures, recomputes nothing but the share. Silent with no block and below the floor. `RenderLike` typedef extended with the optional block. Three new tests; the fixture's pinned suggestion list and `suggestFromRender(render([])) === []` both pass untouched. 16 tests pass, tsc clean. |
| 5. /cad-report's record-health line reports the residue | 738d750 | `read_record` names `coordinator`; the `Record health:` line reports the residue and its top step when the block is present, nothing when it is not. Two rules added: the figure is the render's own and is never recomputed in prose, and the residue is TIME between brackets, never tokens. Health line stays prose (D-05). `report.md` re-pinned 3,065 -> 3,750 B in the same commit. self-verify ok, no `budget-overrun`, no `unbudgeted-surface`. |
| 6. The coordinator writes a marker at each step it can name | e011fd4 | One standing instruction at the top of `<process>` in `context.md` and `plan.md`, in the fenced seam form with `--family lifecycle --event coordinator --step "<this step's name>"`; one written line per file, run once per step. Bracketing unmoved: `context.md` 1 `--bracket-read` / 1 return / 1 checkpoint, `plan.md` 2 of each. Budgets re-pinned: `plan.md` 21,035 -> 21,662 B, `context.md` 16,845 -> 16,821 B. Census green, self-verify ok, full suite 1,414 tests pass, tsc clean. |
Deviations: [deviation] Task 2's Action asserts the fixture is "all 40 lines" (D-12 measured 40 lines / 8,364 B on 2026-08-12). The live file is 52 lines / 10,703 B - verbatim's phase 2 kept running between the CONTEXT measurement and the copy. Copied byte-for-byte as written rather than truncating to 40, since the decision D-12 locks is "byte-for-byte, unredacted", and the phase-1 slice the pins read is unchanged (still the 28 events D-01 measured). Re-verified the redaction claim against the current bytes: zero absolute paths, every field Cadence's own vocabulary, the only free-text `detail` values `risk_surface`, `complete` and the one adjudication line.
[deviation] Task 6 asserts `context.md` will exceed its budget row and must be re-pinned (D-16: 651 B headroom). The standing instruction is 627 B, so `context.md` landed at 16,821 B - 24 B UNDER the 16,845 B ceiling - and the re-pin was not forced by the check. Re-pinned anyway to the measured value, as the task directs; PLAN-2's spend-gate prose will exceed the new row and re-pin it again. `plan.md` and `report.md` did exceed, exactly as stated.
Open items:
1. AC3 is human-verify and is now walkable, which it was not before task 6 landed. Steps: (1) `cd` into a project with Cadence installed, (2) run `/cad-context <N>` then `/cad-plan <N>` on any phase so the markers are written, (3) run `/cad-report <N>` and confirm the `Record health:` line names a coordinator residue in minutes plus the step carrying the most of it, (4) run `/cad-report` on a phase whose trace predates this phase and confirm the line mentions no residue at all.
2. D-17's four `DOCS-CLAIMS.md` rows for `context.md` (CONTEXT-03, CONTEXT-06/07, CONTEXT-15) are now 14 lines staler than CONTEXT measured them, because task 6 inserted the standing instruction at the top of `<process>`. `.planning/DOCS-CLAIMS.md` is not in PLAN-1's lease, so nothing here touched it; PLAN-2 owns the correction and should re-cite AFTER its own `context.md` edits land.
3. Markers ride `context.md` and `plan.md` only, so `/cad-report` prices the coordinator across `/cad-context` and `/cad-plan` and says nothing about the `/cad-execute` and `/cad-verify` half - on the fixture, a further ~18 minutes between the reviewer's return and the verifier's resolve. Extending the standing instruction to `execute.md` and `verify.md` is one line each plus two budget re-pins. Named for the human in the plan's Notes, deliberately not built.
4. `detect-commands --root /data/code/cadence` returns `lint:null, typecheck:null` and `workflow.lint_command` is unset, so Cadence finds no static-analysis command for its own repo even though `tsconfig.ci.json` ships in the tree and CI runs it. Ran `npx tsc -p tsconfig.ci.json` directly instead (exit 0 on every task). Pre-existing, out of this plan's scope.
