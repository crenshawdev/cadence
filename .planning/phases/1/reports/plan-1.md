PLAN COMPLETE
Plan: .planning/phases/1/PLAN-1.md
Tasks: 2 of 2
| Task | Commit | Note |
|---|---|---|
| 1: Record the usage a provider returned on the `provider/request` event | 36c529cd | Three `extractUsage` readers verified against live provider docs 2026-09-01 (OpenAI openapi `ResponseUsage`, Gemini v1beta discovery `UsageMetadata`, DeepSeek create-chat-completion). Extraction in `callStructured` ahead of the three degrading exits; the figure rides `meta.usage` the way `meta.redactions` already does, so `traceProvider`'s signature is unchanged and the one-event-per-call contract holds. Event carries `usage` (normalized pair) + `usage_raw`; neither key when the response carried none. 4 new tests; `review-provider.test.mjs` 82/82, `self-verify.test.mjs` 177/177, `npx tsc -p tsconfig.ci.json` clean. `mergeLayers(` count in `review-provider.mjs` unchanged at 3, so the `self-verify-merge-layers` census check 12 needed no re-pin and `self-verify.test.mjs` was not edited. |
| 2: Document the usage the seam now reads, on the wire and at the seam | 20a68088 | A usage bullet in each of the three review sections of `provider-api.md`, and the event's two keys plus their denomination in `seam-review-provider.md`. `weight-budgets.json` re-pinned in the same commit: `provider-api.md` 6304 -> 7408 B, `seam-review-provider.md` 3666 -> 4493 B. `node cadence-core/bin/self-verify.mjs` reports `ok:true` with 0 problems - no `budget-overrun`, no `unbudgeted-surface`. Cross-checked mechanically that every `u?.<field>` an adapter's `extractUsage` reads appears in that provider's own doc section: 7 fields, 0 missing. |
| Fix pass: `risk_surface` high at review-provider.mjs:642 | 8496214f | The provider-controlled `usage_raw` object reached the `provider/request` event with no credential fence. `usageOf` now runs the SAME fence the outbound payload crosses (`fence()` - `redactUrl` composed with `redactCredentials`), read for its verdict rather than its text: over the size bound OR fence-altered, the raw object is dropped whole and the normalized pair still rides. No second copy of either regex (D-14); `MAX_USAGE_RAW_CHARS` and the D-11 absent-when-not-real rule both intact, and the pair is numbers alone (`tokenCount`) so it cannot carry a credential. One new test, four hostile shapes (snake_case pair, `authorization` echo, URL userinfo, camelCase name) plus a whole-trace scan for the planted values and a clean-usage negative control; it FAILS against the pre-fix tree with `api_key:sk-live-...` in the event. `review-provider.test.mjs` 83/83, `npx tsc -p tsconfig.ci.json` clean, `self-verify.mjs` ok:true 0 problems. `mergeLayers(` still 3, so no census re-pin. `seam-review-provider.md` gained the one clause that keeps it true (a second reason `usage_raw` can be absent) and its `weight-budgets.json` row was re-pinned 4493 -> 4683 B in the same commit. The three medium/low findings in the review were left as dispatched. |
Deviations: none
Open items:
- Static analysis: `workflow.lint_command` is unset and `planning.mjs detect-commands` reports `lint: null` for this root, so there is no lint command Cadence can find. `typecheck` is `npx tsc -p tsconfig.ci.json` and was run at every commit gate.
- Declined the `traceProvider` signature change CONTEXT D-04 anticipated ("costs a `traceProvider` signature change reached from four sites"). The usage rides `meta.usage`, the same carrier `meta.redactions` already uses, which meets D-04's stated rule at all four sites with no change to the four callers; take the parameter if a task ever needs a usage figure that is not the current call's.
- Bounded `usage_raw` at 2048 serialized chars. The usage object is bytes a provider chose and the event lands in a record whose whole ceiling is `MAX_TRACE_BYTES` (1 MiB), so an unbounded copy would let one hostile or broken response rotate the run record away on every append. Over the bound the normalized pair still rides and the raw object is dropped. Every shipped provider's usage object is well under 400 bytes, so no live call reaches this arm.
- Both flagged assumptions in CONTEXT.md resolved as stated rather than as unknowns. The three usage field names were verified against live documentation on 2026-09-01 and are pinned in `provider-api.md`. Whether a provider returns usage on a degraded response is still unobserved on real traffic; the D-04 rule is implemented on all three degraded outcomes and tested against fixtures that carry usage, so it costs nothing if a provider sends none.
- `usage`/`usage_raw` are written but not yet projected: `trace render` exposes them only through `events[]`. PLAN-2 folds them into the render and the `/cad-report` reviewer row.
- Full suite run twice rather than once: `node cadence-core/bin/test.mjs` (no `workflow.test_command` is set and the repo has no `package.json`, so this is the runner the project ships). The first run reported `pass 3687 / fail 0 / skipped 1`; the second was only to read the exit code cleanly (`0`) after a shell pipeline swallowed it. No repair round was needed - the suite was green on the first run.
- Fix pass, left by dispatch: the three non-blocking findings in `.planning/phases/1/REVIEW-risk_surface-plan-1.md` are untouched - the two `medium` (Gemini's `(thoughts ?? 0)` turning a malformed component into a silent undercount, line 1121; usage read only after the non-2xx exit so a usage-bearing 429/500 records none, line 1325) and the one `low` (counts accepted as any finite non-negative number rather than safe integers, line 992).
- Fix pass suite: `node cadence-core/bin/test.mjs` (no `workflow.test_command` set, no `package.json`) green on its one run - `pass 3688 / fail 0 / skipped 1`, exit 0. Static analysis unchanged: `detect-commands` still reports `lint: null` for this root, and `npx tsc -p tsconfig.ci.json` ran clean at the commit gate.
