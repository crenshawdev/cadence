PLAN COMPLETE
Plan: .planning/phases/1/PLAN-1.md
Tasks: 5 of 5

| Task | Commit | Note |
|---|---|---|
| 1: The query grammar and the two git invocations | 8aeb289b | why-query.mjs (parseQuery, probeArgv, bareArgv, lineArgv, classifyResult) + why-query.test.mjs, 22/22 passing. Completed by the prior executor of this plan. |
| 2: The deterministic renderer and the entry cap | 683608d8 | why-render.mjs (renderChain, DEFAULT_TOP=10) + why-render.test.mjs, 5/5 passing. Completed by the prior executor of this plan. |
| 3: The seam | 417049d3 | why.mjs + CONTRACTS row in arg-contract.mjs + why.test.mjs (10/10); arg-contract-adoption.test.mjs green (283 refusals, new row exercised); self-verify ok:true problems:[]. Completed by the prior executor of this plan. |
| 4: Determinism, and the record that is not an input | 417049d3 (no new commit) | Task 4's three required tests (repeat-run byte-identity, trace.jsonl presence/absence, git-only child-process scan) were written into why.test.mjs while completing task 3's own file, since both tasks declare that same file. Re-run standalone at the time: 10/10 passing, including all three. No new diff to commit. Completed by the prior executor of this plan. |
| 5: Register the command surface | cbba5d59 | `skills/cad-why/SKILL.md` (self-contained per D-14, `allowed-tools: [Bash]` only, relays `text` verbatim per D-02); `/cad-why` row in the Support cluster of `references/COMMANDS.md`; `weight-budgets.json` gains `skills/cad-why/SKILL.md: 1293` and re-pins `references/COMMANDS.md` 5196 -> 5393; README "27 skills" -> "28 skills"; `arg-contract.test.mjs`'s literal counts 168 -> 170 and 16 -> 17 to match task 3's committed CONTRACTS row. Verify matched prediction exactly: `self-verify.mjs --root .` `{"ok":true,...,"problems":[]}`, `test.mjs` 2719 tests / 2718 pass / 0 fail / 1 skipped, `prose-agreement.test.mjs` 43/43. Static analysis: `detect-commands` returns `lint:null`, `typecheck:"npx tsc -p tsconfig.ci.json"` - tsc run, exit 0, no diagnostics. `lease-check --phase 1 --plan 1` `ok:true` (staged 5, declared 12). |

Deviations:
- [deviation] LEASE WIDENED, approved at the prior executor's structural checkpoint. `cadence-core/bin/arg-contract.test.mjs` was not in this plan's `files:` list, but its `'every flag in every row declares a complete grammar'` test hard-codes the CONTRACTS table's totals (168 flag entries, 16 scripts), which task 3's fully-in-lease `why.mjs` row correctly moves to 170 and 17. The plan asserted task 5's `files:` list was sufficient to reach `test.mjs` 0 failures; it was not. The user approved adding that path to `.planning/phases/1/PLAN-1.md`'s `files:` list, the edit is in the working tree (UNCOMMITTED - it is a planning doc and belongs to the orchestrator's docs commit, so this executor did not stage it), and `lease-check` returns `ok:true` against it. The two-line count fix is committed in cbba5d59.
- [deviation] Task 5's `human-verify:` clause was NOT taken as a `<checkpoints>` stop. It asks for `/cad-why cadence-core/bin/lib/seam-io.mjs` to be run IN A SESSION and its printed chain compared to the seam's `text` field - a slash command in the user's own session, which an executor cannot invoke. The mechanical half of the Verify is complete and asserted above; the session half is an open item below with the evidence needed to close it in one look. Same routing the v2.5.0 phase-1 and v2.6.0 phase-2 executors took on their own human-verify halves.

Open items:
- HUMAN-VERIFY, task 5: run `/cad-why cadence-core/bin/lib/seam-io.mjs` in a session and confirm the printed chain is byte-identical to the seam's `text` field. The comparand is already measured - `node cadence-core/bin/why.mjs cadence-core/bin/lib/seam-io.mjs --dir .` at cbba5d59 returns `ok:true`, `shown:1`, `total:1` and a `text` of exactly eight lines: `commit 71d2e92b9515bc622dec8d882f1cb9da12514310 (71d2e92b)` / `date: 2026-07-16T12:11:25-04:00` / `subject: refactor(seams): one implementation of the seam output convention` / then `phase:`, `plan task:`, `decision:`, `deviation:`, `review:` each reading `not yet joined`. Those five stated-absent lines are task 2's placeholders that plans 2 and 3 replace with quoted record text, so the human check is about the relay, not about the join.
- Task 4 landed no commit of its own - its Verify was already satisfied by 417049d3 (task 3), which built the three required tests directly rather than in a separate pass. Carried forward from the prior executor's record.
- `skills/cad-why/SKILL.md`'s prescribed invocation was hardened against the `untrusted_input` surface before committing, and the leaner form the prior executor left was not shipped: it inlined the caller-supplied query into a DOUBLE-quoted shell word (`"$ARGUMENTS"`), where `$(...)` and a backtick execute before Node starts. `references/conventions.md`'s caller-derived-text rule states exactly that hazard, but `lib/text-transport.mjs`'s scan cannot see this site - it keys on a FLAG followed by a quoted value, and this is a positional - so self-verify's clean answer here is vacuous rather than an adjudication. The invocation now single-quotes the query and the skill refuses a query containing a single quote rather than escaping it, so the failure direction is a visible refusal instead of an executed substitution. This is the tree's first prose site putting a positional caller-derived value into a shell word; whether `lib/text-transport.mjs` should grow a positional arm is left as a finding for the phase, not built here.
- The plan's own Notes already record that D-13's stated mechanism (a `lib/bulk-output.mjs` register row) does not hold - `BULK_SHAPES` watches only `trace render`, `recall` and `git diff`, so check 20 cannot report an unregistered `/cad-why` site. The decision is met by its other arm, task 2's default entry cap of 10. No `lib/bulk-output.mjs` edit was made. Restated here because AC7 cites D-13.
