PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1: State the `host[:port]` grammar once, beside the slug grammar | 94207af4 | `splitForgeHost` added to `lib/forge-decision.mjs` - one function judges and splits, portless answers a null port, bracketed IPv6 refused as a stated decision. 8 new arms; `forge-decision.test.mjs` 52 pass 0 fail. |
| 2: Refuse a malformed typed forge value at the config write face | 6688a816 | `checkValue` split into `checkType` + `checkGrammar` over a frozen `GRAMMARS` registry; `grammar` marker on `git.forge_repo` (forge_slug) and `git.forge_host` (forge_host); `_meta.types` documents the marker and an unregistered one errors. `config.test.mjs` 99 pass 0 fail; `validate --file .planning/config.json` ok:true. |
| 3: Carry the port from `git.forge_host` into the login match | 8812ecf5 | `teaLoginNameForHost` splits with `splitForgeHost` and passes both halves to `loginNamesHost`; both now-false header sentences corrected. 5 new arms; `issue-decision`+`issue-check`+`issue-filing` 100 pass 0 fail, no assertion loosened. |
| 4: Report whether an origin URL SPELLED a port | a3b7b4a5 | `portSpelled` derived in `splitOrigin` off the same `AUTHORITY` port group, carried on all four `classifyOrigin` shapes; `httpPort` and its readers untouched. 2 new arms; `issue-decision.test.mjs` 33 pass 0 fail. |
| 5: Refuse a `--remote-url` whose port the configured instance does not serve | 6fec1600 | `create` reads the merged config for `--dir` (binding `warnings`, which now rides every create envelope), splits `git.forge_host`, refuses arms (a) and (b) before the PATH check and before the login probe and accepts (c). Two stale header claims corrected. 6 new arms; `forge.test.mjs` 52 pass 0 fail. |
| 6: Refuse a `--remote-url` on the gitlab arm | 9a81077d | The `else` arm of the same `row.wiresRemote` branch, keyed on the table and never on the string `gitlab`; names the `--remoteName origin` conflict and fires before any spawn. 1 new arm; `forge.test.mjs` 53 pass 0 fail. |
| 7: Ask for the port, and carry it whole into the URL setup builds | a6719247 | Question c in both workflows admits `host:port` and names the browser-vs-SSH port distinction; `new-project.md`'s `--remote-url` sentence says WHOLE. Budgets re-pinned 20810->21030 and 25913->26241. `prose-agreement`+`deferred-reads` 78 pass 0 fail; `self-verify` ok:true, empty problems. |
| 8: State the port grammar on the two reference surfaces | e8a964b0 | `config-catalog.md`'s `git.forge_host` row states `hostname:port` and `git.forge_repo`'s says the slug is shape-checked at the write face; `config-reach.md` gains `bin/forge.mjs create` as a reader. Budgets re-pinned 12719->12995 and 23586->23726. Also re-pinned `self-verify.test.mjs` check 12's mergeLayers callsite census 18->19 (task 5's new read; the file is in the plan's lease). `self-verify` ok:true with empty problems, `bin/test.mjs` 3456 pass 0 fail 1 skipped. |
Deviations: none
Open items:
- No `lint` command exists in this project - `planning.mjs detect-commands` reports `lint:null`, `typecheck:"npx tsc -p tsconfig.ci.json"`. The typecheck was run and was clean after every task; there was no lint to run.
- Declined a general `validateValue`-style extension point on the schema: the `grammar` marker resolves through one frozen registry of two named predicates, because task 2's `Verify:` exercises exactly those two plus the unregistered-marker error. Make it pluggable when a task states a third grammar that a registry entry cannot express.
- Declined widening `create`'s config read beyond `git.forge_host`: the merge is already bound and its `config` object is in hand, so a future task needing another persisted key adds a line rather than a read.
- `## Must be true when done` line 8 (GH-102, AC7's reachable clause) is folded in as a UAT verification per the plan's own Notes, not as a task. Re-measured from this execution environment during this dispatch: `GIT_SSH_COMMAND="ssh -o BatchMode=yes -o StrictHostKeyChecking=yes" git ls-remote --exit-code ssh://git@ssh.jcrenshaw.dev:2222/crenshawdev/cadence-archived.git HEAD` exits 0, so the unattended host-key blocker that left it unproven at the `v3.7.1` close is gone. A PORTED-instance origin wired by `create` itself still needs a live ported Forgejo, which this environment does not have; that half belongs to the phase's UAT.
