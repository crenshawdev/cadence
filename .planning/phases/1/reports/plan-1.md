PLAN COMPLETE
Plan: .planning/phases/1/PLAN-1.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: The refusal-hint rule and its exclusion register | afb07999 | `cadence-core/bin/lib/refusal-hints.mjs` created: `CODES`, `REGISTER` (2 token rows, 7 file rows, one-line reason each), `refusalSites(root, register = REGISTER)` and `refusalHintIssues(root, register = REGISTER)`. Verify predicted and observed identically: **243 in-scope sites, 215 hintless issues**, 154 of them in `planning.mjs`, `false` for the excluded-file probe and `false` for the excluded-token probe; every entry carries a non-empty `file` and a `detail` matching `line <N>: `. That pair (243 / 215) is what AC2 asks the phase SUMMARY to state. Typecheck (`npx tsc -p tsconfig.ci.json`) clean; `detect-commands` reports `lint: null`. |
| 2: Wire the rule into self-verify as check 22 | 3425165e | Header register entry 22 added, `refusalHintIssues(root)` called beside `includeConsumerIssues(root)`, `refusal-hints` appended to `checked`. Predicted `False 215 0 True True`; observed exactly that. Three sampled entries (`git-branch.mjs:142` `e.seam`, `planning.mjs:3824` `bad-args`, `worktree-base.mjs:174` `e.seam`) each open on a real `emit`/`fail` emitting `ok:false` with no hint. The tree is now deliberately RED, on `hintless-refusal` and nothing else - AC1's deliverable. Typecheck clean. |
| 3: Fixture tests for the rule, including the injected register | 47feb84f | `cadence-core/bin/refusal-hints.test.mjs`, 23 tests, 0 failures as predicted. The Verify's falsification half was executed: hard-coding the shipped register inside `refusalSites` reddens exactly the two AC6 tests (21 pass / 2 fail); the mutation was reverted and the tree confirmed clean before the commit. Typecheck clean. |
| 4: The CLI-wiring assertions for check 22 | 1dc3b577 | Two CLI tests on `binFixture` - a hintless `bare.mjs` reported as `line 4: no-input` while a hinted `hinted.mjs` is not, and `checked` naming `refusal-hints`. The live-tree assertion is untouched. Whole suite: `prose` 276 pass / 2 fail (both the expected live-tree reds), `routing git planning review` 1834 pass / 0 fail, `other` 847 pass / 0 fail / 1 skipped. `self-verify.mjs --root .` reports exactly one problem kind, `hintless-refusal`. Typecheck clean. |
Deviations:
- [deviation] The plan asserted (Must-be-true bullet 5, and task 4's Verify) that the ONLY failing test after this plan would be `self-verify.test.mjs`'s `the repo itself passes self-verification`. Two tests in that file assert the live tree is clean: the named one, and `entry: a valueless or empty --root refuses instead of linting the cwd`, whose last two lines are `assert.equal(run([]).ok, true)` and `assert.equal(run(['--root', '.']).ok, true)` (self-verify.test.mjs:2261-2262). Both fail, both for the identical reason, and both close the moment PLAN-3's sweep lands. I left the second test exactly as it is rather than filtering `hintless-refusal` out of it: the plan's stated reason for not weakening the first applies word for word to the second. What "done" means is unchanged - PLAN-3 must leave `self-verify.test.mjs` fully green, which is now two assertions instead of one.
- [deviation] Task 4's Verify asks for `node --test cadence-core/bin/self-verify.test.mjs 2>&1 | grep -c "^not ok"` to print `1`. Node's DEFAULT reporter emits no `^not ok` lines at all, so the command as written prints `0` regardless of the result; under `--test-reporter=tap` it prints `2`, for the reason the deviation above states. The substantive half of the criterion holds: the failing assertions are only ever about `hintless-refusal` entries, and `self-verify.mjs --root .` reports no other problem kind.
Open items:
- `refusal-hints` is named by no row in `cadence-core/bin/test.mjs`'s `GROUPS`, so it runs in the derived `other` group rather than in `prose` beside its sibling `include-consumers`. It DOES run in a full `test.mjs` invocation, so nothing is unverified; `cadence-core/bin/test.mjs` is outside this plan's `files:` lease, which is why it was not touched.
- The rule consumes a template literal WHOLE, exactly as `lib/skim.mjs` does, so a refusal written inside a `${...}` interpolation would not be seen. This tree has none; the lean shape met the Verify and the header states the limit.
