PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1. Split `seams.md` into a router plus one cold file per seam | eefdf70f | seams.md 25,068 -> 2,323 B; seam-ask-user.md 1,902, seam-review-provider.md 3,095, seam-spawn-agent.md 19,863. self-verify ok:true / problems []; weight.mjs byte-equal on all four; `grep -c 'route.mjs" resolve' seams.md` = 0; full suite 3266 pass 0 fail. |
| 2. Pin every router branch with a self-verify check and a census row | 73e573c7 | `lib/reference-routers.mjs` + self-verify check 24, `reference-routers` in `checked`. Both negative arms observed live: cold file renamed away -> `ok:false` `reference-router-missing-cold` naming `seam-ask-user.md`; Read line deleted from `seams.md` -> `ok:false` `reference-router-branch-unread` naming the `ask-user` branch. 18 pure-rule tests + 2 live-tree tests; full suite 3286 pass; tsc clean. |
| 3. Re-point every `seams.md` citation that names a specific seam or rule | 0b83858f | 42 citations over 18 surfaces. `grep -rn "seams\.md" cadence-core/workflows cadence-core/references skills agents METHOD.md DESIGN.md` returns NOTHING - no family-level citation survived, so the router is cited by no prose surface. `seam-call-counts` held at 14 and 6 with no re-pin; sixteen budgets re-pinned; self-verify ok:true; full suite 3286 pass. |
| 4. Cold-split the `risk_surface` trigger contract out of `review-triggers.md` | 9fa39dc4 | `references/risk-surface.md` 9,188 B; `review-triggers.md` 40,511 -> 33,250. `grep -c "risk_surface detection"` = 0 in the router, 1 in the cold file; `git-guard.md` rail 4 names `references/risk-surface.md`. prose-agreement 47 pass including the three-way category agreement against `config.schema.json` and `route-table.json`; ROUTERS 4 -> 5 with census marker and registry row moved in the same commit. |
| 5. Cold-split the cross-model reviewer arm out of step 4 | 3124ae9b | `references/review-cross-model.md` 9,383 B; `review-triggers.md` 33,250 -> 26,187. `grep -c "mktemp" review-triggers.md` = 0; self-verify ok:true with `bulk-output`, `scratch-path` and `text-transport` all in `checked`; scratch-readback executes the moved `node -e` composer and passes. bulk-output register 17 -> 18 rows, `4 redirect and 3 file` unchanged; ROUTERS 5 -> 6. |
| 6. Cold-split the outcome record out of step 5 | df67210d | `references/review-record.md` 5,977 B; `review-triggers.md` 26,187 -> 20,153, under half of the pre-split 40,413. All eight reference files byte-equal to `weight-budgets.json`; self-verify ok:true / problems []; full suite 3286 pass 0 fail 1 skip; tsc exit 0. text-transport register held at 36 rows / 20 derived, `trace.test.mjs` BRACKETING for `review-triggers.md` held at 1 with no re-pin; ROUTERS 6 -> 7. Negative arms re-checked against the FINAL register: `review-record.md` moved aside -> `reference-router-missing-cold` naming the `record` branch; its Read line deleted -> `reference-router-branch-unread` naming the same branch. |

MEASURED, before and after (task 6's `node cadence-core/bin/weight.mjs`, not the plan's figures):

| file | before | after |
|---|---|---|
| `cadence-core/references/seams.md` | 25,068 | 2,323 |
| `cadence-core/references/seam-ask-user.md` | - | 1,902 |
| `cadence-core/references/seam-spawn-agent.md` | - | 19,863 |
| `cadence-core/references/seam-review-provider.md` | - | 3,095 |
| `cadence-core/references/review-triggers.md` | 40,413 | 20,153 |
| `cadence-core/references/risk-surface.md` | - | 9,188 |
| `cadence-core/references/review-cross-model.md` | - | 9,383 |
| `cadence-core/references/review-record.md` | - | 5,977 |

An ask-only site reads 2,323 + 1,902 = 4,225 B where it read 25,068. A site that runs risk-surface detection and matches nothing reads 9,188 B where it read 40,413. A default project (`review.reviewers` unset) never opens the 9,383 B cross-model arm at any fire.

Deviations:
[deviation] Task 1. The Action names FOUR reads in `prose-agreement.test.mjs` that resolve to nothing after the move; there are FIVE. IVW-01's `section(doc('cadence-core','references','seams.md'), '## Seam: ask-user', ...)` is the fifth and follows the ask-user seam into `seam-ask-user.md`. Re-pointed with the other four; the task's `Verify:` is met.
[deviation] Task 2. The Action says to seed the register with `review-triggers.md`'s triage-gate branch as "true of the tree as it stands after task 1". It was not: all five triage-gate citations were bare `references/triage-gate.md`, and arm 2 (and phase criterion 2) require a `${CLAUDE_PLUGIN_ROOT}` path. Step 6's RE-READ sentence now spells the plugin-root path - it is the branch's own Read instruction - while the other four stay bare as prose citations. That added 35 bytes, so `review-triggers.md`'s budget was re-pinned in the same commit. Both files are in the plan's declared lease.
[deviation] Task 2. The Action names `cadence-core/references/` as the census row's subjects. That directory lease trips the half-the-plans rail `planning-lease-check.test.mjs` asserts - replayed 2026-08-25 it refuses 21 of the 37 plans declaring under `cadence-core/bin/`, and the rail's own message says to narrow or remove rather than tune. Narrowed to `cadence-core/bin/lib/reference-routers.mjs`, the subject every other register census in `CENSUSES` carries and the only file an edit to which can move either number, since both are read off `ROUTERS`. Tasks 4, 5 and 6 each declare that module and its test, so the row bound every remaining cut in this plan.
[deviation] Task 4. The Action names ONE `## risk_surface detection` read in `prose-agreement.test.mjs`; there are THREE - the three-way category agreement, IVW-01's `pinRecommended` section, and IVW-01's ask-user SITES entry. All three re-pointed at `references/risk-surface.md`.
[deviation] Task 6. The Action does not name `prose-agreement.test.mjs`, but GAT-04's fenced-receipt scan reads `review-triggers.md` for the `adjudication` receipt command and asserts all five settle points are present. That command moved, so the file list in that test follows it to `review-record.md`, exactly as `trace.test.mjs`'s BRACKETING rows travel with a moved dispatch. The file is in the plan's declared lease.
[deviation] Task 6 / the plan's Notes. The Note predicts "a ~17 KB `review-triggers.md` router". The router as first written measured 21,295 B - over the criterion's 20,206 line, because the Note's arithmetic subtracts the moved regions and adds nothing back, while criterion 3 requires the router to CARRY the discriminator grammar, the persistence sentence and each branch decision, all of which are new router prose. It came under only after tightening prose I had authored in tasks 4-6 - specifically dropping from the router four things stated verbatim in a cold file (the `ok:false` example line, the tier-indexing formula, and two enumerations of a cold file's own contents) and correcting one pointer that named step 5 for a rule that had moved out of it. Final 20,153, with 53 bytes of margin. No rule criterion 3 requires hot was dropped, but the criterion has no headroom worth relying on: the next sentence added to this router breaks it.

Open items:
- Comments under `cadence-core/bin/` still cite `references/seams.md` for prose that moved: `lib/surface-scan.mjs:251-255` (the ask-user option cap), `lib/route-relay.mjs:9,31,41` (the relay mandate), `lib/deferred-reads.mjs:37,102` (File round-trip), `lib/dispatch-phrasing.mjs:79`, `lib/window-budget.mjs:16`, `planning/core.mjs:368`, `review-provider.mjs:5,189,196`, plus five `config.schema.json` `max_dispatch_tokens` purposes. Task 3's declared surfaces and its Verify grep both stop at `cadence-core/workflows`, `cadence-core/references`, `skills`, `agents`, `METHOD.md` and `DESIGN.md`, so `cadence-core/bin/` and `config.schema.json` are outside this plan's lease. Each still lands on a router that indexes where the rule went.
- Five budget rows are pinned ABOVE their live size by prose edits that predate this plan: `workflows/debug.md` 6920/6911, `skills/cad-capture/SKILL.md` 5850/5751, `skills/cad-executor-contract/SKILL.md` 12970/12811, `skills/cad-plan-review/SKILL.md` 2353/2343, `skills/cad-verifier-contract/SKILL.md` 10805/10792. The budget check is a ceiling, so nothing reddens. No task here touches those files and none is in the plan's lease.
- No prose surface now cites `references/seams.md` at all - every citation named a specific seam, so none stayed on the family. The router is reachable by name and is held honest by check 24's register, but nothing points a reader at it. Whether the seam FAMILY deserves a citation somewhere (`references/conventions.md`'s `## Subagents and reviews` is the obvious host) is a call for the human.
- `weight.mjs resident` reports all six new references as `zeroResident`, as the plan's Notes predicted: reachability is one hop from a command's eager set and a file cited only by another reference does not count as reached. Pre-existing behaviour, unchanged by this phase.
