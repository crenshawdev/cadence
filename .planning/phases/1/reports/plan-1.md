PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1: The workspace builds a `cadence` binary that serves MCP on stdio | 1c82ce5d | Verify met exactly as predicted: `cargo build --locked` exit 0, `cadence --version` -> `cadence 3.7.12`, initialize answered with `"serverInfo":{"name":"cadence","version":"3.7.12"}`, process exit 0 on stdin close, `git status --porcelain` shows no `target/`, `grep -c 'channel = "1\.'` -> 1. clippy `-D warnings` clean; `npx tsc -p tsconfig.ci.json` exit 0 after the ephemeral `npm install --no-save typescript @types/node` the CI job also does. |
| 2: The typed envelope vocabulary as one serde-tagged enum | d186df60 | `cargo test --locked envelope` -> 6 tests, all pass, including `refused_is_a_successful_call_carrying_its_refusal` and `not_applicable_keeps_its_hyphen`. `grep -n 'not-applicable'` finds the tag at the serde attribute and in two assertions; `grep -c 'structured_error\|CallToolResult::error'` -> 0. clippy `-D warnings` needed `#[cfg_attr(not(test), expect(dead_code, ...))]`: the vocabulary is unused outside its own tests until task 3, and the plain `cargo build` warned. |
| 3: One declared tool, `cadence_version`, answers with an `ok` envelope | 45f9ded8 | `cargo build --locked` exit 0. The three-line pipe returned `structuredContent {"status":"ok","version":"3.7.12","os":"linux","arch":"x86_64"}` with `isError: false`; `tools/list` returned exactly one tool named `cadence_version` whose `outputSchema` is a `oneOf` over all four envelope tags. Also removed envelope.rs's `expect(dead_code)` - task 3 is the condition it was written to detect. |
| 4: The spawned-binary JSON-RPC harness proves the surface over the wire | 8726c7ee | `cargo test --locked --test mcp` -> 4 tests, all pass; `grep -c 'CARGO_BIN_EXE_cadence'` -> 1. First run had 3 of 4 passing - see the deviation below. |
| 5: The test workflow builds and tests the crate beside the Node matrix | 7a916038 | YAML job list -> `['cargo-test', 'node-test', 'self-verify', 'typecheck']`; `grep -c -- '--locked'` -> 3; `git diff --numstat` -> `29 0`, insertions only. `node cadence-core/bin/self-verify.mjs` clean (30 checks, 0 problems). Human-verify still open: the next pull request run showing the job green. |
| 6: A byte-reproducible release archive | 2b941124 | After `cargo build --release --locked`, two runs with `touch -d '2001-02-03'` on the binary and `umask 077` set between them printed the identical line `32ccd28b...  cadence-v9.9.9-x86_64-unknown-linux-musl.tar.gz`, and `cmp` says the two archives are byte-identical. `tar tzf` -> exactly 2 entries, `<name>/cadence` and `<name>/LICENSE`, at 0755/0644 with uid/gid 0 and mtime 0. `sha256sum -c` in the output directory -> OK. Six hostile archive names (`..`, `../evil`, `a b`, `-rf`, empty, `x/../y`) each refused before any file is written. |
| 7: The release workflow cross-compiles the four targets behind its guard | 9e25ffff | `sorted(jobs)` -> `['build', 'guard', 'publish']` and `'workflow_dispatch' in on` -> True; matrix targets are exactly the four excerpt rows; `jobs.build.needs` -> `'guard'`; all 5 `uses:` lines carry a 40-hex SHA (upload-artifact `043fb46d...` # v7.0.1, resolved with `gh api repos/actions/upload-artifact/commits/v7.0.1`). `git diff --numstat` -> `112 0`, additions only, `publish` and the workflow-level `contents: read` untouched. The packaging step's body was extracted and run verbatim as bash with the matrix expressions substituted: it wrote the archive and the fenced sha256 line into `$GITHUB_STEP_SUMMARY`, exit 0. Human-verify still open: a live dispatch showing four green legs, four artifacts and four summary lines. |
Deviations: [deviation] Task 1's Action says `run_serve` follows excerpt's `run_serve` exactly; task 4's Action says closing stdin makes the server exit 0. Copied exactly, both cannot hold: excerpt unwraps `serve()`, and a stdin close before `initialize` returns `ServerInitializeError::ConnectionClosed("initialize request")`, so the process panicked and exited 101. Fixed in `crates/cadence/src/main.rs` (inside the plan's lease): that variant and `Cancelled` return `ExitCode::SUCCESS`, every other startup failure still panics. Task 1's own Verify is unaffected and was re-run green.
Open items:
- Human-verify, task 5: the next pull request run showing `cargo-test` green beside the Node matrix. Nothing is pushed, so this cannot be observed from here.
- Human-verify, task 7: a live `workflow_dispatch` on this branch showing `guard` green with the reachability step skipped, four `build` legs green, four artifacts and four sha256 lines in the run summary. The runner labels `ubuntu-24.04-arm`, `macos-15-intel` and `macos-latest` are excerpt's as of 2026-09 and a deprecated one fails its leg at run time.
- `workflow.lint_command` is unset and `detect-commands` reports `lint: null`, `typecheck: npx tsc -p tsconfig.ci.json` - a Node-only pair that sees none of the Rust. `cargo clippy --locked --all-targets -- -D warnings` was run before every commit as the Rust half and is clean, but nothing configures it, so nothing enforces it on CI.
- The crate version is `3.7.12`, read from `.claude-plugin/plugin.json` as task 1 directs, so `cadence --version` prints `cadence 3.7.12` on a branch whose cycle ships as `4.0.0`. Phase 4 moves the tag, the plugin manifest and the crate together; until then the binary reports the last 3.x number.
- `package.sh` promises identical bytes for identical inputs on one runner image, not across two. zlib's deflate output may differ between implementations, and python's `gzip` is the only mechanism present on both GNU-tar and bsdtar runners. Each target builds on one label, so a dispatch build and a tag build of a given target agree - which is the property the pin needs - but a Linux archive built on macOS would not be expected to match.
- The dependency block is copied whole per D-10 and the plan's own Note, so tree-sitter, the grep crates, `ignore` and `globset` are compiled by every cold build and no phase-1 code calls them. The plan states that trimming is a CONTEXT decision, not an executor's.
