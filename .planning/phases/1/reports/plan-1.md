PLAN COMPLETE
Plan: .planning/phases/1/PLAN-1.md
Tasks: 4 of 4 (+1 fix row)
| Task | Commit | Note |
|---|---|---|
| 1: resolveRange resolves each end on its own and names the end that failed | 4a1b31d4 | `gitLine` (one catch, redactUrl) + `resolveRef(end, ref, top?)` factored out; `resolveRange` reads the toplevel once and resolves each end through it. `planning-detail-sites` census unmoved at 15/6. risk-diff 103 pass, siblings 109 pass, tsc clean. |
| 2: a risk_check that had no diff keeps the resolved end and states its cause on the row | c41adcaa | `baseId`/`headId` taken from the ends that resolved, null only where `resolveRange` returned `''`; `detail` on the appended row on both failure arms, same redacted string as the envelope. risk-diff 105 pass, tsc clean. |
| 3: risk-check run accepts --base <ref> --staged as the staged scope's one spelling | 75113949 | `--staged` read by presence, branched before `resolveRange`; base resolved through `resolveRef`, read as `git diff --cached`; both arms build argv and share one catch. Row/envelope carry `staged: true`, `head: null`, `head_id: null`; ref-range rows gain no field. `--head` now `required: false` with the either-or stated as the seam's own check; `arg-contract-flag-entries` 199 to 200. risk-diff 111 pass, arg-contract pair 15 pass, lease-check 32 pass, tsc clean. |
| 4: risk-check status accepts --base <ref> --staged and finds the staged record | b7a47569 | Named scope widened to `--plan --base` plus exactly one of `--head`/`--staged`; staged ask resolves its base through `resolveRef` and refuses `unresolved-range` with the end-naming detail; `rec.staged = e.staged === true`, `sameRange` split per scope, `wanted` echoes `staged` and the null head. `settledBy` untouched. Census 200 to 201. risk-diff 115 pass, arg-contract pair 15 pass, tsc clean. |
| fix: a staged risk_check binds to the index it read | 740867b7 | Blocker from REVIEW-diff-plan-1.md (risk-check.mjs:931): a staged record identified by `base_id` + `staged` alone was satisfied by any LATER index over the same unmoved base. `resolveIndex(top)` in core.mjs takes git's own index id through the existing `gitLine` (no new catch, `planning-detail-sites` census unmoved); `run` records `index_id` BEFORE the body read, `status` resolves it for the ask, `sameRange`'s staged arm requires it, and a row without it never matches. An unnameable index refuses with a detail opening `index`. risk-diff 118 pass (3 new rows), siblings 109 pass, tsc clean. |
Deviations: [deviation] Task 4 Verify: predicted every risk-diff row green, observed 1 of 115 red - `risk-check status: appending the plan-1 record makes the identical call pass` deep-equals the whole reported `records` shape, and reading `staged` into `rec` adds a normalized `staged: false` to it. The plan named neither that row nor the reported shape. Reported rather than stripped, because a reader that cannot tell which scope a row was written over disagrees with a state machine that treats the two as different scopes, and `empty: false` on that same pin already normalizes an absent field the same way. Pin updated with that reason; no behaviour changed by the fix.
Open items: declined a second index-id read AFTER the staged body read, with the two required to agree. The `Verify:` shape this fix answers to is the mutation the review named (a record satisfying a later index), and taking the id BEFORE the read already fails closed on the narrower window - an index that moves between the two git calls records the earlier id against the later body, so the next status ask sees a different id, reports `stale` and re-runs. Build the agreement check if a task ever states that a run's OWN envelope must name the index it scanned.

Suite: `node cadence-core/bin/test.mjs` - 3761 tests, 3760 pass, 0 fail, 1 pre-existing skip, exit 0. `npx tsc -p tsconfig.ci.json` clean. `node cadence-core/bin/self-verify.mjs` ok:true with no problems (run because the CONTRACTS table its check 2 reads was edited). Static analysis: `detect-commands` reports `lint: null` for this project, so there is no lint command to run; `typecheck` is the tsc line above and was run after every task.

Fix suite (blocker 740867b7): `node cadence-core/bin/test.mjs` - 3764 tests, 3763 pass, 0 fail, 1 pre-existing skip, exit 0. `npx tsc -p tsconfig.ci.json` clean. Static analysis: `detect-commands --root /code/cadence` still reports `lint: null`, so there is no lint command to run; `typecheck` is that tsc line. No arg-contract or CONTRACTS surface changed by the fix, so self-verify was left to the suite.

Fix row 2 (re-arm blocker, coordinator edit outside the executor loop): `risk-check.mjs:345` diffs `<base> <tree-id>` instead of `--cached <base>`, so the recorded `index_id` and the scanned body are one object. Suite: 3764 tests, 3763 pass, 0 fail, 1 pre-existing skip. `npx tsc -p tsconfig.ci.json` clean.
