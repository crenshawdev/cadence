PLAN CHECKPOINT: structural
Plan: .planning/phases/1/PLAN.md
Tasks: 4 of 6
| Task | Commit | Note |
|---|---|---|
| 1: Compute the interview's option list in the pure lib | 30253ab | `interviewOptions()` + `OPTION_CAP` exported from `lib/surface-scan.mjs`; 6 new rows in `surface-scan.test.mjs`, 22 pass / 0 fail. `npx tsc -p tsconfig.ci.json` exits 0 (no lint command: `detect-commands` reports `lint:null`, `typecheck:npx tsc -p tsconfig.ci.json`). |
| 2: The seam returns that option list, and the #206 demo tree proves it | 7a360b6 | `options` on the `detect-surfaces` envelope, `--answered` flag, `CONTRACTS` row in `lib/arg-contract.mjs`. 4 new rows (demo tree evidences exactly the six; `--answered secrets` puts the six-plus-secrets union second and it is not all eight; both refusals): 26 pass / 0 fail. `self-verify --root .` -> `"problems":[]`; tsc exits 0. |
| 3: review-triggers.md's ask renders the seam's options | dc231fe | The three ask bullets replaced by sentence-form paragraphs: one recommendation arm, no fill-a-slot sentence, the eight `- \`token\` - ` category bullets byte-unchanged (CST-02 still reads exactly eight). Recommended count kept as the WORD "eight". Budget re-pinned 39268 -> 39691. `prose-agreement.test.mjs` 33 pass / 0 fail; `self-verify` -> `"problems":[]`. |
| 4: /cad-config gains the deliberate entry point | 48b170e | `## Risk surfaces (\`--surfaces\`)` added to `workflows/config.md` between **Worktree base ref** and **Review provider setup**, `## 1. Route` branch added, `Interactive menu (no args)` / `The walk` headings and numbering untouched, "Four sets" sentence unchanged. Catalog row's Value column now routes to the step; `COMMANDS.md` row and `cad-config/SKILL.md` (`argument-hint` + Routing line) name the arm. Four budgets re-pinned. `self-verify --root .` -> `"problems":[]`; tsc exits 0. Verify's `node --test 'cadence-core/bin/*.test.mjs'` arm is NOT met - see the checkpoint below. |
Deviations:
- [deviation] Task 4's Verify asserts `node --test 'cadence-core/bin/*.test.mjs'` reports zero failures. It reports 2475 tests, 2473 pass, 1 fail: `cadence-core/bin/arg-contract.test.mjs:271` `every flag in every row declares a complete grammar` asserts `entries === 156` and the table now declares 157, because task 2's `detect-surfaces --answered` row is a new flag entry and that census pin is designed to redden on exactly this. The one-line pin bump (156 -> 157) is in `cadence-core/bin/arg-contract.test.mjs`, which plan 1's `files:` lease does not declare, so `lease-check` would refuse the commit. Stopped rather than committing an out-of-lease path or leaving the criterion quietly redefined.
- [deviation] Task 4's Verify has a human-verify half needing a live `/cad-config --surfaces` run, which a sequential executor cannot dispatch. The deterministic half was done in full. The five human steps stand unrun and are recorded under Open items.
Open items:
- Task 4 human-verify, unrun: (1) reinstall the plugin from this branch and `/clear`; (2) run `/cad-config --surfaces` in `/code/cadence`; (3) expect `config.mjs get review.triggers.risk_surface.surfaces` to return `["secrets","destructive","untrusted_input"]` and `detect-surfaces --root . --answered secrets,destructive,untrusted_input` to return `inconclusive: true` with `evidenced: []`, the turn printing those three beside the fact that the structure evidences nothing, and the question offering two options - all eight FIRST and labelled `(recommended)`, the current three second and unchanged - neither pre-selected; (4) decline; (5) expect `git status --porcelain .planning/config.json` to print nothing and `config.mjs get review.triggers.risk_surface.surfaces` to still return those three. The seam half of (3) was confirmed directly: `planning.mjs detect-surfaces --root /code/cadence --answered secrets,destructive` returns `inconclusive:true`, `evidenced:[]` and exactly two options - all eight with the inconclusive reason, then the answered set under "the set already answered, left unchanged".
- The `options` envelope field carries `{surfaces, reason}` per entry and no `recommended: true` flag: the recommendation is the FIRST entry by construction and both prose sites state that, so a boolean would be a second statement of the same fact for a check to drift against.
