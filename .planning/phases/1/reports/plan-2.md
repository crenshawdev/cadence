PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1 - Widen the three two-argument fail wrappers, proven at one site each | 566be152 | `config.mjs`, `route.mjs`, `review-provider.mjs` wrappers take `(reason, detail, hint)` with `...(hint ? { hint } : {})`; `traceProvider`'s arguments untouched. Three proof sites hinted: config's `invalid` from `checkPairs`, route's `unknown-role`, review-provider's `bad-provider` in `resolveProvider`. All three CLIs print a non-empty `hint` with `reason` and `detail` unchanged. `test.mjs routing review` 508 pass / 0 fail. `npx tsc -p tsconfig.ci.json` clean; no lint command exists (`detect-commands` reports `lint:null`). |
| 2 - The shared seam-relay catch arm, one hint across the nine CLIs | 14f003da | One hint text verbatim at all nine `e.seam` arms; the `internal` arm untouched. Each of the nine CLIs run with a valueless flag prints `missing-flag-value` plus the identical hint. Census now lists neither `git-branch.mjs`, `issue-check.mjs`, `land-cleanup.mjs`, `weight.mjs`, `worktree-base.mjs` nor `self-verify.mjs`. `test.mjs git other` 1242 pass / 0 fail / 1 skipped; tsc clean. |
| 3 - config.mjs's remaining refusals name their next step | dd4b069e | All 8 remaining sites hinted (`read` x2, the two non-object-root refusals, `checkPaths` collision, `unknown-key`, `bad-schema`, `check`'s `invalid`); each arm exercised live. Census no longer lists `config.mjs`. `test.mjs routing` 435 pass / 0 fail; tsc clean. |
| 4 - review-provider.mjs's remaining refusals name their next step | 6c67c448 | All 17 remaining sites hinted, including the computed `over-response`/`transport` pair (hint splits with the reason) and one shared `HTTP_HINT` constant at both request paths. `bad-provider`, `no-key`, `bad-command`, `bad-args` exercised live. Census no longer lists `review-provider.mjs`. `test.mjs review` 73 pass / 0 fail; tsc clean. |
| 5 - The publish and release seams name their next step | 08d20942 | All 14 sites hinted. `git-publish.mjs`: an arm-keyed `GATE_HINTS` table behind `gateHint(arm, reason)` for the two relayed `decision.reason` gates, plus `config-parse-failed` x2, `push-failed`, `reap-failed` and the `authorized` arm's `auto-close-off` (sent at the REPOSITORY layer). `release-bump.mjs`: `VERSION_HINTS` behind `versionHint(code)` for the relayed `primary.code`, plus the three unreadable-file refusals, `no-version-field`, `partial-bump` (hint prescribes inspection, never a re-run) and `bad-date`. Census lists neither file. `test.mjs git` 395 pass / 0 fail; tsc clean. |
| 6 - The read-side seams name their next step | 8d2e3972 | All 7 sites hinted: `route.mjs`'s `unresolved` and `bad-table`, `why.mjs`'s `bad-query` and both `git-failed` refusals (one shared `GIT_FAILED_HINT`), `skim.mjs`'s `line-count-drift` and its computed `no-such-file`/`skim-failed` pair (hint splits with the reason). `skim.mjs /nonexistent-abc.mjs` prints `no-such-file` plus a hint; `why.mjs ''` prints `bad-query` plus a hint. `self-verify.mjs --root .` now reports 154 `hintless-refusal` entries and every one names `planning.mjs` (plan 3). `test.mjs routing git review other` 1750 pass / 0 fail / 1 skipped; tsc clean. Whole suite: 2957 pass / 2 fail, and both failures are the live-tree assertions PLAN-1's report already recorded as expected until plan 3 lands. |
Deviations: [deviation] Task 3's Verify names `node cadence-core/bin/config.mjs check` against a repo whose `.planning/config.json` is not JSON as a probe; `check` reads no config file at all (it validates only the `key=value` tokens on its argv), so with no tokens it answers `{"ok":true}` and never reaches a refusal. Verified the same two arms through the commands that do reach them - `config.mjs check nosuchkey=1` for the `invalid` envelope and `config.mjs validate --file <unparseable>` / `set --file <unparseable>` for the `read` refusals - all three printing a non-empty hint with the reason unchanged. Nothing about the task's substance changed. [deviation] Task 5's Verify names `node cadence-core/bin/release-bump.mjs --version 0.0.1 --date notadate`; that spelling has no subcommand, so the dispatch answers `usage` and never reaches the date check. Ran `release-bump.mjs bump --version 0.0.1 --date notadate` instead, which prints `reason:"bad-date"` with a non-empty hint exactly as the criterion intends.
Open items: none
