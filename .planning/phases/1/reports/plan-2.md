PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 3 of 3
| Task | Commit | Note |
|---|---|---|
| 1: risk-surface.md documents the staged spelling beside the ref-range one | d0045630 | Second invocation line `--base <ref> --staged` stated beside the ref-range one, with the shape-(b) callers named, `--staged` stated as standing in place of `--head` rather than beside it, and the record described as it is after 740867b7/61c39022: `staged: true`, `head_id` null, `index_id` naming the `git write-tree` tree the body is read from. `weight-budgets.json` re-pinned 12640 to 13360 from `weight.mjs --root .`. self-verify ok:true, 0 problems; tsc clean. |
| 2: verify.md and debug.md ask the seam over the staged set with --staged | a785ad3d | verify.md's "Apply now" arm invokes `risk-check run --phase <N> --base HEAD --staged` with the phase under verification, debug.md's fix step the same with `--phase 0` and task.md's reason for it. Shape (b), the "reviewer runs `git diff --cached` in the cwd it inherits" phrase, verify.md's ONE-round re-arm + triage-gate re-read and debug.md's `blocking`/CAPPED sentence all kept verbatim; `ok:false` (`no-diff`, `surfaces-unanswered`) stated as not a clean answer at both sites. New prose-agreement test walks every line under `workflows/` and `references/` naming both `planning.mjs` and `risk-check run` and asserts `--base \S+` plus exactly one of `--head \S+` / `--staged`, plus the two staged fire sites; falsified by rewriting verify.md's line to `--base HEAD..STAGED` (1 fail), then reverted. 72 pass / 0 fail. Budgets re-pinned verify.md 17626 to 18374, debug.md 6920 to 7660. self-verify ok:true (bulk-output and text-transport censuses unmoved); tsc clean. |
| 3: execute.md and task.md skip the self-comparing range and record the skip | 163339e7 | Same skip arm in one spelling at all three sites (execute.md `execute_sequential`, task.md `risk_check`, references/execute-parallel.md step 5): when HEAD still names the recorded end, no `risk-check run` and - in execute.md and the parallel path - no `status` call either, `trace append --phase <N> --family outcome --event risk_check_skipped --plan <k> --sha <recorded end>` in its place, no `--detail` so the `text-transport` census does not move. Every pinned sentence kept (`inconclusive`, `plan-<k>-risk.diff`, "never stage it", "delete it once the trigger returns", `risk-check run --phase 0`, `risk-task-{slug}.diff`, both done-withholding sentences); execute-parallel.md still restates neither `inconclusive` nor "never stage it", so PAR-01 stays green. New prose-agreement test reads all three step bodies for the invocation, its `outcome` family and the condition sentence around it, keyed on one shared `risk_check_skipped` constant; falsified by renaming task.md's event (1 fail), then reverted. prose-agreement + trace: 283 pass / 0 fail. Budgets re-pinned execute.md 35735 to 36854, task.md 16483 to 17348, execute-parallel.md 5992 to 6621. self-verify ok:true; tsc clean. |
| fix (diff review, plan 2): stage the fix before the staged risk check | 0fceb073 | Blocking `diff` review of d0045630..163339e7 raised two `high` findings (REVIEW-diff-plan-2.md), both on task 2's work: verify.md's Apply-now arm and debug.md's Resolve fix step invoked `risk-check run --base HEAD --staged` while only DESCRIBING the fix as staged, and nothing in either sequence performs the staging. The findings' stated failure is understated - they assume the seam refuses `no-diff` on an unchanged index. It does not: `--staged` refuses `no-diff` only when git could not READ the range (planning/risk-check.mjs, `diffError`), so an unstaged fix comes back `ok: true, checked: true, empty: true, matches: []`. Reproduced in a scratch repo this session - the same edit answers `matches: []` unstaged and fires the `secrets` match after `git add`. So the consequence is a coordinator committing an unreviewed risky fix, not a loop. Both sites now carry `git add` as a STEP ahead of the invocation line, and both state that `empty: true` on the staged arm is NOT a pass. RSK-10's prose-agreement test gains the ordering pin (a `git add` line before the `--staged` call at each of the two sites, matched over whitespace-collapsed prose because both sentences hard-wrap) and the `empty: true` sentence; falsified by deleting verify.md's staging clause (1 fail, message names the site), then restored. Budgets re-pinned from `weight.mjs --root .`: verify.md 18374 to 18872, debug.md 7660 to 8106. prose-agreement 73 pass / 0 fail; self-verify ok:true, 0 problems; tsc clean. |
Deviations: [deviation] Task 2 Verify predicted `grep -rn "STAGED" cadence-core/workflows cadence-core/references` prints nothing; observed one pre-existing hit, `cadence-core/references/worktree-executor.md:37` ("leaves the flagged changes STAGED"), which is emphasis prose about a checkpoint and not a rev spelling - the same unrelated-prose occurrence ROADMAP.md's overview already accounts for, and it is outside this plan's lease. The `## Must be true when done` criterion the grep stands in for holds: no surface passes a rev value `git rev-parse --verify` rejects, proven by the new prose-agreement test over every invocation line. Lowercased the one `STAGED` task 1 had written into risk-surface.md so the flag's own reference adds nothing to that grep.
[deviation] Task 3 Verify predicted prose-agreement green after the three prose edits; observed 1 of 72 red - `ENFORCEMENT, execute.md: the blocking FAIL arm dispatches a continuation cad-executor` (prose-agreement.test.mjs:1664). That test bounds the risk_surface arm by the FIRST `risk-check run` and the FIRST `risk-check status` in the step, and the Action's literal wording ("do NOT run that plan's `risk-check status` either") puts a second `risk-check status` token AHEAD of the FAIL arm, truncating the slice before the dispatch sentence it asserts on. The instruction is unchanged and the token is not: execute.md says "do NOT run the `status` call that gates it done below either". Nothing about the skip arm's behaviour moved.
Open items: risk-surface.md's staged block states the `--staged` spelling and the record shape (`staged: true`, `head_id` null, `index_id`) but not that `empty: true` on that arm is the answer an unstaged change produces - the caveat now lives only at the two fire sites the review named. Declined to carry it into the reference here: the fix dispatch scopes to the two `high` findings, and the reference is read by callers who already have the sentence at their own site.
prose-agreement.test.mjs:1664 bounds its assertion slice on the first occurrence of two command names in `execute_sequential`, so any future sentence naming `risk-check status` ahead of the FAIL arm silently shortens what that test covers rather than reddening on the thing it guards. Declined to re-anchor it here: it is outside what this plan's tasks caused or need, and re-anchoring a gate-enforcement test is its own decision.

Suite: `node cadence-core/bin/test.mjs` (workflow.test_command is null and the repo has no root package.json, so the project's own runner) - 3766 tests, 3765 pass, 0 fail, 1 pre-existing skip, exit 0. `npx tsc -p tsconfig.ci.json` exit 0. `node cadence-core/bin/self-verify.mjs --root .` ok:true with no problems, run after every task since each edited a budgeted surface. Static analysis: `detect-commands --root /code/cadence` reports `lint: null`, so there is no lint command for this project; `typecheck` is the tsc line above and was run before every commit.

Suite, fix dispatch (2026-09-02, after 0fceb073): same runner - 3766 tests, 3765 pass, 0 fail, 1 pre-existing skip, exit 0, unchanged from the task-3 run because the fix extended RSK-10 rather than adding a test. `npx tsc -p tsconfig.ci.json` exit 0; `self-verify --root .` ok:true with 0 problems after the two budget re-pins. `detect-commands --root /code/cadence` still reports `lint: null`, so there is no lint command to run.
