PLAN CHECKPOINT: structural
Plan: .planning/phases/1/PLAN-2.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1. load_priors reads prior phases' deviations | 8df2a1f | SUMMARY `## Deviations` read added to `load_priors`, bounded most-recent-first at the same 3-file ceiling; `context.md` budget row re-pinned 16821 -> 17445. self-verify ok:true, `tsc -p tsconfig.ci.json` clean. |
| 2. A spend gate before the analyzer resolve | c604fcb | New `<step name="spend_gate">` between `load_priors` and `analyze`; `memory.backend` read, gated `recall` and the `references/recall.md` Read moved into it with its consult-site count; register row re-anchored `analyze` -> `spend_gate` (`read_paragraphs` still 1). `analyze` keeps 1 `--bracket-read`, 1 `--event return`, 1 `--event checkpoint`. Budget re-pinned 17445 -> 19589. trace.test 59/59, prose-agreement 7/7, self-verify ok:true, tsc clean. Anchor move proven positively: the old `analyze` anchor now reports `deferred-read-unread`, the shipped row reports nothing. THIS is the commit the checkpoint below is about. |
| 3. Every surface states one size question AND one spend question | db3f633 | `size_check` opening, guardrail 3, success criterion 3 and `skills/cad-context/SKILL.md`'s objective each restated; every surviving "one size question" line names the spend question on the same line. SPIDR/splitting ban extended to both; Plan shape still the size question's outcome alone; `size_check` unmoved. Budgets re-pinned: `context.md` 19589 -> 19858, `cad-context/SKILL.md` 827 -> 974. self-verify ok:true, tsc clean. |
| 4. Pin the gate's position and the four surfaces' agreement with a test | f307393 | One test in `prose-agreement.test.mjs`: the gate step opens before `<step name="analyze">` AND before `--bracket-read`, with the step name read off the `DEFERRED_READS` recall anchor rather than hardcoded; the four restated regions resolved through `regionLabels` and each held to naming both questions. 8/8 pass. Falsified live: moving the `spend_gate` block below `analyze` fails with the test's own message, and `context.md` was restored byte-identical (`diff` clean). |
| 5. Correct the DOCS-CLAIMS rows this phase's line shifts moved | 2b0f488 | Ten of the eleven `context.md` rows re-pinned to the live file (CONTEXT-01/03/04/06/07/08/14/15/16/17; CONTEXT-02 at line 12 did not move). CONTEXT-03 is the only TEXT change - it asserted `config.mjs get memory.backend workflow.subagent_timeout` reads both keys in one call, and the timeout key is deleted from the seam. Every re-pinned line re-read with `sed -n '<line>p'` and shown to carry its claimed construct. The ledger's own "the line column is run 1's provenance" paragraph amended in the same commit rather than silently contradicted. |

Deviations:
[deviation] The plan's `files:` lease is incomplete for its own task-2 Action. That Action mandates re-anchoring the `cad-context` / `references/recall.md` row in `cadence-core/bin/lib/deferred-reads.mjs` from `analyze` to the gate's step name. Two assertions in `cadence-core/bin/deferred-reads.test.mjs` mirror that register and are NOT satisfiable from inside the lease: `REGISTER_SOURCE` at `:66-115` is a byte-for-byte transcription of the whole register block (the file states this is deliberate - "a `git diff` cannot stand in for it"), and `assertPromotedRow('cad-context', 'references/recall.md', 'analyze')` at `:590` names the old anchor as a literal. `cadence-core/bin/deferred-reads.test.mjs` is not in the plan's `files:` list, so `lease-check` refuses the commit that would fix them. The plan's three named verify commands (trace.test.mjs, prose-agreement.test.mjs, self-verify) are all green and none of them covers this file; the break only appears under `workflow.test_command` (`node --test cadence-core/bin/*.test.mjs`), where the suite is 1413 pass / 2 fail at HEAD. Not worked around: no in-lease edit can satisfy a byte-for-byte mirror of a register the Action requires changing.

Open items:
1. AC5 is human-verify and is now walkable. Steps: (1) `cd` into a project with Cadence installed, (2) run `/cad-context <N>` on a small phase, (3) at the "Analyzer" question answer "Skip it", (4) confirm `.planning/phases/<N>/CONTEXT.md` was still written, (5) run `node cadence-core/bin/planning.mjs trace render --phase <N>` and confirm no `lifecycle/dispatch` and no `routing/resolve` event names `cad-assumptions-analyzer` for that phase.
2. AC6 is human-verify with no machine arm. Steps: (1) `cd /data/code/verbatim`, (2) run `/cad-context 2`, (3) at the "Analyzer" question read the annotation and confirm it presents six named surfaces as its evidence, (4) confirm the recommended (first) option is "Dispatch it", not "Skip it". CONTEXT D-13 records that the skip arm ships with no positive fixture evidence - across 21 archived Cadence phases plus verbatim's two, no phase would have taken it.
3. CONTEXT-06's claim text names a written `trace append ... --event dispatch` invocation, and `context.md` has folded that half onto the resolve's `--bracket-read` since before this phase opened. Only the line was re-pinned: the claim as stated ("every flag exists") is still true of the seam, so rewriting it would be making a stale claim easier to verify, which task 5 forbids. Left for the next `/cad-docs-verify` sweep.
4. No new DOCS-CLAIMS row was added for the `spend_gate` step. Extracting claims from a surface is `/cad-docs-verify`'s job, and task 5's scope was re-pinning rows the line shifts moved.
5. `workflow.lint_command` is unset and `detect-commands --root /data/code/cadence` returns `lint:null, typecheck:null`, so Cadence finds no static-analysis command for its own repo even though `tsconfig.ci.json` ships in the tree and CI runs it. Ran `npx tsc -p tsconfig.ci.json` directly instead (exit 0 on every task). Pre-existing; PLAN-1 reported the same.
