PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1: The SUMMARY commits-table reader | e287d314 | `lib/why-record.mjs` (`parseCommitRows`, `shaMatches`, `COMMITS_HEADING`) + `why-record.test.mjs`, 9/9 passing. Verify matched prediction exactly: 10 rows off `.planning/_archive-v3.4.0/1/SUMMARY.md`, four cells byte-exact, `fix 1`/`fix 2`/`fix 3` as strings, `0053735` prefix-matching `00537356...` and not `0053736...`, a synthetic 8-char row matching its own sha only, the separator row yielding nothing, and a fenced `## ` mid-section not ending the table. Static analysis: `detect-commands` reports `lint:null`; `npx tsc -p tsconfig.ci.json` exit 0. `lease-check --phase 1 --plan 2` `ok:true` (staged 2, declared 7). |
| 2: The storage-tier locator | 2d9ffbc5 | `lib/why-corpus.mjs` (`buildCommitIndex`, `resolveCommit`, `readArtifact`, `RESOLUTIONS`) + `why-corpus.test.mjs`, 10/10 passing. Verify matched prediction exactly: over this repository's own `.planning` the index is 28 dirs / 256 rows / 0 warnings and resolves `00537356...` to `_archive-v3.4.0/1` plan 1 task 2, NOT to the live `phases/1` (which is in the walk, so the negative discriminates); a two-archive-group temp root indexes each summary under its own label; a `SUMMARY.md` that is not a regular file yields 0 rows and exactly one `ENOTREGULAR` warning; two abbreviations prefixing one sha answer `ambiguous` naming both. Static analysis: `tsc` exit 0; `helper-census.test.mjs` 10/10. `lease-check` `ok:true` (staged 2, declared 7). |
| 3: Wire the phase and plan-task edge into the chain | 2d3f0eaa | One index build per invocation in `why.mjs`, one attach point `entry.join`, `lib/why-render.mjs` gains `decorate`/`fieldPhase`/`fieldTask` and keeps a literal string on the entry winning over the join. `why.test.mjs` 14/14, `why-render.test.mjs` 5/5 (untouched, and it pins the `not yet joined` literal). Verify matched prediction exactly: the entry for `00537356...` renders `phase: v3.4.0 phase 1 (_archive-v3.4.0/1)` and `plan task: plan 1, task 2 - ` with `The pure issue-decision core + 15 tests` verbatim; `(phases/1)` appears nowhere; a temp-repo commit no summary names keeps all five fields stated absent; two real-repo runs are byte-identical. Static analysis: `tsc` initially FAILED with TS2345 on `@returns {object}` for the join builders, fixed to `{any}` (the `strict:false` disposition `arg-contract.mjs` states), re-run exit 0. `lease-check` `ok:true` (staged 3, declared 7). |
| 4: The decision edge | 781a1783 | `decisionTokens`, `planTaskBodies`, `contextDecisions`, `decisionsFor` in `lib/why-record.mjs`; `readPhaseRecords` in `lib/why-corpus.mjs` (the plan file by its two documented spellings, never a second plan-file grammar); `joinChain` memoized per directory+plan in `why.mjs`; `fieldDecision` + `quoted` in `lib/why-render.mjs`. `why-record.test.mjs` 18/18, `why.test.mjs` 16/16, `why-corpus`/`why-render`/`helper-census` 25/25. Verify met after the deviation below: task 3 of `_archive-v2.2.0/3/PLAN.md` yields `scope: task` with `D-08 (siblings): the sibling-manifest loop inherits the guard through the` verbatim; `fix 1` falls back to the plan's `## Context` cites including D-01/D-05; a plan citing nothing yields all 16 decisions phase-scoped; the seam run over `lib/capture-file.mjs` renders `decision: cited by this task (D-02)` with the CONTEXT line under it and, on another entry, the `PHASE-SCOPED` label. Static analysis: `tsc` exit 0. `lease-check` `ok:true` (staged 6, declared 7). |
| 5: The deviation edge, and the gap it has to name | 4c1ec3a4 | `parseDeviations`, `DEVIATIONS_HEADING` and the exported `MARKER_GAP` sentence in `lib/why-record.mjs`; `readPhaseRecords` also pulls SUMMARY.md; `fieldDeviation` in `lib/why-render.mjs` renders the gap sentence ALWAYS and the bullets under it. `why-record.test.mjs` 22/22, `why.test.mjs` 18/18, siblings 25/25, `tsc` exit 0. Verify met after the deviation below: the reader returns `_archive-v2.2.0/3`'s deviation bullets with `[deviation]` stripped and byte-exact, and none of its ten `## Open items` bullets (`parseSummarySnippets` returns all 16, so the separation is performed rather than lucky); a fenced `## Open items` inside the section does not truncate the list; the seam run over `lib/capture-file.mjs` renders `deviation: PHASE-SCOPED - ` with the SUMMARY's own bullet quoted; and over BOTH `capture-file.mjs` and `issue-decision.mjs` every joined entry names `corrected by plan-<k> deviation:` as absent, with no `deviation: none` and no empty field anywhere. `lease-check` `ok:true` (staged 6, declared 7). |
| 6: The surviving review finding edge | 3e019635 | `parseAdjudication` + `SURVIVED_RULING` (positional off `RULINGS`, literal pinned in the test) in `lib/why-record.mjs`; `readAdjudications` + `rangeMembers` in `lib/why-corpus.mjs`, with the filename affixes DERIVED by probing `recordName` rather than re-spelled and a hexadecimal gate before either id reaches git argv; per-range and per-directory memos in `why.mjs`; `fieldReview` + `findingLines` in `lib/why-render.mjs`. `why-record.test.mjs` 28/28, `why-corpus.test.mjs` 16/16, `why.test.mjs` 20/20, `why-render`/`helper-census`/`adjudication-record` 50/50, `tsc` exit 0. Verify met after the deviation below: the git-recovered record's survived entry returns with `claim`/`failure_scenario` compared byte-for-byte against `JSON.parse` of the same bytes; deleting `base_id` yields `survivor-without-a-range` and a null range with the finding KEPT; unparseable/array/null records each yield one issue and no findings; `rangeMembers` answers `base..head` (A in, B out, base excluded), returns null for an unknown id and refuses `--upload-pack=...`; and the built seam fixture renders the claim, scenario and counter-evidence verbatim on commit A, `no surviving finding covers this commit` on B, the refuted entry nowhere, and `join UNRESOLVABLE:` when `head_id` names an object the clone lacks. `lease-check` `ok:true` (staged 7, declared 7). |
| 7: The task-attributed declared-files edge | 6a0d5642 | `taskDeclaredFiles` + `declaringTasks` in `lib/why-record.mjs`, beside `parsePlanFiles` and with its exact task-line normalization (backticks, one trailing parenthetical, `{placeholder}` skipped); containment through `covers` from `lib/lease-grammar.mjs` and no fresh comparison; `fieldDeclared` and a SIXTH rendered line in `lib/why-render.mjs`; the queried path threaded into `joinChain`. `why-record.test.mjs` 34/34, `why.test.mjs` 24/24. Verify matched prediction exactly: `_archive-v2.1.0/1/PLAN.md` task 6 yields `cadence-core/bin/planning.test.mjs` from the first line AND `cadence-core/bin/self-verify.mjs` from the indented continuation, with the same test proving the single-line arm misses it; tasks 1, 3 and 7 keep their own sets; a `src/` declaration claims `src/auth.js` through `covers` while a sibling declaring `src/auth.js` does not claim `src/other.js`; the seam names the declaring task; and both byte-identity assertions hold with all six edges joined. FINAL GATE: `self-verify.mjs --root .` `{"ok":true,...,"problems":[]}`, `test.mjs` 2783 tests / 2782 pass / 0 fail / 1 skipped, `tsc` exit 0. `lease-check` `ok:true` (staged 6, declared 7). |

Deviations:
- [deviation] NOT a plan defect, recorded because it broke a blocking gate and the cause is invisible in a diff: two literal U+0000 bytes reached `lib/why-corpus.mjs` and one reached `why.mjs` as memo-key separators in template literals, which `self-verify`'s `nul-byte-in-source` check refused (`ok:false`, two problems) and `self-verify.test.mjs` reddened on. Replaced with `\x1f` escapes, matching `LOG_FORMAT`'s own separator convention; re-run is `ok:true` with `problems: []`. Found by running the gate rather than by reading the file, which is the only way this class is findable.
- [deviation] Task 6's Verify described the fixture `git show a34b0c8a^:.planning/phases/2/ADJUDICATION-risk_surface-plan-2.json` as having a survived entry with `counter_evidence` and "non-survived entries". Read on 2026-08-23 it has exactly ONE entry, `survived`, and that entry carries NO `counter_evidence` field. So the byte-exact assertion covers `claim` and `failure_scenario` (compared against `JSON.parse` of the same bytes, which is what byte-exact means here) and asserts `counter_evidence` reads null rather than an empty string; and the non-survived arm is proved on a REAL second fixture instead - the on-disk `.planning/phases/1/ADJUDICATION-risk_surface-plan-1.json`, whose two entries are both `downgraded` - plus a `refuted` copy of the recovered record, so all three rulings are exercised.
- [deviation] Task 5's Verify asserted `.planning/_archive-v2.2.0/3/SUMMARY.md` carries THREE `## Deviations` bullets. It carries SIX, counted off the file on 2026-08-23 (`Task 2's verify grep...`, `The sibling loop shares readManifest...`, `Inline correctness fix...`, `section_empty is computed in the lib...`, `Task 1 test-count prediction was 19...`, `The §11 chain is seven per-pair test() calls...`). The substance of the criterion - deviations only, no open items, `[deviation]` stripped, the rest byte-exact - is unchanged and met; only the count was wrong, so the test asserts six.
- [deviation] Task 4's Action locked the decision source as `parseContextDecisions` and forbade restating its grammar; its Verify then required a `D-08` cite to carry "that CONTEXT line's own text". Those two cannot both hold. `parseContextDecisions` is the DURABLE-ONLY recall surface: `## Decisions` is a LEGACY fallback read only when `## Durable decisions` is absent entirely, and measured over this repository on 2026-08-23 that heading is present in 27 of 27 CONTEXT files, so the fallback never fires and 243 of the corpus's 435 decision bullets (56%) are unreachable through it. `.planning/_archive-v2.2.0/3/CONTEXT.md` is the named fixture and the exact case: D-01..D-04 are durable, D-05..D-16 are phase-local, and the plan's own task 3 cites D-08 - so through `parseContextDecisions` alone the Verify's D-08 came back as `D-08 - cited here, but the phase's CONTEXT.md carries no such decision`, a false gap. What I did: added `contextDecisions`, which STARTS at `parseContextDecisions` (that grammar is still not restated - the durable half is still that call) and appends the `## Decisions` bullets through the same fence-aware `sectionSpan` every other reader here uses, de-duplicated by id with the durable spelling winning. Not routed as a checkpoint because what "done" means did not move: the acceptance criterion - D-08's own CONTEXT line, quoted verbatim - is unchanged and is what the union delivers. CONTEXT D-10's sentence "Reads `## Durable decisions` first with the documented `## Decisions` fallback" is the belief that turned out wrong about this corpus; the union preserves its ordering intent.
Open items:
- THE ENTRY CAP'S STATED REASON IS NOW MEASURED FALSE, and the number is a decision I did not take. `lib/why-render.mjs`'s header claims ten entries "stays under the byte threshold once each entry carries its joins"; with plan 2's six edges filled, `/cad-why cadence-core/bin/lib/capture-file.mjs` renders 10,137 B over EIGHT entries (seven joined), measured 2026-08-23 - already past `references/conventions.md`'s 10,000-byte line, and a full ten joined entries is around 13 KB. D-13 is still satisfied by its own terms (a default entry cap exists, and check 20 cannot see this site), so I corrected the comment IN PLACE with the measurement rather than lowering `DEFAULT_TOP` - that constant is pinned by `why-render.test.mjs`, which is outside this plan's lease, and a smaller default hides history a reader asked for. Whoever re-decides it now has the number.
- Task 6's `Files:` line named six files and its `Verify:` required `node --test cadence-core/bin/why.test.mjs` to pass over a new seam-level fixture, so `why.test.mjs` is in the commit. It is in the plan's frontmatter lease and `lease-check` is `ok:true`; noting it because the two fields of the task disagreed.
- Not caused by this plan, and it bounds how much of the corpus any join can reach: plan 1's bare-path arm inherits git's default history simplification, so a milestone's merge commit can stand in for the phase commits merged under it. Measured 2026-08-23 on `cadence-core/bin/lib/release-decision.mjs`: 7 commits with the shipped invocation (with or without `--follow`, identically), 10 with `--full-history` - the three missing are `_archive-v2.2.0/3` phase commits collapsed into the merge `0bf62847`. Widened the probe: NONE of that phase's five recorded commits (`2c3c2cb`, `1dd5ad5`, `86929d4`, `67f0379`, `c253a3b`) is reachable from ANY path they touched, so a whole phase's record is invisible to `/cad-why` for a reason that has nothing to do with the join. That is why task 5's seam-level proof runs against `_archive-v3.3.0/1` while its reader-level proof runs against the `_archive-v2.2.0/3` fixture the plan names. D-15/D-17 fixed the invocations in plan 1 and this is not in plan 2's `Verify:`; it is a real reachability limit worth a decision rather than a silent one.
