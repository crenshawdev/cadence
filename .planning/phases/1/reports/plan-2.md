PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 2 of 2
| Task | Commit | Note |
|---|---|---|
| 1: Expose provider review spend on the render `/cad-report` reads | fa5c5812 | `provider_spend` folded in `planning/trace.mjs` beside the `outcomes` projection: `{calls, tokens?, unrecorded?}`, the whole key absent where the scope holds no provider call, `tokens` gated on a figure having landed so an all-unrecorded scope shows no total. A non-numeric `usage.input`/`output` from a hand-edited or foreign-producer line counts unrecorded rather than concatenating onto the sum. 5 new cases in `trace.test.mjs`, 188 pass. Census tests unmoved and green: self-verify 177, planning-lease-check 32, phase-spelling 39. `npx tsc -p tsconfig.ci.json` exit 0. |
| 2: Give `/cad-report` a reviewer row carrying a real cost | 133b33ab | `report.md`: `provider_spend` named in `read_record`'s inventory of what the scratch file carries; a `Cross-model reviews` line added to the `compose` shape below the host-denominated token and gap-terms pair; one rule bullet stating the two-denomination rule, the never-from-a-Dispatches-row provenance (D-15), `unrecorded` never zero, no-line-at-all on an empty scope, and the same guarded `node -e` field read with no whole-file read-back and no `events` array. `weight-budgets.json` row re-pinned 20801 -> 22493 in the same commit. `self-verify` ok:true, 0 problems. `npx tsc -p tsconfig.ci.json` exit 0. Automated Verify met. **The `human-verify` arm is DEFERRED to `/cad-verify 1` by user decision at the checkpoint, not discharged here.** What WAS verified before that decision: the `provider_spend` projection the new `/cad-report` line reads was confirmed three ways against this repository's live record and against the suite. Case (a), usage recorded - `trace render --phase 1` returns `{calls:140, tokens:28521, unrecorded:137}`. Case (b), provider events carrying no usage key - `trace render --phase 2` returns `{calls:61, unrecorded:61}` and `--phase 3` returns `{calls:56, unrecorded:56}`, with no `tokens` key at all on either, so the line reads `unrecorded` and can never read `0`. Case (c), a scope holding no provider call - pinned by `cadence-core/bin/trace.test.mjs:1300`, which asserts `'provider_spend' in r === false`. What that does NOT cover, and is what carries forward as the deferred UAT item: whether a model actually EXECUTING `/cad-report` prints those three states as `report.md`'s prose specifies. |
| Fix pass: the `high` at `trace.mjs:566` (review `diff`, REVIEW-diff-plan-2.md) | d4c2db40 | `providerSpend` selected on `family` alone, so the `Cross-model reviews` line priced two non-review commands and every unsent call. Now `command === 'review'` AND an `outcome` the seam wrote after the request left the machine; `UNSENT_OUTCOMES` is D-04's pre-request refusal set (`bad-provider`, `bad-args`, `no-key`, `bad-payload`, `over-cap`), read back out of the FILE rather than imported, so a reason added at the seam and not here lands as one more unrecorded call - the direction that may overstate the unknown but can never hide spend. Degraded post-send outcomes stay counted (D-04). NOT filtered on `event`: the seam writes one event per call and all 317 provider events on this record read `request`, so that arm of the finding is refuted and left uncoded. The three kept rules are intact and pinned by test: `tokens` still gated on a figure landing (an all-unrecorded scope shows no total, never `0`), the whole key still absent for a scope holding no provider REVIEW call, and `roles` still untouched. Live record after the fix: phase 1 `{calls:127, tokens:35373, unrecorded:123}` against `counts.provider` 141; phases 2 and 3 `{calls:58, unrecorded:58}` and `{calls:49, unrecorded:49}`, no `tokens` key on either. 4 new cases in `trace.test.mjs`, 192 pass. `report.md`: the `read_record` inventory, the `compose` line and a new rule bullet all state what `calls` counts and that the line is not the phase's whole provider bill; `weight-budgets.json` re-pinned 22493 -> 23249. `npx tsc -p tsconfig.ci.json` exit 0, `self-verify` ok:true with 0 problems. |

Full suite (`node cadence-core/bin/test.mjs`, the suite the plan names - `workflow.test_command` is null and this repo has no root `package.json`): 3694 tests, 3693 pass, 0 fail, 1 skipped. GREEN. Re-run at the same HEAD (133b33ab) on the continuation dispatch and identical to the run recorded at the checkpoint. `npx tsc -p tsconfig.ci.json` exit 0 and `node cadence-core/bin/self-verify.mjs` ok:true with 0 problems, both run after the last commit. FIX PASS re-run at HEAD d4c2db40, after the `high` at `trace.mjs:566` was closed: 3698 tests, 3697 pass, 0 fail, 1 skipped. GREEN - the four added cases are the whole delta, and `npx tsc -p tsconfig.ci.json` exit 0 with `self-verify` ok:true, 0 problems, both re-run after that commit.

Deviations: none

Open items:
- **DEFERRED UAT, inherited by `/cad-verify 1`:** task 2's `human-verify` arm. `cadence-core/workflows/report.md` is model-executed prose with no executor, so no automated command in this plan executes the new `Cross-model reviews` line. Run `/cad-report <N>` three times, once per case, and confirm the PRINTED output: (a) a phase holding provider reviews with recorded usage and zero `cad-reviewer` lifecycle dispatches prints a reviewer line carrying a cost figure, not absent and not empty - phase 1 of this cycle is such a scope, `{calls:127, tokens:35373, unrecorded:123}` as of the fix-pass commit; (b) a phase whose provider events carry no usage key prints `unrecorded`, never `0` - phases 2 and 3 are such scopes, `{calls:58, unrecorded:58}` and `{calls:49, unrecorded:49}`; (c) a scope holding no provider REVIEW call prints NO reviewer line at all rather than an empty one, the same silence the `coordinator` block gets. The data behind all three states is confirmed present and correctly shaped (see task 2's note); what is unverified is only the prose's rendering of it. The three figures above are re-stated as of d4c2db40 and MOVE as the record grows - re-read them with `trace render --phase <N>` at verify time rather than expecting these exact numbers.
- Static analysis: `workflow.lint_command` is unset and `planning.mjs detect-commands --root /code/cadence` reports `lint: null`, so `npx tsc -p tsconfig.ci.json` is the only static-analysis command Cadence can find for this project. It ran clean after every task.
- Declined adding a `provider_spend` property to `renderTrace` and the `TraceRender` typedef in `cadence-core/bin/lib/trace.mjs`. The plan named either home as defensible; the fold exists to replace bytes withheld from the CLI RESPONSE, which is the same reason D-08 puts the response bound in `planning/trace.mjs` rather than in `renderTrace`, and putting it on the shared render shape would have cost both files plus a new property four other in-process consumers parse. The denomination rule is documented at the fold, citing the typedef's "They stop HERE either way". Move it into `renderTrace` when a second, non-CLI reader needs the figure - `lib/trace-suggest.mjs` is the likely one once PLAN-3 takes `'cross-model provider calls'` out of `SPEND_EXCLUDES`.
- Consult spend is reported NOWHERE by name, and that is a deliberate decline in the fix pass. `/cad-debug`'s dead-end consult is a real provider call with a real cost; it is now excluded from `provider_spend` because that figure feeds a line labelled `Cross-model reviews` and a consult is not a review. Reporting it under a second name would need a second render key and a second `/cad-report` line - a new user-facing surface, past the one `high` this pass was scoped to, and PLAN-3 owns the prose surfaces. Nothing is hidden meanwhile: `counts.provider` still counts every provider event in scope, and `report.md`'s new rule bullet says the line is not the phase's whole provider bill and names `counts.provider` as where the difference is. Worth a capture if a consult bill ever needs its own line.
- Did NOT touch `report.md`'s `SPEND_EXCLUDES` bullet ("cross-model provider calls. None by design - no lifecycle bracket and no token field on that arm at all"), which becomes stale now that the spend is real. D-13 assigns that surface and the `trace-suggest.mjs` `SPEND_EXCLUDES` array to PLAN-3; both files are in PLAN-3's lease and the two must move together or `self-verify`'s one-list rule is broken mid-flight.
