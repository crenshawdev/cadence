PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 3 of 3
| Task | Commit | Note |
|---|---|---|
| 1. `scanDeclared` stops counting import and constant-declaration lines | 3030a7d4 | Third exemption on the plan-time face and the first that is per LINE. `isIncidentalLine` withholds an import statement (`import`, `from X import`, `#include`, `use`, a `require(...)` binding) or a constant declaration bound to a literal; a mixed declare-and-call line, a dynamic `import(` call and a `const` initialized by a call are NOT withheld. `scanDiff`, `parseDiff`, `signalIn`, `CONTENT_SIGNALS` and the two existing exemptions are untouched - the whole-file diff shows changes confined to `scanDeclared` and its JSDoc. 5 new test rows; `node --test cadence-core/bin/risk-diff.test.mjs` 90 pass 0 fail, the self-match census still green. Replay against the pre-change baseline: 0 levels moved, 5 rows changed the file their raise cites, `_archive-v2.2.0/2` moved its winning surface `secrets` -> `destructive`, `regressions` empty - exactly the plan's measured figures. |
| 2. the replay names the file whose match no longer counts | 9a499ced | `scanDeclared` returns a third field `withheld`, per declared path, computed through the SAME `signalIn` asked twice of one file (whole body, then kept lines only); no second signal table, and `scanDiff`'s return shape is unchanged. `levelFor` states one `reason` per entry in the `risk floor: ` vocabulary, pushed before any deciding arm so it is true on every arm, and a `reason` rather than a `warning` because the floor READ the line and judged it. Measured: 5 reasons across the 28 archived directories. `_archive-v2.2.0/2` names `cadence-core/bin/git-publish.test.mjs` + `secrets` while its cited evidence is now `destructive` on `publish-decision.test.mjs`. 3 new rows in risk-diff.test.mjs (93 pass) and 2 in route.test.mjs (158 pass), including both halves: a file whose only evidence was withheld IS named, one whose category survives on a kept line is NOT, and path evidence is never reported as withheld. |
| 3. every replay row carries the bytes it read | b28039e9 | `declaredBodies` carries `bytes: st.size` on the read arm only - never on the not-yet-created arm or any `unread` arm, where a zero would be indistinguishable from an empty file somebody opened. `levelFor` sums them once per entry and returns `bytes` on every arm (`none`, `hold`, `cite`); `replay` prints it as `bytes_read` beside `plans_found`/`plans_clean`. Nothing was added to `resolve`'s envelope. Measured: all 29 rows carry an integer, `_archive-v2.2.0/5` reports 14853 (README.md is 14,853 B and its other two declared paths do not exist), total 18,168,023 B. 2 new route.test.mjs rows (160 pass): an absent declared path reports 0 with the field present, and an oversized (`> MAX_BODY_BYTES`) declared body contributes no bytes while still withholding the discount. `self-verify.test.mjs` needed no edit - the `mergeLayers` callsite census and its pinned numbers did not move (175 pass). |
Deviations: [deviation] tasks 2 and 3 assert the replay shows "all 28 rows"; it emits 29, because `.planning/phases/1` is itself a phase directory the replay measures and this phase is live. The substance holds unchanged: the 28 archived rows compute exactly the levels the plan enumerates (25 `shipped`/`raised:true`, `_archive-v2.2.0/5` and `_archive-v2.6.0/1` `solo`, `_archive-v2.6.0/5` `shipped`/`raised:false`), `regressions` is empty, and 0 of all 29 rows moved a level. Nothing was changed to accommodate it; the extra row is reported here rather than silently absorbed.
Open items: the constant arm accepts a SCREAMING_SNAKE assignment with no declaration keyword, so a raw `NAME=value` line is withheld wherever it appears in a scanned body, not only in a language's `const`. A dotenv file still evidences `secrets` by `FILE_SIGNALS`/`EXT_SIGNALS` on its path, so no floor on this corpus moves, but a project holding credentials in a source file under some other name loses that body line as evidence. Narrowing the keywordless arm to a declaration context would need a language cue the line-kind grammar does not carry; a task that states one can add it.
Verification of the plan's `## Must be true when done`: all six hold - `node cadence-core/bin/test.mjs` is green (3373 tests, 0 fail, 1 pre-existing skip), `node cadence-core/bin/self-verify.mjs` reports `"problems":[]`, and `npx tsc -p tsconfig.ci.json` is clean at every task. `detect-commands` reports `lint: null` for this project, so there is no lint command to run.
