PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1. Pair worker brackets within a correlation id | 4377cfd | Proof of failure at HEAD: new case `renderTrace: a re-run never pairs across runs, and unpaired names the run` failed `AssertionError: actual undefined, expected '1-aaa'` at `trace.test.mjs:198` (`r.unpaired[0].corr`). The surviving unpaired entry at HEAD was run 2's dispatch - run 2's `return` had closed run 1's stranded dispatch, the exact cross-run pairing defect. After the fix: 21/21 in `trace.test.mjs`, live `planning.mjs trace render --phase 1` still prints `counts` (routing 7, provider 11) and `unpaired: []`. CI trio green: 1279 tests, self-verify `ok:true`, tsc clean. |
| 2. Census the four families' producers | cb3291b | Regression guard, green at HEAD by construction (stated in the test's own header). Failing-capability proved by mutation, three runs. (A) The plan's stated mutation - delete `--family outcome` from `references/review-triggers.md:175` - did NOT fail: 22 pass, 0 fail; `workflows/verify.md:233` is a SECOND outcome producer, so the family still had a writer (deviation below). (B) Delete the `trace append ... --event phase_start` line from `workflows/execute.md:70`: FAIL - `no prose producer writes the anchor 'phase_start'. lifecycle events written down: dispatch, return, checkpoint, dispatch, return, checkpoint`. (C) Delete BOTH outcome producers (`verify.md:233` + `review-triggers.md:175`): FAIL - ``family `outcome` lost its writer - no producer left in the shipped surfaces. Found: routing=[cadence-core/bin/route.mjs] provider=[cadence-core/bin/review-provider.mjs] lifecycle=[...7 sites...] outcome=[]``. All three files restored via `git checkout --`; `git status --short` shows no prose file modified. CI trio green: 1280 tests, self-verify `ok:true`, tsc clean. |
| 3. Ask on a torn config layer whichever layer tore | 0ab7046 | Proof of failure at HEAD, all three new/rewritten cases, stdout EMPTY from the hook (`guardWithGlobal` returned null): `TypeError: Cannot read properties of null (reading 'permissionDecision')` at `git-guard.test.mjs:352` for `a torn USER-GLOBAL layer asks too, naming that layer`; same for `a torn global layer asks on 'release' too` and for the rewritten `a torn GLOBAL layer whose path merely PREFIXES the repo layer asks as itself` - 30 pass / 3 fail. After the fix: 33/33 in `git-guard.test.mjs`, including the two pre-existing cases the plan names (`a torn repo layer never CANCELS a deny the global layer configured`, `the push rail is unchanged by a torn layer`). CI trio green: 1282 tests, self-verify `ok:true`, tsc clean. Risk-surface gate fired (auth/authz) and was CLEARED by the orchestrator's review: two cross-model voices, openai zero findings, deepseek's one medium killed in adjudication (`torn` is a plain disjunction with no layer-specific branching, and the emitted note is `torn[0]`, the warning carrying the true path - both halves unreachable). Committed as staged, nothing changed after review. |
| 4. Read the staged set so renames and non-ASCII paths are seen | 1d24d9a | Proof of failure at HEAD, both original cases, envelopes quoted. (a) `lease-check: a rename is checked on BOTH sides` - HEAD returned `{"ok":true,...,"staged":1,"declared":1,"_exit":0}`: `git mv src/other.js a.txt` destroyed a committed, undeclared file and the destination-only `--name-only` read reported a CLEAN lease. (b) `lease-check: a declared non-ASCII path is admitted` - HEAD returned `{"ok":false,"reason":"undeclared-files","undeclared":["\"src/caf\\303\\251.js\""],"_exit":1}`: the lease refused the very path its own plan declared, because at default `core.quotePath` git hands back the quoted octal spelling. Risk-surface gate fired (untrusted-input parsing feeding an authorization-shaped gate); the orchestrator's three-voice review returned FAILED on a reproduced BLOCKER - `{encoding:'utf8'}` maps every invalid UTF-8 byte to U+FFFD, undoing `-z` one layer below the parser, so `git mv src/caf<0xE9>.js src/caf<0xFF>.js` (emitted correctly as `R100\0src/caf\351.js\0src/caf\377.js\0`, paired correctly) collapsed BOTH sides to one Set entry and the rename source was licensed by the destination's declaration. TWO regression cases written FIRST and observed red against the reviewed staged code: `a rename between two un-decodable paths is never a clean lease` failed `notStrictEqual` on `{"ok":true,...,"staged":1,"declared":1,"_exit":0}` - the collapse, visible as `staged:1` for a two-path rename - and `a staged path that is not valid UTF-8 is refused BY NAME` failed `true !== false` on the same envelope. Fix per the user's chosen option: git's output read as a Buffer, split on the 0x00 BYTE, every path round-trip-checked (`Buffer.from(s,'utf8').equals(raw)`), and any staged path that does not round-trip refused with the DISTINCT reason `unrepresentable-paths` naming it in git's octal spelling (`"src/caf\351.js"`). Every round-tripping path behaves exactly as before. After the fix: 14/14 lease-check cases green (all ten pre-existing plus the two from the first pass). CI trio green: 1286 tests, self-verify `ok:true` (17 checks, zero problems), tsc clean. |
| 5. Gate the paths the executor commits, not the whole index - **DESIGN CUT** | 62c8774 | The task's design was CUT by the user after the `risk_surface` review returned a blocking FAIL with all three voices (openai, deepseek, claude-subagent) converged on one blocker and two reproduced end to end. Not patched - the fourth blocking FAIL in this phase and the third where a fix opened a new hole. **Discard, verified:** `git reset` + `git checkout --` on all four files the task touched; `git diff 1d24d9a --stat -- cadence-core/bin/planning.mjs cadence-core/bin/planning.test.mjs cadence-core/bin/weight-budgets.json skills/cad-executor-contract/SKILL.md` is EMPTY. `lease-check` takes no positionals again (`planning.mjs:2106` is `(dir, _sub, opts) => cmdLeaseCheck(dir, opts)`), the contract's step 4 is a bare `git commit` again, and task 4's un-decodable-path fix stands untouched. The suite is back to 1286 tests from 1292, i.e. the six task-5 cases are gone rather than skipped. `plan-2-risk-task-5.diff` deleted. **Why cut, for the record:** (i) nothing forced the executor to name every path it staged - stage `a.txt` plus `leftover.txt` holding `AWS_SECRET=xyz`, name only `a.txt`, the gate returns `ok:true` with `leftover.txt` under `other_staged`, the pathspec commit lands `a.txt`, `leftover.txt` stays in the index, and the orchestrator's OWN docs commit in `execute.md` (bare `git add` + `git commit`) sweeps it in - `git show HEAD:leftover.txt` printed the secret; (ii) `parseArgs` swallows a path after `--`, so `lease-check --phase 1 --plan 1 -- evil.txt a.txt` binds `evil.txt` as an option value, drops it from the gated set, and turns a hard `undeclared-files` refusal into `ok:true` exit 0 - reachable without malice, since the contract spelled `git commit -- <your paths>` one line below `lease-check ... <your paths>`; (iii) root cause, `other_staged` was `staged.filter(p => !gated.includes(p))` - the seam has no provenance signal at all and cannot distinguish the user's pre-staged file from a path this executor staged and omitted, yet the contract told the executor to treat that whole set as the user's work and never block on it. **Upstream fix shipped in its place:** the motivating complaint (a file the USER staged before the run halts the phase on work no plan touched) is a once-per-run condition, so `execute.md`'s `git_guard` step - which already runs before the first dispatch and already owns the protected-branch guard - now runs `git diff --cached --quiet` and, on a dirty index, names what is staged (`git diff --cached --name-status`) and asks through the ask-user seam: stash (`git stash push --staged`, git 2.35+) / commit it as the user's own / abort, no preselected default, with "never stash, unstage or commit the user's staged work without asking" stated inline. The step states why it lives there: the orchestrator is the only actor that can see a clean starting index, and once it is clean every later `undeclared-files` refusal is provably the executor's own doing. Proof the defect existed at HEAD: `grep -rn "diff --cached" cadence-core/workflows/` returned ZERO hits in `execute.md` (the two hits, `debug.md:113` and `verify.md:208`, are reviewers reading a staged diff, not a starting-index check) - the orchestrator never checked its starting index. `weight-budgets.json`'s `execute.md` entry regenerated from `weight.mjs` in the SAME commit, 22957 -> 24275. Risk-surface gate run and recorded as NO MATCH: no guard is removed (`lease-check` byte-identical to HEAD), no envelope/flag/exit-code/argv change anywhere, and every arm of the new prompt is recoverable - stash preserves (`stash pop`), commit preserves, abort preserves, and on git < 2.35 `--staged` is an unknown option that errors loudly rather than falling back to a bare `git stash` that would also take unstaged work, which is the one shape that could lose data. CI trio green: 1286 tests, self-verify `ok:true` zero problems, tsc clean. |
| 6. Release lockfiles by an ALLOWLIST of names, not a shape rule - **REWORKED after a blocking review** | a42cbde | The first pass (shape rule `[.-]lock\.(?:json\|ya?ml\|toml)$` beside `.lockb?`) fired the `risk_surface` gate and the review returned FAILED, both cross-model voices converging on one blocker AND one remedy: the shape releases every lock RESOURCE a human wrote under the same spelling - `deploy/redis-lock.yaml`, `k8s/leader-lock.yaml`, `terraform/state-lock.toml`, `db/replica.lock.json` - so the ACCEPTED COST row the first pass wrote was ruled unnecessary rather than regrettable. Reworked to an allowlist of 23 package-manager lockfile basenames, matched EXACTLY (case included), everything unrecognized keeping its floor; the ACCEPTED COST test row is deleted, not softened. Strictly narrower than the shape rule the plan authorized, so it needs no new permission. **Proof of failure at the reworked tests' HEAD** (module restored to the committed `/(?:\.lock\|-lock\.json)$/i` via `git checkout HEAD --`, so the red run is against the shipped rule, not against the rejected staged one): `risk-surfaces.test.mjs` 42 pass / 24 FAIL - every one of the 23 allowlist rows plus the case-exactness row. Two failure shapes, both quoted: a name the old rule never reached fails the RELEASE half (`the allowlist releases gradle.lockfile` returned `[{surface:'concurrency',floor:'critical',path:'gradle.lockfile',pattern:'gradle'}]` against `[]`; same for `pnpm-lock.yaml`, `packages.lock.json`, `conda-lock.yml`, `conda-lock.yaml`, `npm-shrinkwrap.json`, `bun.lockb`, `go.sum`), and a name it reached TOO WIDELY fails the PRECISION half (`the allowlist releases Cargo.lock` failed `0 !== 1` at the `vendor/copy-of-Cargo.lock` control, because the old suffix rule released that too; same for `yarn.lock`, `poetry.lock`, `Gemfile.lock`, `composer.lock`, `package-lock.json`, `bun.lock`, `deno.lock`, `uv.lock`, `Pipfile.lock`, `pdm.lock`, `mix.lock`, `pubspec.lock`, `flake.lock`, `Podfile.lock`). `the allowlist is matched EXACTLY` failed `0 !== 1` on `Gemfile.LOCK` - the old rule was case-insensitive. `route.test.mjs` 124 pass / 2 FAIL: `a solo phase declaring pnpm-lock.yaml resolves SOLO` and its `packages.lock.json` twin, both `actual: 'critical', expected: 'solo'` with a non-empty `risk floor:` reason, end to end against the LIVE `route-table.json`. **Non-vacuity is mechanical, not asserted:** each released row builds its surface row from `pathTokens(name)`, the file's OWN tokens, so an empty result can come from nothing but the exclusion (a `conc()` row would have made `bun.lockb`, `go.sum`, `gradle.lockfile` and `npm-shrinkwrap.json` vacuous - their tokens equal no concurrency pattern), and each asserts a near-miss `vendor/copy-of-<name>` under that same row still floors. **STILL FLOORING, green at HEAD by construction and said to be:** all ten controls the rework names - `src/lock.rs`, `src/locking.rs`, `internal/lock/manager.go`, `db/locks.sql`, `src/redis-lock.go`, `deploy/redis-lock.yaml`, `k8s/leader-lock.yaml`, `terraform/state-lock.toml`, `db/replica.lock.json`, `migrations/add-lock.sql` - floor before and after; five of them were released by the rejected shape rule, which is why they are rows now. `deploy/redis-lock.yaml` and `db/replica.lock.json` are also pinned end to end through `resolve` beside the four D-21 floor fixtures, and the D-21 one-plan-per-lockfile resolve loop is unchanged in shape (7 plans, one per lockfile). After the fix: `risk-surfaces.test.mjs` 66/66, `route.test.mjs` 126/126. CI trio green: 1315 tests, self-verify `ok:true` zero problems, tsc clean. Gate: the surface fired once, the orchestrator's review adjudicated it, and this dispatch's instruction is to commit the reworked form - recorded rather than re-fired, since the committed rule is a strict subset of the reviewed one. |
| 7. Record every provider drop-out in the trace, not only the ones past the wire | 3b3d09b | Proof of failure at HEAD, `review-provider.test.mjs` 49 pass / 3 FAIL, all three quoted. (a) The REWRITTEN case `provider trace: a command that issues no request is STILL recorded, naming its reason` failed `0 !== 1` at `:575` (`ev.length`) - `.planning/trace.jsonl` did not exist at all in the fixture cwd after a `no-key` run. (b) `provider trace: an over-cap refusal records itself, from its own process` failed `0 !== 1` at `:603`, same cause, in a subprocess whose config sets `review.max_prompt_tokens: 1`. (c) The census `drop-out: every refusal before the wire writes ONE provider event naming its reason` failed on its first row with a named message: `review/bad-provider: exactly one event, got []`, `0 !== 1` at `:954`. The census covers TEN rows - `bad-provider`, `bad-args`, `no-key`, `bad-payload` for both `review` and `consult`, plus `bad-provider` and `no-key` for `detect-models` - and each row asserts `envelope.reason`, exit code 1, `seen.length === 0` (nothing reached the faked wire), exactly one new event, its `outcome`, `degraded: true`, its `command`, its `provider`, and a non-empty string `detail`. The faked wire is armed to answer `200 {"findings":[]}`, so a row that failed to stop early would fail loudly rather than pass on an outcome the transport produced. After the fix: 52/52 in `review-provider.test.mjs`, including the two the plan names - `an unwritable trace changes the envelope by not one byte` (which now compares two runs that each ATTEMPT a write) and `the destination is the adapter base, and no environment variable moves it` (its `process.env` census is still exactly 3, since nothing added here reads the environment). CI trio green: 1318 tests, self-verify `ok:true` (17 checks, zero problems), tsc clean. **Gate: risk surface run and recorded as NO MATCH.** The one heuristic hit is a test fixture line `OPENAI_API_KEY="test-not-a-real-key"`, which drops under the named placeholder-shaped-secret pre-filter (BOTH conditions hold: an example/test fixture file, and an obvious stub value; four identical lines already exist in that file). The substantive question - a credential-handling seam gaining a new on-disk sink - is closed by a complete enumeration of the 11 `fail()` sites: the resolved `key` value flows to `authHeaders` alone (`:607`, `:647`, `:694`) and into no `detail` anywhere, and `fail('no-key', ...)` fires only when `key` is null and names the env var and the FILE PATH, never a value. The http site's `{status, body}` response body still cannot reach the trace either: `callStructured` records `HTTP <status>` first and `traceRecorded` then blocks `fail()`'s second write, which is why the flag is set BEFORE the write attempt rather than after. Every string newly written was already emitted verbatim on stdout by the same run, is sliced to 200 chars, and lands in `.planning/trace.jsonl`, which is gitignored (`.gitignore:26`), so nothing new is disclosed and nothing reaches history. |
| 8. Refuse a milestone by tag MEMBERSHIP, not by sort order | 45b4d6e | Proof of failure at HEAD, 23 pass / 7 FAIL across the two files, and the live-git rows are the real proof rather than the pure ones. **Live envelope at HEAD**, a hand-built fixture (`### Active` = `v1.9.1`, real repo tagged `v1.9.0` + `v2.0.0`): `{"ok":true,"action":"ask","branch":null,...,"reason":"already-published: the milestone version 1.9.1 does not sort above 2.0.0, which this repo has already published as a tag - ...","warnings":[]}` - a refusal asserting a tag that does not exist. The same shape as a seam test (`published: an untagged maintenance milestone below a higher tag still creates (AC8)`) failed `'ask' !== 'create'`, and the rewritten AC8 collision case failed on `not the highest tag: it did not cause this` (HEAD's reason names `0.2.0`, a release with nothing to do with the refusal). Five pure rows failed too - `EQUAL to a published tag` (`'create' !== 'ask'`, i.e. HEAD ignores the new `publishedVersions` key entirely), the `UNTAGGED maintenance` row's two tagged-neighbour controls, the tagged-prerelease row, the non-string tag element row, and the SCALAR row. **Vacuity, stated:** because HEAD silently ignores an unknown option key, the *`create`* half of every rewritten pure row is green at HEAD for the wrong reason; that is why each such row carries an `ask` control beside it (the tagged neighbours `v1.9.0`/`v2.0.0`, the tagged `v2.4.0-rc.1`) and why the two live-git seam rows exist. After the fix: `branch-decision.test.mjs` + `git-branch.test.mjs` 30/30. **Live regression, this repo:** `node cadence-core/bin/git-branch.mjs decide --branch main` -> `{"ok":true,"action":"create","branch":"cadence/v2.5.0",...}` (Active v2.5.0, newest tag v2.4.0), and the v1.9.1 fixture now returns `create`/`cadence/v1.9.1`. `highestSemverTag` and the now-unused `compareVersions` import are deleted from `git-branch.mjs`; `grep` confirms `publishedVersion`/`highestSemverTag` survive nowhere in `cadence-core`, `skills` or `agents`, and no prose surface cites the guard's reason wording (`grep` for `already-published`/`sort above` over `references`, `workflows`, `skills`, `agents`: zero hits). CI trio green: 1320 tests, self-verify `ok:true` zero problems, tsc clean. **Gate: risk surface run and recorded as NO MATCH.** Not auth, DB, money, concurrency, destructive (this seam only ADVISES - rail 1 runs `checkout -b`, unchanged), or secrets. Not a public API/wire contract: the seam's envelope keys are byte-identical (`ok, action, branch, mode, currentBranch, reason, warnings`), only the `reason` prose changes and nothing matches on it; `decideBranch` is a plugin-internal `lib/` export whose one production caller is updated in the same commit, typed `string[]|null` so a stale caller fails tsc, and `tagCarrying` reads a bare string as a one-tag list so a stale caller keeps the guard ARMED rather than silently disarmed. Not untrusted-input parsing: the tag list already flowed through the same `compareVersions` reader at HEAD (`highestSemverTag` ran `cmp(bare, bare)` on every tag), so no new parser and no new input reach anything - the change adds a non-string skip that HEAD did not have. |
Deviations:
- [deviation] Task 1: the plan quotes the worker key as `${key(e.phase)} ${key(e.plan)}` with SPACE separators; the shipped code at `lib/trace.mjs:259` separates with a literal NUL (`\0`). Kept the NUL separator and prepended `${key(e.corr)}` with the same separator, so the key stays unambiguous for a worker name containing a space. Behaviour is the plan's; only the separator byte differs from the plan's transcription.
- [deviation] Task 2: the plan's Verify step names a mutation that cannot fail. Expected `--family outcome` deleted from `references/review-triggers.md:175` to make the outcome-family assertion fail; observed 22 pass / 0 fail, because `cadence-core/workflows/verify.md:233` writes `--family outcome --event uat_verdict` as well and the assertion is per-FAMILY, not per-file. Recorded the non-failure, then proved failing-capability with the mutation the task's own Action names (delete one `trace append` from `execute.md`, which strands the ANCHOR role) plus a both-producers deletion that fires the family assertion itself. No test was weakened to fit: a per-file producer assertion would overfit the census to today's file layout.
- [deviation] Task 3: the plan's rewrite instruction for the `<repoLayer>.global` anti-conflation case says the reason "must NOT claim the repo layer failed to parse", which cannot be asserted as absence of the repo path - the global path CONTAINS it as a prefix. Asserted the two positive/negative forms that do separate the implementations instead: the reason includes `config layer <repoLayer>.global failed to parse` and does NOT include `config layer <repoLayer> failed to parse`.
- [deviation] Task 4: the plan's rename fixture needs a seed COMMIT (`git mv` only produces a rename record in `diff --cached` when HEAD carries the source), and this machine's global git config sets `commit.gpgsign=true`, which would make the fixture prompt for a passphrase. Added a `leaseGit` helper that runs the fixture's git calls with `GIT_CONFIG_GLOBAL=/dev/null` and `GIT_CONFIG_SYSTEM=/dev/null`, the same neutralization the renumber fixture at `planning.test.mjs:3078-3079` already uses. The seam under test is still run with the ambient environment, so `-M` is exercised as the plan requires rather than against a config that pre-guarantees it.
- [deviation] Task 4: the plan says `-z` alone makes the non-ASCII half work. It does not, and the blocking review reproduced why: `-z` is correct on the wire and is undone by the `{encoding:'utf8'}` decode above it. Added a byte-level read the plan never names - Buffer output, 0x00 split, per-path round-trip check - and a THIRD refusal reason `unrepresentable-paths` beside `undeclared-files` and `no-staged-set`, fail-closed on any staged path that cannot be spelled in UTF-8. The declared side is deliberately NOT made byte-aware: the frontmatter reader is shared with the overlap gate and is out of this task's scope (the user rejected that option explicitly).
- [deviation] Task 5: the task as planned is CUT on the user's instruction after a blocking `risk_surface` FAIL, and NOTHING of its design ships - not the positional paths, not `other_staged`, not `unstaged-files`, not the pathspec commit, not the contract's mechanical `--` assertion. The plan's Must-be-true clause "a file the USER staged before the run neither halts the phase nor rides the executor's commit" is met upstream instead, in `execute.md`'s `git_guard` step, by refusing to START with a dirty index rather than by inferring provenance mid-task.
- [deviation] Task 5: `cadence-core/workflows/execute.md` is NOT in PLAN-2.md's declared `files:` list. Recorded here as a deviation rather than edited into the plan's frontmatter mid-execution, per the user's instruction. It is the file the replacement fix must live in - it is the only surface that runs once, before the first dispatch, and it already owns the protected-branch guard the new check sits beside.
- [deviation] Task 6: **the executed rule is an ALLOWLIST, not the shape rule PLAN-2 prescribes, and this supersedes PLAN-2's own supersession of D-05.** The plan authorized "a basename ending `.lock`/`.lockb`, OR matching `[.-]lock\.(?:json|ya?ml|toml)$`"; the blocking `risk_surface` review rejected the second arm with both cross-model voices converging on the same blocker and the same remedy. Reviewers' reasoning, recorded verbatim in the header comment: the set of package managers is finite and enumerable while the set of names that merely LOOK like a lockfile is not, so an allowlist is strictly more precise in BOTH directions - it releases exactly the generated files nobody wrote, and anything unrecognized conservatively keeps its floor. The committed rule releases strictly FEWER paths than the plan authorized, so it needs no new permission. The ACCEPTED COST the first pass wrote (`deploy/redis-lock.yaml` released with the lockfiles) is deleted from the code comment and from the tests, because the cost is no longer paid.
- [deviation] Task 6: the allowlist carries 23 basenames - the 22 the rework names plus `deno.lock`. Deno was added because the rule being REPLACED (`.lock` suffix) already released it: omitting it would have made this a regression for a current, mainstream manager rather than a tightening of a too-wide rule. Every entry is a name a package manager GENERATES; nothing was added on shape.
- [deviation] Task 6: the pre-existing green test `the exclusion is case-insensitive on the suffix` is REVERSED, not kept. An allowlist matched case-insensitively would release `gemfile.lock` and `CARGO.LOCK`, which no tool writes, so the rewritten row (`the allowlist is matched EXACTLY, so a case variant keeps its floor`) asserts `Gemfile.LOCK` and `cargo.lock` floor while `Gemfile.lock` is released. The direction is conservative - the rewrite makes the classifier floor MORE, never less - and it is stated in the header comment as a deliberate reversal.
- [deviation] Task 6: the pre-existing `a lockfile is excluded from EVERY surface, not from concurrency alone` case used `sessions.lock`, which is not a package manager's file and correctly floors under an allowlist. Rewrote it on `Pipfile.lock` against an auth row holding `pipfile`, with `src/pipfile-parser.py` as the control proving that row can see the word. The claim it pins - the exclusion runs before ANY surface's patterns - is unchanged.
- [deviation] Task 7: the plan's `activeMeta` shape is `{command, provider: opts.provider, model: opts.model, effort: opts.effort}` for all three commands. `cmdDetect` keeps `model: null, effort: null` instead, which is the shipped constructional null its own comment states and its fault test asserts (`ev.model === null`, `ev.tier === null`). Reading `opts` there would start recording a `--model` `detect-models` never sends, turning a null that is true by construction into one that merely happens to be absent.
- [deviation] Task 7: the plan says `fail('bad-command')` in `main` "still writes nothing (no `activeMeta`)". That holds per PROCESS but not across the in-process test harness, which runs several commands in one: without a reset the bracket from the previous command would still be set and `bad-command` would record an event against a subject this call never had. Added `activeMeta = null; traceRecorded = false;` as `main()`'s first act, plus a case pinning it (`drop-out: bad-command records nothing - no command ever began`), which is green at HEAD by construction and says so.
- [deviation] Task 7: the plan splits the harnesses "in-process `runFaked` where the wire is reached and subprocess `runRawIn` where it is not". None of the five drop-outs reaches the wire, so the split used is different and stated: `runFaked` for the eight rows that can run in the shared fault fixture, because in process `seen.length === 0` is directly assertable (the stronger claim); `runRawIn` for the two that cannot - `no-key`, which must run with no key in the environment, and `over-cap`, whose cap is memoized per process.
- [deviation] Task 7: the plan's rewritten `:544` case offers "assert the event carries no HTTP status - or keep an in-process `runFaked` twin that asserts `seen.length === 0`". Both were done rather than either: the subprocess case asserts no `status` field and no `HTTP` in the detail, and the in-process census asserts `seen.length === 0` on that same `no-key` drop-out among the other nine.
- [deviation] Task 7: the two stale header claims the plan names were corrected in BOTH places they appear, not only the file header - `reviewConfig()`'s own docblock made the same "the warnings ride the provider trace event" claim. Both now say a COUNT, non-zero only, and only on an event that was written at all. The `:290` citation became `:360`, not the plan's `:303`: this task's own insertions moved the function.
- [deviation] Task 8: the plan's membership test is "for each tag, strip a leading `v` on both sides and test `compareVersions(tag, milestone) === 0`". Implemented exactly, plus two totality guards the plan does not name and the input requires: a non-string element in the tag list is skipped rather than thrown on (the list is `git tag --list` output, data this module did not write - HEAD's `highestSemverTag` would have thrown on `tag.replace`), and a bare STRING argument is read as the one tag it is instead of as "not a list, therefore nothing published". The second is the fail-open shape the argument change itself creates: a caller left on the superseded scalar would otherwise disarm the guard silently, which is worse than the sort-order bug being replaced. Both carry their own test rows.
- [deviation] Task 8: the plan says the rewritten `:127` case keeps "naming both numbers". Under membership the two numbers are the milestone version and the TAG SPELLING that carries it, which is the same version twice - the higher tag is no longer a comparand and naming it was the defect talking about the wrong release. The seam's AC8 case therefore had its `assert.match(r.reason, /0\.2\.0/)` REPLACED by `assert.doesNotMatch(r.reason, /0\.2\.0/)` plus a `/v0\.1\.0/` tag-spelling assertion; that reversal is one of the two red rows that proved the defect at HEAD.
- [deviation] Task 8: the plan's proof-of-failure instruction ("write the `v1.9.1` test in `branch-decision.test.mjs`, run it, record the failure") cannot fail at HEAD as written - HEAD ignores an unknown `publishedVersions` key, so the row goes green vacuously, the exact shape this plan exists to answer. The `v1.9.1` case was written at BOTH levels: the pure row (with tagged-neighbour `ask` controls that DO fail at HEAD) and a new live-git seam row in `git-branch.test.mjs`, which is where `'ask' !== 'create'` is a real red against real tags. The plan's Verify names only `branch-decision.test.mjs` for this case.
Open items:
- [task 6, deliberate, recorded so it is not a surprise] The allowlist floors a handful of names the replaced `.lock` suffix rule released: legacy or niche manager lockfiles such as `Gopkg.lock`, `glide.lock`, `paket.lock`, `shard.lock`, `Berksfile.lock` and `Puppetfile.lock`. That is the allowlist's stated maintenance cost working as designed - an unrecognized basename keeps its floor, and the remedy is one line in `LOCKFILES` plus one row in `ALLOWLISTED` in `risk-surfaces.test.mjs`. Left out because each entry is a claim about a real generated file and none of these six is current enough to be worth asserting unprompted.
- [task 3, from the cleared risk_surface review, recorded not fixed] The assertion added to the EXISTING repo-layer torn-config test - that a torn repo layer names the repo path - is one the UNFIXED implementation also satisfies, so it does not by itself distinguish the fix. It is harmless beside the separate new case that does distinguish (`a torn GLOBAL layer whose path merely PREFIXES the repo layer asks as itself`), but a reader could mistake it for the guard. Deepseek raised it as a low and granted as much.
- [task 4, from the blocking risk_surface review, recorded not fixed by the user's instruction] `parseStagedNameStatus` accepts a stream whose FINAL record has no trailing NUL, and a stream cut mid-path yields a TRUNCATED path rather than `null`, contrary to its own docblock and the caller's "returned a truncated record" message. Not reachable from real git - `execFileSync` throws ENOBUFS on overflow and a killed or non-zero-exit git is caught into `no-staged-set`, so both real shapes fail closed - and the defect is the unmet contract, not a live hole.
- [task 4, same review, recorded not fixed] The `C` (copy) branch adds the copy SOURCE to the leased set, so it would refuse a lease whose only staged write is the declared destination. Defensive only: the reviewer verified `-M` overrides `diff.renames=copies`, so `C` is unreachable as this seam invokes git.
- [task 4, credit worth keeping] The reviewer fuzzed 500k cases across `A D M T U X R<score> C<score>` with adversarial paths and found ZERO desyncs, and confirmed `-M` overrides both `diff.renames=copies` and `diff.renames=false`. The variable-stride parsing is correct; only the encoding beneath it was wrong.
- [task 5, recorded not fixed by the user's instruction] `parseArgs` (`planning.mjs`) treats ANY `--`-prefixed token as a flag that consumes the next word, so a bare `--` separator binds the following positional as an option value. Harmless for `lease-check` again now that it takes no positionals, but it will bite the next command that takes them.
- [task 5, recorded not fixed by the user's instruction] The `git diff --name-only -- <paths>` equality assertion the cut design put in the contract was blind to a path staged as a DELETION whose file exists untracked in the worktree: the gate judges a deletion, the assertion comes back empty, and a pathspec commit records the worktree file. Reproduced by the reviewer. Recorded because the same shape returns with any future pathspec-commit proposal.
- [task 5, recorded not fixed by the user's instruction] `unrepresentable-paths` and `no-staged-set` are computed over the WHOLE index, so a user-staged non-UTF-8 filename still refuses every plan. The upstream dirty-index check now in `execute.md`'s `git_guard` step is what makes that reachable-but-rare rather than a live halt.
- [task 7, out of scope, recorded not fixed] `fail('bad-payload', e.message)` now copies a `JSON.parse` error message into `.planning/trace.jsonl`, and Node quotes a fragment of the offending input in that message. The fragment is the review payload, which is content the run was about to hand to a third-party model anyway, it is sliced to 200 chars, the file is gitignored, and the identical string already goes to stdout - so this is a wider audience for an already-disclosed string rather than a new disclosure. Recorded because it is the one field in the new event whose content is not fixed by this seam.
- [task 7, deliberate] `traceProvider` now marks the call recorded BEFORE it attempts the write, not after. A write the seam could not complete (an unwritable trace) therefore does not license a second attempt from `fail()` - which keeps "exactly one event per call" true, at the cost that a failed explicit write is never retried with `fail()`'s poorer detail. That is the intended trade and the `an unwritable trace changes the envelope by not one byte` case pins the envelope half of it.
- [task 8, recorded not fixed] The published-version guard now refuses strictly FEWER milestones than it did: only exact tag membership, where sort order refused everything at or below the highest tag. The #87 collision (`v2.4.0` named while `v2.4.0` is tagged) still refuses; what no longer refuses is an untagged version that merely sorts low, which is the defect. Noted so the loosening is a recorded decision rather than a silent one.
- [task 8, from the D-23 supersession] `decideBranch` no longer accepts the `publishedVersion` KEY at all. An in-tree caller left on it fails tsc (the typedef is `string[]|null`), and the tree has exactly one production caller, updated in the same commit - but a caller outside the tree would get `undefined`, i.e. no comparand and no guard. Only the scalar VALUE shape is tolerated (read as a one-tag list), not the old key name.
</content>
