PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1. The per-run scratch rule becomes a check | 879b576 | `conventions.md`'s bulk-output section rewritten to the per-run form; `lib/scratch-path.mjs` written pure and zero-dep with three line-local rules and its accepted gap stated in the header; `scratch-path.test.mjs` pins all six D-11 sites verbatim before AND their converted form after; `bulk-output.test.mjs` fixture re-pointed at `> "$D/render.json"`; budget re-pinned |
| 2. The blocking re-arm cap reads this run's own count | bc8f594 | same-invocation arm, `mktemp -d` + `&&` chain; `(r.outcomes||[])` replaced by an explicit array assertion; refuses `scratch-unreadable` / `scratch-shape`; prose routes a non-zero exit to the STOP-and-ask arm; budget re-pinned 9522 -> 10773 |
| 3. `/cad-progress --trace` stops printing `{}` as a success | 0b04396 | same-invocation arm; read-back refuses on parse failure and NAMES the missing field of `counts`/`roles`/`unpaired`/`capped`; `{}` never reaches stdout; budget re-pinned 10284 -> 11323 |
| 4. `/cad-report` carries its run directory between its two steps | 3a21971 | split arm: `read_record` echoes the directory and a run token, `compose` carries both as literals and refuses `scratch-stale` / `scratch-unreadable` / `scratch-shape`; a SECOND run's complete, well-formed record carrying the FIRST run's token was watched refusing; budget re-pinned 12883 -> 14838 |
| 5. The cross-model payload rides this run's own artifact | 32cc5af | split arm: artifact and payload both inside one `mktemp -d` directory; the payload path is now an explicit fourth ARGUMENT instead of derived from `process.env.TMPDIR`; composer refuses an unreadable brief/artifact and an EMPTY artifact without writing a payload; `--payload` carries the echoed literal behind a token check; shape (b)/(c) paragraph preserved and corrected; budget re-pinned 31419 -> 33781 |
| 6. The inline task risk diff is this run's own | 7816487 | inline arm writes `$D/cadence-risk-task-{slug}.diff` under a `mktemp -d` directory, planned arm untouched, no `.planning/` directory created; `.planning/DOCS-CLAIMS.md` TASK-17 re-pointed 103-105 -> 125-129 with the claim restated as the per-run form; budget re-pinned 7295 -> 7822 |
| 7. Prove each read-back refuses by running it | 941a74d | `scratch-readback.test.mjs` extracts the four shipped `node -e` scripts out of prose and EXECUTES them against a truncated file and a well-formed wrong-shaped one; script count pinned at 4; a negative control proves the unguarded form prints `{}` and exits 0; watched going red naming `triage-gate.md` with that surface's guard stripped from the prose, then green on revert |
| 8. Wire the check into self-verify | 34e36e4 | `scratchPathIssues` called from the same per-surface walk as checks 19 and 20, header entry 21 added, `checked` names `scratch-path`; three wiring rows in `self-verify.test.mjs` (fires, reaches `problems`, live tree clean); re-introducing a fixed shared path at `triage-gate.md` was watched exiting 1 naming that file and `scratch-shared-path`, and reverting restored exit 0 |
Deviations: none
Open items:
- Task 8 required one further edit to `conventions.md`'s rule sentence (folded into 34e36e4): `prose-agreement.test.mjs`'s `TRN-02` row extracts the scratch-path PREFIX out of the rule's own sentence and requires the three converted `trace render` sites to redirect into it. The task-1 wording had dropped the redirect form from that sentence and broken the `rides a file, not the transcript` clause across a line, so the row failed. The rule now states `> "$D/<name>"` inside that sentence, which re-arms `TRN-02` on the per-run prefix instead of the fixed shared one. `prose-agreement.test.mjs` itself is NOT in this plan's lease and was not touched.
- AC4's literal sweep (`rg 'TMPDIR:-/tmp' cadence-core skills agents hooks`) still shows `${TMPDIR:-/tmp}` without `mktemp` at eight lines under `cadence-core/bin/` - the six BEFORE fixtures in `scratch-path.test.mjs`, one fixture in `self-verify.test.mjs`, and the checker's own detail string in `lib/scratch-path.mjs`. Those are the shapes the check must be able to REPORT, not transports. Every prose site the sweep reaches carries `mktemp` on the same line.
- Declined a `scratch-stale` fixture in `scratch-readback.test.mjs`: the plan's `Verify` and `## Must be true when done` name the truncated and wrong-shaped arms, and the stale arm was proved by running it at tasks 4 and 5 rather than pinned. Add it when a task states it, since it is the one arm no shape guard can reach.
- `.planning/DOCS-CLAIMS.md` rows TASK-13 (78-80), TASK-14 (80-82), TASK-15 (85-88) and TASK-16 (100-101) cite line spans in `workflows/task.md` that had already rotted before this phase - TASK-16's bullet now opens at 122. Only TASK-17 was in this plan's scope; the neighbours were left rather than swept.
