PLAN COMPLETE
Plan: .planning/phases/1/PLAN-2.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1: Refuse a primary manifest that carries no `version` field | e59e80b2 | Seam re-classifies a PRIMARY `skip`/`no-version-field` verdict to `ok:false`/`action:"refuse"`/`reason:"no-version-field"`/exit 1 with its own detail sentence; pure core and sibling rows unchanged. Rewrote the version-less test to the refusal (keeps the field-really-removed assertion, the byte-identical CHANGELOG check, adds a byte-identical plugin.json check); new test copies this repo's real plugin.json + marketplace.json into a fixture and pins `ok:true` with a `.claude-plugin/marketplace.json` `action:"skip"` row. `node --test cadence-core/bin/release-bump.test.mjs`: 37/37 pass (36 at baseline; one test became two). |
| 2: Name the changelog's state explicitly on every envelope | 889ec6b7 | `changelog.state` set on all 9 emitting sites: `not-examined` on the four pre-gate refusals, `unreadable` on the unreadable-changelog refusal, `absent`/`ok` from `readChangelog`'s own result inside the gate, inherited by the `partial-bump` spread and the success emit. `section_empty`, `changed`, `promoted` unchanged; the `no-plugin-manifest` skip and the `bad-date` refusal untouched. New test drives absent / present-and-clean / refused-before-the-gate and asserts three distinct VALUES plus key presence on each. 38/38 pass. |
| 3: Refuse an unparseable `--version` by naming the raw argument | 6fb980cc | The raw trimmed `--version` is kept when `normalizeTargetVersion` is left with nothing usable, so `decideManifestBump` returns its own `unparseable-version` with the value quoted; no verdict code minted at the seam and `normalizeTargetVersion` untouched (planning.mjs's `version_drift` consumer). Two tests: `v` and `"  v  "` refuse with `target:"v"`, detail naming it, exit 1, both files byte-identical; `vv` still `unparseable-version`, absent still `no-target-version`, blank still `missing-flag-value`. 40/40 pass; `release-decision.test.mjs` 42/42 still green. |
| 4: Redden when a verdict code never reaches the two documents | 5838f100 | Header verdict sentence now names all seven codes (added `unparseable-version`, `downgrade`, `not-an-upgrade`) and states the one re-classification. New `prose-agreement.test.mjs` check extracts `code: '<token>'` literals from `decideManifestBump`'s body and asserts each appears in release-bump.mjs's leading header comment block and in that function's JSDoc, with a non-vacuity floor of the seven shipped codes and word-boundary matching so `bump` is not satisfied by `partial-bump`. Falsified both directions: removing `not-an-upgrade` from the header failed naming that token and that file; removing `already-at-target` from the JSDoc failed naming release-decision.mjs. Both restored, 42/42 green. |
| 5: Halt the close on the two states, and re-pin the surfaces that name them | 4b4d0e16 | `no-version-field` added to step 2's refusal-code list (milestone.md:68); the changelog halt now names `changelog.state: "absent"` beside `section_empty: true` (milestone.md:84-89). No fifth halt bullet - "Four halts" still holds (D-01). milestone.md 14937 -> 15190 B, `weight-budgets.json` re-pinned in the same commit (D-12). DOCS-CLAIMS.md `MILESTONE-06` -> 67-72 with `no-version-field` in its claim, `MILESTONE-08` -> 84-89 (was stale at 60-62) with the absent state in its claim. `self-verify --root .` `ok:true`, `problems:[]`; `wc -c` == the re-pinned budget; `node --test 'cadence-core/bin/release-*.test.mjs'` 82/82 and `prose-agreement.test.mjs` 42/42. |
Deviations: none
Open items:
- `MILESTONE-07`'s line range in `.planning/DOCS-CLAIMS.md` was moved by one line by task 5's own edit, so it was corrected to 78-83 in the same commit even though the plan named only rows 06 and 08. Not a scope widening - it is staleness this task caused.
- No static-analysis command exists for this tree: `planning.mjs detect-commands --root .` returns `lint:null, typecheck:null` (tsc is not on PATH and there is no `node_modules/.bin`), so step 3's subprocess was skipped as an answer rather than a failure. `release-bump.mjs` carries `// @ts-check`, so its annotations are unchecked in this environment; `tsconfig.ci.json` excludes `*.test.mjs`.
- Declined a `changelog.state` assertion on the `unreadable-changelog` and `partial-bump` envelopes: task 2's `Verify:` names three states in one test, and the two extra paths set the field from the same three-value vocabulary at their emit sites. Add pinning tests when a task states a fourth distinguishing consumer.
- `.planning/DOCS-CLAIMS.md` rows `MILESTONE-04` (cites 41-42) and `MILESTONE-05` (cites 43-45) appear stale against milestone.md's current line numbers. Pre-existing, untouched by this plan, and D-14 records that no mechanical check enforces those ranges.
