PLAN COMPLETE
Plan: .planning/phases/1/PLAN-3.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: Route the empty-set fallback to the arm that brackets it | d7ada952 | Both copies rewritten. `review-triggers.md:202-210` now sends the fallback into step 4's own `claude-subagent` arm - append its `lifecycle/dispatch`, dispatch, CLOSE on parse, `--detail-file` checkpoint arm on a failed/empty/unparseable return - and says explicitly that step 3's rule only guarantees the set is non-empty while the close is owed at step 4 (D-08). `review-cross-model.md:130-138` says the same thing pointing back at that arm by name, since it is the file a model is reading when it hits the fallback (D-07). The advisory bracket inversion further down `review-triggers.md` is untouched (D-09). `weight-budgets.json` re-pinned in the same commit: `review-triggers.md` 20196 -> 20752 B, `review-cross-model.md` 9812 -> 10303 B. `node cadence-core/bin/self-verify.mjs` `ok:true`, `problems: []` - the two `budget-overrun` entries observed before the re-pin (491B and 556B over) are gone. `npx tsc -p tsconfig.ci.json` exit 0. AC2's live half is human-verify and is NOT discharged here (no provider key, no induced failure). |
| 2: State that the routing resolve is owed once per fire, whichever backend serves it | 67868b46 | Three sites, no code. `review-triggers.md` step 1 gains the rule directly under the `route.mjs resolve` block: one resolve per FIRE, owed before any backend is chosen, a panel naming two providers writing exactly ONE resolve against two `provider/request` events, and a cross-model-ONLY fire still owing it - closing with "step 4's 'no lifecycle bracket' is a statement about the BRACKET and never about this line", which is the misreading the Action diagnoses. The step-4 cross-model bullet's rule list goes four to FIVE with the resolve first, so a reader who never leaves step 4 meets it. `review-cross-model.md`'s header names the step-1 resolve first among the rules a caller owes whether or not it reaches the procedure. `route.mjs` untouched (D-05). The three no-resolve paths are untouched (D-06). `weight-budgets.json` re-pinned in the same commit: `review-triggers.md` 20752 -> 21566 B, `review-cross-model.md` 10303 -> 10445 B. `self-verify` `ok:true`, `problems: []`; `npx tsc -p tsconfig.ci.json` exit 0. The `human-verify` COUNT arm (five fires -> five resolves) is not discharged here and is a count, not a join (D-12). |
| 3: Correct the two review references' "no token field" claim | fe5daeb1 | Both bullets read against their LIVE text after tasks 1-2, not against the plan's quotations. `review-triggers.md:207-212` and `review-cross-model.md:17-27` now say the arm still gets NO lifecycle bracket - unchanged, and stated as unchanged - while the call is no longer unpriced, because the seam reads the provider's own reported usage onto the `provider/request` event. The consequence is kept rather than deleted: that figure is a different DENOMINATION and never sums into `roles`, so `cad-reviewer`'s per-role total in `trace render` still covers the claude-subagent voice ONLY, priced on its own event rather than missing (D-01). `review-cross-model.md`'s header rule list gains the same one clause. Verify: `grep -n "no token field\|unmeasured\|adapter extracts\|could exist"` over both files returns exactly one hit, the new true sentence ("every adapter extracts the provider's usage"), and `claude-subagent voice ONLY` survives once per file. `weight-budgets.json` re-pinned in the same commit: `review-triggers.md` 21566 -> 21718 B, `review-cross-model.md` 10445 -> 10738 B. `self-verify` `ok:true`, `problems: []`; `npx tsc -p tsconfig.ci.json` exit 0. Tests: the task's `Verify` names the full suite, which the executor contract allows only once per dispatch at the end, so the targeted run here was the ELEVEN test files that read either reference by name (`deferred-reads`, `dispatch-phrasing`, `adjudication-record`, `reference-routers`, `scratch-path`, `risk-diff`, `scratch-readback`, `route`, `prose-agreement`, `self-verify`, `trace`): 864 pass, 0 fail. |
| 4: Retire the cross-model entry from the spend caveat and re-adjudicate its ledger row | c03da06a | `SPEND_EXCLUDES` is two entries now - `["the orchestrator's own turns", "figureless returns"]` - and its doc comment records WHY the third left and that it must not come back: the entry's stated reason (no bracket, no token field at all) died with plan 1, the spend is folded into `provider_spend` and printed on `report.md`'s `Cross-model reviews` line, and "excluded" became the wrong word for a figure that is reported rather than missing. All four readers moved with the one definition: the R5 evidence `join` comment (three -> two), `trace-suggest.test.mjs`'s committed-fixture `deepEqual` literal, and both files' length assertions (3 -> 2). The fixture re-pin carries its arithmetic per the file's own D-12 rule - the receipt still reads `423,846 of 968,705 recorded tokens (44%)`, unchanged, because that spend was never in this total; only the trailing `; excludes ...` clause is one name shorter. `report.md`: the shape line and the EXCLUDES rule both drop the name, and in its place the rule states where the spend IS reported, that the two denominations are never added, and not to put it back. `weight-budgets.json` re-pinned 23249 -> 23755 B in the same commit. `DOCS-CLAIMS.md` REPORT-12 re-adjudicated on the README-32 precedent: claim text rewritten to the live sentence, anchor re-pinned `72-73` -> `153-159`, resolution `corrected - TRC-12/CST-04, v3.7.10 phase 1 (plan 3 task 4)`, run-1 verdict cell left as recorded. Verify: `grep -n "cross-model provider calls"` over `lib/trace-suggest.mjs` and `report.md` returns ONE hit, the design-record note in the doc comment, and nothing in the exclusion list or the spend line; `node --test trace-suggest.test.mjs prose-agreement.test.mjs` 122 pass 0 fail; `citation-census.test.mjs` 5 pass 0 fail; `self-verify` `ok:true`, `problems: []`; `npx tsc -p tsconfig.ci.json` exit 0. |
Full suite (`node cadence-core/bin/test.mjs` - `workflow.test_command` is null and this repo has no root `package.json`, so this is the runner the project ships): 3698 tests, 3697 pass, 0 fail, 1 skipped, exit 0. GREEN on its one run, no repair round needed.

Deviations: none
Open items:
- Static analysis: `workflow.lint_command` is null and `planning.mjs detect-commands --root /code/cadence` reports `lint: null`, so there is no lint command Cadence can find for this project; `typecheck` is `npx tsc -p tsconfig.ci.json` and runs at every commit gate.
- Task 1's `human-verify` arm (AC2) is undischarged: it needs a live provider API key and an induced provider drop-out. Set a key, point the tier at an unresolvable model id, fire a `blocking` trigger, then `node cadence-core/bin/planning.mjs trace render --phase <N>` and confirm the fire has no `unpaired` entry and its bracket carries a token figure rather than `unrecorded`.
- Task 2's `human-verify` arm (AC5) is undischarged: it needs a phase whose review fires were all served by a provider, then `trace render --phase <N>` counting one `routing/resolve` for the reviewer role per fire. It is a COUNT and not a join - the resolve event carries no trigger field (D-12).
- Task 4 also moved the two GUARDED-READ fallback literals (`trace-suggest.test.mjs:780`, `prose-agreement.test.mjs:2025`) to the shipped two-entry list, which the plan did not name. Required for coherence, not extra surface: those literals only apply on a checkout where the export is absent, and left at three they would make the new `length, 2` assertion fail on the LENGTH rather than on the CLAIM, which is the one thing the guard exists to prevent. Both MSR-02 cases were renamed out of the count for the same reason, and each recorded watched-failure transcript carries a one-line note that the case was renamed so its re-watch recipe still reproduces.
- `report.md`'s cross-model prose says the provider spend is reported and never added to the host total, but does NOT restate what `provider_spend` counts - the fold added on plan 2's fix pass prices only `command === 'review'` calls that were actually sent, leaving `detect-models`, `/cad-debug`'s `consult` and every pre-send refusal out. That rule was already stated in the `What `calls` COUNTS` bullet plan 2 added two bullets below, so it is not restated here; a reader who takes the new sentence as "all provider spend" is corrected within the same rule list.
