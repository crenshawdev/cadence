PLAN COMPLETE
Plan: .planning/phases/1/PLAN-3.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1: The prune-commit search and the milestone label binding | aa806a6c | `PRUNE_ARGV`, `PRUNED_SUMMARY`, `findPruneCommits`, `parsePruneRecords` in `lib/why-corpus.mjs`; 7 tests in `why-corpus.test.mjs`, 23/23 passing. Verify matched prediction on every arm except the merge one (see deviation): 25 prune commits including `72940906`/`a34b0c8a`/`8d9bbac9`, all 40-char, zero warnings; the control (same argv minus `--full-history`) returns 4 against 25; `72940906` binds `v3.5.9` with phases 1 and 2 and a single 40-char parent; a two-close temp repo returns both under their own labels newest-first and deep-equal across two runs. Static analysis: `detect-commands` `lint:null`, `typecheck` `npx tsc -p tsconfig.ci.json` exit 0. `lease-check --phase 1 --plan 3` `ok:true` (staged 2, declared 5). |
| 2: The reverse commit-to-phase map | e0ef35bf | `buildRecoveredIndex`, `mergeCommitIndexes`, `markPresence`, `recoveredDir` in `lib/why-corpus.mjs`; `resolveCommit` gained an ordered-tier walk (single-tier callers unchanged); 6 tests, 29/29 passing. Verify matched prediction exactly: the merged map resolves `73aa7bba` to milestone `v3.5.9`, phase 1, plan 1, task 1 with `Fence-aware heading scans in release-decision.mjs` verbatim and its `recovered.prune` starting `72940906`, cross-checked against `parseCommitRows` of `git show 72940906^:.planning/phases/1/SUMMARY.md`; never `phases/1`; `73aa7bbb...` unresolved; two builds deep-equal; a temp close naming `deadbeef...` returns that row with `present:false`. Timing measured: 79 `git show` calls, 108 ms for the whole merged build. Static analysis: `tsc` exit 0. Siblings `why`/`why-record`/`why-render`/`helper-census` 73/73. `lease-check` `ok:true` (staged 2, declared 5). |
| 3: Wire the recovered map into the chain | dcc411ad | `why.mjs` builds `mergeCommitIndexes(buildCommitIndex(...), buildRecoveredIndex(dir))` once per invocation; `brief()` carries the `recovered` anchor; `fieldPhase` in `lib/why-render.mjs` renders `<milestone> phase <N> (recovered from <parent8>:<tree>)`; `readPhaseRecords`/`readAdjudications` guard `dir.path === null` (task 4 replaces the guard with the git read). 3 tests, `why.test.mjs` 27/27. Verify matched prediction after two test-authoring fixes of my own (a missing `.stdout`, and `--top 20` needed to reach entry 11): the seam over `cadence-core/bin/lib/release-decision.mjs` renders `phase: v3.5.9 phase 1 (recovered from d3313bf7:.planning/phases/1)` and `plan task: plan 1, task 1 - Fence-aware heading scans in release-decision.mjs`, `warnings: []`, and `_archive-v3.5.9` appears nowhere; `00537356` still answers `_archive-v3.4.0/1` with no `recovered from`; two runs byte-identical. Static analysis: `tsc` exit 0. Siblings 68/68. `lease-check` `ok:true` (staged 4, declared 5). |
| 4: The recovered artifacts behind the remaining edges | 22899038 | `artifactReader` and `listRecordNames` in `lib/why-corpus.mjs` give `readPhaseRecords` and `readAdjudications` a second SOURCE and the same readers - `git show <parent>:<tree>/<name>` and `git ls-tree --name-only <parent>:<tree>` - with `repoDir` threaded from `why.mjs`; bounded by the per-directory memo `joinChain` already held. 4 tests, `why.test.mjs` 29/29, `why-corpus.test.mjs` 31/31. Verify matched prediction after one assertion fix of my own (`decisionsFor`'s `phase` arm returns `ids: []` by design): the seam over `cadence-core/bin/release-bump.mjs` renders `c7921b29` as `v3.5.8 phase 2 (recovered from 08583de5:.planning/phases/2)`, plan 2 task 3, `review: 1 surviving finding(s) cover this commit` with `claim` and `failure_scenario` compared against `JSON.parse` of `git show a34b0c8a^:.planning/phases/2/ADJUDICATION-risk_surface-plan-2.json`, D-09/D-08/D-02's own CONTEXT lines compared against `git show a34b0c8a^:.planning/phases/2/CONTEXT.md`, and two recovered `## Deviations` bullets; the temp-repo fixture resolves the decision edge and returns `{records: [], warnings: []}` for the review edge. Static analysis: `tsc` initially FAILED TS1016 (a required `repoDir` after an optional `planCell` in JSDoc), fixed to `[repoDir]`, re-run exit 0. `lease-check` `ok:true` (staged 4, declared 5). |
| 5: The named gap, and the phase number that is never guessed | 2382a9a7 | `touchedPaths`, `closeOver`, `archiveSections` in `lib/why-corpus.mjs` (rows through `parseArchiveRows`, never re-split); `gapLines`/`GAP`/`GAP_ROWS`/`GAP_PATHS` in `lib/why-render.mjs` replacing the unresolved arm's plan-1 placeholder; `commitScope` and a second bounded pass in `why.mjs`'s `joinChain` - ONE `git show --name-only` for the whole unresolved set (measured 24 ms over 144 commits). 6 tests added, 3 plan-1/plan-2 tests updated for the new `phase:` literal (all in this plan's lease). `why.test.mjs` 34/34, `why-corpus.test.mjs` 32/32. Verify matched prediction exactly: a temp-repo commit no record names renders its sha, date, subject, `NOT RESOLVED - ...`, `no close has pruned this commit yet`, `the commit message carries no conventional-commit scope`, `git records 1 path(s) touched` and `f.txt`, with no `phase <digit>`, no `phases/<digit>` and no digit at all on the phase line; a `feat(3-1)` subject renders `commit-message scope: 3-1 - read off the subject line as corroboration and explicitly NOT a resolved phase` while the phase line still reads the gap; an ARCHIVE.md fixture renders `the gap sits under the close at <sha8>, labelled v9.9.0` and its one residue row `bound to the label and to no commit (D-04)`; two runs byte-identical. FINAL GATE: `self-verify.mjs --root .` `{"ok":true,...,"problems":[]}`, `test.mjs` 2808 tests / 2807 pass / 0 fail / 1 skipped, `tsc` exit 0. `lease-check` `ok:true` (staged 5, declared 5). |

Deviations:
- [deviation] Task 1's Action asserts that "`--full-history` can surface merges", which is what makes its merge-refusal arm reachable; measured on git 2.55.0 on 2026-08-23 it cannot. `--diff-filter=D` selects NO merge commit under the default `--diff-merges=off`, because a merge produces no diff to filter. Proved on two built fixtures: an ordinary merge of a branch that deleted the summary (only the branch commit comes back) and an "evil" merge whose own tree performs the deletion (nothing comes back at all). `log.diffMerges=first-parent`, set both with `-c` and in the repo config, does not change it either. What I did: the guard STAYS - adding `--diff-merges=first-parent` to the same argv does make the evil merge come back with two parents, so the shape is producible - and the parse was split out as a pure `parsePruneRecords` export so the refusal is proved over stdout GIT wrote rather than over an invented string. The test asserts both halves: `findPruneCommits` over the merge fixture returns `[]` (the shipped argv does not surface it), and `parsePruneRecords` over the `--diff-merges=first-parent` stdout returns `parent: null` with the refusal naming two parents. The criterion's substance - "a synthetic merge-shaped prune commit returning a reported refusal rather than a parent-derived answer" - is met; the plan's reason for why it was reachable is what turned out wrong.
- [deviation] Task 1's Action states the label binding as a property every close has ("a prune appends `.planning/ARCHIVE.md`'s `## <label>` heading in the commit that removes the directories"). It holds for `72940906` as measured, and for exactly 7 of this repository's 25 closes - `.planning/ARCHIVE.md` did not exist before v3.5.3, so 18 closes bind no label at all. Nothing was changed to compensate: an absent label is reported as absent and the entry says `an unlabelled close (<sha8>)`, which is what `## Must be true when done` already allows ("where a milestone label is available, names it"). Recorded because a reader of task 3's "prints the milestone label beside the phase number" would otherwise expect it on every recovered entry, and it is on 7 closes' worth of them. A test pins the count at 7 so a future ARCHIVE.md backfill reddens rather than passes silently.

Open items:
- THE ENTRY CAP'S BYTE PROBLEM GREW, and the number is still a decision nobody has taken. Plan 2's report measured `/cad-why cadence-core/bin/lib/capture-file.mjs` at 10,137 B over 8 entries; with plan 3's gap block filled it is 11,211 B, `cadence-core/bin/lib/issue-decision.mjs` is 12,481 B over the default 10, and `cadence-core/bin/planning.mjs` is 15,637 B. All past `references/conventions.md`'s 10,000-byte line. D-13 is still met by its own terms (a default entry cap exists, and self-verify check 20 cannot see this site), and `DEFAULT_TOP` is pinned by `why-render.test.mjs`, which is outside this plan's lease. The gap block is itself capped - 10 paths and 3 archive rows, both with the remainder counted rather than dropped - so the growth is bounded per entry; the total is what is not.
- DECLINED a `git cat-file --batch` reader for the recovered summaries. The plan names `git show <prune>^:<path>` and that is 79 subprocesses on this repository; measured at 76 ms for all 79, against a whole-invocation cost of 376 ms on the worst path (`planning.mjs`, 144 commits), so the batch reader would have bought under a fifth of one run for a stdin protocol parser nothing else needs. Build it if the corpus grows an order of magnitude.
- `.planning/ARCHIVE.md`'s residue reaches only the UNRESOLVED arm. A resolved entry never prints it, because for a resolved entry the phase's own SUMMARY and CONTEXT are recoverable and are strictly better sources. This follows D-04 (ARCHIVE.md carries no commit-to-phase edge) but the plan's task 5 sentence "still prints what git history and `.planning/ARCHIVE.md` do carry" reads as if it were unconditional; it is scoped to the gap, which is the only arm that has nothing better.
- NOT CAUSED BY THIS PLAN, and it still bounds how much of the corpus any join reaches: plan 2's report recorded that the bare-path arm inherits git's default history simplification, so a milestone's merge commit stands in for the phase commits merged under it and a whole phase's record can be invisible. This plan's own `--full-history` measurement is the same defect one level up - 4 of 25 closes survive simplification on the prune search - which is evidence for deciding the chain query's flag rather than leaving it inherited. Not in this plan's `Verify:`; worth a decision.
- Carried from plan 1, still open: the human-verify half of plan 1 task 5 (`/cad-why cadence-core/bin/lib/seam-io.mjs` run in a session, compared to the seam's `text`). Its five stated-absent placeholder lines are now four - plan 3 replaced the `phase:` one with the named gap - so the comparand in plan 1's report is stale by that one line.
