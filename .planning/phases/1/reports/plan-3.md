PLAN COMPLETE
Plan: .planning/phases/1/PLAN-3.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1 - The cursor and phase-close refusals name their next step | cd4db472 | All 19 sites below line 800 hinted across `cmdStatus`, `cmdCursorGet`, `cmdCursorSet` and `cmdPhaseDone`; `cmdStatus`'s pinned `no-planning-dir` hint untouched. `unreadable-requirements` and `unknown-phase` hints prescribe inspection/repair, never "re-run and it will finish". Check's `planning.mjs` count 154 to 135, zero left under line 800. `cursor set --dir .planning` prints `bad-args` plus a non-empty hint. `test.mjs planning` 0 failures; `npx tsc -p tsconfig.ci.json` clean; no lint command exists (`detect-commands` reports `lint:null`). |
| 2 - The UAT and payload-reading refusals name their next step | 084610c8 | All 27 sites across `readJsonPayload`, `loadUat` and `cmdUat` hinted. Payload hints name the FILE to repair and the shape it must have, never a retyped value; UAT hints name the checklist item or the command that addresses it (`uat init`, `uat refresh`, re-record without `--source verifier`). Check's `planning.mjs` count 135 to 108, lowest remaining line 1517. `uat record --dir .planning` prints `bad-args` plus a non-empty hint. `test.mjs planning` 931 pass / 0 fail; tsc clean. |
| 3 - The audit, sizing and seeding refusals name their next step | 3f72b7a0 | All 22 sites across `cmdAudit`, `cmdCriteriaCoverage`, `cmdPlanSize`, `cmdCriteriaSize`, `cmdPlanOverlap`, `cmdCiteCount`, `cmdSeedReqs` and `cmdRecall` hinted. Document refusals name the heading or table to add and the command that writes it; `seed-reqs`'s spelling refusal names an accepted spelling (`--phase <value>`, and which shapes are and are not accepted) rather than explaining the round trip; the sizing and overlap hints name the flag or the PLAN files a planner changes. Check's `planning.mjs` count 108 to 86, lowest remaining line 2992. `seed-reqs --dir .planning` prints `bad-args` plus a non-empty hint. `test.mjs planning` 931 pass / 0 fail; tsc clean. |
| 4 - The lease, detection and trace refusals name their next step | be9af058 | All 25 sites across `cmdLeaseCheck`, `cmdDetectCommands`, `cmdDetectSurfaces`, `cmdTraceIgnore`, `cmdReads` and `cmdTrace` hinted, matching the register of `lease-check`'s two shipped hints. Every `trace append`/`close` argument refusal states that NOTHING was appended, so a corrected re-run writes exactly one event; the two record-state refusals that cannot be re-run cleanly (`no-staged-set` truncated, `cmdReads` `read-failed`) prescribe inspection and say the gate is not reporting a clean/empty result. Check's `planning.mjs` count 86 to 61, lowest remaining line 4645 (`cmdRiskCheckRun`, task 5). `lease-check --dir .planning` and `trace close --dir .planning` each print a non-empty hint. `test.mjs planning` 931 pass / 0 fail; tsc clean. |
| 5 - The risk, task-record, adjudication and deferred refusals name their next step | 07064619 | All 31 sites across `cmdRiskCheckRun`, `cmdRiskCheckStatus`, `cmdTaskRecord`, `fireIdentity`, `fireHome`, `cmdAdjudication`, `cmdDeferredRecord`, `cmdDeferredList` and `cmdDeferredCarry` hinted, matching the register of the six shipped hints beside them. Both `surfaces-unanswered` hints name the action that clears the blocking gate and state outright that no arm skips the scan; both `carry-dest-unusable` hints keep the existing BEFORE-`milestone-prune` ordering; `no-diff`/`no-range` say the refusal is not a clean-diff or touched-nothing verdict. Check's `planning.mjs` count 61 to 30, lowest remaining line 6744 (`cmdRenumber`, task 6). `risk-check status --dir .planning` and `deferred carry --dir .planning` each print a non-empty hint. `test.mjs planning` 931 pass / 0 fail; tsc clean. |
| 6 - The renumber, capture, debt and milestone-close refusals name their next step | feb0b1b7 | All 30 sites across `cmdRenumber`, `cmdCapture`, `cmdCaptureSections`, `cmdDebtHarvest`, `cmdMilestonePrune` and the dispatch block hinted. The renumber hints added are all pre-git-state arms, so no new text can reach the unreadable-git assertion; the two git arms and `milestone-prune`'s pinned `partial-prune` hint are untouched. The dispatch-door hint sends the user to the flag the detail names and explicitly states that a bare `--` separator does NOT protect a `--`-prefixed value, routing to the `--<name>-file` transport instead. `unreadable-capture`, `no-git` and both `write-failed` arms say outright that the walk/read did not happen, so the refusal is not a clean-report verdict. Check's `planning.mjs` count 30 to 0; `self-verify.mjs --root .` now `ok:true`, `problems: []`. `milestone-prune --dir .planning` and `recall --query --dir .planning` each print `bad-args` plus a non-empty hint; `nosuchsubcommand` still prints `usage` with NO hint. `test.mjs planning` 931 pass / 0 fail; tsc clean. |
| 7 - The count reaches zero and the tree is green | none (no change needed) | The check named no residue after task 6, so this task changed no file and has no commit. `self-verify.mjs --root .` prints `"ok":true` with `"problems":[]`. `refusalSites('.')` returns the two integers AC2 asks the SUMMARY to state: 243 in-scope sites, 0 hintless. Census by file: planning.mjs 180, review-provider.mjs 19, config.mjs 10, git-publish.mjs 8, release-bump.mjs 8, route.mjs 4, skim.mjs 4, why.mjs 4, and 1 each in git-branch, issue-check, land-cleanup, self-verify, weight, worktree-base. No exclusion was added and the rule was not narrowed. `test.mjs` (all groups) 2959 pass / 0 fail / 1 skipped, including `self-verify.test.mjs`'s live-tree assertion and the two live-tree cases PLAN-1's report recorded as expected-red until this plan landed. `weight.mjs` `ok:true`, no budgeted surface over its pin. `git diff --name-only main...HEAD` lists no path under `cadence-core/workflows/`, `cadence-core/references/` or `skills/cad-*-contract/`. AC4 proved mechanically: over every changed `cadence-core/bin/**.mjs`, the multiset of `reason:` string literals and of positional first arguments to `fail(...)` has an EMPTY removed-set against `main` - the only additions are two new fixture tokens inside PLAN-1's new `self-verify.test.mjs` block. `planning.test.mjs` needed no edit and was not touched. |
Deviations: [deviation] Task 6's Verify names `recall --query --dir .planning` as "the dispatch-level `argRefusal` path"; `--query` is not a declared row for `recall`, so that call never reaches the door refusal - it falls through to `cmdRecall`'s own `recall needs a query`. It does print `bad-args` plus a non-empty hint as the criterion requires, and the door path was verified separately at `renumber remove --n 2.1` and `recall foo --top`, both of which print the new dispatch hint. Nothing about the task's substance changed.
Open items: The `--fields-file` refusal wording in `cmdUat` and several `--<name>-file` transport hints repeat guidance `cadence-core/references/conventions.md` carries in prose; AC5 forbids touching `references/`, so the duplication is accepted rather than reconciled (the phase CONTEXT flags this same shape for the review-provider tokens).
