PLAN COMPLETE
Plan: .planning/phases/1/PLAN-3.md
Tasks: 2 of 2
| Task | Commit | Note |
|---|---|---|
| 1: The parallel path reaches the risk sequence | 8e2a8e4 | Sequence added inside step 5 of `references/execute-parallel.md`, after step 3's merges and step 4's worktree removal and before step 5's `diff` fire: `risk-check run` then `risk-check status`, both carrying `--phase --plan --base --head` with that plan's pre-merge/post-merge HEAD pair from step 3, plus the not-reported-done sentence and the PER-PLAN `plan-<k>` discriminator clause. Everything between the two calls POINTS at `workflows/execute.md`'s `execute_sequential` step; the file carries neither `inconclusive` nor `never stage it` (grep 0 for both). `grep -c risk-check` 0 -> 2. `weight-budgets.json` row re-pinned 4242 -> 5649 in the same commit; the un-repinned tree reported exactly one self-verify problem, `budget-overrun ... 5649B exceeds budget 4242B by 1407B`, and none of any other kind. |
| 2: The checks that hold both halves, with their watched FAIL | 75925d5 | Three checks appended to `prose-agreement.test.mjs` beside the RSK-01/RSK-02 block, under a header comment naming SHA `e4f95a3` and the observation watched there. Mutations run and reverted, each red on exactly its own check with the other two green: (1) execute.md's fire-rule sentence pasted into the reference -> REACHES check; (2) the not-reported-done sentence deleted -> ENFORCEMENT check; (3) the `status` call collapsed to the phase-wide arm with both command names left in place -> RANGE check; (4) the range rewritten to `{PHASE_START, HEAD}`, both names left in place -> RANGE check. (5) The file copied into `git worktree add --detach` at `e4f95a3` and run there: exit 1, 20 pass / 3 fail, with `grep -c risk-check` reproducing 0 for the reference against 2 for the workflow. Whole tree after: `node --test cadence-core/bin/*.test.mjs` exit 0 (2066 tests, 0 fail, 1 skipped) and `node cadence-core/bin/self-verify.mjs` exit 0. |
Deviations: none
Open items: `npx tsc -p tsconfig.ci.json` exits 1 on this tree with 3 errors, all in `cadence-core/bin/planning.mjs` (3717, 3732, 3792) and all pre-existing at HEAD `e4f95a3` - confirmed by stashing this plan's changes and re-running, byte-identical output. The cause is a stale `@type {Set<string>}` JSDoc on `const receipts = []` at 3716-3717, which PLAN-2 left when the value became an array of `{key, sha, base}` objects; the later `.push`/`.some` calls are correct and the runtime is unaffected. `planning.mjs` is outside this plan's `files:` lease, so it is reported rather than fixed.
