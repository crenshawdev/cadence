{
  "status": "gaps",
  "score": "7/9",
  "truths": [
    {
      "n": 1,
      "truth": "A response body whose URL userinfo `@` falls outside the 4096-byte sanitize window yields an excerpt carrying no byte of the planted secret, at #215's shape and at a high-magnitude case",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/bin/lib/redact-url.mjs:104-105 adds SCHEME_USERINFO_CUT / BARE_USERINFO_CUT applied after the terminated rules in redactUrl (:127-132); `node --test --test-name-pattern='EXP-02' cadence-core/bin/review-provider.test.mjs` -> pass 2 / fail 0 (both assert leaked(out)===0 and out.includes('cad:')===false, review-provider.test.mjs:1728-1751)"
    },
    {
      "n": 2,
      "truth": "The sanitizer change did not buy the fix with the truncation contract: both shipped fixtures still assert exactly MAX_HTTP_BODY_BYTES and the proxy excerpt still carries `504 Gateway Time-out`",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "cadence-core/bin/review-provider.test.mjs:1451 and :1469 are still `assert.equal(Buffer.byteLength(...), MAX_HTTP_BODY_BYTES)` (never relaxed to `<=`), :1453 still `assert.match(..., /504 Gateway Time-out/)`; the file passes inside the full-suite run below (2174 pass / 0 fail / 1 skipped)"
    },
    {
      "n": 3,
      "truth": "The end-of-input anchor does not read a port as userinfo, and does not widen redactUrl past URL position",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "node one-liner over lib/redact-url.mjs: redactUrl('https://example.com:8080/path') -> byte-identical as whole input AND mid-body ('see ... for more'); 'key=sk-live-abc123' and 'git@github.com:org/repo.git' both unchanged. `git diff ffeaa3f..c0b0d04 -- cadence-core/bin/redact-url.test.mjs` has 0 deleted lines, so no shipped negative fixture was weakened"
    },
    {
      "n": 4,
      "truth": "`renumber remove` against a phase dir under a repository whose git state cannot be read refuses on BOTH --dry-run and apply, with a reason that is not `uncommitted-work` and a hint that is not 'commit or discard', and phases/<N>/ survives",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "cadence-core/bin/planning.mjs:4277-4287 (uncommittedUnder returns {paths, unreadable: gitDirAbove(cwd)}), :4388-4392 the new `unreadable-git-state` fail() sitting above the dry-run return; `node --test --test-name-pattern='PHS-01' cadence-core/bin/planning.test.mjs` -> pass 3 / fail 0, the family asserting both arms ok:false, reason != uncommitted-work, no /commit or discard/, phases/3/PLAN.md present, ROADMAP.md byte-identical (planning.test.mjs:3922-3985)"
    },
    {
      "n": 5,
      "truth": "The permissive arm is intact: a tree with no repository at all still removes phases/<N>/ and returns ok:true, and the eleven bare-tree renumber fixtures still pass",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "planning.test.mjs:3980-3985 (bare renumberTree(): rBare.ok === true, phases/3 gone, ['1','2'] left) inside the passing PHS-01 run; gitDirAbove returns false with no .git ancestor (planning.mjs:4199-4209); full planning.test.mjs green inside the suite run"
    },
    {
      "n": 6,
      "truth": "The rmSync recursive fallback - the destructive act itself - carries its OWN gate independent of the pre-flight, covering .git at, above and inside the target",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "cadence-core/bin/planning.mjs:4526-4534 - `if (gitDirAbove(dir) || gitDirUnder(target)) throw new Error(...)` before `rmSync(target, {recursive: true})`; gitDirUnder at :4237-4246 probes `lstatSync(join(dir,'.git'))` per directory rather than comparing entry names (c0b0d04); source row `source: the renumber rm fallback's recursive delete is gated by the .git probe` (planning.test.mjs:4110-4141) asserts the guard is present and reddens if the bare `catch { rmSync(...) }` returns; behaviour observed by the two gate falsifiers (nested repo, GIT_DIR repo) passing"
    },
    {
      "n": 7,
      "truth": "Each requirement carries a falsifier whose WATCHED FAILING AT sha resolves to a real commit preceding the fix, and that check fails when re-run against that commit's tree",
      "status": "UNCERTAIN",
      "uat_item": 6,
      "evidence": "Headers present and shas real: review-provider.test.mjs:1651 `WATCHED FAILING AT ae73dd6` (measured 73 and 985 bytes leaked) and planning.test.mjs:3890 `WATCHED FAILING AT ae73dd6` (remove returning ok:true with {\"rm\":\"phases/3\"}); `git log --oneline -1 ae73dd6` resolves and `git merge-base --is-ancestor ae73dd6 HEAD` succeeds. The RE-WATCH itself was not executed here - it needs a detached worktree, which mutates repo state"
    },
    {
      "n": 8,
      "truth": "The whole suite, the typecheck and self-verify are green at HEAD",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "`node --test cadence-core/bin/*.test.mjs` -> tests 2175, pass 2174, fail 0, skipped 1, exit 0; `tsc -p tsconfig.ci.json` exit 0; `node cadence-core/bin/self-verify.mjs` exit 0 with {\"ok\":true,\"problems\":[]} across 24 checks"
    },
    {
      "n": 9,
      "truth": "The two falsifiers the risk-gate round added carry their watch record beside the test, in the form the repo's fifteen other WATCHED FAILING AT blocks use",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "SUMMARY.md:52 states the three fail-opens were closed 'with two more falsifiers watched failing at ffeaa3f', and fec446e's commit body repeats it, but `grep -rn ffeaa3f cadence-core/` returns nothing: `PHS-01: a repository rooted INSIDE phases/<at>...` (planning.test.mjs:4037) and `PHS-01: a GIT_DIR-selected repository...` (:4059) carry no WATCHED FAILING AT block, no observed failure output and no re-watch recipe. The claim is only in a commit message"
    }
  ],
  "passes": [
    { "k": 1, "name": "Cut-userinfo secret no longer leaks in excerpt (AC1)",
      "evidence": "redact-url.mjs:104-105,127-132 adds the two end-of-input alternatives after the terminated rules; `node --test --test-name-pattern='EXP-02' cadence-core/bin/review-provider.test.mjs` passes 2/2, both asserting zero bytes of the planted value and the excerpt still capped" },
    { "k": 2, "name": "Provider suite passes with truncation equalities intact",
      "evidence": "review-provider.test.mjs:1451 and :1469 still assert `=== MAX_HTTP_BODY_BYTES`, :1453 still matches /504 Gateway Time-out/; the file is green inside the 2174-pass suite run" },
    { "k": 3, "name": "Port is not read as userinfo at end-of-input",
      "evidence": "redactUrl('https://example.com:8080/path') byte-identical whole-input and mid-body; 'key=sk-live-abc123' and 'git@github.com:org/repo.git' unchanged; redact-url.test.mjs diff over ffeaa3f..c0b0d04 is additions only (0 deletions)" },
    { "k": 4, "name": "phase remove refuses an unreadable git state",
      "evidence": "planning.mjs:4277-4287 three-state uncommittedUnder, :4388-4392 the `unreadable-git-state` refusal above the dry-run return, :4526-4534 the independent gate on the rmSync fallback; the PHS-01 family passes 3/3 with phases/3 and ROADMAP.md intact on both arms" },
    { "k": 5, "name": "Non-repo remove still succeeds",
      "evidence": "planning.test.mjs:3980-3985 bare-tree arm returns ok:true with phases/3 removed; every bare-tree renumber fixture green in the full run" },
    { "k": 7, "name": "Full suite and self-verify exit 0",
      "evidence": "node --test cadence-core/bin/*.test.mjs -> 2174 pass / 0 fail / 1 skipped, exit 0; tsc -p tsconfig.ci.json exit 0; self-verify.mjs exit 0, {\"ok\":true,\"problems\":[]}" }
  ],
  "gaps": [
    { "name": "The risk-gate round's two falsifiers have no in-file watch record",
      "reason": "behavior wrong - the watch is claimed in prose and in a commit message but is not carried where the convention (and every other falsifier in this tree) puts it, so a future reader cannot re-run it or see what it observed failing",
      "evidence": "cadence-core/bin/planning.test.mjs:4037 (`PHS-01: a repository rooted INSIDE phases/<at> is not deleted out from under itself`) and :4059 (`PHS-01: a GIT_DIR-selected repository counts as present when its state cannot be read`) begin straight at `test(` with only an explanatory comment - no WATCHED FAILING AT header, no observed output, no re-watch recipe. `grep -rn ffeaa3f cadence-core/` returns nothing, while .planning/phases/1/SUMMARY.md:52 and commit fec446e both assert 'two more falsifiers watched failing at ffeaa3f'. Contrast the two that do comply: review-provider.test.mjs:1651 and planning.test.mjs:3890",
      "severity": "minor",
      "missing": "Add a WATCHED FAILING AT ffeaa3f block above planning.test.mjs:4037 covering both cases - the observed failure output at that sha, what it proves, and the re-watch recipe (worktree, the one file to copy, the --test-name-pattern scope) - or, if the watch was never actually run, run it and record what it showed rather than leaving SUMMARY.md:52 asserting it" }
  ],
  "human_checks": [
    { "name": "Re-watch both falsifiers at ae73dd6: `git worktree add --detach <tmp> ae73dd6`, copy cadence-core/bin/review-provider.test.mjs AND cadence-core/bin/review-provider.mjs into <tmp>/cadence-core/bin/, run `node --test --test-name-pattern='EXP-02' cadence-core/bin/review-provider.test.mjs` there; then in a second worktree copy only cadence-core/bin/planning.test.mjs and run `node --test --test-name-pattern='PHS-01' cadence-core/bin/planning.test.mjs`; remove both worktrees",
      "expected": "EXP-02 fails 2/2 naming ~73 and ~985 bytes of SUPERSECRET in the excerpt; PHS-01 fails showing the remove returning ok:true with {\"rm\":\"phases/3\"} and phases/3/PLAN.md gone. Both exit 1",
      "why_human": "Not a limit of inspection - the headers, shas and ancestry all check out from here. The re-watch needs `git worktree add`/`remove` against the live repository, which mutates repo state my read-only verification mandate forbids; give me leave to create and remove a scratch worktree and I can execute it instead" }
  ]
}
