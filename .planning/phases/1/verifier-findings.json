{
  "status": "needs_human",
  "score": "7/9",
  "truths": [
    {
      "n": 1,
      "truth": "A ported `git.forge_host` round-trips through the config write face byte for byte",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "Scratch planning root: `node cadence-core/bin/config.mjs set --file <scratch>/.planning/config.json git.forge_host=forge.example:3001` -> {\"ok\":true,...\"changed\":[{\"key\":\"git.forge_host\",\"value\":\"forge.example:3001\"}]}; `get` -> {\"values\":{\"git.forge_host\":\"forge.example:3001\"}}; file on disk holds the same bytes. Grammar: cadence-core/bin/lib/forge-decision.mjs:277 splitForgeHost, registered as `forge_host` at cadence-core/bin/config.mjs:116-128 and claimed by config.schema.json:57."
    },
    {
      "n": 2,
      "truth": "A malformed typed host or slug is refused at the write face, naming the key and what is wrong, with the file unchanged",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "Scratch run: `forge.example:0443`, `:70000`, `:0`, `-forge.example`, `forge example.com`, `forge.example.com/x`, `git@forge.example.com`, `git.forge_repo=onlyowner` and `git.forge_repo=-bad/name` each answer {\"ok\":false,\"reason\":\"invalid\",\"detail\":[{\"key\":...,\"error\":<grammar sentence>,\"value\":...}]}; md5 of the target file identical before and after all nine refusals. `null` and `group/sub/repo` still write. checkValue = checkType + checkGrammar at cadence-core/bin/config.mjs:156-159; an unregistered marker errors at :147, covered by config.test.mjs:1052."
    },
    {
      "n": 3,
      "truth": "The persisted port reaches the tea login match as a veto, and a portless host resolves exactly as before",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "Direct probe of cadence-core/bin/lib/issue-decision.mjs teaLoginNameForHost over two logins on one hostname (urls :3000 and :3001): `forge.example.com:3001`->three-oh-one, `:3000`->three-thousand, portless->three-thousand (first in list order, unchanged), `:9999`->null, refused host->null, `Forge.Example.COM:3001`->three-oh-one; `:443` matches a login whose url spells no port. Split at issue-decision.mjs:560-570, veto at loginNamesHost :431. Named arms issue-decision.test.mjs:216,221,228,232 pass."
    },
    {
      "n": 4,
      "truth": "`forge.mjs create` refuses a --remote-url whose port the configured instance does not serve, naming both ports, and spawns nothing",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "End-to-end CLI in a scratch root whose .planning/config.json holds git.forge_host=forge.example.com:3001, run with PATH=/nonexistent so any arm reaching the PATH check is distinguishable: `https://forge.example.com/o/r.git` -> refusal naming 3001 and 443; `git@forge.example.com:o/r.git` -> refusal naming 3001 and 22; `https://forge.example.com:3001/...`, `ssh://git@forge.example.com:2222/...` and `https://ssh.example.com/...` all pass the check and reach the PATH check instead. Config value traced config file -> mergeLayers -> splitForgeHost (forge.mjs:435-436) -> refusal at :467-473, which precedes the PATH check at :479 and every spawn. forge.test.mjs:858,879,901,920,939,954,977 assert empty $CAD_ARGV_LOG and empty spawn marker on the refusals and the exact `git remote add origin <url>` argv on the accepts; envelope carries `warnings` (observed `\"warnings\":[]`)."
    },
    {
      "n": 5,
      "truth": "The gitlab arm refuses a --remote-url by naming the --remoteName origin conflict, keyed on the table rather than on the provider string, and spawns nothing",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "End-to-end: `create --provider gitlab --repo o/r --confirmed --remote-url https://gitlab.com/o/r.git` -> {\"ok\":false,\"reason\":\"gitlab's create wires origin itself, through the --remoteName origin pinned in its own create argv...\"}, exit 1, fired before the config read and before the PATH check (the same invocation without the flag reaches the PATH refusal instead). Branch keyed on `row.wiresRemote` at cadence-core/bin/forge.mjs:412-424, never on the string. forge.test.mjs:999 and the untouched :837 arm both pass."
    },
    {
      "n": 6,
      "truth": "The port grammar is stated on the catalog row and both setup workflows ask for it and carry it whole into the --remote-url",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "cadence-core/references/config-catalog.md:53 states `hostname`, or `hostname:port` ... `1`-`65535` with no leading zero, shape-checked when you set it, Type column still str|null; :52 states the slug is shape-checked at the write face. config-reach.md:149 adds `bin/forge.mjs` `create` as a reader. new-project.md:116-118 and adopt.md:103-105 both ask for `host:port` and say it is the browser port, never SSH; new-project.md:188-190 says the confirmed value goes in WHOLE, port and all. self-verify's budgets and config-reach checks pass, so both weight-budgets re-pins are real."
    },
    {
      "n": 7,
      "truth": "Suite green, self-verify clean, and both changed config keys registered in schema and catalog",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "`node cadence-core/bin/test.mjs`: tests 3458, pass 3457, fail 0, skipped 1. `node cadence-core/bin/self-verify.mjs`: {\"ok\":true,...\"problems\":[]} across 30 checks. `config.mjs validate --file .planning/config.json` -> ok:true, checked 10, no errors. No new keys; the two changed keys carry `grammar` markers in config.schema.json:56-57 with `purpose` stating the port, and both appear in config-catalog.md:52-53. Debt-marker scan over the nine touched files found no TODO/FIXME/XXX/HACK/unimplemented on the goal path (one unrelated prose use of the word placeholder in config.schema.json:61). All four mapped requirements FRG-03..FRG-06 are claimed by plan 1 and covered by truths 1-6; none orphaned."
    },
    {
      "n": 8,
      "truth": "A ported SSH origin is reachable unattended - `git ls-remote --exit-code ssh://git@ssh.jcrenshaw.dev:2222/... HEAD` exits 0 under BatchMode",
      "status": "UNCERTAIN",
      "uat_item": 8,
      "evidence": "Not exercised: this pass is barred from touching the network, and the check needs an SSH key for git@ssh.jcrenshaw.dev plus the operator's own known_hosts entry. Nothing in the tree can stand in for the exit status."
    },
    {
      "n": 9,
      "truth": "An origin `create` wires against a live ported Forgejo actually answers",
      "status": "UNCERTAIN",
      "uat_item": 9,
      "evidence": "No live Forgejo on a non-default port exists in this environment. The machine-checkable half is closed by truth 4 (the wired URL is now proved to name the configured port); that the resulting endpoint answers is only observable against a real instance."
    }
  ],
  "passes": [
    { "k": 1, "name": "Ported forge_host round-trips through config", "evidence": "set/get round-trip in a scratch planning root returns forge.example:3001 byte for byte; grammar at forge-decision.mjs:277 wired through config.mjs's frozen GRAMMARS registry and config.schema.json:57" },
    { "k": 2, "name": "A malformed typed host or slug is refused at the write face", "evidence": "nine malformed values (space, leading -, :0443, :70000, :0, trailing /path, git@host, one-segment slug, leading-dash slug) each refused with reason:invalid naming the key and the grammar sentence; target file md5 unchanged; null and group/sub/repo still write" },
    { "k": 3, "name": "The port picks the right tea login", "evidence": "teaLoginNameForHost over two logins on one hostname: :3001 picks the 3001 login, :3000 the 3000 one, portless still returns the first in list order, :9999 and a refused host return null" },
    { "k": 4, "name": "create refuses a --remote-url whose port disagrees", "evidence": "live CLI against a scratch root with git.forge_host=forge.example.com:3001 refuses https://forge.example.com/o/r.git naming 3001 and 443 and git@forge.example.com:o/r.git naming 3001 and 22, ahead of the PATH check and any spawn; the three accept shapes pass through; forge.test.mjs arms assert an empty argv log and empty spawn marker" },
    { "k": 5, "name": "The gitlab arm refuses a --remote-url", "evidence": "live CLI: refusal names the --remoteName origin pinned in the create argv, exit 1, fired at the row.wiresRemote branch (forge.mjs:412) before the config read and any spawn" },
    { "k": 6, "name": "The port grammar is stated on the docs surfaces", "evidence": "config-catalog.md:53 states hostname:port with the 1-65535 no-leading-zero rule; config-reach.md:149 adds create as a reader; both workflows ask for host:port as the browser port and new-project.md:188-190 carries it whole into --remote-url" },
    { "k": 7, "name": "Suite green, self-verify clean, keys registered", "evidence": "test.mjs 3457 pass / 0 fail / 1 skipped; self-verify ok:true with empty problems over 30 checks; config.mjs validate on this repo's own layer ok:true; both changed keys present in config.schema.json and config-catalog.md" }
  ],
  "gaps": [
    {
      "name": "forge.mjs's usage block still calls --remote-url merely \"unread\" on gitlab",
      "reason": "behavior wrong - the file's own record is stale: cadence-core/bin/forge.mjs:114-116 reads \"--remote-url is REQUIRED on the providers whose create argv wires no git remote (forgejo and github), and unread on the one that wires its own (gitlab)\", but as of this phase passing it on gitlab is REFUSED, not ignored. The header's other two stale sentences the plan named (create reads no config; --dir's description) were both corrected.",
      "evidence": "cadence-core/bin/forge.mjs:114-116 usage text vs the refusal at cadence-core/bin/forge.mjs:412-424",
      "severity": "cosmetic",
      "missing": "say in the usage block that a --remote-url passed on a row whose create wires its own remote is refused, rather than unread"
    }
  ],
  "human_checks": [
    {
      "name": "Run GIT_SSH_COMMAND=\"ssh -o BatchMode=yes -o StrictHostKeyChecking=yes\" git ls-remote --exit-code ssh://git@ssh.jcrenshaw.dev:2222/crenshawdev/cadence-archived.git HEAD",
      "expected": "exit 0, with no TTY and no host-key prompt",
      "why_human": "out-of-reach resource, not an unexercised code path: it needs network egress this verification pass is barred from and an SSH key plus known_hosts entry for git@ssh.jcrenshaw.dev that only the operator's machine holds"
    },
    {
      "name": "Against a real Forgejo on a non-default port, let forge.mjs create wire the origin, then run git ls-remote --exit-code <that origin> HEAD",
      "expected": "the create succeeds, origin carries the configured port, and ls-remote exits 0",
      "why_human": "out-of-reach resource: no live ported Forgejo instance exists in this environment, and whether the wired endpoint ANSWERS is only observable against a running instance. Code inspection settles only that the URL names the configured port, which truth 4 already proves"
    }
  ]
}
