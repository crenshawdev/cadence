{
  "status": "gaps",
  "score": "7/8",
  "truths": [
    {
      "n": 1,
      "truth": "The fix_commit VALUE check runs wherever the key is SET, on every ruling, and its refusal names both the field and the ruling",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/bin/lib/adjudication-record.mjs:429-443 - the check now sits above the `refuted` block, gated only on `ruling.fix_commit !== undefined`, and refuses with `${rat} is ${ruling.ruling} and carries no usable fix_commit`; the old copy is gone from the `ruling === 'survived'` arm (:465-478 now holds only the PRESENCE requirement). `node --test cadence-core/bin/adjudication-record.test.mjs` = 52 pass / 0 fail, including the three pre-existing /no usable fix_commit/ arms plus 'RSK-08: an unusable fix commit on a DOWNGRADED or REFUTED ruling is REFUSED', 'RSK-08: a WELL-FORMED fix commit on a downgraded or refuted ruling is still accepted' and 'RSK-08: the SURVIVED arm is refused by that one same check, naming its ruling'."
    },
    {
      "n": 2,
      "truth": "fix_commit '' and null on a downgraded entry are REFUSED rather than accepted with the key silently absent from the stored entry",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "Presence is read as `!== undefined` at cadence-core/bin/lib/adjudication-record.mjs:438, and the entry-emit spread at :512 was changed from truthiness to `ruling.fix_commit !== undefined`, closing the silent drop site. The test arm at cadence-core/bin/adjudication-record.test.mjs:395-419 drives `['not-a-sha', '', null]` x `['downgraded','refuted']` and asserts ok:false plus `res.entries` empty; green in the 52/52 run."
    },
    {
      "n": 3,
      "truth": "trace append refuses a settle receipt that carries no reason at all when the record it settles holds a survived blocker or high marked overridden: true, and nothing is appended - the receipt cannot decline the check",
      "status": "FAILED",
      "uat_item": 3,
      "evidence": "The guard exists and works on every DOCUMENTED receipt shape (cadence-core/bin/planning/trace.mjs:1315-1349, called at :876-879 before appendEvent; 5 new arms in planning-adjudication.test.mjs all green, 27/27). But :1317 - `if (!['survivors','downgraded','refuted'].some((k) => k in fire.settled)) return pass;` - skips the whole check when the receipt carries NO settled figure. Measured against the shipped tree: `trace append --phase 1 --family outcome --event gate_pass --trigger risk_surface --plan 1 --base <base> --sha <head>` with no --survivors/--downgraded/--refuted, over a record holding a survived blocker marked overridden:true, returns ok:true and IS appended, and `risk-check status` then reports the plan `recorded`. `recountReceipt` does not catch it either (trace.mjs:1225 needs all three keys), and risk-check.mjs:710-721 requires a reason only on the `override` event, so a figureless `gate_pass` settles the range unchallenged."
    },
    {
      "n": 4,
      "truth": "The ruling vocabulary is still exactly survived | downgraded | refuted, and overridden: true on a downgraded/refuted entry or on a survived medium/low is still accepted with no receipt demanded",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "RULINGS untouched by the range (git diff 8bf8971e..776c67ef shows no edit to the RULINGS/RAISED_SEVERITIES declarations). cadence-core/bin/adjudication-record.test.mjs:294-302 ('the marker is not restricted to survived, nor to a severity', a downgraded+overridden payload returning ok:true) and :311-317 ('the marker added no fourth RULING', asserting deepEqual [...RULINGS] === ['survived','downgraded','refuted']) both green, unmodified by the phase diff. The new arm 'RSK-08: a record holding NO cleared halt takes a reasonless receipt unchanged' proves a survived medium still settles with no reason."
    },
    {
      "n": 5,
      "truth": "The unfixed-halting-survivor test exists in exactly ONE place, exported from lib/filing-decision.mjs, and the entries face and the payload face answer identically",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "cadence-core/bin/lib/filing-decision.mjs:113-126 `unfixedFromEntries(entries)` is the only statement of the three-field test (:120-121); `unfixedFindings` at :147-151 now reads its set off it rather than restating the filter. A repo-wide grep for `overridden !== true` / `overridden === true` / `HALTING_SEVERITIES` across cadence-core/bin non-test .mjs returns only filing-decision.mjs:120-121 for the classifier - adjudication-record.mjs:472 is the composition-time PRESENCE rule, a different question. adjudication-record.test.mjs:459-495 drives one fixture through both faces and asserts deepEqual of the sets plus the overridden blocker named as the lone haltingSurvivor; filing-decision.test.mjs (40/40) and issue-filing.test.mjs (34/34) green with no edit to either file."
    },
    {
      "n": 6,
      "truth": "Both refusals reproduce end to end over a mixed-ruling fixture, asserted against the STORED record bytes rather than the envelopes",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "cadence-core/bin/planning-adjudication.test.mjs:574-680, 'RSK-08: both refusals land where they are stated, over a mixed-ruling fixture end to end': risk-check run ok:true -> adjudication with fix_commit:'not-a-sha' on the DOWNGRADED entry ok:false with detail matching /fix_commit/ and /downgraded/ and no record written -> corrected payload ok:true with counts {raised:3,survived:1,downgraded:1,refuted:1} -> the written ADJUDICATION-*.json read back and its entries[] asserted -> reasonless gate_pass ok:false with trace.jsonl line count unchanged -> override --detail-file ok:true -> risk-check status ok:true, state 'recorded'. Passing in the 27/27 run (264ms)."
    },
    {
      "n": 7,
      "truth": "The full suite exits 0 with 0 failures and self-verify reports ok:true with problems: []",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "`node cadence-core/bin/test.mjs` -> tests 3563, pass 3562, fail 0, skipped 1, exit 0. `node cadence-core/bin/self-verify.mjs` -> {\"ok\":true, ... ,\"problems\":[]} across all 30 checks including `budgets`."
    },
    {
      "n": 8,
      "truth": "Both references state the two rules and their weight budgets were re-pinned to the new measured sizes",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "cadence-core/references/review-record.md:106-113 states the value is checked wherever the key is set on ANY ruling and that blank/null/junk is refused rather than dropped; cadence-core/references/triage-gate.md:112-121 states the accepted override settle shape (--detail-file beside the three figures) and that a reasonless receipt is refused with nothing appended. weight-budgets.json now reads 23677 / 7790 and `wc -c` on the two files returns exactly 23677 and 7790. prose-agreement.test.mjs 59/59 and citation-census.test.mjs 5/5 green, so the fenced-command censuses and the re-pinned EXECUTE-22 row (245-247) still resolve."
    }
  ],
  "passes": [
    { "k": 1, "name": "A malformed fix_commit is refused on downgraded and refuted", "evidence": "adjudication-record.mjs:429-443 holds the hoisted check outside the survived arm, refusing with `... is <ruling> and carries no usable fix_commit`; the survived arm at :465-478 keeps only the PRESENCE requirement. adjudication-record.test.mjs 52 pass / 0 fail with the three pre-existing /no usable fix_commit/ arms and three new RSK-08 arms." },
    { "k": 2, "name": "An empty or null fix_commit is refused, not silently dropped", "evidence": "Presence read as `!== undefined` at adjudication-record.mjs:438 and the entry-emit spread at :512 changed off truthiness; adjudication-record.test.mjs:395-419 drives '' and null on both downgraded and refuted and asserts ok:false with entries []." },
    { "k": 4, "name": "The ruling vocabulary and the overridden grammar are unchanged", "evidence": "No edit to RULINGS in the phase diff; adjudication-record.test.mjs:294-302 and :311-317 green unmodified, and the new arm 'a record holding NO cleared halt takes a reasonless receipt unchanged' proves a survived medium still settles with no reason." },
    { "k": 5, "name": "One predicate answers the unfixed-halting-survivor question on both faces", "evidence": "filing-decision.mjs:113-126 is the sole statement of the three-field test (grep for `overridden !== true` across non-test bin/*.mjs returns only :121); unfixedFindings:147-151 is the wrapper. adjudication-record.test.mjs:459-495 asserts the two faces deep-equal over one fixture. filing-decision.test.mjs 40/40 and issue-filing.test.mjs 34/34 green, both files untouched." },
    { "k": 6, "name": "Both refusals reproduce end to end over a mixed-ruling fixture", "evidence": "planning-adjudication.test.mjs:574-680 walks risk-check run -> adjudication (refused, then corrected) -> reasonless receipt (refused, nothing appended) -> override --detail-file (accepted) -> risk-check status 'recorded', asserting the stored ADJUDICATION-*.json entries. 27/27 green." },
    { "k": 7, "name": "The full suite and self-verify are green", "evidence": "node cadence-core/bin/test.mjs -> 3563 tests, 3562 pass, 0 fail, 1 skipped, exit 0. node cadence-core/bin/self-verify.mjs -> ok:true, problems: []." }
  ],
  "gaps": [
    {
      "k": 3,
      "name": "A reasonless receipt over a cleared halt is refused",
      "reason": "behavior wrong - the guard is declinable. `overrideAccounted` only runs when the receipt carries at least one of --survivors/--downgraded/--refuted, so a settle receipt carrying NONE of the three skips it entirely and still settles the range. The phase deliberately pinned a test that dropping ONE figure cannot discharge the marker; dropping all three does discharge it, which is strictly easier and leaves the goal's second half ('overridden: true no longer discharges the module's strongest refusal on an unverifiable self-assertion') false as stated.",
      "evidence": "cadence-core/bin/planning/trace.mjs:1317 - `if (!['survivors','downgraded','refuted'].some((k) => k in fire.settled)) return pass;`. Measured on the shipped tree with the same fixture shape the phase's own tests use (scratch repo, risk-check run, adjudication of a survived blocker with overridden:true, record written at phases/1/ADJUDICATION-risk_surface-plan-1.json): `planning.mjs trace append --phase 1 --family outcome --event gate_pass --trigger risk_surface --plan 1 --base <base> --sha <head>` with no settled figures returned ok:true and WAS appended, and `planning.mjs risk-check status --phase 1 --plan 1 --base <base> --head <head>` then returned ok:true with plans[0].state === 'recorded'. Nothing else closes it: recountReceipt (trace.mjs:1225) needs all three keys present, and planning/risk-check.mjs:710-721 demands a reason only on the `override` event name, so a figureless `gate_pass` is an accepted FIRE_RECEIPT. Note the guard IS correct on every documented shape - references/triage-gate.md:98 and :109 both spell all three figures - so the hole is the undocumented-but-accepted figureless receipt.",
      "severity": "major",
      "missing": "Make the check unskippable for the receipts that settle a range. Either run overrideAccounted on every `outcome` receipt in FIRE_RECEIPTS other than the two that settle nothing (`rearm`, `deferral`) instead of keying on a settled figure being present, or refuse a `gate_pass`/`override` that carries no settled figure at all, so the guard cannot be declined by omitting flags. Add an arm beside 'RSK-08: the guard cannot be discharged by dropping one settled figure' in planning-adjudication.test.mjs proving a receipt carrying ZERO figures over the same record is refused and appends nothing."
    }
  ],
  "human_checks": []
}
