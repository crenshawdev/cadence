{
  "status": "needs_human",
  "score": "7/8",
  "truths": [
    {
      "n": 1,
      "truth": "The refusal-hint rule and its exclusion register exist as a real module, and the register is READ as a parameter rather than compiled in",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "cadence-core/bin/lib/refusal-hints.mjs, 515 lines, exports CODES, REGISTER, refusalSites(root, register), refusalHintIssues(root, register). REGISTER carries 2 token rows (usage, internal) and 7 file rows (git-guard.mjs, lib/bulk-output.mjs, lib/text-transport.mjs, lib/release-decision.mjs, lib/why-query.mjs, lib/read-trace.mjs, lib/trace.mjs); 0 rows lack a one-line reason (checked programmatically). Injection proved live, not just by its own tests: refusalSites('.') = 243 with the shipped register, 150 with a substitute adding {token:'bad-args'}, 63 with a substitute adding {file:'planning.mjs'}. node --test cadence-core/bin/refusal-hints.test.mjs = 23 pass / 0 fail, including the two AC6 substitute-register tests at refusal-hints.test.mjs:115 and :136. No stub returns: the `return null` sites (279, 332, 342, 431, 434) are parser no-match sentinels and guarded-read degradation, all covered by named fixture tests."
    },
    {
      "n": 2,
      "truth": "The rule is WIRED into self-verify as check 22 and its output reaches problems[] through the CLI, not just through a unit test",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "cadence-core/bin/self-verify.mjs imports refusalHintIssues (line ~223) and pushes its issues inside run() (the `// 22. refusal hints:` block before `return problems`). Live CLI: `node cadence-core/bin/self-verify.mjs --root .` prints `checked` ending in `..., scratch-path, refusal-hints`. Two wiring tests added at self-verify.test.mjs:2437+ assert a hintless fixture reaches problems as kind hintless-refusal with file and `line 4: no-input`, and that `checked` names refusal-hints."
    },
    {
      "n": 3,
      "truth": "The check fails against the tree as it stood before the sweep, and every entry names a file path and the reason token",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "Ran the tree at 3425165e out of `git archive` into a scratch dir (no repo mutation): `node <pre>/cadence-core/bin/self-verify.mjs --root <pre>` exits 1 with ok:false and 215 problems, ALL of kind hintless-refusal. 0 of the 215 lack a `file`; each detail is `line N: <token>`, e.g. {file:'cadence-core/bin/config.mjs', detail:'line 131: read'}. The one documented non-token spelling appears as `line 133: (no reason key)`, which the module header sanctions for an ok:false site carrying no reason key."
    },
    {
      "n": 4,
      "truth": "The finished tree is green and SUMMARY states the two integers AC2 asks for, the second being 0",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "`node cadence-core/bin/self-verify.mjs --root .` returns {\"ok\":true,...,\"problems\":[]}. refusalSites('/code/cadence') = 243 sites, 0 with hinted false; refusalHintIssues('/code/cadence') = 0 issues. SUMMARY.md line 10 states '243 in-scope refusal sites, 0 hintless' - both integers, and both match the module's own measurement re-run independently here."
    },
    {
      "n": 5,
      "truth": "config, route and review-provider refusals print an envelope whose hint is a non-empty string",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "`config.mjs set nosuchkey=1` -> reason invalid + hint 'each error names the pair it refused - run `config.mjs keys` ...'. `route.mjs resolve --role nosuchrole` -> reason unknown-role + hint 'pass --role as one of the roles the detail lists, spelled exactly, and re-run'. `review-provider.mjs` with no subcommand -> reason bad-command + hint 'name one of those three as the FIRST argument, before any flag, and re-run'. All three wrappers widened to (reason, detail, hint) with the conditional spread (route.mjs:146-149 shown in diff). Computed-hint helpers cannot emit undefined: gateHint (git-publish.mjs:165) and versionHint (release-bump.mjs:252) both carry a non-empty `||` fallback."
    },
    {
      "n": 6,
      "truth": "No reason token literal and no positional fail() first argument was edited, and the suite is green",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "Independent multiset diff over every cadence-core/bin/**.mjs at 44fead5b vs HEAD, counting `reason: '<lit>'` values and `fail('<lit>'` first arguments: REMOVED set is EMPTY (0) and no count DECREASED, so no literal was renamed or dropped. The only deltas are additions - 15 new keys, 12 of them the new register's prose `reason:` rows in lib/refusal-hints.mjs and fixture tokens in refusal-hints.test.mjs - plus three increases (internal 28->29, fail usage 9->10, fail write-failed 3->5). Full suite run once: `node cadence-core/bin/test.mjs` = 2960 tests, 2959 pass, 0 fail, 1 skipped."
    },
    {
      "n": 7,
      "truth": "No weight budget was crossed and no model-facing prose surface was touched",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "`node cadence-core/bin/weight.mjs` returns ok:true over 111 surfaces with none over its pin; self-verify's `budgets` check also passes on the finished tree. `git diff --name-only 44fead5b..HEAD` lists 22 paths: 17 under cadence-core/bin/ and 5 planning documents (.planning/STATE.md, phases/1/SUMMARY.md, phases/1/reports/plan-{1,2,3}.md). Zero paths under cadence-core/workflows/, cadence-core/references/ or skills/cad-*-contract/."
    },
    {
      "n": 8,
      "truth": "The hints read as an instruction to the person at the terminal rather than a restatement of the detail or an explanation of the seam's internals",
      "status": "UNCERTAIN",
      "uat_item": 7,
      "evidence": "Sampled ~35 hints across planning.mjs (164 fail sites now pass a third argument), config.mjs, review-provider.mjs, route.mjs, skim.mjs, why.mjs, git-publish.mjs and release-bump.mjs. Every sampled hint opens with an imperative naming an action ('point --dir at ...', 'pass --role as one of the roles the detail lists ...', 'let the run holding the lock finish and re-run ...', 'rewrite each line listed in `issues` as ...'), and several state explicitly that nothing was written so a corrected re-run is safe. No sampled hint merely repeats the detail. The check itself only asserts the hint KEY is present - it does not test non-emptiness - so this quality claim rests on reading, not on an assertion. The single empty hint literal in the tree (planning.mjs:5800, `hint: ''`) sits on an ok:true helper return, not on an emitted refusal, so no shipped refusal carries an empty hint."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "Pre-sweep tree reports hintless refusals",
      "evidence": "Tree at 3425165e extracted via git archive to a scratch dir and self-verified there: exit 1, ok:false, 215 problems, all kind hintless-refusal, every one carrying a `file` and a detail of the form `line N: <token>` (e.g. cadence-core/bin/config.mjs, 'line 131: read')."
    },
    {
      "k": 2,
      "name": "Finished tree is green and SUMMARY states both integers",
      "evidence": "self-verify --root . returns ok:true with problems: []; refusalSites('.') independently re-measured at 243 sites / 0 hintless, the same pair SUMMARY.md states."
    },
    {
      "k": 3,
      "name": "config, route and review-provider refusals carry a hint",
      "evidence": "Three live runs: config.mjs set nosuchkey=1 (invalid), route.mjs resolve --role nosuchrole (unknown-role), review-provider.mjs with no subcommand (bad-command) each print a non-empty hint alongside an unchanged reason and detail."
    },
    {
      "k": 4,
      "name": "No reason token or fail() first argument changed",
      "evidence": "Independent multiset diff of reason literals and fail() first arguments over 44fead5b..HEAD: empty REMOVED set, no decreased count, additions only. node cadence-core/bin/test.mjs = 2959 pass / 0 fail / 1 skipped over 2960 tests."
    },
    {
      "k": 5,
      "name": "No weight budget crossed and no model-facing prose touched",
      "evidence": "weight.mjs ok:true over 111 surfaces, none over pin; the 22 changed paths are 17 files under cadence-core/bin/ plus 5 planning documents, none under workflows/, references/ or skills/cad-*-contract/."
    },
    {
      "k": 6,
      "name": "The exclusion register is named, reasoned and injectable",
      "evidence": "REGISTER carries 2 token rows and 7 file rows covering exactly the exclusions CONTEXT names (usage, internal, git-guard.mjs, the three static-registry libs, the three sub-envelope-return libs), each with a one-line reason citing its D-number; 0 rows lack a reason. Injectability proved live outside the test file: substituting a register that adds `bad-args` moves the site count 243 -> 150, adding `planning.mjs` moves it 243 -> 63. refusal-hints.test.mjs 23/23 pass."
    }
  ],
  "gaps": [],
  "human_checks": [
    {
      "name": "Read 8-10 of the new hints in cadence-core/bin/planning.mjs and cadence-core/bin/review-provider.mjs and judge whether each names an action in YOUR vocabulary",
      "expected": "Each hint tells the person at the terminal what to type or change next, rather than restating the detail or explaining how the seam works. Sampled examples that should hold: 'point --dir at the .planning/ directory that holds ROADMAP.md, or run /cad-new-project if this project has no roadmap yet'; 'let the run holding the lock finish and re-run, or clear a stale lock the detail names - the harvest is idempotent, so a second pass costs nothing'.",
      "why_human": "This is the one truth the phase ships no assertion for: the check tests that a hint KEY exists, never that its text helps, so there is nothing to run. I sampled roughly 35 of the 243 hints and every one opened with an imperative naming an action, but a sample is not the population and 'in the user's own terms' is a judgment about whose terms - the hints are addressed to you, and the SUMMARY itself names this as the gap /cad-verify should push on."
    }
  ]
}
