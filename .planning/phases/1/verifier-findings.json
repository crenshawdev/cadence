{
  "status": "gaps",
  "score": "9/11",
  "truths": [
    {
      "n": 1,
      "truth": "agents/ holds thirty rung files and the ladder is square - six roles x five rungs, one file per pair",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "`ls agents/*.md | wc -l` = 30. Tallying the frontmatter `effort:` line of every file: 6 low, 6 medium, 6 high, 6 xhigh, 6 max, and each rung group holds six DISTINCT role names (cad-planner, cad-assumptions-analyzer, cad-verifier, cad-reviewer, cad-executor, cad-plan-checker). The analyzer's inversion is preserved per D-01: agents/cad-assumptions-analyzer.md carries effort xhigh, agents/cad-assumptions-analyzer-high.md carries high."
    },
    {
      "n": 2,
      "truth": "rungFile(role, rung) returns a non-null stem for all thirty pairs and every stem names a file on disk",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "Imported RUNG_FILES and rungFile from cadence-core/bin/lib/rung-agent.mjs: 6 roles, 30 stems in the map, and looping the 6x5 cross product gave `pairs: 30 bad: 0` - every pair resolved and every agents/<stem>.md existed. New entries confirmed in the map, e.g. cad-executor {low:cad-executor-low, medium:cad-executor-medium, high:cad-executor, xhigh:cad-executor-xhigh, max:cad-executor-max}."
    },
    {
      "n": 3,
      "truth": "self-verify passes clean over the enlarged ladder - no unbudgeted-surface, no budget-overrun, no effort-enum-drift, no undeclared-rung-agent",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "`node cadence-core/bin/self-verify.mjs` exit 0, `{\"ok\":true, ... \"problems\":[]}` over all 30 named checks, which include budgets, rung-effort, rung-prefix, routing-cells and effort-enums. Supporting: cadence-core/bin/weight-budgets.json now carries 30 `agents/` rows and each of the eleven new rows equals the file's real byte size (e.g. cad-planner-low 429/429, cad-verifier-low 460/460, cad-plan-checker-max 476/476)."
    },
    {
      "n": 4,
      "truth": "Within each role every rung file's post-frontmatter body is byte-identical to its siblings' - one body per role, no behaviour of its own (RNG-01)",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "sha256 of the post-frontmatter body of all 30 files yields exactly 6 distinct digests, one per role, each shared by that role's five rung files (cad-executor 47d85b2e2416 x5, cad-verifier 6dfb20829838 x5, cad-plan-checker 8c570aba329d x5, cad-planner d00d7c9d9de2 x5, cad-reviewer da2061899dea x5, cad-assumptions-analyzer e108e8f43ce5 x5). Spot-check agents/cad-executor-max.md against agents/cad-executor.md: only name/description/effort differ; tools, color, maxTurns: 200 and skills: [cad-executor-contract] land identically and the body is the two-line contract pointer."
    },
    {
      "n": 5,
      "truth": "The phase is purely additive under agents/ - no existing agent file renamed or deleted",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "`git diff --name-status 42295be3..14e387ed -- agents/` is 11 `A` and nothing else - no R, no D. Per commit: e6c6ad5c and 175c7996 touch agents/ not at all, 14e387ed adds all eleven. The same range over the whole tree is 14 A / 16 M with zero R and zero D. Working tree was clean at probe time (only .planning/phases/1/UAT.md untracked), so every probe ran against committed state."
    },
    {
      "n": 6,
      "truth": "The suite passes and no test asserts the agent-file count nineteen",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "`node cadence-core/bin/test.mjs` exit 0: tests 3813, pass 3812, fail 0, skipped 1. The one skip is unrelated to this phase and conditional - `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair # no completed phase in the live roadmap`. The census marker and its test both read thirty now (cadence-core/bin/rung-agent.test.mjs:82-83, `RUNG_FILES names 30 files across the six roles, and is frozen`). The one surviving `19` assertion touching agent names is cadence-core/bin/route.test.mjs:887 `assert.equal(byName.size, 19, ...)`, which counts the distinct stems the ROUTE-TABLE CELLS reach, not the files on disk; PLAN Task 1 named it as deliberately unmoved and the test's own comment at :892-894 cites phase 1 / D-03. cadence-core/bin/prose-agreement.test.mjs:646 carries the string '19 rung files' inside an assertion FAILURE MESSAGE only (the assertion is `assert.deepEqual(wrong, [], ...)`), so it asserts no count."
    },
    {
      "n": 7,
      "truth": "model.effort accepts every rung for every role, and the accepted set is the SAME five rungs for all six - one uniform question per role",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "cadence-core/config.schema.json:19-24 - all six model.effort.<role> keys carry the identical enum [\"low\",\"medium\",\"high\",\"xhigh\",\"max\",null]. Behaviour: `node cadence-core/bin/config.mjs check model.effort.<role>=<rung>` returned ok:true for all 30 pairs. Refusal is uniform too: `=ultra` for each of the six roles returns the byte-identical error 'must be one of: low, medium, high, xhigh, max, null', so no role has to be explained as an exception."
    },
    {
      "n": 8,
      "truth": "A newly-available rung set in config traces end to end - config key -> route resolve -> a real agent file, for all eleven new cells",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "Wrote temp configs setting model.effort.<role> to a rung that did not exist before this phase and ran `node cadence-core/bin/route.mjs resolve --role <role> --file <cfg>`. All eleven new pairs landed: cad-planner low->cad-planner-low, cad-planner medium->cad-planner-medium, cad-assumptions-analyzer low/medium/max -> the three new analyzer files, cad-executor low/medium/max -> the three new executor files, cad-verifier low->cad-verifier-low, cad-reviewer low->cad-reviewer-low, cad-plan-checker max->cad-plan-checker-max. Every resolve returned ok:true with `effort` equal to the configured rung and `agent` naming a file that exists under agents/. The eleven files are therefore reachable from the user-facing seam, not orphaned artifacts."
    },
    {
      "n": 9,
      "truth": "GH-249 traces to a REQUIREMENTS row pointing at Phase 1 (ROADMAP success criterion 5)",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": ".planning/REQUIREMENTS.md:17 carries the RNG-06 bullet under `## Active`, ending 'GH-249 Phase 1'; .planning/REQUIREMENTS.md:516 carries the traceability row `| RNG-06 | Phase 1 | Pending |`. No requirement mapped to phase 1 is unclaimed by the plan - PLAN.md frontmatter declares `requirements: [RNG-06]`."
    },
    {
      "n": 10,
      "truth": "No shipped surface still describes the reverse ladder check Task 1 retired",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "cadence-core/references/seam-spawn-agent.md:15-16 still reads 'Self-verify fails in both directions: a rung with no file, and a rung file no cell reaches.' The second clause is exactly the arm commit e6c6ad5c deleted: cadence-core/bin/self-verify.mjs:1144-1152 states 'This arm used to also file the reverse ... and that half is gone (phase 1, D-03)', and `grep -c \"no cell at any level resolves to it\" cadence-core/bin/self-verify.mjs` returns 0. The check now files undeclared-rung-agent only when `rungFile(role, rung) !== stem` (self-verify.mjs:1164-1168), so a mapped rung file no cell reaches is legal. That is the state of all eleven files this phase shipped, so the doc tells its reader CI would refuse the very files that just landed. PLAN Task 1 listed three prose surfaces to correct (route-table.json `_meta.rungs`, README.md, INTERNALS.md) and its Verify grepped 'both directions' across those three files only, so this fourth surface was never looked at. The three named surfaces ARE correct: route-table.json:4 now says 'a rung-suffixed agent file that map files for no role', and neither README.md nor INTERNALS.md carries the claim."
    },
    {
      "n": 11,
      "truth": "No shipped surface still records the rung ladder as nineteen files (LINEAGE.md excepted by the standing decision PLAN Notes cites)",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "Five sites still say nineteen, and SUMMARY's Goal check asserts 'what is missing is only ... two prose strings still saying nineteen', which is false. (a) cadence-core/bin/lib/census-registry.mjs:199 'the 19 rung file stems across the six roles' and :200-201 `asserted_by: 'the test named \\`RUNG_FILES names 19 files across the six roles, and is frozen\\`'` - that test name no longer exists; the live test is 'RUNG_FILES names 30 files...' (rung-agent.test.mjs:83) and its own marker at :82 reads 30, so the two halves of the `rung-agent-files` census now contradict each other. Nothing catches it: cadence-core/bin/census-registry.test.mjs:137-144 joins marker to row by ID only and :43-46 checks `counts`/`asserted_by` are non-empty strings, never comparing their text. (b) cadence-core/references/seam-spawn-agent.md:18 'one uniform value across all 19 rung files'. (c) cadence-core/bin/lib/read-trace.mjs:42 'no suffix convention is true of all 19 files' and (d) :1220 'The same 19 stems' - both are comments only; the code at :1213-1217 and :1225-1229 derives from RUNG_FILES and is functionally correct. (e) cadence-core/bin/prose-agreement.test.mjs:646, failure-message text only. LINEAGE.md:16 and :37 also say 19 and are NOT counted here - PLAN Notes records leaving them stale as a standing decision this phase does not reopen. README.md:139 ('across 30 rung files') and INTERNALS.md:11 ('30 files cover the six roles') are correct."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "Thirty rung files, five rungs per role",
      "evidence": "`ls agents/*.md | wc -l` = 30; the effort: lines tally 6 low, 6 medium, 6 high, 6 xhigh, 6 max, with six distinct roles inside each rung group. The analyzer's D-01 inversion is intact."
    },
    {
      "k": 2,
      "name": "rungFile resolves all thirty pairs",
      "evidence": "Importing RUNG_FILES/rungFile from cadence-core/bin/lib/rung-agent.mjs and walking the 6x5 cross product: 30 pairs, 0 bad - every pair returns a stem and every stem names an existing agents/*.md."
    },
    {
      "k": 3,
      "name": "self-verify is clean",
      "evidence": "`node cadence-core/bin/self-verify.mjs` exit 0, ok:true, problems:[] over 30 checks including budgets, rung-effort, rung-prefix, routing-cells and effort-enums. weight-budgets.json carries 30 agents/ rows and each new row equals the file's actual byte count."
    },
    {
      "k": 4,
      "name": "Rung bodies identical within a role",
      "evidence": "sha256 over the post-frontmatter body of all 30 files yields exactly six digests, one per role, each shared by that role's five rung files. agents/cad-executor-max.md differs from agents/cad-executor.md in name, description and effort only."
    },
    {
      "k": 5,
      "name": "No existing agent file renamed or deleted",
      "evidence": "`git diff --name-status 42295be3..14e387ed -- agents/` is 11 A, zero R, zero D; all eleven land in 14e387ed and the two earlier commits touch agents/ not at all."
    },
    {
      "k": 6,
      "name": "Suite passes and no test pins nineteen",
      "evidence": "`node cadence-core/bin/test.mjs` exit 0 - 3813 tests, 3812 pass, 0 fail, 1 unrelated conditional skip. The rung-agent census test and its marker both read 30. route.test.mjs:887's `byName.size === 19` counts stems the route-table CELLS reach, not files on disk, and PLAN Task 1 kept it deliberately; prose-agreement.test.mjs:646's '19' is failure-message text that asserts nothing."
    },
    {
      "k": 7,
      "name": "model.effort accepts every rung for every role",
      "evidence": "config.schema.json:19-24 - six identical enums [low, medium, high, xhigh, max, null]. `config.mjs check model.effort.<role>=<rung>` returns ok:true for all 30 pairs, and `=ultra` is refused by all six roles with the byte-identical message 'must be one of: low, medium, high, xhigh, max, null'."
    }
  ],
  "gaps": [
    {
      "name": "A shipped reference still promises the ladder check that Task 1 deleted",
      "reason": "behavior wrong - the doc states a CI guarantee that no longer exists, and states it against exactly the files this phase shipped",
      "evidence": "cadence-core/references/seam-spawn-agent.md:15-16 reads 'Self-verify fails in both directions: a rung with no file, and a rung file no cell reaches.' Commit e6c6ad5c removed the second half: cadence-core/bin/self-verify.mjs:1144-1152 records 'that half is gone (phase 1, D-03)' and `grep -c \"no cell at any level resolves to it\" cadence-core/bin/self-verify.mjs` is 0; check 8 now files undeclared-rung-agent only where `rungFile(role, rung) !== stem` (self-verify.mjs:1164-1168). All eleven new files are mapped rung files no cell reaches, so the doc claims CI would refuse them. PLAN Task 1's Verify grepped 'both directions' across README.md, INTERNALS.md and route-table.json only - those three are correct - and never reached references/.",
      "severity": "major",
      "missing": "Rewrite the second clause of cadence-core/references/seam-spawn-agent.md:15-16 to the narrowed rule, matching the wording route-table.json:4 now uses ('a rung-suffixed agent file that map files for no role'), so the reference no longer promises a refusal for the ordinary state of a complete ladder."
    },
    {
      "name": "Five shipped surfaces still record the ladder as nineteen files",
      "reason": "behavior wrong - stale counts, and one census whose two halves now contradict each other with nothing enforcing agreement",
      "evidence": "cadence-core/bin/lib/census-registry.mjs:199 says 'the 19 rung file stems' and :200-201 names its asserting site as 'the test named `RUNG_FILES names 19 files across the six roles, and is frozen`' - a test name that does not exist, since rung-agent.test.mjs:83 is now 'RUNG_FILES names 30 files...' and its marker at :82 reads 30. census-registry.test.mjs:137-144 joins marker to row by id only and :43-46 checks the strings are merely non-empty, so the contradiction is unenforced and the suite stays green. Also cadence-core/references/seam-spawn-agent.md:18 'all 19 rung files'; cadence-core/bin/lib/read-trace.mjs:42 and :1220 (comments only - the code at :1213-1229 derives from RUNG_FILES and is correct); cadence-core/bin/prose-agreement.test.mjs:646 (failure-message text only). SUMMARY's Goal check states the residue is 'only ... two prose strings', which undercounts by three. LINEAGE.md:16 and :37 are excluded on PLAN Notes' standing decision.",
      "severity": "minor",
      "missing": "Update the five sites to thirty, and re-point cadence-core/bin/lib/census-registry.mjs:200-201's asserted_by at the test's real name so the rung-agent-files census resolves again. Then decide whether census-registry.test.mjs should compare the marker's `asserts:` text to the row's `counts` - today nothing does, which is why this drifted silently."
    }
  ],
  "human_checks": []
}
