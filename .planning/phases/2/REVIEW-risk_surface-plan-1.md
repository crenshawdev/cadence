{
  "trigger": "risk_surface",
  "phase": 2,
  "plan": 1,
  "range": "fc162e3..ce58695",
  "voices": ["openai/gpt-5.6-sol"],
  "rounds": 2,
  "settled": "The two high survivors cited registrableDomain, PUBLIC_TWO_LABEL and the login-selection precedence rule. All three were DELETED in 20b6bd5..ce58695 on the user's ruling, so neither finding has code left to be about. The trigger's one re-arm was spent on the round that raised them and was not fired again.",
  "accepted_limit": "The forgejo call now binds with `tea --remote origin`. When a remote's host names no login, tea falls back to config order, exits 0, and prints its NOTE on stderr, which this seam discards by contract - so the seam cannot see a fallback happen. Accepted as tea's documented behaviour rather than compensated for with a host rule. The fix is a login whose ssh_host names the remote's host; this repository's was corrected 2026-08-15.",
  "findings": []
}
