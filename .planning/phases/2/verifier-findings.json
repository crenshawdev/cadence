{
  "status": "gaps",
  "score": "6/7",
  "truths": [
    {
      "n": 1,
      "truth": "activeVersion returns the milestone, not a line-anchored predecessor, on both residue shapes (the 81bdb5d wrapped-continuation body and a body naming the predecessor in prose above the declaration)",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/bin/lib/branch-decision.mjs:119-137 - two whole-body scans, anchored candidate admitted only on `declared[1] === loose || opensSentence(body, i)`; opensSentence at :83-88 with TRAILING_FURNITURE_RE at :74. Fixtures at cadence-core/bin/branch-decision.test.mjs:288,308 pass (node --test cadence-core/bin/branch-decision.test.mjs -> 26/26, exit 0). End-to-end on the real value: activeVersion(.planning/PROJECT.md) returns 'v3.5.4' (the milestone), and `node cadence-core/bin/planning.mjs audit` on this repo emits no version_drift key."
    },
    {
      "n": 2,
      "truth": "A body whose only version token sits mid-prose still answers, and the four pinned fixtures at branch-decision.test.mjs:237-266 are byte-unchanged",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "git diff 2c88137..HEAD -- cadence-core/bin/branch-decision.test.mjs is `@@ -264,3 +264,64 @@` - additions only, nothing at or above line 266 touched. Fallback fixture at branch-decision.test.mjs:322 ('a body whose ONLY token sits on a continuation line still answers') passes; the `loose` return survives at branch-decision.mjs:139."
    },
    {
      "n": 3,
      "truth": "The DOC-02 prose-agreement pin passes and its remedy text names changing the reader rather than the file",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "cadence-core/bin/prose-agreement.test.mjs:770-792 (comment rewritten, cites phase 2 D-03 as reversing D-07) and :822-831 (failure message now points at lib/branch-decision.mjs's admission rule). `grep -rn \"Fix the section\" cadence-core/` returns nothing - no live occurrence on any shipped surface. The test passes in the run of planning/prose-agreement/git-branch (438/438, exit 0). Note on the literal wording of AC3: the sentence still occurs in this phase's own planning records (.planning/phases/2/PLAN.md:143, CONTEXT.md, reports/plan-1.md) and in .planning/phases/2/UAT.md:22 itself, which quotes it - so 'nowhere in the tree' is unsatisfiable as written; the substantive criterion (no shipped surface carries it) holds."
    },
    {
      "n": 4,
      "truth": "A phase whose Traceability rows are all Deferred no longer holds the cycle open, while the same phase with Pending rows still emits version_drift",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "cadence-core/bin/planning.mjs:1346-1349 `rolledOver` over the already-parsed `rows` with an at-least-one-row guard, wired into `cycleOpen` at :1351-1352 (`!settled(p) && !rolledOver(p)`). Fixtures cadence-core/bin/planning.test.mjs:2667 (no version_drift, deferred:['AUD-01']) and :2685 (Pending twin still returns {doc_version, published_as, cycle_state}) both pass. Behaviour observed at the pre-fix tree: re-run against a `git archive caf3a23` extraction with only planning.test.mjs copied in, the Deferred fixture FAILS and the Pending twin passes (1 fail / 1 pass), matching the pasted watch output."
    },
    {
      "n": 5,
      "truth": "readTags is bounded to the caller's own project root - an umbrella repository's tags no longer reach a non-repository project inside it, and a linked worktree still reads its repository's tags",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "cadence-core/bin/lib/git-tags.mjs:70-75 - `git rev-parse --show-toplevel` then `within(physical(top), physical(projectRoot))`, segment-wise via `root + sep`; permissive `[]` on every failure (D-08). Both callers pass a per-caller root: cadence-core/bin/git-branch.mjs:69 `readTags(dir, dir)` and cadence-core/bin/planning.mjs:1307 `readTags(dir, dirname(dir))`; grep shows no remaining one-argument callsite. Fixtures cadence-core/bin/planning.test.mjs:2718 (umbrella: no version_drift, envelope otherwise unchanged - break 'not-verified', counts {1,0,1,0}) and :2748 (linked worktree still emits version_drift), plus cadence-core/bin/git-branch.test.mjs:177 (umbrella project gets `create`, not `ask`) all pass. Behaviour observed at the pre-fix tree (git archive 487e150 + current test files): both umbrella fixtures FAIL, the linked-worktree fixture passes."
    },
    {
      "n": 6,
      "truth": "DRF-01, DRF-02 and TAG-01 each carry a WATCHED FAILING AT check whose sha is a real commit preceding that fix and which is red against that tree",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "branch-decision.test.mjs:270 -> 2c88137 (ancestor of the DRF-01 fix 58c6a47); planning.test.mjs:2695 and git-branch.test.mjs:173 -> 487e150 (ancestor of the TAG-01 fix caf3a23); planning.test.mjs:2633 -> caf3a23 (ancestor of the DRF-02 fix ca0f2ed). All three shas resolve and `git merge-base --is-ancestor` confirms each precedes its fix. Re-watched all three by extracting the named tree with `git archive` into a scratch dir and copying in only the current test file: DRF-01 25 pass / 1 fail (exactly the pasted output), TAG-01 1 pass / 2 fail, DRF-02 1 pass / 1 fail."
    },
    {
      "n": 7,
      "truth": "node --test cadence-core/bin/*.test.mjs and node cadence-core/bin/self-verify.mjs both exit 0",
      "status": "FAILED",
      "uat_item": 7,
      "evidence": "self-verify exits 0 with `\"problems\":[]` (24 checks including budgets). The suite exits 1: 2183 tests, 2182 pass, 1 fail - `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair` (cadence-core/bin/milestone-prune.test.mjs:433). Cause is `.planning/REQUIREMENTS.md:31` (PHS-01's unwrapped bullet); `git log -1 -- .planning/REQUIREMENTS.md` returns 2c88137, the phase-start commit, and neither milestone-prune.mjs nor its test appears in `git diff --stat 2c88137..HEAD`, so the inputs are byte-identical to the phase-start tree and the failure is inherited, not caused."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "activeVersion answers the milestone on both residue shapes",
      "evidence": "branch-decision.mjs:119-137 two-scan admission (agreement OR opensSentence); fixtures at branch-decision.test.mjs:288 and :308 pass, 26/26 exit 0; live activeVersion(.planning/PROJECT.md) = v3.5.4 and `planning.mjs audit` on this repo emits no version_drift."
    },
    {
      "k": 2,
      "name": "activeVersion still answers on a mid-prose-only token, four pinned fixtures unchanged",
      "evidence": "Diff hunk is `@@ -264,3 +264,64 @@` - lines 237-266 untouched; the continuation-line-only fixture at branch-decision.test.mjs:322 returns v4.2.0 via the retained `loose` fallback (branch-decision.mjs:139)."
    },
    {
      "k": 3,
      "name": "DOC-02 pin's remedy names the reader, not the file",
      "evidence": "prose-agreement.test.mjs:770-792 and :822-831 rewritten to name lib/branch-decision.mjs's admission rule, citing phase 2 D-03; `grep -rn \"Fix the section\" cadence-core/` returns nothing; test passes. Residual occurrences are only in this phase's own planning records and in UAT.md:22's own quotation of the sentence."
    },
    {
      "k": 4,
      "name": "Rolled-over phase is exempt from version_drift",
      "evidence": "planning.mjs:1346-1352 `rolledOver` (>=1 row, all Deferred) gating `cycleOpen`; planning.test.mjs:2667 emits no version_drift, :2685 Pending twin still emits {doc_version, published_as, cycle_state}; both red/green as claimed at caf3a23."
    },
    {
      "k": 5,
      "name": "Tag discovery is bounded to the caller's project root",
      "evidence": "git-tags.mjs:70-75 containment probe on realpaths; both callers updated (git-branch.mjs:69, planning.mjs:1307) with no one-arg callsite left; umbrella fixture returns no version_drift with the envelope otherwise unchanged, linked-worktree fixture still emits one; both fail at 487e150."
    },
    {
      "k": 6,
      "name": "Each requirement carries a WATCHED FAILING AT check that is red at its sha",
      "evidence": "2c88137 / 487e150 / caf3a23 all resolve and each precedes its fix commit; all three checks re-run red against `git archive` extractions of the named trees, with the paired 'must not cost' fixtures green."
    }
  ],
  "gaps": [
    {
      "k": 7,
      "name": "Test suite and self-verify both exit 0",
      "reason": "behavior wrong - `node --test cadence-core/bin/*.test.mjs` exits 1, not 0. self-verify exits 0.",
      "evidence": "Full suite run: tests 2183, pass 2182, fail 1. The failing test is `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair` at cadence-core/bin/milestone-prune.test.mjs:433, which asserts every moved `## Active` bullet is wrapped; `.planning/REQUIREMENTS.md:31` (PHS-01) is a single unwrapped line. `git log -1 -- .planning/REQUIREMENTS.md` = 2c88137 (phase start) and milestone-prune.mjs/.test.mjs are absent from `git diff --stat 2c88137..HEAD`, so the failure is inherited from the phase-start tree, not introduced by any of the five task commits, and its fix site sits outside the plan's lease.",
      "severity": "major",
      "missing": "Wrap `.planning/REQUIREMENTS.md:31` (the PHS-01 bullet) at the repository's prose width so the milestone-prune corpus test holds, then re-run `node --test cadence-core/bin/*.test.mjs` and confirm exit 0. Alternatively amend AC7 to except this inherited red - but the suite is red on the branch either way, which is what a ship gate reads."
    }
  ],
  "human_checks": []
}
