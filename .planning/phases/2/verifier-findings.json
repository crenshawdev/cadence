{
  "status": "gaps",
  "score": "6/9",
  "truths": [
    {
      "n": 1,
      "truth": "archiveRequirements removes a wrapped bullet's whole span (lead line plus indented continuations) and bounds ## Active fence-aware, leaving the trailing column-0 paragraph and unshipped bullets intact",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/bin/lib/milestone-prune.mjs:161 sectionSpan(lines,'## Active') for BOTH ends (re-read at :219 for the Shipped insert point); span walk at :175-181 (`while (j < body.length && body[j].trim() && /^\\s/.test(body[j]))`). Named tests pass: 'a wrapped bullet is removed whole - no orphaned continuation line survives', 'a wrapped-bullet prune leaves the section's trailing paragraph and the unshipped bullet', 'a `## Active` heading inside a code fence is not the section' (node --test --test-name-pattern, 5/5 pass). Wired: planning.mjs:129 imports and :4168 calls archiveRequirements in cmdMilestonePrune."
    },
    {
      "n": 2,
      "truth": "The archived ## Shipped parenthetical is the whole span joined on single spaces, byte-faithful (no lowercasing), with every | escaped at the row interpolation so the row keeps exactly five unescaped pipes",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "cadence-core/bin/lib/milestone-prune.mjs:207 `const cell = (s) => s.replace(/\\|/g, '\\\\|');` applied only at row build, so summaries keeps the bullet's bytes (:183 summaries.set with the untouched join). Tests 'the archived parenthetical is the whole span joined, first letter as authored' and 'a `|` inside a bullet is escaped before it reaches the Shipped cell' pass."
    },
    {
      "n": 3,
      "truth": "The transform run over this repository's own .planning/REQUIREMENTS.md needs no hand repair: zero orphaned continuation lines, every row the run ADDED five-piped, every new parenthetical the whole span, pre-existing rows byte-identical",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "cadence-core/bin/milestone-prune.test.mjs:407-440 corpus test reads the real ROADMAP.md + REQUIREMENTS.md, asserts no span line survives, asserts `unescapedPipes(row) === 5` for every added row and byte-identity for every pre-existing row; it passes. The AC3 deviation is legitimate, not a dodge: CFG-01/RVW-01 carry 7 and 6 pipes today, PLAN.md:159-161 forbids repairing them, and the test pins them as scars (byte-identity assertion) rather than exempting them silently."
    },
    {
      "n": 4,
      "truth": "A Forgejo remote whose SSH endpoint differs from its web host produces a tracker report instead of a skip (the phase goal's tracker half)",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "cadence-core/bin/lib/issue-decision.mjs:331 - any non-empty `tea login list` reading answers `forgejo`; HOST_TABLE.forgejo argv (:215) and resolve (:226) both carry `--remote origin`, so tea binds the login from the checkout's remote. Tests 'forgejo: an SSH host that differs from the login host still reports' and 'forgejo: every call is bound with --remote origin, and none with --login' pass. Value traced end to end: `git remote get-url origin` (issue-check.mjs:194) -> classifyOrigin -> host/slug -> HOST_TABLE argv -> envelope host/repo, pinned by 'the forge call names the --dir repo, not the one the process cwd sits in'."
    },
    {
      "n": 5,
      "truth": "The seam still refuses an affirmative tracker answer for a remote no configured login serves (AC4's second half: skip, with no forge CLI call beyond the login probe)",
      "status": "FAILED",
      "uat_item": 4,
      "evidence": "cadence-core/bin/issue-check.test.mjs:396 'an origin no login names is bound to the REMOTE, never to a login picked here' asserts `envelope.action === 'report'` for origin https://git.stranger.org/org/repo.git while the configured login is forge.example.com. issue-decision.mjs:331 makes `no-login` reachable only when the login array is EMPTY. issue-decision.mjs:161-170 records the consequence: tea falls back to the first login in config order, exits 0, and says so only on stderr, which this seam discards by contract."
    },
    {
      "n": 6,
      "truth": "The five genuine degradations are unchanged and reason-unique, and /cad-land never blocks on the tracker",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "cadence-core/bin/issue-check.test.mjs:303-345 DEGRADATIONS matrix keeps one row per class (key off, no origin remote, unrecognized host, no tea login, missing CLI, nonzero exit, truncated page, renamed field, ref scan fail); the loop at :351 asserts status 0, ok:true, a one-line reason, empty referenced/open, detail:null and reason-uniqueness via SEEN_REASONS. All pass in the full run."
    },
    {
      "n": 7,
      "truth": "node --test 'cadence-core/bin/*.test.mjs' and node cadence-core/bin/self-verify.mjs both run clean",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "Full suite: tests 1948, pass 1948, fail 0. self-verify: {\"ok\":true,...,\"problems\":[]} - 22 checks including budgets, so the re-pinned cad-land weight budget does not overrun."
    },
    {
      "n": 8,
      "truth": "The live seam reads the real tracker on this repository (action report, host ssh.jcrenshaw.dev, repo crenshawdev/cadence, complete open list)",
      "status": "UNCERTAIN",
      "uat_item": 8,
      "evidence": "Only SUMMARY.md's Goal check asserts it; the command spawns `tea` against the live git.jcrenshaw.dev instance, which this pass is barred from (no network). Code-side preconditions are verified (truth 4)."
    },
    {
      "n": 9,
      "truth": "/cad-land's step 1 tracker line names this branch's referenced issues with their states instead of a skip line",
      "status": "UNCERTAIN",
      "uat_item": 6,
      "evidence": "skills/cad-land/SKILL.md:40 invokes issue-check.mjs and :46 now states the unresolved rendering; :33 and :73 state the login rule the seam has. But `git log main..HEAD --grep '#[0-9]'` returns NOTHING on this branch, so a live walk will read the open-list fallback rather than named issues - the condition open item #187 names."
    }
  ],
  "passes": [
    { "k": 1, "name": "Wrapped bullets prune as whole spans, fence-aware", "evidence": "milestone-prune.mjs:161 sectionSpan for both ends, :175-181 span walk; the three named fixture tests pass and planning.mjs:4168 calls the function." },
    { "k": 2, "name": "Archived parenthetical is the whole span, byte-faithful, pipes escaped", "evidence": "milestone-prune.mjs:207 escapes | at the row interpolation only; 'the archived parenthetical is the whole span joined, first letter as authored' and the pipe test pass." },
    { "k": 3, "name": "Prune over this repository's own REQUIREMENTS.md needs no hand repair", "evidence": "milestone-prune.test.mjs:407 corpus test over the real files passes: no orphan lines, every ADDED row five-piped, every pre-existing row byte-identical (CFG-01/RVW-01 pinned as scars per plan scope)." },
    { "k": 5, "name": "The five degradations stay reason-unique", "evidence": "issue-check.test.mjs:303-372 matrix: one row per class, reason-uniqueness enforced by SEEN_REASONS, status 0 and empty referenced/open on every arm; all pass." },
    { "k": 7, "name": "Suite and self-verify run clean", "evidence": "1948/1948 pass, 0 fail; self-verify ok:true with problems: []." }
  ],
  "gaps": [
    {
      "k": 4,
      "name": "Tracker reports for a split-endpoint remote, skips for a stranger",
      "reason": "behavior wrong - the first half is delivered, the second is not. A remote that shares nothing with any configured login no longer skips: it now REPORTS, and the report can carry another server's issues. This is a real gap wearing the ruling's clothes. The user's ruling (delete the login-matching surface) was correct about the domain math being unfixable without a PSL, but the ruling covered the MATCHING rule, not the guard. What shipped replaces a loud, correct skip with a silent wrong affirmative answer on the land path - the exact class of failure the phase goal exists to end - and it is the only phase-2 residual that was NOT filed as an issue (SUMMARY records it under 'Not filed, recorded here').",
      "evidence": "cadence-core/bin/lib/issue-decision.mjs:331 `if (teaLogins.length === 0) return { verdict: 'no-login', ... }` - the no-login arm is now reachable only for an EMPTY login list, so a tea holding logins that serve none of this remote's hosts takes the report arm. cadence-core/bin/lib/issue-decision.mjs:161-170 states the consequence in its own words: tea 'does not refuse', prints its fallback NOTE on stderr, and 'no subcommand reports which login answered' while this seam discards child stderr. cadence-core/bin/issue-check.test.mjs:396-420 pins the new behavior as intended: origin git.stranger.org, login forge.example.com, `assert.equal(envelope.action, 'report')`. The seam's own stated invariant, asserted in the DEGRADATIONS matrix at issue-check.test.mjs:360, is that it 'never returns an affirmative answer about input it could not read'.",
      "severity": "major",
      "missing": "Either (a) a cheap guard that keeps the skip without domain math - e.g. require some login whose `name`, `url` host or `ssh_host` equals the origin host exactly, falling back to today's no-login line otherwise, which costs the split-endpoint case nothing once the login's ssh_host names the remote (the fix HOST_TABLE's header already prescribes); or (b) at minimum, file the accepted residual as a tracked issue alongside #181-#187 so it is carried rather than only narrated in SUMMARY.md."
    }
  ],
  "human_checks": [
    {
      "name": "Run `node cadence-core/bin/issue-check.mjs check --dir /data/code/cadence --base main` and read the envelope.",
      "expected": "action: report, host: ssh.jcrenshaw.dev, repo: crenshawdev/cadence, and an `open` array that is a complete list (no truncation warning). Before this phase the same command printed `tea holds no login for ssh.jcrenshaw.dev: no tracker report`.",
      "why_human": "It spawns `tea` against the live git.jcrenshaw.dev instance - an external service over the network, which this verification pass is barred from touching. No stub can stand in: the property under test is that tea's own `--remote origin` binding resolves the real login."
    },
    {
      "name": "Run /cad-land on this repository and read step 1's tracker line.",
      "expected": "The line names the issues this branch's commits reference with their states, not a skip line. Note before walking: `git log main..HEAD --grep '#[0-9]'` is empty on cadence/v3.5.1, so the walk will see the open-list fallback rather than named issues unless a commit citing a #N lands first - that is open item #187, and the walk should be judged on 'reports rather than skips', not on named issues.",
      "why_human": "Needs the live git.jcrenshaw.dev tracker read through the interactive /cad-land flow; the phase prompt tags AC6 human-verify and forbids attempting it as a machine check."
    }
  ]
}
