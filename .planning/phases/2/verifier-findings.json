{
  "status": "gaps",
  "score": "7/9",
  "truths": [
    {
      "n": 1,
      "truth": "`set git.auto_close=true --global` refuses with reason:\"invalid\", a per-pair detail naming the key and the next step, and a non-empty hint; the same pair at the repo layer applies",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "Probe with CADENCE_GLOBAL_CONFIG at a scratch temp file: `set git.auto_close=true --global` -> {\"ok\":false,\"reason\":\"invalid\",\"detail\":[{\"key\":\"git.auto_close\",\"error\":\"... can only be set in a repository's own config layer ... set it with --file <repo config> instead, e.g. --file .planning/config.json\"}],\"hint\":\"each error names the pair it refused ...\"}, and the global file was NOT auto-created afterwards. In a temp cwd, plain `set git.auto_close=true` -> ok:true and .planning/config.json holds {\"git\":{\"auto_close\":true}}. Rule at cadence-core/bin/config.mjs:193-215 (checkPairs), refusal rides the existing fail('invalid', errors, hint) at :283."
    },
    {
      "n": 2,
      "truth": "The refusal follows the RESOLVED target file rather than the --global flag, seeing through /./ and symlink spellings",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "cadence-core/bin/config.mjs:277-279 computes targetsGlobal = create || (Boolean(GLOBAL_CONFIG) && fileId !== null && fileId === layerIdentity(GLOBAL_CONFIG)); layerIdentity is the merge lib's own function, newly exported at cadence-core/bin/lib/config-merge.mjs:115 with its realpath/parent-realpath/resolve fallback unchanged (no second copy in config.mjs). Probes: `--file $CADENCE_GLOBAL_CONFIG`, `--file <gdir>/./config.json`, and `--file <symlinked-gdir>/config.json` each returned the identical detail[0]; the global file stayed byte-identical after all three. The '' guard is real: GLOBAL_CONFIG='' can never match a target."
    },
    {
      "n": 3,
      "truth": "`check --global` is declared on the arg contract and reports the byte-identical per-pair error the write face refuses on, while accepting an unmarked key",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "cadence-core/bin/lib/arg-contract.mjs:917-924 declares check['--global'] with the same grammar as its three siblings; cadence-core/bin/config.mjs:450-465 reads it through evaluateFlag off that row (not through optFile) and drops the token before checkPairs. Probes: `check --global git.auto_close=true` returned exactly the detail[0] the `set` arm returns; `check --global stakes=critical` -> {\"ok\":true}; `check git.auto_close=true` -> {\"ok\":true}; `check --global` with no pairs -> {\"ok\":true} rather than the old `not a key=value pair`. self-verify reports no unknown-flag / unknown-subcommand (problems: [])."
    },
    {
      "n": 4,
      "truth": "No `\"src\": \"repo\"` key is refused, and the shipped `set --global auto-creates ...` test is untouched",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "Probe: `set stakes=critical --global` -> ok:true and the temp global file holds \"stakes\": \"critical\". The rule reads spec.repo_only, never spec.src (config.mjs:207). git diff 89dcb93a..47be9e04 -- cadence-core/bin/config.test.mjs shows only 2 removed lines (the file header and the ARG-06 loop list), so cadence-core/bin/config.test.mjs:29-38 is byte-identical and passed in the full run. In-process control test at config.test.mjs asserts the shipped schema's repo_only set is exactly ['git.auto_close'] and that no other src:\"repo\" key carries the marker - it passed by name."
    },
    {
      "n": 5,
      "truth": "The refused set is derived from the schema marker, not from a key list",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "Independently falsified rather than taken from SUMMARY. With CADENCE_TEST_SEAM=1 + CADENCE_CONFIG_SCHEMA at a scratch fixture holding two key names the shipped schema has never carried, `set zzz.novel_marked=true --global` refused with the same error shape while `set zzz.novel_unmarked=true --global` returned ok:true. Mutation control: the same fixture with the `\"repo_only\": true` line stripped made the previously-refused key accepted (ok:true). `grep -n auto_close cadence-core/bin/config.mjs cadence-core/bin/lib/config-merge.mjs` returns nothing, so no key name appears in the rule. The committed AC5 test (config.test.mjs, 'SCP-01: a fixture schema marking a DIFFERENT key refuses that key') passed by name and asserts its fixture body does not contain 'auto_close'. SUMMARY's schema-derived claim is CONFIRMED."
    },
    {
      "n": 6,
      "truth": "A multi-pair global set carrying one marked key writes nothing",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "checkPairs is called at config.mjs:282, ahead of the readFileSync at :286 and the atomicWrite at :301, and fail() at :283 exits first. Probe: `set git.auto_close=true stakes=critical --global` against a pre-written global file refused, and sha256sum was identical before and after (6af8b4c7...29f87 both times)."
    },
    {
      "n": 7,
      "truth": "self-verify is clean and the whole suite passes",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "`node cadence-core/bin/self-verify.mjs --root .` -> ok:true, problems: [] across 26 checks. `node cadence-core/bin/test.mjs` -> tests 2969, pass 2969, fail 0 (single full-suite run). Budget re-pin holds: wc -c cadence-core/references/config-catalog.md = 10725 and weight-budgets.json:27 = 10725; weight.mjs --root . -> ok:true. Flag census re-pinned to 179 at arg-contract.test.mjs:303."
    },
    {
      "n": 8,
      "truth": "The layer decision is bound to the file that is actually read and written",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "cadence-core/bin/config.mjs:277 resolves layerIdentity(file) from a PATHNAME; :286 readFileSync(file) and :301 atomicWrite(file) re-resolve that same pathname independently. Nothing carries a file descriptor or a resolved identity between the check and the write, so a directory/symlink swap landing between :279 and :301 writes through a path the scope check already cleared. Confirmed against the cited code, exactly as SUMMARY's open item states."
    },
    {
      "n": 9,
      "truth": "The ROADMAP entry for this phase describes the rule the phase actually shipped",
      "status": "FAILED",
      "uat_item": null,
      "evidence": ".planning/ROADMAP.md:64 still says phase 2 'reads the schema's `\"src\": \"repo\"` marker', and SC2 at :154-156 still says 'The refusal covers all 33 `\"src\": \"repo\"` keys'. The shipped rule deliberately reads `repo_only` and deliberately refuses none of the 33 src:\"repo\" keys (UAT item 4 asserts the opposite of SC2). The supersession is recorded in .planning/phases/2/CONTEXT.md D-01 and PLAN.md's Notes, but the ROADMAP text itself was never reconciled."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "--global refuses a repo-layer-only key; the repo layer takes it",
      "evidence": "set git.auto_close=true --global -> ok:false / reason:\"invalid\" / detail[0].key git.auto_close / error naming --file <repo config> / non-empty hint, and the temp global file was not created. Plain set in a temp cwd -> ok:true with the value in .planning/config.json. Rule: cadence-core/bin/config.mjs:193-215."
    },
    {
      "k": 2,
      "name": "The rule reads the resolved target file, not the flag",
      "evidence": "--file $CADENCE_GLOBAL_CONFIG, <gdir>/./config.json and a symlinked <gdir>/config.json all returned the identical detail[0]; target left byte-identical. config.mjs:277-279 via the exported layerIdentity at lib/config-merge.mjs:115."
    },
    {
      "k": 3,
      "name": "check --global reports the same scope error set refuses on",
      "evidence": "check --global git.auto_close=true returned the byte-identical detail[0] the set arm returns; check --global stakes=critical -> {\"ok\":true}. Row declared at lib/arg-contract.mjs:920-923, read via evaluateFlag at config.mjs:459."
    },
    {
      "k": 4,
      "name": "No src:\"repo\" key is refused",
      "evidence": "set stakes=critical --global -> ok:true with the value on disk; the rule reads spec.repo_only, never spec.src; config.test.mjs:29-38 is byte-identical in the phase diff and passed in the full suite run."
    },
    {
      "k": 5,
      "name": "The refused set is schema-derived, not a literal list",
      "evidence": "A scratch fixture using key names absent from the shipped schema refused the marked key and accepted the unmarked sibling; stripping the marker flipped the refusal to ok:true; no key name appears in config.mjs or config-merge.mjs. Committed test 'SCP-01: a fixture schema marking a DIFFERENT key refuses that key' passed by name."
    },
    {
      "k": 6,
      "name": "A multi-pair refusal writes nothing",
      "evidence": "set git.auto_close=true stakes=critical --global refused and sha256sum of the target was unchanged. checkPairs at config.mjs:282 precedes the read at :286 and the write at :301."
    },
    {
      "k": 7,
      "name": "Self-verify and the whole suite are clean",
      "evidence": "self-verify.mjs --root . -> ok:true, problems: []; test.mjs -> 2969 pass / 0 fail; weight.mjs -> ok:true with config-catalog.md pinned at its actual 10725 bytes."
    }
  ],
  "gaps": [
    {
      "name": "The scope decision is not bound to the file it clears (path re-resolved between check and write)",
      "reason": "behavior wrong - the layer check and the write resolve the same pathname independently, so the check does not govern the bytes that land. CONFIRMED against the cited code, and it is NOT a new finding: this is the risk_surface HIGH the user adjudicated and explicitly OVERRODE, recorded in SUMMARY.md open items.",
      "evidence": "cadence-core/bin/config.mjs:277 layerIdentity(file) resolves a pathname; :286 readFileSync(file) and :301 atomicWrite(file) each re-resolve the same pathname with nothing carried between them. A directory or symlink swap landing in that window writes through a cleared path. Not a regression - before this phase set() applied the pair with no layer check at all. The exposure is the user-global layer, where global-only keys such as workflow.test_command are honoured and that value is a command Cadence later executes.",
      "severity": "major",
      "missing": "No action requested for this phase: the user overrode it on the record and scoped the fd-binding fix (open the target once, check and write through that descriptor, or compare identities after the open) as its own work. Recorded here as confirmation, not as a rework item."
    },
    {
      "name": "ROADMAP still describes the superseded src:\"repo\" rule",
      "reason": "behavior wrong (documentation) - the contract surface contradicts the shipped code and the current UAT",
      "evidence": ".planning/ROADMAP.md:64 ('reads the schema's `\"src\": \"repo\"` marker') and SC2 at :154-156 ('The refusal covers all 33 `\"src\": \"repo\"` keys'). The shipped rule reads `repo_only` and refuses none of the 33 src:\"repo\" keys; UAT item 4 asserts exactly the opposite of SC2. The supersession is recorded in .planning/phases/2/CONTEXT.md D-01 and PLAN.md Notes, but the ROADMAP text itself was never reconciled, so the next reader of the ROADMAP is told the wrong rule.",
      "severity": "minor",
      "missing": "Reconcile ROADMAP.md's phase 2 bullet and SC2 with CONTEXT D-01 - the marker is `repo_only`, the refused set is one key today, and the derivation (not the count) is what SC2 was really asking for."
    }
  ],
  "human_checks": []
}
