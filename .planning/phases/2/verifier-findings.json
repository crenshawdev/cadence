{
  "status": "gaps",
  "score": "7/8",
  "truths": [
    {
      "n": 1,
      "truth": "A `survived` ruling raised at medium or low carrying no `fix_commit` is accepted by the writer and its entry lands in the ADJUDICATION-*.json the real `planning.mjs adjudication` seam writes.",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "Direct probe of buildEntries (cadence-core/bin/lib/adjudication-record.mjs:444-470): `AC1 medium survived NOFIX | ok: true | entry: {...\"severity\":\"medium\",\"ruling\":\"survived\",\"convergent\":false}` - no fix_commit key at all; same for `low`. End to end through the real CLI: `node --test --test-name-pattern='GH-159' cadence-core/bin/planning-adjudication.test.mjs` -> `pass 1`, and that row spawns `planning.mjs adjudication` as a subprocess, then re-reads the written record off disk (planning-adjudication.test.mjs:530-571). Live corpus: .planning/phases/2/ADJUDICATION-risk_surface-plan-1-r2.json holds two `medium survived` entries with no fix_commit key."
    },
    {
      "n": 2,
      "truth": "A `survived` blocker or high carrying neither a fix commit nor the override marker is still refused, and the misspelled key is still refused as unknown.",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "Probe output: `AC2 high survived NOFIX | ok: false | detail: voices[0].rulings[0] survived and carries neither a usable fix_commit nor overridden: true - a finding raised at blocker...`; same for blocker. `AC2 medium fix_comit typo | ok: false | detail: voices[0].rulings[0] carries an unknown key: fix_comit`; the new typo row too: `AC2 blocker overriden typo | ok: false | ... carries an unknown key: overriden`. Value check still unconditional wherever the key is SET: `AC2 medium junk fix_commit | ok: false` on 'zzzzzzz' at medium, and `AC3 blocker ovr+junk commit | ok: false` - the marker buys no exemption. Guard sites: adjudication-record.mjs:213 (RULING_KEYS), :391 (unknownKey refusal), :450-453 (VALUE check), :460-467 (severity-gated PRESENCE requirement)."
    },
    {
      "n": 3,
      "truth": "An overridden blocker/high writes a record carrying `overridden: true` and no fabricated SHA, and `risk-check status` reports that range recorded.",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "`node --test --test-name-pattern='OVERRIDDEN blocking fire' cadence-core/bin/planning-adjudication.test.mjs` -> `pass 1`. That row (planning-adjudication.test.mjs:573-619) spawns four real CLIs in sequence - `risk-check run`, `adjudication`, `trace append --event override --detail-file`, `risk-check status` - asserts `after.plans[0].state === 'recorded'` and `_exit === 0`, then reads the record off disk and asserts `stored.entries[0].overridden === true` and `'fix_commit' in stored.entries[0] === false`. Marker validation confirmed by probe: `overridden: 'yes'` refused with the detail naming the marker; only the boolean literal is accepted (adjudication-record.mjs:418-422, :504)."
    },
    {
      "n": 4,
      "truth": "`issue-filing unfixed` returns no entry carrying a `fix_commit`, and accounts for the removal rather than dropping it silently.",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "Real CLI run against a payload reconstructed from the actual archived record .planning/_archive-v3.7.3/1/ADJUDICATION-risk_surface-plan-2.json (its 4 entries, verbatim claims and rulings), with a stub forge CLI and no network: `{\"ok\":true,\"raised\":2,\"already_fixed\":1,\"already_declined\":0,\"findings\":[ ...only the `medium refuted` entry... ]}` - the `medium survived HASFIX` entry (fix_commit 4a1af326) is absent and counted on the new key. Filter at cadence-core/bin/issue-filing.mjs:278 (`selected.findings.filter((e) => !e.fix_commit)`), before resolveForge is reached. Named row `node --test --test-name-pattern='already committed is never put to the user' cadence-core/bin/issue-filing.test.mjs` -> `pass 1`."
    },
    {
      "n": 5,
      "truth": "The four surfaces state one severity-gated meaning of `survived`, and the drift row reddens when any one is edited apart from the others.",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "`node --test --test-name-pattern='RSK-07' cadence-core/bin/prose-agreement.test.mjs` -> `pass 1`. Row at prose-agreement.test.mjs pins SURVIVED_SURFACES (adjudication-record.mjs, filing-decision.mjs, triage-gate.md, review-record.md) against SURVIVED_MEANING = 'confirmed and not fixed', plus a regex arm refusing any unqualified 'survived ... names the fix commit' sentence. Phrase confirmed present in all four after whitespace collapse (adjudication-record.mjs:93, filing-decision.mjs:40, triage-gate.md collapsed offset 2630, review-record.md:92). Falsification simulated in memory over the four real file bodies, without touching them: dropping the phrase from any one surface -> assertion false (4/4); appending an unqualified restatement to any one -> regex arm catches it (4/4)."
    },
    {
      "n": 6,
      "truth": "`why` renders a fixed survivor differently from a confirmed-unfixed one on the same commit.",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "Live run on this repository: `node cadence-core/bin/why.mjs cadence-core/bin/lib/adjudication-record.mjs` -> `review: 3 surviving finding(s) cover this commit` / `[high] ...:460 ... fix: 3341ffb0` / `[medium] ...:461 ... fix: none - confirmed and left standing` / `[medium] ...:450 ... fix: none - confirmed and left standing`. Traced end to end: stored entry -> parseAdjudication reads `fix_commit` through `str` (lib/why-record.mjs:522-527) -> reviewFor spreads the survivor (why.mjs:246-250) -> findingLines prints the line unconditionally (lib/why-render.mjs:391-393)."
    },
    {
      "n": 7,
      "truth": "The GH-159 reproduction settles end to end with no halt and no live review provider, and every pre-change record still reads without refusing.",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "No-halt half: the GH-159 row settles through four LOCAL subprocesses only - risk-check run, planning.mjs adjudication, trace append (gate_pass, carrying the envelope's own three figures so recountReceipt cross-checks them against the stored rulings), risk-check status -> ok true, _exit 0, state 'recorded'. No provider call anywhere on that path, so CONTEXT's flagged assumption holds and AC7 needs no `human-verify` tag. Old-records half, checked WIDER than the phase's own tests: 83 distinct ADJUDICATION-*.json blobs recovered from every commit in `git rev-list --all` (including the oldest writer shapes the SUMMARY records as untested) run through parseAdjudication + deriveCounts -> `files=83 parsedOk=83 survivorsParsed=73 issues=0`, no count mismatch on any file. Shape census includes 2x `medium survived nofix` (this phase's own record) and 71 pre-change entries. Deferred readers touch only the ADJUDICATION sibling's EXISTENCE (planning/deferred-record.mjs:26, lib/deferred-queue.mjs:29,121), never its entries, so nothing there can refuse a widened record. Full suite: `node --test 'cadence-core/bin/*.test.mjs'` -> tests 3552, pass 3552, fail 0; `node cadence-core/bin/self-verify.mjs` -> `problems: []`."
    },
    {
      "n": 8,
      "truth": "The reference a coordinator reads at the ask step agrees with what `issue-filing unfixed` now returns.",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "cadence-core/references/triage-gate.md:281-282 still says \"A survived `blocker` or `high` is NOT in it - that one is what the gate is halting over.\" Commit 3341ffb0 made that false: lib/filing-decision.mjs:104-107 now excludes an entry only when `e.ruling === 'survived' && HALTING_SEVERITIES.includes(e.severity) && e.overridden !== true`. Real CLI run confirms: a payload holding one overridden survived BLOCKER comes back as `raised 1, already_fixed 0, returned: blocker survived overridden=True`. The same paragraph is also stale on the other side - a survived medium carrying a voluntary fix_commit is now removed by issue-filing.mjs:278 and the paragraph enumerates the excluded set as exactly two things. RSK-07 pins the meaning of `survived`, not this set statement, so nothing reddens."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "A survived medium with no fix commit is stored",
      "evidence": "buildEntries probe: medium and low survived with no fix_commit -> ok true, entry carries no fix_commit key. Real seam: the GH-159 row spawns `planning.mjs adjudication` and re-reads the written record. Live proof on this repo: .planning/phases/2/ADJUDICATION-risk_surface-plan-1-r2.json holds two `medium survived` entries with no fix_commit key beside a `high survived` naming 3341ffb0."
    },
    {
      "k": 2,
      "name": "A survived blocker/high with no fix commit is still refused",
      "evidence": "blocker and high with no fix_commit and no marker -> ok false, detail names blocker or high (adjudication-record.mjs:460-467). `fix_comit` -> `carries an unknown key: fix_comit`; the new `overriden` typo is refused the same way. The VALUE check still fires on 'zzzzzzz' at medium AND beside `overridden: true` at blocker."
    },
    {
      "k": 3,
      "name": "An overridden blocker writes a record and satisfies risk-check",
      "evidence": "`node --test --test-name-pattern='OVERRIDDEN blocking fire' cadence-core/bin/planning-adjudication.test.mjs` -> pass 1. Four real CLI subprocesses; the stored entry carries `overridden: true` with no `fix_commit` key, and `risk-check status` answers ok true, exit 0, state 'recorded'. Non-boolean markers refused by a detail naming the marker."
    },
    {
      "k": 4,
      "name": "issue-filing unfixed skips already-committed findings",
      "evidence": "Real `issue-filing.mjs unfixed` run over a payload rebuilt from the archived record's own four entries: envelope is `raised 2, already_fixed 1, already_declined 0` and the `medium survived HASFIX` entry (4a1af326) is absent from `findings`. Filter at issue-filing.mjs:278, before the forge is touched."
    },
    {
      "k": 5,
      "name": "The four survived surfaces agree and drift reddens",
      "evidence": "RSK-07 passes; the phrase 'confirmed and not fixed' is present in all four surfaces after whitespace collapse. Falsification simulated in memory over the real file bodies: removing the phrase from any one surface fails the assertion (4/4), and appending an unqualified 'survived ... names the fix commit' to any one is caught by the regex arm (4/4)."
    },
    {
      "k": 6,
      "name": "why record tells a fixed survivor from an unfixed one",
      "evidence": "`node cadence-core/bin/why.mjs cadence-core/bin/lib/adjudication-record.mjs` prints three survivors on one commit: `fix: 3341ffb0` on the high, `fix: none - confirmed and left standing` on both mediums. The field is traced from the stored entry through parseAdjudication and reviewFor into findingLines."
    },
    {
      "k": 7,
      "name": "The GH-159 reproduction closes and old records still read",
      "evidence": "The reproduction settles through four local subprocesses with no review provider - AC7's flagged assumption confirmed, no human-verify tag needed. 83 ADJUDICATION-*.json blobs recovered from all of git history parse through parseAdjudication with 0 issues and deriveCounts agrees with entry count on every one; the deferred readers only test the sibling's existence. Full suite 3552/3552, self-verify problems: []."
    }
  ],
  "gaps": [
    {
      "name": "triage-gate.md still tells the coordinator a survived blocker/high can never be in the unfixed set",
      "reason": "behavior wrong - the reference disagrees with the seam this phase changed. `references/triage-gate.md:281-282` states \"A survived `blocker` or `high` is NOT in it\", but 3341ffb0 deliberately made an OVERRIDDEN survived blocker a member so it would stop being dropped silently. A coordinator following that paragraph at the ask step reads the one entry the override case exists to surface as impossible.",
      "evidence": "cadence-core/references/triage-gate.md:281-282 ('`recorded not fixed` disposition. A survived `blocker` or `high` is NOT in it - that one is what the gate is halting over.'); contradicted by cadence-core/bin/lib/filing-decision.mjs:104-107 (`&& e.overridden !== true`) and by a real run of `issue-filing.mjs unfixed` over an overridden-blocker payload, which returns `raised 1` with `blocker survived overridden=true` in `findings`. The same paragraph enumerates the excluded set as exactly two things while cadence-core/bin/issue-filing.mjs:278 now removes a third (any entry carrying a `fix_commit`). SUMMARY.md open items record only the second half of this; the override half - the one this phase created - is unrecorded. RSK-07 pins the meaning of `survived`, not this set statement, so no test reddens.",
      "severity": "minor",
      "missing": "Amend the `**The set is READ, never judged.**` paragraph in `cadence-core/references/triage-gate.md` so it states the two current exclusions (a survived blocker/high that was NOT overridden; an entry whose fix is already committed) and names the override case as a member the coordinator will see, then re-pin the file's byte count in `cadence-core/bin/weight-budgets.json`. Consider extending RSK-07 or adding a row so this set statement is pinned to `unfixedFindings` rather than left to drift."
    }
  ],
  "human_checks": []
}
