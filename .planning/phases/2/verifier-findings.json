{
  "status": "delivered",
  "score": "8/8",
  "truths": [
    {
      "n": 1,
      "truth": "A directory lease overlaps a file and a nested directory another plan declares, and both spellings ride overlaps[].files as separate strings",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/bin/planning.test.mjs:3981-3999 - two named cases assert overlaps.length 1, plans ['PLAN-1.md','PLAN-2.md'], files containing BOTH 'src/' and 'src/auth.js' (resp. 'src/auth/') and length 2. Both pass in the full suite run (2012 pass, 0 fail). Value trace: 'src/' enters as a frontmatter files: item -> parsePlanFiles (cadence-core/bin/lib/planning-files.mjs) -> declared[i].files -> intersects/covers (cadence-core/bin/lib/lease-grammar.mjs:63,76) -> collect() at cadence-core/bin/planning.mjs:1881-1888 -> overlaps[].files in the CLI JSON the test reads."
    },
    {
      "n": 2,
      "truth": "plan-overlap and lease-check reach containment through the one exported predicate, with no local .includes(/.startsWith( comparison over declared paths",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "cadence-core/bin/planning.mjs:164 imports { covers, intersects } from './lib/lease-grammar.mjs'; used at :1883 (cmdPlanOverlap) and :2341 (cmdLeaseCheck). grep for 'files.includes(x)' and 'prefixes.some' in planning.mjs prints nothing (exit 1). Grepping lines 1780-1915 (cmdPlanOverlap) for includes(/startsWith(/indexOf( returns nothing; lines 2090-2400 return only buf.indexOf(0,start) (the NUL-byte staged-side parser, not declared-path containment) and entries.includes(name) inside cmdDetectCommands at planning.mjs:2397, a different function."
    },
    {
      "n": 3,
      "truth": "A census test reddens when the containment body idiom is pasted anywhere under cadence-core/bin/, test files included",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "cadence-core/bin/helper-census.test.mjs:101-110 adds a fifth HELPERS entry, home 'lib/lease-grammar.mjs', re built from the escaped body idiom 'return path\\.startsWith\\(declaration\\);'; :121-125 adds the module to the whole-tree walk case. Live probe in a clean git-archive checkout of 13b49c0: cp lib/lease-grammar.mjs lib/census-probe.mjs && node --test helper-census.test.mjs -> exit 1, assertion names both 'lib/census-probe.mjs (x1)' and 'lib/lease-grammar.mjs (x1)'. Repo tree untouched."
    },
    {
      "n": 4,
      "truth": "The grammar is documented and the surface budget re-pinned in the same commit, with self-verify clean",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "cadence-core/references/plan-frontmatter.md:150-197 - '## What a declared file covers (the lease grammar)' states the trailing-slash directory-lease form, that substring matching would let 'src/auth' license 'src/authority.js', and the three deliberately-not-normalized cases (symlinks, case, '..') each with its reason; :239 adds the redundant-path-segment row in the table's own Code/Means/Payload/Cleared-by shape. weight-budgets.json:36 = 17312; wc -c on the file = 17312; node cadence-core/bin/weight.mjs reports {surface: cadence-core/references/plan-frontmatter.md, bytes: 17312}. Doc + budget land together in 71d544d. node cadence-core/bin/self-verify.mjs -> exit 0, problems: []."
    },
    {
      "n": 5,
      "truth": "./a.txt and src//a.txt are refused with a named diagnostic through BOTH declaration doors, the diagnostic reaches frontmatter_issues, and the spelling reaches neither reader",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "cadence-core/bin/lib/lease-grammar.mjs:104 exports isRefusedSpelling (leading './', '/./' segment, '//'); imported at cadence-core/bin/lib/planning-files.mjs:10 and called on the frontmatter arm (per-item, with a line resolved by a cursor over the files: list's own item lines) and on the task-line arm for BOTH the normalized form and the raw annotated twin, each dropping the declaration and pushing {line, code: 'redundant-path-segment', text: issueText(...)}. planning.test.mjs adds the FULL 2x2 matrix as a generated loop (two spellings x frontmatter/task line) asserting frontmatter_issues carries the code at the declaring line and overlaps carries only 'keep.txt'; plus the twice-in-one-list case asserting lines [6,8]; plus a lease-check case where the sole declaration './a.txt' licenses nothing. All green in the full suite."
    },
    {
      "n": 6,
      "truth": "For already-unambiguous declarations lease-check returns the same verdict and the same staged/declared counts, and a plan declaring no files is still refused",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "git diff --numstat 392ae60..13b49c0 -- cadence-core/bin/planning.test.mjs = 144 insertions, 0 deletions, so every shipped lease-check case (clean lease, undeclared staged path, report-file exemption, PLAN-<k> selection, directory prefix plus the src/auth vs src/authority.js refusal, rename both sides, non-ASCII, both unrepresentable-paths cases) is unedited and green. New cases added: 'lease-check: a two-file clean lease still reports staged: 2, declared: 2 through the shared predicate' and 'lease-check: a plan whose files: list is empty is still refused as undeclared-files, exit 1'. Staged side is not re-normalized (planning.mjs:2336-2341 keeps the repoRel-canonical path and only asks covers)."
    },
    {
      "n": 7,
      "truth": "There is a commit where the two failing-capable tests exist and the fix does not, at which planning.test.mjs fails on exactly those two cases",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "git archive 328ce76 into a scratch dir -> node --test cadence-core/bin/planning.test.mjs exits 1 with tests 374, pass 372, fail 2, the two failures being exactly 'plan-overlap: a directory lease src/ overlaps the file src/auth.js the other plan declares' and 'plan-overlap: a directory lease src/ overlaps the nested directory lease src/auth/'. git show --stat 328ce76 lists cadence-core/bin/planning.test.mjs and nothing else (1 file, 28 insertions). SUMMARY.md:74-76 records the SHA 328ce76 and identifies both cases (by their src/ vs src/auth.js and src/ vs src/auth/ pairs rather than verbatim titles)."
    },
    {
      "n": 8,
      "truth": "Full suite and self-verify both green at the phase tip",
      "status": "VERIFIED",
      "uat_item": 8,
      "evidence": "node --test 'cadence-core/bin/*.test.mjs' -> tests 2012, pass 2012, fail 0. node cadence-core/bin/self-verify.mjs -> {\"ok\":true, ... \"problems\":[]}, exit 0."
    }
  ],
  "passes": [
    { "k": 1, "name": "A directory lease overlaps a file and a nested directory the other plan declares", "evidence": "cadence-core/bin/planning.test.mjs:3981-3999, both cases green; both spellings asserted present with files.length 2; value traced from the frontmatter declaration through covers/intersects (lib/lease-grammar.mjs:63,76) to overlaps[].files at planning.mjs:1881-1888." },
    { "k": 2, "name": "Both consumers reach containment through the shared module, with no local idiom", "evidence": "planning.mjs:164 import; :1883 intersects, :2341 covers; grep for 'files.includes(x)' and 'prefixes.some' prints nothing; no includes/startsWith containment left in either function body (the only hits in range are the NUL-byte parser and cmdDetectCommands:2397)." },
    { "k": 3, "name": "The census test reddens on a pasted-back containment body", "evidence": "helper-census.test.mjs:101-110,121-125; live paste-back probe in a temp checkout of 13b49c0 exits 1 naming lib/census-probe.mjs and lib/lease-grammar.mjs." },
    { "k": 4, "name": "The grammar is documented and its budget re-pinned in the same commit", "evidence": "plan-frontmatter.md:150-197 and :239; weight-budgets.json:36 = 17312 = weight.mjs's reported bytes; both in 71d544d; self-verify exit 0, problems: []." },
    { "k": 5, "name": "./a.txt and src//a.txt refused through both declaration doors", "evidence": "lease-grammar.mjs:104 isRefusedSpelling, called on both arms in lib/planning-files.mjs including the task arm's raw twin; full 2x2 seam matrix plus the repeated-declaration line-number case green in planning.test.mjs; lease-check case shows './a.txt' licenses nothing." },
    { "k": 6, "name": "Unambiguous declarations unchanged, and a fileless plan still refused", "evidence": "planning.test.mjs diff is 144 insertions / 0 deletions, so all shipped lease-check cases are unedited and green; new staged:2/declared:2 and empty-files undeclared-files cases pass." },
    { "k": 7, "name": "The watched FAIL at the tests-only commit is exactly two cases", "evidence": "At 328ce76 (archived to a scratch dir): exit 1, fail 2, both failures the two named plan-overlap cases; git show --stat 328ce76 lists only planning.test.mjs; SUMMARY.md:74-76 records the SHA and identifies both cases." },
    { "k": 8, "name": "Full suite and self-verify both green at the phase tip", "evidence": "2012/2012 tests pass; self-verify exit 0 with problems: []." }
  ],
  "gaps": [],
  "human_checks": []
}
