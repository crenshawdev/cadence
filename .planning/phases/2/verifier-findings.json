{
  "status": "needs_human",
  "score": "6/8",
  "truths": [
    {
      "n": 1,
      "truth": "task.md states the completion rule ONCE, in the risk_check step, in two halves - an absent-planning-root `written: false` (ENOENT / \"no planning root\") reports done with the check's disposition stated, any other reason withholds done - and the skip arm, the record step and the done step point at that one rule instead of restating it, with no `[ -d .planning ]` check beside the seam.",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/workflows/task.md:208-230 (\"THE COMPLETION RULE, stated here and nowhere else\", half one :212-220 naming both seam spellings, half two :222-230). Pointers, no verdict of their own: skip arm :166-168 (\"under the completion rule below ... that same rule's absent-root arm\"), record step :308-313 (\"That reason is the absent-root one the `risk_check` step's completion rule already decided\"), done step :354-355 (\"Whether this block is reached at all was decided by the `risk_check` step's completion rule\"). `grep -n 'any other reason'` prints exactly one line (222) inside <step name=\"risk_check\"> (:149-290); `grep -n '\\[ -d .planning \\]'` prints exactly two (51, the scope-step prohibition, and 226, the rule's own \"never on\" sentence)."
    },
    {
      "n": 2,
      "truth": "The prose pin is load-bearing on BOTH halves and on the one-place clause - it goes red when either half is deleted, when either half is neutered while its words stay on the page, and when a pointer site restates a verdict of its own.",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "cadence-core/bin/prose-agreement.test.mjs:1637-1697 'ENFORCEMENT, task.md: the completion rule decides `written: false`, in ONE place' - passes at HEAD (node --test --test-name-pattern). Falsified four ways in an isolated rsync copy of the tree (project files untouched, copy deleted after): (a) half one deleted -> red, 'task.md no longer contains \"the absent planning root\"'; (b) half two deleted -> red, 'no longer contains \"any other reason\"'; (c) half one negated in place (\"where the run never reports done\") -> red on its own message '...can never finish - the defect GH-246 filed - the reporting half now carries a negation'; (d) the record step rewritten to carry its own \"the run reports done ... any other reason withholds it\" verdict -> red on 'task.md states the completion rule in more than one place again'. Restored copy green each time."
    },
    {
      "n": 3,
      "truth": "A seam test on the existing treeless fixture runs every seam /cad-task calls and proves each answers ok:true / written:false with the absent-root reason and that nothing is scaffolded.",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "cadence-core/bin/planning-task-record.test.mjs:145-217 'treeless run: every seam /cad-task calls answers written:false, and nothing is created' over taskRepo(TASK_COMMITS, {planning:false}) - passes in 435 ms by name. It asserts reason === 'ENOENT' by exact string on the three trace appends and the close, /no planning root/ on task-record, scoped.trace.written === false with scoped.trace.reason === 'ENOENT', then existsSync(dir) === false and existsSync(join(root,'tasks')) === false. The refusal is the seam's, not the machine's: runIn gained an optional env overlay (:64-80) and the row points CADENCE_GLOBAL_CONFIG at a nonexistent path, so no user-global surfaces answer can clear the gate. The test is in the suite (one occurrence in the 3772-test run). Value traced end to end: appendEvent returns reason 'ENOENT' only from appendFileSync (cadence-core/bin/lib/trace.mjs:1119-1121 swallows the lstat ENOENT, :1160-1165 returns any non-ENOENT stat code under its own name, :1201-1204 is the only other reason path) and tracePath is join(planningRoot,'trace.jsonl') with no intermediate directory (:285-287), so the exact string the seam emits is the exact string task.md:214-220 keys the reporting arm on."
    },
    {
      "n": 4,
      "truth": "The risk_check step answers a `surfaces-unanswered` refusal in the run - scan with detect-surfaces, ask the one-time question per references/risk-surface.md, re-run the same risk-check run line with --surfaces - it is pinned, and the seam behaviour it rests on is real (bare call refuses, --surfaces re-run reaches a verdict on a treeless repo).",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "cadence-core/workflows/task.md:180-195 (refusal is neither verdict nor skip; `detect-surfaces --root .`; the question put through the ask-user seam; re-run with `--surfaces <set>`; no config.mjs set on a treeless repo because it would create .planning/config.json). Pin: cadence-core/bin/prose-agreement.test.mjs:1700-1725 'RSK-11, task.md: the surfaces-unanswered refusal is answered in the run, not stopped at' - passes at HEAD, and red in the isolated copy with :180-195 deleted, on its own message about the blocking gate never running. Seam behaviour measured by the row in truth 3: bare risk-check run answers ok:false / reason 'surfaces-unanswered', the --surfaces auth re-run answers ok:true / checked:true. Both commands are real: planning.mjs:304 routes detect-surfaces, planning/risk-check.mjs:191-256 handles --surfaces, references/risk-surface.md:92-115 is the question this arm points at."
    },
    {
      "n": 5,
      "truth": "The done step's Report block carries a `Risk check:` line between `Files:` and `Record:`, worded only in the envelope's own vocabulary, and the block's prose says that line is never dropped while `Record:` still rides a written:true envelope.",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "cadence-core/workflows/task.md:337 - `risk_check_skipped` | `checked: false` + cause | `checked: true, empty: true` | the `matches` list or `inconclusive: true` and the review's outcome, then \"{recorded, or unrecorded because <the reason, in words>}\" - sitting between Files: (:336) and Record: (:338). Prose at :340-355: Record: drops on written:false with the reason stated in its place; the Risk check: line is never dropped; \"unrecorded\" with reason ENOENT is what tells a treeless done block from an adopted one's. `grep -c 'trace close'` is 1 (:322, --role cad-task preserved). No pin holds this line by design (PLAN Notes); it is what the two human checks observe."
    },
    {
      "n": 6,
      "truth": "A live inline /cad-task on a scratch repo with no .planning/ actually completes, printing the Risk check line with the verdict and \"unrecorded\", and no Record: line.",
      "status": "UNCERTAIN",
      "uat_item": 3,
      "evidence": "Artifact side verified (truths 1, 4, 5) and the four seams measured (truth 3), but the coordinator's rendering of them is only prose here; nothing in the tree executes workflows/task.md."
    },
    {
      "n": 7,
      "truth": "After that live run, the scratch repo has no .planning/ and no tasks/<slug>/.",
      "status": "UNCERTAIN",
      "uat_item": 4,
      "evidence": "The seam half is proven (truth 3: existsSync(dir) and existsSync(root/tasks) both false after seven seam calls). Whether a real coordinator run creates nothing along the way - the transient mktemp -d diff at task.md:250-260, the surfaces question, the slug derivation - is not observable without the run."
    },
    {
      "n": 8,
      "truth": "Suite, typecheck and self-verify are green and the weight budget row for task.md equals the file's measured size.",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "node cadence-core/bin/test.mjs -> exit 0, tests 3772 / pass 3772 / fail 0 (25.3 s). npx tsc -p tsconfig.ci.json -> exit 0, no diagnostics. node cadence-core/bin/self-verify.mjs -> exit 0, {\"ok\":true, ... \"problems\":[]} across 30 checks including budgets. wc -c cadence-core/workflows/task.md = 20719 and cadence-core/bin/weight-budgets.json:81 = 20719. No TODO/FIXME/HACK/placeholder marker on any file the phase touched (the two grep hits are `mktemp -d ...-XXXXXX` templates). RSK-11 has a REQUIREMENTS row pointing at Phase 2 (.planning/REQUIREMENTS.md:19, :506) and ROADMAP's Phase 2 entry names it; no requirement mapped to this phase is orphaned."
    }
  ],
  "passes": [
    {
      "k": 1,
      "name": "task.md states the completion rule once and the three sites point at it",
      "evidence": "cadence-core/workflows/task.md:208-230 states the rule once (\"stated here and nowhere else\"), half one :212-220 naming both seam spellings, half two :222-230 with the literal \"any other reason\" - which greps to exactly one line, inside the risk_check step. Skip arm :166-168, record step :308-313 and done step :354-355 point at it and carry no verdict of their own; `[ -d .planning ]` appears only at :51 and in the rule's own \"never on\" sentence :226. The pin cadence-core/bin/prose-agreement.test.mjs:1637-1697 passes at HEAD and went red four ways in an isolated copy: half one deleted, half two deleted, half one negated in place, and the record step given its own verdict."
    },
    {
      "k": 2,
      "name": "Treeless seam test: every seam answers written:false and creates nothing",
      "evidence": "cadence-core/bin/planning-task-record.test.mjs:145-217 passes (435 ms) and is in the 3772-test suite run. Three trace appends and the close assert reason === 'ENOENT' exactly; task-record asserts /no planning root/; the --surfaces re-run asserts ok:true, checked:true with trace.written false and trace.reason 'ENOENT'; afterwards existsSync(dir) and existsSync(root/tasks) are both false. CADENCE_GLOBAL_CONFIG is pointed at a nonexistent path so the bare call's refusal is the seam's, not the developer's machine's."
    },
    {
      "k": 5,
      "name": "surfaces-unanswered arm asks the question and re-runs with --surfaces",
      "evidence": "cadence-core/workflows/task.md:180-195 runs `detect-surfaces --root .`, puts the one-time question through the ask-user seam exactly as references/risk-surface.md:92-115 states it, and re-runs the same `risk-check run --phase 0` line with `--surfaces`, with the treeless no-persist reasoning stated. Pinned at prose-agreement.test.mjs:1700-1725 (passes; red when the paragraph is deleted). Both commands are real seams (planning.mjs:304, planning/risk-check.mjs:191-256), and the seam test measures the bare refusal plus the --surfaces verdict on a treeless fixture."
    },
    {
      "k": 6,
      "name": "Suite, typecheck and self-verify green; task.md weight budget matches",
      "evidence": "node cadence-core/bin/test.mjs exit 0, 3772/3772 pass, 0 fail; npx tsc -p tsconfig.ci.json exit 0 (run separately from the suite); node cadence-core/bin/self-verify.mjs exit 0 with problems:[] over 30 checks including budgets; wc -c task.md = 20719 = weight-budgets.json:81."
    }
  ],
  "gaps": [],
  "human_checks": [
    {
      "name": "Run an inline /cad-task on a fresh scratch git repo that has no .planning/ and no user-global surfaces answer, and read its final report",
      "expected": "The run completes. Its Report block carries a `Risk check:` line naming the verdict the seam returned (or `risk_check_skipped` if nothing landed) followed by \"unrecorded because there is no planning root\", and NO `Record:` line. When the bare risk-check run refuses `surfaces-unanswered`, the coordinator asks the one-time surface question and re-runs with --surfaces rather than stopping.",
      "why_human": "Out of reach, not merely unexercised: this needs a live interactive /cad-task coordinator session in Claude Code, including an ask-user prompt that must be answered by a person mid-run. I cannot start a slash-command session or answer its prompt from here; nothing in the tree executes workflows/task.md, so no probe renders that report. The mechanical half is already proven (seam test + prose pins); only the coordinator's rendering is open."
    },
    {
      "name": "After that same /cad-task run, `ls -a` the scratch repo and look for .planning/ and tasks/<slug>/",
      "expected": "Neither exists - no .planning/ directory and no tasks/<slug>/ directory anywhere in the repo, and `git status` shows only the task's own commits.",
      "why_human": "It observes the aftermath of the live session above, so it inherits the same out-of-reach dependency: without a real /cad-task run there is no aftermath to inspect. The seam-level half is already proven mechanically (nothing exists after seven seam calls on the treeless fixture); what is open is what a coordinator does between those calls."
    }
  ]
}
