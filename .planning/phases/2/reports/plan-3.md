PLAN COMPLETE
Plan: .planning/phases/2/PLAN-3.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: Census the protected-branch guard as unreached by any gate | 648e459 | Two arms appended to `git-guard.test.mjs`, no line of `git-guard.mjs` touched (`git diff --stat` empty across the whole plan range). Lexical census over the guard's own source: none of the five gate names appears as a STRING LITERAL (matched as a quoted value, not as English - the source comments legitimately say "taken off the call" and "the old deny gate"), the `git.*` dotted paths are exactly `on_protected`/`protected_branches`, the `config.*` paths exactly `config.git` (`config.json` excluded by name as the layer FILENAME), and the import set is pinned whole - the one bypass a name-and-key census cannot see is importing the resolver and spelling neither name nor key. Behavioural pin: the protected-branch commit under a repo layer also carrying `review.triggers.risk_surface.gate: "deferred"` is byte-identical (`JSON.stringify` compared) to the same case without it, and the task-branch arm stays null. Falsifier watched: `const gate = 'deferred';` inserted into `git-guard.mjs` reddens the census arm with its own message; guard restored. `test.mjs git` 382/382, `npx tsc -p tsconfig.ci.json` clean. |
| 2: The one-round re-arm cap binds on a deferred fire, keyed to the fire | aac302a | `triage-gate.md` gains the deferred TRIAGE half as three paragraphs placed AFTER the blocking re-arm block (the arm says "the blocking re-arm above already states", which is only true below it) and before the `adjudicated` bullet: rule member by member through the adjudication seam, the record supersedes its member, the re-arm is the same one-round cap, the same narrowed artifact and the same terminal STOP-and-ask, and the round count is read off the highest round ON DISK for that fire rather than by counting `rearm` events under this run's `corr`, with the loop that substitution causes stated. One judgment beyond the Action's words, and it is load-bearing: the round is read off the FILENAMES in the fire's home INCLUDING a member an adjudication has superseded - reading only what is still queued refunds round two at the exact moment its triage clears it. The existing corr-keyed block, its fenced `trace render` read-back included, is byte-identical. Two `deferred-queue.test.mjs` arms pin the cap over the pure faces (a round-2 member answers spent; `queueIdentity` returns no `corr` field and a `corr` written into a member's bytes moves nothing it reads; keyed to the fire, so another trigger or discriminator keeps its round; a superseded round still counts, and an `ADJUDICATION` name alone raises nothing). One `prose-agreement.test.mjs` arm holds the two caps apart: the corr filter quoted whole and required to appear exactly once, its fenced block still `mktemp`-scoped and chained to the render, plus the deferred arm's five load-bearing clauses read off a named anchor. Falsifiers watched: making the cap helper corr-keyed reddens the queue arm; deleting `&&o.corr===r.corr` from the doc reddens the prose arm with its own message. Budget re-pinned 14839 -> 17341. `test.mjs prose` 272/272, `other` 556/557 (the one failure pre-existing, see open items), self-verify `ok:true` `problems: []`, tsc clean. |
| 3: Pin that no stakes level fires `deferred` | ab382a8 | One arm at the end of `route.test.mjs`, three halves. The shipped table read directly - every cell of `SHIPPED_TABLE.review`, so a level or trigger later ADDED to the grid is censused rather than exempt. Then `resolve --role cad-reviewer` at `solo`, `shipped` and `critical` with a config carrying `stakes` alone: no trigger comes back `deferred`, asked of the resolver rather than of its data, so a default injected between the table and the envelope is caught by the half that never reads the table. Third, the door D-03 leaves open is proved open - a `review.triggers.diff.gate: "deferred"` pin resolves to `deferred` - which is what makes this a HOLD rather than a pin on a dead value, and is the only committed test of that path (plan 1 verified it by hand). Falsifier watched: `critical/diff` edited to `deferred` reddens the census half with its own message naming the four quoting surfaces; table restored, `git diff --stat` empty. `test.mjs routing` 365/365, tsc clean. |
| 4: Every statement of the gate vocabulary names five | 8bd5243 | `METHOD.md`'s gate-vocabulary sentence names five with one clause for the new one (the reviewer still runs, what it found is queued, the run finishes, the LAND is what stops), and the same paragraph's downstream "one of the four values above" now reads five - the other shipped statement of the count, eight lines below, and false the moment this ships. The paragraph was re-wrapped LINE-NEUTRAL (5 changed for 5, longest line 111 against the 100 already there) so no other `DOCS-CLAIMS.md` row's cited line range rots, which the plan's "nothing else in the ledger" rule would otherwise have left me unable to repair. `METHOD-45`'s claim text reads "Gate vocabulary (5)" with a `Re-stated v3.5.7` clause in `METHOD-44`'s existing idiom; its verdict columns untouched, no other row re-verified or re-dated. `README.md` and `docs/WORKFLOW.md` are byte-identical across the plan range (both print the per-LEVEL grid, whose cells task 3 holds still). Whole suite `node cadence-core/bin/test.mjs` 2525/2526, self-verify `ok:true` `problems: []`, tsc clean. |
Deviations: none
Open items:
- Task 4's Verify asks for every group of `node cadence-core/bin/test.mjs` to pass; the suite is 2525 of 2526. The one red is `milestone-prune.test.mjs`'s `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair`, which fails on this working tree BEFORE this plan - it reads the live `.planning/REQUIREMENTS.md` and asserts every moved requirement is a WRAPPED bullet, and phase 1's completed `IVW-01` is a single-line bullet. No file this plan touches is in its read set and `.planning/REQUIREMENTS.md` is outside this plan's lease. Pre-existing, carried from plans 1 and 2; the repair is a one-line re-wrap for whichever plan next leases that file.
- `risk-diff.test.mjs`'s hand-copied `RECEIPTS` census still reads four outcome names with no `deferral` row (plan 1's open item, still open, still outside this plan's lease).
