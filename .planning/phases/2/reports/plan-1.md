PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1: the turn count becomes a structured flag on the shared append\|close body | 5924de4 | `--turns` validated in the one shared `append\|close` body beside `--raised` (`requireInt` + non-negative, no comma-grouping exception, `fail('bad-args')` before `appendEvent`); omitted-not-zero via the conditional spread; listed on the `'trace close'` CONTRACTS row only. 5 new seam tests cover `--turns 83` as a NUMBER, the five malformed values plus the bare flag leaving the file byte-identical, an absent flag writing no key, and `--turns 0` as a recorded figure. self-verify exit 0, `npx tsc -p tsconfig.ci.json` exit 0. |
| 2: the render reports turns per dispatch and per role, with their own unrecorded counter | 38f5670 | `brackets[].turns` prefers the terminal's figure, falls back to the dispatch half's, and is OMITTED (not `null`) where neither carried one - the shape the task's Verify names. `roles[].turns` + `roles[].turns_unrecorded` with their own recorded/figures/funded triple; replay identity extended so two closes differing only in turn count are two closes. Non-numeric/non-finite contributes nothing. `TraceRender` typedef and the `roles`/`brackets` JSDoc updated. 7 new render tests; the committed fixture's `roles`/`counts`/`unpaired` deepEquals unchanged; tsc exit 0. |
| 3: `seams.md` states the close-half turn rule once | c20d377 | The `**The bracket rides the resolve.**` paragraph extended, not copied - `grep -c 'ONE statement'` still 1, no second paragraph, provenance argument left in `lib/trace.mjs`'s TOKEN PROVENANCE header. `weight-budgets.json` re-pinned 19258 -> 19726 in the same commit; self-verify exit 0 and `weight.mjs` reports the file at exactly its new entry. New `prose-agreement.test.mjs` check reads the paragraph by its bolded anchor and was WATCHED failing (exit 1, 1 failing test) with each of the three sentences deleted in turn; file restored after each. |
| 4: every close site names the turn count | b118576 | All ten invocations carry `--turns <the tool-call count on the subagent return>`; per-file `grep -c -- --turns` matches D-04's census exactly (context.md 1, plan.md 2, plan-revision.md 2, review-triggers.md 1, execute.md 1, decision-review.md 1, minimalism-review.md 1, verify-deep.md 1) and `grep -rn 'trace close'` over workflows/ and references/ shows no invocation line without it. `weight-budgets.json` re-pinned for all eight files in the same commit; self-verify exit 0, suite 2080 pass 0 fail, tsc exit 0. |
| 5: the producer census binds the turn flag to every close | fe62ebd | `traceCalls` reads `--turns` off a close line the way it reads `tokens`; the existing per-close loop (beside `--role` and `--plan`) asserts it non-empty, with no per-arm or per-file count of its own. WATCHED red: deleting the flag from context.md's one close line fails the census naming `cadence-core/workflows/context.md`; file restored. Suite 2080 pass 0 fail, tsc exit 0. |
| 6: the watched FAIL for MSR-01 | 4b1d659 | One CLI-only falsifier at the end of `trace.test.mjs` asserting all three AC1/AC2 properties. Header carries `WATCHED FAILING AT 97eaf03` (the tip immediately preceding this plan's first implementation commit), the observed unpatched output verbatim (the seam accepted `--turns 83`, reported `written:true`, wrote a line with no `turns` key, rendered tokens alone; `--turns -1` also returned `ok:true`) and the re-watch recipe. Re-watched via the header's own recipe: worktree at 97eaf03 with this file copied in exits 1 with the falsifier among the failures; green on this tree. Final: suite 2081 pass 0 fail, self-verify `ok:true` `problems:[]`, tsc exit 0. |
Deviations: none
Open items:
- Declined the fuller prose shape at the close sites: the six sentences reading `OMIT --tokens on a figureless return (seams.md's bracket rule)` were left naming `--tokens` alone rather than generalized to name `--turns` too. Task 4's `Verify:` pins `grep -c -- --turns <file>` to the exact per-file census (1/2/2/1/1/1/1/1), and a prose mention would have pushed plan.md to 4 and the others past their count, so naming the flag on the command line only is the shape that satisfies it - the turn omit rule is stated in `seams.md`'s ONE statement, which the sites point at by design (task 3 / D-04). Generalize those six sentences if a later task states a count that admits them.
- `detect-commands` reports `lint: null` for this repo, so there is no lint command Cadence can find; the static-analysis step for every task here was `npx tsc -p tsconfig.ci.json` (exit 0 each time) plus `node cadence-core/bin/self-verify.mjs`.
- Pre-existing and out of scope: `self-verify.mjs`'s budget check comment states the budgets are an equality ("must have a budget entry and EQUAL it exactly - a shrink is as much a mismatch as a growth") immediately above a paragraph and an implementation that make it a CEILING (`if (bytes > budget)`). Three untouched surfaces currently sit under their pins - `cadence-core/workflows/debug.md` (6911 vs 6920), `skills/cad-capture/SKILL.md` (5751 vs 5850), `skills/cad-plan-review/SKILL.md` (2343 vs 2353) - which is legal under the code and contradicted by the sentence. Nothing in this plan touches those files; only the surfaces this plan grew were re-pinned.
