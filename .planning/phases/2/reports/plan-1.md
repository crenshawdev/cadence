PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1. State the record's grammar as a pure module | 0387268 | Landed by the PRIOR dispatch; see `reports/plan-1.1.md` for its record. Not re-run here. |
| 2. Add the validating seam that writes the record | 563027c | `adjudication` row declared first (D-12), then `cmdAdjudication` beside `cmdRiskCheck`. 13 new cases in `planning.test.mjs` against a real `mkdtempSync` git repo: the D-06 path, 40-char ids from a 7-char base and a literal `HEAD`, a paraphrased ruling relayed as `bad-payload`, five refused `--discriminator` spellings and a `--trigger` with a separator (no file created anywhere), an unresolvable `--head`, the `--round 1`/`--round 2` re-arm with round one byte-identical, and the forgotten-`--round` refusal naming the existing path. `planning.test.mjs` 423 pass / 0 fail. `arg-contract-adoption.test.mjs` green: 245 declared refusals exercised across 152 entries. The census literal at `arg-contract.test.mjs:297` bumped 145 -> 152 under the widened lease, and the table header's stale "walks all 144" corrected with it. Typecheck clean; no lint command exists (`detect-commands` answers `lint: null`). self-verify `ok:true`, `problems: []`. |
| 3. Ground every citation at the head commit | 6bf8760 | `groundCitations` runs `git cat-file -e <head_id>:<file>` under `-C top` and marks a missing citation `citation_missing: true` without dropping the entry; `citations: {checked}` rides the record and `{checked, missing}` the envelope. MEASURED, and it changed the design: `cat-file -e` exits **128** for an absent path on this git, not the documented 1, and 128 is also what "not a repository" answers - so a per-entry exit code cannot tell the two apart. A one-shot probe (`<head_id>^{commit}`) runs first; only once it succeeds is a later nonzero exit attributable to the path, and a failed probe reports `checked: false` and marks NOTHING. Two new cases: one present + one absent citation (both stored, only the second marked, entry count = findings raised), and a `../outside/...` citation marked rather than crashing. `planning.test.mjs` 425 pass / 0 fail. Typecheck clean, self-verify `ok:true`, `problems: []`. The human-verify half of AC5 (a live cross-model fire, `git checkout <head_id>` then open the cited `file:line`) is NOT met by this executor and stays open. |
| 4. Move the merge so per-voice attribution survives adjudication | 3212db3 | Step 5's `panel` and `adjudicated` bullets now rule PER RAISING VOICE first; one `RULE FIRST, MERGE AFTER` paragraph puts the dedupe and the convergence merge on the ruled set and states why (a merged finding has no raising voice, only a list). The survivor list, its shape and its ordering are untouched - `grep -n multiSelect cadence-core/references/triage-gate.md` still shows the one triage site unchanged. Grep check per Verify: the ruling instruction is line 308, the merge sentence line 312. `weight-budgets.json` re-pinned 34459 -> 35137 in the SAME commit from `weight.mjs --root .`. self-verify `ok:true`, `problems: []`; `test.mjs prose` 265 pass / 0 fail. |
| 5. Make step 5 write the record | b610021 | (a) The scoping sentence now says the trace append and the reported line are the adjudicated arm's alone while the RECORD is written on the blocking arm too, with the advisory arm excluded and its reason stated. (b) The orchestrator composes the payload as a FILE in this run's scratch dir, never `echo`/heredoc, with the per-voice shape and the verbatim-restatement refusal named. (c) One fenced `planning.mjs adjudication --phase ... --payload` invocation, the D-06 path with its discriminator grammar, the not-inside-`reports/` clause and the `--round 2` re-arm rule. No `trace append` line added or altered. Verify greps: `ADJUDICATION-` at line 411, `adjudication --phase` at 408 (one fence). GAT-04 green with exactly four outcome event names; `prose-agreement.test.mjs` 32 pass; `test.mjs prose` 265 pass; self-verify `ok:true`, `problems: []` - which is what proves task 2's CONTRACTS row covers this prose. Weight row re-pinned 35137 -> 38493 in the same commit. |
| 6. Put the same obligation on the blocking arm | 7028a81 | One paragraph in the blocking arm before its two settle points: the receipt says a fire HAPPENED, the record says what was raised and how it was ruled, and the arm points at `references/review-triggers.md` step 5 for the payload, the path and the discriminator rather than restating them. The advisory arm named as writing no record. No fifth fenced outcome event and no existing receipt line touched: `grep -c 'family outcome'` still returns 3 and GAT-04 still collects exactly four names. `weight-budgets.json` triage-gate row re-pinned 11037 -> 11779 in the same commit. self-verify `ok:true`, `problems: []`. Full suite (`test.mjs`): one failure, the pre-existing `milestone-prune.test.mjs:557` corpus case named below. |

Deviations:

[deviation] Task 2's `Verify:` asserts `node --test cadence-core/bin/arg-contract-adoption.test.mjs` passes on the new row, but the plan's `files:` lease did not cover the file the row's arrival actually reddens - `cadence-core/bin/arg-contract.test.mjs:297` asserts the whole-table entry count as the literal `145`. Raised as a structural checkpoint by the prior dispatch; the user approved widening the lease, `arg-contract.test.mjs` was added to PLAN-1 and PLAN-2, and the literal is now `152` in this task's commit. Recorded here because the lease - a locked, gate-proved declaration - was wrong as written, not because the fix was hard.

Open items:

- AC5's human-verify half is unmet and cannot be met from here: it needs one live cross-model gate fire, then `git checkout <head_id>` from the record and opening the cited `file:line`. Task 3's machine half is green.

- PRE-EXISTING, not caused by this plan: `cadence-core/bin/milestone-prune.test.mjs:557` "corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair" fails on this tree - the roadmap names a completed phase while `.planning/REQUIREMENTS.md`'s `## Active` still reads "No cycle open". That is the fourth flagged assumption in `.planning/phases/2/CONTEXT.md`, which records the gap as deliberately uncorrected. No file this plan touches is read by that test.

- The voice ROSTER on the module's return (`voices: [{voice, model}]`, uncommitted at the prior checkpoint) rides task 2's commit, because the record the seam writes carries it: a blocking `gate_pass` fire raises nothing, so it has no entries at all, and a record that cannot say which voices ran is not evidence that any did. No per-voice count rides it - every figure stays derived.

- `groundCitations`' `checked: false` arm is implemented and NOT exercised by a test: reaching it needs git or the repository to disappear between `resolveRange` and the walk, which the harness cannot stage in-process. The arm is one `try` around a single probe and marks nothing by construction (`missing` is an empty Set on that branch).

- `.planning/phases/2/PLAN-1.md` and `PLAN-2.md` carry the orchestrator's uncommitted lease widening. Left unstaged: they are the orchestrator's docs commit, not an executor's.
