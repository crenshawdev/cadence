PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: cmdPhaseDone's two writes become one ordered transition | 6d580c88 | 452 tests / 0 failures; planning.test.mjs untouched; `all-or-nothing` 0 hits and `runTransition` 1 hit inside cmdPhaseDone; typecheck exit 0. Needed a `/** @type {Array<[string, () => void]>} */` annotation on `steps` - without it tsc infers `(string \| (() => void))[][]` and refuses the tuple, the same reason cmdRenumber annotates its own list. |
| 2: absent vs unreadable REQUIREMENTS.md, refusal before the first write | f74d778a | 454 tests / 0 failures; a REQUIREMENTS.md that is a DIRECTORY returns `{"ok":false,"reason":"unreadable-requirements"}` at exit 1 with ROADMAP.md's sha256 byte-identical before and after; an ABSENT one still returns ok:true at exit 0 with the box flipped and `reqs: []`; typecheck exit 0. Refusal runs through `runTransition`'s `preflight` list - phase 1's stage now has its production caller - with `satisfied` answering from a value already in hand, so it performs no I/O and cannot throw (D-12); `lib/file-transition.mjs` untouched. |
| 3: the success envelope says which documents moved | 52485605 | 455 tests / 0 failures; `wrote` deep-equals `['ROADMAP.md','REQUIREMENTS.md']` on the two-document shape and `['ROADMAP.md']` on the roadmap-only one, where `reqs` deep-equals `[]` and `roadmap.now` is `'[x]'` in the same block; pre-existing case "phase-done: flips the box and the phase rows; Deferred untouched; --undo reverses" passes by name; typecheck exit 0 |
| 4: a step failure past the pre-flight is reported, not flattened | 46f625cb | Probe-proven per the plan: a throwaway `planning-probe.mjs` whose REQUIREMENTS.md thunk throws emitted `{"ok":false,"reason":"partial-flip","wrote":["ROADMAP.md"],"detail":"EACCES: permission denied, open 'REQUIREMENTS.md.1234.1.tmp'","hint":"ROADMAP.md was written and the rest was not - ... re-run this command once the cause is fixed ..."}` at exit 1, with the fixture's ROADMAP.md boxed and its REQ-1 row still Pending. Probe deleted before staging; `git status --porcelain` listed only the two leased files. planning.test.mjs 455 / 0 failures; `self-verify.mjs --root .` `ok:true` with `problems: []`; typecheck exit 0. |
Deviations:
[deviation] The plan's `## Must be true when done` asserts `git diff -U0 -- cadence-core/bin/planning.test.mjs | grep -c '^-[^-]'` prints `0` against the phase base, and task 4's Action names the exact expression `e && e.message ? e.message : String(e)` for the partial arm's `detail`. Those two cannot both hold: `planning.test.mjs:5846` ("source: planning.mjs's no-staged-set detail goes through redactUrl") is a COUNT census over that exact idiom, pinned at eleven uses with five wrapped in `redactUrl`, and it exists so that a new caught-error detail site is judged against EXP-01 rather than shipped unnoticed. Spelling the expression differently to dodge the census would be the precise failure the census guards. What I did: kept the idiom, re-pinned the count to twelve, and extended that test's own comment to name `partial-flip` and why it stays UNWRAPPED - its detail is whatever `atomicWrite` threw over `--dir`'s own ROADMAP.md/REQUIREMENTS.md, the same caller-named-path class as `capture --text-file`, and this seam makes no network call. Cost: 8 deleted lines in planning.test.mjs, all inside that one source-census test (7 comment lines and the count assertion). Zero pre-existing `phase-done` cases were edited, which is what AC3 actually requires; the file-wide grep now prints 8 rather than 0.
[deviation] Every task's Verify names `./node_modules/.bin/tsc -p tsconfig.ci.json`. This worktree has no `node_modules` - the repo carries no package.json and CI installs typescript ephemerally - so the literal path is unrunnable here, and `planning.mjs detect-commands --root .` returns `lint:null, typecheck:null` with a warning naming exactly that. Ran the same typecheck through the main checkout's shared install, `/code/cadence/node_modules/.bin/tsc -p tsconfig.ci.json`, with the worktree as cwd: same config, same include set, exit 0 at every task.
Open items:
- Pre-existing failure outside this lease, unrelated to this plan: `node --test 'cadence-core/bin/*.test.mjs'` is 2640 of 2641 passing, and the one red case is `cadence-core/bin/milestone-prune.test.mjs:557` "corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair", asserting `JRN-01 must be a wrapped bullet for this corpus to bite`. It reads this repo's live `.planning/REQUIREMENTS.md`, where JRN-01 is a single-line bullet already marked `| JRN-01 | Phase 1 | Complete |`; both that file and milestone-prune.test.mjs are unmodified since the phase base 5dd8cdca, so the case was already red when this dispatch started. Neither file is in this plan's lease.
- Declined the fuller partial envelope: `cmdRenumber`'s `partial-apply` carries a `failed` key beside `completed`, and `partial-flip` carries only `wrote`. Task 4's Verify asks for ok:false, the reason, `wrote`, a `detail` and a `hint` and nothing more, and with a two-step ordered list the failed document is the next one after `wrote`'s last entry. Add `failed` when a task states a caller that branches on it.
- Declined a `hint` on the `unreadable-requirements` refusal: `fail()` takes one and the plan asked only that the condition's own description supply the detail, which already names the path to repair.
