PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1: `trace close` takes a duration in the spelling the host prints | 04086a80 | `--duration-ms` declared `string`/refuse/refuse on the `trace close` row only (D-06: never `int`); closed digits-and-unit grammar (`parseDurationMs`) in the shared `append|close` body, safe-integer bounded; writes `duration_ms`. No `FLAG_SENTENCES` entry needed, so the `trace-refusal-sentences` census stays at four. Flag-entries census re-pinned 189 -> 190 across 19 rows (D-10). |
| 2: A bracket carries the duration its close reported | 0750231b | `duration_ms` read off the TERMINAL under the same numeric guard `tokens`/`turns` carry; optional key, never `null`, never derived from the two timestamps. `TraceRender` typedef now states which of `ms` and `duration_ms` measures what. |
| 3: Two closes of one dispatch render as one bracket | 0683c108 | `pairedRows` map on the `(corr, phase, plan)` worker key `renderTrace` already builds. A repeat close adopts the first writer's pending entry, fills only empty fields, upgrades `return` -> `checkpoint`/`escalation` one way, adds no bracket, no `unpaired` row, no coordinator span and no `mismatched` row. `seenTerminals` untouched (D-04). A terminal that never had a pending dispatch keeps today's behaviour. |
| 4: An open dispatch names the role it was opened under | 96219875 | `role` on every `unpaired[]` row, off the pending entry the pairing already computed; an empty-string role stays visible. The committed fixture's whole-array `deepEqual` repaired rather than loosened. Checked on this repo's own record: 4 of 4 phase-2 unpaired rows carry a role, one of them `''`. |
| 5: State the host-return dependency where a reader finds it | 6fbf0efb | New exposure bullet beside the window-CEILING bullet in `references/seam-spawn-agent.md`: the three return figures, what each funds, that the rendering can change with no deprecation, and the omit-not-zero / `unrecorded`-not-`0` recovery already in force. The ONE statement of the close line now names `--duration-ms` beside `--tokens` and `--turns`. Six `max_dispatch_tokens` purposes repointed off `references/seams.md` (D-14); `grep -n 'seams\.md' cadence-core/config.schema.json` now returns nothing. `weight-budgets.json` re-pinned 19863 -> 22127, measured. |
Deviations: [deviation] Task 3's Verify says the reversed-order fixture "renders a deep-equal result". The whole render object can never be deep-equal across two orderings: `file` is a scratch path and `events` is the file's own line order echoed back, since the render reports the record rather than editing it. Compared everything the render DERIVES instead - `brackets`, `unpaired`, `mismatched`, `roles`, `counts`, `corr` - which is the whole of what the dedup decides. Observed as a test failure on the first run and fixed in the same task.
Open items: The plan's own Notes leave nine literal `trace close` command lines in `cadence-core/workflows/` and `cadence-core/references/` (`plan.md` twice, `context.md`, `verify-deep.md`, `decision-review.md`, `minimalism-review.md`, `review-triggers.md`, `plan-revision.md` twice) spelling `--tokens ... --turns ...` with no `--duration-ms`, so an orchestrator copying one of those verbatim records no wall clock for that role. Deliberately out of scope here per CONTEXT's scope boundary and D-12; `workflows/execute.md`'s line is PLAN-2's.
Final state: `node cadence-core/bin/test.mjs` 3301 pass / 0 fail; `npx tsc -p tsconfig.ci.json` exit 0; `node cadence-core/bin/self-verify.mjs` `ok:true`, zero problems. `workflow.lint_command` is unset and `detect-commands` reports `lint: null`, so the typecheck arm is the whole of the static analysis Cadence can find here.
