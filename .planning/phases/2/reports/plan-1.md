PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Create the registry module and its four rows | 520761da | `cadence-core/bin/lib/census-registry.mjs` created with nine deeply frozen rows. Verify matched the prediction exactly: 9 rows, 9 distinct ids, all four fields plus a non-empty subject list on every row, and three separate `TypeError` throws (table push, row `id` assign, subject-list push). No length or count export, per D-04. |
| 2 - Add the marker grammar and the unregistered-census rule | ab4a7d37 | `CENSUS_TOKEN` (`CADENCE-CENSUS`) exported, marker head built by concatenation so the file is not its own marked site. Verify matched: an unknown id gives exactly one issue whose message carries both the path and the assertion text; a real id gives `[]`; the token without a following colon gives zero markers. A marker missing `asserts` comes back with `malformed: ['asserts']` rather than dropped, and a marker body is bounded at the next head on the line so a second marker cannot overwrite the first's claim. |
| 3 - Add the lease predicate over the registry | 7fdfa445 | `censusesAtRisk` built on `lease-grammar.mjs`'s `intersects` (subject side) and `covers` (holder side), no second containment rule. Verify matched: phase 5's PLAN-1 lease returns exactly three entries - `self-verify-merge-layers`, `trace-refusal-sentences`, `planning-detail-sites` - and appending their three holders returns an empty list. |
| 4 - Write the registry's own test file | c6c20ae5 | `cadence-core/bin/census-registry.test.mjs`, three arms, 7 tests, 0 failures. Live-tree arm asserts the walk reaches more than 60 modules (213 actual) and includes `lib/census-registry.mjs`, `census-registry.test.mjs`, `self-verify.mjs` and `planning/lease-check.mjs`. Fixture pair proved both ways: deleting the `rung-agent-files` row reddens the registered-id arm, restoring it greens. No row count asserted anywhere (D-04). Same commit carries the row-prose fix - see deviations. |
| 5 - Mark the nine live census sites | 54520d22 | Nine comment-only insertions, one per file, `git diff` confirmed to be 9 added lines all beginning `//` and nothing else. Verify matched the prediction on every arm: the walk finds exactly 9 marked sites and 0 unregistered; `node cadence-core/bin/test.mjs` 3083 pass / 0 fail; `self-verify.mjs` `ok:true` with 0 problems. Deleting each of the nine rows in turn was run as nine separate suites - all nine redden, each message naming the holding file, the line and the marker's own assertion text (e.g. `cadence-core/bin/self-verify.mjs:756 marks a census as `weight-budgets` ... It asserts: weight-budgets.json holds the exact UTF-8 byte size of every budgeted prose surface`). The `self-verify.mjs` marker sits at line 756, below the 104 line `SELFVERIFY-01` pins. |
Deviations: [deviation] The plan asserted (task 1) that the registry module could carry its row prose as written; what is actually true is that the `self-verify-merge-layers` row's `counts` string spelled `` `mergeLayers(` `` literally, and self-verify's merge-warnings rule matches that name-plus-paren on any non-comment line. Task 1's own `Verify:` passed, but the first FULL-suite run at task 4 showed the registry had become the thirteenth file carrying a callsite: `self-verify.mjs` reported `undocumented-merge-warnings`, and three tests failed - `the repo itself passes self-verification`, `check 12: the live tree is SIXTEEN callsites over TWELVE files, each in an arm`, and `entry: a valueless or empty --root refuses instead of linting the cwd` (which asserts self-verify is ok on this tree). What I did: fixed at the MENTION, dropping the paren from the prose with the reason stated in the row, which is the `lib/merge-warnings.mjs` discipline the plan already cites for the marker head, applied to a second lexical rule rather than to an exclusion list. Landed in c6c20ae5. Full suite 3083 pass / 0 fail and self-verify `ok:true` after it. Recorded because the plan's `## Must be true when done` line about a zero-problem self-verify was momentarily untrue between 520761da and c6c20ae5.
Open items: `cadence-core/references/conventions.md` documents the `CADENCE-DEBT` marker convention in a `## Deliberate shortcuts` section and now has no counterpart for `CADENCE-CENSUS`, so the new marker grammar has no prose home outside `lib/census-registry.mjs`'s own header. Out of this plan's lease (`conventions.md` is not declared) and named by no task, so it was not written. Worth a section on the same model when a plan can lease that file - and it must describe the fields in prose without writing a literal marker line, for the reason that section already states about its own token.
