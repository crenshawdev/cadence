PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: Admit `deferred` to the gate vocabulary at the ladder position between `advisory` and `blocking` | 18173e5 | Five names in the same order across `config.schema.json` (4 gate keys), `route-table.json` `gates` and route.mjs `DEFAULT_GATES`. Verified: `config.mjs set review.triggers.<t>.gate=deferred` ok:true for all four triggers; `check ...=nope` refuses with `must be one of: off, advisory, deferred, blocking, adjudicated`; `route.mjs resolve --role cad-reviewer --file <pinned>` returns `deferred` for all four with one warning each; `self-verify --root .` ok:true problems:[]; `test.mjs routing` 364/364, `prose` 268/268, `other` 540/541 (the one failure pre-existing, see open items); `npx tsc -p tsconfig.ci.json` clean. |
| 2: `deferral` becomes the fifth fire receipt `risk-check status` accepts | 15b333c | `FIRE_RECEIPTS` gains `deferral`; both block comments that said a fifth name is a state nothing produces now name it and its producer. No special case - as joinable as `gate_pass`. Two new planning.test.mjs arms run the real seams end to end over a risky range (git repo, `risk-check run`, `trace append`, `risk-check status`): the receipt moves the plan row `unfired` -> `recorded`, and an outcome event named outside the set settles nothing. Falsifier watched: removing `deferral` from `FIRE_RECEIPTS` reddens both arms. `test.mjs planning` 854/854, `other` 540/541 (same pre-existing failure), self-verify ok:true problems:[], tsc clean. |
| 3: The queue artifact seam - `planning.mjs deferred record` | ed15c5d | New pure `lib/deferred-queue.mjs` (filename grammar + payload classification) and `cmdDeferredRecord`; `deferred` added to `TWO_WORD` and a `deferred record` row to `CONTRACTS`. Two dedups: `recordName` moved into `lib/adjudication-record.mjs` (both call sites import it), and `buildEntries` per-finding bounds extracted as the exported `findingIssue` both artifacts validate through. Verified by hand against a scratch repo: writes `DEFERRED-diff-plan-1.json` with findings verbatim and resolved full ids, no `ADJUDICATION-*` appears, a second call answers `record-exists`, `--trigger ../../etc` and `--discriminator .` both refused naming the character rule, a `line: 0` / unknown-key payload refused in `buildEntries` own words. 9 new grammar arms + 5 new seam arms; `test.mjs planning` 859/859, `prose` 268/268, `routing` 364/364, `other` 549/550 (same pre-existing failure), self-verify ok:true problems:[], tsc clean. |
| 4: State the `deferred` arm once, where every fire site already re-reads it | 587d301 | The arm added to `references/triage-gate.md` beside the other four, opening sentence widened to five gates; step 1 and step 6 lists in `review-triggers.md` widened; `config-catalog.md` gate row gains a one-clause meaning. The blocking-counts paragraph now exempts `deferral` alongside `rearm` so the fence and the prose agree. GAT-04 census re-pinned to five receipt names in the same change; the three references re-pinned in `weight-budgets.json` (12840->14839, 39691->39714, 9526->9623). `test.mjs prose` 268/268, `routing` 364/364, `planning` 859/859, `other` 549/550 (same pre-existing failure), self-verify ok:true problems:[], tsc clean. |
Deviations: [deviation] Task 1's Verify spells the config write as `config.mjs set ... --dir <a scratch .planning>`; `config.mjs` has no `--dir` on any face (its CONTRACTS row gives `set`/`get`/`validate` a `--file` and a `--global` only), and the flag is refused as a non-key=value argument. Ran the same four writes through `--file <scratch>/.planning/config.json`, which is the same assertion. [deviation] Task 4's Verify predicts `grep 'deferred' cadence-core/workflows/plan.md cadence-core/workflows/execute.md cadence-core/references/execute-parallel.md` returns nothing; it returns two PRE-EXISTING lines using the ordinary English word - plan.md's `split - deferred slice` plan-shape enum and execute.md's `deferred item routed here resurfaces on its phase`. Neither file was touched by this plan (`git status` clean for both across all four commits) and neither line is a gate arm, so the criterion the grep stands for - no per-gate `deferred` arm added to those three files - holds.
Open items:
- `risk-diff.test.mjs`'s `RECEIPTS` literal (a deliberate hand-copied census of the outcome names a fire can settle at) still reads four names and gets no `deferral` row - that file is outside this plan's `files:` lease, and the plan put the new arms in `planning.test.mjs` instead. Worth a row there when a plan next leases it.
- `milestone-prune.test.mjs`'s `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair` fails on this working tree BEFORE this plan - it reads the live `.planning/REQUIREMENTS.md` and asserts every moved requirement is a WRAPPED bullet, and phase 1's completed `IVW-01` is a single-line bullet. No file this plan touches is in its read set. Pre-existing, out of scope.
