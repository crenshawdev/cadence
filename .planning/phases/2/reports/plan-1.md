PLAN COMPLETE
Plan: .planning/phases/2/PLAN.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1. The targeted run is what verifies a task | 1bd6fc7e | step 2 rewritten to the task's own `Verify:` with a by-name fallback, `workflow.test_command` parenthetical deleted, targeted-re-run sentence folded into the existing three-attempts bullet; contract re-pinned 12970 -> 13118 B |
| 2. One full-suite run per dispatch, immediately before the digest | 08e9ece3 | `<process>`'s closing line replaced by the single suite site (after the last task's commit and report write, before the digest), inline `config.mjs get workflow.test_command`, the hand-rolled `.planning/config.json` read forbidden with its reason, and the null arm stated; contract re-pinned 13118 -> 13965 B |
| 3. Pin both sentences, and pin them against the verifier contract | 6f89ae1b | four EXP-05 tests plus a D-05 pair pin in `prose-agreement.test.mjs`, offset-located, each half with an in-memory falsifier; both sentences also deleted on disk and shown red, then restored with `git checkout --`. `node cadence-core/bin/test.mjs`: 3512 pass, 0 fail |
| 4. The reach row names the contract's one site | 8f72c420 | `config-reach.md:133` third cell now reads "the executor contract's one suite site - one full-suite run per dispatch, immediately before the digest", copied from task 2's committed sentence; the other three honouring surfaces and the reach cell unchanged, one changed line, re-pinned 23726 -> 23808 B. `test.mjs` 3512 pass 0 fail, `self-verify.mjs` ok:true |
Deviations: [deviation] the plan's `## Must be true when done` says "Step 2 names no suite and no config key", but task 1's `Verify:` requires a step-2 line carrying "full test suite per" completed with `task`; the two cannot both hold literally. Followed the `Verify:` (the stated authority): step 2 names the suite only to forbid it per task and to point at its single site, and names no config key.
[deviation] task 2's `Verify:` asserts `perl -ne 'print if /<commit_protocol>/../<\/commit_protocol>/' ... | grep -niE "test|suite"` returns nothing. It never did: `<commit_protocol>` item 3 lists the conventional-commit types "feat, fix, docs, chore, refactor, test, perf, style", so the word `test` matches on the pre-edit tree too (checked under `git stash`). The criterion AC1 actually states - no test INVOCATION in the block - holds: the same range greps clean for `run |pytest|npm test|test\.mjs|suite|test_command`. Also reworded the new prose from "never inside `<commit_protocol>`" to "never inside the commit protocol below", because the literal tag in `<process>` opened the perl range early and swept the new prose into the block's own output.
Open items: none
