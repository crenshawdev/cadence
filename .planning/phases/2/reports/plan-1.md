PLAN COMPLETE
Plan: .planning/phases/2/PLAN.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1. The two defective pairs, as failing-capable cases, committed alone | 328ce76 | Watched FAIL captured at this SHA: `node --test cadence-core/bin/planning.test.mjs` -> exit 1, `tests 374 / pass 372 / fail 2`, the two failures being exactly `plan-overlap: a directory lease src/ overlaps the file src/auth.js the other plan declares` and `plan-overlap: a directory lease src/ overlaps the nested directory lease src/auth/`. `git show --stat HEAD` lists only `cadence-core/bin/planning.test.mjs`. (Node's reporter prints the count as `ℹ fail 2`, not the `# fail 2` the task's Verify quoted - same summary, different reporter prefix.) |
| 2. The lease-grammar module, with `plan-overlap` reading through it | ecd3465 | `node --test cadence-core/bin/lease-grammar.test.mjs cadence-core/bin/planning.test.mjs` -> exit 0, 377 pass / 0 fail; task 1's two cases now green. `grep -n "files.includes(x)" cadence-core/bin/planning.mjs` prints nothing. |
| 3. `lease-check` through the same predicate | fe84b23 | `node --test cadence-core/bin/planning.test.mjs` -> exit 0, 376 pass / 0 fail, all 19 shipped `lease-check` cases green and unedited. `grep -n "prefixes.some" cadence-core/bin/planning.mjs` prints nothing. |
| 4. `./` and redundant separators refused at both declaration doors | eb65ebc | `node --test cadence-core/bin/lease-grammar.test.mjs cadence-core/bin/planning.test.mjs` -> exit 0, 386 pass / 0 fail: unit table over the three refused and the accepted forms, the full two-spelling-by-two-door seam matrix, the twice-in-one-list case reporting lines 6 and 8, and the `lease-check` sole-`./a.txt` refusal. Whole suite 2011 pass / 0 fail; `self-verify` `problems: []`. |
| 5. The lease grammar written down, with its budget re-pinned | 71d544d | `node cadence-core/bin/self-verify.mjs` -> exit 0, `problems: []` (no `budget-overrun`, no `unbudgeted-surface`). `node cadence-core/bin/weight.mjs` reports `cadence-core/references/plan-frontmatter.md` at 17312 B, equal to the re-pinned budget. `grep -n "src/authority.js"` prints the non-substring sentence at line 165. Full suite still 2011 pass / 0 fail. |
| 6. The census row that reddens on a pasted-back containment | 13b49c0 | `node --test cadence-core/bin/helper-census.test.mjs` -> exit 0 with five `exactly one definition of ...` cases. Probe run: `cp cadence-core/bin/lib/lease-grammar.mjs cadence-core/bin/lib/census-probe.mjs` then the same command -> exit 1, the failing assertion naming `lib/census-probe.mjs (x1), lib/lease-grammar.mjs (x1)`; probe removed, never staged. At the tip: `node --test 'cadence-core/bin/*.test.mjs'` -> exit 0, 2012 pass / 0 fail, and `node cadence-core/bin/self-verify.mjs` -> exit 0, `problems: []`. `npx tsc -p tsconfig.ci.json` -> exit 0 at every commit. |
Deviations: none

Open items:
- The task-line pattern `/^\s*-\s*\*\*Files:\*\*\s*(.+)$/gm` in `parsePlanFiles` has a leading `\s*` that matches NEWLINES, so a match begins at the start of whatever blank run precedes the task line and `m.index` names an earlier line than the declaration is written on. Task 4's diagnostic measures to the `**Files:**` marker instead, which is correct and local; the pattern itself is untouched because tightening it (`[^\S\n]*`) changes what the arm MATCHES, which is outside this phase's boundary and belongs to the task-line arm's own grammar. Nothing else reads that offset today, so the defect is latent, not live.
- Declined a `containments: [{covering, covered}]` sibling field on `overlaps[]` and any exported directory-lease/normalization helper beyond `covers`, `intersects` and `isRefusedSpelling`. D-06 fixes the flat string-list shape and the `Verify:` is met by the three exports; a fourth export would have exactly one caller.
- `plans[].files` and `lease-check`'s `declared` fall by one for each REFUSED declaration, because a refused declaration is dropped before the set is built. That is the decided shape (task 4 drops it), and it does not touch D-10, which is about not counting resolved or deduped forms - but a plan author who writes `./a.txt` sees a count one lower than the lines they wrote, with `redundant-path-segment` beside it naming why.
