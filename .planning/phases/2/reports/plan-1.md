PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Retarget the stated two-reader contract | 50b4486 | seam-input.mjs header, seam-input.test.mjs (optionalFlag arms re-spelled on `--branch`) and helper-census.test.mjs's flagValue note no longer claim the `--dir` divergence survives; neither reader body touched, `missing-flag-value` still in the reader. 16/16 tests pass. |
| 2 - `--dir` refuses at the two seams that WRITE | 810c692 | `flagValue(argv,'--dir') \|\| process.cwd()` at git-publish `publish`/`reap`/`authorized` and release-bump `bump`, plus an `e.seam` catch arm ahead of each generic `internal` arm (both `internal` arms kept byte-for-byte, including `redactUrl`, so git-publish's 3-site redaction census still holds). 8 new test rows; 70/70 pass across git-publish, release-bump, config-seams. |
| 3 - `--dir` refuses at `git-branch` and `worktree-base` | 9666b74 | Same migration on `decide`, `tags` and `resolve`, each with its own `e.seam` arm; `git-branch tags` with no `--dir` still prints this repo's 33 tags. worktree-base's header stopped claiming "exit 0" for every run. 6 new test rows; 33/33 pass. |
| 4 - `--dir` refuses at `land-cleanup` and `issue-check` | 04d97ae | Same migration on `cleanup`, `gate` and `check`; `--dir` is the only flag moved and `--timeout-ms` keeps its documented DEFAULT_TIMEOUT_MS fallback (new row pins it). `gate` refuses before stdin is read, proved against a blocking findings payload and against unparseable bytes; issue-check's refusal spawns no forge CLI, proved by the marker file. Both files' "exit 0" / "ok:true on EVERY path" claims re-stated. 9 new test rows; 63/63 pass. |
| 5 - `release-bump --date` validated before `bump()` | 42e16ae | `bad-date` minted as a SEAM-level code (header vocabulary + milestone.md halt bullet); newline arm first with its own sentence, then a whole-string `YYYY-MM-DD` with month/day ranges. Presence tested `!== undefined`, so `--date ''` refuses and an absent one still dates today (UTC). Refusal carries no `manifest`/`siblings`/`changelog`. Verified on fixtures: all five values refuse with CHANGELOG.md byte-identical, and refuse identically on a fixture with no `.claude-plugin/plugin.json`. 12 new test rows. Full suite 2298/2298, `self-verify` ok:true, `tsc -p tsconfig.ci.json` clean. |
Deviations: none
Open items:
- The `bad-date` refusal detail names the flag and the grammar but does NOT echo the offending value back. Declined deliberately: the newline arm exists because a `--date` can carry a forged `## [9.9.9] - forged` release section, and echoing it would put those bytes on a line the closing workflow prints. If a caller ever needs the value, it belongs behind a length cap and a newline strip, not raw.
- `cadence-core/workflows/milestone.md` sits under a BYTE-EXACT weight budget (`cadence-core/bin/weight-budgets.json`, 13307 B, zero headroom like 107 of its 110 entries), and `weight-budgets.json` is not in this plan's `files:` lease. So the halt bullet names `bad-date` with no gloss, and two wordings in that same bullet were tightened ("Report that reason and STOP the close" -> "Report it and STOP", matching the sibling bullet) to land at 13300 B. Nothing stated was dropped. The fuller shape - a parenthetical saying `bad-date` means a `--date` that is not `YYYY-MM-DD`, plus naming `missing-flag-value`, which that same invocation can now also return - needs the budget entry regenerated, which is a one-line change to a file outside this lease.
- `release-bump --date` still reads through the permissive `optionalFlag`, so a BARE trailing `--date` is indistinguishable from an absent one and dates today. Stated by the plan rather than closed here (giving this seam `missing-flag-value` for `--date` too would be two reason codes for one malformed flag); input for phase 4's declarative contract, alongside `planning.mjs`'s own `parseArgs` `--dir` (D-03).
