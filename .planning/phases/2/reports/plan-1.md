PLAN COMPLETE
Plan: .planning/phases/2/PLAN.md
Tasks: 6 of 6 (plus the coordinator's authorized AC7 repair)
| Task | Commit | Note |
|---|---|---|
| 1 - Stop the template pinning `stakes` | 5fc6c6c0 | Deleted line 3 of `templates/config.json`; `git diff --unified=0` shows one removed line and zero added, key order preserved. `validate` ok:true over 46 checked keys. Restated `route.test.mjs`'s message string to "the template writes no stakes, so the schema default stands". 160 pass / 0 fail. |
| 2 - Prove both arms of the unset floor from a template-initialised repo | be3c7143 | New `template-initialised:` test through `floorRoot` with the parsed shipped template as its config, no `ANSWERED` overlay. Clean arm solo/sonnet, unreadable arm (directory at `PLAN-2.md`) ok:true/shipped. 161 pass / 0 fail; diff is 36 insertions and zero deletions, so the four existing floor tests are untouched. Falsified against a tree with `"stakes": "shipped"` restored: clean arm fails `actual: 'shipped', expected: 'solo'`. |
| 3 - Carry `stakes_set` on the resolve and replay envelopes | 9dbd4482 | Inherited uncommitted `route.mjs` edits judged correct and kept: `stakes_set: cfg.stakesSet` beside `stakes` on both envelopes, `readConfig`'s flag carried outward with no re-derivation, `stakesSet` unrenamed at its three read sites, `appendEvent` trace untouched. Added the missing tests: two resolve arms (unset -> false, `stakes: 'shipped'` -> true, both at the SAME level so only the flag separates them) and one replay arm cross-checking `replay` against `resolve` over one `floorRoot` fixture, both set-nesses. CLI verify over four fixtures: `"stakes":"shipped","stakes_set":false` / `"stakes":"solo","stakes_set":true`, replay matching each. 164 pass / 0 fail. |
| 4 - Make `config.mjs get stakes` answer unset instead of reporting the default | eb850c78 | New arm BESIDE the `LEVEL_KEY` one (not a widened regex), gated on the same two conditions: explicit read and no layer value. Warning names `stakes` and `route.mjs resolve`, states the level is decided per phase from the plans in scope, reads no route table. Value line unchanged. Three new arms in `config.test.mjs` (unset -> warning, set -> no `warnings` key at all, keyless -> no stakes warning). Collateral the plan did not foresee: 7 shipped layer-read tests probed `get` with `stakes` as an arbitrary key and count EVERY warning, so the new arm made them read 2 where they pin 1 - 6 switched to a `granularity` probe (the probe was arbitrary, this one stays arbitrary) and the retired-key test kept its deliberate `stakes` probe and now counts the `model.profile` family. 102 pass / 0 fail. `config-seams.test.mjs` 17 pass, unedited; `self-verify.test.mjs` 177 pass, census count unmoved so no re-pin was owed. |
| 5 - Both init workflows state what is actually written, with their ceilings re-pinned | 91f88026 | One replacement sentence in both files, byte-identical after unwrapping (compared by whitespace-normalised extraction, not by eye): "...(standard granularity, research and plan check off, verifier on). Stakes is left unset: it floors at solo when every plan in the phase reads clean, and at the shipped default when any of them cannot be read...". `grep -rn "shipped stakes" cadence-core/workflows/` returns nothing. Neither file asks for a level and neither names a seam, so no `unknown-flag` surface was added. Budget rows re-pinned to the counts `weight.mjs` MEASURES, checked by comparing the two figures: new-project.md 26241 -> 26373 and adopt.md 21030 -> 21162, both equal to measured. Template row left at 1420 against a measured 1397 - it only shrank (D-08). `self-verify` ok:true, `problems: []`. |
| 6 - Hold the README's adaptive-routing claim against a real resolve | 289b9a34 | Second helper `resolvedFromTemplate` beside `resolvedReview`, not a generalisation of it: copies the shipped template into a temp repo's `.planning/config.json`, writes one plan under `phases/3/` declaring `docs/README.md`, writes that file, resolves with `--phase`, and points `CADENCE_GLOBAL_CONFIG` at a path that does not exist so a dev's own global config cannot answer for the template. The claim is located by a stable substring; "below" comes off `route-table.json`'s `stakes_order` and the default off `config.schema.json`'s `keys.stakes.default`, neither hardcoded. Falsified: with `"stakes": "shipped"` restored in the template the arm fails with `resolved shipped against the default shipped`, which is why the level assertion is ordered ahead of the `stakes_set` pin. 60 pass / 0 fail. |
| AC7 repair | 99f5c5fd | Coordinator edit, authorized by the user through the ask-user seam after the structural checkpoint. Added `'--top', '20'` to the `run([...])` call at `why.test.mjs:695`, matching the sibling at `:192-195`, and declared `cadence-core/bin/why.test.mjs` in plan 1's `files:` lease first so `lease-check` still answered on the staged set (ok:true, 19 declared, 1 staged). `node cadence-core/bin/test.mjs` now reports 3574 pass / 0 fail; `self-verify` ok:true, problems []. |
Deviations: [deviation] The plan's `## Must be true when done` asserts `node cadence-core/bin/test.mjs` is green (AC7). It is not, and the repair is outside this plan's `files:` lease. `cadence-core/bin/why.test.mjs:694` ('the record on disk answers /cad-why for a file that task touched, end to end') runs `why` over `cadence-core/templates/config.json` against the LIVE repo and looks up commit `093408c9` in the printed window. Task 1's commit 5fc6c6c0 is a new commit touching that path, so `093408c9` moved from 6th to 7th and fell outside `why.mjs`'s DEFAULT_TOP of 6; `entryFor` returns undefined and the match assertion fails on `undefined`. Nothing about the record or the reader is wrong - the fixture's window closed. Every other suite is green and self-verify reports ok:true with an empty problems array. Repaired by the coordinator under user authorization (commit 99f5c5fd, row above); at checkpoint time `why.test.mjs` was not in this plan's lease, and this repository's own precedent (v3.7.1 phase 1 plan 1 task 1, recorded in the tasks corpus) is that the executor checkpoints rather than widening its own commit.
Open items: The fix is one argument, and this file already establishes it: the sibling test at `why.test.mjs:192-195` runs `['cadence-core/bin/lib/issue-decision.mjs', '--dir', REPO, '--top', '20']` with the comment "the one under test is the oldest, so the cap is lifted rather than the assertion narrowed to the newest ten". Adding `'--top', '20'` to the `run([...])` call at `why.test.mjs:695` is the same move for the same reason. Verified WITHOUT editing the file: `why.mjs cadence-core/templates/config.json --dir /code/cadence --top 20` prints the `093408c9` block, and all five of that test's assertions match against it (phase line, no NOT RESOLVED, `plan task: task 1 - `, `declared by: declared in RECORD.md`, and the `task 1: bound-plan-size (declares ...)` line), with an empty `warnings` array.
