PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1: The surfaced set, own-phase scoped and kinded | 75269066 | New pure module `lib/cite-surfaced.mjs` + 14-case unit table. `node --test cadence-core/bin/cite-surfaced.test.mjs` 14 pass / 0 fail; tsc clean. (Recorded by the prior dispatch; its full record is at `reports/plan-1.1.md`.) |
| 2: The cited mentions, bare and phase-qualified | ee6738e0 | New pure module `lib/cite-cited.mjs` + 11-case unit table. `node --test cadence-core/bin/cite-cited.test.mjs` 11 pass / 0 fail; tsc clean. Replayed over the real corpus: 50 `PLAN*.md`, 1094 mentions, 47 files carrying at least one. (Recorded by the prior dispatch.) |
| 3: The `cite-count` subcommand and its envelope | bfd2180a | `cite-count` row in `CONTRACTS['planning.mjs']` (3 flags, all four fields stated, refuse/refuse), `cmdCiteCount` beside `cmdPlanOverlap`, one `COMMANDS` entry, one header Subcommands entry. Re-verified from scratch against a fresh fixture rather than trusting the working tree: the happy path prints ONE JSON line with `surfaced` {count 5, 2 ids}, `cited` {count 1, explicit id list} and `cited_by_kind`'s four arms, three marked `unjoinable: true`; own-phase exclusion holds both directions on one payload; `total: 441` yields 5 surfaced, never 441; bare `--payload`, bare `--point`, `--point bogus` and absent `--payload` each exit 1 with one `ok:false` line naming the flag. `node --test arg-contract.test.mjs arg-contract-adoption.test.mjs` 13 pass / 0 fail after the census re-pin (the adoption census walks `CONTRACTS` generically, so the three new rows are exercised on both axes). `lease-check` ok, staged 3, declared 8. |
| 4: The `memory.backend` third state | 67288e98 | The handler reads the effective backend across the config layers with the `builtin` schema default. Verified on two constructed fixtures with `CADENCE_GLOBAL_CONFIG` pointed away from the machine's real global layer: `memory.backend: none` exits 0 with `"backend":"none"` and no `--payload` passed; a fixture with no `memory.backend` key exits 1 with `ok:false` naming the missing `--payload`. A payload passed on the `none` path is ignored (`cmdRecall` ignores its query on the same arm) and the `builtin` happy path is byte-unchanged. `warnings[]` rides the envelope when non-empty - a torn layer reads the key as absent and would default a deliberate `none` back to `builtin`. self-verify `ok:true`, `problems: []`; tsc clean. |
| 4b: one reader for `memory.backend` | 696fd59f | Caused by task 4 and found at task 5's suite run: the new inlined `mergeLayers` call made `self-verify.test.mjs`'s check-12 census read 16 callsites over 9 files against its pin of 15. That file is outside this plan's lease. Rather than checkpoint for a second census re-pin, `cmdRecall` and `cmdCiteCount` now share one `memoryBackend(dir)` reader, so the callsite count returns to 15/9 and the off switch cannot come to mean two things at two seams. The census's own comment says a new callsite must choose an arm; the arm chosen is to add none. `recall`'s envelope and the three `cite-count` states re-checked byte-for-byte after the change. |
| 5: The seam's stated behavior, at the CLI | c96796bb | Five `cite-count` cases in `planning.test.mjs`, on scratch trees via `makeTree` and the real binary, with the global config layer pinned off a nonexistent path the way the `recall` runner does. AC1: both sides per item, `cited.ids` an explicit list and a subset of `surfaced.ids`, the four arms asserted with `'cited' in arm === false` on the three unjoinable ones, and the arms proved to sum to the headline. AC2: both directions on ONE payload. AC6: two spawns byte-compared on raw stdout, and the fixture's `trace.jsonl` read back and proved to hold no `lifecycle`/`dispatch` - seeded with an anchor event so an empty file cannot pass it vacuously, and asserted on that event class rather than on byte-identity, so plan 2's `outcome` append will not redden it. Plus the three `memory.backend` states separated by their recorded fields alone. `node --test cadence-core/bin/planning.test.mjs` 462 pass / 0 fail. Falsifier run as the task's `Verify:` requires: deleting the own-phase exclusion from `lib/cite-surfaced.mjs` fails EXACTLY the AC2 case, 4 pass / 1 fail, and the module was restored (`git diff` clean on it). Whole tree after: `node cadence-core/bin/test.mjs` 2839 pass / 0 fail; self-verify `ok:true`, `problems: []`; `npx tsc -p tsconfig.ci.json` exit 0. |

Deviations:
- [deviation] Task 3's `Verify:` requires `node --test cadence-core/bin/arg-contract.test.mjs` to report 0 failures, but `arg-contract.test.mjs:303` pinned the table's total flag-entry count at `assert.equal(entries, 170, ...)` and the three new `cite-count` rows make it 173 - so the criterion was unachievable inside the plan's declared `files:` lease, which did not name that file. The prior dispatch returned a `structural` checkpoint on exactly this. The user APPROVED widening the lease: `cadence-core/bin/arg-contract.test.mjs` is now in PLAN-1's `files:` frontmatter and the pin reads `173`. Measured before and after: the whole-tree run had exactly ONE failure, this pin, and the tree is now 2839 pass / 0 fail. The frontmatter edit itself is left UNSTAGED for the orchestrator's docs commit; `lease-check` reads the working-tree plan file, so every task commit passed the gate.

Open items:
- Stated bound, visible for plans 2 and 3: a decision row's phase is the `<n>` of the `phases/<n>/` segment of its `source`, which every source the corpus walk can emit carries (`phases/<n>/...` live, `<label>/phases/<n>/...` archived). A hand-written `_archive-v*/<N>/CONTEXT.md` row has no such segment, so it reaches `surfaced` as AC2 requires but carries no phase and is therefore unjoinable. That is PLAN-1 task 1's rule verbatim; widening it to "any numeric segment" would join `v2/1/CONTEXT.md` to phase 2 or phase 1 depending on which end it read from.
- Also visible for plans 2 and 3, and locked rather than open: under D-04 plus D-10 a BARE `D-NN` mention can only ever match an ARCHIVED same-numbered phase's decision, since every live own-phase row is excluded from the surfaced set. It is the consequence of the two rules together and the handler says so in place; it is not to be repaired by widening either rule.
- `lib/arg-contract.mjs`'s header still says `arg-contract.test.mjs` walks "all 156" flag rows; the table now holds 173. The count was stale before this work and the plan's Notes deliberately left the correction out of these tasks. Unchanged here.
- On the `memory.backend: none` path a `--payload` that IS passed is read by nobody and nothing on the envelope says so. Left silent deliberately: `cmdRecall` ignores its query on the same arm, and a field saying "payload ignored" is the fourth field task 4 forbids. Named here so a later reader does not mistake it for an oversight.
