PLAN COMPLETE
Plan: .planning/phases/2/PLAN.md
Tasks: 3 of 3
| Task | Commit | Note |
|---|---|---|
| 1: Delete the rung sentence from the canonical body and every rung file | 03b89d0d | 19 agent bodies shed 36-39 B each and collapse to 1 distinct body per role (6 of 6 groups, bodies 141-154 B, matching CONTEXT's plan-time measurement). `rungBody(skill)` and `rungBodyIssue(body, skills)` lost the `rung` parameter; self-verify check 7's call site no longer scrapes `effort:` off the frontmatter. Per D-09 the two rows asserting the body-vs-frontmatter-effort arm were deleted from `rung-agent.test.mjs` and `self-verify.test.mjs`, each replaced by a comment recording that this is a REDUNDANT arm and not a hole - `rungEffortIssue` (check 7b) holds the frontmatter `effort:` against the rung `RUNG_FILES` filed the file under, runs on every agent file rather than only the ones preloading a contract, and is now the only rule reading that field; the same statement is in the lib's own doc comment and in check 7b's comment. Verify: `node --test` on both test files pass 207 fail 0; `grep -rn "Your rung is" agents/` and `grep -rn "and your rung" agents/` both empty; distinct-body count 1 for all six role groups; `self-verify --root .` ok:true problems 0, no `agent-carries-behaviour`, no `budget-overrun`. |
| 2: CI refuses a rung body that differs from its siblings by one byte | 143019e3 | `rungPrefixIssues(bodies)` stated beside `RUNG_FILES`, returning `{code:'rung-prefix-split', role, stems, detail}`; self-verify owns the envelope as check 7d after the `agents/` walk and names `rung-prefix` in the single `checked:` list. Raw-byte comparison per D-04; `normalizeBody` and `rungBodyIssue` untouched. Scoped by `RUNG_FILES`; a role contributing fewer than two bodies yields nothing; majority body is the rank so the detail names the file that broke rank. No `CADENCE-CENSUS` marker added. Verify: `node --test cadence-core/bin/rung-agent.test.mjs` tests 41 fail 0, carrying a row where ONE re-wrapped line break (same length, one byte) is reported while byte-identical bodies are not, plus a row asserting `rungBodyIssue` forgives that same input (the two rules disagree on purpose); `node --test cadence-core/bin/self-verify.test.mjs` tests 177 fail 0, with a fixture root naming two real `cad-executor` stems in which one body is re-wrapped and self-verify reports `rung-prefix-split` at `agents/cad-executor-xhigh.md` naming the role, while `budget-overrun` and `agent-carries-behaviour` both stay silent; `self-verify --root .` prints `problems:[]` with `rung-prefix` in `checked`. Live mutation probe on the real tree: re-wrapping `agents/cad-executor-xhigh.md` alone turns it `ok:false` with exactly one problem naming that file and `cad-executor`; restoring returns `ok:true`. |
| 3: The plan checker's rung arrives in its dispatch prompt | 8ca0dfdc | `<rung>` rewritten: the dispatch prompt names the rung, the four-rung vocabulary (`low`, `medium`, `high`, `xhigh`) is kept, and the block's one real instruction is kept verbatim in force - harder reasoning, stricter borderline BLOCKER vs WARNING, what is checked and how it is reported identical at every rung. Both dispatch sites now carry `Rung: {the effort that resolve returned}` - `workflows/plan.md`'s `check_gate` `<verification_context>` beside its `Task ceiling:` line, and `references/plan-revision.md`'s step-2 narrowed `--attempt 2` re-dispatch - with no rung name written into either prose. No `node` invocation and no `trace close` line added at either site; the `plan-revision.md` consult sentence untouched. Verify: `grep -n "agent file names your rung" skills/cad-plan-checker-contract/SKILL.md` empty; `grep -n "BLOCKER vs WARNING"` still hits at :26; `node --test cadence-core/bin/route.test.mjs` tests 160 fail 0 (all 18 (level, role) cells pinned, unmoved); `self-verify --root .` prints `problems:[]`; `npx tsc -p tsconfig.ci.json` exit 0 (`detect-commands` reports `lint: null`, so typecheck is the whole static-analysis step). Budget: `weight.mjs --root .` showed exactly three rows over after the edit, and only those three were raised - `references/plan-revision.md` 3863->4016, `workflows/plan.md` 29485->29572, `skills/cad-plan-checker-contract/SKILL.md` 6746->6820. The 19 shrinking `agents/` rows were left untouched per D-08. Both prose additions were trimmed once against the lean-first posture before the re-pin (plan-revision.md +340 B -> +153 B, plan.md +155 B -> +87 B). |
Deviations:
- [deviation] Task 3's Verify asserts `node cadence-core/bin/test.mjs` "reports every group green". It does not, and cannot: the run is 3391 tests / 3382 pass / 9 fail, and all 9 are PRE-EXISTING. Proven, not assumed - a detached worktree at the phase base commit `167272d5` runs the same 9 failures with the same names. What was done: the criterion's intent was verified in the form it can be met - the failing rows are all in `cadence-core/bin/planning-renumber.test.mjs`, none of them reads `agents/`, `lib/rung-agent.mjs`, `self-verify.mjs` or any file in this plan's lease, and the failure count and names are identical before and after all three commits. Every other clause of every task's Verify passed as written.
Open items:
- The 9 pre-existing failures above: eight `renumber remove` rows plus `PHS-01: an unreadable git state refuses the remove, a non-repo still removes`. `renumber remove --dry-run` against a plain temp fixture returns `unreadable-git-state` where the test expects the documented non-repo arm. It is not an environment artifact of the harness in the obvious way - `git status --porcelain` in a fresh `mktemp -d` here returns the ordinary "not a git repository" fatal and no `GIT_DIR`/`GIT_WORK_TREE` is set - so the classification itself looks wrong. Outside this plan's `files:` lease and untouched.
- `INTERNALS.md:11` says "Three things keep the price honest" about the rung-file gates. Task 2 makes it four. Nothing in that sentence became false, no acceptance criterion covers it, and `INTERNALS.md` is not in this plan's lease - carried forward exactly as the plan's own Notes flagged it.
