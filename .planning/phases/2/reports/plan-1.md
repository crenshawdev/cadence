PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1. The record body names the task it settles | 28c0819c | `task` added to the record literal, present only when the fire is a task's; `phase` left as the caller's spelling. Checked first that no arm of `planning-adjudication.test.mjs` asserts the record's whole key set - none does (the `deepEqual`s are on `counts`, `voices`, `citations`, entry subsets and file listings), so no assertion needed widening. Two new cases; targeted file 39 pass / 0 fail; `npx tsc -p tsconfig.ci.json` clean. |
| 2. Replace the hand-written settlement with one the seam produced | 346c9743 | Payload composed by a scratch script that reads the committed record and carries `claim`/`failure_scenario` across by reference rather than retyping them, so the seam's byte-identity check proves the round trip. Seam answered `ok:true`, raised 5 / survived 4 / downgraded 0 / refuted 1, `citations.checked` true with 0 missing. Written file: no `note`, `task` = `declines-off-the-tracker`, `phase` = `"0"`, 40-char `base_id`/`head_id` on the header and on all 5 entries, `counter_evidence` preserved. Re-run answered `record-exists`. `git status` showed exactly one modified tracked path. |
| 3. Refuse `--phase <N> --task <slug>` | b8e7e374 | Converse guard added beside the phase-0 one in `fireIdentity`, comparing the caller's own spelling. Two new cases: the refusal leaves BOTH `tasks/some-slug/` and `phases/2/` empty; phase 0 + slug and phase 2 + no slug both still write. Targeted file 41 pass / 0 fail. Census check: the new site is a hand-written `bad-args` detail, not the caught-error idiom `planning-detail-sites` counts, and adds no `requirePhaseArg(` callsite - `planning-lease-check.test.mjs` and `phase-spelling.test.mjs` ran green unchanged (71 pass / 0 fail), so no count was re-pinned. `npx tsc` clean. |
| 4. Recount a task settlement's counts | c59e0e26 | `recordForFire` resolves a home LIST: `phases/<N>/` for every phase but `0`, and every real directory under `tasks/` for `0`. Candidates counted across homes together, so two matching records still answer `''`; `readdirSync` on `tasks/` fenced behind an `lstat` so a symlinked `tasks` is not enumerated out of the tree; a per-home read failure `continue`s rather than returning. Three new cases: a wrong count under `tasks/<slug>/` refused `count-disagreement` with nothing appended, the seam's own figures accepted and appended, and the identical record under `deferred/3/` still omitting the check. Docblocks on `recountReceipt`, `overrideAccounted` and `fireHome` now name which homes "absent" is measured over; the `phase-spelling.test.mjs` comment restating the `phases/` join gained the phase-0 clause. Targeted file 44 pass / 0 fail; the three census holders 263 pass / 0 fail with no count re-pinned; `npx tsc` clean. |

Final suite: `node cadence-core/bin/test.mjs` (no `package.json` in this repo and `workflow.test_command` is null, so the project's own runner) - 3705 pass, 0 fail. `npx tsc -p tsconfig.ci.json` exit 0.

Deviations: none
Open items:
- `workflow.lint_command` is unset and `detect-commands` reports `lint: null` for this project, so there is no lint command Cadence can find and no lint step was run on any task. `typecheck` resolved to `npx tsc -p tsconfig.ci.json` and ran on all four.
- Declined a slug-carrying flag on the `trace append` receipt. The Verify resolves the task home from `--phase 0` alone and the four live `gate_pass` receipts carry no slug, so the walk over `tasks/` met it; add the flag when a repository holds two task records the head sha cannot tell apart, which is the only case the walk answers `''` on.
- AC7's `self-verify` and `weight-budgets.json` halves are not this plan's: no prose file was touched here, and the plan states AC7 rides whichever plan lands last.
