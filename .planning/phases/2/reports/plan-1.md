PLAN CHECKPOINT: blocked
Plan: .planning/phases/2/PLAN.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: Report markdown decoration on a declared path, keeping its bytes | 9ca79259 | `markdown-decorated-path` in `parsePlanFiles`' frontmatter loop only (D-06). Three shapes, one code, at most one issue per item; the declaration stays in `files` byte-exact (D-04). Reference row + weight re-pin 17312 -> 18700 in the same commit (D-11). Verified: 293 tests pass in `planning-files.test.mjs`, `weight.mjs` reports `plan-frontmatter.md` at exactly 18700, `self-verify --root .` `ok:true problems:[]`. |
| 2: Prove the collision reaches the plan-overlap envelope | 43c97fd8 | `plan-overlap --phase 1` over a bold PLAN-1 and a plain PLAN-2: `ok:true`, `overlaps: []`, `frontmatter_issues` = `[{plan:'PLAN-1.md', issues:[{line:6, code:'markdown-decorated-path', text:'- **src/shared.rs**'}]}]`. Falsified: removing task 1's push site reddens this test alone, `frontmatter_issues` coming back `undefined` (the envelope's empty form). 457 tests pass in `planning.test.mjs`. |
| 3: Gate the value-level codes to the two list keys | c103017d | `scopeToListKeys` at the four push sites in `parseFrontmatter`; no field added to `Issue`, nothing filtered in `readFrontmatterList`, so all five readers keep their envelope (D-09). Verified: `goal:` backtick-plus-trailing read for `files` -> `issues: []`; the identical value under `files:` -> `['trailing-value-content','backtick-wrapped-value']`; `unknown-line` inside `requirements:` still reaches a `files:` read; `unterminated-inline-list` under `goal:` still reaches it (bracket-level, not gated). 7 grammar rows added, 300 tests pass in `planning-files.test.mjs`; `planning.test.mjs` and `phase-plans.test.mjs` green. Preamble + weight re-pin 18700 -> 20460 in the same commit; `self-verify` `ok:true problems:[]`. |
| 4: Redden when a grammar code never reaches the reference table | d92ca270 | Code set derived from executable source between `scanValue`'s declaration and the end of `parsePlanFiles`, comments stripped, bounded by symbol; non-vacuity asserted against the 12 shipped codes plus `markdown-decorated-path`. Falsified: deleting the new row makes it fail naming `markdown-decorated-path` and the table that lacks it. 43 tests pass in `prose-agreement.test.mjs`. |

Deviations:
- [deviation] The plan's task 3 Action asserts that when a block item's value-level codes are scoped away with no block key open, "`item-without-key` still fires on that line under D-02, so nothing goes silent". It did not: the item arm returns early on a `scanValue` failure, BEFORE the no-block-key diagnosis, so `goal: something` followed by `- "unbalanced` reported `unterminated-quote` before the gate and reported NOTHING after it - a frontmatter line gone fully silent, which is the one outcome D-02 exists to prevent. Fixed inside task 3 by raising `item-without-key` on that early-continue path when no key is open, which is what the Action says is already true; pinned by a new grammar row. Behaviour change beyond the gate itself, in the loud direction.
- [deviation] CONTEXT's flagged assumption and the plan's Notes both state `detect-commands --root .` returns `lint:null, typecheck:null`. It returns `{"lint":null,"typecheck":"npx tsc -p tsconfig.ci.json","source":{"typecheck":"tsconfig.ci.json"}}`. Ran that typecheck before every commit in this dispatch: exit 0, no diagnostics, each time. Lint is genuinely null.
- [deviation] AC7's second half - `node cadence-core/bin/test.mjs` reports 0 failures - cannot be met from this plan's lease. See the checkpoint below. Its first half holds: `self-verify --root .` is `ok:true` with `problems: []`.

Open items:
- PRE-EXISTING, blocking AC7: `cadence-core/bin/milestone-prune.test.mjs:557` "corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair" fails with `REL-02 must be a wrapped bullet for this corpus to bite` (`milestone-prune.test.mjs:575`). Proved pre-existing by `git stash -u` at the start of this dispatch: it fails identically with every change of mine removed. It is a FIXTURE precondition, not a defect in `archiveRequirements` - the test skips when no phase is complete, and phase 1 being marked `[x]` in ROADMAP.md turned it on, at which point it found `.planning/REQUIREMENTS.md`'s `## Active` bullets for `REL-01`/`REL-02`/`REL-03` (lines 16-18) written as SINGLE lines, while the assertion demands a multi-line wrapped bullet. Every other one of the suite's 2673 tests passes. Repair is one of two edits, both outside this plan's `files:` lease: rewrap those Active bullets in `.planning/REQUIREMENTS.md`, or relax the precondition in `milestone-prune.test.mjs` to skip an unwrapped bullet the way the test already skips a milestone with no completed phase.
- Declined a shared decoration helper in `lib/lease-grammar.mjs` beside `isRefusedSpelling`: D-03 places the rule in `parsePlanFiles` and D-06 confines it to one arm, so `isDecoratedPath` is a local closure with exactly one caller. Promote it if a second arm or a second reader ever needs the same test.
- Declined a `markdown-decorated-path` row in the `parsePlanFiles`-level seam tests for the link and interior-pair shapes: task 2's `Verify:` asks for one `plan-overlap` envelope and AC5 names the decorated line, so the bold shape carries the seam test and `planning-files.test.mjs` carries all three shapes at parser level. Add the other two at seam level if a shape ever diverges between the parser and the envelope.
