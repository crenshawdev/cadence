PLAN COMPLETE
Plan: .planning/phases/2/PLAN-1.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Gate the fix-commit requirement on the raised severity | f728176e | `HALTING_SEVERITIES` declared in `lib/adjudication-record.mjs` and pinned against `lib/filing-decision.mjs` by a new drift row; the `survived` arm splits into an unconditional VALUE check (presence = key set, never truthiness) and a severity-gated PRESENCE requirement. AC3 rows narrowed to blocker/high with the severity explicit, never deleted. 39 tests pass; `npx tsc -p tsconfig.ci.json` clean. |
| 2 - Accept an overridden blocker or high on an explicit marker | f6bee0db | `overridden` added to `RULING_KEYS` and validated to the boolean `true` wherever the key is set, over every ruling - the same disposition the `fix_commit` VALUE check takes, and one check rather than a ruling-scoped one. It satisfies the presence requirement only; a `fix_commit` beside it is still validated. Added a typo row for `overriden` beside the `fix_comit` one. 46 tests pass; typecheck clean. |
| 3 - Stop the shared payload fixtures encoding the old rule | 68b7ad31 | `filing-decision.test.mjs` was the wrong one and its builder is now severity-conditional; `trace.test.mjs` and `planning-adjudication.test.mjs` raise at `high` so their payloads are left alone with the reason stated in their comments; `deferred-queue.test.mjs` pins `fix_commit` on a FINDING, a different grammar, and is unchanged (no diff). 241 tests pass across the four files; typecheck clean. |
| 4 - The GH-159 reproduction settles end to end | fb7a606f | One row over four real seams on a scratch repo: `risk-check run`, `planning.mjs adjudication`, `trace append --event gate_pass` with the envelope's own three figures, `risk-check status`. Status answers `recorded`; the stored entry is a `survived` `medium` with no `fix_commit` key. 21 tests pass; typecheck clean. |
| 5 - An overridden fire settles end to end | 154ef5fb | Second row, same four seams, `override` in place of `gate_pass` with the reason through `--detail-file` because `risk-check.mjs` skips an override receipt whose reason is empty. Stored entry: `survived` `blocker`, `overridden: true`, no `fix_commit`. 22 tests pass; typecheck clean; `self-verify.mjs` reports `problems: []` across 30 checks. Full suite green. |
| fix pass - risk_surface high (`lib/filing-decision.mjs:460`) | 3341ffb0 | `unfixedFindings` reads the `overridden` marker as a third field beside `ruling` and the raised `severity`, so an overridden blocker/high is IN the filing set rather than dropped silently. `fix_commit` still decides nothing here (D-07) - an entry carrying both is in the set and `cmdUnfixed` removes it in plan 2. Six rows added, including the finding's own payload and a non-boolean marker proving a refused payload is still a refusal rather than an empty set. 40 tests pass in the file; `npx tsc -p tsconfig.ci.json` clean; full suite 3547 pass / 0 fail. The two `medium` findings were left alone by dispatch. |
Deviations:
- [deviation] CONTEXT D-07 asserts `lib/filing-decision.mjs` keeps its stated "TWO FIELDS DECIDE IT AND NOTHING ELSE" rule; closing the risk_surface `high` requires that module to read the `overridden` marker, so the header now states THREE fields. D-07's actual subject is unchanged and stated in the comment: `fix_commit` still decides nothing here, and the fix-commit filter still lands in `issue-filing.mjs`'s `cmdUnfixed` as plan 2 task 1 has it. The alternative - keying the exclusion on `fix_commit` presence instead - would have put exactly the field D-07 excludes into this decision.
Open items:
- Task 2's `Verify:` also names `node --test 'cadence-core/bin/*.test.mjs'`, the full suite. Held to the dispatch's single full-suite site after task 5, which Task 5's `Verify:` names as well - the executor contract allows exactly one full-suite run per dispatch.
- Declined a `RULING_KEYS`-driven or per-key validator table for the new `overridden` marker: two inline checks (value, then presence) met every `Verify:` in tasks 1 and 2, and the module's existing arms are all written this way. A table becomes worth it when a third conditional ruling key is stated.
- The plan's own "Open items from the `plan` review" section named three confirmed-unfixed findings. All three were acted on while working: Task 1's "do not touch `RULING_KEYS`" was read as scoped to the unknown-key REFUSAL (Task 2 adds the key, and a `overriden` typo row proves the guard still fires); PRESENT is implemented as `!== undefined` with `null`, `0`, `''` and `false` rows at both a medium and a high proving it is not truthiness; and the entry comment is scoped to the marker-ALONE case, with a row asserting the marker beside a valid commit stores both keys.
- The two `medium` findings in `.planning/phases/2/REVIEW-risk_surface-plan-1.md` are untouched by dispatch instruction: the `downgraded`/`refuted` `fix_commit` value-check scope (line 450) and the unverifiable `overridden` self-assertion clearing a `gate_pass` with no reason on file (line 461).
