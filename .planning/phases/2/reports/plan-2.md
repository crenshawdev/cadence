PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1. Give `trace append` the three structured count flags | 697743f | Row declared first (D-12): `--survivors`, `--downgraded`, `--refuted` and `--round`, all `int`/`refuse`/`refuse`, on `trace append` and on NEITHER `trace close` NOR any other row. Body reads them beside `--raised` through `requireInt` (nothing appended on a malformed value, no comma-grouping exception); each key omitted when its flag was absent, `--survivors 0` stored as 0, `round` stored only when named. The cross-check: `recordForFire` resolves the D-06 path at the named round - `plan-<k>` from `--plan` on a per-plan fire, otherwise a single unambiguous directory hit on trigger + round + the head sha the discriminator ends with - and `recountReceipt` runs `deriveCounts` over the stored entries, refusing `count-disagreement` (both figures named, nothing appended) or `bad-record` on an unparseable file. The writer now builds its filename through the same `recordName` helper, so the two sides resolve one name by one rule. 10 new cases in `trace.test.mjs`, every record fixture written by the REAL `adjudication` seam against an `mkdtempSync` git repo. `trace.test.mjs` 122 pass / 0 fail; `planning.test.mjs` 425 pass / 0 fail; `arg-contract-adoption.test.mjs` green (253 declared refusals across 156 entries); `arg-contract.test.mjs` census literal 152 -> 156 with the header's stale "walks all 152" corrected. Typecheck clean; no lint command exists (`detect-commands` answers `lint: null`). self-verify `ok:true`, `problems: []`. |
| 2. Make the settle receipts carry the derived counts | d1d6e0a | The three counts plus `[--round <round>]` added to the `adjudication` line in `review-triggers.md` and the `gate_pass` and `override` lines in `triage-gate.md`, and to nothing else - `grep -n 'family outcome'` shows the `rearm` line unchanged and the one `trace close` line carries none of them. One clause per file states that the figures are the ones the record seam DERIVED and returned, never hand-counted and never folded into `--detail`, that the seam recounts the rulings against them, and that `--round` is omitted on an ordinary fire. GAT-04 green with exactly four outcome event names; `prose-agreement.test.mjs` 32 pass; `test.mjs prose` 0 fail. Weight rows re-pinned in the SAME commit from `weight.mjs --root .`: review-triggers.md 38493 -> 39268, triage-gate.md 11779 -> 12840. self-verify `ok:true`, `problems: []`. |
| 3. Render the Gates line from the record | d3a1494 | (a) `.planning/phases/<N>/ADJUDICATION-*.json` added to `read_record`'s scoped-artifact list, each-at-most-once rule and the do-not-re-read-the-trace sentence untouched. (b) The Gates line renders the fire's rulings COUNTED and compares them against the event's `survivors`/`downgraded`/`refuted`, with (c) a disagreement NAMED (both figures plus the record path) and a fire with no record reading `unrecorded`, synthesizing nothing. Four rules bullets carry the reasoning, including the read-back bound (one count, never the file dumped whole). (d) The Refuted line is byte-unchanged - `git diff` reports 29 insertions and exactly 2 deletions, the two replaced lines. `grep -n 'ADJUDICATION'` hits the artifact list (66), the Gates line (104) and the rules bullet (205); `grep -n '^Refuted:'` shows line 105 still sourced from SUMMARY deviations. report.md weight row re-pinned 15101 -> 17265 in the same commit. self-verify `ok:true`, `problems: []`; `test.mjs prose` 265 pass / 0 fail. |
| 4. Pin the recount and the render rule with tests | b4d00eb | (a) THE RECOUNT, three cases in `adjudication-record.test.mjs`: `deriveCounts` over a written record reproduces the three figures a fixture outcome event carries; flipping ONE entry's ruling makes the two disagree, decidable from the record and the event alone (and leaves `raised` unchanged, which is why `--raised` alone could not have caught it); and an edited record re-read from disk answers differently. Every fixture is the SEAM's own output - `planning.mjs adjudication` spawned against an `mkdtempSync` git repo, the written file read back - so the file header's "no subprocess" sentence was amended to state that exception and its reason. (b) THE RENDER RULE, one prose-agreement case over `workflows/report.md` read by each line's own anchor via `doc`/`sentenceAround`: the record is in the artifact list, the Gates line names it and says COUNTED and compares all three event figures, a disagreement is NAMED, the unrecorded arm is stated, and the Refuted line still reads SUMMARY deviations and carries no gate finding. FALSIFIED IN BOTH DIRECTIONS by hand and restored: deleting report.md's `A fire with NO record` bullet reddens the prose case (32 pass / 1 fail), and flipping the tampered entry back to its own ruling reddens the recount case (34 pass / 1 fail). Restored tree: `test.mjs prose planning` 1116 pass / 0 fail, `adjudication-record.test.mjs` 35 pass, `prose-agreement.test.mjs` 33 pass. Typecheck clean, self-verify `ok:true`, `problems: []`. |

Deviations: none

Open items:

- PRE-EXISTING, not caused by this plan: `cadence-core/bin/milestone-prune.test.mjs`'s "corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair" is the one failure in the full suite (2451 pass / 1 fail) - the roadmap names a completed phase while `.planning/REQUIREMENTS.md`'s `## Active` still reads "No cycle open", which is CONTEXT's fourth flagged assumption, deliberately uncorrected. No file this plan touched is read by that test; PLAN-1's report carries the same row.

- The `bad-record` refusal deliberately does NOT quote the caught parse error. `planning.test.mjs`'s redaction census pins the `e && e.message ? e.message : String(e)` idiom at exactly 10 sites, 4 of them wrapped, and that file is outside this plan's lease; the detail names the record path instead, which is what a caller acts on, so no censused idiom was added and no lease widening was needed.
- AC6's human-verify arm is unmet from here: it needs a live fire, then `/cad-report 2` showing a Gates line whose survivor count came from the record and `/cad-report 1` showing a pre-format phase as unrecorded.
- `lib/trace-suggest.mjs`'s `parseAdjudication` still reads survivors out of `--detail` and does not read the three new structured counts. CONTEXT puts that reader explicitly out of scope (its own D-03), so the wiring is left for the human.
