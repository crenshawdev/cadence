PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1. read and decide the whole write set, then run the writes as one transition | b03e2f1b | 33 tests / 0 fail, `release-bump.test.mjs` byte-untouched; first `atomicWrite(` at line 278 against the last `readManifest(` at 220 and the changelog read at 258; `grep -c runTransition` = 2; tsc exit 0. A failed step is re-raised here so this commit keeps today's `internal` rendering rather than shipping an interim ok:true-over-a-throw; task 4 replaces the re-raise with the partial envelope. |
| 2. an unreadable sibling refuses the whole run, under its own code | 1b36a7ca | 34 tests / 0 fail; `bump: a SIBLING that would downgrade is recorded as a refusal, not silently written (D-08)` passes BY NAME; `git diff -U0` removed-line count 0; tsc exit 0. Code spelled `unreadable-sibling-manifest` as the plan proposed. The seam header's D-08 exception paragraph moved with the code list, since it asserted the now-false "a SIBLING manifest's refusal leaves top-level ok true" for the unreadable case. |
| 3. the CHANGELOG joins the validated write set | c7921b29 | 35 tests / 0 fail; `bump: rewrites only version...`, `bump: a second run is a noop...` and `bump: one run puts the staged Unreleased body INSIDE the dated section` all pass BY NAME; removed-line count 0; tsc exit 0. Code spelled `unreadable-changelog`. The fixture is built with `fixture({changelog:null})` + `mkdirSync` rather than `rmSync` + `mkdirSync`: same filesystem-shaped, uid-independent directory-at-the-path forcing (D-02), and it needs no new `node:fs` import, which is what keeps the removed-line count at 0. `readText` is now unused in this seam and its import is gone, with the reason recorded where it sat; `lib/seam-input.mjs` is untouched. |
| 4. a write that fails anyway names what landed | bbef0931 | Probe-proven with a throwaway `cadence-core/bin/release-bump-probe.mjs` whose CHANGELOG thunk throws: `{"ok":false,"action":"partial","reason":"partial-bump","target":"2.0.0","manifest":{"from":"1.0.0","to":"2.0.0","bumped":true},"siblings":[],"changelog":{"changed":false,"promoted":false,"section_empty":true},"detail":"probe: forced changelog write failure"}` at exit 1, with `plugin.json` on disk at 2.0.0 and `CHANGELOG.md` byte-untouched. Probe deleted before the commit, `git status --porcelain` listed only `release-bump.mjs`; `helper-census.test.mjs` 10 / 0 fail after the delete; 35 tests / 0 fail; tsc exit 0. `partial-bump` added to the header's SEAM-level code list as well as the refusal-envelope paragraph, so the file's own "a machine code on EVERY path" rule stays true. |
| 5. the halt prose, the budget and the ledger move with the behaviour | 0be88849 | `self-verify --root .` `ok:true` with `problems: []` and `budgets` in `checked`; `wc -c cadence-core/workflows/milestone.md` = 14937 = the re-pinned `weight-budgets.json` entry (was 14222); `unreadable-sibling-manifest` 2 hits, `unreadable-changelog` 1, `partial-bump` 1 in milestone.md; both new codes present in the MILESTONE-06 row; `grep -n "the primary manifest already wrote"` prints nothing; `Four halts` 1 / `Three halts` 0; tsc exit 0. Full `cadence-core/bin/*.test.mjs` run: see the deviation below. |
Deviations: [deviation] Task 5's Verify asserts `node --test 'cadence-core/bin/*.test.mjs'` exits 0. Observed: 2640 tests, 2639 pass, 1 fail - `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair` (`cadence-core/bin/milestone-prune.test.mjs:575`, "JRN-01 must be a wrapped bullet for this corpus to bite"). It is pre-existing at the dispatch base `5dd8cdca` and cannot be caused by this plan: `git diff 5dd8cdca..HEAD` is empty for `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `cadence-core/bin/milestone-prune.{mjs,test.mjs}` and `cadence-core/bin/lib/`, and that test reads nothing this plan changed. The cause is this cycle's own seeded corpus: `.planning/REQUIREMENTS.md:16` states `JRN-01` on a single unwrapped line, and the corpus assertion requires a wrapped span. What I did: left it, because the repair is `.planning/REQUIREMENTS.md`, which is outside this plan's `files:` lease, and every other clause of that Verify passes. Filed as an open item below.
Open items: (1) `.planning/REQUIREMENTS.md:16` - wrap the `JRN-01` Active bullet across lines, or relax the corpus assertion; one pre-existing red in `milestone-prune.test.mjs`, red at the dispatch base, outside this plan's lease. (2) `cadence-core/bin/lib/release-decision.mjs`'s JSDoc names this seam's code set as `(no-plugin-manifest, unreadable-manifest, usage, internal)` and was ALREADY missing `bad-date` and `missing-flag-value` before this phase; the three codes added here (`unreadable-sibling-manifest`, `unreadable-changelog`, `partial-bump`) are deliberately not added there - the plan scopes that file out and names the owner of the seam code list as `release-bump.mjs`'s own header, which did move. Carried, not introduced. (3) `partial-bump` ships with no committed regression test by design: tasks 2 and 3 convert every uid-independent forcing shape into a pre-write refusal and D-02 forbids `chmodSync`, so the arm is probe-proven only - the plan's own Notes state this, and it is not a coverage gap to file. (4) Declined a fuller `readChangelog`: no `lstatSync` regular-file probe, because `readFileSync` already throws `EISDIR` on the directory shape the Verify drives and on the permission wall, so the three states are reached without a second syscall; add the probe when a task names a shape (a fifo, a dangling symlink) that `readFileSync` classifies differently. (5) `cadence-core/bin/release-bump.test.mjs:424-428` still names the retired `optionalFlag` in a comment about the `--date` seam; in this plan's lease but deliberately untouched, as the plan's Notes direct.
