PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1: `unfixed` stops offering findings whose fix is already committed | dda38c4c | `cmdUnfixed` removes every entry carrying a `fix_commit` before the forge is touched; new `already_fixed` envelope key, `raised` and `already_declined` unchanged in meaning. `payloadFor` in the test now attaches `fix_commit` only at blocker/high. AC4 row added, reproducing the archived `medium survived HASFIX` entry. 34/34 pass, `self-verify` clean. `self-verify.test.mjs` needed no re-pin (no `mergeLayers` callsite moved). |
| 2: A survivor carries whether it was fixed | 40a63ff4 | `parseAdjudication` returns `fix_commit` read through the same `str` helper `counter_evidence` uses; the D-11 block comment records that a commit-less survivor is a legitimate stored state and that the reader gained a field and no rule. Two new rows: two survivors differing only on the field, and an earlier-writer record parsing identically with the commit carried. 36/36 pass. |
| 3: The renderer tells a fixed survivor from a confirmed-unfixed one | 60c80e21 | `findingLines` prints a `fix:` line on every survivor - the commit id where one exists, `fix: none - confirmed and left standing` where none does. `fieldReview`'s doc comment records the fourth distinction and why the three answers and the `kept` count are unmoved. 21/21 pass, including the byte-budget rows over the worst measured chain. Corpus check: all 8 `ADJUDICATION-*.json` under `.planning/` read `ok: true`, no issue code, through `parseAdjudication` + `deriveCounts(record.entries)`. |
| 4: `filing-decision.mjs` and `triage-gate.md` state the widened meaning | 128e1381 | Comments only in `filing-decision.mjs` - the `HALTING_SEVERITIES` doc gains the remainder sentence and `unfixedFindings`'s body is untouched (D-07). The `blocking` bullet in `triage-gate.md` gains it in that file's own voice, with the entry mechanics left pointing at `references/review-record.md`. `weight-budgets.json` re-pinned 22348 -> 22604 in the same commit. `self-verify` reports no `budget-overrun` and no `unbudgeted-surface`; `prose-agreement` + `filing-decision` + `self-verify` tests 275/275. |
| 5: `review-record.md` tells a coordinator how to compose both cases | 0b8af4a1 | The unconditional "a `survived` one names the fix commit" is retired (grep returns nothing); replaced by the severity-gated rule, the override case naming `overridden: true` - the spelling read out of `RULING_KEYS` - and the pointer to `triage-gate.md`'s `override` settle point for where the user's reason goes. The "no fourth value" sentence is untouched. `weight-budgets.json` re-pinned 5977 -> 7259. `self-verify` clean; `prose-agreement` 58/58 including the GAT-04 row that reads this file's fenced receipts. |
| 6: A drift row reddens when one surface is edited apart from the others | bef7687d | One row, `RSK-07`, over the four surfaces: each must state `confirmed and NOT fixed` (whitespace-collapsed, case-insensitive) and none may state that a `survived` finding names its fix commit without a severity in the same sentence. Falsified per surface: four mutations, four red runs, each message naming the mutated file, four reverts. The negative arm was falsified separately by restoring the retired sentence in `review-record.md`, which reddens naming it. Tree restored before the commit. |
Deviations: [deviation] Task 3's Verify says 7 `ADJUDICATION-*.json` files exist under `.planning/` today; `find` returns 8. The eighth is `.planning/phases/2/ADJUDICATION-risk_surface-plan-1-r2.json`, the untracked record this phase's own plan-1 review wrote after the plan was authored. All 8 were checked and all read `ok: true` with no issue code, so the criterion is met over a larger set than it named.
Open items:
- `workflow.lint_command` is null and `detect-commands` finds no lint command for this project. `npx tsc -p tsconfig.ci.json` is the only static analysis Cadence can resolve here; it ran clean after every task.
- Task 4's `triage-gate.md` edit is scoped as the plan wrote it, so `references/triage-gate.md:275-281` still enumerates the returned set as excluding exactly two things. After Task 1 a survived `medium` carrying a voluntary `fix_commit` is also excluded, and Task 6's row pins the meaning of `survived` rather than that set statement, so nothing reddens. This is the plan's own second recorded open item, unchanged.
- Declined a configurable spelling for the `fix:` renderer line and for the `already_fixed` envelope key - both `Verify:` rows turn on the literal text, and a key name behind an option is surface no task asks for.
- The plan's other two pre-recorded open items are untouched by this dispatch: the override case still has no consumer in `issue-filing unfixed`, and AC7's second half covers only the records on disk, not the 3 recoverable from git history alone.
Full suite: `node cadence-core/bin/test.mjs` (the command `.github/workflows/test.yml` runs; `workflow.test_command` is null and this repo has no `package.json`) - 3552 pass, 0 fail.
