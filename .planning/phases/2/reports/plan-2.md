PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1: The seam records its own count | a234348e | `cmdCiteCount` appends `family:'outcome'`, `event:'cite_count'` through `appendEvent` before the envelope is emitted, `cmdRiskCheckRun`'s precedent exactly: the caller's own phase spelling, `point` when given, `backend` when `none`, both figures with their id lists and `cited_by_kind`. No `role`, `tokens` or `turns` - it opens no bracket. `{written, reason}` rides the envelope as `trace`, present on every path, reason only on failure. Two new cases in the `cite-count` section: the event lands under the anchor-derived `corr` `2-deadbee` carrying both figures deepEqual to the envelope's, `phase` as the string `"2"`, and no lifecycle fields; a 1,048,576-byte trace returns `{written:false, reason:'size-cap'}`, appends nothing, exits 0, and every other envelope field is deepEqual to the same run against a clean tree. `node --test planning.test.mjs trace.test.mjs` 586 pass / 0 fail. Fixture run read back by hand: the last trace line is `{"corr":"2-deadbee","phase":"2",...,"family":"outcome","event":"cite_count","point":"planned",...}` with `surfaced.count` 2 and `cited.count` 1. `npx tsc` exit 0. |
| 2: The surfaced set reaches disk as this run's payload | 22eede36 | `spawn_planner`'s memory call gains `D="$(mktemp -d "${TMPDIR:-/tmp}/cad-plan-XXXXXX")"`, a `run-token` written beside it, `> "$D/surfaced.json"` on the call, a `cat` chained with `&&` so the step still builds its block in one invocation, and one `echo` of the directory and the token. The registered call string is intact: `lib/bulk-output.mjs` strips the redirect before matching, so the existing `transport: 'none'` row still classifies it and no register edit was needed. self-verify reports NO `bulk-output-unregistered`, `bulk-output-inline`, `scratch-shared-path` or `scratch-fixed-target` for this file - its only problem at this point is `budget-overrun`, which task 6 re-pins by design. Block run by hand against this repository: printed the envelope, echoed `scratch dir: /tmp/cad-plan-c4o8rL  run token: 280711-1787491188`, and `surfaced.json` in that directory parsed `ok=true results=5 total=176` beside the token file. `npx tsc` exit 0. |
| 3: Both count points | b0eaab6b | Two new steps: `count_planned` between `check_size` and `check_gate` (`--point planned`), `count_committed` after `commit` (`--point committed`), each stating its own placement reason from D-05. Each block is ONE seam invocation preceded by the `[ "$(cat ".../run-token" 2>/dev/null)" = "<the echoed run token>" ] \|\| { echo "scratch-stale: ..." >&2; exit 1; }` guard - the shape `references/review-triggers.md` already ships. The `none` arm is stated at both: no search ran and no scratch directory exists, so the token check and `--payload` are dropped and the seam answers backend-off. Advisory stated at `count_planned` and scoped to both - never refuses, never re-dispatches, never edits, and a non-zero exit is reported while the workflow continues. self-verify reports NO `unknown-subcommand` and NO `unknown-flag` for the file. The census regex run over the edited file measures exactly 11 - 9 before this phase plus these two. |
| 4: The under-threshold path counts too | 2eea242c | `inline_plan` now runs the WHOLE memory block (so `surfaced.json` and `run-token` land in this run's echoed directory) and REFERENCES `count_planned` and `count_committed` by name rather than restating either call, with the reason in place: a second spelling is a second seam invocation the census counts and a second copy that can drift. Census regex re-measured after the edit: still exactly 11, which is the proof the inline arm added no third and fourth invocation. `npx tsc` exit 0. |
| 5: The report, and what it may not do | b419b399 | `done` gains one `Citations:` line carrying both counts and the three states apart, with the zero case spelled `it surfaced S and the plan cited ZERO of them` rather than left as a `0` to derive, plus `, record not written: <reason>` where `trace.written` is false. Two paragraphs follow: the three states named against the fields that separate them, and the advisory restated once - not refused, not re-dispatched, not edited, one suggestion unchanged, no branch on the count. Mechanical half of the Verify run on a constructed zero-citation fixture (a phase-4 plan citing nothing against a 2-row payload): both points return `ok:true` exit 0 with `surfaced.count` 2, `cited.count` 0, `cited.ids` `[]`; the plan file is 52 B and sha256-identical before and after both calls; the seeded `lifecycle`/`dispatch` count stays at 1 while `outcome`/`cite_count` goes to 2. The session half is an open item below. |
| 6: The two re-pinned rows | ba076f30 | `weight.mjs --root .` measures `cadence-core/workflows/plan.md` at 29,044 B (7,261 est. tokens); that exact figure is the new `weight-budgets.json` entry - measured, not a rounded-up allowance. The `seam-calls.test.mjs` CENSUS row goes 9 -> 11 with the arithmetic on the row's `note` AND a new derivation block in the file's header, stated beside the ENF-01 figures rather than folded into them: +2 for the two count points, and NOT +4 because `inline_plan` references them. `node --test seam-calls.test.mjs` 3 pass / 0 fail. Falsifier run as the Verify requires: deleting `count_planned`'s invocation line makes exactly that row fail (2 pass / 1 fail) and the file was restored (`git diff --stat` empty). Whole tree after: `node cadence-core/bin/test.mjs` 2841 pass / 0 fail; `self-verify --root .` `ok:true`, `problems: []`; `npx tsc -p tsconfig.ci.json` exit 0. |
Deviations:
- [deviation] Task 5's `human-verify:` clause was NOT taken as a `<checkpoints>` stop. It asks for a `/cad-plan` run on a live phase and its printed report block read back - a workflow the HOST executes, which an executor cannot invoke - and the plan's own Notes say so. The mechanical half is complete and asserted in the row above (zero-citation envelope, byte-identical plan file, dispatch count unmoved); the session half is the first open item below, with the evidence needed to close it in one look. Same routing this repository's phase-1 plan-1 executor took this cycle, and the v2.5.0 phase-1 and v2.6.0 phase-2 executors before it. Task 6 was run afterwards rather than left undone, because it depends only on `plan.md`'s FINAL bytes - which task 5's edit settled - and stopping short of it would have left `self-verify` red on `budget-overrun`, which AC7 forbids.
Open items:
- HUMAN-VERIFY, task 5: run `/cad-plan` on a live phase whose plan cites zero of a non-empty surfaced set, and confirm three things in the session - the `done` report's `Citations:` line names both counts and says the zero case in words; `git diff --stat` over the run shows the plan file's bytes unchanged by `count_planned`; and the run's `trace.jsonl` holds exactly ONE `lifecycle`/`dispatch` for `cad-planner`. The seam-side comparand is already measured at ba076f30 on a constructed fixture: `cite-count --phase 4 --point planned|committed` returns `surfaced.count` 2 / `cited.count` 0 / `cited.ids` `[]` at exit 0, the 52-byte plan file is sha256-identical across both calls, and the two `outcome`/`cite_count` events land beside an untouched dispatch event. So the human check is about the WORKFLOW's wiring - that the echoed directory and token reach both steps - and not about the count.
- DECLINED a prose-compression pass over `cadence-core/workflows/plan.md`. Tasks 2-5 added 6,406 B (22,638 -> 29,044), and roughly 1.3 KB of that is the literals-and-token rule and the advisory restated across `spawn_planner`, `count_planned` and `done`. Every one of those paragraphs is named by a task `Action` (D-14 anticipated the re-pin and task 5 mandates the advisory repetition), so trimming would have traded a stated thing for bytes. The lean form to build if a later task states it: state the carried-literal rule once at `spawn_planner` and let both count steps cite it, which is worth about 1.3 KB of eagerly-loaded workflow prose on every `/cad-plan` run.
- On the `memory.backend: none` arm the workflow drops `--payload`, and a payload that IS passed there is read by nobody with nothing on the envelope saying so. Unchanged from plan 1's record of the same thing; named again because this plan is what puts a caller on that arm.
