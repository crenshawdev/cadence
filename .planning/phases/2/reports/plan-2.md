PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: The safe range enters the shared flag guard | fc96026 | `requireInt` on `Number.isSafeInteger`; `requireCursorNumber` on a finite `<= MAX_SAFE_INTEGER` magnitude bound after the existing `CURSOR_SHAPE` test and `String(n)` round trip, both kept. `requirePhaseArg` inherits and gains no check. 12/12 rows green; all three new rows go red when both guards are reverted in a scratch copy. `planning`/`route`/`trace`: 612 pass, 0 fail. |
| 2: The tracker readers stop answering about a different issue | 45be702 | `normalizeNumber` returns `null` outside the safe range on both arms (its existing "no readable number" answer, which `normalizeList` and `readOneIssue` already act on); `scanIssueRefs` excludes the reference rather than carrying `Infinity`. `normalizeList` still fails the WHOLE read per D-11. Tested through `HOST_TABLE.*.normalize` and `scanIssueRefs`, not by exporting the helper. 21/21 green; `issue-check.test.mjs` 30/30. |
| 3: The two phase-number readers in the planning file layer | 6c4e618 | Round-trip predicate `String(Number(x)) === x` at `parseCaptureSnippets` (no `phase`, tag left in the text) and `parseArchiveRows` (row skipped, `phase: number` return shape unchanged). Four new capture-tag rows plus one archive row, including `9007199254740990.1`, the value a magnitude bound alone would have passed. 278/278 green. |
| 4: `seed-reqs` and `cursor set` refuse a spelling that cannot round-trip | 6c43389 | One predicate, `phaseSpellingRefusal`, written once beside `read()` and called from both faces after `requirePhaseArg` and before any read or write; existing `bad-args` reason, detail quoting both spellings. Fixture with `phases/1.1` and `phases/1.10`: both faces refuse `1.10` at exit 1 with REQUIREMENTS.md and STATE.md sha256-identical, `2` / `2.1` still succeed at both, `phase-done --n 02` still boxes Phase 2. Full suite 2276 pass, 0 fail; `tsc -p tsconfig.ci.json` clean. |
Deviations: [deviation] Task 4's `Verify:` runs the accepted arm as `cursor set --phase 2.1 --status planning`; `planning` is not in `CURSOR_STATUSES` (`ready to plan | context gathered | planned | executed | phase complete | paused`), so that literal answers `bad-status` and can never succeed. The refusal arm is unaffected - the phase spelling is refused ahead of the status check - and the accepted arm was run with `--status planned`, which succeeds at both faces for `2` and `2.1`.
Open items:
- One pre-existing row in `planning.test.mjs` moved with the face it covers: `seed-reqs: --phase 08 reports no-phase-dir naming phases/08` pinned the OLD way the `08 != 8` claim was held (read `phases/08`, report the missing directory). It now asserts the `bad-args` refusal and keeps the `--phase 8` arm. The sibling `plan-overlap --phase 08` row is untouched and still answers `no-phase-dir`, which is what shows the refusal is scoped to the two write faces.
- `cadence-core/references/capture-grammar.md` is the stated prose home of the capture-tag grammar and its `## Out of grammar` table has no row for a phase number that does not round-trip; the four new cases are pinned in `planning-files.test.mjs` only. The file is outside this plan's lease, so no row was added. Nothing binds the two mechanically - no `self-verify` check references that file.
- `planning.mjs`'s own `--dir` remains permissive (CONTEXT D-03, phase 4), and `requireInt`'s range guard changes nothing there.
