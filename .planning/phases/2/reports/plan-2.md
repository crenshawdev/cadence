PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1: The rule that decides which bracket a stopped subagent closes | e9a7912c | New `lib/subagent-trace.mjs` exporting `closeForStop(payload, render)`, pure. Three gates in order: `roleOfAgent` self-filter (D-08), adopt the newest `unpaired` row of that role and quote its `corr`/`phase`/`plan` verbatim (D-03), emit a bare `lifecycle`/`return` with no figure of any kind (D-01). `roleOfAgent` EXPORTED from `lib/read-trace.mjs` rather than copied. 6 new arms in `subagent-trace.test.mjs` plus 1 in `read-trace.test.mjs`; suite 3308 pass / 0 fail, tsc exit 0. |
| 2: Register `SubagentStop` and let it write the close | 518728f6 | New `bin/subagent-trace.mjs` on `read-trace.mjs`'s contract exactly: stdin payload, nothing on any stream, exit 0 unconditionally, every failure silent. Renders unscoped (the payload carries no phase), appends the rule's answer through `appendEvent`, which honours the adopted `corr`. `hooks/hooks.json` registers `SubagentStop` with NO matcher (D-08) and `timeout: 10` - measured 23 ms against a 1,040,275-byte / 4,510-line trace, so 10 s is two orders of headroom on a file the 1 MiB cap bounds. Empty `CONTRACTS` row added; flag-entries census re-pinned 19 -> 20 rows, entries still 190. self-verify `ok:true` 0 problems (no `uncontracted-script`, no `refusal-hints`); suite 3308 pass / 0 fail; tsc exit 0. Scratch-project pipe: a Cadence `agent_type` printed nothing, exited 0, appended one `lifecycle`/`return`, and `trace render` then showed that worker under `brackets` with `unpaired` empty; the same pipe with `general-purpose` appended nothing. `lib/refusal-hints.mjs` and its test were declared defensively and stayed untouched - check 22 never reported either new module. |
| 3: `self-verify` pins the hook event names Cadence registers | e8bd05c1 | New `lib/hook-events.mjs`: a hand-maintained `HOOK_EVENTS` register (one row per event, one line each on what that event is for) plus `hookEventIssues(root, rows = HOOK_EVENTS)`, wired as check 25 in `run(root)` with a header entry and a `hook-events` token on `checked`. A NEW check surface, not the `mdFiles` walk (D-07). Absent file lenient on a partial root, `missing-input` on a full install; unreadable OR shapeless JSON is ONE `unreadable-surface` issue and the run still completes. One direction only, no command-path check. 8 lib arms plus 5 CLI arms, including a renamed `SubagentStopped` reported `ok:false` with the exact spelling in the detail. Suite 3321 pass / 0 fail, self-verify `ok:true` 0 problems, tsc exit 0. |
| 4: The prose says who may write the trace, in all three places | b5a8aaf4 | All three sites corrected together (D-15). `execute.md` gains two `<guardrails>` bullets - the three writers enumerated (dispatch on `--bracket-read`, the hand-written `trace close`, the `SubagentStop` hook) and the hand-written close named a FALLBACK kept on purpose with its two reasons - and its body sentences now agree: "a dispatch that gets none of them is closed by the hook instead, so `trace render` reports as `unpaired` only a worker NEITHER writer closed", and the attribution sentence names both closing writers. `--duration-ms` added to `execute.md`'s close line under the same omit-not-zero rule, which now covers all three figures. `seam-spawn-agent.md`'s bracket rule: the close half is the caller's to WRITE because it alone sees the return, no longer the caller's alone. Budgets re-pinned to measured bytes: `execute.md` 31480 -> 33007, `seam-spawn-agent.md` 22127 -> 22963. `trace.test.mjs` needed no edit - `BRACKETING` still reads execute.md as 1 dispatch / 1 close. Suite 3321 pass / 0 fail, self-verify `ok:true` with no `budget-overrun`, tsc exit 0. |
Deviations: [deviation] Task 4's Action asks for the seam-side correction beside the bracket rule; the plan's own `prose-agreement.test.mjs` MSR-01 arm reads that rule as a SINGLE block terminated by the first blank line and asserts "a rule that drifted into a paragraph of its own is exactly what this refuses". The addition first landed as its own paragraph and reddened that arm (`turns_unrecorded` clause fell outside the extracted block). Merged into the one paragraph instead, which is what the ONE-statement rule requires; observed as a test failure and fixed inside the same task.
Open items: AC2 and AC3 are UNVERIFIED live and belong to UAT. Every automated arm of task 2's Verify passed, including the synthetic-payload pipe against a scratch project, but the plan's own **human-verify** clause needs a real Cadence dispatch that returns with NO hand-written `trace close` issued, then `planning.mjs trace render --phase 2` listing that worker under `brackets`. The execution environment cannot produce a live subagent dispatch, which the plan states directly. Nothing downstream in this plan depended on the outcome, so tasks 3 and 4 ran rather than halting on it.
Final state: `node cadence-core/bin/test.mjs` 3321 pass / 0 fail; `node cadence-core/bin/self-verify.mjs` `ok:true`, zero problems, `hook-events` on `checked`; `npx tsc -p tsconfig.ci.json` exit 0. `workflow.lint_command` is unset and `detect-commands` reports `lint: null`, so the typecheck arm is the whole of the static analysis Cadence can find here.
