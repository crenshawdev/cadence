PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1: the seam names what its spend figure excludes | 6c4e8ac | `SPEND_EXCLUDES` frozen export added to `lib/trace-suggest.mjs` with the drift-reason header; R5's `evidence` built from it (`kind:'info'`, `action:null`, same share arithmetic, no new key or flag). Fixture literal re-pinned, its arithmetic recorded in the test header: the `423,846 of 968,705 recorded tokens (44%)` figures are unchanged and only the trailing `; excludes ...` clause is new. `cadence-core/workflows/suggest.md` byte-identical (`git diff --stat` empty). |
| 2: `/cad-report` states what its figure excludes and prints the gap as its terms | 8e2ee9f | Shape-block spend line names the three sources in the export's own words; new `Gap terms, never a product:` line; EXCLUDES rule re-written to the three sources with `SPEND_EXCLUDES` named as their one source; gap-terms rule names the per-dispatch window figure AS A PROXY and forbids a ratio or single gap number; `burnrate` named as the comparator the USER runs, with the fetches-nothing rail restated; D-08's `--phase` scoping fact stated (12 distinct `corr` ids on this repo's phase 1); `turns` carried into the `Dispatches:` table and the render key list. `weight-budgets.json` re-pinned 6935 -> 9605 in the same commit. |
| 3: `/cad-progress --trace` stops calling the roles block a cost | 059493f | The one-`corr` clause replaced by what the counts actually span; the `roles` block described by what it carries (dispatches, the host's token figure off the returns, turns, and both unrecorded counters) and stated as not the run's price, pointing at `/cad-report` for the reason rather than restating its terms or comparator; the never-`0` rule kept verbatim in substance and extended to govern `turns_unrecorded`. `weight-budgets.json` re-pinned 8957 -> 9447 in the same commit. |
| 4: the watched FAIL on the seam half | cf00dcc | New check over a `render()`-built render (not the fixture) asserting the receipt names all three, read from the frozen export via a NAMESPACE import so an unpatched checkout reaches the assertion instead of dying at module link. Header carries `WATCHED FAILING AT 4b1d659`, the verbatim observed output, the stated guarded-read arm and the re-watch recipe. Watched: exit 1, 23 pass / 2 fail, the MSR-02 check failing on its assertion. |
| 5: the watched FAIL on the prose half | be04f89 | New check asserting `report.md` names all three at BOTH named anchors - the shape block's `Tokens on subagent returns (` line and the `What that token line EXCLUDES` rule - with the names imported from `lib/trace-suggest.mjs` through the same guarded namespace import. Header carries `WATCHED FAILING AT 4b1d659` with its own observed output and recipe. Watched: exit 1, 25 pass / 1 fail, on the assertion. On this tree, deleting `figureless returns` from the spend line reddens it (verified, then restored). |
| 6: the ledger rows travel with the change | 15610c8 | All seventeen settled: the eight flag-list rows now name `--turns` with `corrected - b118576`; `REPORT-05` and `PROGRESS-28` rewritten (`corrected - 8e2ee9f` / `corrected - 059493f`), `PROGRESS-15` re-stated to the live block; every anchor re-pinned to where the LIVE file states its claim, spot-checked with `sed -n`. Verdict cells left as run 1 recorded them; no row added. Dated paragraph added to the re-pinning header naming which rows moved, which three claims were rewritten and why. Four of the eight flag rows also stopped naming the inline `--detail "<what failed>"` that `v3.5.2` replaced with `--detail-file <path>` - re-stating a claim to a half-live flag list would have written a new false row. |
Deviations: none
Open items: none
