PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1: The queue reader - `planning.mjs deferred list` | 393a358 | `lib/deferred-queue.mjs` gains `isQueueName` + `queueIdentity` (pure); `planning.mjs` gains `readQueue` (the one derivation, two homes `phases/*/` and `deferred/*/`) and `cmdDeferredList`; `deferred list` row added to CONTRACTS. Verified by hand on a scratch tree: one member named by phase/trigger/discriminator/round with its finding count; the matching `ADJUDICATION` sibling drops it; `chmod 000` on a phase dir answers `ok:false reason:unprovable-queue` with the dir on `unreadable` and exit 1; `--phase 2` narrows. 6 new pure arms + 5 new seam arms (round-2 supersession, both homes, unreadable dir, unparseable/mis-filed/symlink members, ADJUDICATION symlink does not settle). Two hand-pinned censuses re-pinned in the same change: arg-contract flag entries 164->165, planning.mjs redactUrl detail sites 10/4 -> 11/5 with the fifth site's non-git reason stated. `test.mjs other` 554/555 (the one failure pre-existing, see open items), `test.mjs planning` 440/440, self-verify ok:true problems:[], tsc clean. |
| 2: `planning.mjs status` carries the unadjudicated count | a223b21 | `cmdStatus` reads the same `readQueue` derivation, emitting an ALWAYS-present `deferred: {members, findings, unreadable}` block beside `current`/`total`. No cursor status value, no `AGREE` touch, no drift kind (D-05). Verified: `status` on this repository carries the block with zero count and no members; a member under `phases/1/` is reported with its finding count and deep-equals `deferred list`'s own members; writing the `ADJUDICATION` sibling drops it to zero; an unreadable phase dir rides `unreadable` while `status` stays `ok:true`. No existing status test compared the whole envelope, so none needed the new key. `test.mjs planning` 867/867, self-verify ok:true problems:[], tsc clean. |
| 3: `/cad-land` refuses on an unadjudicated queue, on both publish arms | c844eea | The refusal block sits at the top of step 3, region `3` per `regionLabels`, ahead of both `**(a)` and `**(b)`; no step renumbered. Guardrails gain the one sentence. New `prose-agreement` arm pins region AND order (a call inside 3(a) never runs unattended; a call after either arm has already published), plus the `NOT land-cleanup.mjs gate` clause, the `STOP the land here` rule, the `unreadable` disposition and the triage-gate route. Falsifier watched: deleting the `deferred list` line reddens the arm with `cad-land step 3 no longer asks the queue what is still deferred`. Budget re-pinned 13273 -> 14853. `test.mjs prose` 269/269, self-verify ok:true problems:[]. |
| 4: `/cad-progress` reports the count and routes to the triage | f4baec7 | `deferred` named at all three sites - the `derive` bullet (with the ALWAYS-present rule and the never-from-the-cursor clause), the `report` fenced block's `Deferred:` line, and a new `route` row below every recovery/work row and above the three that end a cycle. No new `Status:` value, `reconcile` untouched. New `prose-agreement` arm pins the key at derive/report/route, the never-off-the-cursor rule at both reading sites, the row's ORDER against five neighbours, and that `reconcile` gained no deferred mapping. Budget re-pinned 11449 -> 13110. `test.mjs prose` 270/270, self-verify ok:true problems:[], `grep -n deferred progress.md` shows lines 38 (derive), 163 (report) and 185 (route). |
| 5: A deferring run leaves the cursor naming its queue, and commits the queue | 12ec391 | `execute.md`'s `state` step gains the deferring `--next-file` branch (pointer names the queue and its count; `--status executed` pinned) and stages `.planning/phases/<N>/DEFERRED-*.json`. One judgment inside the Action's intent: the pathspec is staged whatever `planning.commit_docs` says, stated in one clause - the plan's own reason (gitignored trace, uncommitted REVIEW sibling, gone on a fresh clone) holds under either value, and an untracked member also leaves the tree dirty at `/cad-land` step 2. New `prose-agreement` arm pins the branch, the transport reason, the `--status executed` pin and the pathspec. Budget re-pinned 29218 -> 30671. `test.mjs prose` 271/271, self-verify ok:true problems:[], `grep -n 'next-file' execute.md` -> 476, `grep -n DEFERRED` -> 493. |
| 6: The queue survives the prune - `planning.mjs deferred carry`, called from the close | 207f3a8 | `cmdDeferredCarry` moves `phases/<N>/`'s unadjudicated members to `.planning/deferred/<N>/`, basename preserved, settled ones left behind. Destination type checked AHEAD of the queue read (a symlink is followed by `renameSync`); an unreadable member refuses the whole carry with a hint naming `milestone-prune`; every destination checked before the first rename. Verified by hand on a scratch tree: carry moves one and leaves the settled one; `deferred list` finds the moved member after `phases/4/` is deleted; a second carry onto an existing destination answers `carry-exists` and moves nothing. 5 new seam arms in `planning.test.mjs` (the plan's task-level file list named `deferred-queue.test.mjs`, which is the PURE file per its own header, so the fs arms went where the git fixtures live). `milestone.md` step 3 calls it per pruned phase beside the `risk_surface` carry with the transient/committed difference stated. Censuses: arg-contract 165 -> 166, milestone.md budget 13307 -> 14222. `test.mjs other prose planning` 1697/1698 (the one failure pre-existing), self-verify ok:true problems:[], tsc clean. |
| 7: A carried member stays adjudicable AND re-armable after its phase directory is gone | 456aae9 | `firePhaseDir` -> `fireHome`, resolving `phases/<N>/` then `.planning/deferred/<N>/`, with the `lstatSync` check moved with the resolution and both write faces (`adjudication`, `deferred record`) off the one resolver. Each envelope's `record` path is now `relative(dir, file)` rather than the literal `phases/<N>/`. `recordForFire` left alone, asymmetry stated in the widened site's comment. 4 new arms: the carried triage lands `ADJUDICATION` beside the member and clears it, the re-arm writes `DEFERRED-diff-plan-1-r2.json` into the same home and re-queues at round 2, a live phase directory still wins over a stale carried home, neither home present refuses naming both with no home minted, and a symlink at the carried home is refused unfollowed. Falsifier watched: narrowing the resolver back to `phases/` alone reddens the carried-fire arm. `test.mjs planning other prose` 1701/1702 (the one failure pre-existing), self-verify ok:true problems:[], tsc clean. |
Deviations: none
Open items:
- `milestone-prune.test.mjs`'s `corpus: pruning this repository's own REQUIREMENTS.md needs no hand repair` fails on this working tree BEFORE this plan - it reads the live `.planning/REQUIREMENTS.md` and asserts every moved requirement is a WRAPPED bullet, and phase 1's completed `IVW-01` is a single-line bullet. No file this plan touches is in its read set. Pre-existing, carried from plan 1's report.
