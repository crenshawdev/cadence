PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1 - Declare the plan-time flag and re-pin the flag-entry count | 90578bcb | (dispatch 1, see reports/plan-2.1.md) `--plan-time` added to the `'lease-check'` CONTRACTS row as `required: false, type: 'boolean', value: 'fallback', bare: 'fallback'`, census re-pinned 184 to 185 in the same commit. |
| 2 - Add the plan-time arm to lease-check | ba3a6723 | (dispatch 2, see reports/plan-2.2.md) Branches on `opts['plan-time']` immediately after `parsePlanFiles`, above the `execFileSync` block, so nothing below `parsePlanFiles` is shared (D-11). No git, no staged set, no `no-staged-set` rule; `plan_file` is `planFile` separator-normalized rather than `repoRel`. Clean lease returns `ok` with phase/plan/plan_file/declared; a non-empty result emits directly with `reason: 'census-at-risk'`, a `censuses_at_risk` list carrying each entry's `missing` holder, `counts` and `asserted_by`, and a `hint`. `frontmatter_issues` factored into one `frontmatter` spread both arms use. No new `e.message` detail site. 21 pass / 0 fail on `planning-lease-check.test.mjs`, `npx tsc -p tsconfig.ci.json` exit 0. |
| 3 - Prove the arm against phase 5's PLAN-1 lease and prove it touches no git | 55c8ec1f | (dispatch 2) Five arms appended to `planning-lease-check.test.mjs`, all built on a NON-git `planTimeTree` fixture rather than `leaseRepo`. Phase 5's five declared paths transcribed with the `git show 6645ce4b:` citation in a comment; the at-risk SET asserted as exactly three - `planning-lease-check.test.mjs`, `self-verify.test.mjs`, `trace.test.mjs`. No-git arm shadows `git` on the child PATH with an argv-recording stub and asserts the log file was never created; the executes-nothing arm rewrites the SAME plan file with different Action prose and asserts byte-identical stdout; clean arm returns `ok:true` with `declared: 8`; a phase with no PLAN file still answers `no-plan`. Predicted 26 pass / 0 fail, observed exactly that. `npx tsc` exit 0; `helper-census`, `census-registry` and `prose-agreement` 64 pass / 0 fail. |
| 4 - Replay the arm over every historical plan and record the per-entry counts | 61856a09 | (dispatch 2) Replay arm walks `.planning/` (live `phases/`, every `_archive-v*/`, `tasks/<slug>/`) for `PLAN.md`/`PLAN-<k>.md`, parses each with `parsePlanFiles` and runs `censusesAtRisk` directly - the PREDICATE, not the seam, so 43 plans cost one process. Predicted 27 pass / 0 fail, observed exactly that. Measured 51 plan files walked, 43 declaring under `cadence-core/bin/`, bound 21. Counts: `planning-detail-sites` 15 (35%), `self-verify-merge-layers` 6, `trace-refusal-sentences` 6, `weight-budgets` 2, `deferred-reads-register` 2, the other four 0. The plan's four predicted figures (6/6/2/0) all confirmed at the same absolute counts over a larger corpus; it did not name `deferred-reads-register` (2) or `planning-detail-sites` (15). `.planning/phases/2/census-replay.md` written with one row per entry. `npx tsc` exit 0. |
| 5 - Make a commit-time census refusal distinguishable on the record | 5eff71c6 | (dispatch 2) `undeclared` split on a holder-keyed Map built from `CENSUSES`, joined by equality (one staged file against one holding file, no directory lease on either side). A refusal carrying a census holder emits `reason: 'undeclared-census-files'` plus `census_files`, a `trace: {written}` field and its own hint; a refusal carrying none keeps `undeclared-files` byte for byte. `appendEvent` fires on the census arm ONLY - family `outcome`, event `census_undeclared`, carrying the registry IDS and not the file list. Two fixtures assert the two halves land on opposite sides: the envelope has no `censuses`, the record has no `undeclared`, one trace line in the first case and no `trace.jsonl` at all in the second. Predicted 29 pass / 0 fail on the lease-check file and observed exactly that; `planning-lease-check` + `trace` + `prose-agreement` together 198 pass / 0 fail, so `trace.test.mjs`'s four-sentence census still passes un-re-pinned. `self-verify.mjs` 0 problems, `npx tsc` exit 0. |
| 6 - Fire the check from /cad-plan on both paths | 4051e3b3 | (dispatch 2) New `<step name="check_census">` at plan.md:303-341, between `check_size` (:277) and `count_planned` (:342), invoking `lease-check --phase {N} --plan <k> --plan-time` once per plan file. States the refusal and why the break from `plan-size`/`criteria-size`, the remedy (`files:` list, re-run to `ok:true`), the do-not-continue instruction and the say-it-out-loud rule. One sentence added to `inline_plan` naming the step, no second call spelling. Budget row re-pinned 29737 to 32050, that key alone. All four Verify clauses predicted and observed: self-verify 0 problems; `--plan-time` at exactly line 314, strictly inside (277, 342); inside 303-341 the `files:` remedy and `Do not continue` both present with 0 `AskUserQuestion` and 0 `too_big`; inside 194-226 `check_census` once and `--plan-time` zero times. `npx tsc` exit 0. |
| 7 - Re-pin the EXECUTE-10 citation the new arm moved | b04c7e5a, 6e7fca8b | (b04c7e5a from dispatch 2) The `undeclared-files` emit moved from 331-334 to 440-443 (`reason:` line through `hint:` line, the same four-line shape). Both sides updated: `.planning/DOCS-CLAIMS.md:855`'s `line` cell and `DOCS_CLAIMS_CITATIONS`'s `line`/`start`/`end`, `symbol` untouched, no other row changed. Predicted 5 pass / 0 fail on `citation-census.test.mjs` and observed exactly that; `sed -n '440,443p'` shows the emit. (6e7fca8b, this dispatch, on the amended lease) `cadence-core/bin/seam-calls.test.mjs`'s `plan.md` row re-pinned `calls: 11` to `12` with the derivation the file's header requires, stated as its own `11 -> 12` block rather than folded into the RBK-01 figures: `check_census`'s `lease-check --plan-time` is a NEW invocation (+1), and `--inline` references that step instead of restating the call, which keeps it +1 and not +2. The row's `note` now accounts for the twelfth as correct - the "A twelfth means a call came back" sentence became "A thirteenth ...", and the not-four sentence widened to name `check_census` alongside the two count steps, holding the file at 12 and not 15. Measured the 12 invocations directly against `plan.md` before editing: the twelfth is the `lease-check ... --plan-time` line at plan.md:303. Predicted 3 pass / 0 fail on `seam-calls.test.mjs` and observed exactly that. |
Deviations: [deviation] Task 2's `Verify:` asserts that a lease declaring `cadence-core/bin/planning/` plus `cadence-core/bin/trace.test.mjs` returns `ok:true`. Predicted `ok:false` with two entries remaining and observed exactly that: the registry plan 1 actually built has THREE rows whose subjects intersect `cadence-core/bin/planning/` - `trace-refusal-sentences`, `self-verify-merge-layers` (three of its twelve subject files live under `planning/`) and row (i) `planning-detail-sites`. Declaring one holder clears one row. Run A (declaring only `cadence-core/bin/planning/`) returned `ok:false`, `reason: 'census-at-risk'`, `declared: 1`, naming all three holders; run B (plus `trace.test.mjs`) returned `ok:false` with the remaining two; run C (plus all three holders) returned `ok:true`, `declared: 4`. What I did: took run C as the criterion's substance - a lease that declares the holders passes - and changed no code. The plan's own task 3 and its `## Notes` already state the three-name answer for the same subject pair, so this is task 2's example text lagging the registry, not the registry being wrong.

[deviation] The plan's `## Must be true when done` says `node cadence-core/bin/test.mjs` reports 0 failures. Dispatch 2 observed 3091 tests, 3090 pass, 1 FAIL - `cadence-core/workflows/plan.md instructs exactly 11 seam invocations` in `cadence-core/bin/seam-calls.test.mjs`, actual 12, expected 11 - because task 6's `check_census` step is a genuinely new `${CLAUDE_PLUGIN_ROOT}`-rooted invocation, so the count was right and the pin was stale. That file was outside the plan's `files:` lease and CONTEXT D-05 excluded it from the registry on the grounds that it states its numbers are "DERIVED, never baselined". Dispatch 2 stopped on a `structural` checkpoint rather than re-pinning outside the lease. RESOLVED between dispatches, both ways: PLAN-2's `files:` list was amended to include `cadence-core/bin/seam-calls.test.mjs`, and D-05 was corrected in CONTEXT.md to record that the "DERIVED, never baselined" self-description covers the header arithmetic and NOT the assertion, which compares `seamCalls(text)` against the literal `calls: 11` at `seam-calls.test.mjs:86` - a hand-written count the code must keep true, so the file IS a census by this phase's definition. What I did on the amended lease: re-pinned the row to 12 with its derivation (commit 6e7fca8b). Full suite now 3091 pass / 0 fail, `self-verify.mjs` `{"ok":true,...,"problems":[]}`, `npx tsc -p tsconfig.ci.json` exit 0 - every `## Must be true when done` clause holds.
Open items: (1) `cadence-core/bin/seam-calls.test.mjs` now warrants its own registry row, and this plan did not add one - `cadence-core/bin/lib/census-registry.mjs` is plan 1's module and PLAN-2's Context states this plan never edits it. The row the corrected D-05 implies: holder `cadence-core/bin/seam-calls.test.mjs`, counts the per-workflow seam-invocation figures, asserted by the two generated `${row.file} instructs exactly ${row.calls} seam invocations` tests, subjects `cadence-core/workflows/plan.md` and `cadence-core/workflows/context.md`. Until it exists, a plan that edits either workflow file is not refused at plan time for the count it will move - which is exactly what happened to THIS plan at task 6. (2) The same module's header still carries the pre-correction D-05 text at `cadence-core/bin/lib/census-registry.mjs:44-49`, naming `seam-calls.test.mjs:12-27` as the worked example of what is not a census and saying it "is deliberately absent from this table and owes no marker". CONTEXT.md's D-05 now says the opposite. Whoever adds the row rewrites that paragraph in the same commit, or the module documents a rule its own table breaks. (3) `seam-calls.test.mjs` carries no `CADENCE-CENSUS:` marker (D-06), so the marker-discovery test cannot see the omission - adding the row and the marker are one act, not two. (4) The header paragraph beginning "PLAN-2 task 6 stated 5 for `context.md`" refers to a PHASE 5 plan, not this phase's PLAN-2, and now reads as if it were about the plan that just re-pinned the row above it. Untouched: renaming it is prose churn on a file this plan leased for one number.
