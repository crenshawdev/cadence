PLAN COMPLETE
Plan: .planning/phases/2/PLAN-2.md
Tasks: 3 of 3
| Task | Commit | Note |
|---|---|---|
| 1 - Admit both on-disk re-arm spellings as the base trigger | 1eb2161 | `parseAdjudication`'s regex widened to an optional `re-?arm` clause between the trigger token and the colon; the return gains a `rearm` boolean beside `trigger`/`survivors`/`raised`, and the legacy `of <m>` clause is still read from immediately behind the survivor count on both spellings. Both on-disk rows re-pointed as parsed rows (one per spelling), the `unadjudicated` line still pinned `null`, and a new row pins that no OTHER embedded space is admitted. 21/21 tests pass; `npx tsc -p tsconfig.ci.json` clean. |
| 2 - Scope the re-arm veto and the survivor test to the fire | 9c36dc8 | R1's per-trigger lifetime totals replaced by a `fires[]` list in file order carrying `corr`, trigger, survivors, raised and the re-arm marker; an `outcome/rearm` vetoes the nearest PRECEDING unvetoed non-re-arm fire in its own `(corr, trigger)` group. R1 speaks on unvetoed zero-survivor fires and its evidence reads `<empty> of <total> adjudicated fire(s)`; the raised sum is over the empty fires alone and the two-arm split is unchanged. R2 left trigger-scoped. `MIN_FIRES_FOR_GATE_SUGGESTION` unchanged at 2, doc comment now says it counts unvetoed EMPTY fires. Falsified: flipping the veto loop to oldest-first fails exactly the nearest-preceding test (1 fail of 23) and nothing else. 23/23 pass; tsc clean. |
| 3 - Demonstrate R1 firing on this repo's live record | 093e894 | `node cadence-core/bin/planning.mjs trace suggest` over the real 541-event `.planning/trace.jsonl` emits `{kind:"suggest", subject:"risk_surface reviewers", evidence:"2 of 7 adjudicated fire(s), 0 survivors of 1 raised - the gate caught work; the reviewer set is what looks miscalibrated", action:"review.reviewers"}`. Recorded in `.planning/phases/2/R1-DEMO.md` with the ten `risk_surface` outcome lines verbatim, the suggestion verbatim, and the veto arithmetic. The floor was NOT moved. A regression test carries the same ten lines as string literals (no committed test reads the gitignored trace). 24/24 in-file tests pass. |
Deviations: none
Open items: none
