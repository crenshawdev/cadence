PLAN CHECKPOINT: structural
Plan: .planning/phases/2/PLAN.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Mark `git.auto_close` repo-layer-only in the schema | e6b7a8dc | `"repo_only": true` added to the `git.auto_close` spec, `src` and `purpose` untouched; `_meta.note` extended with the field's meaning and the authorization test a maintainer applies. Verify as predicted: `repo_only===true` filter prints exactly `["git.auto_close"]`, `src==='repo'` still 33, `config.mjs keys` ok:true, self-verify ok:true problems [], `test.mjs` 2960 pass / 0 fail. `npx tsc -p tsconfig.ci.json` exit 0; `workflow.lint_command` unset and `detect-commands` reports `lint: null`, so there is no lint command for this project - typecheck is the whole static-analysis answer here, stated once for the dispatch. |
| 2 - Refuse a marked key at the resolved user-global layer | 9e08b203 | `layerIdentity` exported from `lib/config-merge.mjs` (body and both `mergeLayers` uses unchanged; its header sentence naming the deleted `fsIdentity` refreshed to say why it is exported). `checkPairs(tokens, targetsGlobal)` refuses a `repo_only` spec AFTER `checkValue`, as a per-pair entry on the existing `errors` array. `set` resolves the layer from `create \|\| layerIdentity(file) === layerIdentity(GLOBAL_CONFIG)` ahead of every read and write. Six regression tests added. Verify as predicted, every arm: `--global`, `--file $CADENCE_GLOBAL_CONFIG` and the `/./` spelling all print the identical `detail[0]` with `--file` in the error and a non-empty `hint`; nothing written on the `--global` arm; a repo config takes the same pair (`ok:true`, file holds `true`); the two-pair global set left the target's sha256 unchanged; `set stakes=critical --global` ok:true. `test.mjs routing` 441 pass / 0 fail, `config.test.mjs` 90 pass / 0 fail, tsc exit 0. |
| 3 - `check --global` reports what the write face refuses | 5e8b4afe | `--global` declared on `CONTRACTS['config.mjs'].check` with the siblings' grammar; the dispatch arm reads it through `evaluateFlag` off that row, drops the token, and passes the layer as `checkPairs`'s new second argument. ARG-06's list widened to four subcommands and a new test asserts the `check` entry is `deepEqual` to the one `set` refuses with. Verify as predicted: `check --global git.auto_close=true` ok:false / invalid with that entry, `check --global stakes=critical` `{"ok":true}`, `check git.auto_close=true` `{"ok":true}`, `check --global` with no pairs `{"ok":true}`. self-verify ok:true problems [] (no `unknown-flag`, no `unknown-subcommand`), `test.mjs routing prose` 720 pass / 0 fail, tsc exit 0. **This commit is also what broke the census pin the checkpoint below is about** - it was invisible at this task's `Verify:`, which scopes the suite to `routing prose`, and surfaced at task 5's whole-suite clause. |
| 4 - Prove the refused set comes from the schema marker | 498dbb15 | AC5 test uses `runWithSchema` (which sets the `CADENCE_TEST_SEAM` sentinel, so the fixture is actually read) with a two-key fixture that marks `granularity` - a key the SHIPPED schema deliberately leaves unmarked - and leaves `stakes` unmarked; `auto_close` is asserted absent from the fixture body. Plus the AC4 in-process control: shipped marked set is exactly `['git.auto_close']` and no other `src:"repo"` key carries the marker. Verify as predicted: 93 pass / 0 fail with both tests named; deleting `repo_only: true` from the fixture's marked key failed exactly that one test (92 pass / 1 fail), then restored to 93/0. `test.mjs routing` 444 pass / 0 fail, tsc exit 0. |
| 5 - State the marker in the config catalog and re-pin its budget | a57fdfa4 | One legend clause naming `[repo-layer-only]` as a DIFFERENT marker from `[repo]` and what it means at the write face, plus that marker on the `git.auto_close` row's Key cell. No other row touched - `workflow.max_plan_tasks` is byte-identical. `weight-budgets.json` re-pinned 10452 -> 10725 in the same commit. Three of the four Verify clauses met as predicted: `wc -c` and the pin both print 10725, `weight.mjs --root .` reports the surface at 10725 within its pin with no overrun, self-verify ok:true with `problems: []`, tsc exit 0. The fourth clause - `node cadence-core/bin/test.mjs` reports 0 failures across every group - is NOT met: 2968 pass / 1 fail. See the checkpoint. |
Deviations: [deviation] The plan's `## Must be true when done` requires `node cadence-core/bin/test.mjs` to report 0 failures, and task 3's Action requires declaring a new `--global` row on `CONTRACTS['config.mjs'].check`. Those two cannot both hold inside this plan's `files:` lease. `cadence-core/bin/arg-contract.test.mjs:303` census-pins the CONTRACTS table at `assert.equal(entries, 178)`; the new row makes it 179, and that file is not one of the seven declared paths, so `lease-check` would refuse the commit that fixed it. Confirmed independent of task 5: stashing the catalog and budget edits leaves `arg-contract.test.mjs` at 10 pass / 1 fail with the same `179 !== 178`. Nothing was silently redefined and no in-lease fix exists - the only alternatives are editing that one number, which needs the lease widened, or deleting the row task 3 exists to add.
Open items: none
