PLAN CHECKPOINT: structural
Plan: .planning/phases/6/PLAN.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1: Register the seam-invocation census | ac01bcc7 | Row `seam-call-counts` added, marker planted at the generated-test loop, D-05 header paragraph rewritten with no example. Greps: 1 marker, 0 "deliberately absent from this table". census-registry.test.mjs 7/7, planning-lease-check.test.mjs 29/29 (breadth rail green with the new row). Scratch plan-time run over a lease declaring workflows/plan.md alone returned `census-at-risk` naming `cadence-core/bin/seam-calls.test.mjs` (and `weight-budgets.json`), exit 1. tsc exit 0. |
| 2: Record the new row in the replay record | 46523b87 | Replay re-run at ac01bcc7: 56 plans walked, 48 declaring under `cadence-core/bin/`, bound 24. New row `seam-call-counts` 9 of 48 (19%), worst entry `planning-detail-sites` 15 (31%). Table rebuilt to all twelve registry rows (`phase-spelling-callsites` 4 and `capture-writers-register` 0 were missing), both counting sentences corrected. A fresh replay compared row for row against the file: TABLE MATCHES, 12 of 12. planning-lease-check.test.mjs 29/29, tsc exit 0. |
| 3: The plan-time arm fails closed on a lease it could not read | 3bf5264e | Two arms above `censusesAtRisk`: `issues.length` -> `unparsed-lease`, `declared.length === 0` -> `empty-lease`, each a direct `emit` spreading `base` with a top-level `hint`. Live scratch runs: garbage line -> `unparsed-lease`, exit 1, `frontmatter_issues` on the envelope; `filez:` -> `empty-lease`, `declared:0`, exit 1; valid `files:` -> `ok:true`, `declared:1`, exit 0. planning-lease-check 29/29, trace 122/122, phase-spelling 39/39, reason-census 5/5 (one-directional, tokens need no list entry). self-verify `problems []` (check 22 sees both hints). tsc exit 0. |
| 4: One fixture, two plans, both gates, both signals | 6db19be9 | `writePlanTimePlan` gained a `{name, key, extra}` options argument and `planTimeRaw`/`planTime` a plan-number parameter; no second builder. New arms: one two-plan fixture read by `plan-overlap` (frontmatter_issues -> PLAN-1.md with code `unknown-line`, undeclared -> PLAN-2.md) and by `lease-check --plan-time` (plan 1 `unparsed-lease`, plan 2 `empty-lease`, hint and exit 1 on each), plus a not-a-blanket-refusal arm passing on both plan numbers. 31/31 green. Mutation check: reverting the `unparsed-lease` arm alone, the `empty-lease` arm alone, and both together each leaves 30 pass / 1 fail, the failing test being the new two-plan arm; restored -> 31/0. tsc exit 0. |
| 5: `check_census` states what the seam now returns | badcb33e | The exhaustive one-sentence claim replaced by all three outcomes with their remedies, and the later "the remedy, and the whole remedy" paragraph scoped to the file-naming refusal so it stops reading as exhaustive. No command block added or removed - seam-calls.test.mjs 3/3, `plan.md` still 14 invocations. `plan.md` 33,187 B -> 33,749 B, budget re-pinned by hand to 33,749 (`git diff -U0` = one changed line). self-verify `problems []`, tsc exit 0. |
| 6: Phase 2's UAT items 9 and 10 re-test green | b1b8d868 | Both reset to `pending` with `--fix "<hash>, retest"`, then re-recorded `pass --source verifier` with evidence through `--fields-file`. `uat status --phase 2`: pass 10, fail 0, pending 1 (item 11), `result: partial` - amended AC5. `grep -c "^status: pending"` = 1; the UAT.md diff touches items 9 and 10 and the Summary counts only, item 11 byte-identical. tsc exit 0, self-verify `problems []`. The task's whole-tree half is NOT met: `node cadence-core/bin/test.mjs` is 3266 pass / 1 fail - see the checkpoint. |
Deviations: [deviation] Task 6's `Verify:` and the plan's `## Must be true when done` both require `node cadence-core/bin/test.mjs` green. It is 3266 pass / 1 fail, and the failure is outside this plan's lease. `citation-census.test.mjs`'s `DOCS_CLAIMS_CITATIONS` pins `.planning/DOCS-CLAIMS.md`'s EXECUTE-10 row to `cadence-core/bin/planning/lease-check.mjs:450-453`, the line carrying `reason: 'undeclared-files'`. Task 3 inserted the two fail-closed arms and their comment above it, so that symbol now sits at :498 (verified: `git show HEAD~3:...lease-check.mjs | sed -n '450p'` prints the reason line; the live file carries it at 498). The re-pin is 450-453 -> 498-501 in TWO files, `.planning/DOCS-CLAIMS.md:855` and `cadence-core/bin/citation-census.test.mjs:232-234`, and NEITHER is one of the ten paths this plan declares - `lease-check --phase 6 --plan 1` would refuse the commit. What I did: committed tasks 1-6 as planned, verified each on its own terms, and stopped here rather than staging an undeclared path. This is the same coupling phase 2 hit - its PLAN-2 task 7 was "Re-pin the EXECUTE-10 citation the new arm moved" - and this plan's lease did not carry it.
Open items: none
