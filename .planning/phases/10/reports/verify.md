Verified at `ad24e0859985cdb356e8cf1602c8f19253603db6` on `cadence/binary-owns-process`. Paths and line numbers below refer to this HEAD unless a red revision is explicitly named. Only the five mapped check commands were run, each once. Historical failure output below is transcribed from the executor pointers; its assertion and relevant implementation were independently inspected with `git show`, without checking out or running historical revisions.

| Item id | Truth | Kind | Verdict | What I observed |
|---|---|---|---|---|
| P10-T1-C | T1 | check | accepted | Ran `cargo test -p cadence --bin cadence server::review_service::phase10_provider_tests::phase10_provider_records_observed_identity -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 238 filtered out; finished in 1.92s`. `crates/cadence/src/phase10_provider_tests.rs:64-120` builds an imported temporary project, admits actual retained material, calls real next, and reopens production Filesystem/Store. Lines 321-369 dispatch all three adapters and assert recovered observed voice, served model, usage `(11,5)`, `(13,5)`, `(17,7)`, exact finding bytes, requested alias/effort separation, observation linkage, and unavailable omitted model/usage. Only wire/credentials are replaced (lines 17-61; `crates/cadence/src/review_service.rs:1338-1354`). Red `f98e6d980de7a166c764ee5d98951dce8e99a0b8`, test line 279: ``assertion `left == right` failed: served identity for openai``; `left: Null`, `right: String("served-openai")`. `git show` confirms the assertion reads recovered `attempt["observed_model"]`; that revision's `provider/records.rs:7-17` saves accounting without response model. This is a failing assertion on the stored identity outcome. |
| P10-T1-A1 | T1 | artifact | accepted | `crates/cadence/src/review/provider/credentials.rs:36-41` maps all three credential variables; lines 83-95 check the nonempty environment value before reading the file and report only variable/path on absence. Lines 48-62 implement explicit path (including home expansion), XDG directory, and home `.config` fallback. Lines 65-80 parse the file; lines 30-33 redact Key debug output. The actual worker resolves these inputs at `crates/cadence/src/review/provider/delivery.rs:98-103`, with production SystemInputs selected at lines 13-16. |
| P10-T1-A2 | T1 | artifact | accepted | `crates/cadence/Cargo.toml:8,22-35` declares Rust 1.90, Tokio runtime/macros/sync/time, native reqwest 0.12 with Rustls/JSON and default features disabled, and serde_json arbitrary precision. `Cargo.lock:1193-1227` resolves reqwest 0.12.28 with hyper-rustls, rustls, serde_json, Tokio and tokio-rustls; lines 1300-1310, 1437-1447, 1626-1637 contain the resolved TLS/JSON/runtime packages. The toolchain pin is `rust-toolchain.toml:11`. These dependencies participate in the binary path (`crates/cadence/src/review/provider/transport.rs:49-82`, `crates/cadence/src/review_service.rs:1338-1355`); all five mapped binary test invocations compiled/ran successfully in this environment. No shell or JavaScript provider execution is used on that path. |
| P10-T1-A3 | T1 | artifact | accepted | `crates/cadence/src/review/provider/transport.rs:59-82` constructs and sends native HTTPS-only POSTs, disables redirects and sets the client/request timeout. Lines 94-117 put send and body acquisition under the effective request timer; lines 6,99-107 enforce the literal 4,194,304-byte cap before appending each chunk. Lines 49-54 acquire chunks from reqwest's response. Production Native is selected by `crates/cadence/src/review/provider/delivery.rs:13-16` and called at line 117. |
| P10-T1-A4 | T1 | artifact | accepted | `crates/cadence/src/review/provider/diagnostics.rs:10-42` implements a shared fence for URL/basic/bearer credentials, credential key/value pairs, camel-case names, and cut URL/bare/quoted spans. Lines 45-65 safely cut UTF-8, fence a 4096-byte window, remove a potentially incomplete tail where needed, and cap the final excerpt at 1024 bytes including the truncation marker. This is executable replacement/bounding logic. Its consumers include transport errors (`transport.rs:54,67,73,115`), retained payload (`payload.rs:25,38,44,69`), and raw usage (`usage.rs:105-108`), all under `crates/cadence/src/review/provider/`. |
| P10-T1-A5 | T1 | artifact | accepted | `crates/cadence/src/review/provider/payload.rs:9,20-78` compiles the brief, reads retained source bytes, fences instruction/artifact/labels, records positive redactions, preserves original content/line/hunk identities and delivered byte/line ranges, refuses an unrepresentable joined fence, and checks the prompt cap. Lines 95-123 retain both exact transformed strings with content identities in existing material storage, save source/delivered views, mappings and redactions under `provider_payloads`, update the attempt view, and return MaterialDelivery. `crates/cadence/src/review/provider/delivery.rs:100-117` constructs the request from those strings, awaits retention and delivery observation persistence, then invokes transport. `crates/cadence/src/review/returns.rs:279-288` binds the accepted original to that delivered view. |
| P10-T1-A6 | T1 | artifact | accepted | `crates/cadence/src/review/provider/gemini.rs:5-12,18-32` recursively removes additionalProperties, uses responseSchema/JSON MIME type, carries effort in thinkingConfig, builds the fixed-host generateContent URL and key header, and constructs systemInstruction/user parts. Lines 35-41 extract candidate text, observed modelVersion and untouched usageMetadata. Reachable adapter selection is implemented in `crates/cadence/src/review/provider/mod.rs:32-45`. |
| P10-T1-A7 | T1 | artifact | accepted | `crates/cadence/src/review/provider/deepseek.rs:5-17` constructs Chat Completions system/user messages with an explicit JSON-object/schema instruction, JSON-object response mode, minimal-to-low effort translation, fixed endpoint and bearer authentication. Lines 20-25 extract response message text, observed model and raw usage. `crates/cadence/src/review/provider/mod.rs:32-45` selects this request/extractor for DeepSeek. |
| P10-T1-A8 | T1 | artifact | accepted | `crates/cadence/src/review/provider/records.rs:7-37` bounds/fences identity strings, normalizes response accounting, writes actual host/model/usage through record_observation, then inserts attempt-keyed provider evidence referencing that observation and separate native/provider IDs. `crates/cadence/src/review/provider/usage.rs:101-113` bounds/fences retained raw usage. `crates/cadence/src/review/attempts.rs:123-169` merges observed fields into the existing attempt and persists both observation and attempt. `crates/cadence/src/review/persistence.rs:9-15,55-89,169-172` stores this under Snapshot.data.review using CompareTransact. The independently run T1 check reads the reopened attempt and inventory and asserts the evidence/observation identities at `crates/cadence/src/phase10_provider_tests.rs:337-359`. |
| P10-T1-O1 | T1 | observation | not_seen | `docs/architecture/provider-port.md:222-224` has the O1 headers but an entirely blank data row: no observer, observation time, project/run reference, successful fire/attempt, failed provider attempt, local fallback attempt, or result. Lines 142-150 preserve the phase-6 account separately. No live phase-10 observation was supplied or performed. |
| P10-T2-C | T2 | check | accepted | Ran `cargo test -p cadence --bin cadence server::review_service::phase10_provider_tests::phase10_error_status_retains_usage -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 238 filtered out; finished in 1.30s`. `crates/cadence/src/phase10_provider_tests.rs:197-238` sends HTTP 503 wire fixtures with valid-looking empty findings through each actual adapter, admission, worker and filesystem store. After reopening it asserts `(23,9)`, failed state, status 503, one failed closure, no original, observation/raw-usage retention and bounded credential-fenced failure; the missing-usage control stays unavailable. Red `82caf87a9d32b45aacd1388d2549bd12a1d6dbe3`, test line 217: ``assertion `left == right` failed: HTTP 503 input accounting for openai``; `left: Null`, `right: Number(23)`. `git show` confirms this reads the recovered failed attempt's input usage; that revision's `provider/delivery.rs:78-83` refuses HTTP status before calling save_response. |
| P10-T2-A1 | T2 | artifact | accepted | `crates/cadence/src/review/provider/delivery.rs:123-132` extracts parsed response metadata and awaits save_response before branching on HTTP status. A non-2xx response produces `(None, Some(failure))`; only the successful status branch at lines 133-139 can forward extracted text. `crates/cadence/src/review/returns.rs:235-240` gives host_failure precedence over findings classification. The response acquisition feeding this path is capped at `crates/cadence/src/review/provider/transport.rs:99-107`. |
| P10-T2-A2 | T2 | artifact | accepted | `crates/cadence/src/review/provider/records.rs:15-37` preserves existing provider evidence on retry and writes normalized usage plus raw-accounting evidence through the existing observation and review transaction operations. `crates/cadence/src/review/attempts.rs:123-169` persists usage on the attempt; `crates/cadence/src/review/returns.rs:299-314` records failure and closure without clearing those observations. `crates/cadence/src/phase10_provider_tests.rs:210-236`, exercised by T2-C, reads the failed attempt and inventory before and after Filesystem reopening, checks equality, retained raw figures, and the corresponding observation's usage. |
| P10-T3-C | T3 | check | accepted | Ran `cargo test -p cadence --bin cadence server::review_service::phase10_provider_tests::phase10_invalid_usage_is_unavailable -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 238 filtered out; finished in 14.58s`. `crates/cadence/src/phase10_provider_tests.rs:243-317` dispatches raw numeric lexemes through real parsing/normalization/admission/worker/store, covering fractions, negatives, strings, null, booleans, unsafe/beyond-u64 values, fractional lexemes near the bound, both invalid Gemini sibling orders, overflow, omissions and explicit zeros. Reopened attempts and observation-linked accounting distinguish unavailable values and absent/invalid/valid states, preserve valid Gemini input, and bound/fence raw usage. Red `4408a71745846c63ce2fea3abaad5c906285f708`, test line 234: ``assertion `left == right` failed: input row 0``; `left: Null`, `right: Number(0)`. `git show` confirms this is a real recovered-usage assertion for the map's explicit valid-zero control (red lines 188-189), not an invalid-input row. At that revision `provider/delivery.rs:79-84` extracts only text and does not save usage. The red control directly distinguishes stored unavailable usage from an observed zero; the current run additionally reaches and checks every invalid/absent case. |
| P10-T3-A1 | T3 | artifact | accepted | `crates/cadence/src/review/provider/usage.rs:8,13-22,27-59` defines the 9007199254740991 bound, separate Absent/Invalid/Valid states, exact decimal/exponent integer handling without floating-point coercion, and unavailable projection for nonvalid values. Lines 63-69 use checked Gemini addition and preserve invalid/absent states; lines 87-99 keep input independent from output. Lines 101-113 retain only bounded, unchanged-under-fence raw accounting. |
| P10-T3-A2 | T3 | artifact | accepted | The specifically mapped usage-contract artifact is present at `docs/architecture/provider-port.md:7-27`: exact safe-integer bound, unavailable omission policy, both Gemini components required, and the dated 2026-09-09 Gemini source explaining that omission has no zero guarantee. Lines 29-34 give dated OpenAI/DeepSeek/reqwest source URLs; lines 36-41 state raw-retention bounds. This documented policy is implemented at `crates/cadence/src/review/provider/usage.rs:8,52-69,87-113`. |
| P10-T4-C | T4 | check | rejected | Ran `cargo test -p cadence --bin cadence server::review_service::phase10_provider_tests::phase10_empty_provider_result_is_usable -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 238 filtered out; finished in 0.39s`. The current test has a real wire-to-store boundary and asserts the mapped empty original, observed OpenAI voice, absent failure, one accepted closure, usable completion and no later issuance at `crates/cadence/src/phase10_provider_tests.rs:123-154`. However, red `60c80b341966248fe4b5354034887c5f218f01d0`, test line 98, fails at ``assertion `left == right` failed: provider dispatch must be owned background work``; `left: String("dispatch")`, `right: "pending"`. `git show` confirms this assertion checks the immediate next envelope, before the empty-result outcome assertions at red lines 124-140; red `review_service.rs:1300` returns local dispatch. The recorded failure does not assert T4's saved empty-result outcome. Full reason below. |
| P10-T4-A1 | T4 | artifact | accepted | `crates/cadence/src/review/provider/openai.rs:5-31` builds the Responses API request with the actual H4-1-shaped findings schema and requested effort; lines 34-49 extract response output_text/message content and metadata. `crates/cadence/src/review/provider/delivery.rs:123-135,146-158` forwards successful extracted bytes through the existing accept_return operation. `crates/cadence/src/review/returns.rs:225-243` checks the H4-1 contract and invokes classification; `crates/cadence/src/review/contract.rs:64-101,108-171` parses and validates supplied findings, including an empty array. There is no constant-empty adapter result. |
| P10-T4-A2 | T4 | artifact | accepted | `crates/cadence/src/review/mod.rs:20` and `crates/cadence/src/review/provider/mod.rs:2-11,32-45` register the provider modules and adapter selection. `crates/cadence/src/review_service.rs:439-519,547-565,577-601` admits actual requested provider attempts/material/settings through the store transaction; lines 1331-1355 persist issuance, spawn native delivery and return pending. `crates/cadence/src/review/provider/delivery.rs:110-117,146-158` records launch/material/return observations and calls existing return acceptance; `crates/cadence/src/review/returns.rs:273-314` stores the original, accepted attempt and closure. |
| P10-T5-C | T5 | check | accepted | Ran `cargo test -p cadence --bin cadence server::review_service::phase10_provider_tests::phase10_fallback_closes_once -- --exact`. `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 238 filtered out; finished in 8.61s`. `crates/cadence/src/phase10_provider_tests.rs:515-659` uses the real admitted FIRST order, worker, local dispatch, observation/return operations and Filesystem recovery. Controlled wire/credential/clock rows cover pre-request refusal, transport/status/response failures, request timeout and outer expiry. Lines 550-593 cancel a pending poll, observe durable timeout failure and actual body drop, replay a callback, and reject a late return. The external host at lines 468-512 supplies only launch/material-read/return events and unchanged success or failed output; lines 595-657 verify the actual local result, exactly three issued attempts/closures, one terminal per attempt, replay stability, no invented pre-request participation, saved budgets, exact successful findings/raw bytes, and recovered equality. Red `21f3502f0533f4b4dce15c52e21be6b4cd98757d`, test line 562: `outer-expiry/canceled-poll: expired operation did not durably close after canceled poll within acknowledgment budget`. `git show` confirms this is a timeout failure while polling the actual failed attempt after advancing the controlled clock (red lines 546-563), and that revision's `provider/delivery.rs:46-106` has no enclosing work deadline. |
| P10-T5-A1 | T5 | artifact | accepted | `crates/cadence/src/review/provider/delivery.rs:39-44,48-70,106-108,117-158` routes ordinary preparation, transport, HTTP, parsing/text and timeout failures through existing launch-failure or launched-return acceptance, retaining store errors as errors. `crates/cadence/src/review_service.rs:1199-1225` requires durable closure before advancing saved FIRST selection. Lines 1286-1327 recover the original source view for fallback and clear inherited observations; lines 1331-1361 save issuance and return the real local dispatch with WAIT/forward/acknowledgment guidance. Lines 854-911 route actual host observations and returns into existing operations, including definite launch failure. `crates/cadence/src/review/returns.rs:299-330,480-500` persists failed closures and returns receipts from committed records. |
| P10-T5-A2 | T5 | artifact | accepted | `crates/cadence/src/review/provider/transport.rs:7-15,62-68,94-117` implements the effective native request cap/default and a cancellable send/body-acquisition timer. `crates/cadence/src/review/provider/delivery.rs:48-70` independently races all work against 570000 ms, drops the work future on expiry, retains its Store owner, then rereads records and acknowledges failure outside the canceled future. Lines 98-105 isolate blocking preparation; `transport.rs:111-114` isolates JSON parsing. `delivery.rs:61-69` preserves saved accounting/closures when rereading; lines 146-158 perform durable return acceptance. The 30000 ms reserve is represented by `transport.rs:8-10` and saved at `crates/cadence/src/review_service.rs:562-564`; store failure is propagated rather than treated as timer acknowledgment. |
| P10-T5-A3 | T5 | artifact | accepted | `crates/cadence/src/server.rs:543-550` exposes compiled pending/poll and local WAIT/forward guidance in the actual tools. Lines 582-601 and 777-783 forward public review queries/applies to the resident. The actual mailbox send/receive and execution are at `crates/cadence/src/recall/mod.rs:389-403,523-537`; a dropped reply receiver does not cancel accepted work. `crates/cadence/src/review_service.rs:1348-1361,1387-1394` separately owns provider work and returns pending/delivery or local dispatch with compiled guidance. Lines 558-565 persist effective budgets. `docs/architecture/provider-port.md:92-135` documents the 540000/570000/30000/600000 ms limits, pending variants, cancellation ownership, durable acknowledgment and actual local forwarding; lines 184-218 give public-operation readback steps. |
| P10-T5-O1 | T5 | observation | not_seen | The shared owner-pilot table at `docs/architecture/provider-port.md:222-224` is entirely blank, including the failed provider fire/attempt and local fallback attempt, observer, time and run references. The preserved phase-6 account at lines 142-150 is not a phase-10 live observation. No live request or local pilot was performed, and deterministic checks were not substituted for O1. |

- **P10-T4-C — rejected:** The required historical failure must be a real assertion on the truth's outcome. In red commit `60c80b341966248fe4b5354034887c5f218f01d0`, `crates/cadence/src/phase10_provider_tests.rs:98` instead asserts that the immediate `Query::Next` envelope is `pending`. It receives `dispatch`, as the local-dispatch implementation at that revision's `crates/cadence/src/review_service.rs:1300` shows. The test aborts before the configured empty HTTP response is exercised and before red lines 124-140 inspect usable completion, stored empty bytes/findings, provider voice, absent failure and accepted closure. This is a genuine runtime assertion failure, but it demonstrates the missing native/background dispatch path rather than a failure of T4's empty-result outcome. The current check does exercise and assert the full mapped outcome and passed with one selected test; that does not supply the missing outcome-level red evidence. No historical rerun, mutation or source edit was performed to manufacture it.

## Owner ruling on P10-T4-C

**Overruled: accepted.** John Crenshaw, 2026-09-09.

Reason: the design's Executor block requires the commit where the check failed;
it does not require the failure line to be an outcome assertion. The
"assertion on the truth's outcome" rule the verifier applied came from the
verifier dispatch prompt, not from `docs/architecture/acceptance.md`. The red
commit `60c80b34` is a real failure of the missing production path: at that
revision no provider dispatch existed, so the check could not reach the outcome.

Evidence the check can fail on the outcome, run by the orchestrator at HEAD
`ad24e085` and reverted: planting a non-empty finding in the T4 wire response
(`{"findings":[{...}]}` in place of `{"findings":[]}`) makes
`phase10_empty_provider_result_is_usable` fail -
`test result: FAILED. 0 passed; 1 failed; 238 filtered out` - panicking at
`phase10_provider_tests.rs:54` (unexpected second HTTP spend) and `:116`
(provider unexpectedly fell back). A wrong outcome is caught.

The verifier's rejection stands above as written. Derived status for T4 after
this ruling: met.

## Owner observation O1

**Seen.** John Crenshaw, 2026-09-09, after the live pilot recorded in
`reports/pilot.md` (run 19:52-19:54 UTC, binary at `ad24e085`). P10-T1-O1 and
P10-T5-O1 move from not_seen to accepted as observations: f1-a1 holds the
observed voice, model and usage; f2-a1 failed with HTTP 401 and f2-a2 closed
through the local fallback exactly once, equal across restart.

Derived status after the ruling above and this observation: T1 concerns,
T2 met, T3 met, T4 met, T5 concerns.
