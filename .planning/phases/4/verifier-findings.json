{
  "status": "gaps",
  "score": "9/11",
  "truths": [
    {
      "n": 1,
      "truth": "planning.mjs --dir refuses both the empty and the bare spelling: one JSON line naming the flag, exit 1, nothing on stderr",
      "status": "VERIFIED",
      "uat_item": 1,
      "evidence": "`node cadence-core/bin/planning.mjs status --dir ''` and `... status --dir` each print exactly 1 stdout line {\"ok\":false,\"reason\":\"bad-args\",\"detail\":\"--dir needs a path after it: --dir <planning dir>\"}, exit 1, stderr 0 bytes (measured, no DEP0187). Read through the declared row at cadence-core/bin/planning.mjs:5486, `evaluateFlag(ARGV, '--dir', CONTRACTS['planning.mjs']['*']['--dir'])`, with the row at cadence-core/bin/lib/arg-contract.mjs:318."
    },
    {
      "n": 2,
      "truth": "route.mjs and review-provider.mjs refuse a valueless flag by name instead of swallowing the next flag, each in its own published reason vocabulary, with references/seams.md untouched",
      "status": "VERIFIED",
      "uat_item": 2,
      "evidence": "`route.mjs resolve --role --attempt 2` -> {\"ok\":false,\"reason\":\"usage\",\"detail\":\"resolve --role needs a role name after it: --role <name>\"} (baseline unknown-role:\"--attempt\" gone). `review-provider.mjs consult --payload --provider openai` -> {\"ok\":false,\"reason\":\"bad-args\",\"detail\":\"consult --payload needs a payload file after it...\"} (baseline bad-provider gone). `usage` predates the phase at 953bf52:route.mjs:778-787; `bad-args` is published at cadence-core/references/seams.md:330. `git diff 953bf52..6985052 -- cadence-core/references/seams.md` is empty. Wiring: route.mjs:803 and review-provider.mjs:214 loop every declared row through evaluateFlag. Named pins: route.test.mjs:549, review-provider.test.mjs:634."
    },
    {
      "n": 3,
      "truth": "A bare --role on trace append refuses so no empty-string roles key can be minted, while a bare --plan or --sha still drops and the close still succeeds",
      "status": "VERIFIED",
      "uat_item": 3,
      "evidence": "`trace append --phase 1 --family lifecycle --event dispatch --role --tokens 5` -> {\"ok\":false,\"reason\":\"bad-args\",\"detail\":\"trace append --role needs a role name after it: --role <name>\"}, exit 1, nothing written; a following real --role renders \"roles\":{\"planner\":{...}} with no \"\" key. `trace close ... --plan` and `... --sha` each -> {\"ok\":true,\"written\":true} and the trace.jsonl lines carry no plan/sha key. Dispositions come from the declaration at planning.mjs:3286-3287 (TRACE_GRAMMAR spread from the CONTRACTS rows) and are applied at planning.mjs:3518. Named pin: trace.test.mjs:640."
    },
    {
      "n": 4,
      "truth": "All three dispositions survive at a named flag - fallback, warn and refuse - rather than collapsing into refuse-everything",
      "status": "VERIFIED",
      "uat_item": 4,
      "evidence": "fallback: `issue-check.mjs check --dir /code/cadence --timeout-ms abc` -> ok:true with its full report, warnings []. warn: `route.mjs resolve --role cad-planner --phase 1.abc` -> ok:true with the full resolved bundle plus warnings[\"--phase \\\"1.abc\\\" is not a phase number ... the resolved bundle is unaffected\"]. refuse: `git-branch.mjs decide --dir` -> {\"ok\":false,\"reason\":\"missing-flag-value\",\"detail\":\"--dir\"}, exit 1."
    },
    {
      "n": 5,
      "truth": "There is exactly one CONTRACTS table, self-verify reads it back rather than owning it, and the lint it feeds actually fires when a row loses a flag",
      "status": "VERIFIED",
      "uat_item": 5,
      "evidence": "`grep -c \"const CONTRACTS = {\" cadence-core/bin/self-verify.mjs` -> 0; the definition is cadence-core/bin/lib/arg-contract.mjs:316 and self-verify.mjs:215 imports {CONTRACTS, flagNames, evaluateFlag}. `node cadence-core/bin/self-verify.mjs` -> ok:true, problems [] across 25 checks. Falsifier reproduced on a scratch copy of the tree: emptying the 'plan-overlap' row (arg-contract.mjs:388-390) makes self-verify report {kind:'unknown-flag', file:'cadence-core/workflows/execute.md', detail:'planning.mjs plan-overlap --phase'}."
    },
    {
      "n": 6,
      "truth": "The suite is green and the second reader is gone, not deprecated - seam-input.mjs no longer exports optionalFlag and no divergence arm survives",
      "status": "VERIFIED",
      "uat_item": 6,
      "evidence": "`node cadence-core/bin/test.mjs` -> tests 2373, pass 2373, fail 0 (one full run). `grep -n '^export' cadence-core/bin/lib/seam-input.mjs` -> missingFlagValue, flagValue, readText only. `grep -rn optionalFlag cadence-core/bin` -> two comment lines (lib/seam-input.mjs:22, release-bump.test.mjs:425), no code and no test arm; seam-input.test.mjs's remaining tests are flagValue/missingFlagValue/readText."
    },
    {
      "n": 7,
      "truth": "The rule is stated once in prose with its budget re-pinned, and the typecheck is clean",
      "status": "VERIFIED",
      "uat_item": 7,
      "evidence": "cadence-core/references/conventions.md:76-111 '## Seam arguments' states the three-word disposition vocabulary (refuse/warn/fall back), the separate bare axis vs the value axis with the trace append/close split, and names lib/arg-contract.mjs as the one declaration home. `wc -c` = 14556 and weight-budgets.json:30 = 14556 (exact match). `npx tsc -p tsconfig.ci.json` exits 0 with no diagnostics."
    },
    {
      "n": 8,
      "truth": "The contract is genuinely shared at runtime, not merely imported: the adopting bins read their declared rows through the evaluator and a real value flows end to end",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "Twelve non-test bins import lib/arg-contract.mjs and each calls it: route.mjs:803/820, review-provider.mjs:214, config.mjs:357, self-verify.mjs:1265, planning.mjs:3518/5486 via evaluateFlag; git-branch.mjs:120, git-publish.mjs:279, land-cleanup.mjs:187, worktree-base.mjs:158, issue-check.mjs:315, weight.mjs:49, release-bump.mjs:289/315 via requireFlag on ROWS[sub][name]. End-to-end value trace: `planning.mjs status --dir <scratch>/.planning` reports that exact path in its detail, `git-branch.mjs decide --dir <scratch>` answers about that tree, `weight.mjs --root <copy>` scans the copy's surfaces - each value enters at argv, is classified by the declared row, and lands in the answer."
    },
    {
      "n": 9,
      "truth": "A tenth seam inherits the rules rather than restating them: the declaration is enforced, not merely documented",
      "status": "VERIFIED",
      "uat_item": null,
      "evidence": "On a scratch copy of the tree, adding cadence-core/bin/tenth-seam.mjs with no row makes self-verify report {kind:'uncontracted-script', file:'cadence-core/bin/tenth-seam.mjs'} (check 14), and deleting a flag from an existing row reports unknown-flag against the prose that spells it (check 2). conventions.md:103-107 states the obligation in prose."
    },
    {
      "n": 10,
      "truth": "Every row in the one table describes the behavior its CLI actually has - a declared disposition is the rule the seam applies",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "Three measured spellings where the row says refuse on BOTH axes and the CLI accepts and writes the boolean through. `planning.mjs cursor set --dir <s>/.planning --phase 1 --status planned --next '/cad-plan 1' --total 5 --name` -> ok:true, STATE.md gets `Phase: 1 of 5 (true)` (row: arg-contract.mjs:331). `echo '[{\"name\":\"a\",\"expected\":\"b\"}]' | planning.mjs uat init --phase 1 --sources` -> ok:true, UAT.md front-matter gets `sources: true` (row: arg-contract.mjs:341). `planning.mjs uat record --phase 1 --item 1 --result fail --reason` -> ok:true, UAT.md gets `reason: true` (row: arg-contract.mjs:356). Cause: planning.mjs consults the table at only two sites (planning.mjs:5486 for --dir, planning.mjs:3518 for the trace grammar); its other ~86 declared rows have no runtime reader and its 79 hand-written fail('bad-args') sites still own those refusals. Nothing ties a declaration to behavior - arg-contract.test.mjs checks the table's internal shape only, and self-verify consumes flag NAMES via flagNames, never the required/type/value/bare fields."
    },
    {
      "n": 11,
      "truth": "The one table is complete for the bins that read it - every flag a bin accepts has a row on the subcommand that accepts it",
      "status": "FAILED",
      "uat_item": null,
      "evidence": "`config.mjs get stakes --global` returns {\"ok\":true,\"values\":{\"stakes\":\"shipped\"},\"source\":\"global\"} live, but CONTRACTS['config.mjs'].get (arg-contract.mjs, get row) declares only --file, while the validate and set rows both declare --global. self-verify stays green only because no prose spells the pair (`grep -rn 'config.mjs get' cadence-core --include '*.md'` -> 7 hits, none with --global), so check 2 never reaches it. --global is also still hand-parsed at config.mjs:331-333 (`tokens.indexOf('--global')`) rather than read off any row."
    }
  ],
  "passes": [
    { "k": 1, "name": "planning.mjs --dir refuses the empty and bare spellings", "evidence": "Both spellings print exactly one stdout line {\"ok\":false,\"reason\":\"bad-args\",\"detail\":\"--dir needs a path after it: --dir <planning dir>\"}, exit 1, stderr 0 bytes - no DEP0187. Read through the declared row at cadence-core/bin/planning.mjs:5486." },
    { "k": 2, "name": "route.mjs and review-provider.mjs stop swallowing the next flag", "evidence": "route -> reason \"usage\" naming --role; review-provider -> reason \"bad-args\" naming --payload. Both codes predate the phase (953bf52:route.mjs:778-787; seams.md:330), and seams.md has no diff across 953bf52..6985052. Wired at route.mjs:803 and review-provider.mjs:214; pinned by route.test.mjs:549 and review-provider.test.mjs:634." },
    { "k": 3, "name": "a bare --role refuses; bare --plan and --sha still drop", "evidence": "Bare --role -> ok:false bad-args, nothing appended, and a subsequent render shows only the real role key, no \"\". Bare --plan and bare --sha each -> {\"ok\":true,\"written\":true} with no plan/sha key in trace.jsonl. Dispositions read off CONTRACTS at planning.mjs:3286-3287/3518; pinned by trace.test.mjs:640." },
    { "k": 4, "name": "all three dispositions hold at a named flag", "evidence": "fallback: issue-check --timeout-ms abc -> ok:true, no warning. warn: route resolve --role cad-planner --phase 1.abc -> ok:true full bundle plus the phase warning. refuse: git-branch decide --dir -> ok:false missing-flag-value \"--dir\", exit 1." },
    { "k": 5, "name": "one CONTRACTS table, and it is enforced", "evidence": "self-verify defines no CONTRACTS (grep count 0) and imports it at self-verify.mjs:215 from lib/arg-contract.mjs:316; live run ok:true, problems [] over 25 checks. Falsifier reproduced on a scratch copy: emptying the plan-overlap row yields unknown-flag against cadence-core/workflows/execute.md." },
    { "k": 6, "name": "suite green and optionalFlag gone", "evidence": "test.mjs -> 2373 tests, 2373 pass, 0 fail. seam-input.mjs exports only missingFlagValue, flagValue, readText; optionalFlag survives as two comment lines and no test arm." },
    { "k": 7, "name": "the rule is stated once in prose, and the budget matches", "evidence": "conventions.md:76-111 '## Seam arguments' carries the three dispositions and the bare-vs-value split; file is 14556 bytes and weight-budgets.json:30 is 14556; npx tsc -p tsconfig.ci.json exits 0." }
  ],
  "gaps": [
    {
      "name": "the table declares refuse where the CLI still writes the boolean through",
      "reason": "behavior wrong - a declared disposition that no reader applies. planning.mjs holds 98 of the table's 144 flag rows but consults the table at only two sites, so most of its rows state a rule the bin does not enforce, and for at least three of them the row and the shipped behavior disagree outright.",
      "evidence": "cadence-core/bin/lib/arg-contract.mjs:331 declares cursor set --name {value:'refuse',bare:'refuse'}, yet `planning.mjs cursor set --dir <s>/.planning --phase 1 --status planned --next '/cad-plan 1' --total 5 --name` returns ok:true and writes `Phase: 1 of 5 (true)` into STATE.md. arg-contract.mjs:341 declares uat init --sources refuse, yet the bare spelling writes `sources: true` into the UAT front-matter. arg-contract.mjs:356 declares uat record --reason refuse, yet the bare spelling writes `reason: true` into UAT.md - into the very file this gate is merged onto. The readers that exist are planning.mjs:5486 (--dir only) and planning.mjs:3518 (trace grammar only); the other refusals remain the 79 hand-written fail('bad-args') sites. Nothing catches the divergence: arg-contract.test.mjs checks only the table's own shape, and self-verify reads flag NAMES through flagNames, never the required/type/value/bare fields. The module header (arg-contract.mjs:1-12, 'the rules are DECLARED here, once') carries no caveat that a row may be unread, and conventions.md:103-107 states the same claim in prose.",
      "severity": "major",
      "missing": "Either make the declaration binding for these rows - route the ~12 value-carrying planning.mjs string flags through evaluateFlag the way --dir and the trace grammar already are - or stop the table from asserting a rule nothing applies: correct those rows to the disposition the bin actually has and say so, and add the test that reddens when a declared row has no reader (a per-bin census of declared-vs-read flags, the coverage the optionalFlag census row used to supply)."
    },
    {
      "name": "config.mjs get accepts --global with no row to declare it",
      "reason": "missing - a live, exercised flag with no declaration on the subcommand that accepts it, inside the table whose completeness is the phase's product.",
      "evidence": "`node cadence-core/bin/config.mjs get stakes --global` returns {\"ok\":true,\"values\":{\"stakes\":\"shipped\"},\"source\":\"global\"}, but CONTRACTS['config.mjs'].get in cadence-core/bin/lib/arg-contract.mjs declares only --file, while the validate and set rows each declare --global. self-verify is green only because no workflow prose spells `config.mjs get --global` (7 `config.mjs get` hits under cadence-core, none with --global), so check 2 never reaches the pair; the moment any prose spells it, correct prose reports unknown-flag. --global is also read by hand at config.mjs:331-333 rather than off a row.",
      "severity": "minor",
      "missing": "Add the '--global' boolean row to the config.mjs get subcommand, matching the validate and set rows."
    }
  ],
  "human_checks": []
}
