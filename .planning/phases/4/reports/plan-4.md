PLAN COMPLETE
Plan: .planning/phases/4/PLAN-4.md
Tasks: 3 of 3 (+2 fix passes)
| Task | Commit | Note |
|---|---|---|
| 1: `workflows/milestone.md` carries the records | 2f75f5d0 | Step 3's raw-findings union replaced by `planning.mjs risk-carry --phase <N>` run before the prune, stated as a COPY with its `ok:false` STOP; step 7 deletes `.planning/risk-carry/` on both arms; the commit-staging paragraph names the new transient. New `prose-agreement.test.mjs` arm pins carry-before-prune by file position, the STOP clause inside the carry's own paragraph, and step 7's both-arms delete - falsified twice (moved the fence below the prune: red; deleted the STOP clause: red). `weight-budgets.json` re-pinned 15190 -> 15814 in the same commit. `self-verify` ok:true problems: []. |
| 2: `/cad-land` reads the rulings | 9a787f83 | The 3(b) gate bullet now unions `entries[]` from every `ADJUDICATION-risk_surface*.json` under `.planning/phases/*/` and `.planning/risk-carry/*/`, every round; names `unruled` for any `REVIEW-risk_surface*.md` with no sibling record (both roots plus the legacy `.planning/REVIEW-risk_surface-*.md`); surfaces a non-empty `overridden` beside `findings` on halt; keeps `{"findings":[]}` and the never-parsed sentence. Second `prose-agreement.test.mjs` arm pins the two roots, both keys, every-round, and a NEGATIVE row refusing a raw-`findings` union - falsified by restoring the old sentence (red). Deferred-queue paragraphs and the `deferred list` position untouched; the two existing `cad-land step 3` ordering arms still green. Budget re-pinned 14853 -> 15803 in the same commit. |
| 3: The two references state the adjudicated input | fc4fc220 | `risk-surface.md`'s persistence section names the `ADJUDICATION-risk_surface-<discriminator>[-rN].json` beside the REVIEW file as what the close consumes and the missing sibling as an `unruled-review` halt; the every-write-is-discriminated bullet is unchanged and the outlives-the-phase-dirs bullet is now the `risk-carry` copy to `.planning/risk-carry/<N>/`, pairing by basename. `triage-gate.md`'s `auto_close` carve-out states the three consequences (genuinely-unfixed halt, unruled halt, overridden surfacing) and that a fixed/refuted/downgraded finding is answered work. "confirmed and not fixed" sits in a different section and was untouched; no new sentence pairs `survived` with a fix commit. Both budgets re-pinned in the same commit (10768 -> 11451, 23677 -> 24233). Repo-wide grep over `skills/`, `cadence-core/workflows/`, `cadence-core/references/` finds no surviving instruction to union raw `findings` arrays. |
| FIX pass: `risk_surface` REVIEW-plan-4 (2 findings) | 9018c4af, 4e6f2f3b | **blocker `milestone.md:257`** - step 7 deleted `.planning/risk-carry/` on BOTH arms including the halt, and step 3 had already pruned the phase dirs, so a retried `/cad-land` globbed two empty roots and merged over the blocker. NOT inverted (the both-arms reason is real): step 7 now deletes on NEITHER arm and the clear moves to the one actor that can prove the halt was answered - `/cad-land` step 4, on a merge it CONFIRMED landed, which covers the retry and the manual land the user comes back to finish. A carry still on disk therefore means a close nobody finished and halting the next milestone on it is the answer; a by-hand clear is named for a branch merged outside Cadence. Three dependent sentences moved with it (milestone.md:123, :142, :186) plus `risk-surface.md`'s transient note. **high `SKILL.md:137`** - "no sibling record of the same basename" can never match, so every review landed in `unruled` and every close hard-halted. Replaced with the real rule: same trigger, same discriminator, same round, `REVIEW-`/`.md` swapped for `ADJUDICATION-`/`.json`; `-r2` pairs with `-r2` and a round-1 record never rules a round-2 review. One asymmetry stated because the corpus forces it - a LATER round's record does rule an earlier review, since v3.7.7's phase 2 (D-14's own fixture) shipped `REVIEW-risk_surface-plan-1.md` with only `ADJUDICATION-risk_surface-plan-1-r2.json` beside it, and a strict same-round test would halt exactly the close AC1 says must not halt. `risk-surface.md:177` carried the same false rule and moved with it. Five new `prose-agreement.test.mjs` assertions, each falsified red by restoring the sentence it replaces (step 7's delete, cad-land's missing step-4 clear, `same basename` in the bullet, `same basename` in the reference). Budgets re-pinned in the same commits: milestone.md 15814 -> 16400, cad-land/SKILL.md 15803 -> 17231, risk-surface.md 11451 -> 11790. `self-verify` `ok:true` `problems: []`; `prose-agreement` 67 pass / 0 fail; `npx tsc -p tsconfig.ci.json` clean (no lint command exists - `detect-commands` returns `lint:null`). |
| FIX pass: `diff` REVIEW-plan-4 (1 finding) | c9cf59b3 | **high `SKILL.md:136`** - the 3(b) bullet scanned a legacy `.planning/REVIEW-risk_surface-*.md` for a sibling record it can never have. That file is the pre-`risk-carry` AGGREGATE carry - one union of RAW findings under no discriminator - written by an older `/cad-milestone` and deleted by its step 7; step 7 deletes nothing now (the 9018c4af fix), so an interrupted pre-upgrade close leaves it at the `.planning/` root, it lands in `unruled` at every close, and nothing clears it: one stale file halts every unattended close forever. NOT dropped from the scan - a real leftover can carry a blocker nobody settled, and dropping it makes that invisible. What was missing was the REMEDY, so the bullet now states it: the file can never be ruled, every retry re-halts identically, and it is answered ONCE and BY HAND - read its findings, settle any still genuinely unfixed, delete the file - with nothing clearing it for the user because nothing can tell an aggregate somebody read from one nobody has. That is D-03's own accepted cost ('halts until it rules or clears them') given the clearing act it never named. `references/risk-surface.md` gets the same account plus the correction that step 7 deletes nothing now, so the reference stops disagreeing with the gate. Six new `prose-agreement.test.mjs` assertions inside the LND-02 arm, each falsified red on its own (drop the legacy scan; drop the never-rulable clause; drop `ONCE and BY HAND`; drop `then delete the file`; drop the reference paragraph; restore the stale `step 7 deletes it` claim) - 6/6 reddened exactly one test. Budgets re-pinned in the same commit: cad-land/SKILL.md 17231 -> 18035, risk-surface.md 11790 -> 12640. `self-verify` `ok:true` `problems: []`; `prose-agreement` 67 pass / 0 fail; `npx tsc -p tsconfig.ci.json` exit 0 (lint still `null`). |

Deviations: [deviation] The plan's "Must be true when done" asserts `node cadence-core/bin/test.mjs` is green. It is not, on this machine, and was not before this plan: the full suite reports 9 failures - `renumber remove` x5, `renumber: prose phase refs`, `renumber: a decimal cursor`, `renumber remove: a partial apply`, and `PHS-01: an unreadable git state`. Verified pre-existing and unrelated: `planning.test.mjs` alone is 27 pass / 1 fail at HEAD, at the dispatch base `35dd11a5` and at the `v3.7.7` tag alike, and the base commit's full suite is the same 9 names (3611 pass / 9 fail). Cause found rather than assumed: a stray `/tmp/.git` directory exists on this machine, so `gitDirAbove` (`cadence-core/bin/planning/renumber.mjs:65-76`) walks up from the fixture's `mkdtempSync(tmpdir())` tree and answers "a repo is above this", while `git status` in that tree exits non-zero ("Stopping at filesystem boundary") - so `uncommittedUnder` returns `unreadable: true` and `renumber remove` refuses with `unreadable-git-state` instead of removing. Reproduced by hand outside the test runner. Nothing in this plan touches that path; all three tasks verified against their own targeted runs (`prose-agreement.test.mjs` 67 pass / 0 fail) and `self-verify.mjs` `ok:true` with `problems: []`.
Open items: (1) The suite cannot be green on any machine carrying a `/tmp/.git`, because the fixtures build under `tmpdir()` and `gitDirAbove` only asks whether a `.git` entry exists above, never whether git agrees it is usable from there. Out of this plan's lease (`cadence-core/bin/planning/renumber.mjs`) and out of LND-02's scope; worth a filing of its own, since a CI runner or a colleague's box with the same stray directory sees 9 red tests with no local cause.

Fix-pass note: the pre-existing 9-failure full suite recorded in Deviations above is UNCHANGED after this pass - `node cadence-core/bin/test.mjs` is 3613 pass / 9 fail with the identical 9 names, `/tmp/.git` still present on this machine, and none of them touch a file this pass edited.

Fix-pass note (2): the same 9 pre-existing full-suite failures are UNCHANGED after this pass - `node cadence-core/bin/test.mjs` is 3613 pass / 9 fail with the identical names, the stray `/tmp/.git` still present on this machine, and none of them touch a file this pass edited.
