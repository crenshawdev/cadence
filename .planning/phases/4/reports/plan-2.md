PLAN COMPLETE
Plan: .planning/phases/4/PLAN-2.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1 - Export the fixture harness and move the status banners out | 77c9c620 | Harness exports `PLANNING`, `PLANNING_DIR`, `seamSource`, `makeTree`, `run`, `today` behind the `isEntryFile()` guard copied from issue-check.test.mjs. `planning-status.test.mjs` carries the 4 status regions verbatim (28 arms). Suite tests 3071 / pass 3071 / fail 0; 449 + 28 = 477, the pre-split planning.test.mjs count. |
| 2 - Move the cursor, phase-done and uat banners out | 5a93f99f | 9 regions into 3 files (cursor 26, phase-done 18, uat 64 arms). Every fixture those regions need was declared inside them, so nothing crossed a file boundary. Suite 3071 / 3071 / 0; `grep -c '^// --- ' planning.test.mjs` = 38 as the Verify predicted. |
| 3 - Move the audit, criteria and plan-file banners out | 73a5f5ea | 11 regions into 4 files (audit 36, criteria-coverage 41, criteria-size 11, plans 32). `blockPlanTree`/`oneIdPlanTree` sat inside the `audit` span but serve the plan-file arms, so they travel to planning-plans.test.mjs and are imported by name into planning-audit and planning-criteria-size. `GIT_FIXTURE_ENV` is exported from planning-audit and imported into planning.test.mjs for the task-record arms still resident there. |
| 4 - Move the renumber, seed-reqs, recall, detect and lease-check banners out | 3e4546e9 | 12 regions into 5 files (renumber 25, seed-reqs 8, recall 22, detect 29, lease-check 21). `renumberTree` and `leaseRepo`/`leaseCheck`/`stage` are exported from their new homes and imported back into planning.test.mjs by the two cross-command regions that drive them; `taskRepo`/`runIn`/`TASK_COMMITS` exported from planning.test.mjs for planning-recall's tasks-tier arms. Suite 3071 / 3071 / 0; planning.test.mjs alone passes, which is what proves the cross-file `leaseCheck` import resolves. |
| 5 - Move the trace-ignore, debt-harvest, milestone-prune and capture-sections banners out | 9ed65ec9 | 4 regions into 4 files (trace-ignore 9, debt-harvest 16, milestone-prune 6, capture-sections 4). planning-milestone-prune.test.mjs imports `archive` from planning-recall; its stem stays distinct from the pre-existing milestone-prune.test.mjs, which covers lib/milestone-prune.mjs. `ls planning-*.test.mjs \| grep -v planning-files \| wc -l` = 17 as the Verify predicted. |
| 6 - Move the fire-record and read-back banners out | aec03d17 | 8 regions into 4 files (adjudication 20, deferred 23, cite-count 8, task-record 7). planning-recall repoints `taskRepo`/`runIn`/`TASK_COMMITS` at planning-task-record.test.mjs, which takes `GIT_FIXTURE_ENV` from planning-audit; planning.test.mjs drops both transitional bindings and its now-unused imports. `grep -c '^// --- '` = 3, `wc -l` = 495 (Verify: under 700). |
| 7 - Declare the new stems in the test runner's planning group | 4ba85c1a | The 21 new stems named in `GROUPS.planning`; `--list` shows 26 there and zero `planning-*` under `other`. `test.mjs planning` reports 931 arms (Verify: at least 477), 7.4s wall. The group comment now describes the group as it is, with the measured figure replacing the stale "`planning` alone is ~11s". Suite 3071 / 3071 / 0. |
Deviations: none
Open items:
- The pre-existing failure the plan's Notes flagged (`milestone-prune.test.mjs:557`, the live REQUIREMENTS.md `## Active` bullet grammar) was already closed by 66ce9bd5 before this plan ran, so the measured baseline was `tests 3071 / pass 3071 / fail 0` and nothing carries forward from it.
- Three `planning.test.mjs` imports are cyclic by design and left that way: planning.test.mjs imports `renumberTree` from planning-renumber.test.mjs and `leaseRepo`/`leaseCheck`/`stage` from planning-lease-check.test.mjs, both of which import the harness back. ESM resolves it because nothing is read at module-evaluation time - every use is inside a test callback, and the imported-module `test` is a no-op. The alternative was moving the cross-command arms out of the file the plan says they stay in, which the plan's own fixture rule ("exported from its new home and imported by name, never copied") rules out.
- Verified but not asserted anywhere: 22 `planning-*.test.mjs` files now exist, one of which (`planning-files`) predates this split. No census pins the stem list, so a new stem added later still has to be named in `GROUPS.planning` by hand or it silently runs in `other`.
