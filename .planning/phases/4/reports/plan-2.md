PLAN COMPLETE
Plan: .planning/phases/4/PLAN-2.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1 - State the bulk-output rule once, in conventions.md | 08a34be | New `## Bulk tool output` section beside `## Caller-derived text`, four bullets in the same voice; the distinctive clause appears once in the tree. Re-pinned 8363 -> 9717. |
| 2 - triage-gate.md's re-arm lookup takes the file | c360346 | Fenced two-step: render redirected to `"${TMPDIR:-/tmp}/cad-rearm.json"`, then a `node -e` field read returning the `rearm` count under the envelope's own `corr`. Match is on the event's `trigger` FIELD, per the receipt paragraph's own rule. Live-measured render 14,857 B at `--phase 3`, matching the plan's figure. Re-pinned 8759 -> 9522. |
| 3 - /cad-progress --trace prints the counts without buying the brackets | 935e6f8 | Redirect plus a field read of `counts`/`roles`/`unpaired`/`capped` only; `brackets` and `outcomes` stay in the file. Every reading rule survives verbatim. Guardrail and success criterion reworded from "wrote nothing" to "stored no planning artifact", which the scratch file would have falsified. Re-pinned 9447 -> 10284. |
| 4 - /cad-report composes from the file rather than the envelope | 1c4001a | Render redirected to `"${TMPDIR:-/tmp}/cad-record.json"`; `reads --join` (measured 1,507 B) and `trace window` (5,378 B) left inline. Read-back bound stated with a worked `node -e` field read and an explicit no-`cat`/no-`Read`/no-unfiltered-`grep` rule. `<purpose>`'s "no file is written" narrowed to "no planning file is written". Re-pinned 10985 -> 12343. |
| 5 - The register and the pure rule | 5f105b4 | `lib/bulk-output.mjs` in `lib/text-transport.mjs`'s exact shape: pointer to the rule's one home, frozen `BULK_SHAPES` (3), frozen register (17 rows), three codes, one pure function. Run over every surface of this tree it returns an empty issue list; run with an EMPTY register it reports all 17 occurrences, which is what proves the scan sees them. 17 unit tests. |
| 6 - self-verify walks the register | a445685 | Check 20 called at the same point and in the same way as check 19, header entry added, `checked` names `bulk-output`, no CONTRACTS row (check 14's `lib/*.mjs` reason). Watched failing live: deleting the redirect from triage-gate.md made self-verify exit 1 with `bulk-output-inline` naming that file; restored. 152 self-verify tests green. |
| 7 - The TRN-02 falsifier, watched failing at a named SHA | 617a2a1 | Appended to `prose-agreement.test.mjs` with a `WATCHED FAILING AT 86cd45d` header, the observed output quoted, and the worktree re-watch recipe. Watched at 86cd45d: the named test fails on an assertion and the file exits 1. Also watched biting on THIS tree by deleting progress.md's redirect. Imports nothing this plan added. |
Deviations: none
Open items: The falsifier assembles the distinctive clause from its two halves (`['rides a file', 'not the transcript'].join(', ')`) and asserts the clause's presence with its own message rather than `sentenceAround`'s, because both a literal and `sentenceAround`'s needle-echoing failure text would make this file the SECOND carrier of the clause and falsify the exactly-once check it is asserting. The fuller shape - an exclusion list letting the check ignore its own file - was declined: it would have weakened AC3's `grep -rn` over `cadence-core/` from "one file" to "one file plus a waiver".
