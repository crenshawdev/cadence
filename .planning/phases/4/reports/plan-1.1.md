PLAN CHECKPOINT: blocked
Plan: .planning/phases/4/PLAN.md
Tasks: 4 of 8
| Task | Commit | Note |
|---|---|---|
| 1. The phase-directory grammar rejects a zero-padded fraction | 687bcebc | `PHASE_DIR_NAME` moved to `planning/core.mjs:107` and tightened to `/^[1-9]\d*(?:\.[1-9]\d*)?$/`; scratch tree returns exactly two `phase-dir-grammar` entries (`["1.00","1.01"]` naming `phases/1`, `["2.0"]` naming `phases/2`). Citations re-pinned in REQUIREMENTS.md and `CITATIONS`. |
| 2. The two `phases/` listing filters read that same grammar | b44febf6 | `cmdStatus`'s surviving-directory filter and `cmdRecall`'s corpus walk both call `PHASE_DIR_NAME` now. Closed-milestone tree with `phases/8` + `phases/08`: one `phase-dir` entry, `phase: 8`; `recall zarquon` returns `results: []`. |
| 3. A tree-aware spelling check, wired at the shared fire identity | 3975d813 | `phaseSpellingCollision(dir, parsed)` added at `planning/core.mjs:139`, exported, wired in `fireIdentity` (now `fireIdentity(face, dir, opts)`) ahead of the token rails. `phaseSpellingRefusal` at `:77` and `PHASE_DIR_NAME` at `:107` both unshifted. New `phase-spelling.test.mjs`, 8 arms. |
| 4. The six phase-artifact readers refuse a colliding spelling | d46bdacf | Wired at `criteria-size`, `plan-size`, `plan-overlap`, `cite-count`, `lease-check`, `uat`. The five pinned `planning.test.mjs` rows split into resolve/refuse pairs; the stale `plan-overlap --phase 08` comment in the `seed-reqs` row corrected. `phase-spelling.test.mjs` now 26 arms. TWO of the four line-pinned citations this shifted were re-pinned in this commit (`EXECUTE-10` -> `lease-check.mjs:450-453`, `VERIFY-11` -> `uat.mjs:123-127`, both in `.planning/DOCS-CLAIMS.md` and in `DOCS_CLAIMS_CITATIONS`). The THIRD is the blocker below. |
Deviations:
[deviation] Task 1's Verify says "No entry mentions `1.1`, `1.10` or `8`", but D-03 (locked by the same task) keeps the drift grouping key at the leading digit run, so the `1.00`/`1.01` entry's detail names `phases/1, phases/1.1, phases/1.10` as the legal directories they collide with. Read literally the two clauses contradict each other. Resolved in favour of D-03 and AC1's own wording ("stay legal and produce no entry"): none of `1.1`, `1.10`, `8` earns a drift entry of its own, which is what the criterion asks.
[deviation] CONTEXT D-13 says the line-pinned citations this plan breaks are `status.mjs:28` and `core.mjs:77`. Measured against the live tables, task 4 breaks THREE more that D-13 does not name, because inserting a guard near the top of a module shifts every pinned line below it: `EXECUTE-10` (`lease-check.mjs:440-443`), `VERIFY-11` (`uat.mjs:113-117`) and `skills/cad-verifier-contract/SKILL.md`'s `planning/uat.mjs:489-491`. The first two are re-pinned in d46bdacf. The third is the checkpoint.
Open items: none
