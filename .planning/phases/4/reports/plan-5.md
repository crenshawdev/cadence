PLAN COMPLETE
Plan: .planning/phases/4/PLAN-5.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1: The row door, and one home for the subcommand key | ddf2f6f | `subcommandKey` + `evaluateRow` added to `lib/arg-contract.mjs`; `TWO_WORD` moved out of `self-verify.mjs` and check 2 reads it back (`grep -c "'cursor', 'uat', 'renumber'"` = 0 there, 1 under `bin/lib/`). self-verify ok:true problems:[]; tsc clean. |
| 2: `planning.mjs` gates every declared row at its dispatch | dafa18d | Door wired at the dispatch ahead of `COMMANDS[cmd]`; UAT-8's three spellings each refuse naming their flag with the file byte-unchanged; four dead `--root` guards deleted; `--dir`, `detect-commands --root`, `trace ignore --root` and `debt-harvest --root` lines reproduced character for character; absence unchanged (`cursor set needs --phase <N>`, `capture --kind must be one of ...`, `bad-result`). Before/after matrix over all 184 flag-axis pairs: every `refuse` row now refuses and names its flag. |
| 3: One flag->sentence map, read by the door and by the trace body | 5d3143a | `TRACE_REFUSALS` folded into the one map; the trace loop composes through `argRefusal`, so a bare `--role` returns the byte-identical detail the shipped tree returns. `grep -c "needs a role name after it"` = 1, pinned by a new census in `trace.test.mjs`. `TRACE_GRAMMAR` and the string-flag loop kept: a bare `--sha` on `trace close` still drops. |
| 4: The adoption census - every declared refusal, matched against the shipped CLI | 7269cc6 | New `arg-contract-adoption.test.mjs` spawns the owning script for all 231 declared refusals across 144 entries (~6.7 s); 33 fallback/warn axes and 12 boolean entries counted as skips with reasons, counts asserted. Watched failing: reverting the door's reach reports 116 unapplied refusals naming `planning.mjs cursor set --name`. The three UAT-8 rows also pinned in `arg-contract.test.mjs`, watched failing on a `--name` downgrade, because the census SKIPS a loosened axis. |
| 5: `config.mjs get` declares `--global` and reads it off that row | a4ee0d2 | `get` row declares `--global` with its siblings' grammar; `optFile` reads it through `evaluateFlag` against `CONTRACTS['config.mjs'][cmd]`. All four baselines byte-identical; falsifier watched (deleting the row -> `unknown-key: --global`). Entry pin 144 -> 145. |
| 6: The header and the prose state the reach that shipped | 763cf23 | Module header and `conventions.md`'s `## Seam arguments` state the three things and no more; `weight-budgets.json` row re-pinned 14556 -> 15791 in the same commit and equals the file's byte size. self-verify ok:true problems:[] with no `budget-overrun` and no `unknown-flag`; `npx tsc -p tsconfig.ci.json` zero errors. |

Deviations: [deviation] The plan's Context says re-dispositioning a flag row is out of scope, and `--tokens` made that unachievable: its declared `int`/`refuse` row is NARROWER than the shipped body grammar. The body accepts a strict comma-grouped integer (`146,405`, the form this plugin prints token figures in, so the transcription its own prose models) and refuses a negative one, which `int` accepts. With `refuse` the door rejected `146,405` after the `dispatch` half of the bracket was already written, stranding the worker unpaired forever - two `trace.test.mjs` failures. Re-dispositioned `--tokens` to `value: 'fallback'` on both trace rows (bare axis unchanged, still `refuse`), with the reason stated in the table beside the other reasoned dispositions.

Open items:
- `uat merge --payload` (bare) now answers `bad-args` naming the flag instead of `no-payload`. The "never a read of fd 1" invariant holds, the `no-payload` arm still answers a missing/unreadable/empty path, and `no-payload` reaches no prose surface under `workflows/`, `references/`, `skills/` or `agents/`, so nothing branches on which of the two fires. Test updated with that reasoning.
- The plan's parenthetical falsifier for task 4 - "emptying the `--name` row's `bare` disposition to `fallback`" - does NOT redden the census by itself: a loosened axis is counted as a skip, not a failure. The door-revert falsifier the same sentence names does work. Closed by pinning the three UAT-8 rows in `arg-contract.test.mjs` instead, so both directions are now caught.
- `planning.mjs`'s numeric rows are approximations in the other direction too: `int` accepts a negative that the trace body then refuses (`--turns`, `--raised`). No behaviour changed - the body's refusal wording is identical to the door's - but the rows state less than the bins enforce.
