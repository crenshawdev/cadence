PLAN COMPLETE
Plan: .planning/phases/4/PLAN-1.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1 - Extract the shared core module | 3e17270b | `planning/core.mjs` holds all 28 named symbols, each declared once; `planning.mjs` imports the ones it still uses and still declares 32 handlers. Suite `tests 3071 / pass 3071 / fail 0`, self-verify `"problems":[]` across all 26 checks, `tsc -p tsconfig.ci.json` exit 0, `criteria-coverage` reports `version.plugin` `3.7.0` matching `.claude-plugin/plugin.json` (so `HERE` did not slip a level). The two newly-leased test files are repointed: `trace.test.mjs`'s four-sentence census reads the whole seam and keeps "exactly once"; `self-verify.test.mjs` check 12 moves `files.length` 10 -> 11 with `total === 16` and the per-callsite arm assertion untouched. |
| 2 - Move status, cursor and phase-done into modules | b323f835 | `planning/status.mjs`, `cursor-get.mjs`, `cursor-set.mjs`, `phase-done.mjs`, one handler each, bodies verbatim. `PHASE_DIR_NAME`, `phaseDirGrammarDrift` and `AGREE` travel with `cmdStatus`. `COMMANDS` arrows unchanged. Suite `3071 / 3071 / fail 0`, self-verify `"problems":[]`, tsc exit 0; `grep -c '^function cmd' planning.mjs` prints 28 and each new module prints 1. |
| 3 - Move the plan-and-criteria readers into modules | 9a3979d6 | `uat.mjs`, `audit.mjs`, `criteria-coverage.mjs`, `criteria-size.mjs`, `plan-size.mjs`, `plan-overlap.mjs`. `ORIGIN_EXEMPT` and `LEGACY_REASON` went to `criteria-coverage.mjs` (3 mentions each) and `audit.mjs` names neither (0 and 0), D-06's case. Suite `3071 / 3071 / fail 0`, self-verify `"problems":[]`, tsc exit 0; `grep -c '^function cmd' planning.mjs` prints 22. |
| 4 - Move the record-and-lease commands into modules | bca6a4b1 | `cite-count.mjs`, `seed-reqs.mjs`, `recall.mjs`, `lease-check.mjs`, `task-record.mjs`. `prose-agreement.test.mjs`'s lockfile-lease read is repointed at `planning/lease-check.mjs`, the emitting site, with `assert.match(seam, /reason: 'undeclared-files'/)` unchanged; `node --test prose-agreement.test.mjs` passes 47 of 47. Suite `3071 / 3071 / fail 0`, self-verify `"problems":[]`, tsc exit 0; `grep -c '^function cmd' planning.mjs` prints 17. |
| 5 - Move the project-scan and file-writing commands into modules | 643541b3 | `detect-commands.mjs`, `detect-surfaces.mjs`, `reads.mjs`, `capture.mjs`, `capture-sections.mjs`, `debt-harvest.mjs`, `milestone-prune.mjs`. The three `--root` arms keep their expressions in `COMMANDS`; `detect-commands --root ""` still returns `ok:false` / `bad-args`. Suite `3071 / 3071 / fail 0`, self-verify `"problems":[]`, tsc exit 0; `grep -c '^function cmd' planning.mjs` prints 10. |
| 6 - Move the trace family into one module | 402b2861 | `planning/trace.mjs` holds `cmdTrace` and `cmdTraceIgnore` with fifteen single-use companions; `recordForFire` and `recountReceipt` sat under the adjudication banner and travel here, because `trace close` is what reads them. Both `mergeLayers(` callsites carry their `warnings` destructuring verbatim (arm a, no header marker). SGT-01 repointed at `planning/trace.mjs` with the schema comparison intact - falsified by editing `workflow.max_plan_tasks` to 9 (SGT-01 alone reddens), then reverted. `self-verify.test.mjs` check 12 moves `files.length` 11 -> 12. Suite `3071 / 3071 / fail 0`, self-verify `"problems":[]`, tsc exit 0; `grep -c '^function cmd' planning.mjs` prints 8. |
| 7 - Move the risk-check family into one module | 72ac054c | `planning/risk-check.mjs` holds all three handlers with `surfaceVocabulary`, `RISK_TRIGGER` and `FIRE_RECEIPTS`. `cmdRiskCheck`'s `fail('usage', 'risk-check <run|status>')` is byte-identical and `risk-check --dir .planning` still returns it. The `mergeLayers(` callsite keeps its `warnings: surfaceWarnings` binding, so check 12 stays at twelve files. Suite `3071 / 3071 / fail 0`, self-verify `"problems":[]`, tsc exit 0; `grep -c '^function cmd' planning.mjs` prints 5. |
| 8 - Move the fire-record and renumber commands, and close the entry file | 52068a0b | `adjudication.mjs`, `deferred-record.mjs`, `deferred-list.mjs`, `deferred-carry.mjs`, `renumber.mjs`. Both renumber source rows in `planning.test.mjs` are repointed at `planning/renumber.mjs` keeping every assertion - the `lstatSync` probe, the absent `.name === '.git'` comparison, the recursive `rmSync`, and `gitDirAbove(`/`gitDirUnder(` both before the delete; `node --test planning.test.mjs` passes 477 of 477. Final state: `grep -c '^function cmd' planning.mjs` prints 0, `wc -l` prints 360, `ls planning/*.mjs \| wc -l` prints 30, and the handler total across those modules is 32. Suite `3071 / 3071 / fail 0`, self-verify `"problems":[]`, tsc exit 0. |

Deviations: none. Every `Verify:` was met as written, the `fail 0` baseline included - the two test files the previous dispatch checkpointed on are in the lease now and were updated rather than worked around.

Open items:
- `cadence-core/bin/skim.test.mjs:99` ("every shipped .mjs skims without losing a line") walks `bin/` and `bin/lib/` only, so the 30 modules now under `bin/planning/` fall outside it. Nothing fails; the coverage simply does not reach the new directory, and widening the walk is a one-line change. The file is outside this plan's lease.
- `cadence-core/bin/planning.mjs` carried one dead import, `sectionBound` from `lib/planning-files.mjs`, named only inside a comment. The import-pruning pass dropped it. A deletion the split caused, not a behaviour change.
- PLAN-1 D-06 and task 1 disagree about `DISPATCH_WINDOW_DEFAULTS`. CONTEXT D-06 calls it "genuinely shared"; measured over comment-stripped source its only reader is `cmdTrace`. Task 1's Action names it in the core list, so it is in `core.mjs` as written and `planning/trace.mjs` imports it from there. It costs nothing against AC1. Moving it into `trace.mjs` would make the by-use rule uniform; it was left where the plan put it rather than quietly re-decided.
- `self-verify.test.mjs` check 12's file count is a hardcoded figure and moved twice during this plan (10 -> 11 at task 1, 11 -> 12 at task 6). It was updated per task rather than loosened, because counting independently of the rule is the whole point of that row.
- The per-command section divider banners are layout of the old single file. Where a banner carries real prose it moved with its command verbatim; two were filed by position rather than by use and were corrected - `plan-overlap`'s banner sat above `plan-size`'s comment block, and the `version_drift` import comment explained three imports only `audit` reaches.
- `cmdTraceIgnore` is exported from `planning/trace.mjs` beside `cmdTrace` but nothing imports it: `trace ignore` reaches it through `cmdTrace`, which is the D-07 call edge. The export is kept and explained in a comment rather than removed, so the census that counts 32 handlers still finds it under its own name.
