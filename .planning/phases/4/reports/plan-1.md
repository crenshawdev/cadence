PLAN COMPLETE
Plan: .planning/phases/4/PLAN-1.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - The per-role window ceiling becomes configuration, argued from the record | a79aad2 | Six `workflow.max_dispatch_tokens.<role>` int keys (min 1) + six `config-reach.md` rows carrying the verbatim phrase `trace window report only`; the FINAL-WINDOW-PROXY argument, the p75-rounded-to-25,000 defaults rule and the per-role figures with sample sizes and the 2026-08-17 date added to `references/seams.md`, without weakening the `maxTurns: 200`-is-the-only-bound sentence; `seams.md` (22644) and `config-reach.md` (19521) re-pinned in `weight-budgets.json`. `config.mjs get` returns 200000 / 100000; self-verify 0, tsc 0. |
| 2 - The pure rule - bracket rows plus ceilings become crossings | a7646fe | `lib/window-budget.mjs`: pure (no disk, emit, exit, Date, randomness), returns crossings in self-verify's exact `{kind:'budget-overrun', file, detail}` shape with the `<n> exceeds budget <m> by <d>` tail, per-role unbudgeted counts and an unrecorded count. Ceiling not equality, no shrink arm; every row lands in exactly one bucket. 10 unit tests pass. |
| 3 - `trace window` reports the crossing off the project's own record | e63dbba | `window` arm on `cmdTrace` (`--phase` through `requirePhaseArg`, usage comment and `fail('usage')` list updated), ceilings resolved through `mergeLayers` with `warnings` bound and ridden when non-empty, `DISPATCH_WINDOW_DEFAULTS` as the named unset-layer fallback, `'trace window': ['--phase']` added to `CONTRACTS` in the same commit (D-19). 7 CLI cases added. Live run on this repo: 25 `budget-overrun` problems, compared 153, unrecorded 9, unbudgeted `{"(no role)":10}`. `window-budget.test.mjs` + `trace.test.mjs` 126 pass, self-verify 0, tsc 0. |
| (checkpoint resolution) - the two leased census pins | 5f2230f | Lease GRANTED by the user for `cadence-core/bin/self-verify.test.mjs`, two lines only: the two-segment family token `workflow.max_dispatch_tokens` added to the `placeholder keys expand` fixture, and check 12's independent `mergeLayers(` callsite census raised `assert.equal(total, 12)` -> 13. Watched failing first (exactly those 2 of 2126). Full suite now 2126 pass / 0 fail; the granted path is declared in PLAN-1's frontmatter `files:` so `lease-check` is `ok:true`. |
| 4 - `/cad-report` reaches the check | 609bb20 | `read_record` takes a third seam call - `planning.mjs trace window [--phase <N>]` - and names what it returns (`ceilings`, `problems`, `compared`, `unbudgeted`, `unrecorded`); `compose` gains a `Window budget (from `trace window`)` shape line and a rule stating the crossing is a FINDING that refuses nothing, on the SAME final-window proxy the gap terms already describe, printed as two numbers and never their quotient. `report.md` re-pinned 9605 -> 10985 in the same commit (D-20). self-verify 0, tsc 0. |
| 5 - The MSR-03 falsifier, watched failing at a named SHA | 86cd45d | One CLI-only case against a scratch `.planning` holding a crossing `cad-executor` bracket (275285) and an under-ceiling `cad-verifier` one (47148): the crossing comes back `budget-overrun` with the role named and the arithmetic tail, the under-ceiling dispatch is `compared` but silent, nothing is refused. Header `WATCHED FAILING AT 9b1fe53` (the tip of the unpatched tree, parent of task 1's a79aad2) carrying both observed outputs verbatim and the re-watch recipe. Re-watched: at 9b1fe53 `node --test cadence-core/bin/window-budget.test.mjs` exits 1 with this case failing on its FIRST assertion (`{"ok":false,"reason":"usage",...}`, `false !== true`), 10 pass / 8 fail. |
Deviations: [deviation] the plan's task-1 Action states per-role p75 figures today's `.planning/trace.jsonl` does not reproduce under either nearest-rank or linear interpolation - it asserts `cad-executor` p75 185,999 (n=72), `cad-verifier` p75 83,908 (n=24) and `cad-planner` n=27, while the record measured 2026-08-17 gives `cad-executor` p75 182,631 (n=72), `cad-verifier` p75 82,633 (n=24) and `cad-planner` p75 188,135 at n=28 (the phase-4 planner dispatch closed after the plan was written). Wrote the MEASURED figures into `seams.md` and the schema purposes and stated the nearest-rank rule that produced them; no default changes, since every figure still rounds UP to the same 25,000 multiple, and the task's own Verify grep (`188,135`, `2026-08-17`) still matches.
[deviation] the plan's `## Must be true when done` requires `node --test cadence-core/bin/*.test.mjs` green, and that could not be reached from this plan's original `files:` lease - resolved by the user's explicit two-line grant on `self-verify.test.mjs` (commit 5f2230f).
Open items: The falsifier's re-watch recipe copies `lib/window-budget.mjs` beside this test file as well, because the UNIT tests at the head of the same file import it at load time and a missing module would fail the file before any assertion ran. The falsifier case itself imports nothing this plan added - it reaches the seam through `execFileSync` only - and the copied pure module is inert on the unpatched tree, so the case still dies on its assertions, which is what the re-watch observed.
-  `cadence-core/references/config-reach.md` was re-pinned in `weight-budgets.json` alongside `seams.md` although task 1's Action names only `seams.md`; the six new reach rows grow that surface and D-20 plus the task's own `self-verify` exit-0 Verify require it. Both files are inside this plan's lease.
`self-verify.test.mjs:1555`'s test TITLE still reads "ELEVEN callsites over EIGHT files" while the pins are now 13 and 9 - it was already stale before this dispatch (pinned 12/9). The grant was two lines only, so the title was left alone; it belongs to PLAN-2's lease.
