PLAN COMPLETE
Plan: .planning/phases/4/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1. The phase-directory grammar rejects a zero-padded fraction | 687bcebc | `PHASE_DIR_NAME` moved to `planning/core.mjs:107` and tightened to `/^[1-9]\d*(?:\.[1-9]\d*)?$/`; scratch tree returns exactly two `phase-dir-grammar` entries (`["1.00","1.01"]` naming `phases/1`, `["2.0"]` naming `phases/2`). Citations re-pinned in REQUIREMENTS.md and `CITATIONS`. |
| 2. The two `phases/` listing filters read that same grammar | b44febf6 | `cmdStatus`'s surviving-directory filter and `cmdRecall`'s corpus walk both call `PHASE_DIR_NAME` now. Closed-milestone tree with `phases/8` + `phases/08`: one `phase-dir` entry, `phase: 8`; `recall zarquon` returns `results: []`. |
| 3. A tree-aware spelling check, wired at the shared fire identity | 3975d813 | `phaseSpellingCollision(dir, parsed)` added at `planning/core.mjs:139`, exported, wired in `fireIdentity` (now `fireIdentity(face, dir, opts)`) ahead of the token rails. `phaseSpellingRefusal` at `:77` and `PHASE_DIR_NAME` at `:107` both unshifted. New `phase-spelling.test.mjs`, 8 arms. |
| 4. The six phase-artifact readers refuse a colliding spelling | d46bdacf | Wired at `criteria-size`, `plan-size`, `plan-overlap`, `cite-count`, `lease-check`, `uat`. The five pinned `planning.test.mjs` rows split into resolve/refuse pairs; the stale `plan-overlap --phase 08` comment corrected. `phase-spelling.test.mjs` now 26 arms. Two of the four line-pinned citations re-pinned here (`EXECUTE-10`, `VERIFY-11`); the third was the checkpoint. |
| 4b. Checkpoint resolution: the third shifted citation | 049b26ee | Lease amended to carry `skills/cad-verifier-contract/SKILL.md`. `planning/uat.mjs:489-491` -> `:499-501` in that contract and in the matching `CITATIONS` row. `node --test cadence-core/bin/citation-census.test.mjs` green (5 pass, 0 fail). |
| 5. The queue and record commands refuse it too, and `capture` states its exemption | 5207717f | Wired at `deferred-carry.mjs`, `deferred-list.mjs` and `trace.mjs`'s shared `append`/`close` body; `capture.mjs` took D-08's exemption comment. `trace.test.mjs`'s `1.1`/`1.10` rows survived unchanged, as the task predicted. `phase-spelling.test.mjs` now 37 arms, including the two exemption arms (`capture` tag, `trace render`). |
| 6. A registered census pins the guarded-callsite count | 068aaed5 | `CALLSITES` in `phase-spelling.test.mjs`: 21 callsites, 10 tree-aware, 2 unconditional, 9 exempt, keyed by file + enclosing function + ordinal. `CENSUSES` row `phase-spelling-callsites`, holder that test file, subjects `['cadence-core/bin/planning/']` alone. Falsified: a throwaway callsite in `cmdPlanSize` failed `test.mjs` with `plan-size.mjs:42 (cmdPlanSize #2)`, reverted before the commit. `PHASE5_AT_RISK` in `planning-lease-check.test.mjs` gained the new holder - a set assertion over which censuses that historical lease puts at risk, not a count; the half-the-plans replay rail still passes. |
| 7. The grammar is stated once, in `roadmap-phases.md` | 7fd1c28c | New `## The phase directory` section in `roadmap-phases.md` carrying the whole grammar and the `2.0` sentence; the four paraphrases (`conventions.md`, `cad-health` rule 5, `new-project.md`, `adopt.md`) retired to pointers, and the `PHASE_DIR_NAME` comment repointed. `grep -rn "no zero-padding"` and `grep -rn "bare integer"` over `skills/`, `cadence-core/workflows/` and `cadence-core/references/` now return exactly one line each, both in `roadmap-phases.md` (four each before). All five weight budgets re-pinned in the same commit - `adopt.md` GREW 20741 -> 20778, the other four shrank. `self-verify` `problems []`. |
| 8. The citation census survives its own requirements retiring | 53f5a1b6 | `CITATIONS.length >= 3` replaced with a non-vacuity floor of one, the reason stated in a comment beside it; the set-equality message gained the delete-the-row remedy. `DOCS_CLAIMS_CITATIONS`'s `>= 4` left alone. Falsified: with both `SPL` citations and their two rows removed the census still passes at one row; reverted before the commit. Whole-tree gate: `node cadence-core/bin/test.mjs` 3263 pass / 0 fail, `npx tsc -p tsconfig.ci.json` exit 0, `self-verify` `problems []`. |
Deviations:
[deviation] Task 1's Verify says "No entry mentions `1.1`, `1.10` or `8`", but D-03 (locked by the same task) keeps the drift grouping key at the leading digit run, so the `1.00`/`1.01` entry's detail names `phases/1, phases/1.1, phases/1.10` as the legal directories they collide with. Read literally the two clauses contradict each other. Resolved in favour of D-03 and AC1's own wording ("stay legal and produce no entry"): none of `1.1`, `1.10`, `8` earns a drift entry of its own, which is what the criterion asks.
[deviation] CONTEXT D-13 says the line-pinned citations this plan breaks are `status.mjs:28` and `core.mjs:77`. Measured against the live tables, task 4 breaks THREE more that D-13 does not name, because inserting a guard near the top of a module shifts every pinned line below it: `EXECUTE-10` (`lease-check.mjs:440-443`), `VERIFY-11` (`uat.mjs:113-117`) and `skills/cad-verifier-contract/SKILL.md`'s `planning/uat.mjs:489-491`. The first two were re-pinned in d46bdacf; the third forced the blocked checkpoint.
[deviation] The plan's `files:` lease did not name `skills/cad-verifier-contract/SKILL.md`, so the third re-pin was out of lease. The orchestrator amended the lease to add that path; the re-pin landed as 049b26ee under the amended lease, `lease-check` `ok:true`.
Open items:
- Three planning-record citations of `cadence-core/references/conventions.md` by LINE are now 8 lines stale, because task 7's retirement shrank the phase-directory bullet from 12 lines to 4 and everything below it moved up: `conventions.md:49` (cited from `.planning/phases/3/SUMMARY.md:141` and `.planning/phases/3/reports/plan-3.md:11`), `conventions.md:38-66` (`.planning/phases/2/CONTEXT.md:117,251`) and `conventions.md:98`/`:136` (`.planning/ARCHIVE.md:233`, `.planning/phases/1/ADJUDICATION-risk_surface-plan-1.json`). Not repointed: these are records of what was true when written, which is the disposition phase 5's D-04 already gave archived citations, and no check reads them - `citation-census.test.mjs` walks only `.planning/REQUIREMENTS.md`'s `## Active` section and DOCS-CLAIMS rows naming the planning seam. Live surfaces under `skills/`, `cadence-core/workflows/`, `cadence-core/references/` and `cadence-core/bin/` cite `conventions.md` by line nowhere, measured 2026-08-25.
