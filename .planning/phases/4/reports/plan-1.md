PLAN COMPLETE
Plan: .planning/phases/4/PLAN-1.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1 - Teach the predicate the fix commit, and give it a third answer | cdd99a03 | `unfixedFromEntries` returns `{filing, haltingSurvivors, halting}`; a usable `fix_commit` is asked first and excludes the entry from all three. 7 new arms drive the entries face directly (the 6 the Verify names, plus a blank-`fix_commit` arm proving "usable" means non-blank). filing-decision.test.mjs 47/47, tsc 0. `adjudication-record.test.mjs:523`'s `deepEqual(..., {filing, haltingSurvivors})` broke on the widened return and was repaired in the same commit - caused by this task, file in the plan lease. |
| 2 - Retire issue filing's own fix-commit filter | 5fe59741 | Filter and `notYetFixed` gone; `grep fix_commit cadence-core/bin/issue-filing.mjs` now returns nothing at all. issue-filing.test.mjs 34/34 with no arm deleted, self-verify.test.mjs 177/177, tsc 0. Two figures in the already-committed arm moved because the exclusion moved: `raised` 6 to 5 (the fixed entry is no longer in the module's answer) and `already_fixed` 1 to 0 (it counts what THIS face removed, which is none). Envelope keys unchanged; nothing outside this seam and its own test reads either key. |
| 3 - Pin the one definition with a census row | 2ca18f21 | `HELPERS` row `the genuinely-unfixed test (unfixedFromEntries)`, home `lib/filing-decision.mjs`, pattern built from an escaped string so the file is censused by its own walk. Matches exactly one file across the tree; `lib/adjudication-record.mjs`'s composition-time presence rule does NOT match it. Redden-on-demand proved: pasting the matched body into a second `.mjs` failed the arm naming both files, probe removed, 11/11 green after. No `lib/census-registry.mjs` row added, per the task. |
| 4 - Prove both faces still agree, and that a fixed override stops tripping the settle guard | 74e2b57f | Two-face fixture gained a sixth entry carrying both `overridden: true` and a fix commit; `filing` still deep-equals the payload face's `findings`, and a new assertion pins `halting` empty over a record `buildEntries` accepted. New arm `LND-02: a cleared halt that ALSO names a fix commit takes a reasonless receipt` accepts the `gate_pass`, and asserts both markers reached the stored record. Falsified: removing the precedence line from `filing-decision.mjs` fails exactly that arm and nothing else. The pre-existing refusal over a commit-less record still refuses, unedited. `planning/trace.mjs` untouched. adjudication-record 52/52, planning-adjudication 29/29, self-verify `ok:true`, tsc 0. || fix pass - `risk_surface` FAIL on the committed range | 3d8bae48 | One `high`, `lib/filing-decision.mjs:138`: any non-blank string was trusted as proof of a committed fix, so `fix_commit: "not-a-commit"` on an unoverridden survived blocker emptied all three sets and the fail-closed rail opened on the exact input it exists for. Fixed by an exported `usableFixCommit` shape check (7-40 hex, no I/O), asked in the same one pass. The grammar is this module's own copy of the composer's private `FIX_COMMIT`, pinned BEHAVIOURALLY by a new `adjudication-record.test.mjs` row that drives one 17-value table through `buildEntries` and through the predicate and asserts they accept and refuse the same members - the third hand-maintained-then-compared pair in that file. Falsified: loosening the predicate back to non-blank fails exactly the three new arms and nothing else. filing-decision 51/51, adjudication-record 53/53, helper-census + prose-agreement 76/76 (the census row's conjunction is untouched, and `confirmed and not fixed` survives), tsc 0, self-verify `ok:true`. |

Deviations: [deviation] Task 1's Action stated "Usable here means a non-blank string and nothing more ... do not re-validate the value or call anything to check it", reasoning that `lib/adjudication-record.mjs` already refuses a malformed value at composition time. That premise is false for the one set the constraint governs: `halting` is by the plan's own Notes a rail over records that module NEVER saw, so a composition-time check cannot reach them. The `risk_surface` review found the hole and the fix pass added the shape check the Action forbade, keeping it to one grammar by pinning it against the composer's behaviour rather than restating a rule.
Open items: `node cadence-core/bin/test.mjs` has 9 failures in `planning.test.mjs`'s `renumber remove` / `PHS-01` arms. Confirmed PRE-EXISTING: stashing this pass's three files and re-running that file fails identically at HEAD, and nothing here touches renumber. Also: declined widening `unfixedFromEntries` with a fourth `fixed` array so `already_fixed` could keep reporting a live number - Task 2's Verify only asks that the already-fixed medium not be filed, and the key stays truthful at zero as a count of this face's own removals; add the array when a task states that the figure has to be visible at the seam again.
