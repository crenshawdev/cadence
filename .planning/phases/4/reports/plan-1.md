PLAN COMPLETE
Plan: .planning/phases/4/PLAN.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - The claim dates itself, with the sidecar's name spelled once | c3fd6eca | Verify (a) printed `trace.1.jsonl.claim /tmp/x/trace.1.jsonl.claim`; (b) `grep -rn 'trace\.1\.jsonl\.claim' cadence-core/` returned 0 lines - `ROTATION_CLAIM_FILE` is derived from `ROTATED_TRACE_FILE`; (c) `node cadence-core/bin/test.mjs planning` 1045 pass / 0 fail. Five existing `siblings()` expectations moved to a three-file `ROTATED_SET` through the imported constant. A failed sidecar write is swallowed rather than allowed to reach the catch arm, which would otherwise send an unwritable root down the no-hard-links rename fallback; no sidecar reads as live (D-02), which is exactly the pre-TRC-09 behaviour. `npx tsc -p tsconfig.ci.json` clean; `detect-commands` reports `lint:null`. |
| 2 - Ignore the sidecar, and keep a leaked one reddening the race rows | 430659a2 | Verify (a) `git check-ignore -v .planning/trace.1.jsonl.claim` exited 1 with no output before the edit, exits 0 after naming `.gitignore:34`; (b) exactly one line in `.gitignore`; (c) `node cadence-core/bin/test.mjs planning` 1046 pass / 0 fail with the new row. `npx tsc` clean. |
| 3 - An abandoned claim reads as abandoned, and reaches the eviction the module already has | 593a343b | Verify (a) BEFORE (measured on 93edea1e, ahead of task 1): `{"written":true,"corr":"1-abc1234"}` in 259 ms, `sameIno:true`, `nlink:2`, live record 1,048,675 bytes - over the bound with the claim standing. AFTER: `{"written":true,"corr":"1-abc1234"}` in 1 ms, `sameIno:false`, `nlink:1`, live record 354 bytes, sibling 1,048,575 bytes. (b) same root padded well UNDER the bound: `rotateTrace(dir, 120)` returned `{"rotated":false}` with `sameIno:true`, `nlink:2` and no `.evict.` name in the root - the re-stat over the trigger refused a rotation this writer did not observe, even with the sidecar reading abandoned. `node cadence-core/bin/test.mjs planning` 1046 pass / 0 fail; `npx tsc` clean. |
| 4 - Prove the reclaim on a claim created directly, and prove the cost stops | 913b0495 | `node --test cadence-core/bin/trace.test.mjs` 166 pass / 0 fail with both rows reported: `an ABANDONED claim is reclaimed and the record rotates (TRC-09)` 1.68 ms, `after the reclaim the next append stops paying the budget` 1.11 ms. `node cadence-core/bin/test.mjs planning` 1048 pass / 0 fail. Measured second append after the reclaim: 0 ms, against the 252/252/255 ms per append measured 2026-08-27 while the claim stood; `nlink` on the live record reads 1. `npx tsc` clean. |
| 5 - Prove both sides of the discriminator - a fresh sidecar and no sidecar both leave the claim | 252ba5a9 | Verify (a) `node --test cadence-core/bin/trace.test.mjs` 168 pass / 0 fail, both rows reported at 254.4 ms and 253.4 ms - roughly `ROTATE_WAIT_MS`, which is the live arm waiting out its budget as expected. (b) `node cadence-core/bin/test.mjs` 3488 pass / 0 fail across every group. (c) `node cadence-core/bin/self-verify.mjs` `{"ok":true,...,"problems":[]}` over 30 checks. `npx tsc` clean. |
Deviations:
[deviation] The plan's `## Must be true when done` asserts "A rotation that COMPLETES leaves no sidecar behind - the planning root holds exactly `trace.1.jsonl` and `trace.jsonl` after the existing six-writer race", and task 4's Action repeats it as `siblings(dir)` being `[ROTATED_TRACE_FILE, 'trace.jsonl']`. Task 1's Action and the plan's own Notes state the opposite and give the reason: `held` is already false at `renameSync(temp, file)` while the carry-back loop still runs, so an unconditional unlink in the `finally` deletes the FRESH sidecar of a second process that legitimately took the claim in that window, leaving a standing claim with no sidecar - which D-02 reads as live forever and defeats TRC-09 permanently. The Notes record this as the `plan` review gate's settled answer, reversing CONTEXT's fourth flagged assumption. Built task 1's side: the release is guarded by `held`, a completed rotation leaves an inert sidecar, and the root holds three files. Every `siblings()` expectation - the five that already existed including the six-writer race row, plus the new rows - asserts the three-file set through the imported constant, so a sidecar leaked on an arm that still HOLDS the claim still reddens them. The bullet's other half landed as written: `git check-ignore` exits 0 naming `.gitignore:34`.
[deviation] Task 1's construction (write the sidecar immediately before EVERY `linkSync` attempt) and task 3's (read the sidecar's age in the `EEXIST` arm) are mutually exclusive as literally written: the pre-write overwrites the only evidence a killed claimant left, so `claimAbandoned` would always be reading this process's own fresh stamp, `abandoned` would always be false, and task 3's `Verify:` (a) could never fire. Kept the pre-write - its record-destroying window is real and the after-link ordering has no defence against it - and took the abandoned reading from a `claimAbandoned` call made immediately BEFORE the write on each attempt. The put-back confirm task 3 requires still works and still answers the question the plan wrote it for: it compares the sidecar's `mtime` against the one this process itself stamped, so a refresh by a second reclaimer that won the eviction race is detected and the sibling is put back, while this process's own stamp is not mistaken for one.
Open items:
- A claim carrying NO sidecar - one taken by the code that shipped before TRC-09 - now gets one stamped by the first contender that meets it, because every claimant writes before it links. It reads LIVE on that pass (D-02, proved by task 5's second row) and becomes reclaimable 30 s later rather than never. That is a consequence of the pre-write ordering, not a decision the plan states either way.
- Declined a `.gitignore` rule for the scaffold-time surface: `TRACE_IGNORE_LINE` and `ROTATED_IGNORE_LINE` in `cadence-core/bin/planning/trace.mjs`, which `cmdTraceIgnore` writes at `/cad-new-project` time and `/cad-health --check` reports on, do not name the sidecar - so a killed rotation strands it in a USER's working tree exactly as it would in this one. CONTEXT's scope boundary and D-06 name this repository's own `.gitignore` alone; the plan's Notes raise it for the human. Same shape as the item below.
- Still deferred by CONTEXT and untouched: `.gitignore` rules for the `trace.jsonl.rotate.<pid>.<rand>` temps and the `trace.1.jsonl.evict.<pid>.<rand>` files a killed rotation also strands.
- The put-back branch inside the abandoned eviction is not deterministically reachable from a single-threaded in-process test - the only thing between the age read and the confirming re-read is the rename itself - so it is carried by inspection against `lib/capture-file.mjs:254-272`, which it mirrors. D-08 rules out a spawned holder for this phase, so no test pretends to cover it. The plan's Notes state this.
- `detect-commands` reports `lint:null` for this repository, so no lint command was run at any task; `npx tsc -p tsconfig.ci.json` is the whole static-analysis surface and was clean at every commit.
