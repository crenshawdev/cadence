PLAN COMPLETE
Plan: .planning/phases/5/PLAN-2.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Seal the generation on the reads rotation marker | da6f21a2 | `carried_bytes` added after `file` on the marker `rotateReads` writes, taken from a `statSync` of the LIVE path immediately before the `linkSync` and omitted where that stat throws. New row asserts it equals `statSync(rotatedReadsPath(d)).size`, that `isReadsRotationMarker` still answers true, and that the marker's key order is `ts, event, file, carried_bytes`. 68/68 read-trace rows pass; `npx tsc -p tsconfig.ci.json` exit 0. |
| 2 - Finish the carry the cut never made | 5fba7c1f | Rescue runs after the swap, scoped by a new `leftover` variable so the abandoned-claim eviction (where the evicted path is a second name for the LIVE inode) never triggers it. The seal is read off the HEAD of the record this cut carried away, through a new module-local `sealOf(record)` that fails closed on unreadable, unparseable, marker-less and non-numeric. No seal rescues nothing. A failed stat/read/split/append states `shortfall` beside `rotated:true`; a failed stat states `shortfall:null`. The docblock's "NOTHING CROSSES THE CUT" paragraph now names the one exception. 2 new rows, 70/70 pass. Reproduction proved: with `lib/read-trace.mjs` stashed, the AC1 row FAILs "the racing writer's record is in NEITHER file". `npx tsc -p tsconfig.ci.json` exit 0. |
| 3 - Reserve the rotation marker in the reads admission check | dd84d56e | Marker construction lifted into a module-local `readsRotationMarker(sealed)`; `rotateReads` writes it and `READS_MARKER_BYTES` measures it once at load, rendered at `Number.MAX_SAFE_INTEGER` so the reserve is an upper BOUND rather than a sample of one instance. `appendRead`'s refusal is now `pending + READS_MARKER_BYTES >= MAX_READS_BYTES`, reason unchanged. Two new rows: a `MAX-8` line at a bound-full record is refused `oversized-record` with the record byte-identical and nothing rotated; a line one byte inside the reserve rotates and leaves the live record at or below the bound. 72/72 pass; `npx tsc -p tsconfig.ci.json` exit 0. |
| 4 - Confirm the claim on the leftover-generation eviction too | b2a3e295 | The `if (abandoned)` guard around the confirm-after-claiming removed, so both eviction arms re-stat the sidecar and restore the sibling where the mtime is not the one this call published (`mine === null` included). Both comments stating the exclusion rewritten to say why the discriminator does not answer - it is read before the eviction rename. New row makes the sidecar path a directory so `publish` cannot date it: the leftover generation is byte-identical, no `.evict` path is left, and the writer's own record is in the live record. 73/73 pass. Reproduction proved: pre-fix the row FAILs "the generation was destroyed by an eviction that could not confirm its claim". `npx tsc -p tsconfig.ci.json` exit 0. |
| 5 - Prove the rotation still reaches the seam and the marker no fold | d9eda28e | Test-only, no production code. Two rotations through `appendRead` into a `.planning` root, then `planning.mjs reads` through the existing `seam` helper: `reads.rotated.file` names the sibling on the same `ts` as the live record's marker, `carried_bytes` appears nowhere in the envelope, and `calls`, `byAgent`, `byTool` and `topTargets` each carry the one real read and nothing else. A second row pins the in-process half. 75/75 read-trace rows pass. |
Deviations: [deviation] task 5's Action asserts that `summarizeReads` and `joinReads`, handed a marker carrying the new field, produce no call, no `byAgent` row and no `unresolved` row. Measured: they produce exactly those - `summarizeReads([marker]).calls` is 1 and `joinReads([marker], []).unresolved` is 1 - because both fold every object they are handed by design, which `isReadsRotationMarker`'s own docblock at `lib/read-trace.mjs:140-144` states as the reason the predicate exists. The filter is `planning/core.mjs`'s, not the folds'. The row therefore asserts the guard that is real: the predicate still answers `true` for a marker carrying `carried_bytes`, the unfiltered marker WOULD be billed (asserted, so the filter's load-bearingness is visible), and filtered the way `readReadsRecords` filters it reaches neither fold. The plan's `## Must be true when done` line - the marker in none of `calls`, `byAgent` or the unresolved/coordinator split - is proved on the seam envelope, where it is a fact.
Open items: `publish()` nulls `pending` even when its `renameSync` throws, so on a root whose sidecar path cannot be written the private stamp `reads.1.jsonl.claim.<pid>.<rand>` is left behind - visible in the task 4 row's fixture. Pre-existing, identical to the arm plan 1 reported in `lib/trace.mjs`, and inert residue rather than a lost record; a task that states the release contract should carry both.

Full suite: `node cadence-core/bin/test.mjs` 3638/3638 pass (no `workflow.test_command` is set and the repo has no `package.json` scripts block; `test.mjs` is the project's own runner). `node cadence-core/bin/self-verify.mjs` `ok:true`, 0 problems. `npx tsc -p tsconfig.ci.json` exit 0. No `lint` command exists (`detect-commands` reports `lint:null`).
