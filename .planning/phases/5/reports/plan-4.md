PLAN COMPLETE
Plan: /code/cadence/.planning/phases/5/PLAN-4.md
Tasks: 8 of 8

| Task | Commit | Note |
|---|---|---|
| 1 - Capture actual pause work | c47d1fb97edfbae20ea5a46a7960f7d30cf31886 | Added the pause domain and read-only Git adapter. Captures exact one-line text, scoped phase and retained name/total provenance, HEAD, branch, index, dirty material and explicitly authorized paths. Real temporary repositories cover clean, staged, unstaged, untracked, deleted, renamed, Unicode, non-UTF-8 and shell-metacharacter paths. Missing phase/note and multiline text refuse. A retired interrupted-close cursor keeps its name, total and original provenance with null lifecycle current. Capture stages and commits nothing. Verify prediction: 49 library tests passed, 0 failed, exit 0. Held on both runs. |
| 2 - Durable branch gates | e59a725c6b6d525869007fa89d7a96ef2301699d | Wired internal server/resident/factory pause requests. Persists protected, detached, missing/unknown-base, unrelated-history and integration questions before dependent branch changes. Tests cover occurrence-scoped answers, fresh-child unanswered readback, local branches versus tags, shared-history base advancement, active-version priority, title fallback, missing/published versions, trunk, off, off-base and unavailable effective config. Verify prediction: 109 binary tests passed, 0 failed, exit 0. Initial run: 107 passed and 2 fixture failures. Both corrected runs held the prediction. |
| 3 - Pause commit risk gate | 01828c91100e45028747fed39e7c3ec58fea2f9a | Added pure staged-diff classification and the pause-local gate over Git's actual staged tree, preserving base, null head, index identity, staged scope, authored paths, checked/matched/inconclusive states and removed-line signals. Binary-created store receipts are omitted by import-manifest provenance while active config remains authored; the four existing direct `.planning/phases/<phase>/` review-artifact patterns stay exact. Durable surface and review needs, contracted results, all five consequences, deferred queue evidence, reasoned occurrence-scoped review overrides and one narrowed re-arm survive restart. Verify prediction: 116 passed, 0 failed, exit 0. Initial run had the predicted count but observed 115 passed, 1 failed, exit 101. The corrected exact test and two corrected full runs passed; final Verify was 116 passed, 0 failed, exit 0. |
| 4 - Authorized WIP commit | 8c292297c744fae3d01b122c1c9dc323951fb062 | Stages only captured authorized source paths after branch/base authorization, keeps policy-separated planning documentation and binary-owned receipts outside the WIP, runs Task 3's risk gate on the resulting index, and retains the real WIP SHA. The Git adapter rechecks branch, HEAD, index tree and guarded worktree before and after ordinary hooks. Real repositories prove exact binary bytes, deletion, staged rename, Unicode/spaces, planning separation, unauthorized dirt refusal, clean-tree skip, changed-index refusal, local identity and a failing pre-commit hook that leaves the staged work intact. Verify prediction: 120 passed, 0 failed, exit 0. Initial run observed 119 passed, 1 failed, exit 101; the focused repair passed 1 of 1 and the corrected full Verify held at 120 passed, 0 failed, exit 0. |
| 5 - Committed resume record | c7ed9ad73bf6171d0fedff68c51e9bdd8ba618de | Persists the exact sentence as both reason and `PausedNext`, full phase provenance, guarded branch/head and config identity in one versioned invocation-authorized record whose occurrence remains pending. A second guarded docs commit stages the writer-confirmed store participants, authorized planning documentation and compatible deferred artifacts, verifies committed bytes against the acknowledged `View`, reopens the store and requires clean Git status before success. Dirty starts make WIP plus docs commits; clean starts make only docs. A store failure after WIP reports partial state, and retry creates neither another WIP nor duplicate override. The D-14 projection now gives the gate a real authored-only Git tree identity while the commit adapter separately guards the full index. Focused predictions for exact record/readback, partial retry and D-14 termination were each 1 passed, 0 failed, exit 0; all held. Full Verify prediction: 123 passed, 0 failed, exit 0; held. |
| 6 - Production resume selection | 288ec20c0247a58c0326e9ce9c48ef3f719cc21e | Connects the actually committed native pause to PLAN-3's production selector and continuation reader. A matching phase returns the exact ordinary sentence as a resume action, but continuation remains at `AwaitAcceptance` until a scoped progress answer is durably recorded. Fulfillment and supersession each end the occurrence permission while preserving pause history. Stale native pauses remain stored context and route normal current-phase work. Matching and stale retained legacy sentences follow the same rule without a fabricated native reason, while their imported cursor provenance stays retired and readable. Existing cold/warm conflict tests remain in the same run. Verify prediction: 125 passed, 0 failed, exit 0; held. |
| 7 - Process-loss recovery | 69381b131014729cbd4165a4ea90cf72262da163 | Adds test-only production barriers after the WIP commit, after confirmed resume-record persistence and after the verified final docs commit before reply. A real child is killed with SIGKILL at each boundary. Fresh retries preserve the exact committed source and store-participant bytes, create exactly the intended two commits without duplicates, finish cleanly and expose the same pending native resume action and reason to an independent reader. Focused Verify prediction: 1 passed, 0 failed, 126 filtered out, exit 0; held. Full Verify prediction: 127 passed, 0 failed, exit 0; held on the initial and strengthened runs. |
| 8 - Phase acceptance evidence | eefa03a3f1d698219c44fe4d82e8a19eab95aa75 | Adds an executable seven-row acceptance inventory that names the existing tests for killed-checkpoint readback, checker revision/disposition, gate answer/unanswered state, all four override forms and scoped expiry, the authored W1-W9/P12-P89 selector table, guarded pause/Git/restart behavior and accepted-result refusal. It also requires the three unit-test modules to remain registered and the existing cadence_version-only MCP assertion to remain executable. Verify predicted 49 library and 128 binary tests, 230 package tests total, all seven obligations present, and exit 0. Every target passed and the two predicted unit counts held, but the actual package total was 237 because the estimate omitted the existing 7-test next_action integration target. Clippy, TypeScript, Node and the frozen-tree diff all exited 0; Node passed 3,753 tests. |

Deviations: four verification/history entries detailed below. No acceptance criterion or locked decision was redefined after D-14.

Open items: one unrelated workspace item. Untracked `docs/architecture/overview.md` appeared during Task 3 and remains untouched.

## Resumed run details

Resumed at `e5c213ea` from the committed Task 2 state on branch `cadence/binary-owns-process`. The prior checkpoint report was rotated before this dispatch's first report write to `reports/plan-4.1.md`; this report retains its completed task rows and checkpoint history. D-14 was read as the authoritative resolution before Task 3 implementation. No package was installed, persistent Git configuration changed, hook disabled, push performed, or file outside the plan lease edited. Cargo used `TMPDIR=/tmp` and an empty `RUSTC_WRAPPER`; subprocess stdin was ignored where unused.

Task 3 obtains staged scope with `git diff --cached --name-only -z --no-renames`, obtains the staged tree identity with `git write-tree`, reads only the authored pathspec through a no-ext-diff/no-textconv binary diff, and rechecks HEAD and index identity. The classifier ports the frozen path segment, base-name, extension and changed-line ordering without runtime JavaScript. Non-UTF-8 or binary material remains inconclusive.

D-14 is implemented as provenance, not a filename list. `Session::import_manifest().created` supplies binary-created paths and `ImportManifest.active` removes active configuration from that set. The staged adapter never contains the three current store filenames. A test passes an arbitrary `future-store-participant.jsonl` through the same receipt set and proves it is outside the read. The frozen review-artifact exclusions remain exactly one file below `.planning/phases/<phase>/`; a nested lookalike is read and fires.

The accepted result is a native `AcceptedResult` under contract `cadence.pause.risk-surface.v1` with an evidence reference and an exact serialized fire. The service refuses malformed results, wrong fire identity, wrong staged tree or missing producer evidence. A staged index is always represented with `staged: true`, `head_id: null` and its tree id; the PLAN-2 review receipt uses an explicitly tagged `index:<tree-id>` endpoint and does not claim it is a commit.

## Verification corrections

[deviation] Task 2's first Verify predicted 109 passed, 0 failed, exit 0; observed 107 passed, 2 failed, exit 101. Both failures were fixture tag creation inheriting signing and reporting `fatal: no tag message?`, before the affected assertions. Explicit `--no-sign` creates the intended lightweight fixture tags. Both subsequent Verify runs passed 109 tests. No production criterion changed.

[deviation] Task 3's first Verify predicted 116 passed, 0 failed, exit 0. It observed 115 passed, 1 failed, exit 101. The one re-arm test showed a recorded round-two result on an unchanged current index being recomputed from HEAD as an ordinary round-one clean observation. The service now reuses the persisted fire's base and round when its index is still current. The exact failing test then passed 1 of 1, and both corrected full Verify runs passed 116 of 116. No criterion changed.

Task 3's first clippy run exited 101 on one obfuscated `then(...).unwrap_or_default()` conditional. It was rewritten as an explicit `if` and clippy then exited 0 before commit. TypeScript exited 0 on every Task 3 static-analysis run.

[deviation] Task 4's first Verify predicted 120 passed, 0 failed, exit 0. It observed 119 passed, 1 failed, exit 101. The exact-WIP fixture used `git mv`, so the rename was already represented in the index; attempting to pass its vanished source path to a second `git add` failed. Staging now passes only authorized paths with remaining worktree material and preserves already-staged rename entries unchanged. An initial focused command used an incomplete `--exact` test name and selected 0 tests; it made no verification claim. The corrected module-qualified focused run passed 1 of 1, and the corrected full Verify held at 120 passed, 0 failed, exit 0. No criterion changed.

Task 5 had no Verify deviation. Its three focused predictions and its 123-test full prediction all held. A compile-only check exposed an overlapping uncommitted versioned `ResumeInvocation` shape in the declared Task 5 file; the final implementation preserved its `version` and `preserved_head` fields and consolidated the branch/config retry bindings into that single shape. The rotated untracked checkpoint copy was later absent from the workspace, but this active report continues to retain its task rows and full checkpoint history.

Task 7 had no Verify deviation. Its exact process-loss test first passed 1 of 1 with 126 filtered out. The full binary run passed 127 tests. After strengthening committed-participant byte checks, the full binary run again passed 127 tests. Clippy and TypeScript both exited 0 before commit.

[deviation] Task 8 Verify predicted 230 total package tests and observed 237 passed, 0 failed, exit 0. The estimate omitted the existing 7-test `next_action` integration target. The predicted 49-test library target, 128-test binary target, seven mapped acceptance obligations and successful package outcome all held. No code or criterion changed in response.

## Checkpoint history and ruling

[deviation] The prior dispatch stopped before Task 3 because the final guarded record commit appeared to create a nonterminating review loop: recording a review result changes `decisions.jsonl` and `state.json`, so staging the acknowledged store generation produces a different tree from the one just reviewed. The checkpoint correctly refused to broaden the four frozen review-artifact filename exclusions, move store data outside the Git participant set, or weaken the gate.

The checkpoint cited `crates/cadence/src/import/mod.rs:464` and `:486-496` for the shared history/snapshot transaction, `crates/cadence/src/evidence/persistence.rs:31`, `:40` and `:57` for projection and history serialization, `crates/cadence/src/store/writer.rs:306`, `:308-316` and `:328-333` for the three store participants, and `cadence-core/references/risk-surface.md:37-42` for the then-existing four review-artifact exclusions. No Task 3 code or Verify claim was made in that dispatch.

D-14, committed at `e5c213ea`, resolved the checkpoint. The commit-side gate reviews authored material and never reads the binary's own receipts. The three current store participants are outside the read by that semantic rule, so a later store participant is covered without another filename ruling. The accepted cost is that destructive commands quoted inside an operator answer can reach a receipt ungated, matching the already accepted reviewer-text precedent. Task 3's real staged-receipt test now terminates with a completed clean observation while an authored nested review-lookalike still fires; D-14 resolves the loop as ruled.

## Final evidence

| Exact command | Pass counts and exit codes |
|---|---|
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --lib` | Task 1: two runs, each 49 passed, 0 failed, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence` | Task 2: initial 107 passed, 2 failed, exit 101; two corrected runs each 109 passed, 0 failed, exit 0. Task 3: initial 115 passed, 1 failed, exit 101; two corrected full runs each 116 passed, 0 failed, exit 0. Task 4: initial 119 passed, 1 failed, exit 101; corrected full run 120 passed, 0 failed, exit 0. Tasks 5 and 6: 123 and 125 passed respectively, 0 failed, exit 0. Task 7: two full runs, each 127 passed, 0 failed, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence server::pause_service_tests::pause_blocking_override_is_occurrence_scoped_and_rearm_is_capped_across_restart -- --exact` | Task 3 corrected exact run: 1 passed, 0 failed, 115 filtered out, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence server::pause_service_tests::pause_wip_preserves_exact_bytes_deletion_and_rename -- --exact` | Task 4 corrected exact run: 1 passed, 0 failed, 119 filtered out, exit 0. The preceding incomplete exact filter selected 0 tests and is not counted as verification. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence server::pause_service_tests::pause_commits_exact_resume_record_for_dirty_and_clean_starts -- --exact` | Task 5 focused run: 1 passed, 0 failed, 122 filtered out, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence server::pause_service_tests::pause_record_failure_after_wip_is_partial_and_retry_is_idempotent -- --exact` | Task 5 focused run: 1 passed, 0 failed, 122 filtered out, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence server::pause_service_tests::pause_record_risk_identity_ignores_changed_binary_receipt_bytes -- --exact` | Task 5 D-14 regression: 1 passed, 0 failed, 122 filtered out, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence server::pause_service_tests::pause_survives_process_loss_at_each_real_commit_boundary -- --exact` | Task 7 focused run: 1 passed, 0 failed, 126 filtered out, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence` | Task 8 Verify: 49 library, 128 binary, 6 derivation-consistency, 12 derivation-input, 4 evidence-store, 4 MCP, 7 next-action, 17 store and 10 store-crash tests passed; 237 total passed, 0 failed, exit 0. Doc-tests selected 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo clippy --all-targets -- -D warnings` | Task 3 initial exit 101, corrected runs exit 0; final pre-commit run had no warnings. Tasks 4-8 pre-commit runs exited 0 with no warnings. Tasks 1-2 had clean pre-commit runs as recorded in the prior report. |
| `npx tsc -p tsconfig.ci.json` | Tasks 4-7 pre-commit runs exited 0. |
| `TMPDIR=/tmp npx tsc -p tsconfig.ci.json` | Task 8 Verify: exit 0. |
| `TMPDIR=/tmp node --test </dev/null` | Task 8 Verify: 3,753 passed, 0 failed, 0 skipped, exit 0. |
| `git diff --exit-code v3.7.12 -- cadence-core/` | Task 8 Verify: empty diff, exit 0. |
| `TMPDIR=/tmp npx tsc -p tsconfig.ci.json </dev/null` | Task 3 runs exited 0. Tasks 1-2 had clean pre-commit runs as recorded in the prior report. |
| `node cadence-core/bin/config.mjs get workflow.test_command </dev/null` | Final suite resolution: `workflow.test_command` was null from `global+repo`, exit 0. The root Cargo workspace manifest names `crates/cadence` as its member. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test --workspace` | Final suite: 49 library, 128 binary, 6 derivation-consistency, 12 derivation-input, 4 evidence-store, 4 MCP, 7 next-action, 17 store and 10 store-crash tests passed; 237 total passed, 0 failed, exit 0. Doc-tests selected 0. Prediction held. |
| `git show --no-patch --format='%H %G? %GK %an <%ae>%n%s' c47d1fb97edfbae20ea5a46a7960f7d30cf31886 e59a725c6b6d525869007fa89d7a96ef2301699d 01828c91100e45028747fed39e7c3ec58fea2f9a 8c292297c744fae3d01b122c1c9dc323951fb062 c7ed9ad73bf6171d0fedff68c51e9bdd8ba618de 288ec20c0247a58c0326e9ce9c48ef3f719cc21e 69381b131014729cbd4165a4ea90cf72262da163 eefa03a3f1d698219c44fe4d82e8a19eab95aa75` | All eight task commits reported `G`, key `693AB15F91734B0C` and author `John Crenshaw <john@jcrenshaw.dev>`; exit 0. |
| `git diff --exit-code e5c213ead3d9ad689d43b06a57d852b985761a06 -- cadence-core/ .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/5/CONTEXT.md` | Empty diff, exit 0. Frozen `cadence-core/` and every protected planning file are unchanged from the resumed starting HEAD. |
| `git diff --check e5c213ead3d9ad689d43b06a57d852b985761a06..HEAD` | Exit 0. |
| `git status --short` | Exit 0. Only this exempt report and the unrelated untracked `docs/architecture/overview.md` were listed; no source change or generated artifact remains. |

Task 3's compile-only `TMPDIR=/tmp RUSTC_WRAPPER= cargo check -p cadence` initially exited 101 on an internal visibility error, then exited 0 after using a local receipt-path validation. `cargo test -p cadence --bin cadence --no-run` exited 0 before the task Verify. `node cadence-core/bin/planning.mjs detect-commands --root /code/cadence </dev/null` exited 0 and selected the clippy and TypeScript commands. The Task 3 lease check exited 0 with `ok:true` and six staged declared files. `git diff --check` and the staged diff check exited 0.

Every Task 1-8 commit verifies `G` with key `693AB15F91734B0C` and author `John Crenshaw <john@jcrenshaw.dev>`. All post-commit deletion checks were empty. The final workspace suite was green, and the frozen and protected paths were byte-unchanged from the resumed starting HEAD.


## Interrupted resume checkpoint, preserved as history

Current task: 5 - Commit the durable resume record before reporting success.

A concurrent executor process became active during this dispatch and wrote overlapping uncommitted Task 5 changes in all five declared Task 5 files. The collision was observed while those files were still changing. No Task 5 verification command, static-analysis claim, lease check or commit is recorded for this dispatch. The existing Task 1-4 commits remain the last completed task boundary.

Need: stop the concurrent executor process, decide whether its uncommitted Task 5 changes are to be discarded as previously directed, and restore a single-owner worktree at committed HEAD before resuming Task 5.

The untracked operator file `docs/architecture/overview.md` remains untouched. No stash, reset, checkout, package install, Git configuration change, hook override or push was performed.

## Earlier checkpoint report, preserved verbatim

PLAN CHECKPOINT: structural
Plan: /code/cadence/.planning/phases/5/PLAN-4.md
Tasks: 2 of 8

| Task | Commit | Note |
|---|---|---|
| 1 - Capture actual pause work | c47d1fb97edfbae20ea5a46a7960f7d30cf31886 | Added the pause domain and read-only Git adapter. Captures exact one-line text, scoped phase and retained name/total provenance, HEAD, branch, index, dirty material and explicitly authorized paths. Real temporary repositories cover clean, staged, unstaged, untracked, deleted, renamed, Unicode, non-UTF-8 and shell-metacharacter paths. Missing phase/note and multiline text refuse. A retired interrupted-close cursor keeps its name, total and original provenance with null lifecycle current. Capture stages and commits nothing. Verify prediction: 49 library tests passed, 0 failed, exit 0. Held on both runs. |
| 2 - Durable branch gates | e59a725c6b6d525869007fa89d7a96ef2301699d | Wired internal server/resident/factory pause requests. Persists protected, detached, missing/unknown-base, unrelated-history and integration questions before dependent branch changes. Tests cover occurrence-scoped answers, fresh-child unanswered readback, local branches versus tags, shared-history base advancement, active-version priority, title fallback, missing/published versions, trunk, off, off-base and unavailable effective config. Verify prediction: 109 binary tests passed, 0 failed, exit 0. Initial run: 107 passed and 2 fixture failures. Both corrected runs held the prediction. |

Deviations: two, detailed below: one verification surprise and one structural checkpoint. No acceptance criterion or locked decision was redefined.

Open items: two. The guarded final-record protocol needs the decision below before Tasks 3-8 can proceed. Pre-existing untracked PLAN-2 and PLAN-3 reports and docs/diagrams remain untouched.

## Run details

Starting HEAD: `cd20b7933804ba1936893c74deaf222b3d59cfc3`, branch `cadence/binary-owns-process`. Sequential PLAN-4. Consumed the completed prerequisite reports without redoing their work. No prior PLAN-4 report existed to rotate. Rewrote this report after each task commit with partial status, then checkpointed before Task 3 implementation. It remains uncommitted for the phase documentation commit.

Every task passed its lease gate and static checks before commit. No packages installed, persistent Git configuration changed, hooks disabled or push performed. Temporary repositories used `/tmp`, all Cargo invocations cleared `RUSTC_WRAPPER`, and unused Node and Git subprocess stdin was ignored. Fixture identities were invocation-local; lightweight fixture tags explicitly disable signing. The two task commits use the required signature and author.

Task 1 uses OS pathname arguments, literal pathspecs and disabled optional index refresh. A formatting probe found binary fixture bytes expressed as a text string; corrected to a byte string before Verify. A final fallback check rejects malformed retained cursor input before using its phase evidence; the library Verify was rerun successfully.

Task 2 currently returns Ready only for branch authorization. It does not claim WIP preservation, a committed resume record or pause completion. Protected and base questions are occurrence-scoped, with identities tied to the observed policy and Git/prose inputs. Rechecks precede branch mutation. Existing evidence and lifecycle implementations remain unchanged.

## Verification correction

[deviation] Task 2's first Verify predicted 109 passed, 0 failed, exit 0; observed 107 passed, 2 failed, exit 101. Both failures were fixture tag creation inheriting signing and reporting `fatal: no tag message?`, before the affected assertions. Explicit `--no-sign` creates the intended lightweight fixture tags. Both subsequent Verify runs passed 109 tests. No production criterion changed.

Task 2 clippy exited 101 twice for large enum variants, first in the integration decision and then in the service response and internal choice. Boxed their question payloads; clippy then exited 0 before commit. Both Task 2 typechecks exited 0.

## Structural checkpoint

Current task: 3 - Preserve the pause commit's risk gate.

[deviation] The combined acceptance obligation is UNVERIFIABLE against the current producer and the required frozen exclusions for a final record commit that itself matches a blocking surface. No Task 3 code or tests were written after identifying this condition.

The criterion is Task 3's actual-staged-material decision, recorded contracted result and retained index identity (`.planning/phases/5/PLAN-4.md:63`), including its requirement that an actual recorded no-survivor result clear the applicable blocking gate (`:64`), combined with Task 5's same applicable commit guards, all required store writes before the final commit, coherent committed participants and clean completion (`:75`, `:76`). This is a protocol gap for the required successful matching case, not evidence that a nonmatching or explicitly off case cannot finish.

What the actual code does:

- `crates/cadence/src/import/mod.rs:464` builds a durable decision from the submitted record. Lines 486-496 project that same record into snapshot data and submit both through CompareTransact. A newly acknowledged review result changes both participants.
- `crates/cadence/src/evidence/persistence.rs:31` inserts the complete record into the projection; line 40 serializes it into native_evidence. Line 57 also serializes the complete record into decision history. Staged-scope identity carried by the submitted record therefore becomes bytes in both files.
- `crates/cadence/src/store/writer.rs:306` renders decisions, lines 308-316 advance and render the snapshot, and lines 328-333 make items.jsonl, decisions.jsonl and state.json transaction participants. `crates/cadence/src/store/filesystem.rs:107` resolves ordinary participants beneath the store root. The established producer has no separate review-evidence destination outside those committed participants.
- `cadence-core/references/risk-surface.md:30` requires staged scope, null head_id and the index_id of the tree whose bytes were scanned. Lines 37-39 exclude only ADJUDICATION-*.json, REVIEW-*.md, FINDINGS.json and verifier-findings.json under .planning/phases/. Neither store participant is excluded.
- A permitted exact note such as `verify the DROP TABLE fix on the device` contains the actual destructive signal at `cadence-core/bin/lib/risk-diff.mjs:150`. Preserving that text in the native store can make the final record commit require a blocking review even when the source WIP was already cleared.

Consequence: let T be the staged final-record tree delivered for review. Recording the contracted result for T through the required producer changes decisions.jsonl and state.json. Staging the acknowledged generation for clean completion gives a different tree T2. Treating the result for T as authority over T2 needs a defined rule for the result's own writes. Reviewing T2 and recording that result repeats the mutation. Leaving those writes unstaged fails clean completion and the committed-participant check. Writing them after the final commit is explicitly forbidden. Substituting a tree id for a head commit or silently broadening exclusions would redefine the stated guarantees.

This conclusion comes from the opened code and contracts. It was not presented as a failed executable Task 3 test, a real review judgment or proof that every pause case fails. Task 3's Verify command was not run and no success is claimed for it.

Need: decide and specify how durable review evidence participates in the final guarded record commit without invalidating the material it authorizes. Then update the plan's acceptance protocol and, if necessary, its file lease.

Proposed direction: explicitly distinguish reviewed material from the binary-owned receipt envelope, preserving review of the exact operator sentence and any other authored content, and verify that the only post-review additions are the contracted receipt writes. This requires an explicit acceptance rule; the current narrow exclusions do not authorize silently dropping whole store files from review.

Alternative: give durable review evidence a separate acknowledged storage destination outside the Git participant set. That changes the established producer/store protocol and needs a wider lease. Keeping the matching final-record case refused is safe partial behavior, but does not satisfy the required success guarantee. Disabling the risk gate is not a repair.

Impact: the two committed tasks remain usable prerequisites. Tasks 3-8, AC6 completion and final phase acceptance remain outstanding. No WIP/resume implementation or general range-identity repair was substituted for the missing decision.

## Final evidence

Stopped at the structural checkpoint, so no full suite was run and no PLAN COMPLETE claim is made. Exact executed verification commands and results:

| Exact command | Pass counts and exit codes |
|---|---|
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --lib` | Task 1: two runs, each 49 passed, 0 failed, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo test -p cadence --bin cadence` | Task 2: initial 107 passed, 2 failed, exit 101; two corrected runs each 109 passed, 0 failed, exit 0. |
| `TMPDIR=/tmp RUSTC_WRAPPER= cargo clippy --all-targets -- -D warnings` | Task 1: exit 0. Task 2: exit 101, 101, then 0. Both commits were preceded by a clean run at -D warnings. Test counts not applicable. |
| `TMPDIR=/tmp npx --no-install tsc -p tsconfig.ci.json </dev/null` | Task 1: exit 0. Task 2: two runs, each exit 0. Both commits were preceded by a passing typecheck. Test counts not applicable. |

`node cadence-core/bin/planning.mjs detect-commands --root /code/cadence </dev/null` exited 0 and detected the required clippy and TypeScript commands. `node cadence-core/bin/planning.mjs lease-check --phase 5 --plan 4 </dev/null` exited 0 with ok:true before both commits, respectively four and six staged paths. Rustfmt's initial Task 1 byte-string parse error was corrected; later formatting commands exited 0.

`git log cd20b7933804ba1936893c74deaf222b3d59cfc3..HEAD --reverse --format='%H %G? %GK %an <%ae>%n%s%n%b'` exited 0. Both commits, in task order, verify **G** with key **693AB15F91734B0C**, author **John Crenshaw <john@jcrenshaw.dev>**. Their exact SHAs are in the task table. Both per-commit deletion checks were empty.

`git diff --exit-code cd20b7933804ba1936893c74deaf222b3d59cfc3 -- cadence-core/ .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` exited 0 after Task 2 and again at checkpoint. Frozen cadence-core and all three protected planning files are unchanged.

`git diff --exit-code v3.7.12 -- cadence-core/` exited 0. `git diff --check cd20b7933804ba1936893c74deaf222b3d59cfc3..HEAD` exited 0. There are no uncommitted source changes; only this report and the pre-existing untracked reports/diagram directory remain outside the two task commits.
