PLAN COMPLETE
Plan: .planning/phases/5/PLAN-3.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1. Record the Run 2 section in the ledger header | d4b712d | `## Run 2` inserted after `## Run 1` (lines 10 and 54); five invocations, the first three byte-identical to `:28-30`; counts 562/9/15 = 586 for the re-run arm, 82/1/2 = 85 inv 4, 58/2/2 = 62 inv 5, 8/0/2 = 10 targeted `.mjs`; surface 32 files / 322,989 B; re-pin scope 23/31 docs, 493/548 rows |
| 2. Add the generated `run` column to both claim tables | f81c1d6 | 7 cells on every row and both headers; values generated by a scratchpad join on `doc` + claim TEXT, never typed; `## Reading this ledger` gains what the column means, why a run number, and why a `1` is a signal |
| 3. Re-verdict and rewrite the seven `trace close` rows | 3ff1d60 | All seven `stale` + `corrected - 4110fde - claim rewritten to the live trace close call`; claim text and line read off the live files; `grep -c -- "--event return\|--event checkpoint"` returns 0; callout paragraph added beside README-44's |
| 4. Re-pin every row whose doc moved since `a6b8931` | a829f39 | 23 moved docs / 493 rows in scope; 261 `line` cells changed and 0 rows changed any other cell; 206 from the run-2 report locations, 55 from a measured window read (11 hand-read overrides), 94 citations confirmed still holding, 8 orphans left where run 1 put them |
| 5. Land run 2's verdicts on the rows it joined | d1ddfcf | 10 verdict flips on run-2 rows (README-25, README-42, METHOD-11, METHOD-59, INTERNALS-13, INTERNALS-34, CONTEXT-09, CONTEXT-10, PLAN-27, VERIFY-44); 8 orphans set `stale` + `divergence`; the 55-row changed-wording list closed with zero verdict changes (26 have a run-2 claim at the re-pinned line and it agrees; 29 have none); the seven untouched and still `stale`; 0 `pending` |
| 6. File run 2's new claims, `/cad-capture --cadence` among them | 417eec2 | 385 rows appended to `## Claims added after run 1`, ids continuing each doc's ordinal; ADOPT 29, MINIMALISM-REVIEW 16, REPORT 23, SUGGEST 17; completeness gate passes - all 743 report claims accounted for (385 transcribed, 358 joined, 0 unaccounted); both `--cadence` surfaces verified unedited (`README.md:124`, `COMMANDS.md:48`) |
| 7. Close the ledger out consistent with itself | 1a6642e | 933 rows, all 9 unescaped fields, no empty cell, no `pending`, run values only `1`/`2`, verdicts only `accurate`/`stale`/`unverifiable`, resolutions only `accurate` (847) / `corrected` (28) / `divergence` (57) / `RETIRED` (1); prose reconciled to the tables - the `line` column lead, the run-column counts, the resolution vocabulary and the Run 2 totals |
Deviations: [deviation] Tasks 2 and 7 verify with `awk -F'|' '/^\| [A-Z]/ {print NF}' ... | sort -u` expecting a single value, and task 7 also expects `grep -cE "\|\s*(accurate|stale|unverifiable)\s*\|"` to equal the data-row count. Neither holds on this file and neither ever did: 20 claim cells carry `\|`-escaped pipes, so the awk reported five distinct field counts (8..13) before this plan touched anything, and `:185` states the verdict vocabulary in prose, so the grep is +1 over the rows by construction. Verified instead by splitting on unescaped pipes (`/(?<!\\)\|/`) and counting cells: 933 rows, 9 fields each, verdict cells all in vocabulary.
[deviation] Task 4 asserts the two classes needing a human read "are small". Measured, the `doc` + claim-TEXT join matches 358 of the 548 rows: run 2 was a FRESH extraction under `docs-verify.md` steps 2-4 and decomposed the same prose differently, so 190 rows carry no run-2 claim with their text (`METHOD.md` 41 of 82, `README.md` 20 of 51) and 385 run-2 claims joined to nothing. Handled inside the task instead of by hand-reading 190 rows: an IDF containment measure decides per row whether the cited window still states the claim, validated first against the 256 strongly-joined rows whose live location the report gives (235 exact, 6 within six lines, 15 misses, every miss an "X exists" pointer claim). That produced 55 re-pins with 11 hand-read overrides, 8 hand-read orphans, and 94 citations confirmed. The knock-on is task 6: 385 new rows rather than the ~85 invocation 4 alone would have filed, some of which restate a run-1 row in different words - stated in the section preamble rather than hidden.
[deviation] The plan has task 5 import run 2's verdict onto every `run` `2` row. A row already resolved `corrected - <sha>` carries its PRE-correction text by construction, so run 2's claim at that site is a different sentence and importing its verdict would overwrite a recorded correction with a verdict about words the row does not state. Those 20 rows are held out of the join, keep run 1's verdict and read `1`, and the rule plus its one exception (README-44, whose text the ledger states was itself rewritten to the corrected sentence) is written into `## Reading this ledger`.
Open items: The join is fuzzy by necessity, so the 385 filed rows include an unknown number of near-duplicates of run-1 rows whose wording drifted. Deduplicating them needs a claim-level identity the ledger does not have and would mean re-reading both sides by hand; the preamble names the cost instead. A third cycle could cut it by having `/cad-docs-verify` emit the ledger id it re-verified when it recognises one.
