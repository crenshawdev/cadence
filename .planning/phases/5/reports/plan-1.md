PLAN COMPLETE
Plan: .planning/phases/5/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1: Put `docs/` on self-verify's markdown walk | 11c2040 | `join(root, 'docs')` added to the `mdFiles` `dirs` array; both enumerating comments updated; new test asserts the reported `file` is the `docs/` path, watched failing with the line reverted and passing with it back. self-verify `ok:true` / empty `problems`; 2380 tests pass; `npx tsc -p tsconfig.ci.json` clean. |
| 2: Relocate the cost-to-run section to `docs/COST.md` | c081da1 | 3,839 B page, H1 + bold subtitle, wrapped at 80 (0 lines over 80, 0 over 97). All Verify greps as predicted: cost heading 0 in README, `5,397` 0 across README and all `docs/*.md`, `8,550` 0, `weight.mjs resident` 1, `docs/COST.md` link 1 in README. self-verify `ok:true` / empty `problems`. |
| 3: Relocate the worked example to `docs/EXAMPLE.md` | 7b1cd4f | 2,868 B page, H1 + bold subtitle, prose wrapped at 80 with the four-command fence left as a fence (2 lines over 80, both inside the fence; 0 over 97). Accuracy pass found no correction: `git.issue_check`'s schema purpose, `cad-land` step 1 ("landing closes no issue") and its no-preselected-default publish step all still match the sentence, as do `/cad-suggest`'s description and `cad-verify`'s UAT.md. self-verify `ok:true`. |
| 4: Cut `## The commands`, fold three arguments into How it works | bd7ed83 | Section gone; `^- **`/cad-` bullets now 0 in README and all five `docs/*.md` (21 in README at HEAD). Exactly one line names both `/cad-help` and `cadence-core/references/COMMANDS.md`. The three folded arguments land in three separate paragraphs (3 matching lines) as prose, never a bullet list. self-verify `ok:true`; 2380 tests pass; tsc clean. |
| 5: Move the demand section above Install, hold the plain register | b3caf88 | `## What it asks of you` at line 7, `## Install` at 15. Banned-word grep over the stretch returns 0; the verbatim wrong-tool sentence returns 1; the count regex returns 0 and a read confirms no count of stops in any other spelling; `sed -n '3p'` byte-identical to HEAD's badge line. self-verify `ok:true`. |
| 6: Order the staying sections and compress How it works | f9d9832 | Headings read: demand, Install, The loop, How it works, What a break costs, Where it came from - no `## The commands`, `## A worked example` or `## What it costs to run`. `## How it works` 4,465 B (under 4,500, against 5,914 at HEAD and 6,964 after task 4's fold); `README.md` 14,264 B (under 15,000, against 24,850). CI-drift sentence still 1. self-verify `ok:true`; 2380 tests pass including prose-agreement's 27-skills match; tsc clean. |
| 7: Re-measure `LINEAGE.md`'s Agents and Skills counts | 3209243 | Tree reads 19 `agents/*.md`, 33 `skills/*/`, 27 without `user-invocable: false`. Rows now `Agents / 34 / 19 rung files (6 roles) / 56%` and `Skills / 71 / 33 (27 user-invocable, 6 contract) / 46%`; `\| 34 \| 7 \|` and `\| 71 \| 22 \|` both 0. Table diff shows those two rows and no other. Provenance amended to name the two cells carrying 2026-08-19. 2380 tests pass; tsc clean. |
| 8: Re-point, retire and re-pin every `README-*` ledger row | 6d3fcf3 | 86 rows: 8 to `docs/EXAMPLE.md`, 8 to `docs/COST.md`, 9 RETIRED with `—` in `line`, 4 left at provenance, the rest re-pinned to the post-change `README.md`. `grep -c RETIRED` = 11 (10 rows + the Resolution-values prose line). Throwaway backticked-literal pin checker: 0 misses live across all three files, 44 against `276fbb7`; 27 no-literal rows confirmed by reading their pinned span. Resolution-values counts corrected to accurate 837 / divergence 44 / RETIRED 10. Task 8 also restored three phrases to `README.md` (see deviations). self-verify `ok:true`; 2380 tests pass; tsc clean. |
Deviations:
[deviation] Task 2's Verify says `head -3 docs/COST.md` should show "the same shape `head -3 docs/EVIDENCE.md` shows" - `docs/EVIDENCE.md:3` is a plain paragraph, not a bold subtitle, so both halves cannot hold. Built to D-04's stated "bold one-line subtitle", which `docs/DISCOVERY.md:3` and `docs/WORKFLOW.md:3` both carry; EVIDENCE.md is the odd sibling. The Action also calls the section "three paragraphs"; it is five (HEAD lines 136, 138, 140, 142, 144), and all five moved.
[deviation] Task 2's Action says to cut the whole closing clause "And the skill and agent descriptions ... went from 8,550 bytes to 5,397". Cutting the clause outright would have left the paragraph's own "Three things decide what a turn carries, and all three moved" naming only two. D-10 scopes the cut to the FIGURE, so the clause survives without a number - "were cut to one routing line each" - true of the tree today (52 of 52 skill and agent `description:` values are single-line, measured 2026-08-19) and satisfying the Verify (`5,397` and `8,550` both absent).
[deviation] Task 8's Action lists seven rows to retire; nine retire. Task 6's authorized compression cut two more sentences a ledger row cites: README-53 (the DeepSeek-adapter sentence) and README-55 (the routing parenthetical, "four knobs out - model, effort rung, review gates, deep verify"). Neither is on the plan's list and neither is in task 6's four load-bearing claims, so the cut is authorized and the rows retire under D-07's own stated rule. Recorded in the ledger's new narrative paragraph rather than left for the next sweep to report as vanished claims. The `## Must be true when done` line still holds: every `README-*` row names a `docs/` file, carries `RETIRED - <reason>`, or cites a line that states its claim.
[deviation] Task 8's Action retires README-50 as a departure from D-07's literal list (flagged by the plan itself, and done). Separately, README-49 is a `corrected - <sha>` row whose line the ledger's own rule calls provenance rather than an address, yet the plan lists it among the eight re-pointing rows; it re-points, because its section left `README.md` entirely and a `doc` cell naming a file that carries nothing like the sentence is worse than losing the run-1 line. Its run-1 provenance (`README.md:140`) is recorded in the narrative instead. README-01, README-02, README-25 and README-28 stay at provenance.
[deviation] Task 8's Verify predicts 47 misses when the pin checker runs against HEAD; observed 44 at `276fbb7`. The post-change figure the criterion turns on is met exactly (0 misses across `README.md`, `docs/COST.md` and `docs/EXAMPLE.md`); the baseline differs because this script skips the four provenance rows and only checks rows carrying a backticked literal.
[deviation] Task 6's compression dropped three details ledger rows cite while leaving their sentences standing: README-05's "four-line" state cursor, README-11 and README-52's attribution of the 2,251 deleted lines to v2.2.0, and README-16's "multi-select prompt". Retiring those rows would have recorded a cut that never happened, so task 8 restored the three phrases to `README.md` and bought the bytes back elsewhere in the same section. `## How it works` is 4,488 B, still under task 6's 4,500 criterion; `README.md` is 14,287 B, still under 15,000.
Open items:
`workflow.lint_command` is unset and `detect-commands` reports `lint: null` for this repo - typecheck (`npx tsc -p tsconfig.ci.json`) is the only static-analysis command Cadence can find, and it is what ran on every task.
README-76 retires as the plan directs, but half its claim survives: `README.md:46` states "`/cad-help` prints the full reference inside a session" in new words, while "`/cad-help <name>` shows one entry" is gone with `## The commands`. The next sweep extracts the surviving half as its own row; noted in the ledger narrative rather than split into two rows here.
README-23's claim reads "`model.escalate_on_failure`, on by default" with an `accurate` verdict while README-64 records "off by default" as accurate and the live default is off. Pre-existing ledger contradiction, untouched here because task 8 changes no `verdict` cell; the row was re-pinned to `README.md:78` like its neighbours.
Declined a self-verify check that `docs/` pages carry the sibling H1-plus-bold-subtitle shape. Task 1's Verify asks only that the directory join the existing walk, and D-04 is a convention rather than an invariant with a stated failure mode; add the check when a task states one.
