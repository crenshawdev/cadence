PLAN COMPLETE
Plan: .planning/phases/5/PLAN.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Commit the fixture corpus, its pre-fold query baseline and the harness | 537caa66 | 30 documents over 4 sources (`phases/1/SUMMARY.md`, `phases/2/CONTEXT.md`, `phases/3/SUMMARY.md`, `CAPTURE.md`), 3 corpus tiers; 16 queries, every `hits` list generated by running the seam against the built root. `test.mjs --list` shows `planning-recall-fold` in `planning`, not `other`. `test.mjs planning` 1054 pass / 0 fail with no fold in `tokenize` yet. Falsifier: retargeting entry 1's hits to `zq01` failed with `query "roadmap dependency goal" lost zq01 from its top 5; got zq19`; reverted. |
| 2 - Fold suffixes inside tokenize(), and move the fixture entries the fold changes | 1d6a10ac | Porter 1a + 1b as the last step of `tokenize`, after the stopword filter, module-local and called only by `tokenize`. Full suite 3492 pass / 0 fail - no existing arm moved, matching the plan's measured baseline. `self-verify` ok:true. 2 of 16 fixture entries carry `changed_by_fold` (`phase record trace`, `note record change type`); 14 untouched; `git diff --numstat` on the file is 2 insertions / 2 deletions. |
| 3 - Pin the rule table at tokenizer level, including the ordering that was refuted | 56e5ec00 | 5 table-driven arms, 34 rows, every form run through the real `tokenize` before it was written down. File 7 pass / 0 fail, showing `closes` -> `close` (not `clos`), `classes` -> `class`, `pass` -> `pass`, `notes` -> `note`, `seams` -> `seam`, `named`/`naming` -> `name`, `refused` -> `refuse`, `planned` -> `plan`, `running` -> `run`, `gated` -> `gate`, `agreed` -> `agree`, `freed` -> `freed`. |
| 4 - Prove each inflected pair returns the same documents over the fixture corpus | 2ae99d64 | 8 groups; per group, deep-equal `results` and equal `total` across members, a non-empty guard first, and the other member's exclusive document located by its fixture id in the snippet. File 8 pass / 0 fail. Falsifier: with `foldSuffix` swapped for identity in `tokenize`, 6 arms in the file go red, this one included. |
| 5 - Prove the stopword filter still tests the raw token | b283af79 | `being` -> `be` and `its` -> `it` both land on `STOPWORDS` members and both survive in the index; a `notes` query and a `being` query each return total >= 1 over the fixture corpus; a raw-stopword-only document still tokenizes to nothing. File 12 pass / 0 fail. Falsifier: with the filter moved after the fold, `being` tokenizes to `[]` and the seam query returns nothing. Full suite 3502 pass / 0 fail, `self-verify` ok:true, `npx tsc -p tsconfig.ci.json` exit 0. |
| - post-task hygiene | 5d96ff44 | Dropped the `export` on `corpus()`, which had no importer; `fixtureRoot` stays exported because the regeneration tool must build the same tree the arms rank over. File 12 pass / 0 fail, tsc exit 0. |
Deviations: [deviation] CONTEXT D-03 asserts the fold sends `noted` to `not`, a STOPWORD, and PLAN task 5 asks for an arm of "the same shape for `noted`". Under the rule set D-02 and PLAN task 2 lock - step 1b's cleanup INCLUDING the consonant-vowel-consonant `e` restoration - `noted` folds to `note`, which is not a stopword, so `noted` cannot demonstrate the fold landing a term on a stopword. D-03's parenthetical reads from a two-branch cleanup (restore after at/bl/iz, undouble) that D-02's own worked example rules out: without the cvc branch `named` -> `nam` while `name` -> `name`, which fails AC2. D-03's ORDERING claim is untouched and still true - `being` -> `be` is a stopword landing. What I did: pinned `its` -> `it` as the second stopword-landing arm in `noted`'s place, and pinned `noted` -> `note` as its own arm with the correction stated in a comment, so the record says why D-03's example moved. Task 5's `Verify:` names only `being`, `notes` and the stopword-only negative, and all three hold as written.
Open items:
- GH-93 item 2, the multi-query union, is not delivered (D-06). It changes the `recall` seam signature, which `cadence-core/references/recall.md` pins for three callers - `/cad-context` at `spend_gate`, `/cad-debug` at Hypothesize and `/cad-plan` at `spawn_planner`. AC5 requires this on the phase SUMMARY's `## Open items`.
- GH-93 item 3, the failure records outside the corpus, is not delivered (D-06). It needs a scope decision this cycle did not take, and three measured obstacles sit under its phrasing. First, `parseSummarySnippets` returns 0 on every live `reports/plan-<k>.md`, so it cannot be reused for that tier. Second, `REVIEW-*.md` files are JSON with a markdown extension holding RAW findings, while the adjudicated rulings GH-93 asks for live in `ADJUDICATION-*.json` beside them. Third, any new tier inside `phases/<N>/` needs `milestone-prune`'s residue walk extended, or it goes unreachable at the next milestone close - the exact defect RCL-07 closed. AC5 requires all three obstacles stated on the phase SUMMARY.
- The fold covers Porter steps 1a and 1b only, so forms outside them stay distinct terms: `verifies` reaches `verifi` while `verify` is untouched, `released` reaches `releas`, and `indices` never reaches `index`. Nothing in this phase claims otherwise, and CONTEXT's last flagged assumption already states it; recorded here so a later "the fold does not work for X" report has the boundary written down.
