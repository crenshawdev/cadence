PLAN COMPLETE
Plan: .planning/phases/5/PLAN-1.md
Tasks: 5 of 5
| Task | Commit | Note |
|---|---|---|
| 1 - Seal the generation on the rotation marker | 962dc8c9 | `carried_bytes` added after `file` in `freshRecord`'s marker, `Buffer.byteLength(text)`. New row asserts it equals `statSync(rotatedTracePath(dir)).size`, `rotated` still `{file, ts}`, marker still counted once in `counts.lifecycle`. 172/172 trace rows pass; `npx tsc -p tsconfig.ci.json` exit 0. |
| 2 - Finish the carry-back before the leftover generation is destroyed | 2032c0da | Rescue runs after the swap, scoped by a new `leftover` variable so the abandoned-claim eviction (where the evicted path is a second name for the LIVE inode) never triggers it. No seal or non-numeric seal rescues nothing. Failed stat/read/split/append states `shortfall` beside `rotated:true`; a failed stat states `shortfall:null`. 3 new rows, 175/175 pass. Reproduction proved: with `lib/trace.mjs` stashed, the carry-back row FAILs "the racing writer's event is in NEITHER file". `npx tsc -p tsconfig.ci.json` exit 0. |
| 3 - Reserve the rotation marker in the admission check | 98f72c06 | Anchor scan and marker construction lifted into `rotationMarker(text)`; `freshRecord` and `appendEvent`'s size arm are its two callers, so the reserve cannot drift from the written line. Reserve measured on the size arm only. Two new rows: a `MAX-8` line at a bound-full anchorless record is refused `oversized-event` with the record byte-identical and nothing rotated; a line one byte inside the measured threshold rotates and leaves the record at `MAX-1`. 177/177 pass. Reproduction proved: pre-fix the refusal row returns `{written:true, corr:'7'}`. `npx tsc -p tsconfig.ci.json` exit 0. |
| 4 - Confirm the claim on the leftover-generation eviction too | a3c17c5b | The `if (abandoned)` guard around the confirm-after-claiming removed, so both eviction arms re-stat the sidecar and restore the sibling where the mtime is not the one this call published (`mine === null` included). Comment stating the exclusion rewritten to state why the discriminator does not answer. New row makes the sidecar path a directory so `publish` cannot land: the generation is byte-identical, no `.evict.` file remains, and the writer's own event is in the live record. 178/178 pass. Reproduction proved: pre-fix the row FAILs "the generation was destroyed by an eviction that could not confirm its claim". `npx tsc -p tsconfig.ci.json` exit 0. |
| 5 - Prove the rotation still reaches both trace envelopes after a second cut | 75a57dde | Test-only row: two rotations through `appendEvent`, then `trace render --phase 1` and `trace suggest` through the `run` helper. Both name `rotatedTracePath(dir)` on the same `rotated.ts`, `rotated` is still `{file, ts}`, `capped` is false, `carried_bytes` appears in neither envelope, and with two markers in the record the executor pair is still the only bracket with `unpaired` empty. 179/179 trace rows pass. |
Deviations: none
Open items: `publish()` in `lib/trace.mjs` nulls `pending` even when its `renameSync` throws, so on a root whose sidecar path cannot be written the private stamp `trace.1.jsonl.claim.<pid>.<rand>` is left behind by the `finally`'s `if (pending)` release - visible in the task 4 row's fixture. Pre-existing, on an arm no task here owns, and it is inert residue rather than a lost event; a task that states the release contract should carry it.

Full suite: `node cadence-core/bin/test.mjs` 3630/3630 pass. `node cadence-core/bin/self-verify.mjs` `ok:true`, 0 problems. `npx tsc -p tsconfig.ci.json` exit 0. No `lint` command exists (`detect-commands` reports `lint:null`).
