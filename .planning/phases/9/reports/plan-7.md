PLAN COMPLETE
Plan: .planning/phases/9/PLAN-7.md
Tasks: 7 of 7

| Task | Commit | Note |
|---|---|---|
| P9-7-T1 | deef05dd | Completed-receipt diff admission, replay binding, confirmed acknowledgment and outer continuation guard. |
| P9-7-T2 | 0e0c203c | Atomic definite launch failure with truthful null identity, replay and FIRST advance. |
| P9-7-T3 | 2ff85d38 | Persisted delivery membership, append replay and retained-read authorization. |
| P9-7-T4 | cbc2ad24 | Authoritative risk conversion and ordinary admission short-circuit. |
| P9-7-T5 | 1924bfe1 | Minimalism voice and provenance bypass ordinary routing. |
| P9-7-T6 | 42fb7908 | Typed recursive membership with separate file acquisition and frozen reads. |
| P9-7-T7 | 6873ea6b | Bounded transport ingestion, exact outer decoding and persistent framing state. |

## Resolved checkpoint

The owner struck the dispatch's superseded internal-mocking prohibition and
confirmed the plan's Verify clauses without revision. The two thin public
composition cases in T1 and T4 are authorized. This report replaces the earlier
checkpoint in place, as instructed; that record remains in commit ec68500e.

## Task verification

T1: predicted 7 passed, 0 failed for
`TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --bin cadence gap151_`.
The first invocation failed compilation because the new test attempted to
serialize the transport response wrapper. Corrected that test to inspect its
completed-result variant. The same command then passed 7, failed 0, ignored 0,
filtered 213. No prior test changed. The cases supply decisions, commit results
and delivery responses directly; the single public composition case supplies
review and execution service responses and starts no client or subprocess.

T1 uses the accepted receipt and retained execution basis, with a replay key
covering scope, occurrence, dispatch, transition, diff trigger and round. Saved
bindings precede current routing and source acquisition. Admission uses the
existing contribution and acknowledgment; commit failures expose no response.
Canonical execution dispatches, receipts and patches retain their formats.
The existing risk-trigger inputs and continuation check remain available for T4.
Lease passed for exactly the three T1 source paths; signature G with key
693AB15F91734B0C, author and committer John Crenshaw <john@jcrenshaw.dev>.
No committed deletions.

T2: predicted and observed 7 library tests passed, 0 binary tests selected,
0 failed. Command: `TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --lib --bin cadence gap152_`.
Library filtered 223; binary filtered 220. Tests independently supply intended,
failed and issued records; no admission/observation/return/query chain supplies
inputs. Existing bound ReturnSubmission stays unchanged. The additive
failure_event path owns the sole closure; standalone launch-failure observations
refuse without full return identity. Unissued choices are omitted from the
return receipt's FIRST projection. Lease passed with five T2 paths, signature G,
no committed deletions. Clippy remains unrun.

T3: predicted 4 library and 4 binary passes. The first run stopped at compilation
because the new storage stub omitted resync/remove; implemented those required
stub methods only. Rerun matched 4/4 passes, zero failures, library filtered 230
and binary filtered 220. Command: `TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --lib --bin cadence gap153_`.
Tests supply saved membership, observations, closure state and new acquisition
inputs independently. Delivery contribution checks persisted closure state,
not a caller's timestamp. Later distinct views retain their own append-only
records; existing acquisitions and the original attempt view are not relabeled.
Lease passed four T3 files; signature G, no committed deletions. Clippy unrun.
[deviation] T3's first prediction encountered an incomplete new test stub at
compilation; its targeted repair and rerun produced the stated eight passes.

T4: predicted 3 library and 1 binary passes. First run failed compilation on a
partially moved request home; retained cloned home fields for the added risk
lookup. The same command then matched: 3 library passes (234 filtered), 1 binary
pass (224 filtered), zero failures. Command: `TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --lib --bin cadence gap154_`.
The conversion consumes confirmed saved scope/material/surface observations;
a reference to older matching evidence waits when superseded. No public enum
can assert a clean scan. Non-dispatch arms stop before material acquisition and
admission contribution. The single public admission composition test supplies
unanswered surfaces and three work stubs, all uncalled. Modern pause preserves
these non-dispatch responses. Existing execution risk inputs remain unchanged.
Lease passed four T4 paths; signature G, no committed deletions; clippy unrun.
[deviation] T4's initial predicted passes stopped in compilation; the new
request borrow was repaired without changing prior tests or any criterion.

T5: predicted and observed 1 library pass (237 filtered), 1 binary pass (225
filtered), zero failures. Command: `TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --lib --bin cadence gap155_`.
Minimalism branches before ordinary routing/configuration resolution, consumes
the retained-target specialist selection and saves separate specialist evidence.
Requested overrides remain absent; ordinary and other specialist selection
retain their existing paths. The existing dispatch serializer already omits an
absent override, verified with independently supplied admission/attempt values.
The builder receives independently supplied pinned-panel and routing-failure
alternatives; neither is consumed for minimalism. No prose assertion or repeated
target matrix was added. Lease passed three T5 files; signature G; no committed
deletions. Clippy remains unrun.

T6: predicted and observed 5 library passes, zero failures, 238 filtered.
Command: `TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --lib gap156_`.
A thin traversal consumes typed one-directory resolver observations, sorts and
deduplicates descendant files, and excludes empty and nonempty directory nodes
from file acquisition. Symlink/special nodes refuse. Per-file acquisition and
path validation are separate units. Retention checks directory observations
again before saving, and retained reads never consult live source membership.
The additive I/O default preserves existing flat-directory fixtures. All new
traversal tests use a resolver stub; acquisition/read tests supply their own
observations and saved manifest. Lease passed three T6 files; signature G;
no committed deletions. Clippy remains unrun.

T7: predicted and observed 10 binary passes (226 filtered), 0 selected library
tests (243 filtered), zero failures. Command: `TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --lib --bin cadence gap157_`.
Then predicted and observed 2 existing stream tests passed, 0 failed/filtered:
`TMPDIR=/tmp RUSTC_WRAPPER= CARGO_NET_OFFLINE=true cargo test -p cadence --test phase9_stream stream_`.
The new input reader validates JSON structure, decoded keys, duplicate keys,
UTF-8 and outer escapes incrementally. Decoded candidate counts saturate at the
raw cap plus one; review classification respects actual argument location and
key order. Unrelated operations do not inherit the raw-return cap. Metadata and
whole-frame budgets bound wire storage; read scratch is 1024 bytes. Pending
framing and decoder state survive cancellation. Rejection poisons the stream,
emits an explicit input error and cannot deliver a partial request or suffix.
The installed transport source supplies the integration seam; no network lookup,
dependency change or spawned-client test was used. Exact findings text is never
parsed/reserialized by ingestion. Existing bounded raw-reader defense remains.
Removed the grouped request clone and redundant forwarding copy. Also collapsed
the equivalent nested continuation condition in the touched service file.
All new tests drive the reader/decoder directly with authored wire fixtures and
a counting/chunked input stub; no module-level duplicates were added.
Lease passed four T7 paths; signature G, no committed deletions. All seven task
commits were present before the final verification began.

## Final checks

The sole static-analysis invocation ran after T7 commit 6873ea6b:
`TMPDIR=/tmp CARGO_NET_OFFLINE=true RUSTC_WRAPPER= cargo clippy --all-targets -- -D warnings`.
Predicted exit 0/no warnings; observed exit 0/no warnings. No second invocation.
The configured workflow.test_command was null. Selected the Cargo package full
suite: `TMPDIR=/tmp CARGO_NET_OFFLINE=true RUSTC_WRAPPER= cargo test -p cadence --no-fail-fast`.
Prediction: exit 0 with zero failed targets; aggregate count to be measured.

Full-suite exit: 0. All 43 top-level summaries are green: 243 library tests,
236 binary tests, 679 tests across 40 integration targets, and zero doc tests.
Total: 1,158 passed, 0 failed, 0 ignored, 0 measured, 0 filtered.
Two nested one-test subprocess summaries are already covered by the binary
parent result and are excluded from this total. The initial mechanical sum of
all output summaries was 1,160; excluding those two yields 1,158. No repair or
second full-suite invocation was needed.

| Target | Passed | Failed | Ignored | Filtered |
|---|---:|---:|---:|---:|
| Library unit tests | 243 | 0 | 0 | 0 |
| Binary unit tests | 236 | 0 | 0 | 0 |
| derivation_consistency | 6 | 0 | 0 | 0 |
| derivation_inputs | 12 | 0 | 0 | 0 |
| evidence_store | 4 | 0 | 0 | 0 |
| execution_boundary_compat | 11 | 0 | 0 | 0 |
| execution_store | 18 | 0 | 0 | 0 |
| mcp | 18 | 0 | 0 | 0 |
| next_action | 7 | 0 | 0 | 0 |
| phase7_guard | 84 | 0 | 0 | 0 |
| phase7_lease | 23 | 0 | 0 | 0 |
| phase7_receipts | 11 | 0 | 0 | 0 |
| phase7_risk | 22 | 0 | 0 | 0 |
| phase7_surfaces | 4 | 0 | 0 | 0 |
| phase8_config | 34 | 0 | 0 | 0 |
| phase8_dispatch | 24 | 0 | 0 | 0 |
| phase8_global | 12 | 0 | 0 | 0 |
| phase8_interview | 40 | 0 | 0 | 0 |
| phase8_routing | 44 | 0 | 0 | 0 |
| phase9_admission | 19 | 0 | 0 | 0 |
| phase9_binding | 8 | 0 | 0 | 0 |
| phase9_consumers | 10 | 0 | 0 | 0 |
| phase9_context | 19 | 0 | 0 | 0 |
| phase9_contract | 37 | 0 | 0 | 0 |
| phase9_deferred | 13 | 0 | 0 | 0 |
| phase9_forward | 1 | 0 | 0 | 0 |
| phase9_history | 1 | 0 | 0 | 0 |
| phase9_inventory | 3 | 0 | 0 | 0 |
| phase9_invoking | 2 | 0 | 0 | 0 |
| phase9_manifest | 12 | 0 | 0 | 0 |
| phase9_material | 26 | 0 | 0 | 0 |
| phase9_model | 2 | 0 | 0 | 0 |
| phase9_observations | 7 | 0 | 0 | 0 |
| phase9_policy | 24 | 0 | 0 | 0 |
| phase9_recovery | 10 | 0 | 0 | 0 |
| phase9_returns | 21 | 0 | 0 | 0 |
| phase9_selection | 30 | 0 | 0 | 0 |
| phase9_specialist | 3 | 0 | 0 | 0 |
| phase9_stream | 2 | 0 | 0 | 0 |
| phase9_views | 5 | 0 | 0 | 0 |
| store | 17 | 0 | 0 | 0 |
| store_crash | 33 | 0 | 0 | 0 |
| Doc tests | 0 | 0 | 0 | 0 |

## Before and after

Before: inherited Plan 6 evidence is 523 selected passing tests: 521 integration
tests across 30 targets plus two binary selectors. No baseline suite was rerun.
These are the exact recorded commands that produced that baseline:

```sh
TMPDIR=/tmp CARGO_NET_OFFLINE=true RUSTC_WRAPPER= cargo test -p cadence --test phase7_guard --test phase7_risk --test phase7_lease --test phase8_config --test phase8_routing --test phase8_interview --test phase8_global --test phase8_dispatch --test mcp --test phase9_contract --test phase9_stream --test phase9_material --test phase9_manifest --test phase9_context --test phase9_policy --test phase9_selection --test phase9_specialist --test phase9_admission --test phase9_binding --test phase9_observations --test phase9_returns --test phase9_recovery --test phase9_consumers --test phase9_views --test phase9_inventory --test phase9_deferred --test phase9_model --test phase9_forward --test phase9_invoking --test phase9_history
TMPDIR=/tmp CARGO_NET_OFFLINE=true RUSTC_WRAPPER= cargo test -p cadence --bin cadence review_service::tests::home_request_
TMPDIR=/tmp CARGO_NET_OFFLINE=true RUSTC_WRAPPER= cargo test -p cadence --bin cadence phase9_pause_tests::modern_
```

After: the full-suite command above passed 1,158 tests and covers more targets
than that inherited selection. Do not interpret the difference as new unique
tests. This plan adds 43 unique tests (20 library, 23 binary). Existing integration
harnesses include source modules directly, so they also register some of the same
inline library unit cases; no duplicate composition cases were authored and no
existing harness was changed. The original 30 integration targets now contribute
556 passes within the full-suite result.

## Deviations and open items

The initial internal-stubbing checkpoint was resolved by the owner's ruling,
without changing a criterion or the plan. Three initial task verifications
stopped at compilation (T1, T3, T4); each targeted correction and successful
rerun is recorded above. No acceptance criterion or locked decision was revised.

Open items: none within PLAN-7. The four existing MANUAL.md items remain pending:
the live advisory loop, observed advisory handoff/hooks, historical regression
instruction, and native producer/restart episode. No live exercise is claimed.
Phase-12 workflow ownership and phase-10 settlement remain outside this plan.

All seven task commits have good signatures under key 693AB15F91734B0C, with
John Crenshaw <john@jcrenshaw.dev> as author and committer. Each task passed its
lease gate and post-commit deletion check. No existing test or fixture, manifest,
lockfile, git configuration, or source outside the lease was changed. The
pre-existing .planning/STATE.md edit remains untouched. No network, install,
delegation, push, extra lint run or scope expansion was performed. This report
is committed separately under the owner's explicit instruction.
