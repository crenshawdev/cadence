PLAN COMPLETE
Plan: .planning/phases/5/PLAN-7.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1 - Correct the plan gate README states | 39583ba | `:56` now reads advisory at `solo`, off at `shipped` "because an advisory gate blocks nothing", adjudicated at `critical`; one clause added inside the same physical line, 1 insertion / 1 deletion, `wc -l` still 150, and README-24/26/27/65/66's clauses untouched |
| 2 - Make METHOD's two plan-gate sentences agree with its own trigger table | ffb16a4 | `:309` now reads advisory at `solo`, off at `shipped`, adjudicated at `critical`; `:374`'s adjudicated-list clause reads off at `shipped`; the `diff` sentence at `:310-311` and the `diff`/`phase_diff` members of the `:371-377` list are byte-identical |
| 3 - Remove the staged-diff risk check from METHOD's commit protocol | 1b4086f | the "check the staged diff against the risk-surface list" clause deleted with no replacement; the protocol still names `git add -A`, `{type}({scope}): {description}` and the post-commit glance; paragraph rewrapped 4 lines -> 3, so METHOD.md is one line shorter below `:158` |
| 4 - Retire the risk floor from METHOD and INTERNALS | fa0d4b4 | METHOD `:420-427` rewritten to "Detection sets no floor", naming the v2.7.0 cut of both the name-based detector and the eight `risk.override.<surface>` waivers and pointing the surfaces at the one blocking `risk_surface` review; the same clause inside `INTERNALS.md:13` rewritten in place with its other six cited clauses intact; the destructive-op pre-filter paragraph untouched |
| 5 - Re-pin the ledger cites this phase's gap edits moved | 4c651fe | 112 `line` cells shifted, every changed row differing in its third field only; METHOD -1 below `:158`, `plan.md` +4 below `:329`, `config-catalog.md` -1 below `:51`, `recall.md` +3 below `:6`; `README.md`, `INTERNALS.md`, `new-project.md` and `adopt.md` net to zero and were not edited; the 16 first-below and largest-row reads each show their row's claim |
| 6 - Close the fourteen rows and re-state the ledger's own counts | af72e6f | the twelve deferred rows plus METHOD-25 and NEW-PROJECT-08 (both re-verdicted `stale`) now read `corrected - <sha> - <what changed>` against the eight PLAN-6/PLAN-7 commits, every sha resolving and every claim cell byte-identical to `13094a8`; `grep -ic "prose fix beyond this phase"` is 0; the census re-stated to 845 accurate / 42 corrected / 45 divergence / 1 RETIRED = 933, the 20-of-28 classification re-cut to 20 + 8 + 14, and the fourteen named as their own class in a called-out paragraph |
Deviations:
- [deviation] Task 5's `Verify:` asserts the shift applied to a doc's rows reproduces its per-hunk deltas, and in the same breath that opening a row at its cite shows its claim. For METHOD-88 the two halves conflict: its cite `158-159` STRADDLES the commit-protocol hunk (old 157-158 collapsed to new 157), so the mechanical delta yields `158-158`, which shows only half the claim. Pinned it to its live location `157-158` instead - the read half of the criterion is what the ledger's `:206-216` rule is written to.
- [deviation] The same task says to shift every row that sits below a hunk. Five rows below one do NOT move, because their cite is provenance rather than an address by the ledger's own rule at `:173-181` and `:206-216`: four resolved `corrected - <sha>` (METHOD-01, METHOD-02, METHOD-04, CONFIG-02) and CONFIG-29, resolved `divergence - ... line left at run-1 provenance`. That is the precedent the task itself cites: `a829f39` re-pinned 261 cells and not one `corrected` row.
Open items:
- Declined the fuller shape on task 1: naming `review.triggers.<t>.gate` as the way a user turns the `shipped` plan review back on. The `Verify:` asks only that the line state the three gates and keep the five neighbouring clauses, and the task caps the addition at one clause carrying the reason; `cadence-core/references/review-triggers.md:355-359` already documents the override for a reader who follows it.
- `METHOD.md:317-319` still reads "The defaults encode an opinion about where scrutiny pays: heavy before code exists, heavy before publishing, blocking on risk, merely advisory on an ordinary diff." Both plan and diff are off at the `shipped` default and `diff` resolves advisory at no level, so this is the same defect tasks 1-2 fixed one paragraph above it. No ledger row cites it and no task named it, so it is left standing for phase 6 or the next sweep.
- `.planning/DOCS-CLAIMS.md`'s "As of run 2 the `line` column is the LIVE location" paragraph still carries run 2's own re-pin arithmetic ("493 rows, of which 261 cells moved"). It stays true as a statement about run 2, but after task 5's 112 further shifts and task 6's 14 cites turning into provenance, those numbers no longer census the file.
- The two ledger transcription defects the plan's Notes named remain open and unrouted: INTERNALS-12 reads "`model.escalate_on_failure`, on by default" under an `accurate` verdict against a doc that says off, and CONFIG-CATALOG's `review.triggers.<t>.gate` row publishes per-key schema defaults that `route-table.json` resolves at no stakes level.
