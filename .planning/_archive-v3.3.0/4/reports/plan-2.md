PLAN COMPLETE
Plan: .planning/phases/4/PLAN-2.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1: A reader for the roadmap's per-phase criteria list | e05d471 | `phaseCriteria(text, phase)` in `lib/planning-files.mjs`; block extraction pulled out of `phaseRequirements` into one shared `phaseDetailBlock` so the two readers cannot disagree about where a block ends. Both live heading spellings admitted, bold markers balanced. 4 rows; run over `.planning/ROADMAP.md` reads 4/6/4/5/6 and phase 6 not-found - the plan's stated expectation. |
| 2: The criteria-ceilings seam | 854950e | `planning.mjs criteria-size` with four caller-supplied bounds (`--context-min/max`, `--roadmap-min/max`, no config keys); `--phase` scopes, absent walks `parseRoadmapPhases`. `plan-size`'s conditional-comparison envelope reused (`compared[]`, `within: null`, `*_found:false`). 7 rows including both heading spellings. Live run on this repo: phases 2 and 5 over the 2-5 roadmap ceiling, phase 6 `roadmap_found:false` - the plan's stated expected finding, left alone. `CONTRACTS` row added. |
| 3: Call the seam where each workflow presents a criteria count | 7e29d2b | One invocation each: `context.md` at the tail of `write_context` (`--phase {N}`, 3-7), `new-project.md` and `adopt.md` after the roadmap write and before the `header: "Roadmap"` approval gate (2-5), with the presented criteria-count column now the seam's. Stated as a REPORT, not a gate, and `*_found:false` stated as not-zero at all three. Budgets re-pinned: adopt 14,966 -> 15,627, context 19,032 -> 19,622, new-project 17,858 -> 18,547. |
| 4: `context.md` reads both its config keys in one call | 5d90cb9 | The `spend_gate` read is the batched multi-key form (`memory.backend planning.commit_docs`); `grep -c 'config.mjs'` is 1. The `commit` step names that batch as the source and says it is never re-read. `memory.backend` still gates recall ahead of the call. Budget 19,622 -> 20,033. |
| 5: `plan.md` issues seed-reqs and cursor set in one message | f721313 | Prose only (D-17): substeps 1 and 2 merged into one instructed message carrying both calls, the old substep 3 renumbered to 2 and left last. All three `seed-reqs` reporting rules intact (`orphan_ids`, `no_active_section`, `ok:false` continues). `execute.md`'s `triage-gate.md` re-read untouched (D-16). Budget 21,649 -> 21,788. |
| 6: Pin the per-workflow seam-invocation count with a census | ad1294c | `seam-calls.test.mjs`: non-vacuity row first (a wrapped multi-key call is ONE, a backticked mention is none), then one row per file. `plan.md` **9** as stated. `context.md` **6**, not the stated 5 - see the deviation. Falsified live: a second `config.mjs get` appended to `context.md` reddened exactly that row with the file and expected count in the message; restoring it made the suite pass. |
| 7: Close the phase gate | none (verification only) | `node --test cadence-core/bin/*.test.mjs` exit 0, 1790 pass / 0 fail; `node cadence-core/bin/self-verify.mjs` exit 0, `ok:true`, 0 problems (no `unbudgeted-surface`, `budget-overrun`, `unknown-flag`, `unknown-subcommand` or config-key failure); `npx tsc -p tsconfig.ci.json` clean. Nothing to repair, so the REPAIR LEASE over PLAN-1's files was never exercised - zero edits under it. Every surface either plan changed equals its `weight-budgets.json` row exactly, re-pinned in the task that grew it. |
Deviations:
- [deviation] Task 6 states `context.md` **5**; the reachable count is **6**, and the test asserts 6. The plan's derivation (6 at plan time, -1 for PLAN-1's close collapse, 0 for the config-key fold) omits the +1 that task 3 of this same plan mandates - the `criteria-size` call it requires `context.md` to make. 5 is unreachable while that call exists, so asserting it would fail the suite AC7 gates on. 6 is not a baselined number either: skipping the close collapse, or reading `planning.commit_docs` in a second call, each make it 7, so the row still reddens on every half-done version of this phase's work. `plan.md`'s stated 9 was measured correct and is asserted as stated. The full arithmetic for both files, and this discrepancy, are stated in `seam-calls.test.mjs`'s header so the next reader is not left to rediscover it.
Open items:
- The census carries rows for `context.md` and `plan.md` only - the two files the 2026-08-14 scan actually measured. Declined a walk over every workflow: nothing in the plan states a count for the others, so a row for one of them would pin whatever it holds today, which is the baselining failure the amendment names. Add a row with the measurement that justifies it.
- `criteria-size` over this repo's own `.planning/ROADMAP.md` reports phases 2 and 5 above the 2-5 roadmap ceiling (6 criteria each) and phase 6 `roadmap_found:false`. Left exactly as found, per the plan's Notes - that is the seam working, and editing shipped planning docs to quiet a report is the move it exists to prevent.
- Four budgeted surfaces neither plan touched carry stale headroom, all shrinks from earlier work and all passing (the check is a ceiling): `templates/config.json` -26 B, `workflows/debug.md` -9 B, `skills/cad-capture/SKILL.md` -55 B, `skills/cad-plan-review/SKILL.md` -10 B. Out of this phase's scope; re-pin when one of them is next edited.
