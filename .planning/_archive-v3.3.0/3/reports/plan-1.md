PLAN COMPLETE
Plan: .planning/phases/3/PLAN-1.md
Tasks: 7 of 7
| Task | Commit | Note |
|---|---|---|
| 1 - One protected-branch coercion, consumed by the two readers that already tolerate a string | 0342204 | `lib/protected-branches.mjs` (`resolveProtectedBranches`) + unit test; git-guard and git-publish inline ternaries replaced. String-form arm added to git-publish.test.mjs, `[]` arm to git-guard.test.mjs. Falsified: removing the module's string branch reddens exactly the new git-publish arm and nothing else. |
| 2 - The two readers that silently drop a string form start honoring it | 9b45035 | git-branch `decide` and land-cleanup `cleanup` now call the shared helper; per-consumer string arms added, `config-seams.test.mjs` base arm untouched. Measured D-07 on a real fixture: post-fix `cleanup --dir <fx>` prints `"base":"release"`, the HEAD~1 copy of the same seam on the same fixture prints `"base":"main"`. |
| 3 - The shared argv/file input module | 8338b6a | `lib/seam-input.mjs` exports `optionalFlag` (never throws), `flagValue` (throws `missing-flag-value`) and the `''`-on-failure `readText`; header states the D-03 divergence and names `lib/include-consumers.mjs` as the deliberate second `readText`. 10 arms, including the explicit disagree-on-a-valueless-flag arm. |
| 4 - The five bins import the flag and file readers instead of defining them | 52b1935 | Five `flag` and three `readText` definitions deleted; each bin keeps its call spelling through a one-line adapter binding `const flag = (name) => optionalFlag(argv, name)`. All five seams checked live on a valueless flag - each still answers from cwd, none returns `"reason":"internal"`. |
| 5 - The two throwing-flag bins import the same reader | 8b2aedd | `flagValue` deleted from weight.mjs and self-verify.mjs, imported from `lib/seam-input.mjs`; both catch arms and both `\|\| default` fallbacks untouched. `weight resident --root`, `weight resident --root ""` and `self-verify --root ""` each print `{"ok":false,"reason":"missing-flag-value","detail":"--root"}`. |
| 6 - One reader of "what branch is this repo on" | 3d1beb2 | `lib/git-head.mjs` (`readCurrentBranch`, degrades to `''`, takes the caller's dir) + unit test incl. the not-a-repo arm; the three copies in git-guard/git-publish/git-branch deleted, land-cleanup untouched. `grep "'--abbrev-ref'"` over `bin/*.mjs` and `bin/lib/*.mjs` names only the new module. |
| 7 - A tree-wide definition census that reddens on a re-copy | fcaee97 | `helper-census.test.mjs` walks every `.mjs` under `cadence-core/bin/` and pins each of the four helpers at exactly one definition, by BODY IDIOM rather than by name. Falsified twice: a probe `lib/*.mjs` carrying all four copies reddens 4 of 5 arms, and a probe `*.test.mjs` carrying only the branch reader reddens that arm - each message naming the offending file and the shared home. Both probes deleted. |
Closing AC5 gate (this worktree's tree; PLAN-2 had not landed here): `node --test cadence-core/bin/*.test.mjs` 1739/1739 pass, `node cadence-core/bin/self-verify.mjs` returns `ok:true` with `problems:[]` - no `unbudgeted-surface`, no `budget-overrun`. `npx tsc -p tsconfig.ci.json` clean after every task; `detect-commands` reports `lint: null`, so there is no lint command Cadence can find and that half was skipped.
Deviations: none
Open items:
- Declined a second census rule per helper (a name-based `function <name>(` count beside the body-idiom count). The body idiom alone meets the Verify and catches a paste-back under any name; a name rule would only add a way to trip on a same-named different contract. Add one if a task ever needs a helper pinned by name.
- Not this plan's scope, recorded because the census surfaced it: `planning.mjs:130-132`'s `read()` is a THIRD file-reader contract (null on any failure, no `isFile()` guard), distinct from both `lib/seam-input.mjs`'s `''` form and `lib/include-consumers.mjs`'s guarded null form. It is named in the census's assertion message so it cannot be mistaken for a copy, but nothing pins it.
- `git.protected_branches` still fails `config.mjs validate` as `array_string` when set to a string, exactly as D-08 says it should this phase - all four readers now honor a value the validator calls an error.
