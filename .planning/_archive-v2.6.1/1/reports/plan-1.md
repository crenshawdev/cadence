PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 6 of 6
| Task | Commit | Note |
|---|---|---|
| 1: Close DFC-01 and guard every source byte under `cadence-core/bin` | 1e949bc | Two NULs -> `\0` escapes (18116 -> 18118 B); `binFiles(root, { every: true })` + self-verify check 15 `nul-byte-in-source`; 4 self-verify tests + 1 trace separator test. Mutation proof: planted NUL -> exit 1, `{"kind":"nul-byte-in-source","file":"cadence-core/bin/lib/trace.mjs","detail":"literal U+0000 at byte offset 14802 (1 in file) - type \0 instead"}`; restored -> `"problems":[]`. `grep -rn "const worker" cadence-core/bin/` now prints `lib/trace.mjs:336` without `-a`; `file` reports `JavaScript source, ASCII text`. 182 tests green, tsc exit 0. |
| 2: Make the weight budget exact in both directions | 20c59b6 | Check 4 `bytes > budget` -> exact, second kind `budget-undershoot`; check-4 comment and `METHOD.md:589-595` restated. Mutation proof: one byte deleted from `cadence-core/references/git-guard.md` -> exit 1, `{"kind":"budget-undershoot","file":"cadence-core/references/git-guard.md","detail":"6720B is under budget 6721B by 1B - re-pin the entry"}`; restored -> `"problems":[]`. Clean tree `"problems":[]` (93 surfaces exactly at budget); 133 self-verify tests green, tsc exit 0. |
| 3: Close DFC-03 and pin the dimension count mechanically | f6eed02 | `:113` five -> six; budget entry re-pinned 5344 -> 5343 in the same commit. New `cadence-core/bin/prose-agreement.test.mjs` (word-to-number table, three-way agreement). Patch-and-rerun proof: `:113` back to `five` -> `AssertionError: <success_criteria> says 5 but <dimensions> enumerates 6`; restored -> pass. self-verify `"problems":[]`, tsc exit 0. |
| 4: Close DFC-02 and DFC-04 and move the coupled byte figures together | 98be3d2 | `phase_diff` cell -> `off / advisory / adjudicated` in `review-triggers.md:244` and `docs/WORKFLOW.md:168`; `risk_surface` shape-(c) qualifier dropped. `review-triggers.md` 17,733 -> 17,714 B re-pinned in `weight-budgets.json`, `cad-land/SKILL.md:44`, `cad-plan-review/SKILL.md:39` (still five digits, so both skills keep their own entries). Two tests added, driving `route.mjs resolve --file` once per stakes level. Patch-and-rerun proofs, run twice: `off / off / adjudicated` -> `AssertionError: review-triggers.md states off / off / adjudicated, the resolver returns off / advisory / adjudicated`; `the checkpoint returned` -> `AssertionError: shape (c) is qualified by its producer again, so a /cad-task fire is a shape the row does not admit`; both reverted -> 3 pass. `route.mjs resolve --role cad-reviewer` reports `phase_diff advisory` at the default `shipped`; `grep -rn "off / off" cadence-core docs skills METHOD.md README.md` returns only two test-file hits (see Open items). self-verify `"problems":[]`, tsc exit 0. |
| 5: Re-measure `docs/EVIDENCE.md` and check the four figure sites | 99d4e78 | Re-measured at 98be3d2. Pre-existing 159 B staleness confirmed and corrected in THREE places, not the two named: `cadence-core/workflows/` 200,050 -> 200,209, grand total 475,412 -> 475,551, and `/cad-task` turn-one 5,221 -> 5,380 with the command total 279,064 -> 279,223 (all exactly 716fb60's task.md growth after f8f22cf). This cycle's own moves: `review-triggers.md` 17,733/4,433 -> 17,714/4,429, references subtotal 159,184 -> 159,165, skills 90,734 -> 90,733, six reachable figures -19 each (execute, verify, plan, context, land, milestone, decision-review), four cad-plan-checker dispatch rows -1 each. `zeroResident` 26,306 B unchanged. Three tests added on `weighAll`. Patch-and-rerun proof: one digit changed -> `AssertionError: cadence-core/references/review-triggers.md bytes`; restored -> 6 pass. Hand spot-check: all twelve rows match `weight.mjs --root .` in both columns. self-verify `"problems":[]`, tsc exit 0. |
| 6: Record the closures in the sweep ledger and run the gates | f50a8a0 | `:47-52` hazard note and the three `## Defects filed out of this sweep` bullets rewritten as closed, each naming its closing commit; the `+ DFC-0k` paragraph now states that the suffix carries status; five suffix rows -> `DFC-0k closed <sha>`; `TASK-01` gains `+ DFC-04 closed 98be3d2`. No `DFC-04` bullet invented, no `verdict` cell touched (verified by `git diff -U0`: exactly six table rows changed). All three gates green: `node --test cadence-core/bin/*.test.mjs` 1467 pass / 0 fail, self-verify `"problems":[]`, `npx tsc -p tsconfig.ci.json` exit 0. |
Deviations:
- [deviation] Task 5's action named the twelve-largest row, the per-directory table and the four dispatch rows as the figures that move. Expected only those to differ; observed `/cad-task`'s turn-one figure (5,221) and the 23-command total (279,064) ALSO 159 B stale, from the same `716fb60` cause - `/cad-task` eagerly includes `workflows/task.md`. Corrected under the task's own "update EVERY figure that differs" instruction.
- [deviation] Task 6's `+ DFC-04` append was first applied by a text match on `| stale | corrected - 044806c |`, which matched SEVEN rows (AUDIT-01, AUDIT-02, CONFIG-01, CONFIG-02, EXECUTE-01, NEW-PROJECT-01, PROGRESS-01) rather than `TASK-01` alone. Caught before staging by an id-keyed re-scan; the six intended rows are the only ones in the committed diff, verified with `git diff -U0`.
Open items:
- `detect-commands --root /data/code/cadence` reports `lint:null, typecheck:null`, so Cadence finds no static-analysis command it can detect. The project's real typecheck is `npx tsc -p tsconfig.ci.json` (named in the plan's own gates) and it was run at every task; there is no separate linter in the tree.
- `cadence-core/bin/dispatch-phrasing.test.mjs:254` carries an inline fixture string copying the old `off / off / adjudicated` row. It is a fixture for the dispatch-phrasing rule (a table row is not an imperative dispatch), not a claim about the gate, and that file is outside this plan's `files:` lease - left as-is per the plan's own Notes.
