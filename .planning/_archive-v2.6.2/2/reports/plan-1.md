PLAN COMPLETE
Plan: .planning/phases/2/PLAN.md
Tasks: 9 of 9
| Task | Commit | Note |
|---|---|---|
| 1: Delete the dead `templates/UAT.md` include and its phase-1 waiver bridge | 75f4aac | `/cad-verify` eager 24,533 -> 18,688 as predicted; total 274,839; `WAIVED.length === 0`; self-verify ok:true; 1516 tests pass; tsc 0. D-02's zero-resident claim falsified - see deviations. |
| 2: State the `--tokens` provenance once, in `lib/trace.mjs`'s header | b5a4143 | All four runnable checks pass (one-sentence-per-site `OK`, five figures gone from the weighed surfaces and present once each in the header, `read off the HOST` in exactly one path). `/cad-execute` 28,510, `/cad-plan` 22,651, `/cad-context` 19,459, total 273,338. self-verify ok:true; 1516 tests pass. `review-triggers.md` GREW - see deviations. |
| 3: Trim `cad-land`'s guardrails and its three deferral tails, and close the `seams.md` drift | 064f49e | Held at the risk-surface gate, review fired and PASSED (one blocker adjudicated dead - the cut rule survives operative at `skills/cad-land/SKILL.md:113-118` and in the blanket `No auto-push` guardrail). Committed as staged, then amended message-only to correct a fabricated figure - see deviations. `/cad-land` turn-one 18,209 -> 17,098; `grep -c 'guardrails block' seams.md` = 0; check 13 clean on all three regions; self-verify ok:true; 1516 tests pass. Transient risk diff deleted, never committed. |
| 4: Cut `execute.md`'s duplication and close its three drifts | a5ec70b | All four drift greps as predicted (no `start` step; `workflow.test_command` absent from the batch resolve and present once inside `execute_parallel`; one changed `config-reach.md` row; `execute_parallel` named once). `/cad-execute` turn-one 28,510 -> 27,233, `execute.md` 27,768 -> 26,491, `config-reach.md` 18,623 -> 18,649 (it GREW - the reach cell gained words), zero-resident total 26,306 -> 26,332, twelve-largest rows 5/6 swapped. self-verify ok:true; 1516 tests pass; tsc 0. Two of the task's own verify commands were unsatisfiable as written - see deviations. |
| 5: Cut `context.md`'s duplicated purpose and output-contract prose | f914dbe | Every grep as predicted: the three cut phrases 0, `judged, not scored` 1, `Five sections, nothing else` 1. `/cad-context` turn-one 19,459 -> 19,029, `context.md` 18,632 -> 18,202 (drops below `seams.md` in the twelve-largest table). self-verify ok:true; 1516 tests pass. No deviations. |
| 6: Cut `verify.md`'s duplicated purpose and the `fields_version` legacy archaeology | 0e9d24e | Landed after task 1 as required. `They are NOT legacy` 0, `untraced` 2, `written ONLY through the uat seam` 1. `/cad-verify` turn-one 18,688 -> 17,872, which is exactly the new `verify.md` budget 16,823 + 1,049 as the task predicted. self-verify ok:true; 1516 tests pass. Destination already complete - see deviations. |
| 7: Cut `plan.md`'s duplicated purpose and its design-history contrast | 9ab56dd | Every grep as predicted: `four-agent fan-out\|4 flags` 0, `Research is /cad-context's job` 1, `Both re-dispatches close on their own` 1. The plan's falsification of the sweep row re-checked and CONFIRMED - `grep -n 'Both re-dispatches\|SOME terminal' cadence-core/bin/trace.test.mjs` returns nothing, so `:330-335` stayed. `/cad-plan` turn-one 22,651 -> 22,413. `trace.test.mjs` 51/51 with `BRACKETING` unchanged; self-verify ok:true; 1516 tests pass. |
| 8: Drop the planner and reviewer contracts' `<success_criteria>` blocks | 49ba72e | The pre-check ran first and cleared all ten items - each grepped to an earlier governing instruction in its own contract, hit lines recorded in the commit body - so nothing was reported instead of cut. `grep -c 'success_criteria'` 0 on both, 2 on `cad-plan-checker-contract` (D-18 holds). All seven dispatch rows fell (cad-planner 9,083 -> 8,652 at the top rung, cad-reviewer 4,073 -> 3,796), `skills/` 89,569 -> 88,861. self-verify ok:true; 1516 tests pass including the DFC-03 dimension-count test. |
| 9: Move `cad-health`'s maintainer cross-reference into `lib/branch-decision.mjs` | 366771b | Both runnable checks pass: `sorts above the newest tag` 0 and `v2.4.0` 0 in the SKILL, the destination probe prints `OK` (all four fragments present in `branch-decision.mjs`), `v1.9.1` still 1 - the worked example stayed eager per D-20. `/cad-health` turn-one 7,006 -> 6,585. `branch-decision.test.mjs` 23/23; self-verify ok:true; 1516 tests pass; tsc 0. |

Deviations:
- [deviation] Task 1, D-02: expected `weight.mjs resident` to report a fourth `zeroResident` entry `cadence-core/templates/UAT.md` with `zeroResidentBytes` 26,306 -> 32,098; observed the array unchanged at three entries / 26,306. Cause: `cadence-core/bin/lib/resident-weight.mjs:372-379` walks `cadence-core/references` ONLY, never `templates/`, so no template can ever appear in `zeroResident`. The template IS now reachable from no command (`/cad-verify` reachable fell 100,098 -> 94,253, exactly the 5,845 B removed), but the seam does not report templates. Action taken: `docs/EVIDENCE.md`'s zero-resident table and its "Three reference files..." lead-in were left UNCHANGED, because they remain true as written and `prose-agreement.test.mjs:340-352` asserts them against the live measurement. Making D-02 true would require widening the seam's walk to `templates/` - a change to `resident-weight.mjs`, which this plan's `files:` lease does not declare, and which would change the asserted table's membership by an unknown number of rows. Task 1's own Verify clause naming four entries and 32,098 is therefore unmet and unmeetable as written; AC1 does not mention `zeroResident` and is fully satisfied.
- [deviation] Task 2: expected `cadence-core/references/review-triggers.md` to SHRINK like the other four `--tokens` sites; observed it grow 17,714 -> 17,837 B (+123). Cause: its site stated only two of the three rules (omit, never `--tokens 0`, no ROUTINE clause), so bringing it to the D-11 one-sentence form that carries all three ADDS text there. Same shape as `plan.md:247`, which D-12 already anticipated. The file is eager for no command, so no turn-one figure rose. Consequence handled in the same commit: D-08's coupling re-stated the figure in `skills/cad-land/SKILL.md` and `skills/cad-plan-review/SKILL.md`, both byte-neutral.
- [deviation] Task 2: the plan's site-sentence wording had to avoid the phrase "read off the HOST", because the task's own AC4 check is `grep -rl 'read off the HOST' cadence-core/ skills/ agents/ docs/` printing exactly ONE path - and `cadence-core/bin/lib/trace.mjs` is itself under `cadence-core/`. The six site sentences therefore carry the three RULES and no provenance clause; the operational pointer survives at each site in the command placeholder `<the token count on the subagent return>`.
- [deviation] Task 3: the plan's own D-07 verify command failed on the first shape of the step-3 Read sentence. Expected the trimmed sentence to pass; observed `FAIL`, because the sentence splitter `(?<=\.)\s` put "The reference is 17,837 B and is consulted at exactly ONE step." in a SEPARATE sentence from the `Read` sentence, and the check requires bytes + "consult site" in the SAME sentence. All three arms were restructured to `Read <path> (<N> B, one consult site - <where>) ...` so the figure and the count sit inside the Read sentence itself. Also observed: the phrase required is literally "consult site", not "step".
- [deviation] Task 3: cutting the `(references/seams.md, File round-trip)` citations from the three tails removed `references/seams.md` from `/cad-land`'s one-hop reachable set entirely - reachable fell 63,473 -> 43,787, of which 18,547 is seams.md leaving and only ~1,100 is prose. Checked before accepting: `seams.md` is still reachable from seven other commands (`cad-config`, `cad-context`, `cad-execute`, `cad-new-project`, `cad-plan`, `cad-progress`, `cad-verify`), so it did NOT go zero-resident and `docs/EVIDENCE.md`'s zero-resident table is unaffected. The File round-trip rule is an authoring rule, not a rule `/cad-land` applies at runtime.
- [deviation] Task 3: `cadence-core/bin/git-publish.mjs:3-12` does not by itself carry the third item the plan attributed to it (the `git.auto_close`-plus-non-protected-branch condition); that sits at `:31-34`. The `deferred-reads.mjs` header note cites both spans rather than the one the plan named.
- [deviation] Task 3, self-inflicted: the continuation dispatch's commit message stated `/cad-land` turn-one as "12,957 -> 11,890". Those figures were fabricated - they appear in no measurement, no report row and no plan line. The true pair is 18,209 -> 17,098 (`git show HEAD~1:docs/EVIDENCE.md` against the live `weight.mjs resident`). Caught during task 4's measurement, before any push; corrected by `git commit --amend` on the message only, so the commit hash moved 4f7f07a -> 064f49e and the tree is byte-identical to the reviewed staged diff.
- [deviation] Task 4: the task's verify says `grep -c 'clean worktree' cadence-core/workflows/execute.md` still prints at least 1; observed 0 - both AFTER the edit and at HEAD before it. The phrase is line-wrapped in the source ("start it from a clean\nworktree (`git status --porcelain` empty)"), so the check could never have printed 1. The runtime rule itself is intact at `execute.md:86-87`; the substitute check `grep -c 'not merely a clean index'` prints 1.
- [deviation] Task 4: the task's verify says `git diff -- cadence-core/references/config-reach.md | grep -c 'min_plans_for_parallel\|use_worktrees'` prints 0; observed 2. Both rows are unchanged CONTEXT lines inside the diff hunk, which a bare `grep` over diff output counts. `git diff --numstat` reports `1 1` for the file, and scoping the grep to changed lines (`grep '^[+-][^+-]'`) prints 0, so D-15's "exactly one cell, the other two byte-identical" holds as stated.
- [deviation] Task 4: `docs/EVIDENCE.md`'s zero-resident table needed a re-pin the task did not name - `config-reach.md` is itself a zero-resident surface, so its 26 B growth moved that table's row AND its stated total (26,306 -> 26,332), which `prose-agreement.test.mjs:340-352` asserts. CONTEXT D-04 anticipated exactly this ("editing it moves five figures at once"); the task text listed four.
- [deviation] Task 4: `.planning/DOCS-CLAIMS.md` row EXECUTE-06 claimed "the ten config keys in the single `config.mjs get`". D-17's move leaves nine, so the row's TEXT was corrected alongside its span rather than only re-pointed.
- [deviation] Task 6: the task says to read `planning.mjs:1266-1320` and "add only what is missing there". Expected a gap to fill; observed NONE - both halves of the cut passage (`uat init` writes `fields_version` unconditionally so no seam-written file can present as legacy; legacy now also requires a CONTEXT declaring no ids) were already stated there in full, at `:1282-1288`. Nothing was copied. What was added is a five-line pointer recording that this block is now the tree's ONLY statement of the argument and that `workflows/verify.md` keeps just the additive `untraced` consequence - so AC6's "present in a git-tracked destination" is satisfied by text that predates the cut, and the pointer stops a future reader restoring the workflow prose.
- [deviation] Task 7: cutting `plan.md:8-9` deleted the exact sentence `.planning/DOCS-CLAIMS.md` row PLAN-03 quotes ("4 flags, not ~20"), which no re-point can fix - the claim has no surface left. The row is RETIRED in place (span `—`, resolution stating the v2.6.2 cut) rather than deleted, so the next sweep sees why the claim vanished instead of re-extracting it. The ledger had no retired-row precedent; this is the first.
- [deviation] Dispatch-wide: `planning.mjs detect-commands --root /data/code/cadence` returns `lint:null, typecheck:null`, and `.planning/config.json` sets no `workflow.lint_command`, so Cadence finds no static-analysis command in this project. Stated once here; each task instead runs the static check its own Verify names, `npx tsc -p tsconfig.ci.json` (exit 0 at tasks 1, 4, 9).

Open items:
- Whether `zeroResident` should widen from `references/` to `references/` + `templates/` is a real question the D-02 deviation surfaced, and it is a seam change with its own EVIDENCE-table consequences. Not in this phase's scope (no new self-verify check, no unleased file). Recommend filing it for the phase-2 verify pass or a later cycle.
- `cadence-core/references/config-reach.md:133` still omits `workflows/execute.md` from `workflow.test_command`'s honouring sites, even though `execute_parallel` names the key explicitly after task 4. PLAN.md's Notes already record this as a ledger observation and scope it out of AC5; it is now one grep away from being obviously wrong and is worth closing in phase 3 or at the docs sweep.
- The phase's measured eager saving is 11,639 B across the 23 user-invocable commands (turn-one total 280,684 -> 269,045), against the sweep's ~17,400 B estimate. CONTEXT's flagged assumption predicted the shortfall and named its two causes (D-11 keeps ~200 B per `--tokens` site, D-18 makes one of three contract checklists uncuttable); a third showed up in execution, `review-triggers.md` and `config-reach.md` both GREWING to state a rule correctly. Total measured prose fell 477,012 -> 470,406 B, and dispatch bytes fell a further 431 B per `cad-planner` and 277 B per `cad-reviewer`. No criterion pins a total, so this is reporting, not a miss - the recoverable bytes are in phase 3's deferrals.
- AC7 (a separate UAT walk item per affected command, with numbered steps) is the verifier's to write; the six commands this phase's commits touch are `/cad-verify`, `/cad-land`, `/cad-execute`, `/cad-plan`, `/cad-context` and `/cad-health`.
