# Phase 2: The free cuts - Context

Gathered: 2026-08-10
Feeds: /cad-plan 2

## Scope boundary

In: Prose cuts on the eager path that need no new file, no `lib/deferred-reads.mjs`
register row and no new `weight-budgets.json` row - the dead `templates/UAT.md`
include and its phase-1 waiver bridge, the `--tokens` provenance paragraph
collapsed into `lib/trace.mjs`'s header, per-surface duplication in `cad-land`,
`execute.md`, `context.md`, `verify.md`, `plan.md`, two contract
`<success_criteria>` checklists and `cad-health`'s regression anecdotes - plus
the four `CTW-05` drifts. Each surface is its own commit, carrying its
`weight-budgets.json` re-pin and its `docs/EVIDENCE.md` table updates.

Out: Every deferral behind a `Read`. Nothing in this phase moves prose to a new
reference file or adds a register row - that is phase 3 (`CTW-04`), which shares
`execute.md` and `context.md` and is sequenced after to avoid two rounds of
budget churn on one file. No new self-verify check. No behaviour change to the
sequential execution path (D-17).

Deferred: None.

Plan shape: multiple plans, same phase - /cad-plan breaks it down. Eight surface
commits, each pairing a prose cut with a budget re-pin and EVIDENCE table
updates, plus a deletion commit carrying its own test rework.

**Two ROADMAP phase-2 criteria are superseded here.** Criterion 1's
`/cad-verify` figure of 18,741 is wrong: the `@`-include line is itself 53 B of
the SKILL.md, so the drop is 5,845 B and the eager total lands at 18,688 (D-01).
Criterion 3's "three contract `<success_criteria>` checklists" is two:
`cad-plan-checker-contract`'s is asserted by `prose-agreement.test.mjs` as the
DFC-03 defect fix and stays (D-18). `REQUIREMENTS.md`'s `CTW-03` and `CTW-05`
rows were corrected in place at this gate, on the user's yes, so requirement and
criteria state the same thing.

## Durable decisions

- D-04 (CI cost per cut): Every cut commit updates `docs/EVIDENCE.md` alongside `weight-budgets.json`. A budget re-pin alone lands red. Evidence: `cadence-core/bin/prose-agreement.test.mjs:206-237` (twelve-largest bytes and est tokens plus membership), `:239-266` (per-directory subtotals and grand total), `:298-321` (per-command turn-one figures and the 23-command total), `:323-339` (eager/reachable pairs), `:340-352` (zero-resident), `:354-371` (dispatch bytes). Six of the seven surfaces this phase edits sit in the twelve-largest table; `config-reach.md` sits in the zero-resident table too, so editing it moves five figures at once (`docs/EVIDENCE.md:40-64,84-95,110-119,148-176`).
- D-07 (cad-land shape): `cad-land`'s three deferral tails are TRIMMED around a surviving `Read` sentence per arm, never deleted as blocks, and each surviving sentence keeps its measured-bytes and consult-count clause. Evidence: `cadence-core/bin/lib/deferred-reads.mjs:110-137` anchors three of its four rows in this file at regions `3`, `4(a)` and `4(b)`; `:70-80` is sentence-level matching written against exactly this file's step 4b; `cadence-core/references/seams.md:240-243` mandates the inline figure. If wrong, the phase whose purpose is safe cutting files `deferred-read-unread` - the exact defect check 13 exists to catch.
- D-11 (--tokens shape): Each of the six surviving prose statements keeps ONE compressed sentence carrying all three rules - omit the flag when no figure exists, never `--tokens 0`, and a figureless return is ROUTINE. The ROUTINE clause is not centralised away from any site. Evidence: ROADMAP:80 requires all three "everywhere"; `.planning/CAPTURE.md`'s phase-4 item, closed 2026-08-09 by `045c479`, records the regression where a missing ROUTINE clause made `unrecorded` read as a skipped bracket. Costs ~200 B per site against the sweep's fuller estimate; the saving is the price of a rule the model applies at runtime, which criterion 4 says not to move.
- D-18 (Contract checklists): `skills/cad-plan-checker-contract/SKILL.md`'s `<success_criteria>` block is NOT cut. Evidence: `cadence-core/bin/prose-agreement.test.mjs:69-95` asserts the block exists and states "All six dimensions checked", cross-checked against `<dimensions>`; that assertion IS the DFC-03 defect fix. Relaxing the test to make the cut removes the only mechanical guard against a checker reporting success having skipped Proportionality. The planner and reviewer contracts' blocks do go (D-19).
- D-21 (Rationale destinations): Rationale that "moves to a design-note" must land in a git-TRACKED note. Evidence: `.gitignore:21-23` ignores `/design-notes/dd-*.md` ("candid design explorations stay local"); `git ls-files design-notes` lists 11 tracked dated records, including this cycle's own `design-notes/sweep-2026-08-10-context-weight.md`. Moving text into a `dd-*` note satisfies criterion 4 on the working tree and violates it in the published repo - the precise failure the phase's rationale-preservation rule exists to prevent.
- D-03 (Test rework): The five live tests written against the dead include existing are REWORKED in the deletion commit, never deleted. Evidence: `cadence-core/bin/include-consumers.test.mjs:64-80` (`verifyRoot()` byte-copies the live SKILL.md and asserts the include line is present), `:187-193` (`WAIVED.length === 1` and the row's exact shape) plus its DOWNWARD/UPWARD bound tests; `cadence-core/bin/self-verify.test.mjs:1921-1930`, `:1932-1950` (`'the waived include must still exist to be removed'`, read off the live repo). Deleting them instead removes the AC5 evidence that check 16 fires on a real instance at all.

## Decisions

- D-01 (The dead include): The eager drop is 5,845 B, not 5,792 - the `@`-include line is itself 53 B of the SKILL.md. `/cad-verify` eager lands at 18,688, and `skills/cad-verify/SKILL.md`'s budget row moves 1,102 -> 1,049 in the same commit. Evidence: measured on `skills/cad-verify/SKILL.md:29` (file 1,102 B, line 53 B); `cadence-core/bin/weight-budgets.json:96`; `cadence-core/bin/self-verify.mjs:713-728` (`budget-undershoot` fires on any shrink). Leaving the row at 1,102 fails self-verify on the phase's first commit.
- D-02 (The dead include): `cadence-core/templates/UAT.md` goes zero-resident on deletion, so `docs/EVIDENCE.md`'s zero-resident table gains a fourth row, its total moves 26,306 -> 32,098, and its "Three reference files are reachable from no command at all" sentence is corrected on both the count and the reference/template wording. Evidence: `node cadence-core/bin/weight.mjs resident` (the template appears only in `cad-verify`'s reachable set), `cadence-core/bin/lib/resident-weight.mjs:372-387`, `docs/EVIDENCE.md:110-119`, `cadence-core/bin/prose-agreement.test.mjs:340-352`.
- D-05 (Waiver): The waiver MECHANISM survives with an empty register - `CODES.staleWaiver`, `CODES.expiredWaiver`, the `waived` parameter and the ROADMAP read all stay; only the row and its header bridge paragraph go. `WAIVED.length === 0` is asserted from both the lib test and the self-verify CLI, keeping "growing it is a red build" in the other direction. Evidence: `cadence-core/bin/lib/include-consumers.mjs:60-83` frames the ROW, not the mechanism, as the bridge; `.planning/phases/1/CONTEXT.md` D-16 states the remedy for a future false positive is "a waiver row with its reason, not a widened scan", which presumes a register still exists.
- D-06 (History): `CHANGELOG.md:474-476`'s false justification for this include is left in place as history and answered by the new v2.6.2 entry, not edited. Evidence: `cadence-core/bin/self-verify.mjs:5-9` deliberately excludes DESIGN/LINEAGE/CHANGELOG because they "may name cut keys while explaining the cut".
- D-08 (CI cost): Touching `references/review-triggers.md:106-107` for the `--tokens` cut costs four coupled edits, because its byte count is quoted in two shipped SKILL.md files and asserted. Evidence: `cadence-core/bin/prose-agreement.test.mjs:182-204` asserts both `skills/cad-land/SKILL.md:44` and `skills/cad-plan-review/SKILL.md:39` state `weighAll`'s measured figure; `cadence-core/references/seams.md:240-243`. The `.planning/CAPTURE.md` item calling these figures "checked against nothing" is stale on that point - they are enforced.
- D-09 (Ledger): `.planning/DOCS-CLAIMS.md` rows carrying line spans into cut paragraphs move with the cuts: EXECUTE-20 (`:482`), EXECUTE-21 (`:483`), EXECUTE-22 (`:484`), PLAN-18 (`:565`), VERIFY-DEEP-05 (`:626`). Nothing mechanical validates the ledger, so this is commit discipline, not a gate. Without it the next `/cad-docs-verify` sweep re-verifies against line numbers pointing at unrelated prose.
- D-10 (--tokens destination): `cadence-core/bin/lib/trace.mjs`'s header takes the provenance paragraph as a fourth named contract beside its existing three. Evidence: `cadence-core/bin/lib/trace.mjs:1-33` ("Three contracts, each load-bearing"); `cadence-core/bin/` is not a weighed surface - the 93 budgeted entries are `agents/`, `references/`, `templates/`, `workflows/` and `skills/` only - so the destination is genuinely zero-cost.
- D-12 (--tokens sites): Five FILES but SIX statements. `plan.md` carries a full paragraph at `:182-189` and a second one-clause form at `:247` that already has the target shape. Evidence: `cadence-core/workflows/plan.md:182-189,247-248`, `context.md:122-144`, `execute.md:233-240`, `verify-deep.md:37-42`, `references/review-triggers.md:106-107`. A criterion phrased "five sites" is satisfiable while one of `plan.md`'s two statements stays a duplicate.
- D-13 (--tokens safety): The trace census is unaffected - `cadence-core/bin/trace.test.mjs:736-762` matches `\btrace\s+append\b` COMMAND lines, not the prose around them. But the comment at `:384-388` cites `context.md`'s printed `cad-planner 146,405` and goes stale when `context.md:128-144`'s measured figures leave; it moves with them.
- D-14 (CTW-05): `execute.md:241`'s nonexistent `start` is corrected to `git_guard`. Evidence: `cadence-core/workflows/execute.md:49` opens `<step name="git_guard">`, `:110` is the `phase_start` append, `:119` closes it, `:120` opens `choose_path`; no `<step name="start">` exists anywhere in the file.
- D-17 (CTW-05): `workflow.test_command` is DROPPED from the `:36` eager batch resolve and read where it is consumed, inside `execute_parallel`. It is not made to run on the sequential path. Evidence: `cadence-core/workflows/execute.md:36`, `:367` (its only consumer); `cadence-core/references/config-reach.md:133` names its honouring sites as `workflows/task.md`, `workflows/coverage.md` and the executor contract, not `execute.md`. Running it on the sequential path is a behaviour change to the default execution route needing its own criterion and UAT item; shipping it as a drift-closure commit ships an unreviewed behavioural change.
- D-15 (CTW-05): Only `parallelization.max_concurrent_agents` moves with `execute_parallel`; `min_plans_for_parallel` and `use_worktrees` are read in `choose_path`, which phase 3 keeps eager. One `config-reach.md` reach cell changes, not three. Evidence: `cadence-core/workflows/execute.md:123` and `:139` (both inside `choose_path` 120-171) against `:349` (inside `execute_parallel` 343-402); `.planning/ROADMAP.md:94` and `design-notes/sweep-2026-08-10-context-weight.md:57` both keep `choose_path` eager. `REQUIREMENTS.md`'s CTW-05 row was corrected at this gate to match.
- D-16 (CTW-05): The `seams.md:236-240` drift is closed by DELETING the "and in its guardrails block" clause, not by adding a `references/git-guard.md` citation to `cad-land`'s guardrails. Evidence: `skills/cad-land/SKILL.md:175-189` (guardrails cite no reference) against `:33`, `:40`, `:60` (git-guard cited at steps 1, 2 and 3, already >1 consult site, so the eager justification survives); `design-notes/sweep-2026-08-10-context-weight.md:120-121` records that eager keep as correct. Adding a citation would make the sentence true by growing the surface this cycle exists to shrink and insert a fourth consult site into a file whose deferral arithmetic three register rows depend on.
- D-19 (Contract checklists): Dropping `<success_criteria>` from the planner and reviewer contracts is consistent with the stated authoring convention. Evidence: `cadence-core/references/conventions.md:138-141` scopes that section to "new workflows"; `skills/cad-executor-contract`, `cad-verifier-contract` and `cad-assumptions-analyzer-contract` ship without one; `cadence-core/bin/self-verify.mjs:855` bans the tag only in agent RUNG files. Nothing mechanical fails - the risk is behavioural compliance in the dispatched role, which is why AC7's per-command walk exists.
- D-20 (cad-health): The `cad-health` block splits. The maintainer-facing "must not drift back to sorts-above-the-newest-tag" cross-reference moves to `cadence-core/bin/lib/branch-decision.mjs`'s header; the `v1.9.1` worked example STAYS eager, because it decides a verdict the model issues at runtime. Evidence: `skills/cad-health/SKILL.md:107-113` (the example directly governs "a health check calling it drift"); `cadence-core/bin/lib/branch-decision.mjs:1-25` already carries single-reader-discipline rationale of exactly this kind. Moving the whole block makes `/cad-health` report a legitimate untagged maintenance milestone as drift - the regression the paragraph was written after.

## Acceptance criteria

- [ ] AC1: `skills/cad-verify/SKILL.md` contains no `@`-include of `templates/UAT.md`; `cadence-core/templates/UAT.md` is still on disk with its 5,792 B budget row unchanged; `node cadence-core/bin/weight.mjs resident --root .` prints `/cad-verify` eager as 18,688 B; `include-consumers.mjs`'s `WAIVED` register is empty, with both the lib test and the self-verify CLI test asserting length 0.
- [ ] AC2: `node cadence-core/bin/self-verify.mjs` returns `ok:true` with `problems:[]` when run at EVERY commit this phase creates, not only the last - each cut commit carries its own `weight-budgets.json` re-pin.
- [ ] AC3: `node --test cadence-core/bin/*.test.mjs` passes at every commit this phase creates, including `prose-agreement.test.mjs` against the `docs/EVIDENCE.md` tables updated in those same commits.
- [ ] AC4: The `--tokens` provenance paragraph appears in full exactly once in the tree, in `cadence-core/bin/lib/trace.mjs`'s header; each of the six prose statements (`context.md`, `execute.md`, `plan.md` x2, `verify-deep.md`, `review-triggers.md`) is one sentence naming all three rules - omit when no figure, never `--tokens 0`, a figureless return is ROUTINE.
- [ ] AC5: All four CTW-05 drifts are closed and each is grep-checkable: `execute.md` names no step `start`; `workflow.test_command` is absent from the `:36` batch resolve and read inside `execute_parallel`; `config-reach.md`'s reach cell changed for `max_concurrent_agents` only, with `min_plans_for_parallel` and `use_worktrees` untouched; `seams.md` no longer claims a git-guard consult in cad-land's guardrails.
- [ ] AC6: Every rationale block removed from an eager surface is present in a git-tracked destination - a `cadence-core/bin/**/*.mjs` header or a `design-notes/*.md` that `git ls-files` lists - and `skills/cad-plan-checker-contract/SKILL.md`'s `<success_criteria>` block still exists.
- [ ] AC7: The phase's UAT checklist carries a separate walk item per affected command (`cad-verify`, `cad-land`, `cad-execute`, `cad-plan`, `cad-context`, `cad-health`), each with numbered steps, rather than one bulk check.

## Flagged assumptions

- The phase will land below the sweep's ~17,400 B estimate, by D-11 (~200 B kept per `--tokens` site) and D-18 (one of three contract checklists uncuttable). No criterion pins a total; `REQUIREMENTS.md`'s CTW-03 row was corrected at this gate to state per-surface measured re-pins instead. If the shortfall matters, the recoverable bytes are in phase 3's deferrals, not here.
- D-19's contract-checklist removal has no mechanical failure mode - the only signal that a planner or reviewer's behaviour degraded is AC7's per-command walk. A regression that the walk does not surface ships silently.
- D-09's `DOCS-CLAIMS.md` line-span updates are unenforced by any check; a missed row surfaces only at the next `/cad-docs-verify` sweep.
