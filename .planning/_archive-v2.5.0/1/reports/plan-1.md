PLAN COMPLETE
Plan: .planning/phases/1/PLAN.md
Tasks: 15 of 15
| Task | Commit | Note |
|---|---|---|
| 1. The trace spine - `lib/trace.mjs` and `planning.mjs trace` | 28295af | lib + seam + 20 tests + .gitignore entry; CI trio green |
| 2. The two seam producers - routing and provider events | 8fbf16d | route.mjs `routing/resolve`, review-provider.mjs `provider/request` incl. tier reverse lookup; 11th `mergeLayers` callsite born arm-(a) satisfied |
| 3. Orchestrator lifecycle brackets and the blocked-halt remedy in `execute.md` | b1d657b | anchor at `git_guard`, brackets for both paths, D-08 statements, blocked/missing-PLAN remedy + sequential fallback |
| 4. The outcome family and `/cad-progress --trace` | b619fbe | `outcome/adjudication`, `outcome/uat_verdict`, verifier bracket across all 3 verify-deep arms, `--trace` read-only branch; synthetic 4-family proof green AND its falsifier |
| 5. The `LSP` tool grant on both `cad-executor` rungs | 4356564 | `KNOWN_TOOLS` + both `tools:` lines + 2 teeth tests |
| 6. `planning.mjs detect-commands` | 5d56f0e | 5 lint arms, 5 typecheck arms, `source` per slot, 14 fixture-tree tests; this repo returns the nothing-detected envelope as predicted |
| 7. `workflow.lint_command` across five surfaces + the executor's static-analysis step | 7d0d333 | schema/template/catalog/reach/contract in one commit, `<deviation_rules>` carve-out, 5 config tests |
| 8. `planning.mjs lease-check` and the commit-step lease gate | 5651cda | `parsePlanFiles` reader, one report-file exemption, prefix rule for `dir/`, 10 tests |
| 9. git-guard torn-config diagnostic + CAPTURE.md:227 closed | 6b59c46 | risk_surface checkpoint PASSED in adjudication; torn match anchored per the surviving finding, +1 regression test |
| 10. The `mergeLayers` warnings check over `cadence-core/bin/**.mjs` | c98ac8e | risk_surface checkpoint FAILED, all three blocking items closed before commit (see deviations); 1235 tests green, self-verify ok:true with `merge-warnings` in `checked`, tsc clean |
| 11. `phase_diff` resolves the same through all three surfaces | 8814cc0 | table `shipped` -> advisory, schema default follows, template `review.triggers` block DELETED; +2 route tests incl. one whose fixture IS the template |
| 12. A dependency lockfile stops matching the `concurrency` surface | 52e2809 | basename exclusion before the token match; 22 new rows - 6 lockfiles, 4 real concurrency paths, 5 per-lockfile resolve fixtures (D-21) |
| 13. Fault injection across all six `review-provider.mjs` failure modes | 4856306 | 2nd implementation (module-private transport reference, fake transport, NO socket); risk_surface checkpoint PASSED, both `low`s closed before commit - transport typedef written at the wire types, source-scan guard rewritten to count the bare token. 1268 tests green (49 in this file, 0.54s), self-verify ok:true, tsc clean |
| 14. `git-branch.mjs decide` refuses a version the repo has already published | 8eec49e | tags read at the seam, `decideBranch` stays pure on a `publishedVersion` argument, `compareVersions` reused; +7 pure rows +3 live-git rows; live-repo regression byte-identical (`create cadence/v2.5.0`); `#87(a)` closed in CAPTURE with both arms cited at the lines the plan named, confirmed exact |
| 15. The `/cad-health` drift rule, the verifier's traced-value level 3, and the bug-patterns checklist | 4291ace | check 7 added (prose only, D-24 respected - no seam call, no self-verify check), level 3 modified IN PLACE with no level 5, `references/bug-patterns.md` created (11 patterns, 4247 B) and read by `debug.md` before the first candidate; 4 budget entries regenerated, self-verify ok:true |

Deviations:
- [deviation] task 1, plan-review finding (a), recorded not silently fixed: the plan's Verify expects `corr` `1-abc1234` on BOTH the `phase_start` anchor and the `dispatch` after it, but a `phase_start` line has no PRIOR anchor to scan and would take the phase-only id `1`. Resolved by letting the anchor event derive its own id from its OWN `--sha` (`correlationId(root, phase, ownSha)`), which is the same derivation from the same datum one line earlier and mints nothing (D-06 intact). Without it the lifecycle family splits across two ids and AC3's "all four families under ONE correlation id" is unreachable by construction, as is task 4's synthetic proof. Pinned by the test `the anchor event carries the id it anchors, not the phase-only fallback`.
- [deviation] task 2: the plan's stated construction for the byte-identical-envelope case - "a tmp dir in which `.planning` is a REGULAR FILE, resolving with `--file <tmp>/.planning/config.json`" - CANNOT be byte-identical to a writable root: that path makes the CONFIG layer unreadable too (ENOTDIR is not ENOENT), so `mergeLayers` adds a `config layer ... failed to parse and was skipped` warning the writable root does not have. The byte-comparison instead uses a root whose `.planning/trace.jsonl` is a DIRECTORY - `appendFileSync` fails EISDIR for any uid, equally chmod-independent, with the config layer intact so the two envelopes are genuinely comparable. The plan's ENOTDIR root is kept as its own case, asserting ok:true, no trace file, and exactly ONE warning - the pre-existing config one.
- [deviation] task 2: the routing event's warning field is named `warning_count`, not `warnings`. A field named `warnings` holding a number beside an envelope whose `warnings` is an array of strings is a reader trap. Same value, unambiguous name.
- [deviation] task 2: `route.mjs` and `review-provider.mjs` record NOTHING when no phase is in hand (no `--phase` and no cursor). An event keyed to no phase derives the empty-string correlation id and joins nothing.
- [deviation] task 2: the provider event's CONTENT (reason naming, `degraded`) is asserted in task 13 against the injected transport, as the plan's task 13 itself directs; task 2's provider-side tests assert the two properties provable with no stand-in.
- [deviation] task 5: the task's human-verify half - "dispatch a `cad-executor` on this machine" - was NOT taken as a `<checkpoints>` stop. The plan's own Notes route it explicitly ("Three criteria close outside the executor's reach and belong to /cad-verify's walk"), and an executor cannot spawn the agent that would prove it. The mechanical half is complete and asserted; the dispatch half is an open item below, with nothing claimed about it here.
- [deviation] task 6: `source` is an object with BOTH slots always present (`{lint, typecheck}`) rather than one scalar. Two slots are answered independently and can come from different manifests, so one field could only ever name one of them.
- [deviation] task 7, expected X observed Y: the first `config.test.mjs` cases were written against `get` returning `{value, source}` per key and `validate` reporting under `detail` - both wrong. `get` returns `values: {key: value}` flat with a top-level `source`, and `validate` reports under `errors`. Tests corrected to the observed envelopes.
- [deviation] task 7, expected X observed Y: `config.mjs set workflow.lint_command=` writes the EMPTY STRING, not `null` - `parseToken` takes an unparseable token verbatim, and `=null` is the clearing idiom. The new catalog row therefore says "shell string, or `null`" rather than cloning `workflow.test_command`'s "empty→`null`" wording, which is inaccurate for that key too (open item below). Both spellings are pinned by a test.
- [deviation] task 7: the human-verify half (an executor blocked by a failing lint in a scratch repo) is routed to /cad-verify's walk for the same reason as task 5's, per the plan's Notes. Open item below.
- [deviation] task 9, ORDERING CORRECTED against the plan: the plan says "the torn-layer `ask` precedes the ordinary protected-branch decision". Taken literally that returns `ask` before the protected decision is computed, which DOWNGRADES a configured hard block: with the repo layer torn and the user-global layer carrying `git.on_protected: refuse` on a protected branch, the guard returned `deny` before this task and would have returned `ask` after it - a torn file silently weakening a rail, which is worse than the silence D-17 replaces. The protected decision is now computed FIRST and the torn arm returns `ask` only when that decision is not a `deny`; a `deny` keeps its decision and gains the parse reason. The observable the plan asked for is intact (the torn reason is what the user sees, and it fires on non-protected branches too). Pinned by `a torn repo layer never CANCELS a deny the global layer configured`. This reordering was the subject of task 9's `risk_surface` checkpoint and PASSED adjudication; the one `high` objection (that the preserved `deny` violates "a warning must never CREATE a deny") was killed - the deny originates in the global layer's explicit `on_protected: refuse` and the arm can only preserve an existing deny, never turn `null` or `ask` into one.
- [deviation] task 9: the torn arm is keyed to warnings naming the REPO layer path specifically, per D-17's wording. A torn user-global layer therefore stays silent - it loses the same two keys, but widening beyond D-17 would put an `ask` on every commit in every Cadence project on the machine, and that is a decision to take deliberately rather than in passing.
- [deviation] task 9, review finding applied (user-selected, one of two): the torn match was a bare `w.includes(repoLayer)`. `mergeLayers` warnings are flat strings with no layer field (`lib/config-merge.mjs:55,:162`), so a `CADENCE_GLOBAL_CONFIG` path carrying the repo config path as a PREFIX (`<root>/.planning/config.json.global`) or as a SUFFIX (`<elsewhere>/<root>/.planning/config.json`) satisfied it and was misread as a torn repo layer - an `ask` on a non-protected branch whose repo layer parsed fine. Now anchored on both ends to `config layer <repoLayer> `, the path followed by its delimiting space. Regression test `a torn GLOBAL layer whose path merely PREFIXES the repo layer stays silent` uses exactly that prefix-shaped global path and asserts empty stdout on `feature/x`.
- [deviation] task 10, expected X observed Y: the new check was predicted to report exactly the SEVEN callsites this task owns; it reported EIGHT. The eighth was `lib/merge-warnings.mjs` itself - the rule's own diagnostic string spelled the identifier with its parens, and a lexical rule cannot tell a call from a string literal. That is the correct answer for a lexical rule, so the fix is at the mention (the message now writes the identifier without parens) and the property is stated as an accepted cost in the lib header rather than papered over with a second exclusion, which would have been a place for a real callsite to hide.
- [deviation] task 10, plan-review finding (b) from the plan's own text carried forward: the plan cites `planning.mjs:1309` and `review-provider.mjs:209`/`:231` for the callsites; on the live tree (after tasks 1-9) they sit at `:1327` and `:223`/`:245`. Same callsites, line numbers moved by earlier tasks in this plan.
- [deviation] task 10: the CAPTURE.md:214 item names `self-verify.test.mjs` as where both sides are pinned; they are actually in `cadence-core/bin/dispatch-phrasing.test.mjs:248-257` and `:259-266`. The closure sentence records the correction rather than repeating the item's claim.
- [deviation] task 10, risk_surface review item 1 (BLOCKER), fixed before commit: `git-publish.mjs` `publish()` and `reap()` bound `warnings` at the decision point and mutated anyway. Reproduced first, exactly as reported: a truncated global layer carrying `protected_branches:["release"]` made `reap --branch release` return `{"ok":true,"action":"reaped"}` and the branch was gone, where the same command with an intact layer returns `{"ok":false,"reason":"protected-branch"}`. Both mutating arms now refuse with `{ok:false, reason:"config-parse-failed", detail:<the parse warning>}` before `execFileSync`. PLACEMENT, stated as directed: the gate sits immediately before EACH mutation, not right after the destructure. After the destructure it would also refuse the arms that mutate nothing - `reap`'s already-absent SKIP would stop being `ok:true` and break the idempotent re-run cad-land depends on, and a `decidePublish`/`decideReap` refusal would lose its own named reason behind this one. Nothing between the destructure and the gate does I/O beyond reading git facts, so the narrower siting closes the same hazard. Regression tests per seam with a truncated global layer assert the refusal AND that the ref never reached the bare origin / the branch still exists.
- [deviation] task 10, risk_surface review item 2, fixed before commit: relay lines added at the three prose callers of the new `warnings[]` - `skills/cad-land/SKILL.md` step 4b publish, step 5 cleanup and step 5 reap - and at `cadence-core/references/git-guard.md`'s `git-branch.mjs decide` block. Both files' `weight-budgets.json` entries regenerated in the same commit (`cad-land/SKILL.md` 10467 -> 11096, `git-guard.md` 6166 -> 6446). `lib/route-relay.mjs`'s ISSUE_RE matches `route.mjs resolve` alone, so self-verify files nothing here and the lines are prose-only obligations.
- [deviation] task 10, risk_surface review item 3, fixed before commit: a test per seam now pins the EMISSION, not just the binding - `warnings[] rides the git-publish envelope on the acting AND the skip arms`, `warnings[] rides the envelope, and a torn layer puts the parse failure on it` (git-branch), `warnings[] rides BOTH land-cleanup envelopes, empty and torn`. Stripping `, warnings })` off the emits now fails.
- [deviation] task 10: closing review items 2 and 3 required editing four files the plan's `files:` frontmatter does not declare - `cadence-core/bin/git-publish.test.mjs`, `cadence-core/bin/land-cleanup.test.mjs`, `skills/cad-land/SKILL.md`, `cadence-core/references/git-guard.md`. The edits are the orchestrator's blocking-review direction; `PLAN.md` was left unmodified rather than back-filling its lease list mid-execution.
- [deviation] task 13, DEVIATION FROM CONTEXT D-11, directed by the checkpoint outcome: D-11 reaches the six failure modes through a test-only base-URL override on the `CADENCE_*` env-var precedent (`CADENCE_ROUTE_TABLE`, `CADENCE_GLOBAL_CONFIG`, `CADENCE_PLUGIN_MANIFEST`). That is REPLACED by D-11's own documented fallback: a module-private transport reference tests replace by importing the module. Reasoning, recorded as the decision it is - the precedent D-11 rests on does not hold for a variable that redirects a CREDENTIALED request. The three cited precedents redirect a file READ; this one would redirect a request carrying `authorization: Bearer <the user's real key>` resolved from `$XDG_CONFIG_HOME/cadence/providers.env`. The risk_surface gate on the first implementation found that end to end with a working proof of concept: one attacker-settable variable (a `.envrc` in a cloned repo, a devcontainer env block, a compromised npm script) made an ordinary run transmit the key in cleartext to an arbitrary local listener, and loopback is not a privilege boundary - whichever unprivileged local process binds the port first receives it. The env surface is therefore not fenced, it is GONE: `grep -rn CADENCE_PROVIDER_BASE cadence-core/` returns nothing, and a test asserts the module touches `process.env` in exactly three places, all of them the two known reads.
- [deviation] task 13, first implementation DISCARDED, not patched: `git reset` + `git checkout --` on both files returned them to 52e2809. `CADENCE_PROVIDER_BASE`, `resolveProviderBase`, `LOOPBACK_HOSTS`, the protocol-based `node:http`/`node:https` transport selection and every test exercising them are all gone; production behaviour is `node:https` against the three hardcoded adapter bases, unchanged from before the task.
- [deviation] task 13, MOOT - two findings from the first round's review are not carried forward because the surface they describe no longer exists: (1) `localhost` in the loopback allowlist being a DNS NAME resolved at connect time rather than an address, so a hosts-file or resolver entry walks the fence; (2) `resolveProviderBase` returning the RAW string while validating a PARSED URL, so the validated value and the used value could differ. Both are properties of the env override and of nothing that remains.
- [deviation] task 13: a FAKE transport (canned responses, no socket) rather than a real loopback stub server. The plan's task 13 specifies "a `node:http` stub bound to `127.0.0.1:0`". Directed by the checkpoint outcome and taken: it makes all six modes deterministic, removes the network from the suite entirely - which is what this file's own header already promised - and removes the accept/connect race that made the first round's stub-backed tests hang. The suite now opens no socket, resolves no hostname and speaks no TLS; the whole file runs in 0.54s.
- [deviation] task 13: the stand-in is injected at the `https.request` LEVEL, not at `request()`. `request()` keeps every byte of its own logic - URL parse, payload serialization, content-length, the `timeout` option, the `error` and `timeout` handlers - and only the function it dispatches through moves. That is what lets the timeout mode assert the seam's own wiring (the configured 25ms reached the transport options AND the rejection message) rather than a canned string.
- [deviation] task 13: "driven through the existing `run()` helper" is not reachable and is replaced. A module-private reference is by construction unreachable from a CHILD process, so the six modes run IN PROCESS through a second test-only export, `__runCommandForTests(argv)`, which goes through the same entry unwind (`main().catch(unwind)`) the CLI uses. The test captures `process.stdout.write` and `process.exitCode` around it, so every assertion is still on the ONE JSON line and the exit code the caller sees - `run()`'s contract, not its mechanism. Both exports are `__`-prefixed and documented as test-only at their definitions.
- [deviation] task 13, MOOT: the plan's third Verify bullet (`CADENCE_PROVIDER_BASE=https://evil.example ... still targets api.openai.com`) has nothing left to verify - there is no override to ignore. Its intent is covered without leaving the machine by `fault: the destination is the adapter base, and no environment variable moves it`, which sets three plausible hostile base names, asserts the request still addressed `https://api.openai.com/v1/responses`, and adds the structural assertion above.
- [deviation] task 13, D-10 correction scoped: `review-provider.test.mjs`'s header is rewritten to state the reversal, its date and its reason. It records that the "deliberately untested" half no longer holds while the "no network in the suite" half holds HARDER than before (a fake transport reaches neither a socket nor 127.0.0.1), and that `references/provider-api.md` remains the pin for the wire FORMAT. The other half of the policy, `design-notes/improvement-roadmap.md:5-6`, is untracked and outside the plan's `files:` list - still an open item below.
- [deviation] task 13: seven test cases cover the six named modes plus two adjacent properties. Mode 5 carries both of D-22's malformed arms in one case (`bad-json` for truncated JSON, `no-output` for a 200 with no extractable text). Added beside them: `consult` and `detect-models` degrade through the same mapping (the modes are properties of the shared transport path, not of `review`), and a valid-JSON/wrong-shape 200 is `bad-shape`, kept adjacent so the six cannot be read as the whole set.
- [deviation] task 13, risk_surface review `low` 1, fixed before commit: the `Transport` typedef declared an `any` return, so `const req = transport(...)` was `any` and all six `ClientRequest` uses in `request()` lost the checking they had when `req` came from `https.request`. The typedef is now written at the wire types - `(url: URL, options: import('node:https').RequestOptions, cb: (res: import('node:http').IncomingMessage) => void) => import('node:http').ClientRequest` - with a header sentence saying why a loose return here would silently un-check them. Falsified both ways: with the loose typedef a `req.onnn('error', reject)` typo passed `tsc -p tsconfig.ci.json`; with the typed one the identical typo is `TS2339: Property 'onnn' does not exist on type 'ClientRequest'`, and the untouched file is tsc-clean.
- [deviation] task 13, risk_surface review `low` 2, fixed before commit: the source-scan guard matched only `process.env.NAME` and `process.env[NAME]`, so `const { CADENCE_PROVIDER_BASE } = process.env;` and `const E = process.env; E.CADENCE_PROVIDER_BASE` both PASSED - a fence advertising protection it did not have, over the exact primitive this task removed. It now asserts the count of the BARE `process.env` token (3) and that erasing the two known reads (`process.env.XDG_CONFIG_HOME`, `process.env[name]`) leaves none behind, so a destructured, aliased or computed read fails whatever its shape. The comment now describes that, and says what to do when a legitimate read trips it. Falsified by injecting each of the two bypass shapes into `review-provider.mjs`: both make this test - and only this test - fail (48 pass / 1 fail each time).
- [deviation] task 14, expected X observed Y: the prediction was that the existing 18 branch-decision/git-branch rows pass untouched. One FAILED - `milestone + protected + null name` threw `TypeError: Cannot read properties of null (reading 'startsWith')`. The `off` + null-name arm reaches the new guard with `integrationName` null, because the naming-problem downgrade above it only catches auto/ask. Fixed at the guard (`name !== null && name.startsWith('cadence/')`) rather than by moving the guard, since a null name has no version to compare and its own diagnostic is the useful one; pinned by `published: the naming-problem ask still wins over the published guard`.
- [deviation] task 14: the tag-list clause needs a REAL git repo, which this suite deliberately does not have (`git-branch.test.mjs`'s header: "driven through the seam with an explicit --branch so no live git repo is needed"). Added a second fixture, `taggedFixture`, that `git init`s the same `.planning` tree, makes one empty commit and applies the tags, with identity forced through `GIT_AUTHOR_*`/`GIT_COMMITTER_*` and `GIT_CONFIG_GLOBAL=/dev/null` so it works on a machine with `commit.gpgsign` set globally. The pure `branch-decision.test.mjs` rows still run with no git at all (D-23), which is where the comparison semantics are proved; the three seam rows prove only that the tag list reaches the decision.
- [deviation] task 14: the published guard fires for `auto` and `ask` only, mirroring the naming-problem downgrade one arm above it. The plan lists `off` among the arms that stay unchanged, and the unknown-`auto_branch` default arm is left alone for the same reason - neither one would act on the name.
- [deviation] task 15: the plan says "order the checklist by observed frequency". Nothing in this tree measures bug frequency, so the order is JUDGMENT stated as such and not a measurement - general debugging frequency, with the patterns that make a fix look like it did nothing (stale artifact, swallowed error, overridden value) at the top because they are the ones that waste a whole loop. The reference says it is a filter rather than a script and that matching nothing is information too. If a later cycle measures the `.planning/debug/` corpus, the order is what should be re-derived from it.
- [deviation] plan-review findings left DELIBERATELY UNAPPLIED, recorded here rather than silently fixed, and still standing at task 13's close: (a) the first `phase_start` event has no prior anchor to scan (task 1's deviation above states the resolution taken); (b) task 4 verifies `planning.mjs trace render` rather than the `/cad-progress --trace` flag itself, so the flag's own branch is proved by prose and by the render it calls, not by an end-to-end invocation; (c) AC3's real-run half cannot be satisfied by this phase's own run (the plan's own Notes say so - the producers do not exist until tasks 1-3 commit) and is a UAT item for the next `/cad-execute`.

Open items:
- OPEN ITEM (task 13): `design-notes/improvement-roadmap.md:5-6` still states "wire paths stay untested by design (no network in the suite)", the other half of the policy D-10 reverses. It is UNTRACKED (`git ls-files design-notes/` is empty), it records a dated historical status rather than a live contract, and it is outside the plan's `files:` list - so it is named here rather than edited.
- OPEN ITEM (task 13): the timeout MODE is proved at the seam's own boundary, not against a real stalled socket. The fake emits `timeout` on the request object, which exercises `request()`'s real handler chain (`on('timeout')` -> `destroy(err)` -> `on('error')` -> reject -> `reason:"transport"`) and proves the CONFIGURED 25ms reached the transport options and the message. What no longer has a test is node's own socket-inactivity timer firing, which needs a socket; `resolveTimeoutMs`'s clamp and fallback rows already pin the value that timer would get.
- OPEN ITEM (task 9, second review finding - user DECLINED the fix, recorded instead): with a torn config the arm returns `ask` even when `currentBranch` is null, so a directory that is not a git repo, or a repo with no commits, now speaks where it used to stay silent. Fail-safe direction, and a `git commit` there would fail anyway.
- OPEN ITEM (task 10 review, recorded not fixed): `lib/merge-warnings.mjs` classifies a whole line as a comment when it starts with `*`, `//` or `/*` without tracking whether a block comment CLOSES before executable code on that line, so a callsite formatted `*/ const { config } = mergeLayers(...)` is skipped by both the lint and the callsite count in `self-verify.test.mjs`.
- OPEN ITEM (task 10 review, recorded not fixed): `land-cleanup.mjs` `cleanup()`'s protected-branch coercion lacks the #38 lone-string tolerance that `git-publish.mjs:86-90` and `git-guard.mjs:114-118` both apply, so `protected_branches: "release"` makes it emit `base: "main"` and the workflow checks out a base that may not exist.
- OPEN ITEM (task 10 review, recorded not fixed): `git-branch.mjs`, `git-publish.mjs` and `land-cleanup.mjs` emit `"warnings":[]` on every ordinary run while `route.mjs` and `planning.mjs` use the omit-when-empty form; the rationale comment cites route.mjs as precedent for the opposite of what route.mjs does.
- OPEN ITEM (task 10 review, recorded not fixed): `review-provider.mjs`'s new header sentence claims warnings ride "every provider trace event as config_warnings", but the trace records only a COUNT, and `traceProvider` returns early when there is no cursor phase, so on a repo with no cursor nothing is recorded at all. Its `reviewConfig()` line citation `:290` is also stale - the function is at `:303` after the header insertion.
- OPEN ITEM (task 12): D-05 names exactly two lockfile shapes, `*.lock` and `*-lock.json`, and the implementation matches it literally. `pnpm-lock.yaml` therefore still tokenizes to a `lock` token and still floors a phase to `critical`; so would any future `*-lock.<ext>` spelling. Widening the suffix rule is a one-line change but it would move past a locked decision, so it is recorded rather than taken.
- `cadence-core/workflows/execute.md` grew 19340B -> 22957B, `verify-deep.md` 2529B -> 3650B, `skills/cad-executor-contract/SKILL.md` 10891B -> 12625B, and `skills/cad-land/SKILL.md` 10467B -> 11096B. All plan- or review-mandated content; phase 2 is the context-reduction phase and these are its largest new candidates.
- AC2's dispatch half is unproven here and belongs to /cad-verify's walk: dispatch a `cad-executor` on this machine (no code-intelligence plugin installed) and confirm the dispatch completes normally rather than erroring on the unrecognized `LSP` entry in `tools:`. If it errors, both rung files revert and the grant is recorded as a refuted assumption.
- AC1's behavioural half is unproven here and belongs to /cad-verify's walk, fixture spelled out in the plan's task 7 Verify: a scratch git repo with `{"scripts":{"lint":"exit 1"}}` and no `.planning/config.json`, one source file with a trivial defect, `/cad-task` against a task that edits it. Expect `npm run lint` found by `detect-commands`, three bounded attempts, a `blocked` checkpoint, and NO commit for that task.
- AC3's real-run half is a UAT item for the NEXT `/cad-execute`: that phase's `.planning/trace.jsonl` carries all four families under one `corr` and `trace render` returns an empty `unpaired`.
- Pre-existing, not touched: `workflows/config.md`'s `workflow.test_command` row claims "empty→`null` (none)", but `config.mjs set workflow.test_command=` writes `""`. Same wording appears on other `str|null` rows.
- Task 14's regression assertion inherits a change task 10 made: `git-branch.mjs decide` now emits a `warnings` field on every envelope, so "byte-identical to its output before this task" means before TASK 14, not before task 10. Measured that way and it holds: the pre-task-14 seam (restored from HEAD by `git archive`) and the post-task-14 one both print `{"ok":true,"action":"create","branch":"cadence/v2.5.0",...}` for `decide --branch main` on this repo, `cmp`-identical.
- OPEN ITEM (task 15): `/cad-health`'s new check 7 is prose the model executes, so nothing fails when it is skipped - that is D-24's deliberate tier, with the mechanical half assigned to phase 4 (FRI-03). Until then the rule's only enforcement is the same as every other numbered check in that skill.
- OPEN ITEM (task 15): three surfaces grew against the phase-2 context-reduction budget - `skills/cad-verifier-contract/SKILL.md` 9206B -> 9644B, `skills/cad-health/SKILL.md` 4101B -> 4756B, `cadence-core/workflows/debug.md` 6510B -> 6785B (it had zero headroom) - plus the new `references/bug-patterns.md` at 4247B, which rides no dispatch and is read only inside `/cad-debug` step 1.
