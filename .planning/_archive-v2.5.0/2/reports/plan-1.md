PLAN COMPLETE
Plan: .planning/phases/2/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1: Compose per-command and per-dispatch resident bytes behind `resident` | a2dfa78 | `lib/resident-weight.mjs` + `lib/frontmatter.mjs` created, `parseSkillsField` moved out of `self-verify.mjs` with no re-export. All five plan-time eager figures reproduce exactly (cad-land 32676, cad-execute 25017, cad-verify 20136, cad-plan-review 17511, cad-plan 16438), reachable cad-land 57540 / cad-plan-review 37764, zeroResidentBytes 26095 over exactly config-reach.md + provider-api.md + model-hints.json. AC1 hand-sum verified: cad-land eagerBytes 32676 = `wc -c` of its own `eagerFiles`; `--role cad-executor` returns both rung agents, each `dispatchBytes` = own `agentBytes` + contracts. Both runs one JSON line. 20/20 weight tests, tsc 0, self-verify ok:true. |
| 2: `weight.mjs` CONTRACTS entry + a prose surface that invokes it | 01d220a | Entry `{'*': ['--root'], resident: ['--command','--role']}`; `INTERNALS.md` gains `## What a command carries` with the invocation on its own line. AC2 falsifier run: removing only the `resident:` sub-key yields `unknown-subcommand` / `weight.mjs resident` against INTERNALS.md; restoring returns `ok:true` with `problems:[]`. 109/109 self-verify tests. |
| 3: Record the pre-cut baseline in MEASUREMENTS.md | 1489508 | `## Before` measured at `01d220a` (parent), all 23 commands sorted by eager desc with the five measured ones bolded, 19 agent rows, workhorse mean 20,530.33 B stated, zero-resident 26095 derived. `## After` present and explicitly empty. Table figures written digits-only so a re-run diffs literally. |
| 4: State the size term the break-even rule is missing | 0d11825 | Four sentences added after the every-path clause in `references/seams.md`, leaving the two original clauses untouched; names `weight.mjs resident` as the decider, `references/git-guard.md` as the multi-step case that stays eager, and scopes the inline bytes+consult-count requirement to deferrals from that sentence forward. `seams.md` 17203 -> 18575 B, budget entry regenerated in the same commit; self-verify `ok:true`, `grep -c break-even` = 2. |
| 5: Defer `review-triggers.md` out of `cad-land`'s eager load | 9521954 | Eager 32676 -> 17934, strictly below the 20530.33 mean read from the same envelope (AC3). SKILL.md 11488 B (cap 14000), `grep -c '^@'` = 1, git-guard.md left eager. Reachable 57540 -> 59304 with zero set-membership change - two file-size moves only. |
| 6: Defer `review-triggers.md` out of `cad-plan-review`'s eager load | 48c6b64 | Whole `<execution_context>` block removed; step 2 names the full path, Reads it there, and states 15,134 B / one site. SKILL.md 2353 B (cap 2377), eager 17511 -> 2353, a drop of 15158 >= 15134 (AC4). |
| 7: Make a de-preloaded reference without a Read a self-verify failure | a3f6e1c | `lib/deferred-reads.mjs` with the frozen four-row register and the three kinds, wired as check 13 and named in `checked:`. Live sentence counts are exactly the register's (1/2/1/1), so the check is not passing vacuously. AC5 falsifier run live: deleting task 5's Read sentence yields `deferred-read-unread` naming `cad-land` and `references/review-triggers.md` ("0 of 1"); restoring returns `ok:true`. 118/118 self-verify tests, tsc 0. |
| 8: Fill in the after numbers and prove the scope corrections hold | 292b599 | `## After` + `## Deltas` + `## Scope corrections` written at `a3f6e1c`. AC6 checked mechanically: all 42 table rows re-parsed and compared against a fresh `weight.mjs resident` - 0 mismatches, zero-resident total 26095 unchanged. Every reachable change split by cause (cad-plan-review -20277 = -20253 one-hop artifact + -24 real; cad-land +1764 = +1372 seams.md + 392 own prose; the three workhorses +1372 each, all seams.md). AC7: audit `ok:true`, CTX-01 phase 2 / plan phases/2/PLAN.md / no `unpicked`, `orphans` null, CTX-02 under `## Deferred`. Gates: 1341/1341 tests, tsc 0, self-verify `ok:true` with `problems:[]`. |
Deviations: [deviation] Task 7: the register was specified to raise `deferred-read-missing-skill` whenever a row's SKILL.md is absent, with only an absent `skills/` directory exempt. Every fixture built by `self-verify.test.mjs` creates an EMPTY `skills/`, so that rule reports the whole register against them - measured, not assumed: with the exemption removed, an existing test fails (108/109). The row is now skipped when the skill's own DIRECTORY is absent and raised when the directory exists with no readable SKILL.md, which keeps the kind reachable (a test row proves it fires) while leaving partial fixtures usable. Stated in the lib header and pinned by a test. [deviation] Task 8 expected `seed-reqs --phase 2` to WRITE the Traceability row that clears an `unpicked` CTX-01 and an orphan `phases/2/PLAN.md`; observed `{"seeded":[],"skipped":["CTX-01"]}` and an audit that was already clean on both counts before the run - the row was already present from the `/cad-plan` commit. The command was still run unconditionally as instructed, REQUIREMENTS.md is byte-unchanged, and the observed envelope is recorded in `MEASUREMENTS.md ## Scope corrections` instead of the predicted one.
Open items: none
