PLAN COMPLETE
Plan: .planning/phases/5/PLAN.md
Tasks: 8 of 8
| Task | Commit | Note |
|---|---|---|
| 1 - Runtime-evidence artifact under `docs/`, linked and anchored from README | 1154790 | `docs/EVIDENCE.md` created: turn-one table for all 23 user-invocable commands, eager-vs-reachable for the ten heaviest, per-role dispatch bytes for all 19 rung agents, per-surface rollup and the twelve largest, each table beside its exact `weight.mjs` command, with D-08's three definitions stated and nothing published under a "turn one" label but `eagerBytes`. Figures PROVISIONAL by design; task 8 re-measured them. Trace half recorded as not-fired (D-09). README edited exactly twice: `:38` gains the `docs/EVIDENCE.md` pointer; `:132` keeps `231,422 -> 199,687` and both named commands verbatim and gains a "measured at v2.3.0" frame plus the pointer for current figures. Verify: cad-execute 28,476 / cad-land 18,209 / cad-pause 8,752 reproduced byte-for-byte; `grep -c docs/EVIDENCE.md README.md` = 2; `grep -c '231,422' README.md` = 1; self-verify `"problems":[]`; `npx tsc -p tsconfig.ci.json` exit 0. |
| 2 - Build the claim ledger from the run-1 report | 3a00092 | Coverage check ran first: all 25 AC1-surface filenames appear in `.planning/phases/5/docs-verify-run-1.md`, 0 missing. `.planning/DOCS-CLAIMS.md` written at the `.planning/` root with 547 rows (`id \| doc \| line \| claim \| verdict \| resolution`), verdicts 509 accurate / 18 stale / 20 unverifiable, resolution `accurate` on accurate rows and `pending` on the other 38. Header carries the sweep date, the three verbatim invocation strings, the surface, the counts with the row-vs-headline note, D-14 and D-15's constraints, the NUL-byte search hazard, the `divergence`-is-a-resolution statement (D-05), the positional-id declaration and the doc+claim-text join rule. `docs-verify.md` untouched. Verify: claim rows counted in the report itself (572 table lines - 25 header rows = 547) equals the ledger's 547; zero rows with an empty trailing cell. |
| 3 - File every code defect the sweep found as its own requirement id | 2d51c97 | **DFC ids filed this phase: DFC-01, DFC-02, DFC-03**, all under `## Deferred` in `.planning/REQUIREMENTS.md` (lines 174-176, between `## Deferred` at 153 and `## Out of Scope` at 178), each appearing exactly once. DFC-01: two literal NUL bytes at `cadence-core/bin/lib/trace.mjs:336` make `grep`/`rg` over `cadence-core/bin/**` skip the file without `-a` - the named-in-advance filing, confirmed live (`file(1)` reports the source as `data`). DFC-02: `cadence-core/references/review-triggers.md:244` and `docs/WORKFLOW.md:168` state `phase_diff` as `off / off / adjudicated` against `route-table.json`'s live `off / advisory / adjudicated`; both files are outside the surface and outside the lease, and that one row is the source of the four stale rows METHOD-01, METHOD-02, EXECUTE-02, EXECUTE-03. DFC-03: `skills/cad-plan-checker-contract/SKILL.md:113` still reads "All five dimensions checked" while `:42` of the same file says six - the same fact as METHOD-03, outside the surface and the lease. Verify: `planning.mjs audit --dir .planning` `"ok":true`, zero `unpicked` (its only three breaks are `not-verified` on this phase's own DOC-02/DOC-03/EVD-02, expected mid-cycle); `git status --porcelain` over the four root docs and `cadence-core/` empty before the commit; `node --test cadence-core/bin/planning.test.mjs` 308 pass, 0 fail. |
| 4 - Correct the stale claims in the four root docs | b2bad1a | Eight stale rows corrected, each against a re-run of its own check. README `:36` now says OpenAI and Gemini enforce the schema server-side while DeepSeek gets it injected in-prompt and asserted on return; README `:38` three tables to four (`docs/WORKFLOW.md` carries separators at 37/85/105/164 and 6 figures). METHOD `:91` five dimensions to six with Proportionality named; the `phase_diff` gate cell `off (opt-in)` to `advisory`; "Four of the five fire on their own" to all five firing at the default `shipped` level with `phase_diff` off only at `solo`; METHOD `:301` gains the DeepSeek split. INTERNALS `:55` OpenAI `response_format` to the Responses API `text.format` carrying a strict `json_schema` (`review-provider.mjs:618-619`). CONTRIBUTING `:13` no longer concludes "no `npm install`" from zero runtime deps; it names the typecheck's ephemeral `npm install --no-save --no-package-lock typescript @types/node`, and keeps "The same three checks CI runs" and "`node` and `git` on your PATH" verbatim so two accurate rows keep their claim text. All 15 root-doc unverifiable rows resolved `divergence` with a stated reason; both `README.md:132` rows took `corrected (task 1)`. Verify: zero root-doc rows read `pending`; self-verify `"problems":[]`; `npx tsc` exit 0; every corrected claim re-checked against the tree. |
| 5 - Correct the stale claims in `cadence-core/workflows/` and regenerate budgets | 044806c | Ten stale rows corrected across six files, five unverifiable rows resolved `divergence`. `audit.md` twice: the admission rule is a 2-8 char `[A-Z0-9]` category holding a letter ANYWHERE, so `2FA-01` is admitted and only an all-digit category is refused - confirmed by calling `isRequirementId` directly (`2FA-01` true, `AUTH-01` true, `#7` true, `14-01` false, `2026-08` false). `config.md`: `parallelization.enabled` default `false` to `true` (schema `:45` and `templates/config.json` both ship `true`); the three `per §7` default cells replaced with the real schema defaults, zero `per §7` left. `execute.md` three times: the executor's standing rules live in `skills/cad-executor-contract/SKILL.md`, not `agents/cad-executor.md` (a 15-line stub); `phase_diff` off-by-default to advisory-at-shipped-off-at-solo; adjudicated-only-at-critical to advisory at shipped and adjudicated at critical. `new-project.md`: the written defaults line drops the retired `interactive` and names standard granularity and shipped stakes. `progress.md`: the trace file is written by five workflows plus the reviewer bracket, not two. `task.md`: the `risk_surface` artifact is shape (c), a flagged-diff FILE path, not shape (a) refs, which the wiring table does not admit for that trigger. Budgets regenerated in the same commit for exactly the six edited surfaces (audit 12812->12912, config 19112->19256, execute 27734->27940, new-project 15847->15872, progress 8570->8662, task 4302->4484). Verify: self-verify `"problems":[]`; all 93 budget entries equal live bytes, 0 mismatches; the six `corrected (task 5)` docs are exactly the six `git status` lists as modified; `npx tsc` exit 0. |
| 6 - One byte figure for `review-triggers.md` in both skills | c0a7326 | `skills/cad-land/SKILL.md:44` and `skills/cad-plan-review/SKILL.md:39` both moved `15,376 B` to `17,733 B`, an equal-length six-character replacement in each. Verify: `grep -rn '15,376' skills/` returns nothing; `grep -c '17,733 B'` returns 1 for each file; `weight.mjs --root .` reports 17733 for `cadence-core/references/review-triggers.md`; 0 budget mismatches across all 93 surfaces, so no `weight-budgets.json` edit was needed or made; self-verify `"problems":[]`. `skills/**` was NOT added to the sweep surface and no other figure in either file was touched. |
| 7 - Confirmation pass over the ledger ids, and close every resolution | eddf79c | `.planning/phases/5/docs-verify-run-2.md` did not exist, so the re-verification ran directly against the tree, id by id, without invoking `/cad-docs-verify`. Method recorded in the file: `git diff --name-only a6b8931..HEAD` partitions the ledger - 10 of the 25 surface files changed this phase (349 rows, re-read at HEAD, every corrected row re-checked against what it asserts and every ACCURATE row beside an edit checked for collateral damage, all quoted phrases intact), and 15 did not, with no file under `cadence-core/bin/` changed but `weight-budgets.json` (entry count still 93), so their 198 rows were verified against a doc and a codebase byte-identical to what run 1 read. Delta line, exactly once: **`run-1 stale 18 -> run-2 stale 0 + 18 divergences`**. Run-2 counts 527 accurate / 0 stale / 20 unverifiable. Ledger closed: all 20 `(task k)` markers replaced with their own task's sha (1154790, b2bad1a, 044806c), every `+ DFC-0k` suffix preserved; final resolutions 509 `accurate`, 20 `corrected - <sha>`, 18 `divergence - <reason>`, 0 `pending`, 0 empty cells. |
| 8 - Whole-tree gate sweep | 33c1012 | Gate outputs verbatim: `node --test cadence-core/bin/*.test.mjs` -> `tests 1455 / pass 1455 / fail 0`; `npx tsc -p tsconfig.ci.json` -> exit 0, no output; `node cadence-core/bin/self-verify.mjs --root .` -> `{"ok":true,"checked":"config-keys, invocations, paths, internals-paths, budgets, tools, agent-skills, agent-behaviour, rung-effort, verifier-write-grant, routing-cells, effort-enums, risk-surfaces, config-reach, dispatch-phrasing, route-relay, merge-warnings, deferred-reads, script-contracts","problems":[]}`. Budget drift check: 0 stale entries across 93 budgeted / 93 measured surfaces, so `weight-budgets.json` was correctly left untouched. `docs/EVIDENCE.md` re-measured: turn-one total 278,315 -> 279,064 B, moved by exactly the six workflow files task 5 edited (cad-audit +100, cad-config +144, cad-execute +206, cad-new-project +25, cad-progress +92, cad-task +182); per-surface total 474,663 -> 475,412; date and sha updated to 2026-08-09 at `eddf79c`, with the file stating why the commit carrying it changes no figure in it (`docs/` holds no measured surface). D-08's definitions and every regenerating command left exactly as task 1 wrote them. Verify: all 23 per-command figures in the file reproduce from `weight.mjs resident --root .`, spot-checked byte-for-byte on cad-execute 28,682 / cad-land 18,209 / cad-pause 8,752, and the file's eager total equals the command's; final pathspec-scoped `git status --porcelain` over `docs/ README.md METHOD.md INTERNALS.md CONTRIBUTING.md cadence-core/ skills/ .planning/DOCS-CLAIMS.md .planning/REQUIREMENTS.md` returns nothing. **For SUMMARY.md (AC5): no non-Cadence project had a phase trace, so EVD-02's contingent trace half did not fire and is closed rather than left dangling - checked 2026-08-09 across `/code/*` and `/data/code/*`, eight projects with `.planning/` (`atmos`, `burnrate`, `hindsight`, `jcrenshaw.dev`, `placer`, `reflex`, `tempest`, `weathervane`), none with a `trace.jsonl`.** |
Deviations: [deviation] Task 2's Verify names `grep -nE '\|\s*$' .planning/DOCS-CLAIMS.md` as the empty-trailing-cell check. Expected it to isolate rows ending in an empty cell; observed it matches every line ending in a pipe - 549 of them (547 rows + the header row + the separator) - because a well-formed markdown row always ends in `|`. Ran the check the task's own prose describes instead, `grep -cE '\|\s*\|\s*$'`, which returns 0. The same literal regex appears again in Task 7's Verify and was read the same way there.
[deviation] Task 7's Verify names `grep -c 'pending' .planning/DOCS-CLAIMS.md` returning 0. Expected 0; observed 8, and 0 is unreachable without corrupting the ledger: 6 of the 8 are CLAIM rows quoting the word from `verify.md`'s own UAT vocabulary (`--result pending`, `first_pending`, "the next pending item"), and 2 are the header sentence explaining that `pending` was the transient placeholder. Ran the row-scoped form the criterion means, `grep -cE '\| pending \|$'`, which returns 0: no row's resolution cell reads `pending`.
[deviation] Task 3 expected the sweep to have surfaced code-defect claims; observed none. All 18 stale rows are stale PROSE with the code correct in every one, so zero ledger rows take a `divergence - code defect, filed as DFC-0k` resolution and the correction tasks inherited all 38 unresolved rows.
[deviation] Task 3's no-defect branch prescribes stating verbatim in the ledger header "run 1 found no claim describing a code defect; no DFC id filed". Its second clause is false on this run - the same task mandates the NUL-bytes filing unconditionally - so the header states the first clause and then names the ids that were filed and why each is not a sweep finding. Writing the sentence verbatim would have put a false statement in the artifact whose whole purpose is to be true.
[deviation] Two out-of-surface drifts the run-1 report names in its Cross-group notes were filed as DFC-02 and DFC-03 rather than left as report-only open items. Neither is a code defect under Task 3's letter, and neither file is in this plan's lease (`cadence-core/references/**`, `docs/**`, `skills/cad-plan-checker-contract/**`), so editing them was not available. An open item in an executor report is carried nowhere the next cycle reads; filing is the mechanism this phase exists to build, costs one bullet each, and keeps the root cause tracked while the copied rows are corrected in place. Correcting `review-triggers.md:244` in scope would additionally have moved a zero-slack budget and invalidated the 17,733 B figure AC6 fixes in the same phase.
[deviation] Task 4's METHOD `:91` correction grew that file by three lines, so every `METHOD.md` ledger row below it now sits three lines lower than its `line` cell says. Expected the `line` column to stay an address; observed it is provenance from run 1's sha only. Added a header paragraph to `.planning/DOCS-CLAIMS.md` saying so, and pointing at the join rule (doc plus claim text) that already exists for exactly this reason.
[deviation] `planning.mjs detect-commands --root /data/code/cadence` returns `lint:null, typecheck:null` - the repo has no package.json for it to read. Expected the contract's "say so once and skip" arm; instead ran `npx tsc -p tsconfig.ci.json` as the static-analysis step on every task, because the plan and `CONTRIBUTING.md` both name it as this repo's real typecheck and it exists. It was exit 0 before every commit.
Open items: none
