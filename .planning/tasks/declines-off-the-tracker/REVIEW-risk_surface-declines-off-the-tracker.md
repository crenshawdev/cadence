{
  "findings": [
    {"file":"cadence-core/bin/issue-filing.mjs","line":377,"severity":"high","claim":"The writer treats every failure to read an existing decline file as an empty file, allowing a successful write to erase all prior declines.","failure_scenario":"Make DECLINED.md mode 000 while leaving .planning writable. readFileSync fails with EACCES, the catch silently sets text to empty, and atomicWrite can replace the file because replacement depends on directory permissions. The command succeeds with only the current batch, permanently dropping every older fingerprint."},
    {"file":"cadence-core/bin/issue-filing.mjs","line":379,"severity":"high","claim":"mirrorFiled does not detect appendDeclinedRow rejecting a row and returning the original text, so it can report a decline successfully recorded after writing nothing.","failure_scenario":"Supply forge configuration whose repository slug contains whitespace. appendDeclinedRow returns the unchanged file; atomicWrite succeeds and mirrorFiled returns {ok:true}. A decline-only batch reports success while its fingerprint is absent and will be asked again."},
    {"file":"cadence-core/bin/issue-filing.mjs","line":487,"severity":"high","claim":"Declines are marked as filed only in memory and are not persisted before subsequent accepted findings perform fallible forge creates.","failure_scenario":"For a payload ordered as decline A followed by accept B, A is pushed into filed, then B's create fails and the create-failure path returns before the post-loop mirror."},
    {"file":"cadence-core/bin/issue-filing.mjs","line":236,"severity":"medium","claim":"The ENOENT arm cannot distinguish a genuinely absent DECLINED.md from an existing dangling symlink, despite the function's stated policy that a dangling symlink must refuse.","failure_scenario":"If .planning/DECLINED.md is a symlink whose target is missing, readFileSync raises ENOENT and the code returns an empty set rather than declines-unreadable, causing every decline in the unavailable target to resurface."},
    {"file":"cadence-core/bin/issue-filing.mjs","line":243,"severity":"medium","claim":"A readable but malformed or conflicted decline record is silently accepted as a complete lookup because parse failures are reduced to an empty or partial fingerprint set.","failure_scenario":"If a merge conflict damages a recorded row, parseDeclinedRows skips that line and readDeclines still returns ok:true. Unlike the former incomplete-lookup refusal, the gate then re-asks the omitted settled finding."}
  ]
}
