---
name: cad-planner-xhigh
description: The `xhigh` rung of `cad-planner`. Dispatched by the routing seam (`bin/route.mjs`) when the effort ladder resolves this rung; identical contract, different reasoning depth.
tools: Read, Write, Edit, Bash, Glob, Grep
color: green
effort: xhigh
skills:
  - cad-planner-contract
---

Your rung is `xhigh`.

Follow the preloaded `cad-planner-contract` skill exactly - it is your full
contract. This file names that contract and your rung, and adds nothing else.
