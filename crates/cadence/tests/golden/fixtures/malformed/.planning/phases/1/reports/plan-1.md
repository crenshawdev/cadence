PLAN COMPLETE
Plan: .planning/phases/1/PLAN-1.md
Tasks: 4 of 4
| Task | Commit | Note |
|---|---|---|
| 1 - Stop a delayed repeat close consuming the next dispatch | d14cbf47 | Reproduced first on a scratch fixture: the stolen row rendered `ms: -240000` with `end` = A's stop against `ts` = B's dispatch, exactly the figure CONTEXT D-05 records. Discriminator added in the terminal arm ahead of `pending.shift()`; worker key and `seenTerminals` untouched (D-01). 136 of 136 `trace.test.mjs` tests pass, the three shipped dedup tests and the committed-fixture test unmodified. |
| 2 - Read the worker's own transcript for whether it finished, and when | 3d2e82ae | New `lib/subagent-transcript.mjs` + test, 9 of 9 green, three states as specified. Ground truth re-measured before writing the rule (see Open items): the plan's "last assistant line stopped `end_turn` in all twelve sampled" does not generalize. One transient: the first write of the test file landed a literal U+0000 where a `' '` literal was intended; caught by self-verify check `nul-bytes` and replaced before the commit. |
| 3 - The hook writes nothing for a worker that has not stopped, and stamps the one that has | d0b6b0bc | Termination gate added as GATE 0, ahead of the self-filter and before the render is consulted; the existing GATE 1/2/3 numbering is deliberately unchanged so PLAN-2's "GATE 3" anchor still resolves. Byte ceiling 8 MiB = 3.2x the measured maximum (1,289 transcripts: 115 B to 2,608,524 B, p90 1,127,531, p99 1,789,123). `hooks/hooks.json` untouched. 11 of 11 tests pass. Smoked end to end on a scratch project outside the repo: a `tool_use` payload appended nothing, an `end_turn` payload appended a close carrying the worker's own instant, and the bracket rendered `ms: 271500` rather than an append-time span. |
| 4 - The stop close adopts the dispatch its agent_id belongs to | cad5cffd | GATE 2 joins through `.planning/reads.jsonl` on the payload's `agent_id`: earliest read of that worker, then the latest open dispatch of its role at or before it. Fallback to newest-open on all three declining paths, each with a test. The disk half does its own guarded read and imports only `readsPath` and `MAX_READS_BYTES` from `lib/read-trace.mjs`, so the planning seam stays out of the hook process; it reads nothing when the payload carries no id. 17 of 17 `subagent-trace.test.mjs` tests pass, all six shipped ones included. Smoked through the real hook: two open executor dispatches at 10:00 and 10:10 with the worker's first read at 10:05 closed plan 1, not plan 2. Then `node cadence-core/bin/test.mjs` 3,343 pass / 0 fail / 1 skipped (pre-existing), `self-verify` `ok:true` with no problems, `npx tsc -p tsconfig.ci.json` clean. The arg-contract flag census is still 190 entries - no new `trace <sub>` flag was added, so D-12's marker needed no re-pin. |
Deviations: none
Open items: The plan's task-2 ground truth ("the last assistant line stopped `end_turn` in all twelve transcripts sampled") is a small-sample artifact. Measured 2026-08-26 over 303 subagent transcripts older than a day (a 1-in-4 sample of the 1,289 on this machine): last-assistant `stop_reason` is `end_turn` 254, `tool_use` 14, `null` 34, `stop_sequence` 1. So under the rule the plan specifies, 48 of 303 - 16% - answer NOT-TERMINAL and get no hook close; if that worker's session also dies before the hand-written close, its dispatch stays `unpaired` (the pre-hook behaviour, visible rather than mispriced). The rule itself is unchanged: the plan already classes `tool_use` and `null` as not-terminal, and writing terminal as an exclusion rather than `=== 'end_turn'` is what keeps the one measured `stop_sequence` worker terminal. Stated in `lib/subagent-transcript.mjs`'s header as the accepted cost. Worth a look at phase verification if hook closes are expected to cover every dispatch.
